#include <ansi.h>
inherit ROOM;
void create ()
{
        set ("short", HIC"Brock's locker room"NOR);
        set ("long", HIW"You think you can mess with Brock?I don't think so!Go ahead and run,I will find u!Backstage..In the parking lot..Whereever!Come with numbers,that makes no difference!F-5!Smackdown!Shut your mouth!!!\n"NOR);

       set("exits",
       ([ //sizeof() == 2
               "wiz" : "/d/wiz/wizroom",
               "kz" : "/d/city/kezhan",
                "xot" : "/u/xot/workroom",
                       ]));
     set("chat_room",1);
     set("objects", ([ /* sizeof() == 2 */
      "/u/brock/npc/tila" : 1,
  ]));
       set("outdoors", "changan");
       set("no_time",1);
       set("no_fight",1);
       set("if_bed",1);
       set("sleep_room",1);
       set("no_upgrade_age",1);
       set("valid_startroom", 1);
       replace_program(ROOM);
       setup();
}
