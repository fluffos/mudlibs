inherit BULLETIN_BOARD;
void create()
{
        set_name("蜀山留言板", ({ "board" }) );
      set("location", "/d/shushan/shanmen");
        set("board_id", "shushan_b");
        set("long",     "蜀山留言板。\n" );
set("capacity", 80);
        setup();
        replace_program(BULLETIN_BOARD);
}
