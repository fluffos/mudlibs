// wizard_b.c

inherit BULLETIN_BOARD;

void create()
{
        set_name(" 介绍点pk的经验吧，", ({ "board" }) );
        set("location", "/d/wiz/pk");
        set("board_id", "pk_b");
	set("long",
                "这是一个的板子， post。\n" );
	setup();
	set("capacity", 150);
	replace_program(BULLETIN_BOARD);
}
