# 东方故事(蓝天) — Eastern Story I (ES I)

这是本项目已收录的 `es2`/`haiyang2`/`demonangel`/`xkx2001` 等"东方故事Ⅱ / ES2"家族的上一代祖先引擎——`bin` 目录里自带的旧版驱动名为 `mudos.exe`，`README.md` 自述"东方故事1MUD"，管理员账号约定为 `admin`/`mudren`。虽然中文名字带有武侠色彩，实际却是一个西式 D&D 奇幻世界：注册时只需要一个纯英文角色名（不含中文名机制， `is_chinese()` 一类的功能在此版本尚未出现），新角色可以从人类、 精灵、矮人、半兽人、地精、半身人、蜥蜴人、妖精、魔族、半人马、 黑暗精灵、眼魔、吸血鬼、鸟人、变形虫共 16 个种族中挑选一个，连同性别一起降生在远风镇的"冒险者公会"。

原始存档：`git clone https://github.com/MudRen/es`（commit
`4c2177e`）。本次上线按本项目一贯的方式完成了 GBK→UTF-8 全量转码
与驱动兼容性修复（详见 `NOTES.md`）。

## 内容亮点

- 门派/公会（guild）而非帮派体系：冒险者公会、法师塔、僧侣院、
  骑士团、盗贼公会等，`d/` 下按种族/职业/地域划分出深渊、冒险者、
  死域、东陆、群岛、杀手、骑士、法师、武术家、僧侣、诺登、学者、
  剑士、盗贼等十余个独立分区。
- 六维属性（力量/智慧/敏捷/体质/虔诚/运气）+ 体力/精神力/交谈力
  三条状态条，`score` 指令展现完整角色卡。
- 保留了大量 1991-1995 年间原始 TMI/MudOS 架构的历史痕迹（`adm/
  obj/master.lpc` 的多年修订记录、`feature/*.lpc` 混入式设计等），
  是研究这一脉中文 MUD 引擎最早期形态的第一手样本。
- 与本项目已收录的 `es1_win`（同样译名"东方故事(蓝天)"）内容高度
  相近（约 90% 文件在忽略换行符差异后逐字相同），二者是同一份原始
  代码在不同时间点的快照，按本项目既有先例（如 `jyqxc`/`jyqxc2`）
  各自独立上线并保留各自的编辑历史差异。

## 在线试玩

https://mudlibs.fluffos.info/es1/

## 管理员账号 / Admin account

- **id**: `fluffos`
- **密码 / password**: `Mud@2026`
- **权限 / level**: 通过编辑 `adm/etc/groups`（`(root)` 分组，
  `load_groups()` 在 master 相关调用时读入此文件）加入 `fluffos`
  成员，再以正常注册流程创建该账号。已用 `update /adm/obj/master`
  验证成功（读取+编译 ACL 均通过）。

> 警告：公开架站前请务必修改此默认密码。

## 本地运行

```
cd libs/es1
~/src/fluffos/build-debug/src/driver config.fluffos
```

游戏端口：**40239**。
