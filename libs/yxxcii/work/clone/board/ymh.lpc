//ymh_b.c 飞影的留言版

#include <ansi.h>
inherit BULLETIN_BOARD;

void create() {
  set_name("飛影的工作日志", ({ "board" }));
  set("location", "/u/robert/workroom");
  set("board_id", "ymh");
  set("long", "这是飛影的工作日志，上面记载着飞影每天所作的工作。\n");
  setup();
  set("capacity", 900);
}
