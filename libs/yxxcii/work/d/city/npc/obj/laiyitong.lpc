
// kaoya.c 来一桶

inherit ITEM;
inherit F_FOOD;

void create()
{
	set_name("面", ({"mian"}));
	set_weight(200);
	if (clonep())
		set_default_object(__FILE__);
	else {
		 set("long", "一碗香喷喷的面。\n");
		set("unit", "碗");
		set("value", 200);
		set("food_remaining", 4);
		set("food_supply", 300);
	}
}

