// xiaobaozhai.c
// Design By Robert 蝴蝶君 email:stcoolman@21cn.com

inherit ROOM;

void create() {
  set("short", "小宝斋");
  set("long", @LONG
抚远大将军挂官出逃后，带了七个老婆，躲在这里偷偷开了这个小宝
斋。卖些当年带回来的金银珠宝，收购一些古董倒卖，着实发了不少横财。
韦小宝身后有一块牌子(sign)，上面好象写着什么。
LONG
  );
  set("no_fight", 1);
  set("no_steal", 1);
  set("no_beg", 1);
  set("exits", ([
    "down": __DIR__ "dangpu",
  ]));

  set("objects", ([
    "/quest/weixiaobao": 1,
  ]));
  set("item_desc", ([
    "sign": @TEXT

如找回物品，可获得较高的奖励，如未能找回，必
须相应给与二十两白银做为补偿，则可取消任务。

                          韦小宝字
TEXT
  ]));

  setup();
}
