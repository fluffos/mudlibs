// fish1 大鲤鱼

#include <ansi.h>

inherit ITEM;

void create() {
  set_name("大鲤鱼", "fish");
  set_weight(350);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一条活蹦乱跳的大鲤鱼。\n");
    set("unit", "条");
    set("value", 300);
    set("material", "bone");
  }
  setup();
}
