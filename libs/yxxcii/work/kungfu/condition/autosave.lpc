// autosave.c

#include <condition.h>
#include <ansi.h>

int update_condition(object me, int duration) {
  if (duration >= 1 && duration <= 20) {
    me->save();
    me->apply_condition("autosave", duration + 5);
  }
  return CND_CONTINUE;
}
