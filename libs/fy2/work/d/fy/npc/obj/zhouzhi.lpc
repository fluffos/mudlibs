// token.c


inherit ITEM;

void create() {
  set_name("皱皱的破纸片", ({ "pozhi" }));
  set_weight(10);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "张");
    set("long", "一张皱皱的破纸片，上面似乎可以写字。\n");
    set("material", "paper");
    set("value", 100000);
  }
  setup();
}

void init() {
  add_action("do_print", "print");
}

int do_print(string arg) {
  if (!stringp(arg)) return 0;
  if (stringp(query("targetid"))) return notify_fail("这张纸已经写过字了。\n");
  else set("targetid", arg);
  write(" ＯＫ. \n");
  return 1;
}
