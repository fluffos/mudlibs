// token.c


inherit ITEM;

void create() {
  set_name("刨花油", ({ "bao" }));
  set_weight(100);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "盒");
    set("long", "一盒香喷喷的刨花油。\n");
    set("material", "steel");
    set("value", 1000);
  }
  setup();
}
