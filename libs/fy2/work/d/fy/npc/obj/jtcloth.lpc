// cloth.c
//
// This is the basic equip for players just login.

#include <armor.h>

inherit CLOTH;

void create()
{
	set_name("金丝小褂", ({ "jinsi xiaogua", "cloth" }) );
	set_weight(3000);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "件");
		set("value", 20000);
		set("long", "这是金童最爱穿的一件衣服。\n");
		set("material", "cloth");
		set("armor_prop/armor", 1);
	}
	setup();
}
