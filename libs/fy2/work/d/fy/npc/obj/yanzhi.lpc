// token.c


inherit ITEM;

void create() {
  set_name("胭脂", ({ "yanzhi" }));
  set_weight(10);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "袋");
    set("long", "一盒香喷喷的胭脂。\n");
    set("material", "steel");
    set("value", 1000);
  }
  setup();
}
