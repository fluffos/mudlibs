// token.c


inherit ITEM;

void create() {
  set_name("货票", ({ "huopiao" }));
  set_weight(10);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "张");
    set("long", "一张纸色泛黄的货票。\n");
    set("material", "paper");
    set("value", 1000);
  }
  setup();
}
