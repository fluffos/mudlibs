inherit ROOM;
void create()
{
        set("short", "石巷尽头");
        set("long", @LONG
石巷的尽头是一堵高墙，墙上有一道窄门．墙角有一狗洞，一条
脏稀稀的小河沟从狗洞中流出．时而不时的，一条条浸满血水的绷带
从狗洞中流出．
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"stone3",
  "south" : __DIR__"stone1",
]));
        set("outdoors", "fengyun");
        setup();
        replace_program(ROOM);
}
