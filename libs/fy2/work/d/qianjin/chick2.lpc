#include <room.h>
inherit ROOM;
void create()
{
        set("short", "厢房");
        set("long", @LONG
此房小而整洁。墙上一小窗，窗外湖色尽收眼低。墙角立一琵琶
，上覆红布，似乎很久没有用过的样子。窗侧对联一幅，言道：
                 易求无价宝，难得有情郎。
LONG
);
        set("exits", ([ /* sizeof() == 4 */
  "south": __DIR__"zoulang4",
]));
        set("objects", ([
                __DIR__"npc/ruanhong" : 1,
       ]) );
        create_door("south", "紫檀木门", "north", DOOR_CLOSED);
        setup();
        replace_program(ROOM);
}

