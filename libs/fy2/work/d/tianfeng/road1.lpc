inherit ROOM;
void create() {
  set("short", "石峰边缘");
  set("long", @LONG
这里不但像是已到了沙漠的尽头，简直像是到了天地的尽头。再
往前走，便要跌入百韧不复的深渊中。前面俱是石峰，无边无际，再
也难往前走。
LONG
  );
  set("exits", ([
    "south": "/u/quicksand/desert0",
    "north": __DIR__ "road2",
  ]));

  set("outdoors", "tianfeng");

  setup();
}
