inherit ROOM;
void create() {
  set("short", "迷漫的黄沙中");
  set("long", @LONG
三五人影，似乎正拿着扫把扫地，他们的动作是那么缓慢，却又
是那么的有规律，看来就象是一群没有生命的傀儡，像是恒古以来就
在那时扫着地，一直要扫到世界的末日。
LONG
  );
  set("exits", ([
    "north": __DIR__ "road8",
    "south": __DIR__ "road6",
  ]));

  set("objects", ([
    __DIR__ "npc/shadow": 3,
  ]));
  set("outdoors", "tianfeng");

  setup();
}
