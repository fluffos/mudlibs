// Room: /d/snow/danceroom.c

inherit ROOM;

void create() {
  set("short", "晚月庄");
  set("long", @LONG
这是一间歌舞院，这里有许多美貌的女孩子正在学习着歌艺和舞蹈。一位仙女
般的女舞者正在教她们跳各种美丽的舞蹈。在这里，你忍不住也想去和她们一起
跳起来。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "danceroom",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/daemon/class/dancer/master": 1,
  ]));
  set("no_clean_up", 0);
  set("valid_startroom", 1);

  setup();
}
