// bag.c

inherit ITEM;

void create()
{
	set_name("紫金葫芦", ({ "hulu" }) );
	set_weight(10);
	set_max_encumbrance(6000000);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "个");
		set("long", "一个可放好多东西的葫芦\n");
		set("value", 3);
	}
}

int is_container() { return 1; }
