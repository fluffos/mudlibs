// token.c

#include <ansi.h>
inherit ITEM;

void create() {
  set_name(RED "半干的血滴" NOR, ({ "blood" }));
  set_weight(10);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "滴");
    set("material", "water");
  }
  setup();
}
