inherit ROOM;

void create() {
  set("short", "秘室");
  set("long", @LONG
屋子并不大，有一张床，一张桌子。屋子里根本没有窗，四面的
墙壁竟赫然全都是好几寸厚的铁板。桌子上还摆着一个红木盒子和一
些酒菜，酒菜居然全部原封不动。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "yingou3",
  ]));
  set("objects", ([
    __DIR__ "npc/lang": 1,
    __DIR__ "npc/yuxiang": 1,
  ]));
  setup();
}
