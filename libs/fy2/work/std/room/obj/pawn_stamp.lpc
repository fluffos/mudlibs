// pawn_stamp.c

inherit ITEM;

void create() {
  set_name("讽布", ({ "pawn stamp", "stamp" }));
  set_weight(5);
  if (!clonep()) {
    set("unit", "眎");
    set("long", "眎フ讽布\n");
  }
  setup();
}
