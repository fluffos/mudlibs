// wizard_j.c

inherit "/std/jboard";

void create() {
  set_name("巫师工作进度报告", ({ "job board", "board" }));
  // BUG FIX (deep functional test, AGENTS.md §7.18) -- see
  // obj/board/wizard_b.lpc / d/waterfog/hall1.lpc for the full writeup.
  set("location", "/d/waterfog/jobroom");
  set("board_id", "wizard_j");
  set("long", "这是一个特殊的留言板供巫师报告自己的工作进度。\n");
  setup();
  replace_program("/std/jboard");
}
