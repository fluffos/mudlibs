inherit ROOM; 

void create() 
{ 
        set("short", "海底"); 
        set("long", @LONG 
这里水色变的浑浊起来，周围已看不大清楚了。偶有暗流袭来，卷
起阵阵的泥流。四处长着些高高的海草，密密麻麻把头上遮的一丝光芒
也没有了。
LONG
); 
        set("outdoors", "forest"); 
        set("no_magic",1);
        set("exits", ([ /* sizeof() == 3 */ 
                "east":__DIR__"maze/exit",
                "up": "/d/sea/maze0",
        ])); 
        setup();
        replace_program(ROOM); 
} 
