
// by seawind
inherit ROOM;
void create()
{
        set("short", "幽冥血海");
        set("long", @LONG 四周是一片惨红，无数只怪手从火红的炼狱中伸出，仿佛
要将你扯入无边的黑暗！ 
LONG );
        set("exits", ([
                //"east" : __FILE__,
                //"west" : __FILE__,
                "north" : __DIR__"road6",
                "south" : __DIR__"road4",
                 //"down"      : "/d/changan/broadway3",

        ]));
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

