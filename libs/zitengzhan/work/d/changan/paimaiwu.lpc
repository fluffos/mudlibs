inherit ROOM;

void create() {
  set("short", "拍卖屋");
  set("long", @LONG

这里便是拍卖屋，你可以在此拍卖(paimai)物品。拍卖后的物品可以要(yao)回来。

LONG);

  set("objects", ([ /* sizeof() == 2 */
    //  "/d/wiz/npc/paimaishi" : 1,
    "/d/npc/vr_room/obj/tai": 1,
    // "/d/npc/vr_room/npc/shizhe" : 1,
  ]));
  set("outdoors", 1);
  set("no_fight", 1);
  set("no_magic", 1);
  set("no_steal", 1);
  set("freeze", 1);

  set("exits", ([ /* sizeof() == 2 */
    "down": "/d/city/center",
    //"north" : __DIR__"damen",
  ]));

  setup();
}
