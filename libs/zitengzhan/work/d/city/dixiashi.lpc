// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */
 
// room: /d/city/dixiashi.c

inherit ROOM;

void create ()
{
  set ("short", "兵器铺二楼");
  set ("long", @LONG

这里是兵器铺的二楼，里面到处是刀，剑，枪的，一位老板
坐在柜台里面，笑嘻嘻的看见来这个买卖武器的朋友。

LONG);

// dammy 2010-12-30，修正了房间方向和名称
  set("exits", ([
           "down"      : __DIR__"bingqipu",
      ]));
  set("no_fight", 1);
  set("no_magic", 1);

  set("objects",([
           __DIR__"npc/zhenyuan" : 1,
       ]));

  setup();
}
void init() 
{
        object me = this_object();

 if (!wizardp(this_player()) && this_player()->query_condition("no_pk_time"))
   {
    this_player()->move("/d/city/bingqipu");
   }
  }



