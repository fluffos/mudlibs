//changan city

inherit ROOM;

void create() {
  set("short", "铁匠铺");
  set("long", @LONG

兵器铺的刀剑什么的是要铁匠打的，这地下铁匠铺就是振远镖局
的铁匠铺，各种兵器都是在这里打造的。装备破损了的话，也可
以在这里修复（repair）。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "up": __DIR__ "bingqipu",
    ]));

  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/tiejiang": 1,
    ]));

  setup();
}
