inherit ROOM;

void create() {
  set("short", "提炼房");
  set("long", @LONG

这里是提炼房，房子中间摆放着一尊宝石提炼台，你可以用它来提炼各种宝石。\n

LONG);

  set("objects", ([ /* sizeof() == 2 */
    "/d/npc/vr_room/obj/tai": 1,
  ]));
  set("no_fight", 1);
  set("no_magic", 1);
  set("no_steal", 1);
  set("exits", ([ /* sizeof() == 2 */
    "northwest": __DIR__ "paimaiwu",
  ]));

  setup();
}
