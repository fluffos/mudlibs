inherit __DIR__"guai.lpc";
#include "guai.h"

void set_skills(int max_value, int level)
{
        int guai_value;
        ::set_skills(max_value, level);
        
        if (level==1) guai_value = max_value+max_value/8;
        else if (level==2) guai_value = max_value+max_value/6;
        else guai_value = max_value+max_value/4;

        
        set_skill("kongshoudao", guai_value+random(16));
        set_skill("fengyu-piaoxiang", guai_value+random(16));
        set_skill("sword", guai_value+random(16));
		set_skill("blade", guai_value+random(16));
        set_skill("doom-sword", guai_value+random(16));
		 set_skill("dongyang-blade", guai_value+random(16));
        set_skill("guixi-force", guai_value+random(16));
        set_skill("unarmed", guai_value+random(16));
        set_skill("renshu", guai_value+20+random(16));
        map_skill("spells", "renshu");
        map_skill("force", "guixi-force");
        map_skill("unarmed", "kongshoudao");
        map_skill("sword", "doom-sword");
        map_skill("parry", "doom-sword");
		map_skill("blade", "dongyang-blade");
        map_skill("dodge", "fengyu-piaoxiang");
        set("force_factor", query_skill("force")/4);
        set("mana_factor", query_skill("spells",1));
		  set("MKS",16000);
          set("PKS",10); 
set("class","human");
set("family/family_name","剑客联盟");
       set("chat_chance_combat", 70);
        set("chat_msg_combat", ({
                (: perform_action, "unarmed","shuang" :),
					                (: perform_action, "sword","yanling" :),
					(: perform_action, "blade","yidaozhan" :),
(: cast_spell, "nuhuoyanling" :),
               }) );                       
        carry_object("/d/obj/armor/jinjia.lpc")->wear();
	
	set("bellicosity",query("cor")*query("cor")+random(777));
  if(random(2)==1)
	{
  carry_object("/d/obj/weapon/sword/qinghong.lpc")->wield();
	}
	else
	{
		carry_object("/u/ken/obj/weapon/blade/dongyangdao.lpc")->wield();
	}
}

void init()
{
        add_action("do_none","touxiang");
        add_action("do_none","surrender");
       }
int do_none()
{
        object me = this_object();
        message_vision("想耍赖皮？\n",me);
        return 1;
} 

