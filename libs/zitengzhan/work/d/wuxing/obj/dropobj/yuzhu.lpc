//Cracked by Roath
#include <ansi.h>
inherit ITEM;

void create() {
  set_name(HIG "寒丝羽竹" NOR, ({ "hansi yuzhu", "yuzhu" }));
  set_weight(10);
  setup();
  set("unit", "根");
  set("long", @LONG
镶在武器上加木系伤害，镶在防具上加木抗性。
LONG
  );
  set("value", 100);
  set("armor", "mu_ris");
  set("armor_max", 10);
  set("weapon", "mu_type");
  set("weapon_max", "50");
  set("can_inset", 1);
  setup();
}

void init() {}
