#include <armor.h>
#include <ansi.h>
inherit WRISTS;
#include "zhuangbei.h"

string *names = ({
  "铁护腕",
  "铜护腕",
});

string *ids = ({
  "tie hu wan",
  "tong hu wan",
});

void create() {
  int i = random(sizeof(names));
  set_name(names[i], ({ "wrists" }));
  set("type/kind", "wrists");
  set("type/large", "armor");
  set_weight(2000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "件");
    set("value", 200);
    set("material", "metal");
    set("armor_prop/armor", 20);
  }
  setup();
}
void initobject(object me) {
  object ob = this_object();
  initzhuangbei(me, ob, query("name"), query("long"), 20);
}
