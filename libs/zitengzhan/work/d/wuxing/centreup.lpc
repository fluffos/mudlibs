//Cracked by Roath
inherit ROOM;

void create ()
{
        set ("short", "天台");

        set ("long", @LONG

你走在一条悬浮在空中的石阶上，下方的天市越来越小，上方却看不到终点。
LONG);

        set("exits", 
        ([ //sizeof() == 4
                "down" : __DIR__"centre",
                "up" : __DIR__"shentai",
        ]));

        set("objects", 
        ([ //sizeof() == 1
        ]));


        set("outdoors", "wuxing");

        setup();
}
