inherit ROOM;

void create() {
  set("short", "修炼宝地");
  set("long", @LONG

这里是高手静修的好地方，不时的还会从天掉下几个迷人的猕猴桃来，也许这里就是所谓人间仙境吧。
LONG);

  set("objects", ([ /* sizeof() == 1 */
    "/d/obj/drug/mihoutao": 4,
  ]));
  set("outdoors", 1);
  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "andao2",
  ]));

  setup();
}
