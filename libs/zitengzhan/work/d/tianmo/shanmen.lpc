#include <room.h>
inherit ROOM;

void create() {
  set("short", "山门");
  set("long", @LONG

天魔宫的大门，门口立一石碑，约有三丈馀高，八尺馀阔，上有
三个大字“天魔宫”。山门旁立一迎客小童，真个丰姿英伟，像
貌清奇，比寻常俗子不同。
LONG);
  set("exits", ([ /* sizeof() == 2 */
    "northdown": __DIR__ "eroad3",
    "south": __DIR__ "changlang1",
  ]));
  set("valid_startroom", 1);
  set("no_light", 1);
  set("objects", ([
    __DIR__ "npc/xiaotong": 1 ]));
  create_door("south", "石门", "north", DOOR_CLOSED);
  set("outdoors", 1);
  set("no_clean_up", 0);

  set("valid_startroom", 1);
  setup();

  call_other("/obj/board/shibei_t", "???");
}
