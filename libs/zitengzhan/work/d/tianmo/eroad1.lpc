// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */

//standroom.c used by weiqi...others may hate this format:D
//wroad1.c

inherit ROOM;

void create() {
  set("short", "山路");
  set("long", @LONG
一条小路，静悄悄的，看起来好象没有什么人走过的样子。往西
就是大官道了，而东方黑漆漆的，也不知道通向那里。
LONG);
  set("outdoors", 1);
  set("no_light", 1);
  set("exits",
    ([  //sizeof() == 2
    "eastup": __DIR__ "eroad2",
      "westdown": "/d/changan/zhongnan",
    ]));



  setup();
}
