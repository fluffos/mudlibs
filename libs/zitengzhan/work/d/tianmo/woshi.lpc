// Room: some place in 西牛贺洲
// inside2.c

inherit ROOM;

void create()
{
  set ("short", "卧室");
  set ("long", @LONG

天魔宫弟子的卧室，所以屋里的摆设非常的简单。
一张大床，一些被子，气味不太好啊。。。。

LONG);
set("exits", ([ /* sizeof() == 4 */
"west": __DIR__"changlang1",
]));
set("objects", ([
               "/d/city/npc/obj/qin": 1]));

        set("no_clean_up", 0);
        set("sleep_room",1);
        set("if_bed",1);
        setup();
        replace_program(ROOM);
}
