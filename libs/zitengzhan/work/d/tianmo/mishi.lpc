inherit ROOM;
#include <ansi.h>

void create() {
  object qin;

  set("short", "密室");
  set("long", @LONG
密室里黑咕隆咚的，什么也看不清。。。
LONG);
  setup();
  if ("/d/tianmo/obj/tianmoqin"->in_mud() == 0) {
    qin = new("/d/tianmo/obj/tianmoqin");
    qin->move(this_object());
  }
}
