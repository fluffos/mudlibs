inherit ROOM;
#include <room.h>
void create() {
  set("short", "长廊");
  set("long", @LONG

这是一条深深的长廊，不远处可以看到天魔宫的宫门，四周雕龙
画凤，极尽繁华雍容之能事。


LONG);
  set("objects", ([
    __DIR__ "npc/xiaomotou": 1,
  ]));
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "changlang2",
    "north": __DIR__ "shanmen",
    "east": __DIR__ "woshi",
  ]));
  create_door("north", "石门", "south", DOOR_CLOSED);
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
