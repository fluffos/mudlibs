//sgzl
//huarong 2004/7
#include <ansi.h>
#include <room.h>
inherit ROOM;

void create() {
  set("short", HIB "深海" NOR);
  set("long", @LONG

通向波士顿神殿的唯一通道，四处长着些高高的海草，
密密麻麻把头上遮的一丝光芒也没有了。

LONG);


  set("exits", ([
    "north": __DIR__ "maze1",
  ]));

  set("objects", ([ /* sizeof() == 3 */
    "/d/shengdoushi/12gong/npc/guigui": 1,
  ]));

  setup();
  replace_program(ROOM);

}
