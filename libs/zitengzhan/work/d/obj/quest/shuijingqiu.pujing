//Cracked by Roath
// 朸趕岍賜﹞昹蚔暮﹞唳掛ㄣㄝㄤㄟ
/* <SecCrypt CPL V3R05> */
 
inherit ITEM;

int do_locate(string arg);
int do_task();

void create()
{
        set_name("阨儒", ({"shuijing qiu","qiu"}));
        set_weight(80);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("long", "涴岆珨璃蚚衾扆梑隍囮昜璃腔阨儒ㄛ坳撿衄準歇腔藹薯﹝\n"+
                            "夔蚚(locate)韜鍔梑善昜腔湮衙弇离ㄐ\n");
                set("unit", "璃");
                set("value", 300);
        }
}

void init()
{
        object ob = this_player();

  add_action("do_locate","locate");
  add_action("do_task","task");
}

int do_task()
{
   string output;
   output="阨儒爵芼茬珋堤珨茯芛苤趼ㄩ\n\n"+"/adm/daemons/questd"->dyn_quest_list();
   this_player()->start_more(output);
   return 1;
}

int query_autoload() { return 1; }

int do_locate(string arg)
{
   if ( ! arg)

   return notify_fail("locate <昜>\n");
   else
   "/adm/daemons/questd"->locate_quest(this_player(),arg宥w)欉6o~m#ㄊ