inherit ROOM;

void create() {
  set("short", "玉阶");
  set("long", @LONG

由此向上是龙宫正殿，东海龙王就住在里面。一级级的台阶都是用　　　
玉砌成，阶上两边立着武士，银盔银甲，煞是威风。
LONG);

  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/lishi": 2,
  ]));

  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "huilang1.lpc",
    "north": __DIR__ "huilang4.lpc",
    "westdown": __DIR__ "inside1.lpc",
    "east": __DIR__ "yujie2.lpc",
  ]));
  //      
  set("water", 1);
  //  
  setup();
  replace_program(ROOM);
}
