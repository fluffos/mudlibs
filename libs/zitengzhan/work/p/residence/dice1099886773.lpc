// a skeleton for user rooms
//Last Modified By PKYOU@DHXY 2002/05/14
#include <ansi.h>
inherit ROOM;
void create() {
  set("short", "狙杀天狼星");
  set("long", @LONG
                   以杀止杀
                   狙遍天下
    从广场走过来，你可以看见这个方圆几百里的空地，这是狙杀天狼星的总
部，具有悠久的历史，四面八方都是用来暗杀天狼星的器械，冷、热兵器样样
齐全。
    全帮上下以杀死超级变态—如花（天浪星）为第一任务，人人勇字当头，
视死如归，不杀如花誓不罢休。

LONG
  );
  set("exits", ([
    "northeast": "/d/eastway/guangchang1.lpc",
  ]));
  set("objects", ([
  ]));
  set("owner", "bigtie7001");
  set("banghui", "『狙杀文月』");

  setup();
}

void init() {
  object ob = this_player();
  if ((string)ob->query("banghui") != environment(ob)->query("banghui")) {
    message(
      "vision",
      YEL "【" MAG "帮会消息" YEL "】顺风耳：" + YEL "" + ob->query("name") + "于" + NATURE_D->game_time() + "企图闯入" + environment(ob)->query("banghui") + "总坛,被扔了出来！\n" NOR,
      users()
    );
    message_vision(HIR "突然从里面冲出来几个守卫拖着$N就走,$N象小鸡一样,被扔了出来。\n" NOR, this_player());
    ob->move("/d/city/kezhan");
  } else return;
}
