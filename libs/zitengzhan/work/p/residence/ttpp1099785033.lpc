// a skeleton for user rooms
//Last Modified By PKYOU@DHXY 2002/05/14
inherit ROOM;
void create()
{
set("short", "逍遥派兵器库");
set("long", @LONG
逍遥派为大唐第一大帮派，弟子众多
帮主巫行云为了使其弟子能够勤于修行
提高修为，特创建逍遥派兵器库

LONG
);
        set("exits", ([
"east" : "/p/residence/ttpp1099784818.lpc",
]));
set("objects", ([
        "/d/obj/weapon/rack":2,
       ]) );
        set("owner", "bigtie7001");
        set("banghui", "逍遥派");
        set("no_fight",1);

setup();
}

