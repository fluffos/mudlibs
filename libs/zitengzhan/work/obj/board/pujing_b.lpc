#include "ansi.h"

inherit BULLETIN_BOARD;

void create() {
  set_name("岩石", ({ "board" }));
  set("location", "/u/pujing/workroom");
  set("board_id", "pujing_b");
  set("long", "这是一个供各位交流讨论的" + HIG + " BBS " + NOR + "板。\n");
  setup();
  set("capacity", 500);
  replace_program(BULLETIN_BOARD);
}
