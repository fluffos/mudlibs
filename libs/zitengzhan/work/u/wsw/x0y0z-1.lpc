//changan city
//#include "road.h"
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", HIR "世外桃源-地下室" NOR);
  set("long", @LONG

这里便是世外桃源的城中地下室，摆着**。。
LONG);
  set("exits",
    ([
      "up": __DIR__ "x0y0z0",
    ]));
  set("objects",
    ([
      // "/d/playercity/npc/master" : 1,
    ]));
  //[0022]  set("objects", ([ "/d/obj/weapon/throwing/shi.lpc" : 1, ]));

  setup();
}
