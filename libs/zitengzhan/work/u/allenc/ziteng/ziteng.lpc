//Cracked by Roath
//ziteng temple

inherit ROOM;

void create() {
  set("short", "后殿");
  set("long", @LONG
皇宫的后殿烟雾缭绕。殿前有一小香桌，上面燃着檀香，烟火的香气扑
鼻，周围放满了后人供奉的鲜果，香油等食物。殿上供奉着五位神仙，
据说他们是紫藤的开山祖师。还有一块金光闪闪的匾额（bian），上面不
知道写着什么东西。四周没有出口，看来你得好好看（look）一下了。
LONG);

  set("item_desc",
    ([
      "bian": "       [1;35m紫[1;33m藤[1;31m仙[1;36m境 \n", ]));

  set("exits",
    ([  //sizeof() == 4
    "south": "/d/city/center",
    ]));

  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/allenc": 1,
      //__DIR__"npc/babala" : 1,
      //__DIR__"npc/dodo" : 1,
      //__DIR__"npc/faint" : 1,
      //__DIR__"npc/huang" : 1,
    ]));
  set("no_fight", 1);
  set("no_magic", 1);

  //        set("outdoors", "changan");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
