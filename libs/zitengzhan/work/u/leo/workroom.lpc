#include <ansi.h>
#include <room.h>
inherit ROOM;

void create() {
  set("short", BLINK + HIG "九龙冰室" + NOR);
  set("long", @LONG 
                              ≡
    菩      明      本      何    禅
    提      镜      来      处    宗  ∧
    本      亦      无      惹    ≡  六
    无      非      一      尘        祖
    树      台      物      埃        慧
                                      能
                                      ∨
LONG);
  set("item_desc", ([ /* sizeof() == 1 */
    "wiz": "房子里云雾缭绕，什么也看不清楚。\n",
    "yingke": "房子里云雾缭绕，什么也看不清楚。\n",
    "kz": "房子里云雾缭绕，什么也看不清楚。\n",
  ]));
  set("exits", ([ /* sizeof() == 2 */
    "wiz": "/d/wiz/wizroom",
    "yingke": "/d/wiz/guest",
    "kz": "/d/city/kezhan",
  ]));

  set("no_fight", "1");
  set("no_time", "1");
  set("no_kill", "1");
  set("no_magic", "1");
  set("valid_startroom", 1);
  setup();
}
