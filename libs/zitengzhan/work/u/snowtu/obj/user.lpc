inherit NPC;
inherit F_SAVE;

void create() {
  set_name("使用者物件", ({ "user object", "user", "object" }));
}

string query_save_file() {
  if (query("save_file")) return query("save_file");
  return 0;
}
