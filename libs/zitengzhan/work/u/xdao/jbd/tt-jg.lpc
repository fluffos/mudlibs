inherit ROOM;
#include <ansi.h>;
void create() {
  set("short", "密室");
  set("long", @LONG
一间气派的密室，中间摆放着个蒲团，似乎和封印有些关系。
LONG);
  set("exits", ([
    "west": __DIR__ "tt-owner",
  ]));
  set("no_clean_up", 1);
  setup();
}
