inherit ROOM;
int do_test(string arg);
void create() {
  set("short", "大漠");
  set("long", @LONG
沙漠尽头火红的地平线正渐渐暗淡下去，一堵墙垣似的火烧
云已隐没在沙丘 后面，唯有一长束橘红色的余晖告诉人们，
太阳是在这儿被不可抗拒的时间送走的。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "kz": "/d/city/kezhan",
  ]));
  set("objects", ([
  ]));
  set("no_clean_up", 1);
  set("no_time", 1);
  set("no_magic", 1);
  set("valid_startroom", 1);
  set("alternative_die", 1);
  setup();
  replace_program(ROOM);
}
void alternative_die(object who) {
  who->set("sen", who->query("max_sen"));
  who->set("kee", who->query("max_kee"));
}
