// by better
inherit ROOM;
#include <ansi.h>

void create ()
{
  set ("short", "比武大会讨论室");
  set ("long", @LONG

这里就是每星期比武大会讨论室,欢迎大家踊跃报名,报名请post留言
规则如下:比武大会按低，中，高，三个不同的等级进行,相同IP玩家
不能互相比武,比武大会时间定于每周六晚8点准时举行。

低等级参赛要求:武学10W以上，50W以下,第一名可以获得武学10w,道行10年,2w潜能，攻击200武器一把，防御200盔甲一件
中等级参赛要求:武学50w以上，100w以下,第一名可以获得武学15w,道行15年，4w潜能,攻击500武器一把，防御500盔甲一件
高等级参赛要求:武学100w以上,第一名可获得武学20w,道行20年,6w潜能，攻击800武器一把，防御800盔甲一件
每个等级第一名出现可以向上一个等级第一名挑战，如果失败，奖励减半，如果胜利，奖励翻倍。
备注:三个等级拿到的武器和盔甲只有24小时的使用权，并且只能用于练级，不能恶意PK。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "north" : "/d/city/kezhan",
  ]));
  set("objects", ([ /* sizeof() == 2 */
  ]));
set("no_time", 1);
  set("freeze", 1);
    set("no_fight", 1);
  set("chat_room",1);
    set("no_magic", 1);

    setup();
     call_other("/u/better/better_b", "???");
}
