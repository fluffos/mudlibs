// by better

inherit BULLETIN_BOARD;

void create()
{
          set_name("比武大会留言板", ({ "board" }) );
          set("location", "/u/better/biwuchang");
          set("board_id", "better_b");
          set("long",     "这是一个供比武大会讨论的留言板。\n" );
        setup();
        set("capacity", 200);
        replace_program(BULLETIN_BOARD);
}
