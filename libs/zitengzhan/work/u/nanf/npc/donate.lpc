// pet.c writted by Beeby. 07/28/2000
#include <ansi.h>
// inherit NPC;
inherit F_DBASE;
inherit F_SAVE;

private string SAVE_NAME="donate_save";
private nosave int can_save=1;

void create()
{	
	seteuid(getuid());	
}
string query_save_file()
{
	return sprintf(__DIR__"donates");
}

int restore()
{
	string file;
	if( stringp(file = this_object()->query_save_file()) )
		return restore_object(file);
	return 0;
}
void init()
{	
	seteuid(getuid());
}

void initial(object ob)
{
	/*ob->set("curr_period",1);
	ob->set("period1/amount_intended",0);
	ob->set("period1/amount_confirmed",0);
	ob->set("period1/amount_needed",NEED_AMOUNT);
	ob->save();*/
}