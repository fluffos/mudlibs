// bawang-qiang.c 【霸王枪法】
//lestat modi for dtxy 2001
//modified by huarong 2004/10
inherit SKILL;
#include <ansi.h>

int get_current_day_phase();
nosave mapping *day_phase = NATURE_D->query_day_phase();

string *skill_head = ({
  "$N运足精神，使出一招",
  "$N手腕一翻，一式",
  "$N似欲退走，可是忽地一个转身，百忙中使出一招",
  "$N一个跨步，使一招",
  "$N一按枪把,使出一招",
});
string *skill_tail = ({
  "，手中$w连续刺向$n面门，哽嗓，前心。",
  "，手中$w一挺刺向$n$l。",
  "，手中$w势若飞瀑刺向$n的$l。",
  "，手中$w如飞，发出点点寒光。",
  "，手中$w带出一道寒气，由下而上疾挑$n$l",
});

mapping *action = ({
  ([ "action":
    "$N使一招" + HIW "「迎门三不过」" NOR "，手中$w连续刺向$n面门，哽嗓，前心",
    "damage": 20,
    "dodge": 5,
    "parry": -10,
    "damage_type": "刺伤"
  ]),
  ([ "action":
    "$N运足精神，抖起$w，一招" + HIG "「拨草寻蛇」" NOR "刺向$n$l",
    "damage": 20,
    "dodge": 15,
    "parry": -10,
    "damage_type": "刺伤"
  ]),
  ([ "action":
    "$N一个跨步，使一招" + HIC "「高山流水」" NOR "，手中$w势若飞瀑刺向$n的$l",
    "damage": 25,
    "dodge": 20,
    "parry": -10,
    "damage_type": "刺伤"
  ]),
  ([ "action":
    "$N一按枪把，啪！地一声抖出" + HIM "「五朵枪花」" NOR "。上下左右，虚虚实实刺向$n全身要害",
    "damage": 25,
    "dodge": -10,
    "parry": -5,
    "damage_type": "刺伤"
  ]),
  ([ "action":
    "只见$N一个斜跨，使个" + HIR "「举火烧天势」" NOR "，手中$w带出一道寒气，由下而上疾挑$n$l",
    "damage": 35,
    "dodge": 20,
    "parry": 0,
    "damage_type": "刺伤"
  ]),
  ([ "action":
    "只见$N手腕一翻，手中$w从意想不到的角度刺向$n的$l。\n这一招" + HIB "「灵蛇出洞」" NOR "快若闪电，毒辣无比",
    "damage": 15,
    "dodge": 5,
    "parry": -25,
    "damage_type": "刺伤"
  ]),
  ([ "action":
    "$N使个" + MAG "「飞鸟投林势」" NOR "，身形斜飞，从侧面突出一枪，径刺$n$l",
    "damage": 30,
    "dodge": 15,
    "parry": 0,
    "damage_type": "刺伤"
  ]),
  ([ "action":
    "$N似欲退走，忽听脑后金风有声，于是突然一回身，双手把大枪一抖，\n"
    + "“唰”一声奔$n的$l刺去。这一招又快、又狠，正是\n\n\n"
    + "      " + RED + "〓" + HIR + BLINK + "回马枪" + NOR + RED + "〓" + NOR + "\n\n",
    "damage": 70,
    "force": 40,
    "dodge": -90,
    "parry": -90,
    "damage_type": "刺伤"
  ]),
  ([ "action":
    "$N似欲退走，忽听脑后金风有声，于是突然一回身，双手把大枪一抖，\n"
    + "“唰”一声奔$n的$l刺去。这一招又快、又狠，正是\n\n\n"
    + "      " + RED + "〓" + HIR + BLINK + "回马枪" + NOR + RED + "〓" + NOR + "\n\n",
    "damage": 110,
    "force": 80,
    "dodge": -90,
    "parry": -90,
    "damage_type": "刺伤"
  ]),
  ([ "action":
    "只见$N一个斜跨，使个" + HIR "「举火烧天势」" NOR "，手中$w带出一道寒气，由下而上疾挑$n$l",
    "damage": 135,
    "force": 80,
    "dodge": -90,
    "parry": -90,
    "damage_type": "刺伤"
  ]),
});

int valid_learn(object me) {
  object ob;
  if (me->query("family/family_name") != "将军府")
    return notify_fail("霸王枪法乃将军府不传之密！\n");
  if ((int)me->query("max_force") < 100)
    return notify_fail("你的内力不够，没有办法练霸王枪法。\n");
  if (!(ob = me->query_temp("weapon"))
    || (string)ob->query("skill_type") != "spear")
    return notify_fail("你必须先找一柄枪才能练枪法。\n");

  return 1;
}

int valid_enable(string usage) {
  return (usage == "spear") || (usage == "parry");
}

//rewrited by huarong for dtxy2004  2004/2
mapping query_action(object me, object weapon) {
  int i;
  mapping *m_actions;
  if (me->query("bawang-qiang")) {
    if (random(1)) {
      return action[9];
    }
    return action[8];
  }
  i = me->query_temp("BWQ_perform");
  if (!me->query_temp("BWQ_perform")) {
    if (me->query("skill_bawang-qiang") == 0) return action[random(7)];
    if (random(2)) return action[random(7)];
    m_actions = me->query("skill_bawang-qiang");
    return m_actions[random(sizeof(m_actions))];
  } else return action[i];

}

int practice_skill(object me) {
  object weapon;

  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "spear")
    return notify_fail("你必须先找一柄长枪或者是类似的武器，才能练枪法。\n");

  if ((int)me->query("kee") < 30
    || (int)me->query("force") < 5)
    return notify_fail("你的体力不够练这门枪法，还是先休息休息吧。\n");
  me->receive_damage("kee", 20);
  me->add("force", -5);
  write(RED "十步杀一人，千里不留行。\n" NOR);
  return 1;
}

string perform_action_file(string func) {
  return __DIR__ "bawang-qiang/" + func;
}

mixed hit_ob(object me, object victim, int damage_bonus) {
  object weapon = me->query_temp("weapon");
  string msg;
  int wound = 0, x, y, tforce;
  int phase = get_current_day_phase();

  if ((string)me->query("family/family_name") != "将军府")
    return 0;
  if (!me->query_temp("pfm_zhuiming")) return 0;

  if ((int)me->query_temp("pfm_zhuiming") == 1) {
    msg = HIC "\n接着$N" + HIC + "冷不丁一回身，右臂一抖，“唰”一枪奔$n" + HIC + "后心便刺，\n\n"
      +NOR + RED + "        〓" + HIY + BLINK + "犀牛望月" + NOR + RED + "〓\n\n" NOR;
  } else if ((int)me->query_temp("pfm_zhuiming") == 2) {
    msg = HIM "\n接着$N" + HIM + "枪刺$n" + HIM + "脚下，故作强弩之末状，待$n" + HIM + "暗自庆幸之时，$N" + HIM + "突然枪往上挑\n\n"
      +NOR + RED + "        〓" + HIY + BLINK + "海底捞月" + NOR + RED + "〓\n\n" NOR;
  } else if ((int)me->query_temp("pfm_zhuiming") == 3) {
    msg = HIB "\n$N" + HIB + "右手回枪，自左方虚晃一枪，待$n" + HIB + "收招之时，$N" + HIB + "枪势连绵，如痴如醉\n\n"
      +NOR + RED + "        〓" + HIY + BLINK + "听花醉月" + NOR + RED + "〓\n\n" NOR;
  } else {
    msg = HIR "\n$N" + HIR + "左脚落地，左掌闪电朝$n" + HIR + "右肩切去，右手持枪，同时笔直向$n" + HIR + "心口点出！\n\n"
      +NOR + RED + "        〓" + HIY + BLINK + "流星追月" + NOR + RED + "〓\n\n" NOR;
  }


  tforce = (int)victim->query_skill("force", 1);

  if ((int)me->query_temp("zhuiming_hit") == 1
    && (int)me->query_cor() > (int)me->query_cps()
    && (int)me->query_cor() - (int)me->query_cps() <= 5) {
    x = (int)me->query_skill("bawang-qiang", 1);
    y = (int)me->query_skill("lengquan-force", 1);
    if (phase < 0 || phase > 5)
      wound = x * 4 / 3 + y * 4 / 3 + random(x / 2 + y / 2);
    else
      wound = x * 2 / 3 + y * 2 / 3 + random(x / 2 + y / 2);
    wound -= victim->query_temp("apply/armor_vs_force");
    if (wound < 0) wound = 0;
    tforce = tforce / 2;
  }

  if (((int)me->query_skill("spear", 1)) > random(tforce)) {
    if ((int)me->query_temp("pfm_zhuiming") < 4) {
      switch (random(3)) {
        case 0: {
          victim->receive_damage("kee", damage_bonus / 2);
          victim->receive_wound("kee", wound);
          msg += HIR + "只见一道血光闪过，$n" + HIR + "已然中招！\n" NOR;
          break;
        };

        case 1: {
          victim->receive_damage("sen", damage_bonus / 2);
          victim->receive_wound("sen", wound * 2 / 3);
          msg += HIR "$N" + HIR + "暗使巧力，在$n" HIR + "的伤口上留下一道深深的血槽！\n" NOR;
          break;
        };

        case 2: {
          victim->receive_damage("kee", damage_bonus / 4);
          victim->receive_damage("kee", damage_bonus / 4);
          victim->receive_wound("kee", wound);
          victim->receive_wound("sen", wound * 2 / 3);
          msg += HIR + "$n" + HIR + "突然觉得一阵刺痛，原来已然中枪...\n" NOR;
          break;
        };
      };
      if (wizardp(me) && me->query("env/combat") == "verbose")
        tell_object(
          me,
          sprintf(HIM + BLINK + "damage_bonus: %d  wound: %d\n" NOR, damage_bonus / 2, wound)
        );
    } else {
      victim->receive_damage("kee", damage_bonus / 3);
      victim->receive_damage("kee", damage_bonus / 3);
      victim->receive_wound("kee", wound + random(wound / 3));
      victim->receive_wound("sen", wound + random(wound / 3));
      msg += HIR + "$n" + HIR + "突然觉得一阵剧痛，胸口早已被戳出一个窟窿...\n" NOR;
      if (wizardp(me) && me->query("env/combat") == "verbose")
        tell_object(
          me,
          sprintf(
            HIM + BLINK + "damage_bonus: %d  wound: %d\n" NOR,
            damage_bonus / 3,
            wound + random(wound / 2)
          )
        );
    }
  } else {
    switch (random(3)) {
      case 0: {
        msg += HIG "\n$n" + HIG + "默运真气，小心翼翼的躲开了攻击！\n" NOR;
        break;
      };

      case 1: {
        msg += HIG "\n$n" + HIG + "朗声一笑，侧身闪过！\n" NOR;
        break;
      };

      case 2: {
        msg += HIG "\n只听“嗖”的一声，$n" + HIG + "早已侧身闪过！\n" NOR;
        break;
      };
    };
  }
  return msg;
}

void skill_improved(object me) {
  int m_skill = me->query_skill("bawang-qiang", 1);


  if (m_skill > 200 && m_skill % 10 == 0) {
    tell_object(me, HIW "你对霸王枪法的领悟加深了，你的霸王枪法进入了一个新的境界！\n" NOR);
    if (me->query("zichuang/times") > 7) {
      tell_object(me, HIW "然而你的功夫已经练至顶峰，不能再创出新招了。\n" NOR);
      return;
    }
    if (random(me->query("kar")) < 20) {
      tell_object(me, HIW "然而你的心中居然产生了一种失落感！\n" NOR);
      return;
    }
    if ((random(me->query("int") / 2) + (int)me->query_skill("literate") / 50) < 10) {
      tell_object(me, HIW "然而你的心中升起一种惆怅的感觉，仿佛有一丝重要的东西没有抓住。\n" NOR);
      return;
    }
    tell_object(me, HIW "你突然福至心灵，对霸王枪法领悟出了一招新的用法！\n" NOR);
    tell_object(me, "请为这招取一个名字：");
    input_to((: call_other, __FILE__, "name_skill", me :));
  }
  return;
}

void name_skill(object me, string arg) {
  string *banned_name = ({
    "你", "你", "我", "他", "她", "它", "爸", "妈",
    "爷", "奶", "巫师", "天神", "神仙",
  });
  mapping *m_actions;
  mapping m_act = ([]);
  int content, i = strlen(arg);
  string msg;

  m_actions = me->query("skill_bawang-qiang");
  if (!pointerp(m_actions)) m_actions = ({});

  content = me->query("str") + 2 * me->query_skill("unarmed", 1) / 10;
  m_act += ([ "damage": content ]);
  content = -me->query("spi");
  m_act += ([ "dodge": content ]);
  content = -me->query("cps");
  m_act += ([ "parry": content ]);
  content = me->query("con") + 2 * me->query_skill("force", 1) / 10;
  m_act += ([ "force": content ]);
  m_act += ([ "damage_type": "刺伤" ]);
  if (!arg || arg == "") {
    tell_object(me, "请为你的新招式取一个名字：");
    input_to((: call_other, __FILE__, "name_skill", me :));
    return;
  } else while (i--) {
    if (arg[i] <= ' ') {
      tell_object(me, "招式名字不能使用控制字元，请重新设定一个名字：");
      input_to((: call_other, __FILE__, "name_skill", me :));
      return;
    }
    if (strsrch(arg, "　") >= 0) {
      tell_object(me, "招式名字不能有空格，请重新设定一个名字：");
      input_to((: call_other, __FILE__, "name_skill", me :));
      return;
    }
    if (i % 2 == 0 && !is_chinese(arg[i..<0])) {
      tell_object(me, "请您用「中文」为你的新招式取名字：");
      input_to((: call_other, __FILE__, "name_skill", me :));
      return;
    }
  }
  if (member_array(arg, banned_name) != -1) {
    tell_object(me, "对不起，这种名字会造成其他人的困扰。请重新设定一个名字：");
    input_to((: call_other, __FILE__, "name_skill", me :));
    return;
  }
  if ((strlen(arg) < 8) || (strlen(arg) > 16)) {
    tell_object(me, "对不起，招式名字必须是 ４ 到 ８ 个中文字：");
    input_to((: call_other, __FILE__, "name_skill", me :));
    return;
  }
  //   if(!arg||arg==" ")arg="霸王枪秘传必杀技"+chinese_number(sizeof(m_actions)+1);
  m_act += ([ "name": arg ]);
  msg = skill_head[random(sizeof(skill_head))] + BLINK + MAG "「" + arg + "」" NOR + skill_tail[random(sizeof(skill_tail))];
  m_act += ([ "action": msg ]);
  m_actions += ({ m_act });
  me->set("skill_bawang-qiang", m_actions);
  me->add("zichuang/times", 1);
}

int get_current_day_phase() {
  // ref. /adm/daemons/natured.c:init_day_phase()
  mixed *local = localtime(time() * 60);
  int t = local[2] * 60 + local[1];
  int i;
  for (i = 0; i < sizeof(day_phase); i++)
    if (t >= day_phase[i]["length"])
      t -= (int)day_phase[i]["length"];
    else
      break;
  return (i == 0 ? sizeof(day_phase) - 1 : i - 1);
}
