inherit ROOM;
void create() {
  set("short", "怡红院二楼");
  set("long", @LONG
这里的摆设既华贵又不俗气，都是精工细雕的器具。让人一看就知道
这里的主人非同一般，据说全京城的美女都喜欢到她这里来,要是你不信可
以试试(ask ye about 美容)。
LONG
  );
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/ye-linglong": 1,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "down": __DIR__ "yihongyu",
  ]));
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
