//over's board.c

inherit BULLETIN_BOARD;

void create() {
  set_name("孤独板块", ({ "board" }));
  set("location", "/u/over/workroom");
  set("board_id", "over_b");
  set("long", "寂寞空间，孤独留言\n");
  setup();
  set("capacity", 300);
  replace_program(BULLETIN_BOARD);
}
