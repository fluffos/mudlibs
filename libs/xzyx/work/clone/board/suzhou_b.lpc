inherit BULLETIN_BOARD;

void create() {
  set_name("顾客流言簿", ({ "board" }));
  set("location", "/u/suzhou/kezhan");
  set("board_id", "suzhou_b");
  set("long", "这是一个专供游客使用的留言板。\n");
  setup();
  set("capacity", 1000);
  replace_program(BULLETIN_BOARD);
}
