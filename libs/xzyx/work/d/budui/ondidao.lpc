inherit ROOM;

void create() {
  set("short", "特种部队兵营");
  set("long", @LONG
这里就是特种部队的兵营了，里面云集了特种部队全部的精英，个个都是身怀绝技，
想成为特种部队的一员，到这里来报名是最好不过了。这里四周什么都没有，看上去
显得很空荡，可是仔细一看，又有一条路通向底层，别犹豫了，快下去吧！
LONG
  );


  set("exits", ([
    "down": __DIR__ "rukou",
    "east": __DIR__ "bdguangchang",
  ]));
  set("objects", ([ __DIR__ "npc/tezhongbing1": 3, ]));
  setup();
  replace_program(ROOM);
}
