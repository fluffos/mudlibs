// rukou.c 入口
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "入口");
  set("long", @LONG
这里就是特种部队的入口，边上竖着个牌子(paizi)。
只见两个拿着激光枪的合成人虎视耽耽的望着你，边上还
坐着一个威武不凡的特种战士，似乎有点地位。
LONG);
  set("objects", ([
    "/kungfu/class/budui/jiang": 1,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "down": __DIR__ "didao1",
    "up": __DIR__ "ondidao",
  ]));
  set("item_desc", ([
    "paizi": "欢迎加入特种部队，特种部队是您最佳的选择！\n"
  ]));
  setup();
}
