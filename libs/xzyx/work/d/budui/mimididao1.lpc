inherit ROOM;

#include <ansi.h>


void create() {
  set("short", HIY "密道" NOR);
  set("long", @LONG
这里就是特种部队的秘密地道了，这里看上去很深，不知道
里面有些什么，如果你武功不济，还是回去吧。
LONG);

  set("exits", ([
    "out": __DIR__ "mimijidi",
  ]));
  set("objects", ([ "/kungfu/class/budui/shitailong": 1, ]));
  setup();
  replace_program(ROOM);
}
