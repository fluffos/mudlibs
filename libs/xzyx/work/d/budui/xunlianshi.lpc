// xunlianshi.c 训练室
inherit ROOM;

void create() {
  set("short", "训练室");
  set("long", @LONG
这里是特种部队军的训练室，是训练队员的地方
这里的要求是十分严格的，不是每个队员都可以
通过最后的测试。在训练室的西边是出去
的地道，东边是一个办公室。
LONG);
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "didao1",
    "east": __DIR__ "bangongshi",
  ]));
  setup();
}
