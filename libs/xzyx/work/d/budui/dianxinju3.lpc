inherit ROOM;

void create() {
  set("short", "特种电信局书房");
  set("long", @LONG
到了电信局的书房后，你才感到电信局的不一般，它的书房布置得有条有理，
恰到好处，让人乍一看觉得这里的书非常的多，但仔细一看，其实也就这么多。
这里有许多舒适的长沙发，供来客们休息，阅读之用。
LONG
  );

  set("objects", ([
    __DIR__ "npc/bookshop": 1,
  ]));
  set("exits", ([
    "west": __DIR__ "dianxinju1",


  ]));


  //      set("objects",([__DIR__"npc/robot-food": 1,]));
  setup();
}
