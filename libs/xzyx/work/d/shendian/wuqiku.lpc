inherit ROOM;

void create() {
  set("short", "武器房");
  set("long", @LONG
这里是圣岛的武器房，兵器架上及地上都散乱的摆放着一些普通的兵
器，比如激光剑、激光刀，你可以随便的挑上一件，如果现在没有你可以
稍微等一下，也许会有别人来归还的。
LONG
  );

  set("valid_startroom", 1);
  set("exits", ([
    "south": __DIR__ "lianwu",
    "west": __DIR__ "lianwuchang",

  ]));
  set("objects", ([
    "/d/shendian/npc/cao": 1,
  ]));

  setup();
}
