#include <room.h>

inherit ROOM;

void create() {
  set("short", "过道");
  set("long", @LONG
这里是进入圣殿的必经之路，雪白地墙壁，看不到一丝
的污点，使人油然升起一种圣洁的感觉，
LONG);
  set("exits", ([
    "south": __DIR__ "guodao1",
    "southeast": __DIR__ "jieyindian",
  ]));
  set("objects", ([
    "/d/shendian/npc/hang": 1,

  ]));
  set("outdoors", "shendian");
  setup();
}
