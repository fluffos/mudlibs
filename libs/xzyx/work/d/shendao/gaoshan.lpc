#include <room.h>

inherit ROOM;

void create() {
  set("short", "进山路");
  set("long", @LONG
由这里开始,地势已经一步步的高了起来,"紫艳"高山是圣岛最高的一座
山,山上风景迷人,登上山头可以把圣岛全收入眼底,所以来此游玩的人很多.
LONG);
  set("exits", ([
    "east": __DIR__ "lukou",
    "northwest": __DIR__ "gaoshan1",
  ]));
  set("objects", ([
    //                "/d/shendian/npc/zhoubingung" : 1,
  ]));
  set("outdoors", "shendao");
  setup();
}
