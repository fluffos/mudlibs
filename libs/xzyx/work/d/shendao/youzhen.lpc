#include <room.h>

inherit ROOM;

void create() {
  set("short", "邮政局");
  set("long", @LONG
这是圣岛人们寄信收信的地方,大厅不是很宽,但来来往往办事
的人很多,据说在这里工作的人员,都是一些年轻漂亮的小姐,哇噻
真够味.
LONG);
  set("exits", ([
    "west": __DIR__ "zhouyanjie2",
  ]));
  set("objects", ([
    //                "/d/shendian/npc/zhoubingung" : 1,
  ]));
  //set("outdoors", "shendao");
  setup();
}
