#include <room.h>

inherit ROOM;

void create() {
  set("short", "小型包厢");
  set("long", @LONG
这里咖屋为了方便一些谈情说笑的年轻人而特意设立的小包厢.
四条小凳子,一张小桌子,昏暗的灯光.
LONG);
  set("exits", ([
    "out": __DIR__ "kafe",
  ]));
  set("objects", ([
    //                "/d/shendian/npc/zhoubingung" : 1,
  ]));
  set("outdoors", "shendao");
  setup();
}
