#include <room.h>

inherit ROOM;

void create() {
  set("short", "高速路");
  set("long", @LONG
这条高速是圣岛通向外面的主要通道之一，往来穿窜的车辆很似凭繁。
虽然现在已经很好的控制环保了，但众多的车辆发出的几许燥音，仍然
使人感到不舒服。
LONG);
  set("exits", ([
    "north": __DIR__ "gaosulu2",
    "south": __DIR__ "gaosulu",
  ]));
  set("objects", ([
    //                "/d/shendian/npc/zhoubingung" : 1,
  ]));
  set("outdoors", "shendao");
  setup();
}
