#include <room.h>

inherit ROOM;

void create() {
  set("short", "出租车站");
  set("long", @LONG
出租车停靠点.大凡出租车归站后都停放在这里,车站有
专门的人员看护.LONG );
	set("exits", ([
		"northwest"  : __DIR__"chezhan",
	]));
        set("objects", ([
//                "/d/shendian/npc/zhoubingung" : 1,
        ]));
	set("outdoors", "shendao");
	setup();
}
