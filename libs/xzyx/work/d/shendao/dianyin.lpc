#include <room.h>

inherit ROOM;

void create() {
  set("short", "电影院");
  set("long", @LONG
这就是圣岛的电影院,最近开放了什么情侣包厢之类的,所以来此
消费的年轻人都多了起来,电影院门口照例是一些小贩在叫声叫卖着,
几个卖黄牛票的人也不甘辛苦地来回走动着,三个售票窗口都被挤得
满满的.
LONG);
  set("exits", ([
    "west": __DIR__ "zhouyanjie3",
  ]));
  set("objects", ([
    //                "/d/shendian/npc/zhoubingung" : 1,
  ]));
  set("outdoors", "shendao");
  setup();
}
