inherit ROOM;

void create() {
  set("short", "小路");
  set("long", @LONG
这是一条通往岛内的小路。前面好像可以看见一个山谷。
不知道那里有什么东西，你不自觉的想去看看。
LONG);
  set("exits", ([
    "west": __DIR__ "shatan2",
    "south": __DIR__ "sgus",
  ]));
  //        set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
