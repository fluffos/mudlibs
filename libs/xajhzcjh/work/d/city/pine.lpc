// 文件位置：d\city\pine.c
//2008-02-12

inherit ROOM;
void create() {
  //房间名称叙述
  set("short", "大榕树上");
  set("long", @LONG
你现在正攀附在大榕树的树干上，从这里可以很清楚地望见树下的
一切动静，而不被人发觉，似乎是个干偷鸡摸狗勾当的好地方。
LONG);
  setup();
  replace_program(ROOM);
}
