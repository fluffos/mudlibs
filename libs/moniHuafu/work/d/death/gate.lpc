// TIE@FY3
#include <ansi.h>
inherit ROOM;
void create()
{
        set("short", "高三鬼门关");
        set("long", @LONG
猛一惊醒，不觉身在高三鬼门关，高大的教学楼阴气森森，几个面目狰狞
的语文，数学老师手持武器把守城门，物理、政治老师手持铁链正在拖拽着几
个人影走了进去．．．．．．．，城门楼上高悬一幅牌匾：
[31m

			高三就是鬼门关
[37m
LONG
        );
        set("exits", ([
                "south" : __DIR__"naihe",
	"east" : __DIR__"aihe2",
        ]) );
        set("objects", ([
                __DIR__"npc/niutou":1,
		__DIR__"npc/mamian":1,
		__DIR__"npc/panguan" : 1,
        ]) );
	set("no_fight",1);
	set("no_magic",1);
	set("coor/x",-1020);
	set("coor/y",0);
	set("coor/z",-100);
	setup();
        replace_program(ROOM);
}
 
