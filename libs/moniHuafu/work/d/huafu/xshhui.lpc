inherit ROOM;
void close_path();
void create() {
  set("short", "学生会");
  set("long", @LONG
这里就是学生会了，所谓学生会，无非就是找几个学生开会。能“有幸”坐在这
儿的，不是在学代会被选出来的可怜虫，就是大扫除来打扫房间的同学。整间房间的
中央只有一张破破烂烂的长方桌，桌上放着铺着斑驳陆离脏得恶心的绿色桌布。四面
的墙上挂满了各种各样的告示文件。一块白板（board）上写满了歪歪扭扭的字迹。
LONG
  );
  set("exits", ([
    "north": __DIR__ "wlang",
  ]));
  set("objects", ([
    __DIR__ "npc/zhuxi": 1,
  ]));
  set("NONPC", 1);
  set("valid_startroom", 1);
  set("coor/x", -20);
  set("coor/y", 10);
  set("coor/z", 0);
  setup();
}
