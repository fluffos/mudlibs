// pingpong.c 乒乓球具

inherit ITEM;

void create() {
  set_name("乒乓球具", ({ "pingpong", "ball" }));
  set_weight(300);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一副乒乓球具。\n");
    set("unit", "副");
    set("value", 800);
  }
}
