// badminton.c 羽毛球具

inherit ITEM;

void create()
{
	set_name("羽毛球具", ({"badminton","ball"}));
	set_weight(300);
	if (clonep())
		set_default_object(__FILE__);
	else {
		set("long", "一副羽毛球具。\n");
		set("unit", "副");
		set("value", 800);
	}
}