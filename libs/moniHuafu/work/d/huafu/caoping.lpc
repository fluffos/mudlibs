// Room:/d/huafu/caoping.c
// 有小鸟。给人打来练功
inherit ROOM;

void create()
{
        set("short", "草坪");
        set("long", @LONG
这里是一片翠绿的草坪，草坪上栽种着几棵不知名的热带植物，散乱的
放着几块石头，不过可惜因为常有人来坐，早已脏得很了。你最好不要坐上
去。草地上还停着几只小鸟，也许已经麻木了吧，它们并不怕人。
LONG
        );
	set("valid_startroom",1);
        set("exits", ([ 
  "west" : __DIR__"zhxd1",
]));
        set("objects", ([
        __DIR__"npc/bird" : 5,
                        ]) );

	set("outdoors","华师附中");
	set("coor/x",20);
	set("coor/y",40);
	set("coor/z",0);
	setup();
}