// 电脑内部
// 涉及呆呆的杀毒job
inherit ROOM;
void reset();
void create()
{
        set("short", "电脑内部");
        set("long", @LONG
这里就是电子阅览室的Dell品牌电脑的内部了，只见这台机子一副愁眉苦脸的
样子，仿佛生了一场大病，而这里却有几个小程序，显得志得意满，又蹦又跳的耀
武扬威，原来它就是罪魁祸首——病毒！看来你如果不把它打倒，是别想离开这台
电脑的了。
LONG
        );
        set("objects", ([
        __DIR__"npc/bingdu" : random(3),
                        ]) );
	set("valid_startroom",1);
	set("coor/x",1000);
	set("coor/y",1000);
	set("coor/z",1000);
	setup();
}
void reset()
{
    object room;
    ::reset();
}
