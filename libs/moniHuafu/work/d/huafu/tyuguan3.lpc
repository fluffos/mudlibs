inherit ROOM;
#include <ansi.h>
void create()
{
        set("short", "体育馆三楼");
        set("long", @LONG
这里就是华师附中的体育馆了，体育馆的三楼好像没什么特别的
用处，所以这次棋艺比赛，这里就拿来当比赛的场地了，这里比的是
中国象棋，如果你对此有一番研究也可以找人来捉对厮杀，房间里摆
着一块上好红木制的中国象棋棋墩。下面的二楼也有比赛，上面的四
楼是乒乓球场。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "up" : __DIR__"tyuguan4",
  "down" : __DIR__"tyuguan2",
  "east" : __DIR__"zhonglou3",
]));
	set("valid_startroom",1);
        set("objects", ([
        __DIR__"obj/cch" : 1,
                        ]) );
        set("coor/x",-60);
	set("coor/y",-20);
	set("coor/z",60);
	setup();
}

