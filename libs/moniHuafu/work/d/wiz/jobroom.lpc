// jjgod@vrhf
inherit ROOM;
void create() {
  set("short", "办公楼一楼教学处");
  set("long", @LONG
这里是华师附中的教学处，教学处是管理学籍的地方，
这里的教学处主任负责给大家办理校徽和借书证，有了校
徽你就可以随意出入校门，而有了借书证，你就可以去图
书馆借书了。赶快买一个吧，价钱很公道的！这里过去西
边，是学校的锦鲤池，每天都有不少同学到那里去钓鱼。
LONG
  );
  set("no_fight", 1);
  set("no_magic", 1);
  set("no_spells", 1);
  set("exits", ([ /* sizeof() == 1 */
    "up": __DIR__ "courthouse",
    "south": "/d/huafu/libzixi",
    "north": "/d/huafu/whroad2",
    "west": "/d/huafu/jlchi",
    "enter": __DIR__ "hall",
  ]));
  set("objects", ([
    __DIR__ "npc/zhuren": 1,
    __DIR__ "obj/votebox": 1,
    __DIR__ "npc/libiliang": 1,
  ]));
  set("coor/x", -60);
  set("coor/y", 20);
  set("coor/z", 0);
  setup();
  call_other("/obj/board/wizard_j", "???");
  replace_program(ROOM);
}
