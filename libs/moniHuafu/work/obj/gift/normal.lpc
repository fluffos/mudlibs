
#include <ansi.h>

string give_gift();

private mixed *story = ({
        "最近华师附中没有什么特别的新闻。",
});

void create()
{
        seteuid(getuid());
}

mixed query_story_message(int step)
{
        return step < sizeof(story) ? story[step] : 0;
}
