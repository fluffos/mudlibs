#include <mudlib.h>



void create(object ob)

{

	object ob1;

    if ( !ob ) return ;

	set_level(1);

	set_name( "farmer's wife", "胖农夫的妻子" );

	add( "id", ({ "farmer", "wife" }) );

   set_short( "胖农夫的妻子" );

	set_long(

		"胖农夫的妻子也一样很胖....不过他的先生似乎并不这麽认为。\n"

	);

	set( "max_hp", 35 );

	set( "hit_points", 35 );

	set( "race", "human" );

	set( "gender", "female" );

	set( "alignment", 200 );

set( "wealth/silver", 20 );

	}
