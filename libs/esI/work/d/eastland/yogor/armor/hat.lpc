#include "yogor.h"

inherit ARMOR;

void create() {
  set_name("wondering hat", "逍遥帽");
  add("id", ({ "hat" }));
  set_short("逍遥帽");
  set_long("一顶有庄子力量加护的帽子．\n");
  set("unit", "顶");
  set("type", "head");
  set("weight", 40);
  set("material", "cloth");
  set("armor_class", 6);
  set("defense_bonus", 3);
  set("extra_skills", ([ "hide": 3 ]));
  set("value", ({ 100, "gold" }));
}
