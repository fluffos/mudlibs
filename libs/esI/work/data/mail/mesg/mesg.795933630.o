#adm/daemons/mailmesg.c
remain ({"ruby",})
mesg " comm.c 的 telnet_neg()         default        if (ch & 0x80) continue; <- 去掉这行...0x80 是 7th bit 的 hex, 去掉这行後 driver 就可以显示或输入中文.. 以下是如果你要用 ed() 来修改档案: ed.c 的 prntln() if (*str =' ' || *str > DEL)              ^^^^^^^^^^^^^                        把这段去掉..用途: 当我们要显示中文时不要让 driver call 下面的 putcntl() file.c 的 read_file() ...... 不详.. Annihilator 以前提过一次, 这次他只说新 driver 已经有support.... 这个我再研究看看, 不行了再求救 :) "
