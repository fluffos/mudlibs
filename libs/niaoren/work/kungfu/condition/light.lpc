// light.c
// sunpoet@UW, 2000/07/09

#include <condition.h>

int update_condition(object obj, int duration) {
    if (duration--==1) {
        message_vision("$N的火把燒盡了，$N隨手扔了。\n", obj, obj);
        return 0;
    }
    obj->apply_condition("light", duration);
    return CND_CONTINUE;
}
