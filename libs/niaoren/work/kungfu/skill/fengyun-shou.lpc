// fengyun-shou.c -風云手

inherit SKILL;

mapping *action = ({
([	"action" : "$N一式「風滿長空」，以掌化爪，五指如鉤，直逼$n的膻中要穴",
	"force" : 120,
        "dodge" : 5,
	"damage": 15,
        "lvl" : 0,
        "skill_name" : "風滿長空",
	"damage_type" : "瘀傷"
]),
([	"action" : "$N左手虛晃，一式「烈火騰云」，右手上下直擊，反扣$n的肩井大穴",
	"force" : 130,
        "dodge" : 10,
	"damage": 20,
        "lvl" : 8,
        "skill_name" : "烈火騰云",
        "damage_type" : "瘀傷"
]),
([	"action" : "$N使一式「草深霧澤」，掌指齊出，拍拿并施，拿向$n的三路要害",
	"force" : 150,
        "dodge" : 15,
	"damage": 20,
        "lvl" : 16,
        "skill_name" : "草深霧澤",
        "damage_type" : "瘀傷"
]),
([	"action" : "$N左手鷹抓，右手蛇舉，一式「天目昭輝」疾扣$n的中節大脈",
	"force" : 180,
        "dodge" : 20,
	"damage": 30,
        "lvl" : 24,
        "skill_name" : "天目昭輝",
        "damage_type" : "瘀傷"
]),
([	"action" : "$N使一式「雷震四方」，四面八方出現無數掌影，一爪突出，抓向$n的胸口",
	"force" : 220,
        "dodge" : 25,
	"damage": 35,
        "lvl" : 40,
        "skill_name" : "雷震四方",
        "damage_type" : "內傷"
]),
([	"action" : "$N兩手環扣，全身關節啪啪作響，一式「水到渠成」，擊向$n的$l",
	"force" : 270,
        "dodge" : 30,
	"damage": 40,
        "lvl" : 48,
        "skill_name" : "水到渠成",
        "damage_type" : "內傷"
]),
([	"action" : "$N一式「山高林密」，十指齊伸，遮天蔽日般地籠罩$n的全身要穴",
	"force" : 330,
        "dodge" : 35,
	"damage": 48,
        "lvl" : 54,
        "skill_name" : "山高林密",
        "damage_type" : "內傷"
]),
([	"action" : "$N岳立霆峙，一式「地老天荒」，在一陣暴雷聲中，雙手同時拍中$n的七道死穴",
	"force" : 400,
        "dodge" : 56,
	"damage": 60,
        "lvl" : 0,
        "skill_name" : "地老天荒",
        "damage_type" : "內傷"
])
});

int valid_enable(string usage) { return usage=="hand" || usage=="parry"; }

int valid_combine(string combo) { return combo=="yingzhua-gong"; }

int valid_learn(object me)
{
	if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
		return notify_fail("練風云手必須空手。\n");
	if ((int)me->query_skill("hunyuan-yiqi", 1) < 20)
		return notify_fail("你的混元一氣功火候不夠，無法學風云手。\n");
	if ((int)me->query("max_neili") < 100)
		return notify_fail("你的內力太弱，無法練風云手。\n");
	return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
	level   = (int) me->query_skill("fengyun-shou",1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
}

int practice_skill(object me)
{
	if ((int)me->query("qi") < 30)
		return notify_fail("你的體力太低了。\n");
	if ((int)me->query("neili") < 20)
		return notify_fail("你的內力不夠練風云手。\n");
	me->receive_damage("qi", 25);
	me->add("neili", -5);
	return 1;
}

