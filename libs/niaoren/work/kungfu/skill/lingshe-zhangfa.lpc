//lingshe_zhangfa.c 靈蛇杖法

#include "ansi.h"

inherit SKILL;

mapping *action = ({
([      "action": "$N使出一招「靈蛇出洞」，手中$w大開大闔掃向$n的$l",
        "force" : 100,
        "dodge": -3,
        "damage": 18,
        "lvl" : 10,
        "skill_name" : "靈蛇出洞",
        "damage_type": "挫傷"
]),
([      "action": "$N手中$w陣陣風響，一招「搖頭擺尾」向$n的$l攻去",
        "force" : 140,
        "dodge": -5,
        "damage": 24,
        "lvl" : 20,
        "skill_name" : "搖頭擺尾",
        "damage_type": "挫傷"
]),
([      "action": "$N舉起$w，居高臨下使一招「靈蛇下澗」砸向$n的$l",
        "force" : 180,
        "dodge": -10,
        "damage": 30,
        "lvl" : 30,
        "skill_name" : "靈蛇下澗",
        "damage_type": "挫傷"
]),
([      "action": "$N一招「靈蛇挺身」，手中$w猛烈掃向$n的$l",
        "force" : 220,
        "dodge": -5,
        "damage": 36,
        "lvl" : 40,
        "skill_name" : "靈蛇挺身",
        "damage_type": "挫傷"
]),
([      "action": "$N使出一招「蛇游蟒走」，$w左右迂回向$n的$l掃去",
        "force" : 260,
        "dodge": -3,
        "damage": 42,
        "lvl" : 50,
        "skill_name" : "蛇游蟒走",
        "damage_type": "挫傷"
]),
([      "action": "$N手中$w一揮，使出一招「蛇纏左右」，忽左忽右掃向$n的$l",
        "force" : 300,
        "dodge": -5,
        "damage": 48,
        "lvl" : 60,
        "skill_name" : "蛇纏左右",
        "damage_type": "挫傷"
]),
([      "action": "$N使出一招「巨蟒下山」，高舉手中$w，劈頭砸向$n的$l",
        "force" : 340,
        "dodge": -10,
        "damage": 54,
        "lvl" : 80,
        "skill_name" : "巨蟒下山",
        "damage_type": "挫傷"
]),
([      "action": "$N使出一招「靈蛇出洞」，手中$w猛一探，直掃$n的$l",
        "force" : 380,
        "dodge": -3,
        "damage": 60,
        "lvl" : 100,
        "skill_name" : "靈蛇出洞",
        "damage_type": "挫傷"
]),

});

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
        level   = (int) me->query_skill("lingshe-zhangfa",1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
//      return action[random(sizeof(action))];
}

int valid_enable(string usage) { return (usage == "staff") || (usage == "parry"); }

int valid_learn(object me)
{
        if ((int)me->query("max_neili") < 70)
                return notify_fail("你的內力不夠。\n");
        return 1;
}

int practice_skill(object me)
{
        object weapon;

        if (!objectp(weapon = me->query_temp("weapon"))
        || (string)weapon->query("skill_type") != "staff")
                return notify_fail("你使用的武器不對。\n");
        if ((int)me->query("qi") < 50)
                return notify_fail("你的體力不夠練靈蛇杖法。\n");
        me->receive_damage("qi", 20);
        return 1;
}

mixed hit_ob(object me, object victim, int damage_bonus, int factor)
{
        if( random(me->query_skill("hamagong")) > 10 ) 
        {
        victim->apply_condition("snake_poison", random(me->query_skill("hamagong")/10) + 1 +
        victim->query_condition("snake_poison"));
        tell_object(victim, HIG "你覺得被擊中的地方一陣麻木！\n" NOR );
        tell_object(victim, HIG "原來是被杖頭竄出的毒蛇咬了一口！\n" NOR );
        }
}
string perform_action_file(string action)
{
        return __DIR__"liangshe-zhangfa/" + action;
}
