// jingang-quan.c -大金剛拳

inherit SKILL;

mapping *action = ({
([	"action" : "$N一招「黑虎掏心」，右拳蓄勢而發，擊向$n的$l",
	"force" : 180,
        "dodge" : -5,
	"lvl" : 0,
	"skill_name" : "黑虎掏心",
	"damage_type" : "瘀傷"
]),
([	"action" : "$N一式「烈火錐」，雙掌輪流下擊，拳勢如焰，  啪爆響",
	"force" : 200,
        "dodge" : 5,
	"damage": 10,
	"lvl" : 10,
	"skill_name" : "烈火錐",
        "damage_type" : "瘀傷"
]),
([	"action" : "$N騰空飛起，一式「八方雨」，雙手雙腿齊出，令$n無可躲藏",
	"force" : 230,
        "dodge" : 15,
	"damage": 10,
	"lvl" : 20,
	"skill_name" : "八方雨",
        "damage_type" : "瘀傷"
]),
([	"action" : "$N雙掌虛含，掌緣下沉，一式「掌心雷」，緩緩向$n推出",
	"force" : 270,
        "dodge" : 5,
	"damage": 20,
	"lvl" : 30,
	"skill_name" : "掌心雷",
        "damage_type" : "瘀傷"
]),
([	"action" : "$N一臂前伸，一臂后指，一式「五指山」，攻向$n的身前身后",
	"force" : 320,
        "dodge" : 10,
	"damage": 15,
	"lvl" : 40,
	"skill_name" : "五指山",
        "damage_type" : "瘀傷"
]),
([	"action" : "$N一式「有去無回」，兩手十指虛點$n的全身三十六道要穴",
	"force" : 380,
        "dodge" : 25,
	"damage": 25,
	"lvl" : 48,
	"skill_name" : "有去無回",
        "damage_type" : "瘀傷"
]),
([	"action" : "$N兩目內視，雙手內籠，一式「天龍唱」，四面八方響起震人心魄的龍吟",
	"force" : 450,
        "dodge" : 15,
	"damage": 35,
	"lvl" : 54,
	"skill_name" : "天龍唱",
        "damage_type" : "瘀傷"
]),
([	"action" : "$N似笑非笑，雙拳無形無定，一式「如來笑」，驟然擊向$n的前胸",
	"force" : 530,
        "dodge" : 20,
	"damage": 50,
	"lvl" : 60,
	"skill_name" : "如來笑",
        "damage_type" : "瘀傷"
])
});

int valid_enable(string usage) { return  usage=="cuff" || usage=="parry"; }

int valid_learn(object me)
{
	if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
		return notify_fail("練太祖長拳必須空手。\n");
	if ((int)me->query_skill("zixia-shengong", 1) < 10)
		return notify_fail("你的紫霞神功火候不夠，無法學太祖長拳。\n");
	if ((int)me->query("max_neili") < 50)
		return notify_fail("你的內力太弱，無法練太祖長拳。\n");
	return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
	level   = (int) me->query_skill("jingang-quan",1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
}

int practice_skill(object me)
{
	if ((int)me->query("qi") < 30)
	{
		if((string)me->query("gender") == "男性")
			return notify_fail("你的體力太低了。歇會兒吧，哥兒們！\n");
		if((string)me->query("gender") == "女性")
			return notify_fail("你的體力太低了。歇會兒吧，姐兒們！\n");
	}
	if ((int)me->query("neili") < 20)
		return notify_fail("你的內力不夠練太祖長拳。\n");
	me->receive_damage("qi", 30);
	me->add("neili", -10);
	return 1;
}
