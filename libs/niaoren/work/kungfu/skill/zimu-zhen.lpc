// zimu-zhen.c 子母針法

#include <ansi.h>

inherit SKILL;

mapping *action = ({
([	"action":		"$N雙手連環揮舞，一招「穿心針」，$w直奔$n的胸腹要害",
	"dodge":		-10,
	"damage":		200,
	"poison": 30,
	"lvl" : 0,
	"skill_name" : "穿心針",
	"post_action":  (: call_other, WEAPON_D, "throw_weapon" :),
	"damage_type":	"刺傷"
]),
([	"action":		"$N身形晃動，一招「破氣針」，手中$w如一串銀線，往$n咽喉打去",
	"dodge":		-20,
	"damage":		300,
	"poison": 40,
	"lvl" : 60,
	"skill_name" : "破氣針",
	"post_action":  (: call_other, WEAPON_D, "throw_weapon" :),
	"damage_type":	"刺傷"
]),
([	"action":		"$N五指微張，一招「吸血針」，手中$w飛蚊般往$n全身叮去",
	"dodge":		-40,
	"damage":		400,
	"poison": 50,
 	"lvl" : 90,
	"skill_name" : "吸血針",
	"post_action":  (: call_other, WEAPON_D, "throw_weapon" :),
	"damage_type":	"刺傷"
]),
([	"action":		"$N大喝一聲，一招「攝魂針」，$w幻化出奪目光亮，打向$n",
	"dodge":		-50,
	"damage":		500,
	"poison": 60,
	"lvl" : 100,
	"skill_name" : "攝魂針",
	"post_action":  (: call_other, WEAPON_D, "throw_weapon" :),
	"damage_type":	"刺傷"
]),
([	"action":		"$N錯步一躍，一招「奪魄針」，$w帶著陣陣陰風，打向$n全身",
	"dodge":		-60,
	"damage":		600,
	"poison": 70,
 	"lvl" : 110,
	"skill_name" : "奪魄針",
	"post_action":  (: call_other, WEAPON_D, "throw_weapon" :),
	"damage_type":	"刺傷"
]),
([	"action":		"$N雙目精芒閃動，一招「離別針」，手中$w全數脫手飛出，排山倒海般打向$n全身",
	"dodge":		-60,
	"damage":		700,
	"poison": 80,
	"lvl" : 120,
	"skill_name":  "離別針",
	"post_action":  (: call_other, WEAPON_D, "throw_weapon" :),
	"damage_type":	"刺傷"
]),
([	"action":		"$N縱聲長嘯，一招「天魔針」，$w竟然帶著一股強烈勁風，掃向$n",
	"dodge":		-70,
	"damage":		800,
	"poison": 90,
	"lvl" : 130,
	"skill_name" : "天魔針",
	"post_action":  (: call_other, WEAPON_D, "throw_weapon" :),
	"damage_type":	"刺傷"
]),
([	"action":		"$N眼中殺氣突然大盛，一招「地煞針」，$w飛蝗般打向$n全身",
	"dodge":		100,
	"damage":		900,
	"poison": 100,
	"lvl" : 200,
	"skill_name" : "地煞針",
	"post_action":  (: call_other, WEAPON_D, "throw_weapon" :),
	"damage_type":	"刺傷"
]),
});

int valid_enable(string usage) { return (usage == "throwing") || (usage == "parry"); }

int valid_learn(object me)
{
	object weapon;

	if( (int)me->query("max_neili") < 500 )
		return notify_fail("你的內力夠練子母連環針, 再多練練吧。\n");

	if ((int)me->query_skill("biyun-xinfa", 1) < 100)
		return notify_fail("你的碧云心法火候太淺。\n");

	if ( !objectp(weapon = me->query_temp("weapon"))
	|| ( string)weapon->query("skill_type") != "throwing" )
		return notify_fail("你手上并沒有暗器，怎么練？\n");

	return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
	level   = (int) me->query_skill("zimu-zhen",1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
}

int practice_skill(object me)
{
	object weapon;

	if (!objectp(weapon = me->query_temp("weapon"))
	|| (string)weapon->query("skill_type") != "throwing")
		return notify_fail("你使用的武器不對。\n");
	if ((int)me->query("qi") < 50)
		return notify_fail("你的體力不夠練子母針法。\n");
	me->receive_damage("qi", 5);
	return 1;
}

string perform_action_file(string action)
{
	if ( this_player()->query_skill("zimu-zhen", 1) >= 50 )
		return __DIR__"zimu-zhen/" + action;
}
mixed hit_ob(object me, object victim, int damage_bonus, int factor)
{object weapon;weapon=me->query_temp("weapon");
              
        if(weapon->query("name")=="子母飛針" && weapon->query("id")=="zimuzhen" && me->query_temp("marks/子母針") ){
          victim->apply_condition("zm_poison", random(me->query_skill("zimu-zhen")) + 10 +
	  victim->query_condition("zm_poison"));
	}
}
