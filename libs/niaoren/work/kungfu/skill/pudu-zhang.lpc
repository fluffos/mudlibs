// pudu-zhang.c 普渡杖法

inherit SKILL;

mapping *action = ({
([	"action":"$N使一招「黃牛轉角」，手中$w自下而上，沉猛無比地向$n的小腹挑去。",
	"force" : 180,
        "dodge" : -10,
	"damage": 15,
	"lvl" : 0,
	"skill_name" : "黃牛轉角",
	"damage_type":"挫傷"
]),
([	"action":"$N快步跨出，一招「野馬追風」，左手平托$w，右掌猛推杖端，頂向$n的胸口。",
	"force" : 200,
        "dodge" : -10,
	"damage": 20,
	"lvl" : 9,
	"skill_name" : "野馬追風",
	"damage_type":"挫傷"
]),
([	"action":"$N高舉$w，一招「猛虎跳澗」，全身躍起，手中$w摟頭蓋頂地向$n擊去。",
	"force" : 230,
        "dodge" : -5,
	"damage": 30,
	"lvl" : 18,
	"skill_name" : "猛虎跳澗",
	"damage_type":"挫傷"
]),
([	"action":"$N一招「獅子搖頭」，雙手持杖如櫓，對准$n猛地一攪，如同平地刮起一陣旋風。",
	"force" : 270,
        "dodge" : -10,
	"damage": 35,
	"lvl" : 27,
	"skill_name" : "獅子搖頭",
	"damage_type":"挫傷"
]),
([	"action":"$N橫持$w，一招「蒼龍擺尾」，杖端化出無數個圓圈，凝滯沉重，把$n纏在其中。",
	"force" : 320,
        "dodge" : -15,
	"damage": 40,
	"lvl" : 36,
	"skill_name" : "蒼龍擺尾",
	"damage_type":"挫傷"
]),
([	"action":"$N全身滾倒，$w盤地橫飛，突出一招「大蟒翻身」，杖影把$n裹了起來",
	"force" : 380,
        "dodge" : 5,
	"damage": 55,
	"lvl" : 45,
	"skill_name" : "大蟒翻身",
	"damage_type":"挫傷"
]),
([	"action":"$N雙手和十，躬身一招「胡僧托缽」，$w自肘彎飛出，攔腰向$n撞去。",
	"force" : 450,
        "dodge" : -5,
	"damage": 55,
	"lvl" : 53,
	"skill_name" : "胡僧托缽",
	"damage_type":"挫傷"
]),
([	"action":"$N一招「慈航普渡」，$w如飛龍般自掌中躍出，直向$n的胸口穿入。",
	"force" : 530,
        "dodge" : -5,
	"damage": 60,
	"lvl" : 60,
	"skill_name" : "慈航普渡",
	"damage_type":"挫傷"
]),
});

int valid_enable(string usage) { return (usage == "staff") || (usage == "parry"); }

int valid_learn(object me)
{
	if ((int)me->query("max_neili") < 100)
		return notify_fail("你的內力不夠。\n");
	if ((int)me->query_skill("hunyuan-yiqi", 1) < 20)
		return notify_fail("你的混元一氣功火候太淺。\n");
	return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
	level   = (int) me->query_skill("pudu-zhang",1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
}

int practice_skill(object me)
{
	object weapon;

	if (!objectp(weapon = me->query_temp("weapon"))
	|| (string)weapon->query("skill_type") != "staff")
		return notify_fail("你使用的武器不對。\n");
	if ((int)me->query("qi") < 50)
		return notify_fail("你的體力不夠練普渡杖法。\n");
	me->receive_damage("qi", 30);
	return 1;
}
