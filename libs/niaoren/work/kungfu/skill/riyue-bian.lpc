// riyue-bian.c 日月鞭法

#include <ansi.h>

inherit SKILL;

mapping *action = ({
([	"action":		"$N端坐不動，一招「裂石式」，手中$w抖得筆直，對准$n的胸腹要害連刺數鞭",
	"dodge":		-10,
	"damage":		100,
	"lvl" : 0,
	"skill_name" : "裂石式",
	"damage_type":	"刺傷"
]),
([	"action":		"$N身形一轉，一招「斷川式」，手中$w如矯龍般騰空一卷，猛地向$n劈頭打下",
	"dodge":		-20,
	"damage":		200,
	"lvl" : 60,
	"skill_name" : "斷川式",
	"damage_type":	"刺傷"
]),
([	"action":		"$N力貫鞭梢，一招「破云式」，手中$w舞出滿天鞭影，排山倒海般掃向$n全身",
	"dodge":		-40,
	"damage":		300,
	"lvl" : 90,
	"skill_name" : "破云式",
	"damage_type":	"刺傷"
]),
([	"action":		"$N力貫鞭梢，一招「分海式」，手中$w舞出滿天鞭影，排山倒海般掃向$n全身",
	"dodge":		-50,
	"damage":		400,
	"lvl" : 100,
	"skill_name" : "分海式",
	"damage_type":	"刺傷"
]),
([	"action":		"$N力貫鞭梢，一招「裂空式」，手中$w舞出滿天鞭影，排山倒海般掃向$n全身",
	"dodge":		-60,
	"damage":		500,
	"lvl" : 110,
	"skill_name" : "裂空式",
	"damage_type":	"刺傷"
]),
([	"action":		"$N力貫鞭梢，一招「佛光普照」，手中$w舞出滿天鞭影，排山倒海般掃向$n全身",
	"dodge":		-60,
	"damage":		600,
	"lvl" : 120,
	"skill_name":  "佛光普照",
	"damage_type":	"刺傷"
]),
([	"action":		"$N力貫鞭梢，一招「金剛伏魔」，手中$w舞出滿天鞭影，排山倒海般掃向$n全身",
	"dodge":		-70,
	"damage":		700,
	"lvl" : 130,
	"skill_name" : "金剛伏魔",
	"damage_type":	"刺傷"
]),
([	"action":		"$N力貫鞭梢，一招「佛法無邊」，手中$w舞出滿天鞭影，排山倒海般掃向$n全身",
	"dodge":		100,
	"damage":		800,
	"lvl" : 200,
	"skill_name" : "佛法無邊",
	"damage_type":	"刺傷"
]),
});

int valid_enable(string usage) { return (usage == "whip") || (usage == "parry"); }

int valid_learn(object me)
{
	object weapon;

	if( (int)me->query("max_neili") < 500 )
		return notify_fail("你的內力不足，沒有辦法練日月鞭法, 多練些內力再來吧。\n");

	if ((int)me->query_skill("hunyuan-yiqi", 1) < 50)
		return notify_fail("你的混元一氣功火候太淺。\n");

	if ( !objectp(weapon = me->query_temp("weapon"))
	|| ( string)weapon->query("skill_type") != "whip" )
		return notify_fail("你必須先找一條鞭子才能練鞭法。\n");

	return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
	level   = (int) me->query_skill("riyue-bian",1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
}

int practice_skill(object me)
{
	object weapon;

	if (!objectp(weapon = me->query_temp("weapon"))
	|| (string)weapon->query("skill_type") != "whip")
		return notify_fail("你使用的武器不對。\n");
	if ((int)me->query("qi") < 50)
		return notify_fail("你的體力不夠練日月鞭法。\n");
	me->receive_damage("qi", 5);
	return 1;
}

string perform_action_file(string action)
{
	if ( this_player()->query_skill("riyue-bian", 1) >= 50 )
		return __DIR__"riyue-bian/" + action;
}

