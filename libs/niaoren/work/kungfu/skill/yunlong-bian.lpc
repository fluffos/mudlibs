// yunlong-bian.c 云龍鞭法

#include <ansi.h>

inherit SKILL;

mapping *action = ({
([	"action":		"$N單手一揚，一招「開天辟地」，手中$w抖得筆直，對准$n當頭罩下",
	"force":		100,
	"dodge":		10,
	"damage":		10,
	"lvl" : 0,
	"skill_name" : "開天辟地",
	"damage_type":	"劈傷"
]),
([	"action":		"$N身形一轉，一招「龍騰四海」，手中$w如矯龍般騰空一卷，猛地擊向$n太陽穴",
	"force":		150,
	"dodge":		10,
	"damage":		20,
	"lvl" : 10,
	"skill_name" : "龍騰四海",
	"damage_type":	"劈傷"
]),
([	"action":		"$N唰的一抖長鞭，一招「矯龍出水」，手中$w抖得筆直，刺向$n雙眼",
	"force":		200,
	"dodge":		30,
	"damage":		30,
	"lvl" : 20,
	"skill_name" : "矯龍出水",
	"damage_type":	"刺傷"
]),
([	"action":		"$N力貫鞭梢，一招「破云見日」，手中$w舞出滿天鞭影，排山倒海般掃向$n全身",
	"force":		200,
	"dodge":		40,
	"damage":		40,
	"lvl" : 30,
	"skill_name" : "破云見日",
	"damage_type":	"刺傷"
]),
([	"action":		"$N運氣于腕，一招「開山裂石」，手中$w向一根銅棍般直擊向$n",
	"force":		250,
	"dodge":		50,
	"damage":		50,
	"lvl" : 40,
	"skill_name" : "開山裂石",
	"damage_type":	"內傷"
]),
([	"action":		"$N單臂一揮，一招「玉帶圍腰」，手中$w直擊向$n腰肋",
	"force":		300,
	"dodge":		-20,
	"damage":		60,
	"lvl" : 50,
	"skill_name" : "玉帶圍腰",
	"damage_type":	"內傷"
]),
([	"action":		"$N高高躍起，一招「大漠孤煙」，手中$w筆直向$n當頭罩下",
	"force":		350,
	"dodge":		-50,
	"damage":		70,
	"lvl" : 60,
	"skill_name" : "大漠孤煙",
	"damage_type":	"內傷"
])
});

int valid_enable(string usage) { return (usage == "whip") || (usage == "parry"); }

int valid_learn(object me)
{
	object weapon;

	if( (int)me->query("max_neili") < 200 )
		return notify_fail("你的內力不足，沒有辦法練云龍鞭法, 多練些內力再來吧。\n");

	if ((int)me->query_skill("force", 1) < 40)
		return notify_fail("你的基本內功火候太淺。\n");

	if ( !objectp(weapon = me->query_temp("weapon"))
	|| ( string)weapon->query("skill_type") != "whip" )
		return notify_fail("你必須先找一條鞭子才能練鞭法。\n");

	return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
	level   = (int) me->query_skill("yunlong-bian",1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
}

int practice_skill(object me)
{
	object weapon;

	if (!objectp(weapon = me->query_temp("weapon"))
	|| (string)weapon->query("skill_type") != "whip")
		return notify_fail("你使用的武器不對。\n");
	if ((int)me->query("qi") < 50)
		return notify_fail("你的體力不夠練云龍鞭法。\n");
	me->receive_damage("qi", 5);
	return 1;
}

string perform_action_file(string action)
{
	return __DIR__"yunlong-bian/" + action;
}
