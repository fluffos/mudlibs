// sun-finger.c 一陽指 book: 一陽指訣

inherit SKILL;

mapping *action = ({
([	"action" : "$N一式「陽關三疊」，真氣自指間噴薄而出，連出三指，快如電
閃，點向$n的$l",
	"force" : 100,
	"dodge" : 25,
	"damage": 10,
	"lvl" : 0,
	"skill_name" : "陽關三疊",
	"damage_type" : "內傷"
]),
([	"action" : "$N身形一轉，一步跨到$n身后。$n急忙轉身，$N又如閃電般躍回，
一式「魯陽返日」，右手食指疾出，指向$n的$l",
	"force" : 120,
	"dodge" : 25,
	"damage": 20,
	"lvl" : 20,
	"skill_name" : "魯陽返日",
	"damage_type" : "內傷"
]),
([	"action" : "$N向左踏出一步，左手輕揚，右手食指挾著雄渾內力疾點$n的$l，
招式光明坦蕩，正是一式「陽春白雪」",
	"force" : 160,
	"dodge" : 20,
	"damage": 40,
	"lvl" : 40,
	"skill_name" : "陽春白雪",
	"damage_type" : "內傷"
]),
([	"action" : "$N身影飄起，一式「陽鉤揮天」，自上而下，左腳彎曲如鉤，  
向$n的右肩，$n側身相避，$N右手趁勢點向$n的$l",
	"force" : 190,
	"dodge" : 30,
	"damage": 50,
	"lvl" : 60,
	"skill_name" : "陽鉤揮天",
	"damage_type" : "內傷"
]),
([	"action" : "只見$N臉上忽地一紅，左手一收，一式「烈日驕陽」，右手食指
斜指，向$n的$l點去",
	"force" : 220,
	"dodge" : 30,
	"damage": 70,
	"lvl" : 70,
	"skill_name" : "烈日驕陽",
	"damage_type" : "內傷"
]),
([	"action" : "$N左掌斜削，突然間變掌為指，嗤的一聲響，一式「丹鳳朝陽」，
使出一陽指力，疾點$n的$l",
	"force" : 250,
	"dodge" : 20,
	"damage": 80,
	"lvl" : 90,
	"skill_name" : "丹鳳朝陽",
	"damage_type" : "內傷"
]),
([	"action" : "$N反身一躍，閃身到$n背后，左膝一屈，右手食指回身一式「陰
陽交錯」，射向$n的后腦",
	"force" : 280,                                                  
	"dodge" : 25,
	"damage": 100,
	"lvl" : 100,
	"skill_name" : "陰陽交錯",
	"damage_type" : "內傷"
]),
([  "action" : "$N一聲大吼, 一式「三陽開泰」，用盡全力向$n的$l點去",
	"force" : 300,
	"dodge" : 10,
	"damage": 120,
	"lvl" : 130,
	"skill_name" : "三陽開泰",
	"damage_type" : "內傷"
]),
});

int valid_enable(string usage) { return usage=="finger" || usage=="parry"; }

int valid_combine(string combo) { return combo=="haotian-zhang"; }

int valid_learn(object me)
{
	if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
		return notify_fail("練一陽指必須空手。\n");
	if (me->query("gender") != "男性")
		return notify_fail("你陽氣不足，無法學一陽指。\n");
	if ((int)me->query("max_neili") < 700)
		return notify_fail("你的內力太弱，無法練一陽指。\n");
	if ((int)me->query_skill("kurong-changong", 1) > 80 ||
	    (int)me->query_skill("xiantian-qigong", 1) > 80)
		return 1;
	else
		return notify_fail("你的本門內功火候不夠，無法學一陽指。\n");
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
	int i, level;
	level   = (int) me->query_skill("sun-finger", 1);
	for(i = sizeof(action); i > 0; i--)
		if(level > action[i-1]["lvl"])
			return action[NewRandom(i, 20, level/5)];
}

int practice_skill(object me)
{
	if ((int)me->query("qi") < 50)
		return notify_fail("你的體力太低了。\n");
	if ((int)me->query("neili") < 30)
		return notify_fail("你的內力不夠練一陽指!\n");
	me->receive_damage("qi", 30);
	me->add("neili", -10);
	return 1;
}

string perform_action_file(string action)
{
        return __DIR__"sun-finger/" + action;
}
