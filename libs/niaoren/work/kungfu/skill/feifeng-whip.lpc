// feifeng-whip.c 飛鳳鞭法

#include <ansi.h>

inherit SKILL;

mapping *action = ({
([	"action":  "$N向上躍起，一招「鳳凰展翅」，手中$w自下而上，擊向$n的臉頰",
	"force":  100,
	"dodge":  0,
	"damage":  20,
	"lvl" : 15,
	"skill_name" : "鳳凰展翅",
	"damage_type":	"刮傷"
]),
([	"action":  "$N一招「彩鳳棲梧」，手中$w直繞向$n的$l",
	"force":  110,
	"dodge":  -10,
	"damage":  30,
	"lvl" : 20,
	"skill_name" : "彩鳳棲梧",
	"damage_type":	"內傷"
]),
([  "action":  "$N一招「鸞鳳和鳴」，手中$w騰空一卷，一聲脆響入磬，猛地向
$n劈頭打下",
	"force":  120,
	"dodge":  -15,
	"damage":  40,
	"lvl" : 40,
	"skill_name" : "鸞鳳和鳴",
	"damage_type":	"劈傷"
]),
([  "action":  "$N踏上一步，沖著$n輕佻一笑，手中$w卻毫不停留，一招「游龍
戲鳳」，掃向$n的$l",
	"force":  130,
	"dodge":  -30,
	"damage":  60,
	"lvl" : 50,
	"skill_name" : "游龍戲鳳",
	"damage_type":	"刺傷"
]),
([  "action":  "$N躍在半空，一招「龍飛鳳舞」，手中$w如游龍洗空，長鳳戲羽，
亂雨傾盆般分點$n左右",
	"force":  150,
	"dodge":  -35,
	"damage":  70,
	"lvl" : 60,
	"skill_name" : "龍飛鳳舞",
	"damage_type":	"刺傷"
]),
([  "action":  "$N向前急進，手中$w圈轉如虹，一招「龍鳳呈祥」，罩向$n前胸",
    "force":  170,
	"dodge":  -40,
	"damage":  85,
	"lvl" : 75,
	"skill_name" : "龍鳳呈祥",
	"damage_type":	"內傷"
])
});

int valid_enable(string usage) { return (usage == "whip") || (usage == "parry"); }

int valid_learn(object me)
{
	object weapon;

	if ((int)me->query_skill("kurong-changong", 1) < 20)
		return notify_fail("你的枯榮禪功火候太淺。\n");
	if ( me->query("gender") != "女性")
		return notify_fail("大老爺們學這飛鳳鞭？天資不足吧。\n");
	if ( !objectp(weapon = me->query_temp("weapon"))
	|| ( string)weapon->query("skill_type") != "whip" )
		return notify_fail("你必須先找一條長鞭才能練鞭法。\n");

	return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
        level   = (int) me->query_skill("feifeng-whip",1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
}

int practice_skill(object me)
{
	object weapon;

	if (!objectp(weapon = me->query_temp("weapon"))
	|| (string)weapon->query("skill_type") != "whip")
		return notify_fail("你使用的武器不對。\n");
	if ((int)me->query("qi") < 50)
		return notify_fail("你的體力不夠練飛鳳鞭法。\n");
	me->receive_damage("qi", 10);
	return 1;
}


