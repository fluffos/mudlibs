//sougu.c 搜骨鷹爪功

inherit SKILL;

mapping *action = ({
([	"action" : "\n$N雙臂一攏，左爪直攻$n下盤，右爪卻挾風雷之勢推向$n腰間，一虛一實，正是一招「推爪式」",
	"force" : 170,
	"dodge" : 5,
	"damage_type" : "抓傷",
	"lvl" : 0,
	"damage" : 15,
	"skill_name" : "推爪式"
]),
([	"action" : "\n$N身形陡起，如雄鷹擊日般“倏”地從$n頭頂越過，使一招「蓋爪式」雙爪疾向$n腦后擊下",
	"force" : 190,
	"dodge" : 20,
	"damage_type" : "抓傷",
	"damage" : 25,
	"lvl" : 5,
	"skill_name" : "蓋爪式"
]),
([	"action" : "\n$N就地一滾，突然到了$n身前，使一招「掏爪式」，右爪直奔$n小腹",
	"force" : 215,
	"dodge" : 20,
	"damage_type" : "抓傷",
	"damage" : 35,
	"lvl" : 10,
	"skill_name" : "掏爪式"
]),
([	"action" : "\n$N左爪護胸，右爪凝勁后發，一招「托爪式」，緩緩托向$n的$l",
	"force" : 245,
	"dodge" : 5,
	"damage_type" : "抓傷",
	"damage" : 45 ,
	"lvl" : 20,
	"skill_name" : "托爪式"
]),
([	"action" : "\n$N身形詭異，疾速前扑，使一招「撩爪式」，直撩$n襠部",
	"force" : 280,
	"dodge" : 25,
	"damage_type" : "抓傷",
	"damage" : 55,
	"lvl" : 35,
	"skill_name" : "撩爪式"
]),
([	"action" : "\n$N身形飄忽，陡然轉到$n身后，一招「鎖爪式」，雙爪直向$n咽喉插下",
	"force" : 320,
	"dodge" : 35,
	"damage_type" : "抓傷",
	"damage" : 65,
	"lvl" : 60,
	"skill_name" : "鎖爪式"
]),
([	"action" : "\n$N一聲厲嘯，身形拔起，使出「千爪式」，由半空盤旋下擊，$n頓覺漫天爪影，罩向全身死穴",
	"force" : 365,
	"dodge" : 40,
	"damage_type" : "抓傷",
	"damage" : 95,
	"lvl" : 85,
	"skill_name" : "千爪式"
])
});

int valid_enable(string usage) { return usage=="claw" ||  usage=="parry"; }

int valid_learn(object me)
{
	if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
		return notify_fail("練搜骨鷹爪功必須空手。\n");
//	if ((int)me->query_skill("hunyuan-yiqi", 1) < 20)
//		return notify_fail("你的混元一氣功火候不夠，無法學般若掌。\n");
	if ((int)me->query("max_neili") < 100)
		return notify_fail("你的內力太弱，無法學搜骨鷹爪功。\n");
	if ((int)me->query("str") < 24)
		return notify_fail("你的臂力太差，無法學搜骨鷹爪功。\n");
	return 1;
}

string query_skill_name(int level)
{
	int i;
	for(i = sizeof(action)-1; i >= 0; i--)
		if(level >= action[i]["lvl"])
			return action[i]["skill_name"];
}

mapping query_action(object ob, object weapon)
{
	int i, level;
	level = (int)ob->query_skill("sougu", 1);
	for(i = sizeof(action); i > 0; i--)
		if(level > action[i-1]["lvl"])
			return action[NewRandom(i, 20, level/5)];
}

int practice_skill(object me)
{
	if ((int)me->query("qi") < 30)
		return notify_fail("你的體力太低了。\n");
	if ((int)me->query("neili") < 20)
		return notify_fail("你的內力不夠練鷹爪功。\n");
	me->receive_damage("qi", 25);
	me->add("neili", -5);
	return 1;
}

string perform_action_file(string action)
{
	return __DIR__"sougu/" + action;
}
