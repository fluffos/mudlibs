//
// mingwang-jian.c 冥王劍
// 
//

inherit SKILL;

mapping *action = ({
([      "action" : "$N怒目圓睜，使出一招「冥王」，手中$w一抖，直刺$n的$l  ",
        "skill_name" : "不動",
        "force" : 100,
        "dodge" : -10,
        "lvl" : 0,
        "damage" : 30,
        "damage_type" : "刺傷",
]),
([      "action" : "$N使出一式「起手出劍」，足下一滑，劍芒閃向$n的$l  ",
        "skill_name" : "起手出劍",
        "force" : 140,
        "dodge" : 5,
        "lvl" : 8,
        "damage" : 30,
        "damage_type" : "刺傷",
]),
([      "action" : "$N使出一招「山崩」，縱身躍入半空，$w如山崩般劈向$n的$l  ",
        "skill_name" : "山崩",
        "force" : 180,
        "dodge" : -10,
        "lvl" : 16,
        "damage" : 40,
        "damage_type" : "割傷",
]),
([      "action" : "$N使出「千旋轉」，將$w舞得如紡車一般，忽然一劍刺向$n的$l  ",
        "skill_name" : "千旋轉",
        "force" : 220,
        "dodge" : 10,
        "lvl" : 24,
        "damage" : 48,
        "damage_type" : "刺傷",
]),
([      "action" : "$N使出一招「降魔舞」，全身勁氣貫于$w中，劍氣直指$n的$l  ",
        "skill_name" : "降魔舞",
        "force" : 250,
        "dodge" : 10,
        "lvl" : 32,
        "damage" : 54,
        "damage_type" : "刺傷",
]),
([      "action" : "$N單足獨立，使出一式「鶴舞」，一道劍光如白影般向$n的$l掃去  ",
        "skill_name" : "鶴舞",
        "force" : 280,
        "dodge" : 15,
        "lvl" : 40,
        "damage" : 60,
        "damage_type" : "割傷",
]),
([      "action" : "$N使出一招「龍騰」，手中$w如游龍般扑向$n的$l  ",
        "skill_name" : "龍騰",
        "force" : 300,
        "dodge" : 20,
        "lvl" : 48,
        "damage" : 66,
        "damage_type" : "割傷",
]),
([      "action" : "$N飛身一躍，居高臨下使出「花雨滿天」，頓時劍尖如雨刺向$n的$l  ",
        "skill_name" : "花雨漫天",
        "force" : 320,
        "dodge" : 25,
        "lvl" : 80,
        "damage" : 80,
        "damage_type" : "刺傷",
]),
});


int valid_enable(string usage) { return usage == "sword" || usage == "parry"; }



int valid_learn(object me)
{
	if ((int)me->query("max_neili") < 100)
		return notify_fail("你的內力不夠。\n");
         if ((int)me->query_skill("force", 1) <= 30 )
		return notify_fail("你的基本內功火候不夠。\n");
return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
	level   = (int) me->query_skill("mingwang-jian",1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
}

int practice_skill(object me)
{
	object weapon;

	if (!objectp(weapon = me->query_temp("weapon"))
	|| (string)weapon->query("skill_type") != "sword")
		return notify_fail("你使用的武器不對。\n");
	if ((int)me->query("qi") < 50)
		return notify_fail("你的體力不夠，練不了冥王劍。\n");
	me->receive_damage("qi", 20);
	return 1;
}
