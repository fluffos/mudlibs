俠客行一百常用指令詳細解說如下：


    本游戲的玩家可以使用五大類指令：系統指令、聊天指令、
冒險指令、武功指令以及特殊指令。基本指令是ＭＵＤ一般都有
的指令，故用英文表示。括號內表示可用的命令縮寫。


【系統指令】 

    help
    指令格式：help <主題>。
    例如：help newbie或 help intro﹔也可以看地圖或者門派
說明，例如：help map或者 help map_all。 這個指令提供你針
對某一主題的詳細說明文件，若是不指定主題，則提供你有關主
題的文件。

    alias
    指令格式：alias <欲設定之指令> <系統提供之指令>。
    有時系統所提供之指令需要輸入很長的字串, 在使用時 (尤
其是經常用到的) 或許會感覺不方便, 此時你即可用此一指令設
定并替代原有之指令。例如“alias sc score”就會以 sc 來取
代score 指令。“alias” 后不加參數則列出你所有的替代指令。
“alias sc”會消除 sc 這個替代指令。其中可以用$1、$2、$3
等來取代第一、第二、第三個參數，或是 $* 取代所有的參數，
例如：“alias xunwen ask $1 about $2”，則每當你打“xunwen
waiter name” 就會取代成：“ask waiter about name”。

    cls
    指令格式：cls。
    清除屏幕信息。

    describe
    指令格式：describe <欲設定之對自己的描述句子|none>。
    這個指令讓你設定當別人用 look 指令看你時，對你的描述。
none參數則取消以前的設定。

    exit(quit)
    指令格式：quit。
    當你想暫時離開時, 可利用此一指令。

    passwd
    指令格式：passwd。
    這個指令可以修改你的人物密碼。當然要求先輸入舊口令了。

    save
    指令格式：save。
    把你辛苦奮斗的結果存起來。

    who
    指令格式：who [-l|-p|-w|<id>]。
    這個指令可以列出所有在線上的玩家及其等級。-l選項列出
較長的按門派輩分排列的訊息。-i只列出玩家的本門弟子。-w只
列出線上所有的巫師。<id>則列出指定玩家的同門弟子。

    finger
    指令格式：finger [使用者姓名]。
    這個指令, 如果沒有指定使用者姓名, 會顯示出所有正在線
上玩家的連線資料。反之, 則可顯示有關某個玩家的連線, 權限
等資料。

    id
    指令格式：id [here]。
    這個指令可以讓你知道物品的英文名稱及名字。只打id會顯
示你身上所攜帶物品的名稱。id here 則顯示所有跟你在同一個
環境里的物件名稱。

    inventory(i)
    指令格式：inventory。
    可列出你目前身上所攜帶的所有物品。

    nick
    指令格式：nick <外號, 綽號> | none。
    這個指令可以讓你為自己取一個響亮的名號或頭銜，你如果
希望在綽號中使用 ANSI 的控制字元改變顏色，可以用以下的控
制字串：

          $BLK$：黑色
          $HIW$：白色
          $RED$：紅色
          $HIY$：黃色
          $HIB$：藍色
          $GRN$：綠色
          $BLU$：深藍色
          $YEL$：土黃色
          $MAG$：淺紫色
          $CYN$：藍綠色
          $WHT$：淺灰色
          $HIC$：天青色
          $HIM$：粉紅色
          $HIG$：亮綠色
          $HIR$：亮紅色
          $NOR$：恢復正常顏色
    其中系統自動會在字串尾端加一個 $NOR$。
    nick none 取消綽號。

    hp
    指令格式：hp。
    這個指令可以顯示你的精、氣、內力、精力、食物、飲水以
及經驗等數值。

    score
    指令格式：score。
    這個指令可以詳細顯示你的精、氣、內力、精力、食物、飲
水以及經驗、正氣(戾氣)數值。

    title
    指令格式：title。
    顯示你目前的階級和頭銜。
       
    set
    指令格式：set <變數名> [<變數值>]。
    這個指令讓你設定一些環境變數，不加參數時會顯示你目前
設定的環境變數，不指定變數值，則內定值為“YES”。 取消變
數設定請用 unset指令，這些變量會隨著您的人物被儲存下來。
至于有哪些環境變數可以設定，請見 help settings。
    環境變量設置包括：
    brief <任意非零值>：設定移動時只看所在地簡短的名稱，
如果您覺得網絡速度太慢，或者是對區域已經十分熟悉，您可以
考慮采用 brief模式以減輕網絡負擔。
    combatd   <任意非零值>：設定打斗時不顯示打斗信息。
    no_accept <任意非零值>：設定不接受別人 give 的禮物。
    no_tell   <任意非零值>：設定不接收任何人tell你的信息。
    no_teach  <任意非零值>：設定您現在不教您的徒弟技能， 
              由于教徒要是要費精神，當徒弟不乖時……。 
    wimpy     [<逃跑時“氣”的百分比>]：當您的精或氣低于
              這個百分比時就會自動找機會逃命。

    unset
    指令格式：unset <變數名>。
    這個指令讓你刪除環境變數的設定。

    wimpy
    指令格式：wimpy [<逃跑時“氣”的百分比>]。
    當您的精或氣低于這個百分比時就會自動找機會逃命。

    suicide
    指令格式：suicide [-f]。
    如果因為某種原因你不想活了，你可以選擇自殺。自殺分兩
種：suicide為重新投胎，suicide -f 為永遠的除去玩家資料，
系統會要求你輸入密碼以確認身份。

    time
    指令格式：time。
    這個指令讓你知道現在現實生活中的北京時間。

    time
    指令格式：time。
    這個指令讓你知道現在在游戲的時辰。

    uptime
    指令格式：uptime。
    這個指令告訴您《俠客行一百》已經連續執行了多久。

    version
    指令格式：version。這個指令會顯示游戲目前所用的MudOS 
driver版本。

    wizlist
    指令格式：wizlist。列出目前所有的巫師名單。


