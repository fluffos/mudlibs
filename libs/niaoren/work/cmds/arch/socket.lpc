// socket.c

inherit F_CLEAN_UP;

int help();

int main(object me, string arg)
{
        write(dump_socket_status());
        return 1;
}

int help()
{
        write("你可以用這個指令顯示當前 socket 程序會話情況。\n");
        return 1;
}
