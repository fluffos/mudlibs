inherit ROOM;

void create()
{
        set("short", "三無宮");
        set("long",@LONG
這里可以買香到后面參拜西方大士。几個進香客正圍著一個老年僧
人討價還價。
LONG );
        set("exits", ([
                "southeast" : __DIR__"doushuai",
		"north" : __DIR__"cibeiyuan",
	]));
	set("objects",([
                "/d/wudang/npc/guest" : 4,
		__DIR__"npc/laoniansengren" : 1,
        ]));
//        set("no_clean_up", 0);
        setup();
}
