// Room: /d/henshan/jigaoming.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "極高明台");
        set("long", @LONG
這里是福嚴寺最高處。唐鄴侯李泌手書 "極高明" 三個大字于此，
意即佛相極高 (高無見頂相) ，佛光極明 (明不借他光) ，佛是至高無
上的，故曰極高明台。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "northdown"  : __DIR__"sansheng",
        ]));
        set("outdoors", "henshan");
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

