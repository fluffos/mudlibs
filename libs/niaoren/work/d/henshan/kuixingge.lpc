// Room: /d/henshan/kuixingge.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "奎星閣");
        set("long", @LONG
奎星閣是南岳大廟的第二進。閣為一個大舞台，閣左為鼓亭，右是
鐘亭。鄉俗年節唱戲，這里總是熱鬧非凡。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "south"  : __DIR__"lingxingmen",
           "east"   : __DIR__"zhongting",
           "north"  : __DIR__"zhengchuan",
        ]));
        set("outdoors", "henshan");
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

