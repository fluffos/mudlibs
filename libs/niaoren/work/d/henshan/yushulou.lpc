// Room: /d/henshan/yushulou.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "御書樓");
        set("long", @LONG
御書樓為南岳廟歷代藏書之處，天子御賜，賢達樂捐，這里早已洋
洋大觀。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "south"  : __DIR__"jiayingmen",
           "north"  : __DIR__"dadian",
        ]));
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

