// Room: /d/henshan/zhengchuan.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "正川門");
        set("long", @LONG
正川門是南岳大廟的第三進。院內是玄德宗殿，關聖殿，觀音閣等
樓宇。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "south"  : __DIR__"kuixingge",
           "north"  : __DIR__"yubeiting",
        ]));
        set("outdoors", "henshan");
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

