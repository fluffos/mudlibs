// Room: /d/henshan/banshan.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "半山亭");
        set("long", @LONG
終于到了半山亭。這里離到祝融峰無論高程還是行程，均恰好是一
半之徑，路邊小亭顯然已極古久，但仍為過往客旅遮風避雨。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "southdown"  : __DIR__"shanlu4",
           "northup"    : __DIR__"shanlu5",
        ]));
        set("no_clean_up", 0);
        set("outdoors", "henshan");
        setup();
        replace_program(ROOM);
}

