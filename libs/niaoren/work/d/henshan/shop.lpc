//Room: /d/henshan/shop.c

inherit ROOM;

void create()
{
	set("short", "小店");
	set("long", @LONG
這里是衡陽縣城的一間小店，賣些簡單的飲食，專門賺那些游客
的錢。
LONG );
	set("exits", ([
		"north" : __DIR__"hengyang1",
		"up"    : __DIR__"huiyan",
	]));
	set("objects", ([
	      __DIR__"npc/xiaoer2" : 1,
	]));
//        set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}

