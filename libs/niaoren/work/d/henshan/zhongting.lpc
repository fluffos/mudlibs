// Room: /d/henshan/zhongting.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "鐘亭");
        set("long", @LONG
鐘亭內設鐵鐘一口，重九千斤。每逢山洪暴發，鳴鐘鎮洪，亦以示
警，聲聞二十里外。 
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "west"   : __DIR__"kuixingge",
        ]));
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

