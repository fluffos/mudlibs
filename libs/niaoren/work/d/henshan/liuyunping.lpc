// Room: /d/henshan/liuyunping.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "流云坪");
        set("long", @LONG
每逢雨霽，在后山深壑中升騰起來的云山霧海，涌過南天門山脊，
向前山傾瀉，直如銀河飛瀉，蔚為壯觀，這就是南天門的流云奇景。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "westdown"  : __DIR__"nantian",
        ]));
        set("outdoors", "henshan");
        set("no_clean_up", 0);
        setup();
}

