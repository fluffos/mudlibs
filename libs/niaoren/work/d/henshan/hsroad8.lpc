// Room: /d/henshan/hsroad8.c 林間道
// Modified by Java on Feb.14.1998
inherit ROOM;

void create()
{
	set("short", "林間道");
	set("long", @LONG
這里是一條林間小道，在樹叢中蜿蜒。西北方連著一條黃土路，南
面的道路要寬一些。
LONG );
        set("outdoors", "henshan");

	set("exits", ([
		"northwest" :"/d/wudang/wdroad9",
		"south"     : __DIR__"hsroad7",
	]));

        set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}

