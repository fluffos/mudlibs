// Room: /d/henshan/jiayingmen.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "嘉應門");
        set("long", @LONG
這里是地方官迎接京師御史官來祭南岳的地方，是南岳大廟第五進
門了。門上雕塑也較精細些，懸彩燈籠時時換新。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "south"  : __DIR__"yubeiting",
           "north"  : __DIR__"yushulou",
        ]));
        set("outdoors", "henshan");
        set("no_clean_up", 0);
        setup();
}

