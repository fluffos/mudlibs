// Room: /d/henshan/tianzhu.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "天柱峰");
        set("long", @LONG
你爬上衡山五峰之一的天柱峰，四望群山縹緲云霧中，每每天陰地
濕時候，這里就是天柱云氣的所在了。 
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "westdown"   : __DIR__"shanlu10",
           "eastdown"   : __DIR__"shanlu7",
        ]));
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

