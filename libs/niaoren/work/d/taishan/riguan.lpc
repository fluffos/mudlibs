// Room: /d/taishan/.c
// Date: CSWORD 96/03/25

inherit ROOM;

void create()
{
	set("short", "日觀峰");
	set("long", @LONG
這里是位于南天門西面的山嶺，怪石攢簇，最高的地方一石
卓立，名為君子峰。往北走便是泰山的西天門。
LONG );
	set("exits", ([
		"westup" : __DIR__"yuhuang",
		"eastup" : __DIR__"tanhai",
	]));
	set("objects", ([
		__DIR__"npc/tianmen" : 1,
	]));
//	set("outdoors", "taishan");
	set("no_clean_up", 0);
	setup();
	"/clone/board/taishan_b"->foo();
}

