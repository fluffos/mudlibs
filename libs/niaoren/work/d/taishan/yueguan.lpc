// Room: /d/taishan/.c
// Date: CSWORD 96/03/25

inherit ROOM;

void create()
{
	set("short", "月觀峰");
	set("long", @LONG
這里是位于南天門西面的山嶺，怪石攢簇，最高的地方一石卓立，
名為君子峰。往北走便是泰山的西天門。
LONG );
	set("exits", ([
		"east" : __DIR__"nantian",
		"northup" : __DIR__"xitian",
	]));
	set("no_clean_up", 0);
	set("outdoors", "taishan");
	setup();
	replace_program(ROOM);
}

