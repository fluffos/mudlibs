// Room: /d/taishan/.c
// Date: CSWORD 96/03/25

inherit ROOM;

void create()
{
	set("short", "丈人峰");
	set("long", @LONG
這里位于玉皇頂以西里許的地方，形狀有如一個傴僂的老人。
靠西有黃華洞，因洞路極為危險，后人在進路處寫上「回車岩」。
從這里往北走便是岱頂的北天門。
LONG );
	set("exits", ([
		"east" : __DIR__"yuhuang",
		"northup" : __DIR__"beitian",
	]));
	set("objects",([
		__DIR__"npc/wei-shi1" : 2,
	]));
//	set("no_clean_up", 0);
	set("outdoors", "taishan");
	setup();
}

