// Room: /d/taishan/.c
// Date: CSWORD 96/03/25

inherit ROOM;

void create()
{
	set("short", "寶藏嶺");
	set("long", @LONG
這里是泰山的一個名勝。嶺上有一座狀似獅子的巨石，稱為獅
子崖。最出名的還是要數在東北面的東岳廟，也稱上岳廟，是古代
封禪的地方，那里有一塊唐玄宗的紀泰山銘。
LONG );
	set("exits", ([
		"westup" : __DIR__"bixia",
	]));
	set("outdoors", "taishan");
	set("no_clean_up", 0);
	setup();
}

