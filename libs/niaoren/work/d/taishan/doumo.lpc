// Room: /d/taishan/.c
// Date: CSWORD 96/03/25

inherit ROOM;

void create()
{
	set("short", "斗母宮");
	set("long", @LONG
上了一段山路，游人一般都會在這里稍為休息。這里的地勢已
經比泰山鄰近的徂徠山為高，從這里往東北走便是石經峪。
LONG );
	set("exits", ([
		"eastup" : __DIR__"shijin",
		"southdown" : __DIR__"yitian",
	]));
	set("objects",([
		__DIR__"npc/jian-ke" : 1,
	]));
//	set("no_clean_up", 0);
	set("outdoors", "taishan");
	setup();
}

