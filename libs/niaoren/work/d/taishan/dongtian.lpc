// Room: /d/taishan/.c
// Date: CSWORD 96/03/25

inherit ROOM;

void create()
{
	set("short", "東天門");
	set("long", @LONG
這里是岱頂的東面出口。往東依次經過伏虎門峽口、鷹愁澗等，
便可下山。
LONG );
	set("exits", ([
		"west" : __DIR__"tanhai",
	]));
	set("no_clean_up", 0);
	set("outdoors", "taishan");
	setup();
}

