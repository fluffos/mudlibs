// Room: /d/taishan/.c
// Date: CSWORD 96/03/25

inherit ROOM;

void create()
{
	set("short", "西天門");
	set("long", @LONG
這里有兩塊巨石壁立，有如給人斬削過似的，正是岱頂的西面出口。
LONG );
	set("exits", ([
		"southdown" : __DIR__"yueguan",
	]));
	set("no_clean_up", 0);
	set("outdoors", "taishan");
	setup();
}

