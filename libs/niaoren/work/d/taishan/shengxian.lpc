// Room: /d/taishan/.c
// Date: CSWORD 96/03/25

inherit ROOM;

void create()
{
	set("short", "升仙坊");
	set("long", @LONG
這里是十八盤的中間，從龍門至此的一段稱為緩十八，從這里
直上南天門的一段稱為緊十八。由此刻攀石而上，后面的人可以看
到前面的人的鞋底，前面的人回頭則可見到后面的人的發頂。在這
里一般人每走十來步便要稍一休息。
LONG );
	set("exits", ([
		"northup" : __DIR__"nantian",
		"southdown" : __DIR__"longmen",
	]));
	set("objects", ([
		__DIR__"npc/jianchu" : 1,
	]));
//	set("no_clean_up", 0);
	set("outdoors", "taishan");
	setup();
	replace_program(ROOM);
}

