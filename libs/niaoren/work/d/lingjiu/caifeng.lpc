//ROOM: caifeng.c

inherit ROOM;

void create()
{
	set("short", "瀟湘館");
        set("long",@LONG
這是『靈鷲宮』的裁縫室。几個女子圍坐在一塊，飛針走線，苦練女
紅，難道她們是為了以后找一個好婆家嗎？ 
LONG  );
        set("exits", ([
		"south" : __DIR__"changl10",
        ]));
        set("objects",([
                CLASS_D("lingjiu")+"/tbyang" : 1,
       	]));
//	set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}
