inherit ROOM;

void create()
{
    set("short", "小屋");
    set("long", @LONG
這是專供仆人休息的小屋。屋里放著五六張床，擠擠地占掉了屋子的一
大半地方。屋角很散亂地放了一些日常生活用具。中央放著一張桌子，上面
擺一茶壺和几個杯子。
LONG );
    set("exits", ([
        "east"  : __DIR__"houyuan",
    ]));
    set("objects", ([
        __DIR__"npc/puren" : 3,
    ]) );    

    set_light(0) ;
//    set("no_clean_up", 0);
    set("sleep_room","1") ;
    setup();
    replace_program(ROOM);
}
