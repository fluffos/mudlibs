inherit ROOM;
 
void create()
{
        set("short", "小路");
        set("long",@LONG
這是一條通往后山的小徑。小路兩旁長滿了雜草。由于人們很少走這條路，
甚至連路中央也稀稀疏疏冒出了草芽。東邊有一個小山洞。
LONG);
        set("exits", ([
            "east"   : __DIR__"road5",
            "north"  : __DIR__"road2",
        ]));
        set("objects", ([
            __DIR__"npc/dushe": 4,
	]) );
        set("outdoors", "taohua");
//        set("no_clean_up", 0);
        setup();
}
