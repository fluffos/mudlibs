// Room: chufang.c
inherit ROOM;

void create()
{
	set("short", "廚房");
	set("long", @LONG
這是桃花島的廚房，中央擺著大圓桌，周圍則放著几張竹椅。窗口
還擺著几盆水仙花。你吸了一下鼻子，啊．．．好香！也不知到底是花
香還是飯香。平時桃花島的弟子和仆人都在這里用餐。
LONG );
	set("exits",([
	    "east" :__DIR__"qianyuan",
	]));

	set("objects", ([
	    __DIR__"npc/yan" : 1,
	    __DIR__"npc/yapu" : 1,
	]) );
//	set("no_clean_up", 0);
	setup();
}
//void reset ()
//{
//	::reset();
//	this_player()->delete_temp("taohua/茶飯") ;
//}

