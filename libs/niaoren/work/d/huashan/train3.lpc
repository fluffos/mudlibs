// Room: train1 華山操練房
// Date:  2000.04.19 by leos
#include <room.h>

inherit ROOM;

void create()
{
      set("short", "華山操練房");
      set("long", @LONG
這里擺滿了大刀長劍、沙袋綁腿等，是華山弟子日常練功的地方。
LONG );

      set("exits", ([ /* sizeof() == 1 */
            "south"   : __DIR__"train2",
      ]));
//      set("no_clean_up", 0);
      set("objects", ([
              "/d/shaolin/npc/mu-ren" : 10,  //五個木人
      ]));
      setup();
      replace_program(ROOM);
}
