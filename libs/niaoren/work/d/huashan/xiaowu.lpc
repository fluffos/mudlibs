// Room: /d/huashan/xiaowu.c

#include <room.h>
inherit ROOM;

void create()
{
        set("short", "林間小屋");
        set("long", @LONG
這里就是華山劍宗的的根本之地。二十五年來，劍宗弟子臥薪嘗膽，
只是為了奪回華山門派掌門，為此，付出了極大犧牲。
    屋里陳設極為簡陋。這么多年來一直沒有長久隱匿的念頭使得劍宗弟
子能忍辱負重，只待重開生天。
LONG );
        set("exits", ([ /* sizeof() == 4 */
             "west": __DIR__"pingdi",
        ]));
        set("objects", ([
             CLASS_D("huashan") + "/feng-buping": 1,
        ]));
        set("valid_startroom","1");
//        set("no_clean_up", 0);

        setup();
        "/clone/board/jianzong_b"->foo();
        replace_program(ROOM);
}

