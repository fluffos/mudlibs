// pingxinshi.c

inherit ROOM;

void create()
{
        set("short", "平心石");
        set("long", @LONG
百尺峽盡頭，兩邊岩崖勢欲相合，中夾兩塊巨石，前邊一塊刻有“驚
心石”三字。過此回望，石上刻有“平心石”三字，示人們以險境已經過
去，已進入平安之途了。頓時心情至此一舒。
LONG );
        set("exits", ([ /* sizeof() == 2 */
            "westdown" : __DIR__"baichi",
            "eastup"   : __DIR__"laojun",
        ]));
        set("no_clean_up", 0);
        set("outdoors", "xx" );

        setup();
}
