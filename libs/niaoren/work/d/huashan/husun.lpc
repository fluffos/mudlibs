// husun.c

inherit ROOM;

void create()
{
        set("short", "猢猻愁");
	set("long", @LONG
前面又是一處十分陡峭的山嶺，上面有四只鐵猿，盤踞台畔，據說以
前從華山水帘洞出來的猿猴，每到這里就要返回去，連它們也感到難于超
越，此嶺因而得名。
LONG );
        set("exits", ([ /* sizeof() == 2 */
            "northdown" : __DIR__"laojun",
            "southup"   : __DIR__"shangtianti",
        ]));
        set("objects", ([ /* sizeof() == 2 */
            "/d/wudang/npc/monkey": 1,
            "/d/wudang/npc/little_monkey": 2,
        ]));
        set("no_clean_up", 0);
        set("outdoors", "xx" );

        setup();
}
 
