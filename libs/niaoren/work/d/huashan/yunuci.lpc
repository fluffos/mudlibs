// yunuci.c

inherit ROOM;

void create()
{
        set("short", "玉女祠");
	set("long", @LONG
玉女祠里供奉的到底是哪位玉女，有許多不同的說法，現在所能看到
的只一尊呆板的石像。祠中大石上有一處深陷，凹處積水清碧，這是玉女
的洗臉盆，碧水終年不干。
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
            "east" : __DIR__"yunu",
        ]));
        set("resource/water", 1);
        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}
 
