// Room: /huanghe/huanghe1.c
// Java. Sep 21 1998

#include <room.h>

inherit ROOM;
void create()
{
	set("short", "黃河岸邊");
	set("long", @LONG
這里是黃河岸邊。黃河流淌到這里，已經泥沙渾濁了。黃色的河水
在河床里翻滾著，咆哮著，卷起一個個巨大的漩窩。西邊有個寨子。
LONG );
	set("exits", ([
		"south" : __DIR__"tiandi4",
		"north" : __DIR__"weifen",
		"east"  : __DIR__"huanghe2",
		"west"  : __DIR__"huanghegate",
	]));
	set("objects", ([
		__DIR__"npc/hou" : 1,
	]));
//	set("no_clean_up", 0);
	set("outdoors", "huanghe");
	setup();
}

