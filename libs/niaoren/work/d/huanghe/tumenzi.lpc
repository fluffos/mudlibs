// Room: /huanghe/tumenzi.c
// Java Sep. 22 1998

#include <room.h>

inherit ROOM;

void create()
{
	set("short", "土門子");
	set("long", @LONG
土門子也是以前駐軍的地方，周圍全是沙漠，中間一塊小小的綠洲，
本來這里有當地的牧民居住，可是前不就來了一伙馬賊占據了這里以這
里為大本營四出劫掠，無惡不做。
LONG );
	set("exits", ([
		"south"     : __DIR__"wuwei",
		"northeast" : __DIR__"shixiazi",
	]));
	set("no_clean_up", 0);
	set("outdoors", "huanghe");
	setup();
}

