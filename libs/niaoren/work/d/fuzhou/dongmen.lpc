// Room: /d/fuzhou/dongmen.c
// Date: May 12, 98  Java

inherit ROOM;

void create()
{
	set("short", "福州東門");
	set("long", @LONG
東門外是福州平原。從這里看去，可以見到密密的叢林。
出東門十里地就是鼓山了。
LONG );
	set("exits", ([
                "east" : __DIR__"shulin",
                "west" : __DIR__"dongxiaojie",
	]));
	set("outdoors", "quanzhou");
	set("no_clean_up", 0);
	setup();
}

