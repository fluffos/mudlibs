// Room: /d/fuzhou/kedian.c
// Date: May 12, 98  Java

inherit ROOM;

void create()
{
	set("short", "客店");
	set("long", @LONG
山野客店十分簡陋。一個小二臨台買賣，一片破門扇扑在地
上，顯得相當忙亂。柜台上貼著個牌子(paizi)。
LONG );
	set("exits", ([
		"east" : __DIR__"erbapu",
	]));
	set("item_desc", ([
		"paizi" : "即日起只供應吃喝，不留宿。\n",
	]));
	set("objects", ([
		__DIR__"npc/xiaoer2" : 1,
	]));
//	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}

