// Room: /d/fuzhou/fzroad5.c
// Date: May 12, 98  Java

inherit ROOM;

void create()
{
	set("short", "閩贛古道");
	set("long", @LONG
福建境內數不盡的山坎相連。密林幽谷相繼，終日不見天光。
山中毒虫猛獸出沒無常，行路艱辛。路邊有一年輕漢子倚在樹上
冷眼看著你。
LONG );
	set("exits", ([
		"westup"    : __DIR__"fzroad6",
		"northeast" : __DIR__"fzroad4",
	]));
        set("objects", ([
		"/d/qingcheng/npc/fang" : 1,
	]));
	set("outdoors", "quanzhou");
	setup();
	replace_program(ROOM);
}

