// Room: /city/biaojudy.c
// YZC 1995/12/04 

inherit ROOM;

void create()
{
	set("short", "福威鏢局大院");
	set("long", @LONG
大院里顯得有些凌亂，趟子手和鏢師們進進出出，似乎
都在滿地里忙碌著什么。一棵很大的榕樹栽在院子中央，給
火辣辣的福州帶來一點涼爽。
LONG );
	set("exits", ([
		"out"   : __DIR__"biaoju",
		"north" : __DIR__"biaojuzt",
	]));
	set("objects", ([
		__DIR__"npc/shi" : 1,
	]));
	set("outdoors", "quanzhou");
//	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}

