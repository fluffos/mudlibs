// Room: /d/fuzhou/fzroad7.c
// Date: May 12, 98  Java

inherit ROOM;

void create()
{
	set("short", "閩贛古道");
	set("long", @LONG
進入江西，還是不盡的山坎和密林幽谷，終日不見天光。放眼
山中毒虫猛獸出沒無常，行路艱辛。一到這里就聞到一股腥風扑鼻
而來。
LONG );
	set("exits", ([
		"northwest" : "/d/henshan/hsroad2",
		"eastup"    : __DIR__"fzroad6",
	]));
        set("objects", ([
		__DIR__"npc/huananhu" : 1,
	]));
	set("outdoors", "quanzhou");
	setup();
	replace_program(ROOM);
}

