// Room: /d/fuzhou/fzroad6.c
// Date: May 12, 98  Java

inherit ROOM;

void create()
{
	set("short", "閩贛古道");
	set("long", @LONG
福建境內數不盡的山坎相連。密林幽谷相繼，終日不見天光。
山中毒虫猛獸出沒無常，行路艱辛。一個粗豪漢子站在當道。
LONG );
	set("exits", ([
		"westdown" : __DIR__"fzroad7",
		"eastdown" : __DIR__"fzroad5",
	]));
        set("objects", ([
		"/d/qingcheng/npc/yu" : 1,
	]));
	set("outdoors", "quanzhou");
	setup();
	replace_program(ROOM);
}

