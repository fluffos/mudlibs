// Room: /d/fuzhou/fzroad10.c
// Date: May 12, 98  Java

inherit ROOM;

void create()
{
	set("short", "酒肆");
	set("long", @LONG
酒店門口一個酒招子半拉子挂著，店主人老蔡卻不見了。兩
間茅屋的小店顯得十分空落，只有一個青袍年輕憨子靠在酒爐上
冷冷地瞧著你。
LONG );
	set("exits", ([
		"south"     : __DIR__"fzroad9",
	]));
        set("objects", ([
		"/d/qingcheng/npc/yuyan" : 1,
	]));
	set("outdoors", "quanzhou");
	setup();
}

