// Room: /d/fuzhou/fzroad11.c
// Date: May 12, 98  Java

inherit ROOM;

void create()
{
	set("short", "閩中大道");
	set("long", @LONG
這里密邇城郊，來往行人也漸漸都了起來。福州的官話聽起
來刺耳難懂，可是抑揚頓挫，別有一番風味。
LONG );
	set("exits", ([
		"east"   : __DIR__"ximen",
		"west"   : __DIR__"fzroad9",
	]));
        set("no_clean_up", 0);
	set("outdoors", "quanzhou");
	setup();
}

