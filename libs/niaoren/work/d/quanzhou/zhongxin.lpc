// Room: /d/quanzhou/zhongxin.c
// Date: May 7, 96   Jay

inherit ROOM;

void create()
{
	set("short", "城中心");
	set("long", @LONG
泉州是一個安靜的港城。雖然這里是城中心，人們也只是腳步輕輕地
過往，并不喧嘩。東面是一個港口，從那里可以出海。西面有一條小巷。
LONG );
	set("exits", ([
                "north" : __DIR__"beimen",
                "south" : __DIR__"nanmen",
                "east"  : __DIR__"haigang",
                "west"  : __DIR__"ximen",
	]));
        set("objects",([
		__DIR__"npc/youxun" : 1,
        ]));
//	set("no_clean_up", 0);
	set("outdoors", "quanzhou");
	setup();
}

