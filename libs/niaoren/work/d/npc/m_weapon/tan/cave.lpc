//cave.c

inherit ROOM;

void create()
{
	set("short", "湖心小洞");
	set("long",
	     "這里是潭中的一個小洞，里面零零落落的有几塊石頭。\n"
	);
	set("objects" , ([
             __DIR__"item/stone" : 1,
             ]) );
	setup();
}
