//cedong.c

inherit ROOM;

void create()
{
	set("short", "側洞");
	set("long",
	     "這里是一個小小的側洞，里面有張梳妝台，上邊擺放著一些女人的用品。\n"
	);
	set("exits", ([
		"east" : __DIR__"dongnei",
	]));
	set("objects",([
		__DIR__"npc/jinhua" : 1,
	]));
//        set("no_clean_up", 0);

	setup();
}
