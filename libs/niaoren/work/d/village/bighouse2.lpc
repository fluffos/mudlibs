// Room: /d/village/bighouse2.c

inherit ROOM;

void create()
{
        set("short", "內室");
	set("long", @LONG
這里是一間寢室，屋子中間有一張大床，床上的被褥居然是綢緞的，
看來這個土財主對自己并不是十分刻薄。屋里擺著几只木箱子，牆角里有
一只大柜子(cabinet)。
LONG );
        set("exits", ([ /* sizeof() == 1 */
            "west" :"/d/village/bighouse1",
        ]));
        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}
 
