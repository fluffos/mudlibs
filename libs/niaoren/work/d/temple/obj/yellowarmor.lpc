// 黃之鎧 /u/belldandy/12god/yellowarmor.c

#include <armor.h>
#include <ansi.h>

inherit ARMOR;

void create()
{
        set_name( HIY"黃之鎧"NOR, ({ "yellow armor", "armor" }) );
        set("long", "這是一件馬神專用鎧甲。\n");
        set_weight(2000);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("unit", "件");
                set("material", "leather");
                set("wear_msg", "$N從背包拿出一件$n穿在身上。\n");
                set("value", 700000);
//              set("no_drop", "如此神器再世難求啊。\n");
                set("no_get","這是神的專屬裝備！\n");
                set("armor_prop/armor", 220);
                set("armor_prop/strength", 3);
        }
        setup();
}

void owner_is_killed()
{
       destruct(this_object());
}

/*int query_autoload()
{
       return 1;
}*/
