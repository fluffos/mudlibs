// Room: /d/hengshan/kutianjing.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "苦甜井");
        set("long", @LONG
北岳廟的右下角，有二井相距不過三尺，而水質一苦一甜，稱
為苦甜井。甜井中水味清洌甘甜，道家弟子奉為聖水，游人到此，
每感口枯舌燥，飲此水確有消暑清熱之益。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "west"  : __DIR__"beiyuemiao",
        ]));
        set("resource/water", 1);
        set("outdoors", "hengshan");
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

