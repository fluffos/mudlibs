// Room: /d/hengshan/sanjiaodian.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "三教殿");
        set("long", @LONG
這里是全寺最高處的三教殿，也是懸空寺最大的殿宇。三層九
脊，全部由每根插入崖石的木梁承受，真是 "公輸天巧"， 古今奇
跡。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "down"   : __DIR__"xuankong1",
        ]));
        set("no_clean_up", 0);
        setup();
}
