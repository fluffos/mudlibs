// Room: /d/hengshan/huixiantai.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "見性峰山道");
        set("long", @LONG
你走在見性峰山脊小路上，聽著山下廟宇的鐘鼓，眼前白云漂
蕩，感覺幽逸極了。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "southdown"  : __DIR__"beiyuedian",
           "northup"    : __DIR__"shandao2",
        ]));
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

