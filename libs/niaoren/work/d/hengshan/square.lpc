// Room: /d/hengshan/huixiantai.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "見性峰廣場");
        set("long", @LONG
這里是恆山主峰見性峰峰頂的廣場。在這里俯瞰群山，才理會
得何以恆山是華夏北方萬山之宗，這里也是威震江湖的恆山派的根
本重地。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "westdown"  : __DIR__"shandao2",
           "north"     : __DIR__"baiyunan",
        ]));
	set("objects", ([
		__DIR__"npc/yi" : 1,
		__DIR__"npc/qing" : 1,
	]));
        set("outdoors", "hengshan");
//        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

