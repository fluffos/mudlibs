// Room: /d/hengshan/yuyang.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "玉羊游云");
        set("long", @LONG
東峰峭壁崖頂上，白石累累，散布有致，在云霧拂動下，似有
動感，象正在吃草的羊只。這就是和“金雞報曉”齊名的恆山勝景
“玉羊游云”。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "westdown"    : __DIR__"beiyuedian",
        ]));
        set("outdoors","hengshan");
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

