// Room: /d/hengshan/baiyunan.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "白云庵");
        set("long", @LONG
這里就是恆山派掌門人的居所。庵內供奉白衣觀世音，除地上
一個蒲團，四壁蕭然。恆山派威震江湖，沒想到主庵居然簡朴一至
如斯。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "south"  : __DIR__"square",
        ]));
	set("objects", ([
		__DIR__"npc/xian" : 1,
		__DIR__"npc/zheng" : 1,
		__DIR__"npc/qin" : 1,
	]));
//      set("no_clean_up", 0);
        setup();
        "/clone/board/hengshan_b"->foo();
}

