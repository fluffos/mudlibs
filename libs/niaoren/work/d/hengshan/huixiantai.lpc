// Room: /d/hengshan/huixiantai.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "會仙台");
        set("long", @LONG
走出北岳殿，就能看見一個石窟，緊靠在絕壁中鑿出的一個小
室，就是會仙台。台上排列群仙雕像，密密麻麻，不能勝數。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "eastdown"  : __DIR__"beiyuedian",
        ]));
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

