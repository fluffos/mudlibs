// Room: /d/hengshan/yunge.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "云閣虹橋");
        set("long", @LONG
過完棧道，就到云閣虹橋。前往恆山主峰見性峰，都要在這里
跨過唐峪河。拱橋臥波，水天亮麗，倍覺胸襟舒暢。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "east"      : __DIR__"daziling",
           "northdown" : __DIR__"zhandao",
        ]));
	set("objects", ([
		__DIR__"npc/zhi" : 1,
	]));
        set("outdoors", "hengshan");
//        set("no_clean_up", 0);
        setup();
}

