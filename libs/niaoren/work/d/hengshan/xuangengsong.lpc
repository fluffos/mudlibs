// Room: /d/hengshan/xuangengsong.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "懸根松");
        set("long", @LONG
就在虎風口這個風口險地，卻有一株古松，雖被大風吹得樹根
裸露，千百年來仍迎風挺立，這就是恆山著名的 "梵宮懸根" 。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "east"      : __DIR__"hufengkou",
        ]));
        set("outdoors", "hengshan");
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

