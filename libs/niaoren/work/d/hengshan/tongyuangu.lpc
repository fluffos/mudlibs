// Room: /d/hengshan/tongyuangu.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "通元谷");
        set("long", @LONG
通元谷孤處見性峰下，相傳為張果老成仙前修道之所，由于他
道號 "通元" ，這里也就成了通元谷。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "eastup"  : __DIR__"guolaoling",
        ]));
        set("outdoors", "hengshan");
        set("no_clean_up", 0);
        setup();
}

