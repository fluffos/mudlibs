// fu-zhangfang 帳房
// By Lgg,1998.10

inherit ROOM;

void create()
{
        set("short", "帳房");
        set("long", @LONG
這里是侯員外家的帳房。屋內青磚鋪地，進門處橫擺著一張桌
子，一個管家正坐在桌后，小眼睛不住地跟著你轉，好象又在打什
么壞主意。
LONG
        );
        set("exits", ([
                "east" : __DIR__"fu-datang",
        ]));
        set("objects", ([
                __DIR__"npc/guanjia" : 1,
        ]));

        setup();
}
