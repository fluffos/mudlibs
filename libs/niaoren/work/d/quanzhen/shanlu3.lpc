// shanlu3.c 山路
// By Lgg,1998.10

inherit ROOM;

void create()
{
        set("short", "山路");
        set("long", @LONG
前面好象有一個大山谷，走到這里，樹木多了些，全都是柏樹。
西面是一條彎曲的山路，隱藏在山間，要在夜里還真看不出來。
LONG
        );
        set("outdoors", "quanzhen");
        set("exits", ([
                "southwest" : __DIR__"shanlu2",
                "northup" : __DIR__"cuipinggu",
        ]));
        set("no_clean_up", 0);

        setup();
}
