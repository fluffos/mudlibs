// xiaolu1.c 后山小路
// By Lgg,1998.10

inherit ROOM;

void create()
{
        set("short", "后山小路");
        set("long", @LONG
這里是后山上的小路。這條小路好象不是人工刻意修出來的，
也許只是以前的采藥人踩多了，現出這樣一條小路來。
LONG
        );
        set("outdoors", "quanzhen");
        set("exits", ([
                "northeast" : __DIR__"xiaolu2",
                "southwest" : __DIR__"houshan",
        ]));
        set("no_clean_up", 0);
        setup();
}

