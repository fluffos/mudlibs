//Room: /d/dali/yuxuguanqian.c
//Date: June. 29 1998 by Java

inherit ROOM;
void create()
{
	set("short","玉虛觀前");
	set("long",@LONG
東面一座廟觀，門前匾上寫著“玉虛觀”三個大字，西邊一個
碧水蕩漾的小湖，四周綠柳成蔭，實乃修心養性的好地方。
LONG);
	set("objects", ([
	   __DIR__"npc/xiaodaogu": 2,
	]));
	set("outdoors", "dali");
	set("exits",([ /* sizeof() == 1 */
	    "north"  : __DIR__"yuxuguan",
	    "south"  : __DIR__"hudiequan",
	]));
	setup();
	replace_program(ROOM);
}

