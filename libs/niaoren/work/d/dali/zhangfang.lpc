//Room: /d/dali/zhangfang.c
//Date: June. 29 1998 by Java

inherit ROOM;
void create()
{
	set("short","帳房");
	set("long",@LONG
這里是鎮南王府的帳房，王府每天的銀米出入，往來消耗，
全由這里經手，包括家丁下人的薪俸賞賜，在在都是麻煩多多。
這里卻從來沒出過差錯。
LONG);
	set("objects", ([
	   __DIR__"npc/huo": 1,
	]));
	set("exits",([ /* sizeof() == 1 */
	    "east"  : __DIR__"wfdating",
	]));
	setup();
	replace_program(ROOM);
}

