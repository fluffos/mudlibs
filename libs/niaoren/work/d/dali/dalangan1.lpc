
//Room: /d/dali/dalangan1.c
//Date: June. 29 1998 by Java

inherit ROOM;
void create()
{
	set("short","");
	set("long",@LONG
依著一棵大榕樹，擺夷人在此建了一座相當大的杆欄。下層這里
有一滿圈的十几頭牛羊。旁邊的樓梯修得頗寬。看來這里是本地的體
面人家。
LONG);
	set("objects", ([
	   __DIR__"npc/shanyang": 3,
	   __DIR__"npc/cow": 2,
	]));
	set("outdoors", "dali");
	set("exits",([ /* sizeof() == 1 */
	    "west"  : __DIR__"nongtian4",
	    "up"    : __DIR__"dalangan2",
	]));
	setup();
}

