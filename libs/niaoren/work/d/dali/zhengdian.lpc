//Room: /d/dali/zhengdian.c
//Date: June. 29 1998 by Java

inherit ROOM;
void create()
{
	set("short","正殿");
	set("long",@LONG
這里是大理國皇宮的正殿，大理皇帝通常都在這里處理國事，
但現在皇帝不在，所以顯得不那么拘謹，往南是長廊。
LONG);
	set("exits",([ /* sizeof() == 1 */
	    "south"  : __DIR__"hualang",
	]));
	set("no_clean_up", 0);
	setup();
}

