//Room: /d/huashan/shop.c

inherit ROOM;

void create()
{
	set("short", "小吃店");
	set("long", @LONG
這里是石梁邊的一間小吃店，賣些簡單的飲食，專門賺那些游客
的錢。
LONG );
     	set("exits", ([
            "west" : __DIR__"shiliang",
       	]));

        set("objects", ([
            "/d/city/npc/xiaoer2" : 1,
       	]));
//        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

