//Room: sheshenya.c 舍身崖
//Date: Sep 22 1997

inherit ROOM;

void create()
{
      set("short","舍身崖");
      set("long",@LONG
這是一處險峻的懸崖峭壁，相傳真武帝在此舍身得道。崖上立有鐵柵欄，
防止有人自殺步真武帝的后塵。
LONG);
      set("exits",([ /* sizeof() == 1 */
          "east"     : __DIR__"nanyanfeng",
      ]));
      set("outdoors", "wudang");
      set("no_clean_up", 0);
      setup();
}
