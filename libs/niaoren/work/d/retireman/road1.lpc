// Room: /d/reitreman/load.c
// leos 2000/08/27

inherit ROOM;

void create()
{
        set("short", "寬闊大道");
        set("long", @LONG
這是一條可供十輛馬車並行的寬闊大道，看來這條路只通往一個地方，在這條
道路兩旁是一排排整齊高大的樹林，仔細一看原來是經過專人修剪過的，而樹
林四週盡是一片綠油油的草地。
LONG );
        set("no_fight", 1);
        set("valid_startroom", 1);
//      set("no_clean_up", 0);
        set("exits", ([
                "north" : __DIR__"road2",
                "west" : "/d/taishan/yidao3",
        ]));

        set("no_clean_up", 0);
        setup();
}
