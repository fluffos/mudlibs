// Room: /d/reitreman/load2.c
// leos 2000/08/27

inherit ROOM;

void create()
{
        set("short", "寬闊大道");
        set("long", @LONG
到了這兒，這條寬闊的大道景色也開始改變了，在道路兩旁不再是一排排整齊高大的樹林，替換
樹林的是一尊尊比人還高上數倍的不知名的神像和英雄的雕塑，在前方是一塊寬大的廣場，而不
遠處似乎是一棟十分雄偉的建築物。
LONG );
        set("no_fight", 1);
        set("valid_startroom", 1);
//      set("no_clean_up", 0);
        set("exits", ([
                "north" : __DIR__"road3",
                "south" : __DIR__"road1",
        ]));

        set("no_clean_up", 0);
        setup();
}
