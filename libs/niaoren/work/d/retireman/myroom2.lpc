// Room: /wizard/leos/workroom.c
// leos 2000/03/08

inherit ROOM;

void create()
{
        set("short", "宇宙中某處地方");
        set("long", @LONG
四週是一片虛無飄渺的空間，這兒是大巫師鳥人一號的測試區。
LONG );
        set("valid_startroom", 1);
//      set("no_clean_up", 0);
        set("exits", ([
                "north" : __DIR__"myroom1",
        ]));

        set("objects", ([
            "/u/leos/shadow" : 1,
        ]));

        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}
