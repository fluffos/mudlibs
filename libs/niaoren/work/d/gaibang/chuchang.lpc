// chuchang shi

inherit ROOM;

void create()
{
	set("short", "儲藏室");
	set("long", @LONG
這里是丐幫的儲藏室，你現在只能看見一個個鎖著的箱子。
LONG );
	set("exits", ([
		"west" : __DIR__"mishi",
		"up" : __DIR__"undertre",
	]));
        set("objects", ([ 
            "/clone/food/badun" : 4
        ]));
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
