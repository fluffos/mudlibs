/*  <SecCrypt CPL V3R05>  */
 
//  Room:  /d/city/club3
inherit  ROOM;

void  create  ()
{
    set  ("short",  "聚義廳");
    set  ("long",  @LONG
這是一間寬敞明亮的大廳，廳的正中間是一根圓形的柱子。柱
子上貼滿了各種廣告：有換房的，有賣家具的，但其中最多的還是
找打牌搭檔的廣告。廳的東南西北各有個門，里面傳出“拱啊！”
“斃了！”等的叫聲。
LONG);

    set("light_up",  1);
    set("no_magic",  1);
    set("no_fight",  1);
    set("exits",  ([  /*  sizeof()  ==  2  */
        "down"  :  __DIR__"clubpoem",
        "north":  __DIR__"piggy",
        "south":  __DIR__"qishi",
    ]));
    set("no_clean_up", 0);
    setup();
    replace_program(ROOM);
}
