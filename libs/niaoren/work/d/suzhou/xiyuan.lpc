// Room: /d/suzhou/xiyuan.c
// Date: May,31 1998 Java

inherit ROOM;

void create()
{
	set("short","戲園子");
	set("long",@LONG
這是一座蘇州城里鼎鼎有名的戲園，戲園中生，旦，淨，
墨，丑一應具全，場中一個大戲台，台上正在唱著《遇長春》，
台下不時地傳來票友們的叫躍，喝彩聲。你也忍不住想坐下來
看看戲，放松一下筋骨。
LONG);
//	set("no_clean_up", 0);
	set("objects", ([
		__DIR__"npc/piaoyou": 3,
	]));
	set("exits",([ /* sizeof() == 1 */
		"northeast" : __DIR__"beidajie1",
		"north"     : __DIR__"majiu",
	]));
	setup();
	replace_program(ROOM);
}
