// Room: /d/suzhou/yunhematou.c
// Date: May 31, 98  Java

inherit ROOM;

void create()
{
	set("short", "運河碼頭");
	set("long", @LONG
這里是運河碼頭。它是朝廷漕運要塞，水陸必經之處。運
河中來往船只滿載穿梭，極其繁忙。
LONG );
	set("outdoors", "suzhou");
	set("no_clean_up", 0);
	set("exits", ([
		"southeast" : __DIR__"xidajie1",
	]));
	setup();
	replace_program(ROOM);
}

