// Room: /d/suzhou/leitai.c
// Date: May 31, 98  Java

inherit ROOM;

void create()
{
	set("short", "擂台前廣場");
	set("long", @LONG
這里是蘇州擂台前面的一個大型廣場，有許多人正在擂台
上比武，到處是人山人海，如果你也想試試身手，那么就請跳
上擂台比試比試吧。
LONG );
	set("outdoors", "suzhou");
	set("no_clean_up", 0);
	set("exits", ([
		"east"  : __DIR__"hutong1",
	]));
	setup();
	replace_program(ROOM);
}

