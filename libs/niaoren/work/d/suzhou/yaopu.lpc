// Room: /d/suzhou/yaopu.c
// Date: May 31, 98  Java

inherit ROOM;

void create()
{
	set("short", "立春堂");
	set("long", @LONG
這是一家藥鋪，一股濃濃的藥味讓你几欲窒息，那是從藥
柜上的几百個小抽屜里散發出來的。藥房先生卻不在店內。一
名小伙計站在柜台后招呼著顧客。
LONG );
	set("outdoors", "suzhou");
//	set("no_clean_up", 0);
	set("exits", ([
		"south"  : __DIR__"dongdajie2",
	]));
	set("objects", ([
		__DIR__"npc/huoji" : 1,
	]));
	setup();
}

