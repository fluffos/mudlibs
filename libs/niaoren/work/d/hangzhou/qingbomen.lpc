// qingbomen.c
// Date: Nov.1997 by Venus
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "清波門");
        set("long", @LONG
園門口有一大門樓，大匾上刻著“清波門”。門樓下站著几個嬉
嬉哈哈的少女，惹人墜步相觀。在往西就進了聚景，往東便回到大路。
LONG);
        set("exits", ([
            "east"   : __DIR__"road14",
            "west"   : __DIR__"jujingyuan",
        ]));
        set("objects", ([
            __DIR__"npc/girl" : 2,
        ]));
        set("outdoors", "hangzhou");
//        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}
