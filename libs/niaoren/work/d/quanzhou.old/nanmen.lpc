// Room: /d/quanzhou/nanmen.c
// Date: May 7, 96   Jay

inherit ROOM;

void create()
{
	set("short", "泉州南門");
	set("long", @LONG
這里是南方第一港泉州的南門。南門外是一片荒野。
LONG );
	set("exits", ([
//		"south" : __DIR__"bamboo",
                "north" : __DIR__"zhongxin",
	]));
	set("no_clean_up", 0);
	set("outdoors", "quanzhou");
	setup();
	replace_program(ROOM);
}

