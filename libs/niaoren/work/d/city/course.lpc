// Room: bedroom.c
// Date: Sep.6 1997

#include <room.h>
inherit ROOM;

void create()
{
      set("short", "貓窩");
      set("long", @LONG
這里真是好棒的一間愛的小屋，竹藤扎就的窗外是一輪靜靜
的小湖，湖畔的柳枝條兒點著湖面一圈圈四散漫開。靠東是一張
電動大床，床頭小柜上插滿了九百九十九朵艷麗的紅玫瑰，映在
對面的穿花大鏡，真是滿室春風。
    這里屬于小貓和蒙面奇俠的二人世界。
LONG );
      set("exits", ([ /* sizeof() == 1 */
          "east": __DIR__"jiaowai1",
      ]));
      set("valid_startroom","1");

      set("no_fight", 1);
//      set("no_clean_up", 0);
      set("sleep_room", 1);
      setup();
      create_door("east", "垂花小門", "west", DOOR_CLOSED);
}
