// Room: /city/kedian2.c
// YZC 1995/12/04 

inherit ROOM;

void create()
{
        set("short", "武館聯合飯廳");
        set("long", @LONG
這裡是揚州武館的專屬餐廳，這裡到處是一些初入江湖的新手，
你注意到不遠的桌上有一些難吃的東西可以拿來吃(get XXX , eat
badun , drink jiudai)，餐廳裡面好像還有一間房間可用進去，
可是那些打菜的老鳥郤一直在ㄋ一ㄠ你,你想也許可以用enter進去
那間房間。
LONG );
        set("exits", ([
                "north"  : __DIR__"wuguan5",
                "enter" : __DIR__"wuguan6",
        ]));
        set("objects", ([
            "/clone/food/badun" : 10,
            "/clone/food/jiudai" : 10,
        ]));
//      set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

