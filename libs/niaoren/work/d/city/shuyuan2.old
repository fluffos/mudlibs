// Room: /d/city/shuyuan2.c

inherit ROOM;

void create()
{
        set("short", "書院書庫");
        set("long", @LONG
這里是書院的圖書館，窗明几淨，一塵不染。壁上的書架(board)堆
滿俠客行的先賢們的文選，記錄了俠客行的風風雨雨和無數淒婉的、遙
遠磨滅不去的傳說。
LONG);
        set("exits", ([ /* sizeof() == 2 */
            "down" : __DIR__"shuyuan",
         ]));
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
        "/clone/board/shuyuan_b"->foo();
}
