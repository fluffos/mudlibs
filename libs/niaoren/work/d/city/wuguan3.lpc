// square.c

inherit ROOM;

void create()
{
        set("short", "練武場");
        set("long", @LONG
這裡是揚州武館的練武場，場上立著幾個木人看來是讓新手練習的好對象，
你可以用"fight mu"來練習，記得武器要用雞骨頭，身上可以到雜貨店去買
金絲甲來穿，你發現南方的牆壁(WALL)有點不對，你可以用break這個指令，
牆上有塊牌子(list)。
LONG );
        set("exits", ([ /* sizeof() == 2 */
            "west"     : __DIR__"wuguan2",
        ]));
        set("objects", ([
            "/d/shaolin/npc/mu-ren" : 5,
        ]));
        set("item_desc", ([
                "list" : "那鍋牆其實是虎爛的，哈哈哈！
                          你被鳥人一號騙了！\n",
        ]));
//        set("no_clean_up", 0);
        set("outdoors", "xx" );

        setup();
}


