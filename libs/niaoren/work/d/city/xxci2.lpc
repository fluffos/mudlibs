// Room: /d/npc/xianxianci.c

inherit ROOM;

void create()
{
	set("short", "先賢祠－巫師部");
	set("long", @LONG
這里是紀念歷代巫師的地方。每位在這里被供奉的巫師都為本MUD
做出過杰出的、不可磨滅的貢獻。我們永遠都會愛戴、懷念他們。
抬頭一張橫匾，上書七個閃閃發光的金字：俯首甘為孺子牛
注：排名不分先后。按字母順序。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
		"down" : __DIR__"xxci1",
	]));
	set("no_fight",1);
	set("no_clean_up", 0);
	set("objects", ([ /* sizeof() == 1 */
		"/d/npc/five" : 1,
		"/d/npc/fcxy" : 1,
		"/d/npc/jungu" : 1,
		"/d/npc/doll" : 1,
		"/d/npc/wind" : 1,
	]));

	setup();
	replace_program(ROOM);
}
