// square.c

inherit ROOM;

void create()
{
        set("short", "預備練武場");
        set("long", @LONG
這裡是揚州武館的練武場，場上立著幾個木人看來是讓新手練習的好對象，
牆上有塊牌子(list)。
LONG );
        set("exits", ([ /* sizeof() == 2 */
            "west"     : __DIR__"wuguan5",
        ]));
        set("objects", ([
            "/d/shaolin/npc/mu-ren" : 5,
        ]));
        set("item_desc", ([
                "list" : "不會的時候多多問線上的老手！！\n",
        ]));
//        set("no_clean_up", 0);
        set("outdoors", "xx" );

        setup();
}


