// moon 2000.11.20

#include <room.h>
inherit ROOM;

void create()
{
     set("short", "西訓練室");
      set("long",@LONG 
這裡豎著幾個靶人，上面傷痕累累，是唐門弟子日常訓練暗器並鍛鍊的地方。
LONG );

      set("exits", ([ /* sizeof() == 1 */
            "east"   : __DIR__"gongchan1",
            "north"   : __DIR__"liengong3" 
      ]));
//      set("no_clean_up", 0);
      set("objects", ([
              "/clone/npc/muren" : 5,  
      ]));
      setup();
}
