//moon 2000.12.31
#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", HIR"湖心亭"NOR);
        set("long",@LONG
煙波浩淼中有一座紅亭，四周竹籬掩映，錯落有致，這就是唐方
和蕭秋水定情所在了。
LONG );
        set("exits", ([
            "west" : __DIR__"hucing1",
            "south" : __DIR__"matou",
        ]));
//     set("no_clean_up", 0);
        setup();
}
