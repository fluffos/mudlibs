// moon 2000.11.20

#include <room.h>
inherit ROOM;

void create()
{
      set("short", "進階鍛鍊室");
      set("long",@LONG 
這裡豎立著較多靶人，是唐門弟子進階訓練用的地方。
LONG );

      set("exits", ([ /* sizeof() == 1 */
            "west"   : __DIR__"gongchan2",
            "south"   : __DIR__"liengong1" 
      ]));
//      set("no_clean_up", 0);
      set("objects", ([
              "/clone/npc/muren" : 20,
      ]));
      setup();
}
