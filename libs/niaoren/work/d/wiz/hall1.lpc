// Room: /d/wiz/hall1.c

inherit ROOM;

void create()
{
	set("short", "巫師交誼廳");
	set("long", @LONG
這裡是巫師聊天討論的地方﹐如果巫師們要開會的話﹐也多半會選在這
裡舉行﹐往東是巫師會客室。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
"east" : "/d/wiz/entrance",
"north" : "/d/wiz/jobroom",
]));
	set("no_clean_up", 0);

	setup();
}
