// Room: /d/wizard/wizard_room.c
inherit ROOM;
void create()
{
        set("short", "巫師休息室");
        set("long", @LONG
  這裡就是「鳥人世界」巫師的休息所在，剛粉刷過的四周牆壁上光
禿禿的什麼都沒有，屋子的正中央放著一張楠木桌，上面擺著一本
巫師留言簿(board)。
LONG );
        set("exits", ([ /* sizeof() == 1 */
                "down" : __DIR__"guest_room",
                "east" : __DIR__"jail",
        ]));
        set("no_fight", "1");
        set("no_clean_up", 0);
        setup();
        call_other("/clone/board/wiz_b", "???");
        replace_program(ROOM);
}
