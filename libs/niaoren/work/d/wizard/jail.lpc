// Room: /wizard/leos/workroom.c
// leos 2000/03/08

inherit ROOM;

void create()
{
        set("short", "機器人處理中心");
        set("long", @LONG
四週是一片黑暗，沒有入口更沒有出口，恭喜你來到這！
被大神抓包的機器人們來到這兒只有一種結局====>
"送到報廢廠廢掉所有武功，再關上一佰年。"PS:沒有特赦！
LONG );
        set("valid_startroom", 1);

        set("objects", ([
            "/u/leos/shadow1" : 1,
        ]));

        set("no_transmit", 1);
        set("no_quit", 0);
        set("no_shout", 0);
        set("valid_startroom", 1);
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}
