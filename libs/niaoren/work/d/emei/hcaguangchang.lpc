//Room: hcaguangchang.c 華藏庵廣場
//Date: Oct. 2 1997 by That

inherit ROOM;

void create()
{
      set("short","華藏庵廣場");
      set("long",@LONG
這里是峨嵋華藏庵的廣場，地上鋪著青磚。正前方有一道台階，通往大
雄寶殿。殿前有個丈許高的大香爐，香爐前的香台上，紅燭高燒，香煙飄繞。
廣場上有几個小師太正在練功。自此左右各有一條走廊通向后殿。
LONG);
      set("objects", ([
           CLASS_D("emei") + "/xu" : 1,
      ]));
      set("outdoors", "emei");
      set("exits",([ /* sizeof() == 1 */
          "north"   : __DIR__"hcazhengdian", 
          "southup" : __DIR__"hcadadian", 
          "west"    : __DIR__"hcawest1", 
          "east"    : __DIR__"hcaeast1", 
      ]));
      set("valid_startroom","1");
//      set("no_clean_up", 0);
      setup();
      replace_program(ROOM);
      "/clone/board/emei_b"->foo();
}
