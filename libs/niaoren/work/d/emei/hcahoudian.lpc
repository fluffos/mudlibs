//Room: hcahoudian.c 華藏庵后殿
//Date: Oct. 2 1997 by That

inherit ROOM;
void do_chat();

void create()
{
      set("short","華藏庵后殿");
      set("long",@LONG
這是峨嵋山華藏庵的后殿。是峨嵋派掌門人傳教弟子功夫的地方。這里供
有一尊普賢菩薩像。兩旁靠牆放著几張太師椅，地上放著几個蒲團。掌門人正
在向几個女弟子傳授武功。
LONG);
      set("objects", ([
           CLASS_D("emei") + "/miejue" : 1,
      ]));
      set("no_get_from", 1);
      set("no_get", 1);
      set("no_steal", 1);
      set("no_clean_up", 0);
      set("exits",([ /* sizeof() == 1 */
          "north"   : __DIR__"hcadadian", 
      ]));
      setup();
}
