// /d/xingxiu/mogaoku.c
// Jay 3/17/96

inherit ROOM;

// can put some book about budism here.

void create()
{
        set("short", "莫高窟");
        set("long", @LONG
這里牆壁上繪制著與佛教相關的畫面，有仙女飛天，還有反彈琵琶。洞中
還有很多佛像和經書。你被這些瑰麗的文化之寶驚呆了。
LONG );
        set("exits", ([ 
             "north" : __DIR__"yueerquan",
        ]));
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

