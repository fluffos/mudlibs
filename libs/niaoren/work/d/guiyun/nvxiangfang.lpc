// Room: /guiyun/nvxiangfang.c
// Date: Nov.18 1998 by Winder

inherit ROOM;

void create()
{
	set("short", "女廂房");
	set("long", @LONG
這里是歸云庄女弟子的休息室，擺著几張木床。床旁都有一支高腳
架子，用來挂衣物。屋角一張几凳，放了一盆蘭花，散著淡淡幽香。
LONG );
        set("sleep_room",1);
	set("no_clean_up", 0);

	set("exits", ([
		"east" : __DIR__"huating",
	]));
	setup();
	replace_program(ROOM);
}

