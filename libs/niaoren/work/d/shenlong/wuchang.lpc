// wuchang.c
inherit ROOM;

void create()
{
	set("short", "練武場");
	set("long", @LONG
這是一個寬大的練武場，有几個手執短劍的少年少女在這里練功。
旁邊站著几人,有男有女,年紀輕的三十來歲,老的已有六七十歲,身上
卻不帶兵刃.北邊是一條走廊.
LONG );
	set("exits", ([
		"north" : __DIR__"zoulang",
	]));
	set("objects", ([
                "/d/shaolin/npc/mu-ren": 5,
	]));
        setup();
	replace_program(ROOM);
}

