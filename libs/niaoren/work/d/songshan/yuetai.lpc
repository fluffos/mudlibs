// Room: /d/songshan/yuetai.c
// Date: CSWORD 96/03/25

inherit ROOM;

void create()
{
	set("short", "月台");
	set("long", @LONG
這里是法王寺的月台。從月台東望，太室山巒高聳入云，寺東
有雙峰插云，中間闕如。因名 "嵩門" 。中秋佳節，明月自峰中升
起，正當嵩門之中，銀光洒滿山谷，這就是登封八景之一的 "嵩門
待月" 。
LONG );
	set("exits", ([
		"east" : __DIR__"fawangsi",
	]));
	set("objects", ([
		__DIR__"npc/fei" : 1,
	]));
//	set("no_clean_up", 0);
	set("outdoors", "songshan");
	setup();
	replace_program(ROOM);
}

