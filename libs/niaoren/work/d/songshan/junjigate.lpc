// Room: /d/songshan/junjigate.c
// Date: CSWORD 96/03/25

inherit ROOM;

void create()
{
	set("short", "峻極禪院山門");
	set("long", @LONG
嵩山絕頂，古稱 "峻極" 。這里是本是佛教大寺，但百年來
卻已成為嵩山派掌門人的住所。西上可以到達封禪台。
LONG );
	set("exits", ([
		"southdown" : __DIR__"shandao6",
		"northup"   : __DIR__"chanyuan",
		"westup"    : __DIR__"fengchantai",
	]));
	set("objects", ([
		__DIR__"npc/tang" : 1,
		__DIR__"npc/wan" : 1,
	]));
//	set("outdoors", "songshan");
	set("no_clean_up", 0);
	setup();
}

