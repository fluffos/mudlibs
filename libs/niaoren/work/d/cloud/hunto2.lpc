// hunto2.c 黃土路(二)
// 2000.8 by andy

inherit ROOM;

void create()
{
        set("short", "黃土路");
        set("long", @LONG
到了這裡路行漸廣，再往南就是邪神山莊了，聽說那是個一群土匪
盤據的山莊；你的心裡不由得開始起了畏懼之心......
LONG
        );
        set("outdoors", "cloud");
        set("exits", ([
                "north" : __DIR__"hunto",
                "south" : __DIR__"ganman",
        ]));

        setup();
        replace_program(ROOM);
}
