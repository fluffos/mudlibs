// /clone/board/cloud_b.c

inherit BULLETIN_BOARD;

void create()
{
        set_name("邪神山莊留言版", ({ "board" }) );
        set("location", "/d/cloud/ganman");
        set("board_id", "cloud_b");
        set("long", "這是一個供玩家交流並且回報問題的留言板。\n" );
        setup();
        set("capacity", 100);
}

