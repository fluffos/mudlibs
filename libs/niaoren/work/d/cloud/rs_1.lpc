// rs_1.c 右邪一進
// 2000.8 by andy

#include <ansi.h>
inherit ROOM;

void create()
{
          set("short",HIG "右邪一進"NOR);
        set("long",
"這裡是右邪一進，平時這裡少有人煙，除了一些打雜的雜役外，幾乎沒人。\n"
"                                              \n"
"由此往西是"HIG"邪雲廣場"NOR"，往東是"HIG"右邪二進"NOR"，往南則是休息室。\n"
"                                \n"
);
//      set("no_fight", 1);
//      set("valid_startroom", 1);
//      set("no_clean_up", 0);
        set("outdoors", "cloud");
        set("exits", ([
                "south" : __DIR__"qkroom2",
                "west" : __DIR__"cloud_c",
                "east" : __DIR__"rs_2",
        ]));

//      set("objects", ([
//              __DIR__"" : 1,
//      ]));

        set("no_clean_up", 0);
        setup();
}
