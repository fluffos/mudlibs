// ls_3.c 右邪三進
// 2000.8 by andy

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short",HIY "左邪三進"NOR);
        set("long",
"這裡是左邪三進，平時這裡少有人煙，除了一些打雜的雜役外，幾乎沒人。\n"
"                                              \n"
"由此往西是"HIW"施工中"NOR"，往東是"HIG"右邪二進"NOR"，往南是？，往北則是？。\n"
"                                \n"
);
//      set("no_fight", 1);
//      set("valid_startroom", 1);
//      set("no_clean_up", 0);
        set("outdoors", "cloud");
        set("exits", ([
//              "north" : __DIR__"",
//              "south" : __DIR__"",
//              "west" : __DIR__"",
                "east" : __DIR__"ls_2",
        ]));

//      set("objects", ([
//              __DIR__"" : 1,
//      ]));

        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}
