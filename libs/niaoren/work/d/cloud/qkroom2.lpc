// qkroom2.c 休息室
// 2000.8 by andy

inherit ROOM;

void create()
{
        set("short", "休息室");
        set("long", @LONG
這是一間超大的寢室，陳設十分簡陋。靠牆兩側整齊的擺放著國軍通用
的上下鋪四人床，滿屋子都是呼呼的打酣聲。西邊有張床是空的，你躡
手躡腳地走了過去。你想也許可以在這兒偷偷摸個小魚睡個小覺(SLEEP)
。
LONG );
        set_light(0);
        set("sleep_room", "1");
        set("no_fight", "1");
//      set("hotel",1);
        set("no_clean_up", 0);
        set("exits", ([
                "north" : __DIR__"rs_1",
        ]));
}

