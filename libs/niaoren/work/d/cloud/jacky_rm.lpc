// jacky_rm.c 無悔居客廳
// 2000.8 by andy

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short",HIW "無悔居前廳"NOR);
        set("long",
"這裡是無悔居，平時無名與他的妻子回憶坐鎮於此；此地亦是他倆用心佈置\n"
"，牆上掛著多幅名家畫作，讓人目不暇給，不由得想在這裡住下來。\n"
"                                      \n"
"由此往北是？，往南則是無悔園。\n"
"                                      \n"
);
//      set("no_fight", 1);
//      set("valid_startroom", 1);
//      set("no_clean_up", 0);
        set("exits", ([
//              "north" : __DIR__"",
                "south" : __DIR__"jacky_f",
        ]));

// 一個松島菜菜子(nanako.c)
//        set("objects", ([
//                __DIR__"npc/nanako" : 1,
//        ]));

//        set("no_clean_up", 0);
        setup();
}
