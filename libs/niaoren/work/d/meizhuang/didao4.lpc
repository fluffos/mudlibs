// didao4.c
inherit ROOM;

void create()
{
        set("short", "甬道");
        set("long", @LONG
你一到這里，迎面就聞到一股腐爛的泥土的味道，不過從前面的
亮光中，你又看到了希望，你不禁開始加快腳步，想看看那里到底有
些什么東西。 
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
            "north" : __DIR__"didao5",
            "up" : __DIR__"didao3",
        ]));
        set("no_clean_up", 0);

        setup();
}

