// qishi.c
inherit ROOM;

void create()
{
        set("short", "棋室");
        set("long", @LONG
這里便是二庄主黑白子的棋室了，棋室里兩張石凳，一張石几，
石几上用黑白子的指力刻著縱橫九九八十一道，這里除了簡單的放著
兩盒用玉做成的棋子外，什么都沒有，顯是怕雙方對弈時分心。
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
            "east" : __DIR__"zoulang2",
        ]));
        set("objects", ([ /* sizeof() == 1 */
             __DIR__"npc/heibai-zi.lpc" : 1,
        ]));
        set("no_clean_up", 0);

        setup();
}
