// zoulang3.c
inherit ROOM;

void create()
{
        set("short", "走廊");
        set("long", @LONG
走廊到了這里似乎就到了盡頭，從走廊的那一頭傳來陣陣的燒飯
的香味，飢腸漉漉的你，走到這里，再也忍受不了那種扑面而來的香
氣的誘惑，只有加快腳步，去看個究竟。 
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
            "north" : __DIR__"chufang",
            "south" : __DIR__"zoulang2",
        ]));
        set("no_clean_up", 0);

        setup();
}

