// qhpo.c
inherit ROOM;

void create()
{
        set("short", "奇槐坡");
        set("long", @LONG
這里就是奇槐坡了，這里種著大大小小，千奇百怪的槐樹，有的
槐樹一直向天，有的又曲折盤繞，你從這里隱隱約約聽到一陣陣的琴
聲，似有似無，若隱若現。
LONG
        );
        set("outdoors", "meizhuang");
        set("exits", ([ /* sizeof() == 2 */
            "eastup" : __DIR__"xiaodao",
            "north"  : __DIR__"senlin1",
        ]));
        set("no_clean_up", 0);

        setup();
}

