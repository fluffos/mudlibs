// xiuxishi.c
inherit ROOM;

void create()
{
        set("short", "休息室");
        set("long", @LONG
這里是梅庄弟子練功勞累的時候，休息的地方，這里經常可以聽
到如雷的  聲，這里簡單的放著几張墊子，几床棉被，几個枕頭，而
且梅庄弟子雖然經常打掃這里，但這里仍然凌亂不堪，很多梅庄弟子
一進這里，倒頭便睡，顯是過于疲勞的緣故。 
LONG
        );
        set("sleep_room", "yes");
        set("exits", ([ /* sizeof() == 1 */
            "west" : __DIR__"zoulang2",
        ]));
        set("no_fight", "yes");
        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}

