// Room: /d/xiangyang/xiaorong1.c
// Date: Jan. 8 1999 by Winder

inherit ROOM;

void create()
{
	set("short", "胡同");
	set("long", @LONG
這里是襄陽城內老百姓的居住區。只見小弄兩邊是有些小
店面，有米行、小雜貨鋪等。一些居民從這里進進出出。
LONG );
        set("outdoors", "xiangyang");

	set("exits", ([
		"west" : __DIR__"jiekou2",
	]));
	set("objects", ([
		__DIR__"npc/kid" : 2,
	]));
	setup();
	replace_program(ROOM);
}

