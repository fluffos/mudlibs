// midao1.c
// by shilling 97.2

inherit ROOM;

void create()
{
	set("short", "秘密通道");
	set("long", @LONG
這是一段又長又黑的秘密通道，隱約可以感覺到四周是冰冷而堅硬的石
壁，你什么了看不清楚，只可以靠摸索著前進。
LONG );
	set("exits", ([
		"out" : __DIR__"shishi3",
		"north" : __DIR__"midao2",
	]));
	set("no_clean_up", 0);
	setup();
}
