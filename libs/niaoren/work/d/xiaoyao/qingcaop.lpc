// qingcaop.c
// by shilling 97.2

inherit ROOM;

void create()
{
        set("short", "青草坪");
        set("long", @LONG
這里是樹林中的一片比較大的空地，名曰：青草坪。因為處于密林之中
所以這里平時很少有人來，但是不知道為什么，這里卻經常有人跡出現。你
看，又一個來了。
LONG );
	set("exits", ([
		"north" : __DIR__"xiaodao3",
		"south" : __DIR__"xiaodao5",
		"east" : __DIR__"xiaodao4",
		"west" : __DIR__"xiaodao1",
	]));
	set("outdoors", "xiaoyao");
	set("objects", ([
		__DIR__"npc/suxingh": 1,
//		__DIR__"npc/xue": 1,
	]));
	set("valid_startroom", 1);
//	set("no_clean_up", 0);
	setup();
	"/clone/board/xiaoyao_b"->foo();
}
