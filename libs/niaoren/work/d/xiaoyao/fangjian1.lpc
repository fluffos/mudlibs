// fangjian1.c
// by shilling 97.2

inherit ROOM;

void create()
{
        set("short", "房間");
        set("long", @LONG
可以看出這里以前曾經是一個下棋的地方，房間除了一張石几，兩只軟
椅之外，空蕩蕩的一無所有，石几上刻著縱橫十九道棋路，棋局上布著二百
多顆棋子，卻是黑白對峙，竟然是一局沒有下完的棋。
LONG );
	set("exits", ([
		"south" : __DIR__"shiji1",
	]));
	set("no_clean_up", 0);
	setup();
}
