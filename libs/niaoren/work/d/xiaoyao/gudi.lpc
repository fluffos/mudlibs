// gudi.c
// by shilling 97.2

inherit ROOM;

void create()
{
        set("short", "谷底");
        set("long", @LONG
但聽得耳邊轟聾聾的響聲越來越大，山崖也逐漸變得傾斜，而不再象上
面那么陡峭筆立了，終于到達谷底了。只見上面的上崖一條大瀑布如玉龍懸
空，滾滾而下，傾入了一座清澈異常的大湖之中。甚為壯觀。
LONG );
        set("exits", ([
//               "out" : __DIR__"damen",
//               "north" : __DIR__"shishi1",
		"up" : __DIR__"yanfeng",
		"west" : __DIR__"hubian",
	]));
	set("outdoors", "xiaoyao" );
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
