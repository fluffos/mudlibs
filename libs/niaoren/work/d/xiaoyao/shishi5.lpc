// shishi5.c
// by shilling 97.2

inherit ROOM;

void create()
{
	set("short", "地下石室");
	set("long", @LONG
這里是地下石室的最下面一層了，四周是黑乎乎，說不出的陰深恐怖，
你可以聞到強烈的腐爛的東西所發出的氣味。看來還是快點離開比較好。
LONG );
	set("exits", ([
		"up" : __DIR__"shishi4",
	]));
	set("objects",([
		__DIR__"npc/xiaoyaozi" : 1,
	]));
//	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
