// liangong.c

inherit ROOM;

void create()
{
	set("short", "石屋");
	set("long", @LONG
這是樹林中的一間石屋，屋內顯得什么清淨和簡朴，房間中并沒有什么
特別的擺設在這里，不會受到任何東西的騷擾，是個修煉內功的好地方。
LONG );
	set("exits", ([ /* sizeof() == 2 */
		"south" : __DIR__"xiaodao4",
	]));
	set("no_clean_up", 0);
	setup();
}
