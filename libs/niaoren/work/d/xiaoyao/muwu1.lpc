// muwu1.c
// by shilling 97.2

inherit ROOM;

void create()
{
	set("short", "木屋");
	set("long", @LONG
這里是一間不大的木頭屋子，但是麻雀雖小，五藏俱全，屋子中只是疏
疏落落地擺著几件家具，所以也不顯得怎么小。
LONG );
	set("exits", ([
		"north" : __DIR__"xiaodao5",
	]));
 	set("objects", ([
		__DIR__"npc/kanggl": 1,
	]));
//	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
