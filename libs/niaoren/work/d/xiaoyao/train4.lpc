// Room: train2 逍遙派操練房
// Date:  2000.04.19 by leos
#include <room.h>

inherit ROOM;

void create()
{
      set("short", "逍遙派操練房");
      set("long", @LONG
這里擺滿了大刀長劍、沙袋綁腿等，是逍遙派弟子日常練功的地方。
LONG );

      set("exits", ([ /* sizeof() == 1 */
            "north"       : __DIR__"train3",
	    "south"       : __DIR__"train5",
      ]));
//      set("no_clean_up", 0);
      set("objects", ([
              "/d/shaolin/npc/mu-ren" : 15,  
      ]));
      setup();
      replace_program(ROOM);
}
