// xiuxis.c
// shilling 97.2

inherit ROOM;

void create()
{
	set("short", "逍遙小屋");
	set("long", @LONG
這是一間在樹林中的小屋，屋子中間擺著几張大床，几張桌子，看來是
個休息的地方，室中光線柔和，使人更增睡意。
LONG );
        set("exits", ([ 
		"east" : __DIR__"xiaodao2", 
	]));
        set("no_fight",1);
        set("no_steal",1);
        set("sleep_room",1);
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
