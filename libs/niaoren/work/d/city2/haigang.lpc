
inherit ROOM;

void create()
{
	set("short", "塘沽口");
	set("long", @LONG
這里就是大海之濱，浪花拍打著礁石，發出陣陣響聲，海面上一群群海
鷗『歐歐..』地叫著，不時有魚兒躍出海面，濺起層層的浪花。邊上有艘大
船(chuan)，船上坐著几個漁夫打扮的人，乘船可渡過大海到達海的另一端。
西面過天津城就是北京了，東面是一望無際的大海。
LONG );
	set("exits", ([
                "west" : __DIR__"road10",
	]));
	set("item_desc", ([
                "chuan" : "一艘大帆船，造得很結實，看來可以出遠海的。\n",
	]));
	set("objects", ([
                __DIR__"npc/flowerg":2,
		"/d/shenlong/npc/chuanfu" :1,
	]));
//	set("no_clean_up", 0);
	set("outdoors", "city2");
	setup();
	replace_program(ROOM);
}

