// Room: /d/city2/lab.c

inherit ROOM;

void create()
{
	set("short", "實驗室");
	set("long", @LONG
這里燈火通明，每天都有虫子在熬夜。
LONG );
	set("exits", ([ /* sizeof() == 1 */
		"east" : __DIR__"Sino-CO",
	]));
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
