
inherit ROOM;

void create()
{
	set("short", "康府內堂");
	set("long", @LONG
這里是康府內堂，一個仆役正在和丫環們聊天。南邊是康府
后院，北邊是書房，西邊是一座佛堂。
LONG );
        set("outdoors", "city2");
//	set("no_clean_up", 0);
	set("exits", ([
  		"north" : "/d/city2/shufang",
  		"south" : "/d/city2/kang3",
  		"west" : "/d/city2/fotang",
	]));
	set("objects", ([
  		"/d/city2/npc/puyi": 1,
  		"/d/city2/npc/yahuan": 2,
	]));
	setup();
}
