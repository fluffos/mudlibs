// Room: /d/city2/Sino-CO.c

inherit ROOM;

void create()
{
	set("short", "明光村");
	set("long", @LONG
這里有一班泥虫，整天在泥潭打滾，鑒于大家都是單打獨斗，某天一虫
疾呼，我們都在一起玩，何不金蘭結義?共同為MUD的繁榮做點貢獻。于是神
州結義就誕生了!
LONG );
	set("exits", ([ /* sizeof() == 3 */
		"west" : __DIR__"lab",
		"south" : __DIR__"ximenwai",
		"east" : __DIR__"domitory",
	]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
