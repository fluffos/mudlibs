inherit ROOM;

void create()
{
	set("short", "西四北大街");
	set("long", @LONG
這是一條長長的街道。東邊就是游人們常去的文廟，那里長年香火不斷，
煙霧繚繞。南邊是西單。
LONG );
        set("outdoors", "city2");
	set("exits", ([
		"north" : __DIR__"xisi2",
		"east" : __DIR__"wenmiao",
		"south" : __DIR__"xidan",
	]));
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}


