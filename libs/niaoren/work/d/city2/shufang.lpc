#include <room.h>
inherit ROOM;

void create()
{
	set("short", "康親王書房");
	set("long", @LONG
這里是康親王書房，房中書架上整整齊齊地堆放著很多書。不過書上積了
一些灰塵，顯然是很久沒有動過了。而書櫥里的几件精巧玉器卻更顯得玲瓏剔
透，光彩照人。
LONG );
	set("exits", ([
		"south" : __DIR__"neitang",
	]));
	set("objects", ([
		"/d/city2/npc/kang" : 1,
	]));
//	set("no_clean_up", 0);
	setup();
}

