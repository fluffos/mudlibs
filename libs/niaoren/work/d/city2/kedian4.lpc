
inherit ROOM;

void create()
{
	set("short", "西客房");
	set("long", @LONG
這里是西客房，一個掌柜和兩個伙計躲在角落里瑟瑟發抖。
LONG );
	set("exits", ([
		"east" : __DIR__"kedian2",
	]));
	set("objects", ([
		"/d/city2/npc/zhanggui":1,
		"/d/city2/npc/huoji":2,
	]));
//	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}

