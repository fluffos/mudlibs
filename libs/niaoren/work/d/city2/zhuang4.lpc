#include <ansi.h>

inherit ROOM;

void create()
{
    set("short", "大院");
    set("long", @LONG
這是一個破舊的大院。院內十分寬闊，可容百人。正中
一口天井,再往里是座大廳.
LONG );
    set("exits", ([
	"north" : "/d/city2/zhuang5",
	"south" : "/d/city2/zhuang3",
    ]));
    set("objects", ([
		"/d/shenlong/npc/zhang3": 1,
    ]));
    set("outdoors", "city2");
//    set("no_clean_up", 0);
    setup();
}


