#include <ansi.h>

inherit ROOM;

void create()
{
    set("short", "長廊");
    set("long", @LONG
這是條長長的走廊。四處黑沉沉的.
LONG );
    set("exits", ([
		"north" : "/d/city2/zhuang9",
		"south" : "/d/city2/zhuang5",
    ]));
    set("objects", ([
		"/d/city2/npc/dizi": 1,
    ]));
    set("outdoors", "city2");
//    set("no_clean_up", 0);
    setup();
    replace_program(ROOM); 
}


