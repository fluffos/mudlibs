
#include <room.h>
inherit ROOM;

void create()
{
	set("short", "東廠暗道");
	set("long", @LONG
這是一條暗道，是專為鰲拜從鰲府直接到東廠用的。
LONG );
	set("exits", ([
		"southwest" :  __DIR__"aobai11",
		"northeast" : __DIR__"bingqiku",
	]));
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
