// Room: /city2/qianzhuang.c

inherit ROOM;

void create()
{
	set("short", "錢庄");
	set("long", @LONG
這是一家老字號的錢庄，已有几百年的歷史，在全國各地都有分店。
它發行的銀票信譽非常好，通行全國。不管你走到哪里，在任何一個本
庄分店都可以任意存取。牆上挂著一個牌子(paizi)。
LONG );
	set("item_desc", ([
        "paizi" : "\n本庄有利息，至少百分之一。\n
      存錢        deposit或者cun\n
      取錢        withdraw或者qu\n
      錢幣兌換    convert或者duihuan\n
      查帳        check或者chazhang\n",
	]));
	set("exits", ([
		"west" : __DIR__"wangfu1",
	]));
	set("no_clean_up", 0);
	set("no_fight", 1);
	set("no_beg", 1);
	set("objects", ([
		__DIR__"npc/qianlaob" : 1
	]));

	setup();
}

