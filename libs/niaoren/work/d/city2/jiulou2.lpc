
inherit ROOM;
inherit F_DEALER;

void create()
{
	set("name", "全聚德酒樓");
	set("short", "全聚德酒樓二樓");
	set("long", @LONG
這里是雅座，文人學士經常在這里吟詩作畫，富商土豪也在這里
邊吃喝邊作交易。這里也是城里舉辦喜宴的最佳場所。牆上挂著價格
牌子(paizi)。你站在樓上眺望京城，只覺得心曠神怡。
LONG );
	set("item_desc", ([
		"paizi" : "本店出售烤鴨及上等花雕酒。\n",
	]));
	set("exits", ([
		"down" : __DIR__"jiulou1",
	]));
	set("objects", ([
		"/d/city2/npc/cheng":1,
	]));
//	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
