#include <ansi.h>

inherit ROOM;

void create()
{
    set("short", "庄府大廳");
    set("long", @LONG
這是一間靈堂。堂上供了七八個牌位.看來一座靈堂上供的
是一家死人.一陣陰風吹過,蠟燭突然滅了.
LONG );
    set("exits", ([
		"east" : "/d/city2/zhuang5",
    ]));
    set("objects", ([
		"/d/city2/npc/dizi": 1,
    ]));
//    set("no_clean_up", 0);
    set("outdoors", "city2");
    setup();
    replace_program(ROOM); 
}


