
inherit ROOM;

void create()
{
	set("short", "戲院后台");
	set("long", @LONG
這里是戲院后台，几個戲子正在化妝。從北邊可以上台。
LONG );
        set("outdoors", "city2");
	set("exits", ([
  		"west" : "/d/city2/xiyuan",
  		"northup" : "/d/city2/stage",
	]));
        set("objects", ([
                "/d/city2/npc/xizi" : 2,
        ]));
//	set("no_clean_up", 0);
	setup();
}
