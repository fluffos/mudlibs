// Room: /city2/dangpu.c

inherit ROOM;

void create()
{
	set("short", "當鋪");
	set("long", @LONG
這是一家以買賣公平著稱的當鋪，一個五尺高的柜台擋在你的面前，
柜台上擺著一個牌子(paizi)，柜台后坐著賈老板，一雙精明的眼睛上上
下下打量著你。
LONG );
	set("no_fight", 1);
	set("no_beg",1);
	set("item_desc", ([
		"paizi" : "\n公平交易\n",
	]));
	set("objects", ([
		"/d/city2/npc/jia" : 1,
	]));
	set("exits", ([
		"east" : __DIR__"nandaj1",
	]));
//	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}

