//RJQLWCH.C

inherit ROOM;

void create()
{
	set("short", "練武場");
	set("long", @LONG
走進了銳金旗的練武場，發現這里有些奇怪。練武的教眾
們并不是在相互切磋，而是穿戴著特制的護具，同一架奇怪的
裝置打斗。裝置從不移開原地，只是它似乎具備用不完的各種
兵器，進攻起來絲毫不弱于江湖好手。
LONG );
	set("exits", ([
		"north" : __DIR__"rjqyuan",
	]));

	set("no_clean_up", 0);
//	set("outdoors", "mingjiao");
	setup();
	replace_program(ROOM);
}
