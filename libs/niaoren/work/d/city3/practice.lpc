// Room: practice
// Date: Feb.14 1998 by Java

#include <room.h>
inherit ROOM;

void create()
{
      set("short", "武館操練房");
      set("long", @LONG
這里擺滿了石鎖簸箕、大刀長劍、沙袋綁腿等，還有几個精心扎
制的橡皮人，是武館弟子日常練功的地方。
LONG );

      set("exits", ([ /* sizeof() == 1 */
          "down"   : __DIR__"wuguan",
      ]));
//      set("no_clean_up", 0);
      set("objects", ([
          __DIR__"npc/xiangpi-ren" : 5,
      ]));
      setup();
      replace_program(ROOM);
}
