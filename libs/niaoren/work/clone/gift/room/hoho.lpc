// square.c  LEOS給kid的練功房

inherit ROOM;

void create()
{
        set("short", "ㄚ信專用練武場");
        set("long", @LONG
這裡是LEOS給ㄚ信專用的練武場除了木人什麼東沒有。
LONG );
        set("exits", ([ /* sizeof() == 2 */
            "out"     :"/d/huashan/square",
        ]));
        set("objects", ([
            "/d/shaolin/npc/mu-ren" :35,
        ]));
        set("no_clean_up", 0);
        set("outdoors", "xx" );

        setup();
}
