// /clone/board/towiz_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("任務系統意見留言本",({"board"}) );
	set("location", "/d/jingzhou/guangchang");
	set("board_id", "quest_b");
	set("long","請玩家把對任務系統的意見在這里post出來。\n");
	setup();
	set("capacity", 30);
}

