// /clone/board/lpc_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("進度及修改情況記錄本", ({ "board" }) );
	set("location", "/d/wizard/lpcroom");
	set("board_id", "lpc_b");
	set("long", "請各位巫師把你做了的工作在這里公布出來。\n"+
		"加強巫師之間工作的一致性！\n" );
	setup();
	set("capacity",100);
}

