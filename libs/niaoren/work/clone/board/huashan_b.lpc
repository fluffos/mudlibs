// /clone/board/huashan_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("華山派弟子留言板", ({ "board" }) );
	set("location", "/d/huashan/buwei1");
	set("board_id", "huashan_b");
	set("long", "有有用的話就往這里說，亂灌水殺無赦。\n" );
	setup();
	set("capacity", 30);
}

