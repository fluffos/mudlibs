inherit BULLETIN_BOARD;

void create()
{
        set_name("星宿派弟子留言板", ({ "board" }) );
        set("location", "/d/xingxiu/xxh2");
        set("board_id", "xingxiu_b");
        set("long", "這是一個供星宿派弟子交流的留言板。\n" );
        setup();
        set("capacity", 50);
        replace_program(BULLETIN_BOARD);
}
