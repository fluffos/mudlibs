inherit BULLETIN_BOARD;

void create()
{
        set_name("顧客流言簿", ({ "board" }) );
        set("location", "/u/suzhou/kezhan");
        set("board_id", "suzhou_b");
        set("long", "這是一個專供游客使用的留言板。\n" );
        setup();
set("capacity",1000);
}
