// /clone/board/soo_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("風兒留言板", ({ "board" }) );
	set("location", "/u/winder/workroom");
	set("board_id", "soo_b");
	set("long", "這是一個留言板。\n" );
	setup();
	set("capacity", 100);
}

