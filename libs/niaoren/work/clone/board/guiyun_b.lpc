// /clone/board/taohua_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("歸云庄記事板", ({ "board" }) );
	set("location", "/d/guiyun/qianyuan");
	set("board_id", "guiyun_b");
	set("long", "這是一個專供桃花島弟子在島外使用的留言板。\n" );
	setup();
	set("capacity", 100);
}

