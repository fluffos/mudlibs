#ifndef FORMOSA_GLOBALS_H
#define FORMOSA_GLOBALS_H
/* Empty global include — FluffOS requires this path when the config
 * key is set so leftover scan_config_line buffers cannot inject a
 * bogus filename into every compilation unit. */
#endif
