环境变数

你可以用 set 指令设定一些有用的环境变数，这些变数也会随着你
的人物被储存下来，如果有不需要的环境变数，也可以用 unset 删
除。详细的使用方法请看 help set 及 help unset。

以下是目前有用的环境变数：

block_tell      <任何非零的值>  当你不愿意和别人talk时。

brief           <任何非零的值>  设定移动时只看所在地简短的
                                名称，如果您的网路速度不是
                                很快，或者是对区域已经十分
                                熟悉，建议您打开 brief模式
                                以减轻网路负担。

brief_message                   设定战斗时不显示成功的dodge
                                和parry，以减少不必要的增加
                                网路负担。

no_teach        <任何非零的值>  设定你现在不教你的徒弟技能，
                                由于教徒弟要花费一些精神，当
                                徒弟不乖的时候．．．

no_accept       <任何非零的值>　设定你现在不接受任何人的礼物。

public          <任何非零的值>  对别人公开你设定的 email 地址。

wimpy           <百分比>        当您的精气神低于这个百分比时
                                就会自动找机会逃命。

allow_map       <任何非零的值>  设定次项为关闭地图,想再次打开
                                想再次打开用unset allow_map

no_follow       <任何非零的值>  不让别人跟随你。

no_top          <任何非零的值>  不想让自己上榜。

last_action     <字符串>        与wimpy配合使用的设置，如果设置
                                该项， 则用该命令取代逃跑。
                                比如:set last_action quit
                                     wimpy 30
                                则当精气神低于30%时quit.
                                如果不想设置，则unset last_action
                                就可以了。
allowed_player <字符串>         允许该玩家删除你的okip。注意这样你就不受
                                 IP限制，能随便登陆。
