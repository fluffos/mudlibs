// by bugbug & alading @ 缥缈水云间, 2003-4-5
//changan city

inherit ROOM;
#include <ansi.h>
void create() {
  set("short", "宝贵丹药铺");
  set("long", @LONG
这就是传说中的宝贵丹药铺，相传这个铺子的后台老板是杨梅。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "out": __DIR__ "center",
    ]));

  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/doctor": 1,
    ]));


  //        set("outdoors", "changan");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
