// by bugbug & alading @ 缥缈水云间, 2003-4-5
inherit ROOM;

void create() {
  set("short", "美容院");
  set("long", @LONG

坐落在长安城内的一家超时空的现代化美容院，里面装备齐全，是你
美容花钱的好去处！小心点，别乱问，可是直接从存款里扣的！
相关指令：ask meirongshi about (割双眼皮,隆胸,拉皮,)
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "north": "/d/wiz/entrance",
  ]));
  set("objects", ([
    "/d/changan/npc/meirongshi": 1,
  ]));
  set("no_clean_up", 1);

  setup();
}
