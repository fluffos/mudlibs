#include "../path.h"

inherit "/world/agriculture/farm_room.lpc";
string buf;
create_farm()
{
  set_short("A farming plot");
  add_exit("south", ROOM_PATH + "road8.lpc");
  add_exit("north", ROOM_PATH + "mill_farm18.lpc");
}
