#include "../path.h"

inherit "/world/agriculture/farm_room.lpc";
string buf;
create_farm()
{
  set_short("A farming plot");
  add_exit("west", ROOM_PATH + "mill_farm9.lpc");
  add_exit("south", ROOM_PATH + "mill_farm12.lpc");
}
