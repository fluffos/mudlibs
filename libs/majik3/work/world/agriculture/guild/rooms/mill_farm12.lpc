#include "../path.h"

inherit "/world/agriculture/farm_room.lpc";
string buf;
create_farm()
{
  set_short("A farming plot");
  add_exit("west", ROOM_PATH + "mill_farm10.lpc");
  add_exit("north", ROOM_PATH + "mill_farm11.lpc");
}
