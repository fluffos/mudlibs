#include "food.h"

mapping food_info = ([
	"name" : "小笼包",
	"id" : "ksbc",
	"need" : (["zhurou" : 2, "xiaomai" : 5, ]),
	"time" : 300,
	"unit" : "笼",
	]);

void create()
{
    set("no_buff", 1);
	create_food(food_info);
}