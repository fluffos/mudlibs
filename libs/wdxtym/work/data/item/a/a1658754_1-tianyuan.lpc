// ITEM Made by player(渡唐:a1658754_1) /data/item/a/a1658754_1-tianyuan.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Tue Mar  7 15:58:57 2023
#include <ansi.h>
#include <weapon.h>

inherit SWORD;
inherit F_ITEMMAKE;

void create() {
  set_name("天元[2;37;0m", ({ "tianyuan" }));
  set_weight(9000);
  set("item_make", 1);
  set("unit", "柄");
  set("long", "这是由[1;35m神铁[2;37;0m炼制而成的一柄剑。
圆圆[2;37;0m
剑柄上刻着一行小字：渡唐(a1658754_1)
");
  set("value", 2700000);
  set("point", 286);
  set("material", "magic stone");
  set("wield_msg", "[1;36m$N[1;36m一声清啸，抖出一柄[1;36m$n[1;36m。[2;37;0m\n");
  set("unwield_msg", "一挥已入剑鞘[2;37;0m\n");
  if (!check_clone()) return;
  restore();
  init_sword(apply_damage());

  setup();
}

string long() { return query("long") + item_long(); }
