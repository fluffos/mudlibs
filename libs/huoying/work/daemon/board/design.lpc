inherit BULLETIN_BOARD;

void create() {
  set_name("设计讨论板", ({ "design board", "board" }));
  set("location", "/world/area/wizard/academy");
  set("board_id", "design");
  setup();
  set("capacity", 200);
  replace_program(BULLETIN_BOARD);
}
