inherit BULLETIN_BOARD;

void create() {
  set_name("闲聊留言板", ({ "chat board", "board" }));
  set("location", "/world/area/wizard/power");
  set("board_id", "chat");
  setup();
  set("capacity", 200);
}
