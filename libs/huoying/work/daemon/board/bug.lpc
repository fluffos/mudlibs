inherit BULLETIN_BOARD;

void create() {
  set_name("臭虫回报板", ({ "bug board", "board" }));
  set("location", "/world/area/wizard/bug");
  set("board_id", "bug");
  setup();
  set("capacity", 200);
}
