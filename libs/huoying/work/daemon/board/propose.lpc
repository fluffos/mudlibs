inherit BULLETIN_BOARD;

void create() {
  set_name("建议讨论板", ({ "propose board", "board" }));
  set("location", "/world/area/wizard/propose");
  set("board_id", "propose");
  setup();
  set("capacity", 200);
}
