// Modify By River 98/12
inherit ROOM;
#include <ansi.h>
#include <room.h>

void create()
{
	set("short", "走廊");
	set("long", @LONG
这是一条走廊，两侧雕栏画砌，盘龙描凤。处处显示出这里不是寻常的地
方。北边有一条小河，河上有座小桥，似乎通向后宫。
LONG
	);

	set("exits", ([
		"北-小桥" : __DIR__"xiaoqiao",
		"南-正厅" : __DIR__"huating",
                "西-大殿" : __DIR__"dadian",
                "东-厢房" : __DIR__"xiangfang", 
	]));
	set("coor/x",-310);
  set("coor/y",-410);
   set("coor/z",-20);
   setup();
}
