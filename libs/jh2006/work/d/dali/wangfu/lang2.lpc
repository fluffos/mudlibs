// Modify By River 98/12
inherit ROOM;

void create() {
  set("short", "长廊2");
  set("long", @LONG
这是镇南王府内的一条长廊，长廊一直通向王府内，廊边雕满了花边，透
过缕空的花纹，你可以看见廊外的树木花草。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "南-长廊4": __DIR__ "lang4",
    "西-王府大厅": __DIR__ "dating",
  ]));

  set("coor/x", -270);
  set("coor/y", -440);
  set("coor/z", -20);
  setup();
}
