// Room: /u/qingyun/jyzj/shanlu1.c
// Written by qingyun
// Updated by jpei

inherit ROOM;

void create()
{
	set("short", "山路1");
	set("long", @LONG
这是一条昆仑山上到处可见的山路。只是这里已随处可见梅树，梅花盛开
时到处一片火红，花香飘溢。
LONG	);
	set("exits", ([
		"西-山路2" : __DIR__"shanlu2",
		"东-昆仑山麓" : "/d/kunlun/klshanlu",
                "南下-雪地" : __DIR__"xuedi1",
	]));
	set("outdoors", "昆仑山");
	set("no_clean_up", 0);

	setup();
}
