// /u/dubei/miaojiang


inherit ROOM;

void create()
{
        set("short", "小镇");
        set("long", @LONG
这里快到镇中心了，路旁有一个小吃摊，一个苗族少女正在那里卖一些苗疆
特有的水果和小吃。
LONG
        );

        set("exits", ([
                "北-小镇" : __DIR__"jiedao1",
                 "南-小镇" : __DIR__"jiedao3",
                 "东南-药王居" : __DIR__"ywj",
                 "东-小摊" : __DIR__"xiaotan", 
        ]));
        set("objects",([
                __DIR__"npc/miaobing" : 1,

        ]));

        set("outdoors", "苗疆");

        setup();
}
