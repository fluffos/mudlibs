inherit ROOM;

void create()
{
	set("short", "石板路");
	set("long", @LONG
    你走在一条长长的石板路上，道旁古树参天，鸟鸣不绝，这条
路上冷冷清请，你走了好长时间，也看不见一个人。
LONG	);
	set("exits", ([ /* sizeof() == 2 */
   "西-灵塔林" : __DIR__"talin",
  "东-石板路" : __DIR__"shanlu-3",

   
]));
	set("no_clean_up", 0);
	set("outdoors", "tianlongsi");

	set("coor/x",-450);
  set("coor/y",-320);
   set("coor/z",30);
   setup();
}
