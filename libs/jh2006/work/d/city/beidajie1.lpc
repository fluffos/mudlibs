// Room: /d/city/beidajie1.c

inherit ROOM;

void create()
{
	set("short", "北大街1");
	set("long", @LONG
这是一条宽阔的青石街道，向南北两头延伸。北边是北城门通向城外。南
边显得很繁忙。东边是一家小吃店，店面窄小，恐怕只供应外卖。西边就是城
隍庙。
LONG
	);
        set("outdoors", "扬州");

	set("exits", ([
		"东-小吃店" : __DIR__"xiaochidian",
		"南-北大街2" : __DIR__"beidajie2",
                "西-城隍庙" : __DIR__"chmiao",
		"北-北门" : __DIR__"beimen",
	]));

	set("objects", ([
		CLASS_D("shaolin") + "/xingzhe" : 1
	]));
	set("coor/x",120);
  set("coor/y",20);
   set("coor/z",0);
   set("coor/x",120);
  set("coor/y",20);
   set("coor/z",0);
   setup();
}
