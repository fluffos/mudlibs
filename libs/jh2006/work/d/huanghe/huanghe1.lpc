// by xiaoyao

#include <room.h>

inherit ROOM;
void create()
{
	set("short", "黄河岸边1");
	set("long", @LONG
这里是黄河岸边。黄河流淌到这里，已经泥沙浑浊了。黄色的河水
在河床里翻滚着，咆哮着，卷起一个个巨大的漩窝。西边有个寨子。
LONG );
	set("exits", ([
		"南-田地4" : __DIR__"tiandi4",
		"北-渭汾流域" : __DIR__"weifen",
		"东-黄河岸边"  : __DIR__"huanghe2",
		"西-黄河帮寨门"  : __DIR__"huanghegate",
	]));
	set("objects", ([
                __DIR__"npc/hou" : 1,
	]));

	set("outdoors", "黄河");
	set("coor/x",120);
  set("coor/y",100);
   set("coor/z",0);
   set("coor/x",120);
  set("coor/y",100);
   set("coor/z",0);
   setup();
}

