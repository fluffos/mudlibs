// by XiaoYao

#include <room.h>

inherit ROOM;
void create()
{
	set("short", "田地2");
	set("long", @LONG
这里林木渐渐稀疏，不远处有一些村庄，炊烟袅袅升起。村庄周围
是一块块的田地，田里有一些正在耕作的农人。
LONG );
	set("exits", ([
		"西南-树林5" : __DIR__"shulin5",
		"东-田地1"      : __DIR__"tiandi1",
	]));
	set("objects", ([
		__DIR__"npc/cow" : 1,
	]));
	set("outdoors", "黄河");
	set("coor/x",90);
  set("coor/y",80);
   set("coor/z",0);
   setup();
}

