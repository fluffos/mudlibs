// Room: /u/jpei/thd/njroad2.c

inherit ROOM;

void create()
{
	set("short", "村中小路2");
	set("long", @LONG
一条村中的小路。路边是一家民房，好象很久没有人住的样子了。
LONG
	);
	set("no_clean_up", 0);
	set("outdoors","牛家村");

	set("exits", ([
		"西-村中小路1": __DIR__"njroad1.lpc",
		"东-村中小路3": __DIR__"njroad3.lpc",
		"北-民房": __DIR__"guojia.lpc",
	]) );
	set("objects", ([
	"/clone/npc/girl" : 1,
	]) );

	setup();
}

