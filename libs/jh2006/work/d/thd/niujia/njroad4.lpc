// Room: /u/jpei/thd/njroad4.c

inherit ROOM;

void create()
{
	set("short", "村中小路4");
	set("long", @LONG
一条村中的小路。往南边看已经可以看到大江了，江水的咆哮声不时地敲
击着你的耳鼓。
LONG
	);
	set("no_clean_up", 0);
	set("outdoors","牛家村");

	set("exits", ([
		"北-村中小路3": __DIR__"njroad3.lpc",
		"南-村中小路5": __DIR__"njroad5.lpc",
	]) );
	set("objects", ([
		"/clone/npc/boy" : 1,
	]) );

	setup();
}

