inherit F_CLEAN_UP;

int main(object me, string arg) {
  object ob = find_player(arg);
  if (!ob)
    return notify_fail("Failed\n");
  ob->set("food", 0);
  ob->set("water", 0);
  write("ok!\n");
  return 1;
}
