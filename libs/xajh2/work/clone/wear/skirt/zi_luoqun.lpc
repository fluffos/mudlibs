//hong lingqun.c
#include <armor.h>
#include <ansi.h>
inherit SKIRT;


void create() {
  set_name(MAG "紫罗裙" NOR, ({ "zi luoqun", "qunzi", "skirt" }));
  set_weight(1000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "条");
    set("value", 1000);
    set("material", "silk");
    set("armor_prop/armor", 1);
    set("armor_prop/dodge", 4);
    set("female_only", 1);
  }
  setup();
}
