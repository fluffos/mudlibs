// 汴京   /d/city/capital/lss_houyuan.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit BROOM;
void create()
{
	set("short",GRN "后院" NOR);
	set("long", @LONG
步入后院，山石池水的布置愈见可爱，几株牡丹娇艳地从山石之中伸出华贵
的花朵，更有说不出名的各种花草，散发着迷人的芬芳，让人飘飘欲仙。北面假
山后又有一座小楼，红柱白墙，甚是好看。
LONG
	);
	set("objects", ([ 
         ]));
	set("exits", ([  
	    "west" : __DIR__"lss_ningcui_tang", 
	    "north" : __DIR__"lss_xiaolu_ge",
	    "east" : __DIR__"lss_houmen",
         ]));     
    set("detail",([
    ]));          
    set("outdoors","capital");
	setup(); 
}
void init()
{
}
