// 汴京   /d/city/capital/junyi_qiao.c
#include <room.h>
#include <ansi.h>
#include "bianjing.h"
inherit STREET;
void create() {
  set("short", HIW "浚仪桥" NOR);
  set("long", @LONG
浚仪桥北接浚仪街，向北直通大内右腋门，那里有尚书省，御使台等等机关
过桥南去是一片纵横街巷，民舍店铺紧密相连。东去沿着汴河北岸不久就可到天
汉桥，西边是一条大街直通宜秋门。北面街东有一个大院，就是都亭驿。
LONG
  );
  set("objects", ([
  ]));
  set("exits", ([
    "northwest": __DIR__ "chengqing_jie2",
    "east": __DIR__ "tianhan_qiao",
    "northeast": __DIR__ "duting_yi",
  ]));
  set("detail", ([
  ]));
  setup();
}
void init() {}
