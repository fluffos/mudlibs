// 汴京   /d/city/capital/std.c
                               
#include "bianjing.h"
inherit BROOM;

#include <room.h>
#include <ansi.h>    

void create()
{
    set("short","");
    set("long", @LONG
    
LONG
    );
    set("objects", ([
        __DIR__"npc/,
    ]));

    set("detail",([
    ]));
    set("outdoors", "capital");
    setup();
}
