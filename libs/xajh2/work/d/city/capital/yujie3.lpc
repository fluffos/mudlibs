// 汴京   yujie3.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
	set("short",HIW "御  街" NOR);
	set("long", @LONG
自宣德门前往南，一条笔直的大道，宽二百余步，人称“天街”。大道正中
是御路，平时禁止行人往来穿行，御路两旁是两带深数尺宽一丈的御沟，灌流金
水河水，沟中种植了荷花无数，沟边更有桃梨海棠等各种花树，春香满街，姹紫
不断。大道两旁是千步廊，尽开些店铺茶肆，放与平民作买卖。道上人来人往，
车马喧沸，络绎不绝。
LONG
	);
	set("objects", ([        
         ]));
	set("exits", ([  
	    "north" : __DIR__"yujie2",
	    "south" : __DIR__"yujie4",
    ]));
    set("detail",([
    ]));
	setup();
}
void init()
{
}
