// 汴京   yujie6.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
	set("short",HIW "御  街" NOR);
	set("long", @LONG
朱雀门以南，宽阔的御街笔直地向南延伸，直到南熏门。两边是一个个整齐
的里坊，街巷井然。东边是一条巷子，里面就是太学了，有数千名学子在那里攻
读经书古文，学习各种文化知识。北面是蔡何湾。西边临街有一家豪华的酒店。
LONG
	);
	set("objects", ([        
         ]));
	set("exits", ([  
	    "north" : __DIR__"longjin_qiao",
	    "south" : __DIR__"yujie7",
        "west" : __DIR__"yuxian_dian",
        "east" : __DIR__"taixue",
    ]));
    set("detail",([
    ]));
	setup();
}
void init()
{
}
