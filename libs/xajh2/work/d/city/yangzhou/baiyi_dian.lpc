// Room: /d/city/yangzhou/baiyi_dian.c

inherit ROOM;
#include <ansi.h>



void create() {
  set("short", YEL "沈家白衣铺" NOR);
  set("long", @LONG
店内略有些小，几个人在试看衣服，就显得很拥挤。边上有个裁缝，正给一
小伙子量身材。一个中年男子拿了一套衣服正要去后边的更衣室试衣服。店虽小，
可店内陈列的衣服倒不少，各款式的男女成衣应有尽有。看来你是能在此挑选到
自己喜欢的衣服了。
LONG
  );
  set("no_clean_up", 0);
  set("no_kill", "yangzhou");

  set("objects", ([
    __DIR__ "npc/shen_wanfu": 1,
  ]));
  set("exits", ([ /* sizeof() == 3 */
    "north": __DIR__ "caiyi_dongjie",
  ]));
  setup();
  //place_program(ROOM);
}
