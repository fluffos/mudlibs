// /d/city/lanzhou/yipin_tang/npc/obj 入堂的介绍信

inherit ITEM;
void create() {
  set_name("入堂信", ({ "rutang xin", "letter", "xin" }));
  set("unit", "封");
  set("long", @LONG
这是一封赫连长在写给赫连云鹰的信件，上面用封条封得严严实实。
LONG);
  setup();
}
