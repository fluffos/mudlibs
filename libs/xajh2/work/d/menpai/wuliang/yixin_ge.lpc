inherit ROOM;


void create() {
  set("short", "怡心阁");
  set("long", @LONG
阁里到处被打扫得一尘不染，窗明几净，可以看到外面松竹掩映，时有微风
入户，门后桌上还燃着一炉檀香，氲氲的香气轻轻横散，使人俗念尽消，心情完
全平静下来。
LONG
  );
  set("exits",
    ([
      "east": __DIR__ "kefang",
    ]));
  setup();
  //place_program( ROOM );
}
