// room:/d/dali/wangfu_pailou2.c   writen by lala July-25-97

#include <room.h>
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "王府牌楼");
  set("long", @LONG
这是为了在建筑风格上同西面的那一座牌楼对称而修建的一座牌楼，同样高达
二丈，由黄玉制成，感觉分外巍峨庄严。你抬头望去，但见牌楼上四个大金字“国
家柱石”。西面就是王府大院了。
LONG
  );
  set("item_desc", ([
    "west": "只见一座雕刻着二龙抢珠的绿琉璃瓦朱红影壁墙严严实实地挡住了你的视线。\n",
  ]));
  set("exits", ([
    "west": __DIR__ "wangfu_dayuan",
  ]));
  set("objects", ([
    __DIR__ "npc/shinv": 2,
    __DIR__ "npc/shinv2": 2,
  ]));
  setup();
  //place_program(ROOM);
}
