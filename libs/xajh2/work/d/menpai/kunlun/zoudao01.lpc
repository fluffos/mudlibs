// zoudao01.c
// Ffox 98-4-13 20:06

#include "kl_r.h"


void create() {
  set("short", "走道");
  set("long", @LONG
一条走道，往北是女弟子卧室，来这里的人都轻手轻脚的，生怕打扰了别人
。但由于弟子们一向很刻苦，经行这里的人并不多，往南去就是回廊。
LONG);
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "nvsleep",
    "south": __DIR__ "huilang02",
  ]));

  set("objects", ([
    __DIR__ "npc/nvdizi": 2,
  ]));

  setup();
  //place_program(ROOM);
}
