// xiaolu01.c
// Ffox 98-4-13 19:55

inherit ROAD;


void create() {
  set("short", "小路");
  set("long", @LONG
小路是那样的清凉、湿润。它穿过草坪，那垂拂双肩的树枝和和打湿双脚的
草叶，为一种宁静所笼罩。听着从远处深深的竹林中传来的鹧鸪的鸣声，脚下的
小路明亮而弯转向北边的山脚处延伸过去。
LONG);
  set("kunlun/water", 1);
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "northwest": __DIR__ "quan",
    "south": __DIR__ "grass",
  ]));
  set("objects", ([
    __DIR__ "npc/watdizi": 1,
  ]));
  setup();
  //place_program(ROOM);
}
