// yaofang.c
// Ffox 98-4-13 20:20

#include "kl_r.h"


void create() {
  set("short", "药房");
  set("long", @LONG
一走进这里，一股浓郁的药味就扑鼻而来。如果你的鼻子灵的话，一定可以
闻出这气味里至少有数十种天然珍贵药物的气味，比如长白山的山参、天山的雪
莲、大兴安岭的熊胆……昆仑派自有其特别的药品。平常总有一个药师在这里制
药，难为他竟然能够忍受这里的这种气味，不过又说回来，与这许多的药物接触
，身体也自比常人结实。
LONG);
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "langdao01",
  ]));
  set("objects", ([
    __DIR__ "npc/yaotong": 1,
    __DIR__ "npc/yaoshi": 1,
  ]));
  setup();
  //place_program(ROOM);
}
