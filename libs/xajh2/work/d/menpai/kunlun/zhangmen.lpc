// zhangmen.c
// Ffox 98-4-13 20:22

#include "kl_r.h"


void create() {
  set("short", "掌门居室");
  set("long", @LONG
这里是掌门的住所，昆仑派如此一个大门派，掌门的住所确如此，没有看见
确实的话真是很难相信。这里只有一张桌、一把椅子、一张床，桌子上放着释、
道、儒等书各两三卷，桌边一个小香炉，飘着缕缕轻烟。
LONG);
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "langdao04",
  ]));
  add("search_things", ([
    "桌子": "/clone/book/taigong",
    "床": "/clone/book/shuowen",
  ]));
  set("objects", ([
    __DIR__ "npc/puren": 1 + random(1),
  ]));
  setup();
}
