// master.c 昆仑派掌门人。
// Ffox 98-1-17 9:12

inherit F_MASTER;
#include "kunlun_quest.h"

string *skill_name = ({
  "kunwu-jian", "xuanzhu-jian", "liangyi-jian",
  "baihe-quan", "panheng-zhang",
  "kunlun-bu", "yinzhi-bufa",
  "liangyi-xinfa"
});

string when(int timep) {
  int t, d, h, m, s;
  string time;
  t = timep;
  s = t % 60; t /= 60;
  m = t % 60; t /= 60;
  h = t % 24; t /= 24;
  d = t;
  if (d) time = chinese_number(d) + "天";
  else time = "";
  if (h) time += chinese_number(h) + "小时";
  if (m) time += chinese_number(m) + "分";
  time += chinese_number(s) + "秒";
  return (time);
}

mapping *go_where = ({
  ([
    "where": "厨房",
    "give_msg": "白鹿子对$N说道：听厨房大师傅说厨房现在缺人手，你去那儿帮忙吧。",
  ]),
  ([
    "where": "药房",
    "give_msg": "白鹿子对$N说道：听药房执事说药房现在缺人手，你去那儿帮忙吧。",
  ]),
  ([
    "where": "蔬圃",
    "give_msg": "白鹿子对$N说道：蔬圃现在正是忙的时候，你去帮帮忙吧。"
  ]),
});

string leave_kunlun(object);



void create() {
  set_name("白鹿子", ({ "bai luzi", "bai", "master" }));
  set("title", "昆仑派第三代掌门人");
  set("gender", "男性");
  set("age", 55);
  set("long", @LONG
昔日英雄，走遍关外，罕逢对手，曾生擒白虎于昆仑，为人耿直。得上代掌门之
真传，对弟子谆谆教导，昆仑派在他手下，如今是逐渐发扬光大。作为昆仑第三
代掌门人，此人名振江湖。武林中有谁不识？
LONG);
  set("attitude", "peaceful");
  set("positive_score", 30000);
  set("per", 40);
  set("str", 40);
  set("int", 40);
  set("con", 40);
  set("dex", 40);
  set("inquiry", ([
    "此地": (: about_here :),
    //    "出师" : (: leave_kunlun :),
  ]));
  set("chat_chance", 5);
  set("chat_msg", ({
    "白鹿子在大厅里面踱来踱去，看见你来了也没有怎么注意。\n",
    "白鹿子对你说道：昆仑派现在是一片昌盛的势头啊！\n",
    "白鹿子抚摩了一下神像，一副若有所思的样子。\n",
  }));

  create_family("昆仑派", 3, "掌门人");
  set_wugong("kunwu-jian", 120, 120, 1);
  set_wugong("xuanzhu-jian", 150, 150, 1);
  set_wugong("liangyi-jian", 150, 150, 1);
  set_wugong("baihe-quan", 120, 120, 1);
  set_wugong("panheng-zhang", 150, 150, 1);
  set_wugong("kunlun-bu", 120, 120, 1);
  set_wugong("yinzhi-bufa", 150, 150, 1);
  set_wugong("liangyi-xinfa", 120, 120, 1);
  set_wugong("literate", 150, 150, 0);
  set_xiuwei("sword", 5200);
  set_xiuwei("dodge", 3200);
  set_xiuwei("unarmed", 3200);
  set("max_force", 3000);
  set("force", 3000);
  set("force_factor", 240);
  set("combat_exp", 15000000);
  setup();
  carry_object("/clone/wear/cloth/cloth5")->wear();
  carry_object("/clone/weapon/sword/sword1")->wield();
}

void init() {
  add_action("do_qingjiao", "qingjiao");
  add_action("do_quest", "quest");

}


void attempt_apprentice(object ob) {
  if (ob->query_condition("kill_kunlun"))
    return;
  if (ob->query("gender") == "无性") {
    command("haha " + ob->query("id"));
    command("say 我昆仑派乃是江湖正派，怎么能收阉割之人啊！");
    return;
  }

  if (ob->query("family")) {
    command("sorry " + ob->query("id"));
    command("say 我昆仑派乃是江湖正派，不收来历不明的弟子。");
    return;
  }

  if (ob->query("have_enter_kunlun")) {
    command("angry " + ob->query("id"));
    command("say 你还想回到昆仑派来？");
    return;
  }
  if (ob->query("mmis/出师/昆仑派")) {
    command("heng " + ob->query("id"));
    command("say 你已经离开昆仑派，怎么还想回来不成？");
    return;
  }
  command("say 嗯，不错，不错，你很有眼光。\n"
    "            将来光大我昆仑派还需依靠你了。");
  if (ob->query("kunlun_task"))
    ob->delete("kunlun_task");
  if (ob->query("kunlun_work"))
    ob->delete("kunlun_work");
  if (ob->query("canyue"))
    ob->delete("canyue");
  command("recruit " + ob->query("id"));
  ob->delete("ffox");
  ob->set("class", "kunlun");
  ob->set("have_enter_kunlun", 1);
}

int do_qingjiao(string str) {
  object me = this_player();
  string skname, who;
  //  string strline,id1="",id2="",name;
  int i;
  int last_time = 0, now_time = 0;
  //  908193380
  if (!kl_dizi(me)) return 0;
  if (!str) return 0;
  if (sscanf(str, "%s from %s", skname, who) != 2)
    if (sscanf(str, "%s %s", who, skname) != 2) return 0;
  if (wizardp(me)) printf("who is " + who + "?\n");
  //    if( !present( who, environment(me) ) ) return 0;
  if (me->query("class_score/昆仑派") < 1200) {
    command("say 你还是多做一点事情吧，闲时可以去参阅一下典籍。");
    return 1;
  }
  /*
    if( !kl_mark( "query", me, "last_time", 1, 1 ) )
        kl_mark( "set", me, "last_time", time()-5000, 1 );
    if( time() - kl_mark( "query", me, "last_time", 1, 1 ) -3600 > 0 )
        return notify_fail( "白鹿子对你这几天的表现好象不太满意！\n" );
    if( wizardp(me) ) printf( "这是测试的断点。\n" );
*/
  last_time = kl_mark("query", me, "last_time", 1, 1);
  now_time = time();
  if ((now_time - last_time - 1800) > 0) {
    message_vision("白鹿子很疑惑地看了$N一眼，接着摇了摇头。\n", me);
    write("白鹿子对你这几天的表现好象不太满意！\n");
    write("你上次任务的时间已经是：" + when(now_time - last_time) + "以前了。\n");
    return 1;
  }
  for (i = 0; i < sizeof(skill_name); i++)
    if (skname == skill_name[i]) return 0;
  command("sorry " + me->query("id"));
  write("看起来他不想教你这门功夫。\n");
  return 1;
}


int do_quest(string str) {
  object me = this_player();
  string add_level = "canyue/level";
  mapping work;
  int level;
  int last_time;
  if (!kl_dizi(me)) return 0;
  if (kl_mark("query", me, "kunlun_task/where", 1, 1)) {           //如果有干活的地方
    if (kl_mark("query", me, "kunlun_task/success", 1, 1) >= 2) {    //如果成功点数为2奖励
      command("nod " + me->query("id"));
      command("say 听说你这一段时间在那边干得不错啊！");
      me->add("class_score/昆仑派",
        kl_mark("query", me, "kunlun_task/point", 1, 1));
      add_level += sprintf("%1d", kl_mark("query", me, "kunlun_task/level", 1, 1));
      kl_mark("add", me, add_level, 2, 1);
      kl_mark("add", me, "kunlun_task/level", 1, 1);
    } else if (kl_mark("query", me, "kunlun_task/success", 1, 1) <= -2) {
      command("shake " + me->query("id"));        //如果成功点数为-2…
      command("say 你真是个好吃懒做的家伙，这么点事都干不好。");
      me->add("class_score/昆仑派",
        kl_mark("query", me, "kunlun_task/point", 1, 1));
      kl_mark("add", me, "kunlun_task/level", -1, 1);
    } else {
      command("angry " + me->query("id"));
      command("say 不是让你到" +
        kl_mark("query", me, "kunlun_task/where", 1, 1) + "去帮忙吗？");
      return 1;
    }
    if (kl_mark("query", me, "kunlun_task/point", 1, 1) >= 0)
      write("你的师门忠诚度升高" + chinese_number(
        kl_mark("query", me, "kunlun_task/point", 1, 1)) + "点。\n");
    else write("你的师门忠诚度下降" + chinese_number(
      0 - kl_mark("query", me, "kunlun_task/point", 1, 1)) + "点。\n");
    kl_mark("delete", me, "kunlun_task/where", 1, 1);
    kl_mark("delete", me, "kunlun_task/point", 1, 1);
    me->delete("task_time");
    kl_mark("delete", me, "kunlun_task/success", 1, 1);
    me->delete("quest");
    last_time = time();
    kl_mark("set", me, "last_time", last_time, 1);
    return 1;
  }
  level = kl_mark("query", me, "kunlun_task/level", 1, 1);         //任务等级参数
  if (level <= 3) {
    if (level == 0) kl_mark("set", me, "kunlun_task/level", 1, 1);
    work = go_where[random(3)];
    kl_mark("set", me, "kunlun_task/where", work["where"], 1);
    message_vision(work["give_msg"] + "\n", me);
    me->set("quest/quest", "到" + work["where"] + "帮忙");
    me->set("task_time", time() + 500000);
    return 1;
  }
  last_time = time();
  kl_mark("set", me, "last_time", last_time, 1);
  if (!give_quest()) return 0;
  return 1;
}
/*
string leave_kunlun( object me )
{
    object this;
    me = this_player();
    this = this_object();

    if( me->query("mmis/出师/昆仑派"))
    {
        return "你不是已经离开我昆仑派了吗？";
    }
    if( me->query("family/family_name") != "昆仑派" )
    {
        command( "? "+me->query("id") );
        return "你好象不是我昆仑派弟子啊？";
    }
    if( me->query_temp("昆仑派/出师比划"))
    {
        return "我不是让你先跟为师我比划一下吗？";
    }
    
    if( me->query( "class_score/昆仑派" ) < 3000 )
    {
        message_vision(CYN"$N对$n道：“你在我门下多年，为昆仑派所做"
"未免太少了一些。”\n"NOR,this,me );
        if(me->query("class_score/昆仑派") > -50 )
            me -> add( "class_score/昆仑派",-10);
        return "你还是先为光大昆仑派多干点事吧！\n";
    }

    if( me->query_jibie("liangyi-xinfa") > 150 )
    {
        if( me->query("age") < 20 )
        {
            message_vision(CYN"$N对$n说道：“你年纪轻轻就出去，人家会以为我昆仑派"
"无人的。”\n"NOR,this,me);
            if(me->query("class_score/昆仑派") > -50 )
                me -> add( "class_score/昆仑派",-10);            
            return "再等两年再说吧。\n";
        }
        else
        {
            me->set_temp("昆仑派/出师比划",1);
            return "你先跟为师比划一下，让我看看你功夫如何了。";
        }
    }
    else
    {
        return "以你现在内功修为，难于在江湖上独挡一面，还是跟为师再学学吧！";
    }
    return "";
}        
*/
int accept_fight(object ppl) {
  object this;
  this = this_object();

  if (ppl->query_temp("昆仑派/出师比划")) {
    if (this->is_fighting()) {
      return notify_fail("现在白鹿子不能跟你比划。\n");
    }
    this->set("eff_kee", this->query("max_kee"));
    this->set("kee", this->query("max_kee"));
    this->set("eff_sen", this->query("max_sen"));
    this->set("sen", this->query("max_sen"));
    this->set("force", this->query("max_force"));
    this->set("force_factor", 200);
    this->set("force", 5000);
    this->set("max_force", 5000);
    this->set("env/attack", 75);
    message_vision(CYN "$N道，“好徒儿，进招吧。”\n", this);
    return 1;
  }
  return ::accept_fight(ppl);
}

int lose_bihua(object ppl) {
  object this;
  this = this_object();
  if (ppl->query_temp("昆仑派/出师比划")) {
    command("chat* fu " + ppl->query("id"));
    command("chat 昆仑派弟子" + ppl->query("name") + "(" + ppl->query("id") + ")" + "正式离开昆仑派。");
    command("say 真是青出于蓝胜于蓝啊，你的功夫已经比为师的高了。");
    command("say 以后行走江湖，你可不要危害武林啊。");
    ppl->delete_temp("昆仑派/出师比划");
    ppl->set("title", "普通百姓");
    ppl->delete("family");
    ppl->delete("class");
    ppl->delete("class");
    ppl->set("mmis/出师/昆仑派", 1);
    LOG_D->do_log(ppl, sprintf("你打败白鹿子，获得白鹿子的允许，从昆仑派出师。\n"));
    log_file("chushi", sprintf("%s(%s)于%s顺利从昆仑派出师。\n", ppl->query("name"),
      getuid(ppl), ctime(time())));
    message_vision("$N恭恭敬敬地跪了下来，向$n磕了九个响头。\n", ppl, this);
  }
  return 1;
}

int win_bihua(object ppl) {
  object this;
  this = this_object();
  if (ppl->query_temp("昆仑派/出师比划")) {
    command("pat " + ppl->query("id"));
    command("say 看你学业不精，还是好好再和为师多多学习啊！");
    ppl->delete_temp("昆仑派/出师比划");
    if (ppl->query("class_score/昆仑派") > -50)
      ppl->add("class_score/昆仑派", -10);
  }
  return 1;
}
