// long shu shu ding : /d/shaolin/longshu_shuding.c

inherit ROOM;



void create() {
  set("short", "龙树树顶");
  set("long", @LONG
这里是龙树的树顶，从这里往下看，可以将整个龙树院尽收眼底。树顶的一
株枝桠上有一个很大的鸟窝。你听见背后有一阵翅膀拍打的声音，似乎有一双鸟
眼从身后注视着你。
// xli 1999.4.6 QC 至此... 谁的鸟眼? :)
LONG
  );
  set("exits", ([
    "down": __DIR__ "longshu_shushao",
  ]));

  set("objects", ([
    __DIR__ "npc/heiying": 1,
  ]));

  setup();
}
