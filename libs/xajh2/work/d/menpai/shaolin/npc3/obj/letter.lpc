// letter.c
//by hydra 19980514

inherit ITEM;



void create() {
  set_name("推荐信", ({ "tuijian xin" }));
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一封推荐信。\n");
    set("unit", "封");
  }
  setup();
}
