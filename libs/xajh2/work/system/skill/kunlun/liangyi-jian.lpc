// liangyi-jianfa
//  两仪剑法



inherit SKILL;
mapping *action = ({
  ([ "action":
    "$N将手中$w急速亮出，使出一招「无风无浪」，往前似若有无向$n的肩头销了过去。",
    "lian":
    "$N将手中$w急速亮出，使出一招「无风无浪」，往前似若有无地销了过去。",
    "sen": 10,
    "jibie": 15,
    "dodge": 30,
    "parry": 20,
    "zhaoshi": "「无风无浪」",
    "damage": 75,
    "wound": 60,
    "damage_type": "割伤" ]),
  ([ "action":
    "$N手中$w耀出点点剑光，正是一招「半雨半晴」，向$n迎头罩下。",
    "lian":
    "$N手中$w耀出点点剑光，正是一招「半雨半晴」。",
    "sen": 20,
    "jibie": 30,
    "dodge": 40,
    "parry": 10,
    "zhaoshi": "「半雨半晴」",
    "damage": 90,
    "wound": 75,
    "damage_type": "割伤" ]),
  ([ "action":
    "$N奋力吼出一声，使出一招「壮气蒿莱」，举起手中$w，向$n猛刺过去。",
    "lian":
    "$N奋力吼出一声，使出一招「壮气蒿莱」，举起手中$w，向前猛刺过去。",
    "sen": 30,
    "jibie": 45,
    "dodge": 20,
    "parry": 20,
    "zhaoshi": "「壮气蒿莱」",
    "damage": 105,
    "wound": 85,
    "damage_type": "割伤" ]),
  ([ "action":
    "$N一个猛回头，正是一招「秦娥望月」，反手一剑向$n的右腿奋力刺去。",
    "lian":
    "$N一个猛回头，正是一招「秦娥望月」，反手一剑向下奋力刺去。",
    "sen": 40,
    "jibie": 60,
    "dodge": 40,
    "parry": 30,
    "zhaoshi": "「秦娥望月」",
    "damage": 110,
    "wound": 90,
    "damage_type": "割伤" ]),
  ([ "action":
    "$N脸现悲凄之神态，竟是一招「霸陵伤别」，一股气势逼人的剑气向$n扑头而去。",
    "lian":
    "$N脸现悲凄之神态，竟是一招「霸陵伤别」，一股气势逼人的剑气向周围散开。",
    "zhaoshi": "「霸陵伤别」",
    "sen": 50,
    "jibie": 75,
    "dodge": 10,
    "parry": 60,
    "damage": 120,
    "wound": 100,
    "damage_type": "割伤"
  ]),
});

string *parry_msg = ({
  "$n使出一招「玉阶空伫立」，$n的这一招就差一点而没有击中$N。。\n",
  "$n一招「西风残照」，一柄$w尽数封死$N的招式。。\n",
  "$n手握$w，一股剑气，将$N逼开。。\n",
  "$n一个转身，跃起三丈高，避开了$N的这一招。。\n",
});

string *unarmed_parry_msg = ({
  "$n使出一招「玉阶空伫立」，$n的这一招就差一点而没有击中$N。。\n",
  "$n一招「西风残照」，双掌飞舞，尽数封死$N的招式。。\n",
  "$n以掌当剑，横切一掌，将$N逼开。。\n",
  "$n一个转身，跃起三丈高，避开了$N的这一招。。\n",
});

int valid_learn(object me) {
  //    object ob;
  if ((int)me->query("max_force") < 1000)
    return notify_fail("你的内力不够，没有办法练两仪剑法。\n");
  return 1;
}

int valid_lian(object me) {
  object weapon;
  weapon = me->query_temp("weapon");
  if (!weapon || weapon->query("weapon_type") != "sword")
    return notify_fail("你必须先找一把剑才能练习剑法。\n");
  if ((string)me->query("using_force") != "liangyi-xinfa")
    return notify_fail("两仪剑法必须靠两仪心法来的支持才能练习，否则容易入魔。\n");
  return 1;
}
string query_xiuwei_type() { return "sword"; }
string query_weapon_type() { return "sword"; }

int query_dengji() { return 6; }

mapping query_action(int jibie) {
  int i, size;

  size = sizeof(action);
  for (i = 0; i < sizeof(action); i++)
    if (action[i]["jibie"] > jibie) {
      size = i;
      break;
    }
  return action[random(size)];
}

string query_parry_msg(object weapon) {
  if (weapon)
    return parry_msg[random(sizeof(parry_msg))];
  else
    return unarmed_parry_msg[random(sizeof(unarmed_parry_msg))];
}

string query_zhaoshi(int jibie) {
  int i;
  for (i = 0; i < sizeof(action); i++) {
    if (action[i]["jibie"] >= jibie)
      return action[i]["zhaoshi"];
  }
  return "";
}

int query_youxiao_jibie() { return 11; }

int query_parry_ratio() { return 10; }
