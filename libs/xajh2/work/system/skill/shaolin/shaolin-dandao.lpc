//shaolin-dandao.c 少林单刀  二级刀 
//by wyh


inherit SKILL;

mapping *action = ({
  ([ "action":
    "$N手中$w横推，一招「傲睨自若」，由上至下向$n$l砍去",
    "lian":
    "$N手中$w横推，一招「傲睨自若」，由上至下割下",
    "zhaoshi": "傲睨自若",
    "dodge": 5,
    "sen": 2,
    "jibie": 0,
    "parry": 8,
    //                "force":                 10,
    "damage": 10,
    "damage_type": "割伤",
  ]),
  ([ "action":
    "$N一招「八面玲珑」，刀锋自下而上划了个半弧，$w一提一收，平刃挥向$n的颈部  ",
    "lian":
    "$N一招「八面玲珑」，刀锋自下而上划了个半弧，$w一提一收，平刃挥出",
    "zhaoshi": "八面玲珑",
    "dodge": 10,
    "parry": 18,
    //                "force":                 20,
    "sen": 1,
    "jibie": 8,
    "damage": 15,
    "damage_type": "割伤",
  ]),
  ([ "action":
    "$N使出一招「进退维谷」，将$w舞得如白雾一般压向$n ",
    "lian":
    "$N使出一招「进退维谷」，将$w舞得如白雾一般劈出",
    "zhaoshi": "进退维谷",
    "dodge": 13,
    "sen": 2,
    "parry": 12,
    "jibie": 15,
    //                "force":                 30,
    "damage": 20,
    "damage_type": "割伤",
  ]),
  ([ "action":
    "$N一招「避实击虚」，$w缓缓的斜着向$n推去 ",
    "lian":
    "$N一招「避实击虚」，$w缓缓的斜着推出",
    "zhaoshi": "避实击虚",
    "sen": 3,
    "jibie": 25,
    "dodge": 15,
    "parry": 30,
    //                "force":                 30,
    "damage": 20,
    "damage_type": "割伤",
  ]),
  ([ "action":
    "$N手拖$w，转身跃起，一招「拨乱反正」，一道白光射向$n的胸口 ",
    "lian":
    "$N手拖$w，转身跃起，一招「拨乱反正」，一道白光射出",
    "zhaoshi": "拨乱反正",
    "dodge": 10,
    "sen": 3,
    "jibie": 34,
    "parry": 8,
    //                "force":                 60,
    "damage": 50,
    "damage_type": "割伤",
  ]),
  ([ "action":
    "$N挥舞$w，使出一招「白鹤舒翅」，上劈下撩，左挡右开，齐齐罩向$n  ",
    "lian":
    "$N挥舞$w，使出一招「白鹤舒翅」，上劈下撩，左挡右开",
    "zhaoshi": "白鹤舒翅",
    "dodge": 10,
    "parry": 9,
    //                "force":                20,
    "sen": 2,
    "jibie": 45,
    "damage": 30,
    "damage_type": "割伤",
  ]),
});

string *parry_msg = ({
  "$n一招「方兴未艾」，手中$w架住了$N的所有攻势.\n",
  "$n身形飘出，一招「改弦更张」躲开了$N的所有攻势.\n",
  "$n见招架不即，一招「推襟送抱」，手中$w化作一片刀光硬封了出去.\n",
});

int valid_learn(object me) {
  if (me->query_jibie("buddhism") < 30)
    return notify_fail("你的佛学修为不够，强练绝技会伤及身体。\n");
  if ((int)me->query("max_force") < 100)
    return notify_fail("你的内力不够,无法练少林单刀！\n");
  return 1;
}

int valid_lian(object me) {
  object ob = me->query_temp("weapon");

  if (!objectp(ob) || (string)ob->query("weapon_type") != "blade")

    return notify_fail("你必须先找一把刀才能练！\n");
  return valid_learn(me);
}

mapping query_action(int jibie) {
  int i, size;
  size = sizeof(action);
  for (i = 0; i < sizeof(action); i++)
    if (action[i]["jibie"] > jibie) {
      size = i;
      break;
    }
  return action[random(size)];
}

string query_zhaoshi(int jibie) {
  int i;
  for (i = 0; i < sizeof(action); i++) {
    if (action[i]["jibie"] >= jibie)
      return action[i]["zhaoshi"];
  }
  return "";
}

string query_xiuwei_type() {
  return "blade";
}

string query_weapon_type() {
  return "blade";
}

int query_dengji() {
  return 2;
}

string query_parry_msg(object weapon) {
  return parry_msg[random(sizeof(parry_msg))];
}

int query_youxiao_jibie() {
  return 7;
}

int query_parry_ratio() {
  return 6;
}
