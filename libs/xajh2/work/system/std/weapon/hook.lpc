// hook.c

#include <weapon.h>

inherit WEAPON;

varargs void init_hook(int damage, int wound, int flag) {
  init_weapon(damage, wound, flag | EDGED | SECONDARY);
  set_weapon_type("hook");
  set("skill_type", "short");
  set("verbs", ({ "impale", "point" }));
}
