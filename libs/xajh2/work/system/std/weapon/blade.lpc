// blade.c

#include <weapon.h>

inherit WEAPON;

varargs void init_blade(int damage, int wound, int flag) {
  init_weapon(damage, wound, flag | EDGED);
  set_weapon_type("blade");
  set("skill_type", "blade");
  set("verbs", ({ "slash", "slice", "hack" }));
}
