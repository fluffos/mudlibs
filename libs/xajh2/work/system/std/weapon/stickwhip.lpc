// filename : /std/weapon/stickwhip.c

#include <weapon.h>

inherit WEAPON;

varargs void init_stickwhip(int damage, int wound, int flag) {
  init_weapon(damage, wound, flag | EDGED);
  set_weapon_type("stickwhip");
  set("skill_type", "whip");
  set("verbs", ({ "whip", "slam" }));
}
