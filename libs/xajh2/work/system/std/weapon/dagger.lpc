// dagger.c

#include <weapon.h>

inherit WEAPON;

varargs void init_dagger(int damage, int wound, int flag) {
  init_weapon(damage, wound, flag | EDGED | SECONDARY);
  set("skill_type", "short");
  set_weapon_type("daggar");
  set("verbs", ({ "slice", "pierce", "thrust" }));
}
