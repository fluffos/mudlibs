#include <comando.h>
#include <daemons.h>

int comando(int argc, string *argv) {

  if (argc != 2) {
    notify_fail("Sintaxis: upload [origen] [destino]\n");
    return 0;
  }


  //efun::call_other(DOMINIOS, "domainlord_q",  **del dominio** ) != TP->nombre_q()

  return 1;
}
