#include <comando.h>

int comando(int argc, string *argv) {

  printf(
    "Puntos de Vida: %d/%d  Energia: %d/%d\n",
    TP->pv_q(),
    TP->pv_max_q(),
    TP->pe_q(),
    TP->pe_max_q()
  );
  return 1;
}
