#include <comando.h>
#include <tablas.h>

int comando(int argc, string *argv) {

  write("Necesitas " + efun::call_other(
    TABLA_COSTES,
    "coste_q",
    TP->nivel_q() + 1
  ) + " puntos de experiencia para avanzar a nivel " + (TP->nivel_q() + 1) + ".\n");
  return 1;
}
