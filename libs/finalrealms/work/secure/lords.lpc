/* FluffOS PORT NOTE: same gap as secure/gods.lpc's query_gods() -- master.
 * lpc's create2() also calls query_lords() here unconditionally. No lord
 * names survive anywhere in this archive (the commented-out example in
 * query_boo() below is a template, not lost content), so this returns an
 * empty array -- a correct, content-neutral completion of the function
 * signature master.lpc already expects, not invented content. */
string *query_lords() { return ({ }); }

string query_boo(string str);

string query_boo(string str)
{
  /*
 switch(str)
 {
  case "example":
      return "of Something";
  default:
    return 0;
 }
   */
    return 0;
}  
