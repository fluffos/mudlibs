// Room: /adm/guild/guildhall.c

inherit ROOM;

void create() {
  set("short", "巫师公会");
  set("long", @LONG
这里就是巫师公会了，一个半球型的大浮岛漂浮在七彩的云间，平
坦的地面铺著一层碧绿草皮，草地尽头是一栋纯白的大型建筑，希腊式
的圆柱跟波斯风格的圆顶，营造出一种庄严的气氛，往北是开会用的会
议厅，东边是研究 LPC 的巫师学院，往西则是通往世界各地的传送站　
LONG
  );

  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "meetingroom",
    "east": __DIR__ "academy"
  ]));

  setup();
  load_object("/daemon/board/wizard");
}
