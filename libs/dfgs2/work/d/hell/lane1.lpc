// Room: /d/hell/lane1.c

inherit ROOM;

void create() {
  set("short", "阴暗小巷");
  set("long", @LONG
一条阴暗的小巷子　和鬼城其他地方相比　这个巷子才是真正的「
阴暗」　你几乎必须摸索著前进　往东似乎亮了一点　虽然在鬼城你看
不到几处光亮　.
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "square8",
  ]));
  set("objects", ([
    __DIR__ "npc/demogorgon": 1
  ]));

  setup();
  replace_program(ROOM);
}
