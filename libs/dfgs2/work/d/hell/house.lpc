// Room: /d/hell/house.c

#include <room.h>

inherit ROOM;

void create() {
  set("short", "普华精舍");
  set("long", @LONG
这是一间相当整洁的精舍　桌椅摆设都打扫得一尘不染　北边的一
张矮桌上供著一尊地藏菩萨神像　神像前烧著檀香，旁边的柜子上整齐
地堆著许多经书，往南是精舍的出口　
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "square4",
  ]));
  set("objects", ([
    __DIR__ "npc/bonze": 1
  ]));

  setup();
  create_door("south", "大门", "north", DOOR_CLOSED);
}
