// Room: /d/hell/city3.c

inherit ROOM;

void create() {
  set("short", "酆都城");
  set("long", @LONG
这里是酆都城的街道，你的北边不远处有一个广场，广场上有座三
层楼高的石台，台前列著一队鬼卒把守，你的东西两边各有一条小巷子
通往两边的屋舍之间，酆都城的城门就在你的南边远处　
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "square1",
    "south": __DIR__ "city2",
  ]));

  setup();
  replace_program(ROOM);
}
