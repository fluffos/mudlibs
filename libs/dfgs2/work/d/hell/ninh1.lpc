// Room: /d/hell/ninh1.c

inherit ROOM;

void create() {
  set("short", "奈何桥上");
  set("long", @LONG
这里就是有名的奈何桥了，从这里你可以听到桥下血水滚滚流过的
声音，以及千万条怨毒大蛇吐信的嘶嘶声，你最好小心一点，掉下去可
是会永不超生的，你的北边一片浓雾中有一幢城楼的影子，往南则是你
来的黄泉路　
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "ghost_gate",
    "south": __DIR__ "death_start",
  ]));

  setup();
}
