// Room: /d/snow/npath2.c

inherit ROOM;

void create() {
  set("short", "溪边小路");
  set("long", @LONG
这里是一条溪边的小路，从这里可以望见北边的山间一道瀑布从崖
上垂下，穿过山坡上的一片树林，以及不远处的草地，往你的东南边流
过，溪水冲激著溪床的岩石，发出轰轰的响声，往西沿著小路可以回到
雪亭镇的大街，小路向东南可以通往溪边　
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "southeast": __DIR__ "npath3",
    "west": __DIR__ "npath1",
  ]));
  set("outdoors", "snow");

  setup();
}
