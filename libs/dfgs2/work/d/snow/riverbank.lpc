// Room: /d/snow/riverbank.c

inherit ROOM;

void create() {
  set("short", "河边空地");
  set("long", @LONG
这里是河边的一处空地，湍急的河流从你的北边往东南边流去，离
这里的河岸往北一丈多远的地方可以看见一个水车，南边则是一间破旧
的大宅院，墙壁上一个大缺口足足可以让人通过　
LONG
  );
  set("outdoors", "snow");
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "ruin2",
  ]));

  setup();
}
