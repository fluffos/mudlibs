// Room: /d/snow/herb_shop.c

inherit ROOM;

void create() {
  set("short", "小药铺");
  set("long", @LONG
这里是一间小药铺，连像样一点的招牌都没有，只在门边挂著一块
不甚起眼的木牌，上面写著「顺生堂」三个字，老旧的柜台後面陈列著
一列药柜，药柜的每个抽屉按药铺的规矩都没有任何标签，西面的墙上
吊著几束晒乾的甘草，像这种小药铺没有钱烧檀香祛除药味，只好用甘
草替代　
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "epath",
  ]));
  set("objects", ([
    __DIR__ "npc/herbalist": 1,
  ]));

  setup();
}
