// Room: /d/snow/square_e.c

inherit ROOM;

void create() {
  set("short", "广场");
  set("long", @LONG
这里是雪亭镇广场东边，往东是一条僻静的巷子，西边有一株高大
的榕树，南边是一家打铁铺子，不过店门不在这一边　
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "epath",
    "west": __DIR__ "square",
  ]));
  set("outdoors", "snow");

  setup();
  replace_program(ROOM);
}
