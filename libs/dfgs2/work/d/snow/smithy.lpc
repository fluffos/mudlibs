// Room: /d/snow/smithy.c

inherit ROOM;

void create() {
  set("short", "打铁铺子");
  set("long", @LONG
这里是一间打铁铺子，墙上挂了许多铁器的半成品跟工具，铺子中
间一个烧著熊熊火焰的火炉映得你在墙上的影子一晃一晃的，旁边还有
一个沈重的大铁砧，铺子的出口就在东边　
LONG
  );
  set("detail", ([ /* sizeof() == 2 */
    "铁砧": "这个大铁砧看起来十分沈重，上面满是敲打的痕迹　
",
    "铁器": "这些铁器有锅子　铲子　铁　　耕田用的锄头　犁等等　
",
  ]));
  set("no_clean_up", 0);
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/smith": 1,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "square_s",
  ]));

  setup();
}
