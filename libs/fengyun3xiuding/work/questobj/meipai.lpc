#include <ansi.h>
#include <armor.h>

inherit ITEM;

void create()
{
	set_name(CYN "梅牌" NOR, ({ "meipai"}) );
	set_weight(1);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("long", "西方魔教的信物。\n");
		set("unit", "块");
		
		
	}
	setup();
}

