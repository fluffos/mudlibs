inherit CMD;

private void main( mixed* arg )
{
  object target;
  string target_name;

  target = arg[0];
  target_name = target->query_name();

  target->init_stats();

  out("Branin's first command. Duck, it might not work.");

  return;
}
