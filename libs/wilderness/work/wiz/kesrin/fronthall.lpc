/* fronthall.c
** Coded by Kesrin@Wilderness
** Code generated with RoomMaker v0.9.1 coded by Onyx@RedDragon
** Do not remove this header.
*/


inherit ROOM;

void setup(){
    set_light(1);
    set_brief("Kesrin's House -- Front Entryway");
    set_long("This is the front entryway to a small house.  A small area directly in front of a swirling portal on the south wall seems to be set aside for people to remove their shoes.  To the east, a training area of some sort can be seen, and beyond that, a walled-in garden.  Directly ahead (to the north) sleeping quarters can be seen.");
    set_exits( (["east" :  "/wiz/kesrin/dojo.lpc", 
                 "north" : "/wiz/kesrin/kesrbed.lpc"]) );
    set_objects( (["/std/portal.lpc" : ({ "/wiz/branin/workroom.lpc", "A swirling maelstrom of color.  From somewhere inside a meeping snake can be heard."})
    ]) );
}

