/* kesrbed.c
** Coded by Kesrin@Wilderness
** Code generated with RoomMaker v0.9.1 coded by Onyx@RedDragon
** Do not remove this header.
*/


inherit ROOM;

void setup(){
    set_light(1);
    set_brief("Kesrin's Bedroom");
    set_long(
"This small room is covered in thin mats of some fibrous material.  A closet
on the far wall sits open, revealing a rolled-up pad and a pillow.  Some
clothes also hang in the closet.  "
);

    add_item("closet", "The closet is small, and contains merely a thin pad, apparently used for sleeping, a pillow, and some clothes.\n");

    add_item("pad", "The sleeping pad appears to be filled with feathers of some sort, and while soft, wouldn't do much to shield the user from the uncomfortable floor\n");

    add_item("pillow", "Just a simple pillow, filled with some something resembling wheat chaff.\n");
    
    add_item("clothes", "Several robes and simple cloth pants hang here, all apparently sewn by the same hand, judging by the craftsmanship.\n");

    add_item("mats", "Some sort of fibrous material forms these mats, which are rectangular in shape and tiled to cover the wooden floor.\n");

    set_exits( ([
"south" :  "/wiz/kesrin/fronthall.lpc"
    ]) );
    set_objects( ([
    ]) );
}
