
inherit WEAPON;

void setup()
{
  set_id("zweihander");
  add_id("sword");
  set_adj("flaming", "fiery", "large");
  set_proper_name("a flaming two-handed zweihander");
  set_combat_messages("weapon");
  set_weapon_skill("edged");
  set_in_room_desc("A large flaming two-handed zweihander lies on the ground.");
  set_long("A long sword of unknown metal. Posessing a double ring guard with fluted grooves running down the blade, the sword is a work of art. The handle is wrapped in ivory, and the hil appears to be made out of a hard golden metal. The ring guards are polished silvery bright, and you cannot bear to start too long at the blade. You cannot tell if the blade is simply flaming metal, or made entirely out of fire.");
  set_dmg( ([ "edged" : ({ 4, 10, 5 }),
	    "fire"  : ({ 4, 10, 10 })
	    ])
	   );
  set_size(MEDIUM);
  set_weight(200);
}

