
inherit WEAPON;

void setup()
{
  set_id("zanza");
  add_id("sword");
  set_adj("zanza", "stone");
  set_proper_name("a gigantic stone zanza sword");
  set_combat_messages("weapon");
  set_weapon_skill("edged");
  set_in_room_desc("A gargantuan stone sword with a thick wooden handle lies on the floor.");
  set_long("The sword is tremendous, the blade is made entirely out of stone. A thick wooden handle runs out the base of the blade. The entire weapon creaks when it is shifted.");
  set_dmg( ([ "edged" : ({ 5, 12, 1 })
	    ])
	   );
  set_size(MEDIUM);
  set_weight(900);
}

