/* Do not remove the headers from this file! see /USAGE for more info. */

inherit WANDERING_MONSTER;
inherit M_BLOCKEXITS;

void setup()
{
    set_name("the demon");
    set_id("demon");
    set_gender(1);
    set_proper_name("a demon wrapped in hellfire");
    set_in_room_desc("A large demon with bat-wings and wrapped in flames roars at you.");
    set_long("The demon is tall yet not thin. A thin coat of muscles over its tall and lanky frame give it the appearance of weakness, but you know it is just a sham. Filled with the powers of the Underworld, the demon possesses a strength that mortal men can only hope to compare.");

    set_max_health(42);
    set_wander_time(5);
    set_wander_area("wiz_area");

    set_skills( ([ "hand to hand":40,
		 "dodge":20,
		 "claw":40,
		 ])
		);
    set_attributes( 80, 75, 35, 50, 80, 15, 20, 75, 90);
    set_dmg( ([ "edged" : ({ 1, 6, 0 }) ]) );
    set_weapon_skill("claw");
    
	     
}
