
inherit WEAPON;

void setup()
{
  set_id("dagger");
  set_adj("rondel", "steel", "shiny");
  set_proper_name("a shiny steel rondel dagger");
  set_combat_messages("piercing");
  set_weapon_skill("piercing");
  set_in_room_desc("
  set_long("The broadsword gleams with reflected light. The weapon is made entirely of steel. Both double ring guards on the hilt are crafted and engraved with swirling motifs of ivy leaves. Made entirely out of steel, this weapon appears to be both functional, and a beautiful work of art.");
  set_dmg( ([ "edged" : ({ 4, 10, 2 })
	    ])
	   );
  set_size(MEDIUM);
  set_weight(300);
}

