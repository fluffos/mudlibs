
inherit WEAPON;

void setup()
{
  set_id("mace");
  add_adj("black", "steel");
  set_proper_name("a black steel mace");
  set_combat_messages("blunt");
  set_weapon_skill("blunt");
  set_in_room_desc("A flanged black steel mace lies on the ground.");
  set_long("An otherwise normal mace. However, it appears to made out of a strange black metal, much like steel. The metal is dull, and unreflective. You feel a strange hum if you place the mace close to your body.");

  set_dmg( ([ "blunt" : ({ 3, 12, 4 }),
	    "electricity" : ({ 3, 10, 5 }),
	    ])
	   );
  set_size(MEDIUM);
  set_weight(400);
}

