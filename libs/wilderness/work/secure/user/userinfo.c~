/* Do not remove the headers from this file! see /USAGE for more info. */

/*
** userinfo.c -- user information
**
** 950821, Deathblade: created.
*/

#include <config.h>
#include <mudlib.h>
#include <security.h>

void sw_body_handle_new_logon();
object query_body();
void save_me();

varargs void modal_push(function input_func,
                        mixed prompt,
                        int secure,
                        function return_to_func);
varargs void modal_func(function input_func,
                        mixed prompt,
                        int secure);
void modal_pop();

nomask void set_stat(string stat, int value);

/* states */
#define GENDER_QUERY		0
#define GOT_GENDER		1
#define GOT_EMAIL		2
#define GOT_REAL_NAME		3
#define GOT_URL			4
#define GOT_STR                 5
#define GOT_AGI                 6
#define GOT_SPD                 7
#define GOT_END                 8
#define GOT_NTL                 9
#define GOT_INS                10
#define GOT_WIT                11
#define GOT_WIL                12

private string 	email;
private string 	real_name;
private int 	ed_setup;
private string  url;
private int     str;
private int     agi;
private int     spd;
private int     end;
private int     ntl;
private int     ins;
private int     wit;
private int     wil;

//temporary new user vars
nosave private int n_gen = -1;

//### wah! get rid of this. need by sw_body.c; should move to NEW_USER_D
protected nomask int query_n_gen()
{
   return n_gen;
}

nomask void set_ed_setup(int code)
{
    ed_setup = code;
    save_me();
}

nomask int query_ed_setup()
{
    return ed_setup;
}

//### ACK!  should disappear. need something more secure than this
nomask string query_email()
{
    return email;
}

nomask void set_real_name(string new_name)
{
   if(this_user() != this_object())
      error("illegal attempt to set real name\n");

   real_name = new_name;
   save_me();
}

nomask void set_email(string new_email)
{
   if(this_user() != this_object())
	error("illegal attempt to set email address\n");

    email = new_email;
    save_me();
}

nomask void set_url(string new_url)
{
   if(this_user() != this_object())
	error("illegal attempt to set URL\n");

    url = new_url;
    save_me();
}

nomask string query_url()
{
    return url;
}

protected nomask int int_parse(string input) {

  int strsize = strlen(input);
  int value;
  int power;
  for(int i = 0; i<strsize; i++) {
    if((input[i]) != 58) {
      power = 1;
      for(int j = 0; j < (strsize - i - 1); j++)
	power *= 10;
      value += ((input[i] - 48) * power);
    }
    write("Value is " + value + "\n");
  }

  return value;
}

protected nomask varargs
void userinfo_handle_logon(int state, mixed extra, string arg)
{
  int value;
   switch(state)
   {
      case GENDER_QUERY:
         modal_push((: userinfo_handle_logon, GOT_GENDER, 0 :),
                       "Are you male or female? ");
         break;

      case GOT_GENDER:
         arg = lower_case(arg);
         if(arg == "y" || arg == "yes")
         {
            write("Ha, ha, ha. Which one are you?\n");
            return;
         }
         if(arg == "n" || arg == "no")
         {
            write("Well, which one would you have liked to be, then?\n");
            return;
         }
         if(arg == "f" || arg == "female")
            n_gen = 2;
         else if(arg != "m" && arg != "male")
         {
            write("I've never heard of that gender.  Please try again.\n");
            return;
         }
         else
         {
            n_gen = 1;
         }

	 write("\nCharacter attributes.  You have a total of 400 points to "
	       + "spend.  Each attribute (there are 8) must be between 30 "
	       + "and 70.");
	 /*	 modal_func((: userinfo_handle_logon, GOT_STR, 0 :),
                  "Strength: ");
	 break;
	 
   case GOT_STR:
     value = int_parse(arg);
     if(value < 30 || value > 70) {
       write("Please choose a number that is in bounds.\n");
       modal_func((: userinfo_handle_logon, GOT_STR, 0 :),
		  "Strength: ");
     }
     else {
       this_object()->set_stat("str", arg);
       str = value;
       write("You have " + (400 - str) + " points left.\n");
       modal_func((: userinfo_handle_logon, GOT_END, 0 :),
		  "Endurance: ");
     }
     break;
   case GOT_END:
     value = int_parse(arg);
     if(value < 30 || value > 70) {
       write("Please choose a number that is in bounds.\n");
       modal_func((: userinfo_handle_logon, GOT_END, 0 :),
		  "Endurance: ");
     }
     else {
       this_object()->set_stat("end", arg);
       end = value;
       write("You have " + (400 - str - end) + " points left.\n");
       modal_func((: userinfo_handle_logon, GOT_AGI, 0 :),
		  "Agility: ");
     }
     break;

   case GOT_AGI:
     value = int_parse(arg);
     if(value < 30 || value > 70) {
       write("Please choose a number that is in bounds.\n");
       modal_func((: userinfo_handle_logon, GOT_AGI, 0 :),
		  "Agility: ");
     }
     else {
       this_object()->set_stat("agi", arg);
       agi = value;
       write("You have " + (400 - str - end - agi) + " points left.\n");
       modal_func((: userinfo_handle_logon, GOT_SPD, 0 :),
		  "Speed: ");
     }
     break;

   case GOT_SPD:
     value = int_parse(arg);
     if(value < 30 || value > 70) {
       write("Please choose a number that is in bounds.\n");
       modal_func((: userinfo_handle_logon, GOT_SPD, 0 :),
		  "Speed: ");
     }
     else {
       this_object()->set_stat("spd", arg);
       spd = value;
       write("You have " + (400-str-end-agi-spd) + " points left.\n");
       modal_func((: userinfo_handle_logon, GOT_NTL, 0 :),
		  "Intelligence: ");
     }
     break;

   case GOT_NTL:
     value = int_parse(arg);
     if(value < 30 || value > 70) {
       write("Please choose a number that is in bounds.\n");
       modal_func((: userinfo_handle_logon, GOT_NTL, 0 :),
		  "Intelligence: ");
     }
     else {
       this_object()->set_stat("ntl", arg);
       ntl = value;
       write("You have " + (400-str-end-agi-spd-ntl) + " points left.\n");
       modal_func((: userinfo_handle_logon, GOT_INS, 0 :),
		  "Insight: ");
     }
     break;

   case GOT_INS:
     value = int_parse(arg);
     if(value < 30 || value > 70) {
       write("Please choose a number that is in bounds.\n");
       modal_func((: userinfo_handle_logon, GOT_INS, 0 :),
		  "Insight: ");
     }
     else {
       this_object()->set_stat("ins", arg);
       ins = value;
       write("You have " + (400-str-end-agi-spd-ntl-ins) + " points left.\n");
       modal_func((: userinfo_handle_logon, GOT_WIT, 0 :),
		  "Wits: ");
     }
     break;

   case GOT_WIT:
     value = int_parse(arg);
     if(value < 30 || value > 70) {
       write("Please choose a number that is in bounds.\n");
       modal_func((: userinfo_handle_logon, GOT_WIT, 0 :),
		  "Wits: ");
     }
     else {
       this_object()->set_stat("wit", arg);
       wit = value;
       write("You have " + (400-str-end-agi-spd-ntl-ins-wit) + 
	     " points left.\n");
       modal_func((: userinfo_handle_logon, GOT_WIL, 0 :),
		  "Willpower: ");
     }
     break;

   case GOT_WIL:
     value = int_parse(arg);
     if(value < 30 || value > 70) {
       write("Please choose a number that is in bounds.\n");
       modal_func((: userinfo_handle_logon, GOT_WIL, 0 :),
		  "Willpower: ");
     }
     else {
       this_object()->set_stat("wil", arg);
       wil = value;
     }

     if((str+end+agi+spd+ntl+ins+wit+wil) != 400) {
       write("Your attributes do not add to 400.  Try again, genius.\n");
       modal_func((: userinfo_handle_logon, GOT_STR, 0 :),
		  "Strength: ");
     }
     else {
     /*
       write("\n"
       "The following info is only seen by you and administrators\n"
       "  if you prepend a # to your response.\n"
       "\n"
       "You cannot gain wizard status without valid responses to these questions:\n");
       
       modal_func((: userinfo_handle_logon, GOT_EMAIL, 0 :),
		  "Your email address: ");

break;
   case GOT_EMAIL:
         email = arg;
         modal_func((: userinfo_handle_logon, GOT_REAL_NAME, 0 :),
                       "Your real name: ");
         break;

      case GOT_REAL_NAME:
         real_name = arg;

         modal_func((: userinfo_handle_logon, GOT_URL, 0 :),
                       "Your home page address (if any): ");
         break;

      case GOT_URL:
         url = arg;

         /*
         ** Done with this series of inputs
         */
         modal_pop();

         /*
         ** Let's move on to introducing the character to the mud.
         */
         if(file_size(NEW_PLAYER) <= 0)
         {
            sw_body_handle_new_logon();
            return;
         }

         more_file(NEW_PLAYER, 0, (: sw_body_handle_new_logon :));
   }
}

