FIXME in file /secure/simul_efun/objcalls.c line 79:

 The member_array(item, users()->query_body()); crap is to workaround
 a parser bug in MudOS v22.2a21.

Code:
deep_useful_inv_parser_formatted(object ob)
{
    mixed ret = ({});
    object *inv;
