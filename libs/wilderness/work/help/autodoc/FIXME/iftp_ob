FIXME in file /obj/mudlib/iftp_ob.c line 92:

 validate the dir?

Code:
	    remote_dir = args;
	    printf("New dir: %s\n", args);
	}
	break;
FIXME in file /obj/mudlib/iftp_ob.c line 107:

 validate the dir?

Code:
	    this_user()->query_shell_ob()->set_pwd(args);
	    printf("New dir: %s\n", args);
	}
	break;
