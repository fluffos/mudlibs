FIXME in file /secure/user/failures.c line 63:

 hmm... this count is total, not since last login

Code:
    printf("You had " +
	   M_GRAMMAR->number_of(count, "failed login attempt") +
	   " since your last login.\n");

