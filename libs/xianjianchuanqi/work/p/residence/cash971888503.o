// a skeleton for user rooms
inherit ROOM;
void create()
{
set("short", "仙界总行兵器库");
set("long", @LONG
这是海洋银行的兵器库 各种宝物应有尽有 如果想要这里没有的宝物可以问员工拿

LONG
);
        set("exits", ([
"east" : "/p/residence/cash971888263.c",
]));
set("objects", ([
	"/clone/weapon/changjian":1,
       ]) );
	set("owner", "cash");
	set("class", "仙界总行　");
	set("no_fight",1);

setup();
}
