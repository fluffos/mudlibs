// a skeleton for user rooms
inherit ROOM;
void create()
{
set("short", "仙界总行休息室");
set("long", @LONG
这里是海洋银行的休息室 在这里你可以放心睡觉 练功 没有人敢来杀你

LONG
);
        set("exits", ([
"west" : "/p/residence/cash971888263.c",
]));
set("objects", ([
       ]) );
	set("owner", "cash");
	set("class", "仙界总行　");
	set("no_fight",1);
	set("sleep_room", "1");

setup();
}
