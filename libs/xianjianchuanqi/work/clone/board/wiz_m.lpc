// /clone/board/wiz_b.c

inherit BULLETIN_BOARD;

void create()
{
    set_name("会议记录簿", ({ "board" }) );
	set("location", "/d/wizard/wizard_meet");
	set("board_id", "wiz_m");
    set("long", "这是一本专供巫师使用的会议记录本。\n");
	setup();
	set("capacity", 60);
	replace_program(BULLETIN_BOARD);
}

