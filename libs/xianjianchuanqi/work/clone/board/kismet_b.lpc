inherit BULLETIN_BOARD;

void create() {
  set_name("命之烙印", ({ "board" }));
  set("location", "/u/kismet/workroom");
  set("board_id", "kismet_b");
  set("long", "这是一个供来到天命居的贵宾交流的留言板。\n");
  setup();
  set("capacity", 100);
  replace_program(BULLETIN_BOARD);
}
