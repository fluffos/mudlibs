inherit BULLETIN_BOARD;

void create()
{
	set_name("远野奈津子", ({ "board" }) );
	set("location", "/u/explorer/workroom");
	set("board_id", "niushi_b");
	set("long", "She's pretty, and has a big breast; she's sexy,
she's cute! come to http://dynamix.lachesis.com to have a look!" );
	setup();
	set("capacity", 60);
	replace_program(BULLETIN_BOARD);
}
