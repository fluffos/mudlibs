// Room: /open/zijinggong/3.c / zijinggong
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "[1;34m雷电门[0;37;0m");
  set("long", @LONG
这里是雷电门堂主雷护法的休息室.墙上挂着一副草书,硕大的一个“雷”字历历在目。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": "/open/zijinggong/group",
  ]));
  set("group", "zijinggong");

  setup();
  replace_program(ROOM);
}
