// Room: /open/zijinggong/8.c / zijinggong
#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "厢房");
	set("long", @LONG
这里一间专门为客人安排的房间，屋里空气清新，光线良好，墙角是一张床
屋中间放着一张桌子，桌子上放了几本书。
LONG
	);
	set("group", "zijinggong");
	set("exits", ([ /* sizeof() == 1 */
  "east" : "/open/zijinggong/5",
]));

	setup();
	replace_program(ROOM);
}
