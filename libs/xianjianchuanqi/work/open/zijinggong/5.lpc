// Room: /open/zijinggong/5.c / zijinggong
#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "[1;35m紫云轩[0;37;0m");
	set("long", @LONG
紫云阁是宫主休息的地方，也是帮派的禁地，是帮中最重要的地方，大家都要
全力保护这里，保护宫主的安全，(squall)是这里的长客，北面的墙上挂了着一大
幅他的画像(huaxiang)。
LONG
	);
	set("exits", ([ /* sizeof() == 6 */
  "down" : "/open/zijinggong/group",
  "west" : "/open/zijinggong/8",
  "south" : "/open/zijinggong/9",
  "up" : "/open/zijinggong/10",
  "north" : "/open/zijinggong/6",
  "east" : "/open/zijinggong/7",
]));
	set("group", "zijinggong");
	set("item_desc", ([ /* sizeof() == 2 */
  "huaxiang" : "身高1.75 ，他看起来约十多岁，他的武艺看上去空前绝后，出手似乎极重。",
  "squall" : "一个不错的wiz，人很好",
]));

	setup();
	replace_program(ROOM);
}
