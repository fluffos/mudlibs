// Room: /open/zijinggong/9.c / zijinggong
#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "小竹门");
	set("long", @LONG
这是一扇半掩着的竹门，北面是宫中要地，南面是宫主常爱去的花园。
LONG
	);
	set("group", "zijinggong");
	set("exits", ([ /* sizeof() == 2 */
  "south" : "/open/zijinggong/11",
  "north" : "/open/zijinggong/5",
]));

	setup();
	replace_program(ROOM);
}
