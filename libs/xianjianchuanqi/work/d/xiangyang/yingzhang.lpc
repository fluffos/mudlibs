// /d/xiangyang/junying.c
inherit ROOM;
void create()
{
        set("short", "营帐中");
        set("long", @LONG
    这是蒙古攻击襄阳的临时军营，营帐中有许多大将、谋士
走来走去，大声的谈论、争执。
LONG
        );
        set("outdoors", "襄阳");

        set("exits", ([
                "out" : __DIR__"junying",
        ]));
             set("objects",([
//                 "u/benlong/npc/weishi" : 3,       
 ]));

        setup();
        replace_program(ROOM);
}
