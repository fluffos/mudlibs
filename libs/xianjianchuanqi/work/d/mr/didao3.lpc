// by skydream
#include <ansi.h>
#include <room.h>
inherit ROOM;
void create()
{
               set("short",BLU"慕容地道"NOR);
         set("long",@long
这里是只有慕容弟子能够进入的地道。
long);
          set("exits",([
             "south" : "/d/mr/didao2",
]));
          setup();
                replace_program(ROOM);
}
