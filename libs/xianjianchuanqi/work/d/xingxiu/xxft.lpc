 // xxft.c 厨房

#include <ansi.h>
inherit ROOM;


void create ()
{
  set ("short", "[1;31m厨房[0;37;0m");
  set ("long", @LONG
你刚刚走进这里，一阵阵食物的香气穿来，想不到星宿里也有这样的美味
这里的美味都是由厨子做出来的!你要吃饭必须经过他同意,但是因为这些都是
很珍贵的美味佳肴可不要随便浪费。
LONG);

  set("no_sleep_room", 1);
  set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/chuzi.lpc" : 1,
]));
  set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"xx3.lpc",
]));
  set("no_fight", 1);

  setup();
  replace_program(ROOM);
}
