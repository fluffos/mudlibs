inherit ROOM;

void create()
{
        set("short", "新闻发布室");
        set("long", @LONG
这里就是「仙剑狂侠」巫师的发布新闻的所在，是伟大的刷刷神仙做的!
除了一个发布用的留言板，什么都没有。
LONG );

        set("exits", ([ /* sizeof() == 1 */
                "south" : __DIR__"wizard_room",
        ]));
        set("no_fight", "1");
        set("no_clean_up", 0);
        setup();

        call_other("/clone/board/news_b", "???");
        replace_program(ROOM);
}


