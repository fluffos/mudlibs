// /d/mingjiao/zhoulang7.c
// Jay 4/3/97 -qingyun
#include <room.h>
#include <ansi.h>
inherit ROOM;
void create()
{ 
       set("short","走廊");
       set("long", @LONG
这是一条走廊。一边是青砖墙壁，一边是古木雕成的围栏虽是普通过道，
却也隐射出江湖第一大教制端严。西边是狮王殿，南面通往锐金旗，东南边
是教内监狱入口。
LONG);    
	set("exits", ([		
		"south" : __DIR__"wu4",				
		"north" : __DIR__"jinqi",
		"northeast" : __DIR__"rukou",
	]));             
	set("objects",([
		__DIR__"npc/peng" : 1,
	]));    
        set("outdoors", "明教光明顶");     
        setup();
        replace_program(ROOM);
}
