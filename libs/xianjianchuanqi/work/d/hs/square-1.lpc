// square-1.c
// by KEN.Q.Z.ZHANG
// last update 02/23/1997

inherit ROOM;

void create()
{
	set("short", "山路");
	set("long", @LONG
这是崎岖不平一条山路，甚是难走，东南的方向是练武场。西北的方向通往山上。
LONG	);
	set("exits", ([ /* sizeof() == 2 */
		"southeast"  : __DIR__"square",
		"northwest"  : __DIR__"square-2"
	]));
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	setup();
}