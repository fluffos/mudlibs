// yunu-siguo-1.c
// by KEN.Q.Z.ZHANG

inherit ROOM;

void create() {
  set("short", "山路");
  set("long", @LONG
这条里通往是思过崖的一条小山路。
LONG);
  set("exits", ([ /* sizeof() == 3 */
    "northup": __DIR__ "yunu-siguo-2",
    "southdown": __DIR__ "yunu"
  ]));
  set("outdoors", "huashan");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
