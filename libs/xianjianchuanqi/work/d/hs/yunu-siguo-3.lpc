// yunu-siguo-3.c
// by KEN.Q.Z.ZHANG

inherit ROOM;

void create()
{
	set("short", "山路");
	set("long", @LONG
这里是一条陡峭的小窄道，南面是通往玉女峰。再往上走就到了玉女峰的绝顶。
LONG	);
	set("exits", ([ /* sizeof() == 3 */
		"northup"		: __DIR__"yunu-siguo-4",
		"southdown"	: __DIR__"yunu-siguo-2"
	]));
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}