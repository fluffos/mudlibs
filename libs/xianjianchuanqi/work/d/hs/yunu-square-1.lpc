// yunu-1.c
// by KEN.Q.Z.ZHANG

inherit ROOM;

void create()
{
	set("short", "山路");
	set("long", @LONG
但见山势险峻，树木清幽，鸟鸣嘤嘤，流水淙淙，四五座粉墙大屋依着山坡或高或
低的构筑。南面是练武场。
LONG	);
	set("exits", ([ /* sizeof() == 3 */
		"north" : __DIR__"yunu",
		"south" : __DIR__"square",
	]));
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}