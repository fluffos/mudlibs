// square-2.c
// by KEN.Q.Z.ZHANG

inherit ROOM;

void create()
{
	set("short", "小路");
	set("long", @LONG
山路渐渐的平坦开来，走起来没有那么累了。
LONG	);
	set("exits", ([ /* sizeof() == 2 */
		"northeast"  : __DIR__"square-3",
		"southeast"  : __DIR__"square-1"
	]));
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	setup();
}