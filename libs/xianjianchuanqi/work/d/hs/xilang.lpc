// Room: xiaozhu-cookroom.c
// by KEN.Q.Z.ZHANG

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIW "西廊" NOR);
  set("long", @LONG
走廊西边传来阵阵饭菜香气，不问可知那儿便是华山派的厨房。
LONG);
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "xiaozhu",
    "west": __DIR__ "chufang",
  ]));
  set("no_clean_up", "0");
  setup();
}
