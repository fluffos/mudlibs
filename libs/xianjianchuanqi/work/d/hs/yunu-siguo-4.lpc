// yunu-siguo-4.c
// by KEN.Q.Z.ZHANG

#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "峭壁");
	set("long", @LONG
这里非常的陡峭，一不小心很容易滑到崖谷之中。再往上走就是思过崖。
LONG	);
	set("exits", ([ /* sizeof() == 3 */
		"up"	: __DIR__"siguo",
		"southdown"	: __DIR__"yunu-siguo-3"
	]));
	set("outdoors", "huashan" );
	set("no_clean_up","0");
	setup();
}

