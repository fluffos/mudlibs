// yunu-siguo-2.c
// by KEN.Q.Z.ZHANG

inherit ROOM;

void create()
{
	set("short", "山路");
	set("long", @LONG
山势越来越陡峭，这条山路也越来越崎岖。
LONG	);
	set("exits", ([ /* sizeof() == 3 */
		"northup"	: __DIR__"yunu-siguo-3",
		"southdown"	: __DIR__"yunu-siguo-1"
	]));
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}