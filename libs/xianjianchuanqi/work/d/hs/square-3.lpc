// square-3.c
// by KEN.Q.Z.ZHANG

inherit ROOM;

void create()
{
	set("short", "小路");
	set("long", @LONG
路面很平坦，走起来很轻松，不一会儿，北面隐约看到有几个房间，西南面是通往
练武场的小山路。
LONG	);
	set("exits", ([ /* sizeof() == 2 */
		"southwest"  : __DIR__"square-2",
		"northup"  : __DIR__"xiaozhu"
	]));
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	setup();
}