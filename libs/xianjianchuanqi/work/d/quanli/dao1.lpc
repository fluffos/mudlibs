// Room: /d/quanli/road.c

inherit ROOM;
#include <command.h>
void create()
{
	set("short", "孤岛");
	set("long", @LONG
看起来这里就是传说中的权力岛了，前面是一片茂密的树林。
LONG
	);
	
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"dao2",

]));
set("no_magic",1);
set("no_dazuo",1);

set("outdoors", "quanli");

        set("coor/x",0);
	set("coor/y",12200);
	set("coor/z",0);
	setup();
	
}
void init()
{	
	add_action("do_dive","dive");}
int do_dive()
{
	object me;
	object room;
me = this_player();
	message_vision("$N一个纵身，潜入湖中．．．\n",me);
	room = load_object(__DIR__"lake1");
	me->move(room);
	return 1;	
}
