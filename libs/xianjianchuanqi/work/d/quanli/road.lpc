// Room: /d/quanli/road.c

inherit ROOM;

void create() {
  set("short", "权力之路");
  set("long", @LONG
从这儿一直往北走，就能到权力帮了。权力帮是天下第一大帮,很少人有不知道的。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 1 */
    "south": "/d/laowu/street",
    "north": __DIR__ "a",
  ]));
  set("coor", ([ /* sizeof() == 3 */
    "z": 0,
    "y": 12060,
    "x": 0,
  ]));

  setup();
  replace_program(ROOM);
}
