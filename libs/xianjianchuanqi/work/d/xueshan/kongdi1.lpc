// /d/xueshan/kongdi1
// by dubei
 
inherit ROOM;

void create()
{
        set("short","石洞");
        set("long",@LONG 
这里是一个宽敞的石洞，最里有一块青石，正好可以横卧一人，
青石旁边有一些饮水用具。石壁上面刻着一些文字。
LONG);
        set("exits",([
                "out": __DIR__"kongdi",

        ]));
              set("objects",([
                "/kungfu/class/xueshan/laozu" : 1,
        ]));
        setup(); 
        replace_program(ROOM);

}
 
 