inherit ROOM;

void create()
{
	set("short", "密道");
	set("long", @LONG
这里是密道，前面隐约能看见亮光，听见一些声音，看样子到洞口了。
你走到洞口前，立刻看见了熟悉的道路。
LONG
	);
        set("outdoors", "dali");

	set("exits", ([
		"enter" : __DIR__"shanlu3",
        "out" : __DIR__"midao4",
	]));

	setup();
	replace_program(ROOM);
}
