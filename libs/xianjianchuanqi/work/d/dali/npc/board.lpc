// common_b.c

inherit BULLETIN_BOARD;

void create()
{
    set_name("留言板", ({ "board" }) );
    set("location", "/u/b/bbb/workroom");
    set("board_bbb", "common_bbb");
    set("long", "这是杀手用来请大家提意见的留言板。\n" );
	setup();
	set("capacity", 30);
	replace_program(BULLETIN_BOARD);
}
