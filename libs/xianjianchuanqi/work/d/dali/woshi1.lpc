inherit ROOM;

void create() {
  set("short", "卧室");
  set("long", @LONG
这里是镇南王的卧室，房子不大，床上被子叠的整整齐齐，窗边有
张小桌，放着几本书。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "lang5",
  ]));

  setup();
  replace_program(ROOM);
}
