#include <room.h>

inherit ROOM;

void create()
{
	set("short", "兵器房");
	set("long", @LONG
这是物品房，堆满了杂物，但是房子里并不杂乱，王府内的仆人
收拾的井井有调。房子角放着几把长剑。
LONG
	);

        set("exits", ([
		"west" : __DIR__"lang6",
	]));

	set("objects", ([
                __DIR__"obj/chanjian" : 2,
	]) );

        setup();
}

