//shuifang.c

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", HIY"水房"NOR);
        set("long", @LONG
一进屋，你看见屋内放着几口大缸（gang），好象是装水用的，不过缸似
乎都是空的。这里空荡荡的没有一个人，可能水房管事暂时离开了。
LONG
        );
        
	set("item_desc",([
               "gang" :"一口水缸，但里面空荡荡的一滴水也没有。\n",
	]));

        set("exits", ([
               "west" : __DIR__"shilu2",               
        ]));
        
//      set("objects", ([
//           __DIR__"npc/muren" : 1,
//      ]));

        setup();
}
