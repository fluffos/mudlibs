// Room: /d/city/paimaiwu
inherit ROOM;

void create ()
{
  set ("short", "拍卖屋");
  set ("long", @LONG
这是扬州的拍卖屋
LONG);

  set("no_magic", 1);
  set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"yueqidian",
]));
  set("no_fight", 1);
  set("objects", ([ /* sizeof() == 1 */
  "/paimai2/paimaishi" : 1,
]));

  setup();
  replace_program(ROOM);
}
