// Room: /d/gumu/shitou.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create() {
  set("short", "[1;36m石室[2;37;0m");
  set("long", @LONG
此地供各弟子打坐静修之用。地上仅有几个蒲团，几个古墓弟子正在闭目
领悟。正面的石墙(qiang)上刻着不少文字, 仔细一看，原来都抄录着四书五
经等古籍，用来给古墓弟子提高修养。
LONG
  );
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "obj/ping": 1,
  ]));
  set("exits", ([ /* sizeof() == 3 */
    "north": __DIR__ "lgf",
    "west": __DIR__ "gmqs",
    "south": __DIR__ "yaofang",
  ]));
  set("item_desc", ([ /* sizeof() == 2 */
    "west": (: look_door, "west" :),
    "qiang": "墙上写满了各种古籍文字，似乎可以读(read1)啊。
",
  ]));

  setup();
  replace_program(ROOM);
}
