//room: /d/city2/test01.c
// ahxia 2000.12.20
inherit ROOM;
void create()
{                                 
    set("short", "考场");
    set("long", @LONG
这里是单人考场，可以通过kaoshi要求发题，用answer来回答。
LONG );
    set("exits", ([
	"out" : __DIR__"test00",
      ]) );
//set("no_magic",1);
    set("objects", ([
	__DIR__"npc/kaoguan": 1,
      ]));
    setup();
}

/*
void init()
{
    add_action("do_ban","ansuan");
}

int do_ban(string arg)
{
    write("这里不允许暗算他人耶～～还是好好考试吧。\n");
    return 1;
}
*/
