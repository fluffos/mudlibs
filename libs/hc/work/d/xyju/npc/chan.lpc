#include <ansi.h>
inherit NPC;
void create() {
  set_name("蝉", ({ "chan" }));
  set("level", 5);
  set("race", "野兽");
  set("age", 4);
  set("long", "这是一只叽叽叫的蝉。\n");
  set("limbs", ({ "头部", "身体" }));
  set("verbs", ({ "bite", "poke" }));
  setup();
}
