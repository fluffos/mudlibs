// /u/swordman/user/unpc/link.c
//created by pl2npc.c

inherit NPC;

void create()
{
    set_name("莫千军", ({ "npc link" }) );
    set("gender", "男性");
    set("age", 14);
    set("mud_age", 4985);
    set("birthday", 979983855);
    set("combat_exp", 170452);
    set("potential", 1000);

    set("qi", 100);
    set("eff_qi", 100);
    set("max_qi", 100);

    set("food", 256);
    set("water", 236);

    set("title", "逍遥派第四代弟子");

    set("jing", 100);
    set("eff_jing", 100);
    set("max_jing", 100);


    set("str", 22);
    set("int", 26);
    set("con", 10);
    set("dex", 22);
    set("kar", 28);
    set("per", 19);

    set("shen", -2538000);

    set("family", ([ /* sizeof() == 7 */
  "generation" : 4,
  "title" : "弟子",
  "privs" : 0,
  "master_name" : "薛慕华",
  "enter_time" : 980052502,
  "family_name" : "逍遥派",
  "master_id" : "xue muhua",
]));
    set("party", ([ /* sizeof() == 2 */
  "rank" : "[1;37m罚恶使者[2;37;0m",
  "party_name" : "[1;37m第一代[2;37;0m",
]));

    set("MKS", 74);
    set("DIED", 1);

//set skills
    set_skill("unarmed", 2 );
    set_skill("sword", 7 );

    setup();
    if (query("gender") == "男性")
        carry_object("/clone/cloth/male"+(1+random(8))+"-cloth")->wear();
    else
        carry_object("/clone/cloth/female"+(1+random(8))+"-cloth")->wear();
}
