inherit ROOM;

void create() {
  set("short", "怒蛟殿");
  set("long", @LONG
这里是怒蛟帮的大殿，感觉上设计有些独特，使你一走进来感到一种庄严。
大殿显得十分宽敞，不时有人进出，却异常安静，没有一丝喧哗的声音。再往里
就是议事厅了，议事厅门前站了两名身穿蓝衣的侍卫，禁止闲杂人等入内打扰帮主工作。
LONG);

  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/ji.lpc": 1,
    __DIR__ "npc/dizi.lpc": 2,
  ]));
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "yishiting",
    "south": __DIR__ "guangchang",
  ]));

  setup();
  replace_program(ROOM);
}
