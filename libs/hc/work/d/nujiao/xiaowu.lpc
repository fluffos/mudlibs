inherit ROOM;

void create()
{
  set ("short", "小屋");
  set ("long", @LONG
小屋二百尺许见方，除了一桌一椅一席和多个酒壶外，便是杂乱堆在
地上的一大堆断竹，其中一些被破了开来，削成一条又一条长若六尺许
的扁竹窄条。
LONG);

  set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/lang.lpc" : 1,
]));
  set("no_clean_up", 1);
  set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"xiaolu",
]));
  setup();
  replace_program(ROOM);
}

