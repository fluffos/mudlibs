inherit ROOM;

void create() {
  set("short", "走廊");
  set("long", @LONG
一条宽敞明亮的走廊.不时有弟子往这里经过。
在前方不远处就是弟子门练功的地方了。
LONG);

  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "liangongshi",
    "west": __DIR__ "bingqishi",
    "south": __DIR__ "xiuxishi",
    "east": __DIR__ "guangchang",
  ]));

  setup();
  replace_program(ROOM);
}
