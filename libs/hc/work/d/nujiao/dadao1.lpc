inherit ROOM;

void create() {
  set("short", "沿岸大道");
  set("long", @LONG
这是沿着怒蛟岛岸边铺设的青石大道，不时有一队队战马载着帮众。
在沿岸大道穿梭巡逻，充满了紧张的气氛。右边就是船码头了.
LONG);

  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/ling.lpc": 1,
  ]));

  set("outdoors", "/d/nujiao");
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "dadao",
    "east": __DIR__ "matou",
  ]));
  setup();
  replace_program(ROOM);
}
