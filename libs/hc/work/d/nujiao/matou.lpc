inherit ROOM;

void create() {
  set("short", "码头");
  set("long", @LONG
这里就是怒蛟岛东边的码头，码头一边是烟波浩瀚的洞庭湖，
碧绿的湖面波光荡漾，另一边是山岭连绵的怒蛟岛。
LONG
  );
  set("no_clean_up", 0);
  set("outdoors", "/d/nujiao");
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "dadao1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/dizi.lpc": 2,
  ]));

  setup();
}
