inherit ROOM;

void create() {
  set("short", "浅滩");
  set("long", @LONG
望着碧波千里的洞庭湖，听湖水拍打着沙滩，你不由一阵心旷神怡。
这里是怒蛟岛临湖的浅滩，因为怕敌人由此偷袭，滩后林前还设有了望楼，监视这片湖面。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "nujiaofeng",
  ]));
  set("outdoors", "/d/nujiao");

  setup();
}
