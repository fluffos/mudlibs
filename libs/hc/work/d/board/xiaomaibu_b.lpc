// /clone/board/xiaomaibu_b.c
// by xxin
inherit BULLETIN_BOARD;
void create()
{
 set_name(HIY"发黄的价格表"NOR, ({ "board" }) );
        set("location", "/d/city/xiaomaibu");
        set("board_id", "xiaomaibu_b");
        set("long",
                "一张古老的价格表，供顾客留言。\n" );
	setup();
	set("unit","个");
	set("capacity", 30);
	replace_program(BULLETIN_BOARD);
}
