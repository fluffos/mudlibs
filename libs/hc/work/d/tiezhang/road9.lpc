inherit ROOM;
void create() {
  set("short", "小路");
  set("long", @LONG
这是一条小路，两边树立着高大的松树和杉树，给人一种清净悠远的
心情。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "road10",
    "west": __DIR__ "road8",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "tiezhang");
  setup();
  replace_program(ROOM);
}
