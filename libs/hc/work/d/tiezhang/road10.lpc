inherit ROOM;
void create() {
  set("short", "小路");
  set("long", @LONG
这是一条小路，两边树立着高大的松树和杉树，给人一种清净悠远的
心情，路的尽头有一间房子，从门口挂出的酒旗可以看出，似乎是一间小
酒店。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "inn",
    "west": __DIR__ "road9",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "tiezhang");
  setup();
}
