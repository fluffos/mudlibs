// tiezhang.c 
inherit ITEM;

void create() {
  set_name("铁手掌", ({ "tie shouzhang", "shouzhang", }));
  set_weight(200);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "块");
    set("long", "这是铁掌帮的信物，一块镔铁铸造的手掌。\n");

  }
}
