#include <room.h>

inherit ROOM;

void create()
{
      set("short", "练武场");
      set("long", @LONG
这里摆满了大刀长剑、沙袋绑腿等，是铁掌帮弟子日常练功的地方。
LONG );

    set("exits", ([ /* sizeof() == 2 */
         "east" : __DIR__"zoulang",
            ]));
      set("objects", ([
              "/d/shaolin/npc/mu-ren" : 5,  //四个木人
      ]));
      setup();
      replace_program(ROOM);
}
