#include <room.h>

inherit ROOM;

void create()
{
      set("short", "走廊");
      set("long", @LONG
这里是一条通往铁掌山后山的走廊，东西两面不时传来阵阵声响。好
象是铁掌帮弟子门练功的地方。
LONG );

    set("exits", ([ /* sizeof() == 2 */
        "east" : __DIR__"wuchang1",
              "west" : __DIR__"wuchang2",
        "south" : __DIR__"xiaoyuan",
        "north" : __DIR__"houshan",
      ]));
      setup();
      replace_program(ROOM);
}

