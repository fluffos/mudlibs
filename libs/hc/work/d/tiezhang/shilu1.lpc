// Room: /u/hanbo/tiezhang
// HANBO 2000/28/08

inherit ROOM;
void create()
{
    set("short", "猴爪山山路");
    set("long", @LONG
这里五座山峰排列得就和五根手指一模一样，中间的最高，两旁        
顺次矮下来，这还不奇，最奇的是每座山峰又分三截，就如手指的指
节一般！
LONG
    );
    set("exits", ([ /* sizeof() == 3 */
        "westup" : __DIR__"shilu2",
        "eastup" : __DIR__"songlin",
        "eastdown" : __DIR__"shilu",
]));
    set("no_clean_up", 0);
    set("outdoors", "tiezhang" );
    setup();
    replace_program(ROOM);
}
