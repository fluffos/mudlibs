
inherit ROOM;
void create()
{
    set("short", "长老院");
    set("long", @LONG
这是长老们打坐练功休息的地方，静得很，只能听到微微的吐纳
声，你有什么疑问可以向长老们请教。
LONG
    );
    set("exits", ([ /* sizeof() == 2 */
                "west" : __DIR__"zt",
	]));
    setup();
    replace_program(ROOM);
}
