inherit ROOM;

void create() {
  set("short", "西楼梯");
  set("long", "从这里上去是本帮的议事厅，平时只有楼主和各位堂主可以上去。\n");

  set("exits", ([
    "east": __DIR__ "zt",
    "up": __DIR__ "yishiting",
  ]));
  setup();
  replace_program(ROOM);
}
