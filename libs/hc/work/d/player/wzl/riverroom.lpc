
inherit ROOM;
void create()
{
    set("short", "忘年轩");
    set("long", @LONG
这是一间简陋的小屋，屋内显得什么清净和简朴，房间中并没有什么
特别的摆设在这里。地上坐着个小男孩，满身沾满了泥巴也不在乎，
灵巧的双手在捏也捏也……你静静地看着，不忍打扰这里的安宁恬静。
哦，对了，屋主在楼上等你呢！
LONG
    );
    set("exits", ([ /* sizeof() == 2 */
                "east" : __DIR__"ground2",
                "up":     __DIR__"riverroom2",
]));
    setup();
    replace_program(ROOM);
}
