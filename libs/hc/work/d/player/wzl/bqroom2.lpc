inherit ROOM;

void create() {
  set("short", "兵器房");
  set("long", @LONG
这里是雾中楼的兵器房，四周放这几个兵器架，上面是雾中楼各
弟子平时训练用的刀、剑。
LONG
  );
  set("exits", ([
    "north": __DIR__ "fightroom1",
  ]));
  set("objects", ([
    "/clone/weapon/changjian.lpc": 3,
    "/clone/weapon/blade.lpc": 3
  ]));

  set("no_fight", 1);

  setup();
}
