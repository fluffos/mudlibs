#include <ansi.h>
inherit BULLETIN_BOARD;
void create() {
  set_name(HIG "震源的工作记录" NOR, ({ "board" }));
  set("location", "/u/epic/workroom");
  set("board_id", "epic_b");
  set("long", "有事请留言。\n");
  setup();
  set("capacity", 100);
}
