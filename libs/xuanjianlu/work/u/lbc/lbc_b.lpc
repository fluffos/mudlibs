// /u/lbc/lbc_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("阿陆的记事本", ({ "board" }) );
	set("location", "/u/lbc/workroom");
	set("board_id", "lbc_b");
	set("long", "这是一本普通的记事本。\n" );
	setup();
	set("capacity", 500);
	replace_program(BULLETIN_BOARD);
}
