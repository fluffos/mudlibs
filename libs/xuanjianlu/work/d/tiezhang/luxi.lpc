// Room: luxi.c

inherit ROOM;

void create()
{
	set("short", "泸溪");
	set("long", @LONG
这里是湘西的一个小镇。虽然地处偏僻，但这里来往的行人仍然很多。
西方四十里处，就是此处有名的铁掌山。
LONG	);
	set("outdoors", "tiezhang");
	set("exits", ([
		"southeast" : "d/emei/emroad3",
		"west" : __DIR__"htl",
	]));

	setup();
	replace_program(ROOM);
}
