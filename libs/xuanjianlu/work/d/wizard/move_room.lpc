//By Knight 12/10/2001
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", HIW "转站记录室" NOR);
  set("long", @LONG
这里是给从其他游戏站点转来这里的玩家做记录的地方。
墙上一块崭新的记录板，整整齐齐的列着所有转站玩家
的记录。
LONG);

  set("exits", ([
    "south": "/d/wizard/wizard_room"
  ]));

  set("valid_startroom", 1);
  set("no_fight", "1");
  set("no_clean_up", 0);
  set("cost", 0);
  setup();

  call_other("/clone/board/move_b", "???");
}
