// 怒剑狂刀


//mingfang-2

inherit ROOM;

void create() {
  set("short", "富人居");
  set("long", @LONG

这座宅子从结构上看还是相当不错的，显示出这户人家先前似乎很
阔。不过四周景物与房子极不搭配，显得这座房子不伦不类。现在
富人不在家，两个儿子更是整日里游手好闲，不务正业。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "southeast": __DIR__ "sichou",
      "west": __DIR__ "beidajie-2"
    ]));

  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/xiaopizi": 2,
    ]));



  set("no_clean_up", 0);
  setup();
}
