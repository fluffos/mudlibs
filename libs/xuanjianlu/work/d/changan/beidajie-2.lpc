// 怒剑狂刀

//beidajie-2.c

inherit ROOM;

void create() {
  set("short", "西安北大街");
  set("long", @LONG

这里的路相当的宽，能容好几匹马车并行，长长的道路通向北方。远
远的能看到西安的北门。西面传来阵阵丝竹声和欢笑声，那里是
天下闻名的“乐坊”。东边是城中的一座武馆。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "south": __DIR__ "beidajie-1",
      "north": __DIR__ "beidajie-3",
      "east": __DIR__ "mingfang-5",
      "west": __DIR__ "cfpu",
    ]));
  set("objects", ([
    __DIR__ "npc/youke": 1,
  ]));

  set("outdoors", "xi'an");
  set("no_clean_up", 0);
  setup();
}
