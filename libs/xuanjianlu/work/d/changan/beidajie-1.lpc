// 怒剑狂刀

//beidajie-1.c


inherit ROOM;

void create ()
{
        set ("short", "西安北大街");

        set ("long", @LONG

这里的路相当的宽，能容好几匹马车并行，长长的道路通向北方。远
远的能看到西安城的北门。南边是钟楼广场，游人相当的多。西边是马
厩，养有各种马匹。东边是家客店。
LONG);

        //set("item_desc", ([" *** " : " *** ", ]));

        set("exits", 
        ([ //sizeof() == 4
     "south" : __DIR__"center",
                "north" : __DIR__"beidajie-2",
                "east" : __DIR__"kedian",
               "west" : __DIR__"majiu",
        ]));




        set("outdoors", "xi'an");
   set("no_clean_up", 0);
        setup();
}
