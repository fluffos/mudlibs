// chen

inherit ROOM;
void create() {
  set("short", "布店");
  set("long", @LONG
这是一个布店，之声不断，店里面各种各样的布匹应有尽有，
都是由对面的染料坊染的，品类齐全，价格低廉，引来无数
市民，讨价还价之声不断。
LONG);
  set("exits", ([
    "west": __DIR__ "dongshi",
    //"north" : __DIR__"nongtian-2",
  ]));
  //set("outdoors","xi'an");
  setup();
}
