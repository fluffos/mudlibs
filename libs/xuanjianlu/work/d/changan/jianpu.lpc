// 怒剑狂刀

//jianpu.c

inherit ROOM;

void create() {
  set("short", "剑铺");
  set("long", @LONG

刚一进门就看到兵器架上摆着各种剑，从质量上乘的青刚剑到价格
低廉的短剑，是应有尽有。老板是张三，在江湖中默默无闻，但卖
的剑却相当不错。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "north": __DIR__ "dongdajie-1",
    ]));

  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/zhangsan": 1,
    ]));



  set("no_clean_up", 0);
  setup();
}
