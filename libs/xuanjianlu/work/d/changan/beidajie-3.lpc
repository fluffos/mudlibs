// 怒剑狂刀

//beidajie-3.c

inherit ROOM;

void create ()
{
        set ("short", "西安北大街");
        set ("long", @LONG

这里的路相当的宽，能容好几匹马车并行，长长的道路通向北方。远
远的能看到西安的北门。西面是一片绿油油的农田，远远的看见农田
里劳作的农夫，那里是天下闻名的“乐坊”。东边是城中的一座武馆。
LONG);

        //set("item_desc", ([" *** " : " *** ", ]));

        set("exits", 
        ([ //sizeof() == 4
     "south" : __DIR__"beidajie-2",
     "north" : __DIR__"beimen",
	 "west" : __DIR__"dadao",
	 "east" : __DIR__"dadao-1",
        ]));
        set("objects", ([
                        __DIR__"npc/youke": 1,
        ])); 

        set("outdoors", "xi'an");
   set("no_clean_up", 0);
        setup();
}

