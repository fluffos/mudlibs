// 怒剑狂刀

//xidajie-2.c

inherit ROOM;

void create() {
  set("short", "西安西大街");
  set("long", @LONG

你走在一条宽阔的石板大街上，东面就快到钟楼广场了，可以看到钟楼
耸立于前。南边有一个武馆但已荒废很久了很少有人出入。北面是一个
财主的庄园，飞檐挂角，显的极有气势。
LONG);


  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4

    "west": __DIR__ "xidajie-3",
      "north": __DIR__ "caizhu",
      "south": __DIR__ "wuguan",
      "east": __DIR__ "xidajie-1",
    ]));

  set("outdoors", "xi'an");
  set("no_clean_up", 0);
  setup();
}
