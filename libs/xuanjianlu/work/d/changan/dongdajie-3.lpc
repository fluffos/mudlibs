// 怒剑狂刀

//dongdajie-3.c

inherit ROOM;

void create ()
{
        set ("short", "西安东大街");
        set ("long", @LONG

你走在一条宽阔的石板大街上，西面就快到钟楼广场了，可以看到钟楼
耸立于前。南北皆是规模不小的宅院，北面院子里有俩个不务正业的小
痞子，南面的一所宅院住着一个铁匠。
LONG);

        //set("item_desc", ([" *** " : " *** ", ]));

        set("exits", 
        ([ //sizeof() == 4
        "north" : __DIR__"dongshi",
                "south" : __DIR__"dadao-7",
                "west" : __DIR__"dongdajie-2",
                "east" : __DIR__"dongmen",
        ]));

         set("objects",([
                         __DIR__"npc/youke": 1,
                         __DIR__"npc/xiaopizi": 1,
         ]));

        set("outdoors", "xi'an");
   set("no_clean_up", 0);
        setup();
}

