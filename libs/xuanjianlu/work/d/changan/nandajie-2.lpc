//怒剑狂刀

//nandajie-2.c


inherit ROOM;

void create ()
{
        set ("short", "西安南大街");
        set ("long", @LONG

南大街上商家众多，大大小小的店铺比邻而立，看起来是相当的豪
华富足。宽阔的石板路直通南北，向北通到城中心，向南是南门，
西边一间朴素的民房，东边通向望南街。
LONG);

        //set("item_desc", ([" *** " : " *** ", ]));

        set("exits", 
        ([ //sizeof() == 4
     "south" : __DIR__"nandajie-3",
                "north" : __DIR__"nandajie-1",
                "west" : __DIR__"mingfang",
                "east" : __DIR__"wangnanjie",
        ]));
         set("objects", ([
			 __DIR__"npc/youke": 1,
         ]));

        set("outdoors", "xi'an");
   set("no_clean_up", 0);
        setup();
}
