// 怒剑狂刀

//xidajie-3.c

inherit ROOM;

void create() {
  set("short", "西安西大街");
  set("long", @LONG

你走在一条宽阔的石板大街上，东面就快到钟楼广场了，可以看到钟楼
耸立于前。南边有一个武馆但已荒废很久了很少有人出入。北面是一个
市场，来来往往，人群络绎不绝。
LONG);


  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4

    "west": __DIR__ "ximen",
      "north": __DIR__ "xishi",
      "south": __DIR__ "dadao-4",
      "east": __DIR__ "xidajie-2",
    ]));

  set("outdoors", "xi'an");
  set("no_clean_up", 0);
  setup();
}
