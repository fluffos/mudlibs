// 怒剑狂刀


//zhonglou-2.c

inherit ROOM;

void create()
{ 
   set("short","钟楼二层");
   set("long", @LONG
   这是钟楼的第二层，有一口其大无比的钟(bell)，四周墙壁上有数
盏龛灯，周围是木制墙壁，绘有彩色壁画。
LONG);
    set("item_desc", ([
		"bell" :"一口大钟，历史悠久,已不知是何年何月所造,上面写满了字\n",
	  
		]));
    set("exits",([
         
		 "down" : __DIR__"zhonglou-1",
		 
		 ]));
	set("objects",([
        __DIR__"npc/youke": 1,
    ]));

	setup();
}
