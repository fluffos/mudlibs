inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIR "炮船内部" NOR);
  set("long", @LONG
黑黑的船舱，偶尔有一两只老鼠跑过。
这样的地方肯定有好东西，你不如找找(search)看。      
LONG);


  set("exits", ([
    "up": __DIR__ "boat2",
    "north": __DIR__ "boat6",
  ]));
  set("objects", ([
    "/d/shenlong/npc/qingbing1": 1,
    "/d/shenlong/npc/qingbing3": 1,
    "/d/shenlong/npc/qingbing5": 1,
  ]));
  set("cost", 1);
  setup();
}
