
#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", "长廊");
        set("long", @LONG
蜿蜒的长廊好像没有尽头一般，往南是刚才走过
的长廊，往北则是一望无际的长廊。
LONG
        );
        set("cost", 1);
        set("outdoors", "shenlong");

        set("exits", ([
                "north": __DIR__"changlang3",
                "south": __DIR__"changlang1",
        ]));

        set("objects", ([
        ]));

        setup();
}
