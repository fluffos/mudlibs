#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "长廊");
  set("long", @LONG
来到这里眼前豁然开朗，前面就是神龙教的大厅，
后面是走过的长廊。
LONG
  );
  set("cost", 1);
  set("outdoors", "shenlong");

  set("exits", ([
    "north": __DIR__ "dating",
    "south": __DIR__ "changlang2",
  ]));

  set("objects", ([
  ]));

  setup();
}
