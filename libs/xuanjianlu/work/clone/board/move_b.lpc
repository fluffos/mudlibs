//Cracked by Roath
// /clone/board/wiz_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("转站记录", ({ "board" }) );
	set("location", "/d/wizard/move_room");
	set("board_id", "move_b");
	set("long", "这是一个专供巫师记录转站id的留言板。\n" );
	set("wizard_only", 1);
	setup();
	set("capacity", 200);
	replace_program(BULLETIN_BOARD);
}

