inherit ITEM;
void create()
{
        set_name(HIW"元宵"NOR, ({ "yuan xiao",}));
        set_weight(100);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                 set("long", HIW"小刀发的礼物，你可以eat xiao！\n");
                set("unit", "颗");
                 set("value",0);
                 set("give",1);        
                 set("drop",1);
                 set("ty_gift",1);  
        }
    setup();
}
void init()
{
    if (environment()==this_player())
        add_action("do_tilian", "eat");
}
int do_tilian(string arg)
{
  object me = this_player(); 
  if(!id(arg)) return 0;
  if(me->is_busy()) return notify_fail("你上一个动作还没有完成。\n");
                tell_object(me, "摇那么多次干嘛?。\n");
               else
        {
        message_vision("$N一口吃掉" + this_object()->name() +
                       ",爽!\n", me);

                tell_object(me, HIY "你觉得似乎自己经验增加了300w。\n" NOR);
 tell_object(me, HIM "你觉得似乎自己潜能增加了50w。\n" NOR);

                  me->add("tygift/2008", 1); 
                me->add("combat_exp", 3000000);
                me->add("potential", 500000);

                   me->set("tygift/20050311", 1);

        destruct(this_object());
        return 1;
}
 }
