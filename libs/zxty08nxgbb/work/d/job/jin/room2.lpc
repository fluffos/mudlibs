// Room: /d/job/jin/room2.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create()
{
	set("short", "西外街一");
	set("long", @LONG
这里就是恶人谷的西外街，你从在空荡荡的街上，感觉似乎有无数鬼异
的眼睛在暗处盯着你，你不由得打了个寒颤。
LONG
);
	set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"room1",
  "west" : __DIR__"room3",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
