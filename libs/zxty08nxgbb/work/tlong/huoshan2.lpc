// Room: /u/wenhua/huoshan.c

inherit ROOM;

void create()
{
        set("short", "火山");
        set("long", @LONG
到这里来后你什么都看不见了，，，，，只是有被火烧身的感觉。！！！！
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
  "west" : "/d/city/guangchang",
]));
        set("no_clean_up", 0);
             set("objects", ([
  "/u/wenhua/tlong/dragon2": 1,
    ]));

        setup();
        replace_program(ROOM);
}
