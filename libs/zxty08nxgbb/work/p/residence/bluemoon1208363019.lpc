inherit ROOM;
void create() {
  set("short", "烟波瀛洲阁门廊");
  set("long", @LONG
進入大門，映入眼簾的是一條幽深的門廊，門廊用朱漆造就，
飛檐上雕龍畫鳳，盡顯名家手筆，門廊兩側假山怪石，蒼松翠竹掩
映。不如門廊，鳥語啾啾，你不覺得心曠神怡，精神為之一振。

LONG
  );
  set("exits", ([
    "north": "/p/residence/bluemoon1208363382",
    "south": "/p/residence/bluemoon1208362634.lpc",
  ]));
  set("objects", ([
  ]));
  set("owner", "bigtie7001");
  //         set("class", "$HIC$烟$HIW$波$HIB$瀛洲$HIR$阁");
  set("banghui", "$HIC$烟$HIW$波$HIB$瀛洲$HIR$阁");
  set("valid_startroom", 1);
  set("no_fight", 1);

  setup();
}
void init() {
  add_action("do_invite", "yaoqing2");
  //add_action("do_setowner","setowner");
}
int valid_enter(object me) {
  int flag;
  // always let owner go in:
  if ((string)me->query("banghui") != (string)query("banghui")
    //         if((string)me->query("class") != (string)query("class")
    && (string)query("invite") != (string)me->query("id")
    && (string)me->query("id") != (string)query("owner"))
    return 1;

}
int do_invite(string arg) {
  object me;
  me = this_player();
  if ((string)me->query("id") != (string)query("owner"))
    return 0;
  set("invite", arg);
  write("你邀请" + arg + "来你的房间。\n");
  return 1;
}
