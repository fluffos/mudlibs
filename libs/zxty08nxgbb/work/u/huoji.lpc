// huoji. 药铺伙计
inherit NPC;
inherit F_DEALER;
string ask_me();

void create()
{
	set_name("药铺伙计", ({ "yaopu huoji", "huoji" }));
	set("str", 20);
	set("gender", "男性");
	set("age", 18);
	set("long", "他是平神医从乡下招来帮忙的。\n");
	set("combat_exp", 250);
	set("attitude", "friendly");
	set("vendor_goods", ({
                __DIR__"obj/jinchuang",
		__DIR__"obj/yangjing"
	}));
	
	        set("inquiry", ([
               "test"  : (: ask_me :),
        ]) );


	setup();
	//carry_object(__DIR__"obj/linen")->wear();
}
string ask_me()
{

	object *inv,me = this_player(),wear,ob;

mapping map;
map = me->query_skill_map();

return "你激发的force是："+map["force"] ;
	
	//return "skill0="+me->query_skill("unarmed",0)+"\n skill1="+me->query_skill("unarmed",1)+"\n skill2="+me->query_skill("unarmed",2);
	
	/*if (me->is_busy())
		return "你BUSY的时间是"+me->query_busy();
		else return "你不BUSY！";
	//return "你的定做盔甲是"+me->query("jia/name")+"("+me->query("jia/id")+"),"+"me->query(jia/make)="+me->query("jia/make");

	/*inv = all_inventory(me);
	
	if( sizeof(inv)<1 )
	return "你是裸体!";
	
	for(int i=0; i<sizeof(inv); i++) {
                if (inv[i]->query("equipped"))
                {
                        return "你装备了"+inv[i]->query("name");
                }else {
                	return "你带着"+inv[i]->query("name");
                }
 }*/
	

	/*if (me->query_temp("armor")) {
		//return "wear id = "+wear->query("id");
		return "wear";
	}
	else return "wear is null";*/
}
