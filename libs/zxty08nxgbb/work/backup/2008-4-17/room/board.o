inherit BULLETIN_BOARD;

void create()
{
        set_name("BOARD_NAME",({"board"}));
        set("location","ROOM");
        set("board_id", "zjb_b");
        set("long", "这个是星空为大家写的帮派留言版。\n");
        setup();
        set("capacity", 100);
        replace_program(BULLETIN_BOARD);
}
