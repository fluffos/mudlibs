// Room: /u/songe/mishi.c

inherit ROOM;
#include <ansi.h>

void create() {
  set("short", HIY "天地会总坛" NOR);
  set("long", @LONG
这里是天地会的总坛，有很多江湖义士在一起商谈着什么......
LONG
  );

  set("exits", ([
    "south": "/d/city/yanju.lpc",
  ]));

  set("objects", ([
    "/u/songe/job/chen": 1,
  ]));

  setup();
}
