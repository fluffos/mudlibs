#include <ansi.h>;
inherit ROOM;

void create() {
  set("short", HIM "书房" NOR);
  set("long", @LONG
描述待定
LONG
  );
  set("no_fight", 1);
  set("exits", ([
    "west": "/u/squall/room",
  ]));
  setup();
}
