// Room: /d/wizard/wizard_room.c

inherit ROOM;

void create() {
  set("short", "工作情况室");
  set("long", @LONG
这是巫师工作情况公布室，房子中间耸立一块记事碑。上面密密麻麻的
写着巫师们的工作情况，巫师们把自己的工作内容和工作中碰到的一些问题
都写在这儿。
LONG);
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "wizard_meet"
  ]));

  set("valid_startroom", 1);
  set("no_fight", "1");
  set("no_clean_up", 0);
  setup();

  call_other("/clone/board/wiz_j", "???");
}
