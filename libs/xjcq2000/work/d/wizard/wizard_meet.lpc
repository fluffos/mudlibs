// Room: /d/wizard/wizard_room.c

inherit ROOM;

void create() {
  set("short", "巫师会议室");
  set("long", @LONG
在宽敞明亮的大会议室里，正中放着一张红木大圆桌。巫师通常都在这
里讨论通过一些重要建设和修改的提案。巫师会议记录簿(board)。 南北和
东边都通往比武擂台。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "wizard_room",
    "north": "/cmds/leitai/leitai",
    "south": "/cmds/leitai/leitai1",
    "east": "/cmds/leitai/leitai2",
  ]));

  set("valid_startroom", 1);
  set("no_fight", "1");
  set("no_clean_up", 0);
  setup();

  call_other("/clone/board/post_b", "???");
  replace_program(ROOM);
}
