// Room: /u/jpei/thd/bookroom.c

inherit ROOM;

void create() {
  set("short", "书房");
  set("long", @LONG
这里是黄药师读书的地方。窗明几净，一尘不染。一张古意盎然的书案放
在房间正中，案上摆着几本翻开了的线装书籍。
LONG
  );

  set("exits", ([
    "west": __DIR__ "hall.lpc",
  ]));

  set("objects", ([
    __DIR__ "obj/tupu": 1,
  ]));

  setup();
}
