#include <ansi.h>
inherit ROOM;
void create() {
  set("short", HIW "小屋" NOR);
  set("long", @long
这是一间平平常常的小房间，里面的椅子上坐着一位英气十足的中年人。
long);
  set("exits", ([
    "out": __DIR__ "huayuan3",
  ]));
  set("objects", ([
    "/kungfu/class/kunlun/hezudao.lpc": 1,
  ]));
  setup();
}
