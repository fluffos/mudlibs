// hsroad3.c

inherit ROOM;

void create()
{
	set("short", "黄土路");
	set("long", @LONG
你走在一条坑坑洼洼的土路上，弯弯曲曲的通向山里。两边的稻田一望
无际，不过据说这里也是强盗经常出没的地方。
LONG
	);
        set("outdoors", "华山村");

	set("exits", ([
		"north" : __DIR__"sexit",
		
	]));

	setup();
	replace_program(ROOM);
}

