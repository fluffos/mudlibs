// baota02.c 宝塔二楼
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "宝塔二楼");
  set("long", @LONG
这里同样是一片昏暗，仔细一看，有一盏摇摇弋弋的小灯(deng)，发出淡
的微光。墙(wall)上写满了古怪的梵文。墙上蜘蛛网结满了，看不出什么人在
这里住过。可是这盏小灯怎么又不会熄灭呢。可是看看四壁光溜溜的，看不出
有什么出路，唯一的方法还是下去。
LONG);
  set("exits", ([
    "down": __DIR__ "baota01"
  ]));

  setup();
}
