//     xiedi02.c 雪地
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIW "雪地" NOR);
  set("long", @LONG 
这里是山脚下的一块雪地，四周都是高山，看来是没有什么出路了，雪越
积越厚，你举步艰难，腿都软了，跟本走不动。看来不能在往前走了，你只好
往回走了。
LONG);
  set("exits", ([
    "southdown": __DIR__ "xuedi01",
  ]));

  setup();
}
