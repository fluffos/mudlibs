// liangong-fu.c

inherit ITEM;

void create() {
  set_name("待定", ({ "dan" }));
  set("long", "待定。\n");
  set_weight(300);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("material", "paper");
    set("value", 60);
    set("unit", "张");
  }
  setup();
}
