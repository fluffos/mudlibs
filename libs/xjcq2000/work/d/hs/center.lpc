// center.c
// last update 02/23/1997
// update by KEN.Q.Z.ZHANG

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIW "正气堂" NOR);
  set("long", @LONG
这是华山掌门岳不群的居所「有所不为轩」，此处是前厅，厅上挂着「正气堂」三
字匾额。厅中很宽敞，却没什么摆设，靠墙放着两把交椅，这里是岳不群议事、授徒的
地方，后面是书院。
LONG);
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "square",
    "south": __DIR__ "shuyuan",
  ]));
  /*
	set("objects", ([ 
	__DIR__"npc/yue-buqun": 1,
	__DIR__"npc/ning-zhongze": 1
	]) );
*/
  set("valid_startroom", 1);
  setup();
  //         "/clone/board/huashan_b"->foo();
}
