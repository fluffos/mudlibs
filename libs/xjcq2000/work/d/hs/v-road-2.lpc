// valley-road-2.c 华山瀑布下山涧

inherit ROOM;

void create() {
  set("short", "山涧");
  set("long", @LONG
这里周围都是灌木，东南的方向和东面的方向似乎能走过去。
LONG);
  set("exits", ([ /* sizeof() == 2 */
    "eastup": __DIR__ "v-road-3",
    "southwest": __DIR__ "v-road-1"
  ]));
  set("outdoors", "huashan");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
