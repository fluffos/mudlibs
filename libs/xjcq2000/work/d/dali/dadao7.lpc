inherit ROOM;

void create() {
  set("short", "大道");
  set("long", @LONG
你在大道上，两边都是杨柳树，这条路上来往的人并不多.
出。北面就到四川境内了。
LONG
  );
  set("outdoors", "dali");
  set("exits", ([ /* sizeof() == 1 */
    "north": "/d/chengdu/eroad2",
    "south": __DIR__ "dadao6",
  ]));
  set("no_clean_up", 0);

  setup();
}
