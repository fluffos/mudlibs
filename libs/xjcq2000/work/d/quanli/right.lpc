// Room: /d/quanli/right.c

inherit ROOM;

void create() {
  set("short", "空房间");
  set("long", @LONG
这是一间什麽也没有的空房间。
LONG
  );

  set("no_dazuo", 1);
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "door",
  ]));

  set("coor", ([ /* sizeof() == 3 */
    "z": 0,
    "y": 12230,
    "x": 10,
  ]));
  setup();
  replace_program(ROOM);
}
