// Room: /d/quanli/door.c

inherit ROOM;

void create() {
  set("short", "大门");
  set("long", @LONG
正前方是一扇大门，门是用红木做的，上面有几个大字：权力帮。
LONG
  );
  set("item_desc", ([ /* sizeof() == 3 */
    "north": (: look_door, "north" :),
    "门": "一扇红木大门。
",
    "gate": "一扇红木大门。
",
  ]));
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "tree",
    "west": __DIR__ "left",
    "north": __DIR__ "nei",
    "east": __DIR__ "right",
  ]));
  set("no_magic", 1);
  set("outdoors", "quanli");
  set("no_dazuo", 1);
  set("coor", ([ /* sizeof() == 3 */
    "z": 0,
    "y": 12230,
    "x": 0,
  ]));

  setup();
}
