inherit ROOM;
#include <room.h>

void create() {
  set("short", "内堂");
  set("long", @LONG
权力帮的帮主堂，历代帮主休息的地方。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "nei",

  ]));
  set("objects", ([
    __DIR__ "npc/badp": 1,
    "/d/fy/npc/qiushui": 1,
  ]));
  set("outdoors", "quanli");
  set("no_dazuo", 1);
  set("no_magic", 1);
  set("coor/x", 0);
  set("coor/y", 12230);
  set("coor/z", 0);
  setup();


}
