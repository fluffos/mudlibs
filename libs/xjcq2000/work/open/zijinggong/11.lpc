// Room: /open/zijinggong/11.c / zijinggong
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "花园");
  set("long", @LONG
幽静的一个花园，种满了奇花异草，空气中迷漫着芳香。天空中百鸟飞舞，好
一派神仙娴静的去处。
LONG
  );
  set("group", "zijinggong");
  set("exits", ([ /* sizeof() == 1 */
    "north": "/open/zijinggong/9",
  ]));

  setup();
  replace_program(ROOM);
}
