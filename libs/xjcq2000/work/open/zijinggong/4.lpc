// Room: /open/zijinggong/4.c / zijinggong
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "[1;31m神风堂[0;37;0m");
  set("long", @LONG
这里是神风堂堂主风护法的执法室.墙上挂着一副草书,硕大的一个“训”(xun)字历历在目.
历年执法长老都要在这里商讨会议，在房间的正中放着一快训教大石碑[1;32m（shibei)[0;37;0m
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "down": "/open/zijinggong/12",
    "south": "/open/zijinggong/group",
    "north": "/open/zijinggong/13",
  ]));
  set("group", "zijinggong");
  set("item_desc", ([ /* sizeof() == 2 */
    "xun": "违者将被强逐出宫，永远不得在加入本帮。情节恶劣者将被tk处理。",
    "shibei": "本帮帮规宫规:1.同门齐心合力.2.禁止恶意pk.3.注意尊长爱幼.4.配合wiz工作.5.听从宫主号令.",
  ]));

  setup();
}
