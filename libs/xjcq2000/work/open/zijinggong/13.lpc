// Room: /open/zijinggong/13.c / zijinggong
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "[1;35m紫[1;37m晶[1;33m宫  [1;32m小分舵[0;37;0m");
  set("long", @LONG
这里是紫晶宫的后殿，内设有机关和密道，屋子的下面是一条小溪，北面是
一片树林,东面的大门已经用巨石封住，西面的墙上有些符号(fuhao)，隐约看上
去象写了些什么字。
LONG
  );
  set("group", "zijinggong");
  set("exits", ([ /* sizeof() == 1 */
    "south": "/open/zijinggong/4",
  ]));
  set("item_desc", ([ /* sizeof() == 1 */
    "fuhao": "你仔细一看，好想是什么地图{out;w;n;n;n;n;e;n;n;w}",
  ]));

  setup();
}
