// Room: /open/zijinggong/12.c / zijinggong
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "暗房");
  set("long", @LONG
这是本宫关押犯人的地方.令人毛骨悚然,里面枯骨满地,四周是用玄铁
筑成铁栅栏.一看就知道没有人能被关押而活着出来.
LONG
  );
  set("group", "zijinggong");
  set("exits", ([ /* sizeof() == 1 */
    "up": "/open/zijinggong/4",
  ]));

  setup();
}
