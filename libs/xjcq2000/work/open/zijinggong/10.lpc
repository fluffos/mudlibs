// Room: /open/zijinggong/10.c / zijinggong
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "[1;32m楼阁[0;37;0m");
  set("long", @LONG
这里鸟羽花香，是帮派的禁地,也是宫主和她的夫君休息的地方.墙上挂了一幅俩人
的结婚照片,真是英雄配美人.窗边有一株梅花伸进窗来,显得格外文雅。
LONG
  );
  set("group", "zijinggong");
  set("exits", ([ /* sizeof() == 1 */
    "down": "/open/zijinggong/5",
  ]));

  setup();
  replace_program(ROOM);
}
