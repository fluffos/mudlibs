inherit BULLETIN_BOARD;
#include <ansi.h>

void create() {
  set_name(HIM "天翔的私人留言板" NOR, ({ "board" }));
  set("location", "/u/jrx/workroom");
  set("board_id", "jrx_b");
  set("long", "这是一个天翔为了更好的交流而设的留言板。\n");
  setup();
  set("capacity", 100);
}
