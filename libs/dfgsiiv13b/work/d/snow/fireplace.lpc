// Room: /d/snow/fireplace.c

inherit ROOM;

void create()
{
    set("short", "╛ず");
    set("long", @LONG
瞷タ籰罽帝澜╛ぇい, 繷郴よ留留废
, ぃ筁陪礛⊿Τ快猭, 硂柑鲤纠縉τ跑眔疩堵, 
縩糷玴玴η.
LONG
    );
    set("exits", ([ /* sizeof() == 1 */
	"out" : __DIR__"inn_kitchen",
    ]));

    set("objects", ([
	__DIR__ "npc/obj/woodsword": 1,
	__DIR__ "npc/obj/woodblade": 1
    ]));

    setup();
    replace_program(ROOM);
}
