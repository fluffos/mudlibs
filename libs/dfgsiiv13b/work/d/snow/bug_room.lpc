#include <room.h>
inherit ROOM;

void create()
{
	set("short", "約初");
	set("long", @LONG
硂琌約初碭词攫祡祡辅辅いァΤ
痙ēㄑチ渤厨┮綝笿┣钵瞷禜┕玭羘供猰撤
獸马約初
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
		"south" : "/d/snow/square_w",
	]));
	set("outdoors", "snow");
	set("no_fight", 1);			// prevent pk when post

	setup();
	load_object("/daemon/board/bug_board");
}
