// Room: /d/snow/square_w.c

#include <room.h>
inherit ROOM;

void create()
{
    set("short", "約初");
    set("long", @LONG
硂柑琌撤獸马約初﹁娩┕狥ǎ約初いァ篲攫產
刺畒辅﹁娩ぃ环矪刺玡﹃縊泞糶帝都刺
約初綫帝俱霍ホ狾
LONG
    );
    set("exits", ([ /* sizeof() == 3 */
	"south" : __DIR__"square_sw",
	"west" : __DIR__"inn_hall",
	"east" : __DIR__"square",
	"north" : __DIR__"bug_room",
    ]));
    set("objects", ([
	"/d/snow/obj/stone" : 1,
    ]));
    set("outdoors", "snow");
    set("no_fight", 1);	// prevent pk when post

    setup();
    load_object("/daemon/board/common");
    create_door("west", "┍", "east", 0);
}
