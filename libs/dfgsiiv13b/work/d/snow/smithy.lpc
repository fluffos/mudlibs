// Room: /d/snow/smithy.c

inherit ROOM;

void create()
{
	set("short", "ゴ臟綫");
	set("long", @LONG
硂柑琌丁ゴ臟綫鲤本砛臟竟Θ珇蛤ㄣ綫い
丁縉帝旱旱縑膌琈眔鲤紇娩临Τ
℉臟痾綫碞狥娩
LONG
	);
	set("detail", ([ /* sizeof() == 2 */
		"臟痾" : "硂臟痾癬ㄓだ℉骸琌篤ゴ勃格
",
		"臟竟" : "硂ㄇ臟竟Τ羚藐臟埋バノ維繷瞤单单
",
]));
	set("no_clean_up", 0);
	set("objects", ([ /* sizeof() == 1 */
		__DIR__"npc/smith" : 1,
]));
	set("exits", ([ /* sizeof() == 1 */
		"west" : __DIR__"square_s",
]));

	setup();
	replace_program(ROOM);
}
