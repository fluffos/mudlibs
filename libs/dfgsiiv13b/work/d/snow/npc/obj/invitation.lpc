// invitation.c

inherit ITEM;

void create()
{
	set_name("璣动┇", ({ "invitation" }) );
	set_weight(20);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "眎");
		set("long",
			"眎パ猌狶幅孽驾┮祇璣动┇Τ瑈ōだ︗猌狶\n"
			"Μ硂眎璣动┇\n");
		set("value", 1);
	}
}
