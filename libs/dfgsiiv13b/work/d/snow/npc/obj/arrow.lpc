inherit ITEM;
void create()
{
        set_name("臟絙", ({ "iron arrow", "arrow" }));
        set_weight(300);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("unit", "");
                set("value", 4000);
                set("long", "臟籹Θ絙, も弘灿.\n");
        }
        setup();
}
