// Room: /d/snow/mill.c

inherit ROOM;

void create()
{
	set("short", "縤");
	set("long", @LONG
硂柑琌丁縤难穨縤柑丁砆琜ó
沮ó锣笆绢筁假れ禸れ禸崩笆碭当絃锣笆いァ
ホ縤┬à帮帝ㄇ陈砋
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
		"out" : __DIR__"lane2",
	]));
	set("objects", ([
		__DIR__"npc/miller": 1
	]));

	setup();
	replace_program(ROOM);
}
