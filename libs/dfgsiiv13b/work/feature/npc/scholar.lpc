/*  scholar.c

    Copyright (C) 1994-2000 Annihilator <annihilator@muds.net>

    This program is a part of ES2 mudlib. Permission is granted to use,
    modify, copy or distribute this program provided this copyright notice
    remains intact and subject to the restriction that this program MAY
    NOT be used in any way for monetary gain.

    Details of terms and conditions is available in the Copyright.ES2 file.
    If you don't receive this file along with this program, write to the
    primary author of ES2 mudlib: Annihilator <annihilator@muds.net>
*/

#include <attack.h>
#include <statistic.h>

// prototypes
mixed do_chat(mixed chat);
varargs string name(int raw);
void kill_ob(object ob);
varargs int is_fighting(object ob);
int query_level();
string rank(string politeness);

int accept_fight(object ob)
{
    if( is_fighting() ) {
        int differ;

        differ = query_level() - (int)ob->query_level();
        if( differ > 10 ) {
            do_chat( name() + "帝弧笵" + ob->rank("respectful")
        	+ "ぶ" + rank("self") + "︑讽叫毙\n");
            return 0;
        } else {
            do_chat( name() + "弧笵硂︗" + ob->rank("respectful") + "螟笵琌稱秤\n");
            return 0;
        }
    }

    if( ob->is_fighting() ) {
	do_chat( name() + "弧笵ち絉猌美临琌虫ゴ縒矮и卑皚\n");
	return 0;
    }

    if( query_stat_current("kee") < query_stat_maximum("kee") ) {
	do_chat( name() + "弧笵单穦ㄠ琵и齿\n");
	return 0;
    }

    do_chat( name() + "弧笵ぃ幢叫界┷\n");
    return 1;
}

void
accept_kill(object ob)
{
    do_chat( name() + "弧笵ぃ" + rank("self") + "︙眔竜硂︗"
	+ ob->rank("respectful") + "ぐ或獶璶笆もぃ㎡\n");
}

void accept_negotiation(object who, string key)
{
    switch(key) {
    case "peace":
        if( !is_killing(who) ) {
            do_chat( name() + "癭ō临搂弧笵弧弧\n");
            remove_enemy(who);
            who->remove_enemy(this_object());
        }
        else do_chat( name() + "弧笵紀杠ぶ弧さらぃ琌獽琌и\n");
        return;
    case "mercy":
        if( is_killing(who) ) {
            do_chat( name() + "弧笵琂礛さぱ碞那\n");
            remove_killer(who);
            remove_enemy(who);
            who->remove_killer(this_object());
            who->remove_enemy(this_object());
            who->gain_score("martial fame", - who->query_level() * 10);
        }
    }
}

void
win_fight(object ob)
{
    do_chat( name() + "も弧笵┯琵硂︗" + ob->rank("respectful")
	+ "も痙薄" + rank("self") + "︓稰脖薄\n");
}
