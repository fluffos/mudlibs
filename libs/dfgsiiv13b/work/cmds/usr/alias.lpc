/*  alias.c

    Copyright (C) 1994-2000 Annihilator <annihilator@muds.net>

    This program is a part of ES2 mudlib. Permission is granted to use,
    modify, copy or distribute this program provided this copyright notice
    remains intact and subject to the restriction that this program MAY
    NOT be used in any way for monetary gain.

    Details of terms and conditions is available in the Copyright.ES2 file.
    If you don't receive this file along with this program, write to the
    primary author of ES2 mudlib: Annihilator <annihilator@muds.net>
*/

#include <command.h>

inherit F_CLEAN_UP;

void create() { seteuid(getuid()); }

int main(object me, string arg)
{
    int i;
    mapping alias;
    string verb, replace, *vrbs;
    object link;

    SECURED_COMMAND;

    if( !(link = me->link()) )
	return notify_fail("⊿Τ硈絬ン\n");

    if( !arg ) {
	alias = link->query_all_alias();
	if( !sizeof(alias) ) {
	    write("ヘ玡⊿Τ砞﹚ヴ︙ alias\n");
	    return 1;
        } else  {
	    write("ヘ玡砞﹚ alias Τ\n");
	    vrbs = keys(alias);
	    for(i=0; i<sizeof(vrbs); i++)
		printf("%-15s = %s\n", vrbs[i], alias[vrbs[i]]);
	    return 1;
	}
    }

    if( sscanf(arg, "%s %s", verb, replace)!=2 )
	link->set_alias(arg, 0);
    else if( verb=="alias" )
	return notify_fail("ぃ盢 \"alias\" 砞﹚ㄤノ硚\n");
    else if( verb=="" )
	return notify_fail("璶砞ぐ或 alias\n");
    else
	link->set_alias(verb, replace);

    write("Ok.\n");
    return 1;
}

int help (object me)
{
    write(@HELP
Α : alias <饼砞﹚ぇ> <╰参矗ㄑぇ>
 
Τ╰参┮矗ㄑぇ惠璶块﹃, ㄏノ(ぷㄤ琌竒盽ノ)
┪砛穦稰谋ぃよ獽, (﹑)ノ砞﹚蠢Τぇ
 
絛ㄒ:
    'alias sc score' 穦 sc  score 
    'alias' ぃ把计玥┮Τ蠢
    'alias sc' 穦埃 sc 硂蠢 (狦Τ砞杠)
 
ㄤいノ $1, $2, $3 .... ㄓ材材材把计┪琌 $* 
┮Τ把计
    'alias pb put $1 in $2'

玥–讽(﹑)ゴ
    pb bandage bag

碞穦Θ
    put bandage in bag
    
HELP
    );
    return 1;
}
