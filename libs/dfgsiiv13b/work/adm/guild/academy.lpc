// Room: /adm/guild/academy.c

inherit ROOM;

void create()
{
	set("short", "畍厩皘");
	set("long", @LONG
硂柑琌畍╯ LPC よ狦琌穝も畍舧ㄓ硂柑
㎝ㄤが癚阶狦琌ρも叫ぃ氮穝も拜肈
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
		"west" : __DIR__"guildhall",
	]));

	setup();
	load_object("/daemon/board/lpc");
	replace_program(ROOM);
}
