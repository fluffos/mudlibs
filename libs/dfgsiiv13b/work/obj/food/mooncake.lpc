// manto.c

inherit COMBINED_ITEM;
inherit F_FOOD;

void create()
{
	set_name("る绘", ({"mooncake", "cake"}));
	if( !clonep() ) {
		set("unit", "帮");
		set("base_unit", "");
		set("base_value", 1);
		set("base_weight", 300);
		set("food_stuff", 140);
		set("long", "蛾る绘碞钩ぱる獹妓\n");
	}
	setup();
}

