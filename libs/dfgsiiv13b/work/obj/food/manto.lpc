// manto.c

inherit COMBINED_ITEM;
inherit F_FOOD;

void create()
{
	set_name("腃繷", ({ "manto" }));
	if( !clonep() ) {
		set("unit", "帮");
		set("base_unit", "");
		set("base_value", 10);
		set("base_weight", 100);
		set("food_stuff", 80);
		set("long", "腃繷 ... 蛤稱钩い家妓稰笆\n");
	}
	setup();
}

