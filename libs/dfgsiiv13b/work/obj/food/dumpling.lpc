// dumpling.c

inherit COMBINED_ITEM;
inherit F_FOOD;

void create()
{
	set_name("ψ", ({ "beef dumpling", "dumpling" }) );
	if( !clonep() ) {
		set("unit", "帮");
		set("base_unit", "");
		set("base_value", 10);
		set("base_weight", 60);
		set("food_stuff", 80);
		set("long", "糛糛ψ\n");
	}
	setup();
}

