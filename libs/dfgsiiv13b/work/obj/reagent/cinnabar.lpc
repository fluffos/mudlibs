// cinnabar.c

inherit REAGENT_ITEM;

void create()
{
	set_name("ò", ({ "cinnabar" }));
	set_weight(500);
	if( !clonep() ) {
		set("unit", "");
		set("long", "贺︹膓荐眔‥琌芬う璶\n");
		set("unit_value", 0.6);
	}
	setup();
}
