// bag.c

inherit ITEM;

void create()
{
	set_name("陈ガ砋", ({ "bag" }));
	set_weight(700);
	set_max_encumbrance(30000);
	if( !clonep() ) {
		set("unit", "唉");
		set("value", 130);
		set("long", "唉陈ガ砋ㄓ杆ぃぶ狥﹁\n");
	}
	setup();
}

int accept_object() { return 1; }
