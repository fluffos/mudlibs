// Room: /d/snow/school.c

inherit ROOM;

void create() {
  set("short", "[1;35m过去时空[0m 书院");
  set("long", @LONG
这里是一间宽敞的书院，虽然房子看起来很老旧了，但是打扫得很
整洁，墙壁上挂着一幅山水画，意境颇为不俗，书院的大门开在北边，
西边有一扇木门通往边厢。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "sroad2",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/teacher": 1,
  ]));

  setup();
}
