#include <ansi.h>
inherit ROOM;
void create() {
  set("short", "[31m[1m火龙幻界[0m[1m--[36m蓝色结界[0m");
  set("long", @LONG
来到此地, 你往四周探了探, 这里的环境跟刚才你看到的地方不太一样
. 地上画了许多奇形怪状的图案, 更奇特的是, 上方竟有一道蓝色光芒照射
着这里, 你不禁感到惊奇, 在如此的地方竟有类似极光的景象, 真令你大开
眼界。
LONG);

  set("objects", ([ /* sizeof() == 1 */
    "/open/fire-hole/npc/dragon-b.lpc": 1,
  ]));
  set("exits", ([ /* sizeof() == 5 */
    "northwest": __DIR__ "f-22.lpc",
    "southeast": __DIR__ "f-29.lpc",
    "northeast": __DIR__ "f-23.lpc",
    "west": __DIR__ "fire1.lpc",
    "southwest": __DIR__ "f-28.lpc",
  ]));
  set("outdoors", 1);

  setup();
}
