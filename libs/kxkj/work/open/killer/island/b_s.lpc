#include <ansi.h>
inherit ROOM;
void create() {
  set("short", "[1;37m船舱[2;37;0m");
  set("long", @LONG
除了木制的地板上由于受不了你的重量而多出了两个破洞之外，这里似乎什么
东西也没有，或许你应该往南边前进吧。
LONG);

  set("exits", ([ /* sizeof() == 2 */
    "out": __DIR__ "is15",
    "south": __DIR__ "b_r01",
  ]));

  setup();
}
