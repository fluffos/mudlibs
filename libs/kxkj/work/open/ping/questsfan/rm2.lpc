// Room: /u/d/dhk/questsfan/rm2
inherit ROOM;
#include <ansi.h>
void create() {
  set("short", "山林[1;34m奇[0m路");
  set("long", @LONG
一踏入嵩山的土地，就可以感觉到非常舒适、怡然自得
的感觉，一路上薄薄的轻雾，宛若此地灵气充沛、生机无限
之感，令人不禁感叹嵩山真不愧贵为五岳知首呀。

LONG);

  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/man1.lpc": 1,
  ]));
  set("exits", ([ /* sizeof() == 2 */
    "eastdown": __DIR__ "rm1",
    "eastup": __DIR__ "rm3",
  ]));
  set("outdoors", "/u/d");

  setup();
}
