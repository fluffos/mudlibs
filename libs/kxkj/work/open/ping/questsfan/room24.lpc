// Room: /u/d/dhk/questsfan/room24
inherit ROOM;
#include <ansi.h>
void create() {
  set("short", "方[0;37m晶[0m层");
  set("long", @LONG
这一区的结晶就规则多了，全是长方形式的结晶，
大大小小虽然不同。但是若取之排列想必是造石屋 (还
是高级的那种) 的好建材吧！虽然此地的结晶依然没有
绚丽的颜色，但是它们具有方正、井然有序的气质。若
硬要形容它，那我只能用＂雅″这一个字来形容。

LONG);

  set("outdoors", "/u/d");
  set("no_transmit", 1);
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "room23",
    "north": __DIR__ "room25",
  ]));

  setup();
}
