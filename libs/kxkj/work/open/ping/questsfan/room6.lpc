// Room: /u/d/dhk/questsfan/room6
inherit ROOM;
#include <room.h>
#include <ansi.h>
void create() {
  set("short", "[0;32m后山草丛[0m");
  set("long", @LONG
这里就是大理平南城的后山，但是这里的杂草又高又
长竟比你还高，足足有两丈长。不过此地的风大的异常，
若不多亏这些草，说不定有被风吹倒的危险呢！

LONG);

  set("out_door", 1);
  set("no_transmit", 1);
  set("item_desc", ([ /* sizeof() == 1 */
    "out": "q
",
  ]));
  set("exits", ([ /* sizeof() == 2 */
    "enter": __DIR__ "room5",
    "north": __DIR__ "qroom/room0",
  ]));

  setup();
}
