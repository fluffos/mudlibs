// Room: /u/f/fire/room/room1-27.c
inherit ROOM;
#include <ansi.h>
#include "marksman.h"
void create() {
  set("short", "[1;33m强者之道[2;37;0m");
  set("long", @LONG
这里的四周的墙上张贴着数百幅画像，有很大的有很小的，全部
都是多年前英雄图像。从天花板垂下来的钟摆，不时摇摆着发出滴答
滴答的声音，在生命之泉的另一边御房也是有挂一个。
LONG);

  set("exits", ([ /* sizeof() == 3 */
    "south": __DIR__ "room1-25.lpc",
    "west": __DIR__ "room1-24",
    "north": __DIR__ "room1-29.lpc",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    C_NPC "/bowman1.lpc": 2,
  ]));

  setup();
}
