// Room: /u/f/fire/room/room1-32.c
#include <ansi.h>
#include "marksman.h"
inherit ROOM;

void create() {
  set("short", "[32m民房[2;37;0m");
  set("long", @LONG
这是一间长沙城里的民众所居住的民房，你看到一个简陋的小床
，一张木工造成的小椅子，四四方方的桌子。桌子上放着一些小茶壶
，你似乎看到民众正在做东西。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "room1-10",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    C_NPC "/woman": 1,
    C_NPC "/man": 1,
  ]));

  setup();
}
