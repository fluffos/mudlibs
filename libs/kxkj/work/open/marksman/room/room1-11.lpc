// Room: /u/f/fire/room/room1-11.c
inherit ROOM;
#include <ansi.h>
#include "marksman.h"
void create() {
  set("short", "[32m长沙道[2;37;0m");
  set("long", @LONG
喧嚷的人声，也较为平息了。突然间，你闻到一股浓冽的药香，
不断的朝你飘过来。你仔细的分辨一下方位，似乎是从北边的店铺传
出来的，应该是一间药店。
LONG);

  set("outdoors", "/open/marksman");
  set("exits", ([ /* sizeof() == 3 */
    "east": __DIR__ "room1-9",
    //  "north" : __DIR__"room1-12.lpc",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/open/marksman/npc/oldman": 1,
  ]));

  setup();
}
