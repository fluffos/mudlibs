// Room: /u/f/fire/room/room1-7.c
inherit ROOM;
#include <ansi.h>
#include "marksman.h"
void create() {
  set("short", "[32m长沙道[2;37;0m");
  set("long", @LONG
你走在这长长大大的长沙道上，只感觉这的风景十分美丽，风儿
轻轻的往你的脸庞吹动，似乎把你乌黑的秀发吹散。你看到长沙城的
民众是否都在闲逛着，显示出没有战争的和平城镇。
LONG);

  set("objects", ([ /* sizeof() == 1 */
    C_NPC "/girl": 1,
  ]));
  set("outdoors", "/u/f");
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "room1-4",
    "east": __DIR__ "room1-10.lpc",
  ]));

  setup();
}
