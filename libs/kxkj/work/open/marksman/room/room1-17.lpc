// Room: /u/f/fire/room/room1-17.c
inherit ROOM;
#include <ansi.h>
#include "marksman.h"
void create() {
  set("short", "[1;37m天龙客栈[2;37;0m");
  set("long", @LONG
你正站在天龙客栈之中，虽说这里是城内最大的客栈，这里似乎
是武林人士所休息的地方。里面只摆饰着一些简单的家俱，看到客栈
里面的小二正在忙的不可开交。
LONG);

  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "room1-16",
    "up": __DIR__ "room1-18.lpc",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    C_NPC "/cheng-chong": 1,
  ]));

  setup();
}
