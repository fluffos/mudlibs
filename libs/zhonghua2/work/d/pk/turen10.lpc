// Room: /d/pk/turen10.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create() {
  set("short", "[1;31m宰人场[2;37;0m");
  set("long", @LONG
这里阴森恐怖，地上到处是残肢碎尸，无处不是暗红色的血滴。你心中
忐忑不安，万分警惕，不知道从哪里就会窜出一个黑影。
LONG
  );
  set("trds", "1");
  set("exits", ([ /* sizeof() == 3 */
    "southwest": __DIR__ "turen12",
    "north": __DIR__ "turen6",
    "west": __DIR__ "turen9",
  ]));
  set("no_clean_up", 0);

  setup();
}
