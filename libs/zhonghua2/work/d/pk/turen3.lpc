// Room: /d/pk/turen3.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create() {
  set("short", "[1;31m剁人场[2;37;0m");
  set("long", @LONG
这里阴森恐怖，地上到处是残肢碎尸，无处不是暗红色的血滴。你心中
忐忑不安，万分警惕，不知道从哪里就会窜出一个黑影。
LONG
  );
  set("trds", "1");
  set("exits", ([ /* sizeof() == 3 */
    "northeast": __DIR__ "turen1",
    "east": __DIR__ "turen4",
    "south": __DIR__ "turen7",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
