#include <ansi.h>
inherit BULLETIN_BOARD;

void create() {
  set_name(HIG " 算活草  " + HIR "灌水版" NOR, ({ "board" }));
  set("location", "/u/kan/workroom");
  set("board_id", "shc_b");
  set("long", HIR "奉 旨 灌 水 ，违 者 处 斩 ！\n" NOR);
  setup();
  set("capacity", 100);
}
