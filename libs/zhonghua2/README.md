# 中华英雄苏州站

## 游戏名称

**中华英雄苏州站**（登录横幅完整标题为《中华英雄II武侠世界》，本压缩包是
其中"苏州站"这一具体服务器实例，2006 年 3 月 1 日开站）。核心引擎属于
**ES II 引擎系列**的衍生分支（master 文件署名一路可追溯 "Lil" →
"Annihilator" → "Xiang for XKX" → "Doing Lu for hell"，最后由
"Linux@lxtx" 改写为本作），与本项目其它多个"终极地狱"/"轩辕"系列同属一个
大家族，但内容和玩法已经改动很多，是相对独立的一支。

## 简介

传统武侠题材，玩家扮演初入江湖的普通百姓，通过拜师学艺、行走江湖来逐步
成长。角色创建时可以分别输入姓、名（比如"张"+"三"＝"张三"），系统还提供
"猛士/智慧/耐力/敏捷/均衡"五种角色类型可选，兼顾了传统武侠与一定的养成
玩法。

## 如何运行

```
cd libs/zhonghua2
~/src/fluffos/build-debug/src/driver config.fluffos
```

游戏端口：**40028**

## 如何连接游玩

```
telnet 127.0.0.1 40028
# 或
python3 scripts/mudclient.py 127.0.0.1 40028
```

注册流程比较直观，没有隐藏的编码/客户端门槛：

1. 英文名字 → 确认创建新角色 `y`。
2. 依次输入中文姓氏、名字（各自最多两个汉字，也可以只输入名不输入姓）。
3. 设置"管理密码"（找回密码用）和"普通密码"（日常登录用），各输入两次
   确认。
4. 选择角色类型（1-5，回车默认走"均衡型"）。
5. 选择性别（`m`/`f`），随后就会正式进入游戏世界（起始地点是"世外桃源"）。

## 已知问题

游戏本身有一个"系统清理"精灵，每隔一段时间会自动清空调试日志文件——这是
原作者自带的设计，与游戏本身是否正常运行无关，不用在意。除此之外没有发现
影响主线注册、拜师、`look`/`score` 等基础游玩体验的问题。
- 本轮重新验证：LPC 代码已全部跑过格式化工具，并在重新编译的最新驱动上
  完整回归测试通过（含真实中文姓名"萧"/"峰"注册、重新登录、look/score/
  quit）。**WASM（浏览器/纯 JS）模式下目前无法登录**——这个服务端的版本
  同步系统依赖真实的网络 socket 功能，而 WASM 沙盒环境不提供这项功能，
  会导致每个新连接在看完开场画面后立刻被断开，连输入英文名字的提示都
  看不到；这是 WASM 环境本身的限制，并非游戏内容或本次移植的问题，原生
  （telnet）模式完全不受影响。
- **2026-07 深度功能测试（真实游玩，含两次完整 15 分钟断线重连等待）**：
  修复了 5 个真实 bug，详见 NOTES.md「深度功能测试」一节。其中最严重的
  一个是断线超时强制退出流程（`user_dump()`）里的 `tell_room()` 调用因
  绑定顺序问题绕过了已有的参数类型保护，在断线满 15 分钟强制退出时会
  抛出未捕获的运行时错误——已修复并用真实的完整 15 分钟等待重新验证过
  （驱动全程存活、无报错、重连后角色状态完整）。其余 4 个：每次登录/
  重连都会向玩家打印一句无意义"无效的参数。"（`league.lpc` 缺少
  `"check"` 分支）；每条悄悄话（whisper）都会重复显示两遍（`whisper.lpc`
  的复制粘贴 bug）；开服后第一个老玩家重新登录会直接卡死（`messaged.lpc`
  懒加载时的未捕获错误)；以及"惊雁宫"/"舞蝶山庄"/"麒麟窟"神秘入口系统
  在开服后第一次尝试开启时必定失败且永久失效（`rebornd.lpc` 里 `.c`→
  `.lpc` 改名后遗留的切片宽度错误 + 两处缺失的空指针检查）。
- 2026-07 WASM 适配补丁之后：上述版本同步（versiond）拦断已在 mudlib 侧
  修复——versiond 因缺少 socket 功能而加载失败时，登录流程现在视同
  "版本正常"直接放行；同时本机/回环连接也不再受 IP 封禁和同 IP 连接数
  限制的影响。

## 管理员账号 / Admin account

- **ID**: `fluffos`　**中文名**: 浮云
- **普通密码（日常登录用）**: `Mud@2026`
- **管理密码（找回/改密用）**: `Admin@2026`（本 lib 强制两个密码不能相同，
  因此管理密码与标准约定不同，特此说明）
- **权限**: `(admin)`（本 lib 巫师等级表最高级；通过 `adm/etc/wizlist`
  授予，登录后进入巫师休息室，`update /d/wizard/wizard_room.lpc` 实测
  编译成功）。
- 存档：`data/login/f/fluffos.o` + `data/user/f/fluffos.o`。
- 注意：`feature/dbase.lpc` 有防盗号保护，已在 wizlist 中登记为
  `(admin)` 的 ID 无法在注册流程中设置密码（也无法把登录对象的 id 设成
  该名字）。如需重建账号：先从 wizlist 删除 `fluffos`，注册完成后再加
  回并重启。
- **警告**：如要对公网开放本服务器，请务必先修改这两个密码。
