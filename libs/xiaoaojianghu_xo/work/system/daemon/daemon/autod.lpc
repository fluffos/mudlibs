//autod.c
//圣人曰： 不得已而为之，吾所不为也，然不可拘迂于此。
//圣人说：明知不可为偏要为之，那是笨带鱼。:-)
// force 一个玩家做个命令，那我定义一个alias就避开了
// 根本不可能有效果的。(trill)
// 圣人曰：知其不可为而为之，真圣人也。
// 既然是原本无法解决的东西，那么解决到什么程度都是好的，
// PIG trill.....(lin)
//圣人还曰:两人为何吵架?当和平共处以求安定团结.否则被nerd删掉
//谁也玩不成才是苯带鱼..TWO BIG PIG (lnwm)

int do_command( object who, string cmd );
void create()
{
    seteuid(ROOT_UID);
}
int do_command( object who, string cmd )
{
    if( !who->force_me( cmd ) )
        return 0;
    return 1;
}
    
