// kill_suzhou.c

inherit F_CLEAN_UP;

int update_condition(object me, int duration)
{
    me->apply_condition("kill_suzhou", duration - 1);
    if( !duration )
    {
        write("由于时日长久，苏州官府决定放弃对你的追捕。\n");
        return 0;
    }
    return 1;
}


