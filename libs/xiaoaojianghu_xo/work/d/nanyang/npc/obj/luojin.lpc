#include <armor.h>

inherit HEAD;

void create() {
  set_name("青布罗巾", ({ "luo jin", "cap" }));
  set_weight(200);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "块");
    set("material", "cloth");
    set("armor_prop/armor", 1);
  }
  setup();
}
