inherit ROOM;
void create() {
  set("short", "南阳北门");
  set("long", @LONG
这里向北可翻过伏牛山到洛阳，但因常有强人，一般行人不敢单个通过。只
因南阳伏牛山一派武林中人行侠仗义，多次与盗贼交手，采使强盗略有收敛。
LONG);
  set("exits", ([
    "south": __DIR__ "dajie1",
  ]));
  set("outdoors", "nanyang");
  setup();
}
