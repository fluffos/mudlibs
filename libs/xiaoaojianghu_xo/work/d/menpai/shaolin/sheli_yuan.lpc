// Room: /d/shaolin/sheli_yuan.c
// Date: 05/30/97 07:14PM
// by leonhart

inherit ROOM;

void create() {
  set("short", "舍利院");
  set("long", @LONG
    这里是做法事后烧化骨殖的地方，迎面的香案上杂乱摆着几块灵牌，两边
的布幔已经被熏的有些发黑了，地上到处洒着纸钱，几个僧人正在忙着清理架
子上的骨灰坛，似乎没有注意你的到来。
LONG
  );

  set("exits", ([
    "east": __DIR__ "fatang",
  ]));
  set("objects", ([
    __DIR__ "npc/xuliang": 1,
  ]));

  setup();
  replace_program(ROOM);
}
