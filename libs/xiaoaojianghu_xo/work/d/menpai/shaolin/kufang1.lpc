// Room: /d/shaolin/kufang1.c
// Date: 1997 1 10
// by fencer

inherit ROOM;

void create()
{
        set("short", "防具库");
        set("long", @LONG
这是一间比较宽敞的房间，由于库房装修，各种各样的防具都没有收在柜子
里，而是摊开了挂在架子上。同武器库相比，防具库中的防具数量更多一些，这
同少林僧人习武是为防身健体而非好勇斗狠的一贯宗旨相符。
LONG
        );

        set("exits", ([ /* sizeof() == 1 */
                "east" : __DIR__"kufang.lpc",
        ]));
        set("objects", ([
                __DIR__"npc/xuwen" : 1,
                ]) );
        setup();
        replace_program(ROOM);
}

