// Room: /d/shaolin/shanmen_guangchang.c

#include <ansi.h>

inherit ROOM;

void create() {
  set("short", CYN "广场" NOR);
  set("long", @LONG
从山路甬道拾级而来，弯过一座小山坡，你眼前陡然一阵宽阔。一处平坦的
广场出现在眼前。广场碎石铺地，干净整洁，周围为成荫绿树所环抱。广场尽头
矗立一座雄伟的寺院。寺院背依嵩山挺拔的山势，青瓦黄墙，甚是气魄宏伟。那
就是闻名天下的嵩山少林寺。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "shanmen.lpc",
    "southdown": __DIR__ "shaoshi_dadao2.lpc",
  ]));

  setup();
  replace_program(ROOM);
}
