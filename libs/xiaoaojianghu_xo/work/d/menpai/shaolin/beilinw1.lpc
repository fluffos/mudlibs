// Room: /d/shaolin/beilinw1.c
// Date: 1997 1 10
// by fencer

inherit ROOM;

void create()
{
        set("short", "碑林");
        set("long", @LONG
这里十分安静，到处都是石碑，其中不乏历代高僧的真迹和名人的墨宝。
有些由于年代久远，石碑上的字迹已经模糊不清了。少林历代高僧的佛塔灵藏
都在这里，一座座佛塔约有二三人高，每一座都精工雕凿。有几个小沙弥正在
这里认真的清扫落叶。
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
                "east" : __DIR__"dadao1.lpc",
                "south": __DIR__"beilinw2.lpc",
        ]));
        
        setup();
        replace_program(ROOM);
}

