//舍利子
//shelizi.c
//by leonhart

inherit ITEM;

void create() {
  set_name("舍利子", ({ "shelizi", "sheli" }));
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "高僧坐化后留下的佛骨，浑圆如意，晶莹透明\n");
    set("unit", "枚");
    set("value", 2000);
  }
  setup();
}
