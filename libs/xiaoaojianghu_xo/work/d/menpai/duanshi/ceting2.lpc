// room:/d/menpai/duanshi/ceting2.c  by lala Aug-2-97

#include <room.h>
#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "侧厅");
        set("long", @LONG
这里是王府侧厅，四壁悬满了字画，居中一块横匾，上写「自在堂」三个大
字，下面是一行小字“丁卯自题”，四个角落里摆了好些珍奇古玩，上首是一张
圆桌，几个圆凳。几个家丁一旁侍立。
LONG
        );
    set("exits", ([ 
                "west"  : __DIR__"zoulang27",
                        "north" : __DIR__"canting",
                        "east"  : __DIR__"yanhui_ting",
        ]));
        set("objects", ([
                        __DIR__"npc/jiading1" : 4,
                ]));
        setup();
        replace_program(ROOM);
}                       


