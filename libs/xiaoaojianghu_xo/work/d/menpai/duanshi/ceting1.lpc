// room:/d/menpai/duanshi/ceting1.c  by lala Aug-2-97

#include <room.h>
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "侧厅");
  set("long", @LONG
这里是王府侧厅，四壁悬满了字画，居中一块横匾，上写「逍遥堂」三个大
字，下面是一行小字“丁卯自题”，四个角落里摆了好些珍奇古玩，上首是一张
圆桌，几个圆凳。几个侍女一旁侍立。
LONG
  );
  set("exits", ([
    "east": __DIR__ "zoulang12",
    "north": __DIR__ "huating2",
    "west": __DIR__ "huating1",
  ]));
  set("objects", ([
    __DIR__ "npc/shinv": 4,
  ]));
  setup();
  replace_program(ROOM);
}
