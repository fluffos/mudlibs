// Room: /u/zodiac/xuanwu/neiyuan.c

inherit ROOM;

void create()
{
    set("short", "内院");
    set("long", @LONG
玄武堂的内院，是堂主休息的地方。平时是闲人免进的。当玄武堂日常事物
比较清闲时，堂主会来这里小做休息。东面是一个书房，西边是休息室，堂主有
时也在里面会见本堂的核心成员。
    庭院并不很大。再往北是一处缓坡。再往北就是深不见底的悬崖。
LONG
    );
    set("exits", ([ /* sizeof() == 4 */
        "east"  : __DIR__"shufang",
        "west"  : __DIR__"tangzhu_xiuxi_shi",
        "south" : __DIR__"zhengting",
        "north" : __DIR__"xuanya2",
    ]));
    set("no_clean_up", 0);
    set("outdoors","xuanwu");
    setup();
}

