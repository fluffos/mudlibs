// Room: ceting1.c

inherit ROOM;

void create()
{
        set("short", "侧厅");
        set("long", @LONG
从正厅往北, 穿过一条长长的走廊. 是一座空空的大殿, 大殿里几乎没有什
么陈设. 更显得大厅的空旷和冷漠. 冰凉的大理石地板倒是纤尘不染, 显然经常
有人打扫. 大厅的尽头有一扇小小的门虚掩着, 那里通向一个小偏厅.
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
                "south" : __DIR__"dating",
                "north" : __DIR__"pianting",
        ]));
        set("objects", ([
                __DIR__ "npc/saodi_puren" : 1,
        ]) );
        setup();
}

