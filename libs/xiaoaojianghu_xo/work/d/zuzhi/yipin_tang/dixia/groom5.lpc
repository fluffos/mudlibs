// Room: /u/ysg/room/groom5.c

inherit ROOM;

void create() {
  set("short", "卫士房");
  set("long", @LONG
一走近这里，就感觉到一股森严的气氛。这里靠近总舵关押人犯的地方，所
以到处戒备森严。卫士房里灯火通明，一列红衣卫士刀、剑出鞘，如临大敌，让
你也不由得紧张起来。
LONG
  );

  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "midao26",
  ]));

  set("no_clean_up", 0);
  set("objects", ([
    __DIR__ "npc/weishi2": 2,
  ]));
  setup();
  replace_program(ROOM);
}
