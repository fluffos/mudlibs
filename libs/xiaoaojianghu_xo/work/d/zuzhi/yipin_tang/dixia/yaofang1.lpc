// Room: /u/ysg/room/yaofang1.c
// (C) ysg 5/9/1997

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", "药房");
        set("long", @LONG
这里是玄云坛的药房。还没走进大门，隔着老远，你就闻着一股浓郁的药香
。刚走进去，一位弟子就向你走了过来，殷勤的问你需要些什么药。药房不大，
正对着门的那一面墙搁着一个大橱，橱里面琳琅满目的放着各式各样的成药。
LONG
        );

        set("exits", ([ /* sizeof() == 1 */
            "west" : __DIR__"midao18",
]));      

        setup();
}

