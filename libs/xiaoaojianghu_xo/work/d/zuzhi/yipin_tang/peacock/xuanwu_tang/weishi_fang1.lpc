// Room: /u/zodiac/xuanwu/weishifang1.c

inherit ROOM;

void create()
{
    set("short", "卫士房");
    set("long", @LONG
一品堂是西夏男儿们梦寐以求的地方，少年的豪情令他们在一品
堂中常年干着这又危险，又乏味的卫士之职。当他们换班后回到这里
，往往不能马上休息，而是兴奋的谈论他们心目中偶像们那不可思议
的身手。拳风剑影伴随着低低的激动的话语，到深夜才能宁静。“总
有一天，我要击败他们，取代他们！”。内心一遍又一遍地重复着这
誓言，少年们睡下了，他们中有谁能活到那一天呢？
LONG
    );
    set("exits", ([ /* sizeof() == 1 */
        "west" : __DIR__"zhengmen",
    ]) );
    set("objects", ([ /* sizeof() == 3 */
        __DIR__"npc/weishi2"    : 1,
        __DIR__"npc/weishi3"    : 2,
    ]) );
    set("no_clean_up", 0);

    setup();
    replace_program(ROOM);
}

