// 汴京  wangjing_lou.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit BROOM;
void create()
{
        set("short","望京楼");
        set("long", @LONG
望京楼建在顺天门之上，高三层，飞檐重脊，流金彩绘；初时本来无名，因
唐朝令狐淘曾有“因上此楼望京国”之句，遂起名曰“望京楼”。三层重檐上高
悬一块竖匾，写着“望京楼”三个大字，楼皆有回廊，站在廊上，倚着栏杆而望，
东京城尽在眼下。
LONG
        );
        set("objects", ([ 
         ]));
        set("exits", ([ 
            "down" : __DIR__"xinz_men",
         ]));
        set("detail",([
        ]));
        set("outdoors", "capital");
        setup();
}
void init()
{
}

