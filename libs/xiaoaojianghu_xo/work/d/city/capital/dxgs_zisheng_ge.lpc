// 汴京     /d/city/capital/dxgs_zisheng_ge.c
#include <room.h>
#include <ansi.h>
#include "bianjing.h"

inherit BROOM;  
void create()
{
    set("short",RED "资圣阁" NOR);
    set("long",@LONG
资圣阁是唐天宝年间，玄宗皇帝下旨修建的。阁楼高耸，滴水檐远远伸出楼
柱之外；真宗咸平四年，增建相国寺的时候，从颍川郡迎来了五百铜罗汉，放置
在楼上，以供奉养。都中市民每当夏天时都喜欢在这里乘凉，有“资圣熏风”的
美名。
LONG);
    set("exits",([
        "down" : __DIR__"dxgs_zisheng_men",
    ]));
    set("objects",([
    ]));
    set("detail",([
    ]));
    setup();
}
void init()
{
    ::init();
}

