//汴京STDROOM:    STREET
inherit ROOM;

void setup() {
  seteuid(getuid());
  this_object()->reset();
  if (!this_object()->query("outdoors"))
    this_object()->set("outdoors", "capital");
}
void init() {}
string query_room_type() {
  return "capital_street";
}
