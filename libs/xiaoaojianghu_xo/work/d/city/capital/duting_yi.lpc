// 汴京   duting_yi.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit BROOM;
void create()
{
        set("short","都亭驿");
        set("long", @LONG  
这里是供远道入京的客人住宿休息的驿所，后面一片房舍，三层小楼，几十
间房子都出租给房客。驿站里还有一个兵营，几十名军头日夜出门巡逻，检查行
人防备火灾。院里放着干沙，破布，水桶等等救火用具，墙角一座七八丈高的了
望塔，木干搭成，用来监察火情。出门去是浚仪桥。
LONG
        );
        set("objects", ([        
         ]));
        set("exits", ([  
            "southwest" : __DIR__"junyi_qiao",
            "northeast" : __DIR__"yujie4",
    ]));
         set("outdoors","capital");
    set("detail",([
    ]));
        setup();
}

