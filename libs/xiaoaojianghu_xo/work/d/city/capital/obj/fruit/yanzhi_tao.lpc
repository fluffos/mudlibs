// 汴京 FRUITs:    wumi_tao.c
#include <ansi.h>
inherit ITEM;
inherit F_FRUIT;
void create()
{
    set_name(RED"胭脂桃"NOR, ({"yanzhi tao","tao","fruit"}));
    set_weight(60);
    set("long", "红艳艳的一个桃子，看上去肯定好吃的狠。");
    set("unit", "个");
    set("value", 5);
    set("fruit_remaining", 2); //可以吃5口
    set("food_supply", 6);   //每口加10的food
    set("water_supply", 20);  //每口可以加10的water
    set("eat_msg", "$N拿起$n，大大的啃了一口。\n"); 
    set("end_msg", "$N仔细的啃完了这个$n。\n");  
    set("over_msg", "吃这么多？小心撑破肚皮！！！！\n");
    set("gifts",([
        "per" : 1,
        "int" : 1,
        "spi" : 1,
     ])); 
    set("core", "桃核");     //name
    set("core_id", ({"tao hu", "core"}) ); //id
    set("core_unit", "个");    //unit
    set("core_long", "一个桃核。\n"); //long

    setup();
}
void init()
{
    add_action("do_eat","eat");
}

