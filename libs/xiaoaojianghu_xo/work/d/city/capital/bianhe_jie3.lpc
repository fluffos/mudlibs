// 汴京   bianhe_jie3.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"

inherit STREET;

void create()
{
        set("short",HIW "汴河大街" NOR);
        set("long", @LONG
横连新郑门到新宋(朝阳)门的大街是汴京城中最宽的大街(天街除外)，但是
也只有三十步宽。大街沿着汴河的北岸，北边都是临街的大店铺，花红柳绿，车
马喧沸。东面不远就是宋门了，南边是一溜高高的楼房，原是五代时一个叫周景
威的大将军出钱盖的。
LONG
        );
        set("objects", ([        
         ]));
        set("exits", ([  
            "west" : __DIR__"dxgs_qiao",
            "east" : __DIR__"song_men",
    ]));
    set("detail",([
    ]));
        setup();
}
void init()
{
    ::init();
}

