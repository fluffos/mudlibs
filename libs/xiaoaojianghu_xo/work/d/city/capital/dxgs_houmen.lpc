// 汴京     /d/city/capital/dxgs_houmen.c
#include <room.h>
#include <ansi.h>
#include "bianjing.h"

inherit BROOM;
void create()
{
    set("short","后门" );
    set("long",@LONG  
出了后门北面是一条小巷，直往北面通去。南面是高大的资圣阁，是唐代建
筑的旧物，至今岿然耸立。东西一带都是一些杂用的破房子。
LONG);
    set("exits",([
        "south"    : __DIR__"dxgs_zisheng_men",
        "north"    : __DIR__"tianshui_xiang1",
    ]));
    create_door("north","院门","south",DOOR_CLOSED);
    set("detail",([
    ]));
    set("objects",([
    ]));
    set("outdoors","capital");
    setup();
}
void init()
{
    ::init();
}

