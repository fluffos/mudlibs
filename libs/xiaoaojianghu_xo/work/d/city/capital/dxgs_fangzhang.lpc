// 汴京     /d/city/capital/dxgs_fangzhang.c
#include <room.h>
#include <ansi.h>
#include "bianjing.h"

inherit SROOM;  
void create()
{
    set("short", "方丈" );
    set("long",@LONG
一间小小的房间，收拾得干干净净。这里是大相国寺方丈的居所。
LONG);
    set("exits",([
        "west" : __DIR__"dxgs_zisheng_men",
    ]));
    set("detail",([
    ]));
    set("objects",([
    ]));
    create_door("west","木门","east",DOOR_CLOSED);
    setup();
}
void init()
{
    ::init();    
}

