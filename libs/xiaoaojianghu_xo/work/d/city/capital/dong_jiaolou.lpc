// 汴京  dong_jiaolou.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
        set("short",HIW "东角楼" NOR);
        set("long", @LONG   
皇城的四角上，都建有高大的角楼在城墙之上，既美观漂亮，有可以起到了
望警戒的作用；城头上，不时有队形整齐的守卫官兵走过。城下的广场上一个人
也没有。两边绕过几座供官员上朝前休息的厢房，就进了左掖门。
LONG
        );
        set("objects", ([ 
         ]));
        set("exits", ([    
            "west" : __DIR__"zuoye_men",
            "east" : __DIR__"shizi_jie1",
         ]));
        set("detail",([
        ]));
        set("outdoors", "capital");
        setup();
}

