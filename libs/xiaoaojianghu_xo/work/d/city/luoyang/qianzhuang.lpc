// rooms:/d/luoyang/qianzhuang.c

#include <room.h>

//inherit "system/std/room/bank";
inherit BANK;

void create()
{
        set("short", "钱庄");
        set("long", @LONG
这儿是一家老字号的钱庄，它在全国都有分店，所以可以通存通取。一走进
屋子，就看见许多人正在存取银钱，柜台后的伙计忙忙碌碌。耳朵里听见的都是
哗啦哗啦数钱的声音，就连外面嘈杂的声音这里也听的不太清了。门旁边的墙上
贴着一张告示，上面有钱庄的规矩。
LONG
        );
        set("exits", ([ 
                "south"  :__DIR__"xishi",
        ]));
        set("objects", ([
            __DIR__"npc/qianlaoban": 1 ,  
        ])); 
        setup();
//      replace_program(ROOM);
}                       

