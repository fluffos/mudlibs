//Yangzhou : tianjing.c
//by trill 03/20/97 

inherit ROOM;

void create() {
  set("short", "天井");
  set("long", @LONG
天井甚小，纵深十余步，宽仅五六步，前有洞室，左有门与官衙的院子相通。
在门的北侧，有悬蹬十数级，可由此登山。在洞口的西侧，有石阶数级，可以临
水。如此小的天井内有如此景致，游人到此多凝思玩赏一会，对治此园的工匠不
禁生出几分敬意。
LONG
  );
  set("exits", ([ /* sizeof() ==  */
    "north": __DIR__ "hushi_shandong",
    "northup": __DIR__ "shixing_ting",
    "east": __DIR__ "guanya",
  ]));
  set("objects", ([ /* sizeof() == */
  ]));
  set("detail", ([ /* sizeof() == */
  ]));
  setup();
  replace_program(ROOM);
}
