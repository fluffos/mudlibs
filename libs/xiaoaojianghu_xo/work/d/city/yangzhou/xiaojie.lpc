// Room: /d/city/yangzhou/xiaojie.c

#include <ansi.h>
inherit ROOM;

void create()
{
    set("short", YEL"草河小街"NOR);
    set("long", @LONG
小街西边是扬州官衙的高墙，多开漏窗，倚墙种着槐树，透过槐树、漏窗，
依稀可看到墙内是个园子。东边是官河，杨柳、碧桃临河而植。小街北边通向扬
州的府学「资政书院」；东面是通泗桥，连接着官河对岸的十里长街；南边多为
民宅；西面是通泗街。
LONG
    );
    set("no_clean_up", 0);
    set("outdoors", "yangzhou");
    set("no_kill", "yangzhou");
    set("objects", ([
        __DIR__"npc/youke1" : 1,
        __DIR__"npc/liu_yan" : 1,
    ]));
    set("exits", ([ /* sizeof() == 4 */
        "north" : __DIR__"bei_xiaojie",
        "west" : __DIR__"tongsi_jie",
        "south" : __DIR__"nan_xiaojie",
        "east" : __DIR__"tongsi_qiao",
    ]));

    setup();
    replace_program(ROOM);
}

