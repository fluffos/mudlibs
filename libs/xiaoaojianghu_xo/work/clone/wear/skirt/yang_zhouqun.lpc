//bailing_qunzi.c
#include <armor.h>
#include <ansi.h>
inherit SKIRT;
void create()
{
    set_name(GRN "翡翠撒花洋绉裙" NOR, ({"yang zhouqun", "qunzi", "skirt"}));
    set_weight(1000);
    if ( clonep() )
        set_default_object(__FILE__);
    else {
        set("unit", "条");
        set("value", 2000);
        set("material", "silk");
        set("armor_prop/armor", 1);
        set("armor_prop/dodge", 4);
        set("female_only", 1);
    }
    setup();
}
