// lvcha.c

inherit ITEM;

void create()
{
	set_name("绿茶", ({ "lv cha", "cha", "tea" }) );
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("long",	"这是普普通通的绿茶。\n");
		set("unit", "包");
	set("value", 300);
    	}
	setup();
}
