//indoor.c zip@hero 2000.1.27 房子内部
#include <ansi.h>
#define ID 300
inherit ROOM;
void create() {
  set("short", "[1;37m移[1;35m花[1;36m宫[37;0m");
  set("long", @LONGLONGAGO
   在这里，花开得正盛、菊花、牡丹、蔷薇、梅、桃、兰、曼陀罗、夜来香、郁金香…
这些本不该在同一个地方开放更不该在同一个时候开放的花，此刻却全都在这里开放了。
左手边有一个巨大的玉琢书柜, 里面放着的就是武林中人梦寐以求的移花宫不外传之秘!
LONGLONGAGO);
  set("exits", ([
    "out": "/p/fystreet/street" + (string)ID,
  ]));
  set("owner", "yuanbao");
  set("no_fight", 1);
  set("no_magic", 1);
  set("sleep_room", 1);
  set("if_bed", 1);
  set("NONPC", 1);
  set("no_dazuo", 1);
  set("no_study", 1);
  set("coor/x", 40 + (ID % 2) * 20);
  set("coor/y", 20 + (ID / 100) * 10);
  set("coor/z", 0);
  setup();
  call_other("/p/fystreet/board/yuanbao_board", "???");
}
#include "indoor_func.lpc"
