//indoor.c zip@hero 2000.1.27 房子内部
#include <ansi.h>
#define ID 1000
inherit ROOM;
void create()
{	set("short","[1;37m聚狼宅[37;0m");
	set("long",@LONGLONGAGO
这里虽然聚集了一群饿狼，但是他们的心地着实不坏！
每当神州的MM们遇到什么困难，他们都会义不容辞的挺身而出！
在MM的嘴里，他们有着很好的口碑！很受MM们的喜欢！
LONGLONGAGO	);
	set("exits",([
        "out":"/p/fystreet/street"+(string)ID,
		]));
	set("owner","yazi");
       set("no_fight",1);
       set("no_magic",1);
       set("sleep_room",1);
       set("if_bed",1);
       set("NONPC",1);
       set("no_dazuo",1);
       set("no_study",1);
	set("coor/x",40+(ID%2)*20);
	set("coor/y",20+(ID/100)*10);
	set("coor/z",0);
	setup();
   call_other("/p/fystreet/board/yazi_board","???");
}
#include "indoor_func.lpc"
