//indoor.c zip@hero 2000.1.27 房子内部
#include <ansi.h>
#define ID 1101
inherit ROOM;
void create() {
  set("short", "八戒居[37;0m");
  set("long", @LONGLONGAGO
在正面的墙上是一为美女,赤裸裸的.和尚美女也风流.
她隐逸的朝着你微笑个不停.丝丝的秀发让你 想忘掉所有空.执嫩的小手正朝着你张狂.
忍不住口水都流它个满地落花.
自古风流人尽爱.
和尚美女也成唉
不是螟蛉叫凤凰.
难得春萧值千金
LONGLONGAGO);
  set("exits", ([
    "out": "/p/fystreet/street" + (string)ID,
  ]));
  set("owner", "liuren");
  set("no_fight", 1);
  set("no_magic", 1);
  set("sleep_room", 1);
  set("if_bed", 1);
  set("NONPC", 1);
  set("no_dazuo", 1);
  set("no_study", 1);
  set("coor/x", 40 + (ID % 2) * 20);
  set("coor/y", 20 + (ID / 100) * 10);
  set("coor/z", 0);
  setup();
  call_other("/p/fystreet/board/liuren_board", "???");
}
#include "indoor_func.lpc"
