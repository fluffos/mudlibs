// By FunBoo@sz

#include <ansi.h> 
inherit BULLETIN_BOARD; 
        
void create() 
{ 
	set_name("武功测试留言簿", ({ "board" }) );  
	set("location", "/d/city/muoukt"); 
	set("board_id", "muoub" ); 
	set("long", "武功测试留言簿\n" ); 
	setup(); 
	set("capacity", 300); 
}       
