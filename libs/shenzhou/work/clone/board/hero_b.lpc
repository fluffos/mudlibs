// /clone/board/hero_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("英雄留言板", ({ "board" }) );
 set("location","/d/wulin/guangchang");
	set("board_id", "hero_b");
	set("long", "这是一块泰山英雄留言板。\n" );
	setup();
	set("capacity", 200);
	replace_program(BULLETIN_BOARD);
}

