// Room: /u/s/sueplan/newplan/area/room5
inherit ROOM;

void create ()
{
  set ("short", "[1;37m天同[1;33m星[0m");
  set ("long", @LONG
武王所以能够讨纣成功，称霸天下，原是托父亲文王贡献甚多。后
来，帮助武王，扬威四方的许多军师，将军、忠臣、都是文王所聘用的
。就是说，文王善于调和融合，使部属们忠心爱国。所以虽然然壮志未
酬，但文王去世后，太白金星便请他的灵魂永留‘天同星’，掌管‘融
合’和‘温顺’。

LONG);

  set("no_transmit", 1);
  set("light_up", 1);
  set("exits", ([ /* sizeof() == 2 */
  "out" : __DIR__"room4.lpc",
  "north" : __DIR__"room6.lpc",
]));

  setup();
}
