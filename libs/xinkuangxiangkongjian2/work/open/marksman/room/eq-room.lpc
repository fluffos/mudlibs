// Room: /open/marksman/room/eq-room.c
inherit ROOM;
#include <ansi.h>

void create ()
{
  set ("short", "[1;33m射手EQ室[2;37;0m");
  set ("long", @LONG
这是一间师兄姐要给新加入门派的师弟妹EQ时，都会放在这里
给后进的弟子们使用，当然后进的弟子可要勤练功才行，不姑负大
家的期许。

LONG);

  set("no_fight", 1);
  set("exits", ([ /* sizeof() == 1 */
  "down" : __DIR__"room1-31",
]));
  set("light_up", 1);

  setup();
}
