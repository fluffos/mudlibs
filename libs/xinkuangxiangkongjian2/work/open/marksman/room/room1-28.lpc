// Room: /u/f/fire/room/room1-28.c
inherit ROOM;
#include <ansi.h>
#include "marksman.h"
void create() {
  set("short", "[1;33m强者之道[2;37;0m");
  set("long", @LONG
从这里可以更加的清楚的音乐，不只高亢的琴声，还有钗h旋律
简单而柔和的钢琴声。更令人注意的是在这宫殿的贵族似乎是跟着这
个音乐的节拍在走动，各各脸上露出一副快乐的表情。
LONG);

  set("exits", ([ /* sizeof() == 3 */
    "south": __DIR__ "room1-26.lpc",
    "north": __DIR__ "room1-30.lpc",
    "east": __DIR__ "room1-24",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    C_NPC "/bowman1.lpc": 2,
  ]));

  setup();
}
