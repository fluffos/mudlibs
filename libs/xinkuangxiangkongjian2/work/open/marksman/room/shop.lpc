// Room: /crystal/fantasy/Lbird_shop.c
inherit ROOM;

#include <ansi.h>
#include <room.h>
void create() {
  set("short", "[1;36m长沙镇[1;35m─[1;33m凤凰买卖店[2;37;0m");
  set("long", @LONG
这里是长沙镇著名的凤凰栖息地，它们是为了伴随李广而飞来这里，
由一位小女还照顾它门，也答应当李广门下的座骑，你若是本派门生，
即可买一只来骑，详情请看(sign)。
LONG);

  set("objects", ([ /* sizeof() == 1 */
    "/open/marksman/npc/shopgirl": 1,
  ]));
  set("item_desc", ([ /* sizeof() == 1 */
    "sign": "    如果你想购买一只凤凰的话，在长沙镇中你必须拿到凤凰
认养执照ㄛ，如果你已经符合规定了，请遵照已下程序购买。

          1.选择凤凰--list.
          2.拿出钱来购买凤凰--shop.
          3.购买凤凰专用哨子--shop come-sum.

",
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "room1-11",
  ]));
  set("light_up", 1);

  setup();
}
