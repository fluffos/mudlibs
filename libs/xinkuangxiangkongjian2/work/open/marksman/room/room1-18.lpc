// Room: /u/f/fire/room/room1-18.c
inherit ROOM;
#include <ansi.h>
#include "marksman.h"

void create ()
{
  set ("short", "[1;37m天龙客栈二楼[2;37;0m");
  set ("long", @LONG
这里是天龙客栈的二楼，给过路的旅客们歇歇脚用的。在这里你
可以遇到武林人士。靠窗台的地方摆了约莫七、八副桌椅，擦得光亮
鉴。
LONG);

  set("objects", ([ /* sizeof() == 1 */
  "/open/marksman/npc/small-two" : 1,
]));
  set("exits", ([ /* sizeof() == 2 */
  "down" : __DIR__"room1-17",
  "enter" : __DIR__"room3-1",
]));

  setup();
}
