// Room: /u/f/fire/room/room1-12.c
inherit ROOM;
#include <ansi.h>
#include "marksman.h"
void create ()
{
  set ("short", "[1;31m杨家药店[2;37;0m");
  set ("long", @LONG
你走进一间充满着药味的房子中，这里是一间药铺。在这里你可
以买到各式各样的药材。在房子的四周是一排的木柜子，在柜子上到
处写满了各式的药品名称，令你看了都觉得眼花潦乱。在柜子前面还
有一位长满白胡子的老人不断的开启不同的抽屉拿取药材。
LONG);

  set("objects", ([ /* sizeof() == 1 */
C_NPC"/yangming-teck" : 1,
]));
set("outdoors","/open");
  set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"room1-11",
]));

  setup();
}
