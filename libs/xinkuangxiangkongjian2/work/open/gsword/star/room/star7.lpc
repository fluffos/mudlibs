// Room: /u/b/blue/room/star1.c
#include <ansi.h>
inherit ROOM;

void create ()
{
  set ("short", "[1;37m天枢楼[2;37;0m");
  set ("long", @LONG
这是七星塔中的最后一层, 看了看四周, 心中深感不可思议, 
周围尽是一堆堆的白骨, 白骨旁散落着一堆堆的武器及装备, 你
正打算将功力凝聚到十成时, 楼主已经冲到你的身边, 以下了重
手,令你此时不知所措。
LONG);

  set("exits", ([ /* sizeof() == 2 */
  "up" : "/open/gsword/room/g5-1.lpc",
  "out" : __DIR__"room14.lpc",
]));
  set("light_up", 1);
  set("objects", ([ /* sizeof() == 1 */
  "/open/gsword/star/npc/star2.lpc" : 1,
]));

  setup();
}
