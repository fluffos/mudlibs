// n_area35.c by konn
inherit ROOM;
#include "/open/gblade/room/n_area.h"

void create()
{
        set("short", "客栈");
        set("long",
             n_area(35)
);

        set("exits", ([
            "west"  : __DIR__"n_area33",
      ]));

        set("objects", ([
      ]));

        setup();
}
