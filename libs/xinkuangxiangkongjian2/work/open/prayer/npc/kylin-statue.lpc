#include <weapon.h>
inherit ITEM;
void create() {
  set_name("[31m血麒麟[0m", ({ "red-kylin", "kylin" }));
  set("unit", "个");
  set("long", @LONG

	破邪洞窟中最恐怖的怪兽, 全身如火炎般通红. 一般的高手
	碰上牠非得丧命不可!!其身上的铁链是圣火教教主每次练功
	后所加上去的. 算算铁链共有十数条, 可见圣火教教主的武
	学造诣非常之高, 匪夷所思!!

LONG);
  set("no_get", 1);
  setup();
}
