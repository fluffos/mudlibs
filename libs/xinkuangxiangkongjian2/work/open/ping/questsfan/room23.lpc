// Room: /u/d/dhk/questsfan/room23
inherit ROOM;
#include <ansi.h>
void create() {
  set("short", "菱[0;32m晶[0m层");
  set("long", @LONG
这一区的结晶就没有颜色了，皆呈现透明且各晶石的
菱角会闪烁着些许的微光。而且延途的结晶都呈不规则又
菱菱角角的形状，与之前色彩缤纷、灿烂有着天差地远的
差异。其反映出多变、出奇之气质。若硬要形容它，那我
只能用＂变″这一个字来形容。

LONG);

  set("outdoors", "/u/d");
  set("no_transmit", 1);
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "room22",
    "east": __DIR__ "room24",
  ]));

  setup();
}
