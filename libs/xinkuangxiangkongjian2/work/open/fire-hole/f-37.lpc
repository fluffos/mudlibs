#include <ansi.h>
inherit ROOM;
void create ()
{
  set ("short", "[31m[1m火龙幻界[0m[1m--[35m紫色结界[0m");
  set ("long", @LONG
来到此地, 你往四周探了探, 这里的环境跟刚才你看到的地方不太一样
. 地上画了许多奇形怪状的图案, 更奇特的是, 上方竟有一道白色光芒照射
着这里, 你不禁感到惊奇, 在如此的地方竟有类似极光的景象, 真令你大开
眼界。
LONG);

  set("outdoors", 1);
  set("exits", ([ /* sizeof() == 4 */
  "northwest" : __DIR__"f-31.lpc",
  "southeast" : __DIR__"f-45.lpc",
  "west" : __DIR__"f-36.lpc",
  "east" : __DIR__"f-38.lpc",
]));
  set("objects", ([ /* sizeof() == 1 */
  "/open/fire-hole/npc/dragon-p.lpc" : 1,
]));

  setup();
}
