inherit BULLETIN_BOARD;

void create() {
  set_name("[1;33m图形MUD讨论板[0m", ({ "board" }));
  set("location", "/u/w/wade/grapic_room.lpc");
  set("board_id", "grapic_b");
  set("long", "图形MUD讨论板\n");
  setup();
  set("capacity", 300);
  // 设这样就能让那些人 discard post 啰
  set("master", ({ "wade" }));
}
