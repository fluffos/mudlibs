// inn_2f.c

inherit ROOM;

void create ()
{
  set ("short", "[1;35m过去时空[0m 饮风客栈二楼");
  set ("long", @LONG
你现在来到了饮风客栈的二楼，这家客栈有个很奇怪的规矩，就是只留宿
名头够大的客人，如果你只是一个无名小卒，不论出再多银两都租不到一间空
房，墙边有一道楼梯通往一楼的茶座。
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "down" : __DIR__"inn",
]));

  setup();
}
