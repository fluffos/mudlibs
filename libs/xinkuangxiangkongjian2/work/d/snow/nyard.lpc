// Room: /d/snow/nyard.c

inherit ROOM;

void create ()
{
  set ("short", "[1;35m过去时空[0m 书房");
  set ("long", @LONG
这里是一间整理得相当干净的书房，红木桌椅上铺着蓝绸巾，显得
十分考究，西面的立着一个书架，上面放着一排排的古书，往南走出房
门可以看到天井。
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"inneryard",
]));
  set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/girl" : 1,
]));

  setup();
}
