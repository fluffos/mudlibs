// Room: /d/snow/school2.c

#include <room.h>

inherit ROOM;

void create ()
{
  set ("short", "[1;35m过去时空[0m 淳风武馆教练场");
  set ("long", @LONG
你现在正站在一个宽敞的教练场中，地上铺着黄色的细砂，许多人
正在这里努力地操练着，北边是一间高大的兵器厅，往东则是武馆师父
们休息的大厅。
LONG);

  set("item_desc", ([ /* sizeof() == 1 */
  "west" : (: look_door,     "west" :),
]));
  set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/trainee" : 3,
  __DIR__"npc/fist_trainer" : 1,
]));
  set("outdoors", "snow");
  set("exits", ([ /* sizeof() == 3 */
  "west" : __DIR__"school1",
  "north" : __DIR__"weapon_storage",
  "east" : __DIR__"schoolhall",
]));

  setup();
}
