/*
 * On a road at Silvere — south of mainroad6.
 *
 * Strategy A FluffOS room. Original (south mainroad8) is
 * mainroad7_ldmud.lpc. Archive stub: no AddDetail. South
 * mainroad8 is loaded this slice.
 * Archive keeps “seems to be”.
 */

mapping exits;
mapping extras;

void create() {
  exits = ([
    "north": "/d/silvere/rooms/south/mainroad6",
    "n": "/d/silvere/rooms/south/mainroad6",
    "south": "/d/silvere/rooms/south/mainroad8",
    "s": "/d/silvere/rooms/south/mainroad8",
  ]);
  extras = ([ ]);
}

string query_short() { return "On a road at Silvere"; }

string query_long() {
  return
    "This is a broad road, leading from north to south. To the south is "
    "the citygate, to the north is the palace of the king. The buildings "
    "to the left and right seems to be somewhat foggy, as if they are not "
    "really here.\n";
}

string long() { return query_long(); }

string query_exit(string dir) {
  if (!exits || !dir)
    return 0;
  return exits[dir];
}

string query_exit_string() {
  return "The obvious exits are north and south.\n";
}

string query_item(string s) {
  if (!extras || !s)
    return 0;
  return extras[s];
}

int id(string str) {
  return str == "road" || str == "here";
}
