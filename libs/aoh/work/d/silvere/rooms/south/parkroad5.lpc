/*
 * On a road at Silvere — west of parkroad4.
 *
 * Strategy A FluffOS room. Original (west parkroad6) is
 * parkroad5_ldmud.lpc. Archive stub: no AddDetail. West
 * parkroad6 is not loaded this slice (wall+trees stub;
 * parkenter / park interiors stay closed).
 * Archive keeps “didn't decide yet” and trailing space.
 */

mapping exits;
mapping extras;

void create() {
  exits = ([
    "east": "/d/silvere/rooms/south/parkroad4",
    "e": "/d/silvere/rooms/south/parkroad4",
  ]);
  extras = ([ ]);
}

string query_short() { return "On a road at Silvere"; }

string query_long() {
  return
    "This is a small road from a crossing to the east toward the citypark. "
    "The buildings to the north and south are somewhat foggy, as if someone "
    "didn't decide yet, how they should look like or what to be found there. \n";
}

string long() { return query_long(); }

string query_exit(string dir) {
  if (!exits || !dir)
    return 0;
  return exits[dir];
}

string query_exit_string() {
  return "The obvious exit is east.\n";
}

string query_item(string s) {
  if (!extras || !s)
    return 0;
  return extras[s];
}

int id(string str) {
  return str == "road" || str == "here";
}
