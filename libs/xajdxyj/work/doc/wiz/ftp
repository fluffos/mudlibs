〖 FTP 用法指南 〗

    本 MUD 支持 FTP 文件传输，可以用 WINDOWS 自带的 FTP 软件或
    CtueFTP 等来登录。
    
    WINDOWS 自带的 FTP 的使用步骤：
       点击 WINDOWS 开始菜单中的“运行”，键入 ftp ，回车，运行
       ftp 后键入 open 202.117.11.28 1998 ，回车，然后输入自己在
       MUD 中的 id 和 password 就可以登录了，如果登录失败，还可匿
       名登录，id 是 anonymous ，password 是任意的电子邮件地址，
       例如：fof@mud

    FTP 常用命令：
       ? / help  取得帮助
       !         dos shell，可以察看哪个目录有什么文件
       user      换另一个用户登录
       open      连接到一个 ftp 服务器
       close     断开与服务器的连接
       bye       推出 ftp 程序
       get       下载一个文件
       put       上传一个文件
       cd        改变服务器上的当前目录
       lcd       改变本地硬盘的当前目录
       ls        列目录
       dir       列目录
       ascii     字符模式
       binary    二进制模式
       mkdir     建立新目录
       rmdir     删除目录
       ...

    CuteFTP 的使用方法如下：
      在 Add Host 或 Edit Host 的 General 栏里填上 Host
      Address、User ID & Password，在 Advanced 栏里默认
      的端口 21 改为 1998，再把 Simple dir list 前面的
      小框勾上，就可以连接了。
      注意：不能带目录下载，上传可以，改变当前目录请用
      Ctrl+d，文件操作没有问题。因为权限问题，有些目录
      是不能上传的，请各位巫师把自己编辑的文件传到 /open
      下自己的目录中去。


〖西游记〗 FoF (9/25/99)
