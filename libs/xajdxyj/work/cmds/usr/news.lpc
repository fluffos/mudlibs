// 神话世界·西游记版本４．５０
/* <SecCrypt CPL V3R05> */
 
// news.c
#include <login.h>
inherit F_CLEAN_UP;

int help();

int main(object me, string arg)
{

this_player()->start_more(read_file(MOTD),1);
      return 1;
}

int help()
{
   write(@TEXT
可以用这个命令来看西游记大学生版的最新变动。
TEXT
   );
   return 1;
}
