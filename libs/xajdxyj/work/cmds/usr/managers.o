// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */
 
// managers.c

inherit F_CLEAN_UP;

int main(object me, string arg)
{
   string *list;

   write(MUD_NAME + "目前的管理人员有：\n");
   write("园长　　　：　格斗之迷(fof)\n");
   write("副园长　　：　西风(wester)\n");
   write("专职园丁　：　(无)\n");
   write("园丁学徒　：　(无)\n");
/*
   list = sort_array(SECURITY_D->query_wizlist(), 1);
   for( int i = 0; i < sizeof(list); i ++ )
      printf("%-15s%c", list[i],  (i%5==4) ? '\n' : ' ');
*/
   write("\n");
   return 1;
}

int help(object me)
{
write(@HELP
指令格式 : wlist

用途 : 列出目前本ＭＵＤ所有的管理人员名单。
HELP
     );
     return 1;
}

