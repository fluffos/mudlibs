// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */
 
#include <ansi.h>

inherit F_CLEAN_UP;

int main(object me, string arg)
{
   object *user ;
        user = users();
        TASK_D->init_dynamic_quest(1);
        message("vision", HIW "\n【任务】欢乐园任务重新分配 ．．．", users());
	message("vision", HIW "分配完毕。\n" NOR, users()); 
	return 1;
}

int help(object me)
{
write(@HELP

更新task.
HELP
    );
    return 1;
}
