
#include <ansi.h>
inherit ITEM;

void create()
{
  set_name( HIR "一颗头颅" NOR , ({"toulu"}));
  set_weight(90);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "颗");
    set("long", HIR "通缉犯的头颅。\n"NOR);
    set("value", 0);
  }
   set("is_monitored",1);
   setup();
}
