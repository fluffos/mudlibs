// Room: /u/fof/workroom.c

inherit ROOM;

void create()
{
	set("short", "空房间");
	set("long", @LONG
这是一间什么也没有的空房间。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "aolai" : "/d/4world/center",
  "kezhan" : "/d/city/kezhan",
]));
        set("objects", ([ /* sizeof() == 1 */
  "/d/changan/npc/qianmian" : 1,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
