// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */
 
// entrance.c

#include <login.h>
#include <room.h>
#include <ansi.h>

inherit ROOM;

void create()
{
    set("short", HIY"⊙"HIG"欢乐园"HIY"⊙"NOR);
    set("long", @LONG

欢迎来到欢乐园进行西游之旅！
欢乐园 Mud 是由西安交通大学网络中心 Internet 学会 Mud 小组管理，
使用。
LONG);

    set("exits", ([
        "north" : __DIR__"system",
        "west" : __DIR__"register",
        "east": "/d/city/xuanwu-n3",
    ]) );
    set("no_fight", 1);
    set("no_magic", 1);
    set("valid_startroom", 1);

    setup();
    call_other( "/obj/board/query_b", "???" );
    call_other( "/obj/board/pop_bbs", "???" );
}

