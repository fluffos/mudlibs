// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */
 
// Room: /d/wiz/punish.c

inherit ROOM;

void create()
{
   set("short", "死牢");
   set("long", @LONG

进了这里，你活下来的希望已经很小了。
LONG
   );
   
   set("valid_startroom", 1);

   setup();
   
}

void init() // FoF changed 2000-1-12
{
	object who = this_player();
	if( !wizardp(who) ) {
    	who->set("startroom", __FILE__);
		who->save();
		add_action("block_cmd", "", 1);
		call_out("set_free", 4320, who);
		message_vision("一个声音吆喝道：$N，老老实实给我蹲三天！\n", who);
	}
	call_out("hehe", 2, who);
}

int block_cmd()
{
	string verb = query_verb();

	if( verb == "say" ) return 0; //allow say
	if( verb == "help" ) return 0;
	if( verb == "look" ) return 0;
	if( verb == "quit" ) return 0;
	return 1;
}

// added by bula, just for fun.

void hehe()
{
    if( ! this_player() ) return;
    
	tell_room(environment(this_player()), "\n" +
     "隔壁的牢房里传来阵阵惨叫声！\n" +
     "你隐隐约约听到隔壁牢房里有人大喊：你招还是不招！\n" +
     "隔壁的牢房里传来阵阵皮鞭声！\n" +
     "你隐隐约约听到隔壁牢房里有人喊到：我们的政策是坦白从宽，抗拒从严！\n" +
     "你隐隐约约听到有人说话，可又听不大清楚！\n" +
     "隔壁的牢房里有人笑道：你早些招了，就不必吃这顿鞭子了！\n");
	return;
}

void set_free(object who)
{
    if( ! who ) return;
    
	message_vision("两个狱卒走过来，把$N领了出去。\n", who);
	tell_object(who, "你的刑期已满，出去好好做人吧！\n");
	who->move("/d/city/kezhan");
	who->set("startroom", "/d/city/kezhan");
	who->set("robot_accusing", 0);
	who->save();
	return;
}

