// Room: /d/changan/name-office.c

inherit ROOM;

void create() {
  set("short", "户管所");
  set("long", @LONG
这里是掌管西游记户籍的户管所，你可以到这里来更改名字。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "eside5",
  ]));
  set("no_clean_up", 0);
  set("objects", ([ /* sizeof() == 3 */
    __DIR__ "npc/nofficer": 1,
    "/d/obj/misc/tables": 1,
    "/d/obj/misc/chairs": 2,
  ]));

  setup();
  replace_program(ROOM);
}
