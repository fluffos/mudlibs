// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */

#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", HIR"革命历史博物馆"NOR);
	set("long", @LONG
这里是革命历史博物馆，走进了这里就如同回到了中国的近现代史。
一件件展品都分别记载了中国人民革命的一段历史，或是血泪史，或是
悲痛史；或为沉痛，或为悲壮；但都是历史的真实。我们在这里缅回革
命先烈的同时不该望记他们所流过的鲜血，更不该忘记那一段段曲辱的
历史。

LONG
	);
	set("exits", ([ /* sizeof() == 1 */
		"out" : __DIR__"guangchang",
	]));
	set("no_magic", "1");
	set("no_clean_up", 0);
	set("no_steal", "1");
	set("no_fight", "1");

	setup();
	replace_program(ROOM);
}

