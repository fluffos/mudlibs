// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIR "天安门城楼" NOR);
  set("long", @LONG
这里是中国北京的天安门城楼，是中国的象征，她高大而且庄
严，朱红的色彩使得她优雅而且美丽站在这里，可以看到整个北京
正在以日新月异的高速度发展，整个北京发出勃勃的生机。

LONG);
  set("no_fight", 1);
  set("no_magic", 1);
  set("no_steal", 1);
  set("outdoors", "beijing");
  set("exits", ([ /* sizeof() == 1 */
    "southdown": __DIR__ "center",
  ]));
  replace_program(ROOM);
  setup();
}
