// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */
 
//standroom.c used by weiqi...others may hate this format:D
//bingdao.c
//by FoF (10/25/1999)

inherit ROOM;

void create ()
{
   set ("short", "冰道");
   set ("long", @LONG

好险的一条冰道！旁边是万丈深渊，往下一看，不由得头晕目眩，
你赶紧闭上眼睛，生怕一个不慎，摔得粉身碎骨。

LONG);

   //set("item_desc", ([" *** " : " *** ", ]));
   //for look something.

   set("exits", 
   ([ //sizeof() == 4
     "up" : __DIR__"bingya",
     "southwest" : __DIR__"fabao-room",
   ]));
   
   set("objects", 
   ([ //sizeof() == 1
   ]));

   set("outdoors", "moon");

   setup();
}

