// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */
 
// entrance.c

#include <login.h>
#include <room.h>
#include <ansi.h>

inherit ROOM;

void create()
{
    set("short", "国宾馆大厅");
    set("long", @LONG

大厅相当的大，能容几百人开宴会。大厅内各国使节往来不断，不
同方言充斥耳间。桌上摆了各种新鲜水果，美味佳肴，招待着四方
来客。一群舞女随着节拍翩翩起舞，娥娜多姿。
LONG);

    set("exits", ([
        "north" : __DIR__"system",
        "east": "/d/city/xuanwu-n3",
    ]) );
    set("no_fight", 1);
    set("no_magic", 1);
    set("valid_startroom", 1);

    setup();
    call_other( "/obj/board/query_b", "???" );
    call_other( "/obj/board/pop_bbs", "???" );
}

