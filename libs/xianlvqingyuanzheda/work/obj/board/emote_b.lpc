// by bugbug & alading @ 缥缈水云间, 2003-4-5

inherit BULLETIN_BOARD;
#include <ansi.h>

void create()
{
            set_name("仙侣情缘emote征求板", ({ "board" }) );
          set("location", "/u/mudos/kezhan3");
          set("board_id", "emote_b");
          set("long",     "这是供巫师和玩家互相交流，共同增加仙侣情缘emote的讨论板。\n" );
	setup();
          set("capacity", 100);
	replace_program(BULLETIN_BOARD);
}

