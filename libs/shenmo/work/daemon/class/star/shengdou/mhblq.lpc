// 双子---魔皇崩裂拳

#include <ansi.h>
#include <combat.h>

inherit SSERVER;

int perform(object me, object target)
{
	int damage;

	if( !target ) target = offensive_target(me);

	if( !target
	||	!target->is_character()
	||	!me->is_fighting(target) )
		return notify_fail("你不在战斗中。\n");

        if( target->is_busy() )
                return notify_fail(target->name()+"已经够忙了，你使的再漂亮也不会注意到。\n");

	if( me->query("force") <= 600 )
		return notify_fail("你的内力不够！\n");

	if( me->query("sen") <= 300 )
		return notify_fail("你的精力不够！\n");


	if( !target || !me->is_fighting(target) ) return 0;

	if( !living(me) || me->is_ghost() ) return 0;

	if( !target || environment(me) != environment(target) || !me->is_fighting() ) return 0;


       message_vision(HIB "\n$N冷冷喝道：魔皇崩裂拳，双手抱拳，猛运劲力，全身气劲激荡不已。\n" NOR, me,target);

       message_vision(HIC"\n$n疑是猛招前奏，暗自提防。不料$N一击至地，顿时地裂天崩，轰鸣不息。\n",me,target);

         if( me->query("combat_exp")*5/10 > random(target->query("combat_exp")) ){

              message_vision(HIR"$n一时把持不住，平衡失调，扑倒在地。\n\n"NOR,me,target);

	 	target->start_busy(3+random(1));

          }else{
              
             message_vision(HIG"$n大惊，急忙沉身扎马，勉强控住平衡。\n"NOR,me,target);
               }

        me->add("force",-200);
        me->start_busy(1+random(1));
            return 1;
}

         
