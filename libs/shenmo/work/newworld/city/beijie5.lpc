//大槐国city

#include <room.h>
inherit ROOM;
#include <ansi.h>
void create() {
  set("short", "诗礼街");
  set("long", @LONG

自高宗崇文颂武以来,这里便是王侯将相中文官之贵居住之地。
大路两旁松柏耸立，幽雅别致。
北边乃中书街，东临三公府邸，往西则是三品以下文官之官驿。
LONG);
  set("exits", ([
    "north": __DIR__ "zhongshu1",
    "west": __DIR__ "wenguan",
    "east": __DIR__ "beijie4",
  ]));
  set("objects", ([
    //    __DIR__"npc/" : 1,
  ]));
  create_door("west", "朱漆大门", "east", DOOR_CLOSED);
  set("no_clean_up", 0);
  setup();
}
