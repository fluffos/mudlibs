#include <ansi.h>

inherit ROOM;

void create ()
{
set ("short",HIM"般若寺"NOR );
set ("long", HIY+@LONG

化生寺是长安城里规模最大的一家丛林了。整日里香烟袅绕，颂经声
不绝于耳。来这里上香还愿的善男信女也特别多，若到了皇道吉日，
更是人山人海。

LONG+NOR);

set("exits", ([
	"south" : __DIR__"siyuan",
        "north" : __DIR__"xijie2",
]));

set("objects",([ 
      __DIR__"npc/sengren" : random(3),
]));

set("outdoors", "city");
set("no_clean_up", 0);
setup();
replace_program(ROOM);
}


