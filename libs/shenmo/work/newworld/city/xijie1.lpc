//大槐国 city

inherit ROOM;
#include <ansi.h>

void create ()
{
set ("short","西城里");
set ("long", @LONG

这里便是都城的商业区，东面就快到城中心了，耸立于前。
北面静静的是一家书局，来往多是些读书人。南面是一家
钱庄，整天看见客人进进出出，显得生意很兴隆。

LONG);

set("exits", ([ 
	"south" : __DIR__"bank",
	"north" : __DIR__"bookstore",
	"west" : __DIR__"xijie2",
	"east" : __DIR__"center",
]));

set("objects", ([
// __DIR__"npc/libai" : 1,
]));

set("outdoors", "city");
set("no_clean_up", 0);
setup();
}


