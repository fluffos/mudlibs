//大槐国 city

inherit ROOM;
#include <ansi.h>
void create() {
  set("short", "西城里");
  set("long", @LONG

这里便是都城的商业区，东面就快到城中心了。北边是家大的
米店，里面人来人往，忙碌非常。南边却是家精巧别致的首饰
店，往西就是西门了。

LONG);

  set("exits", ([
    "south": __DIR__ "shoushi",
    "north": __DIR__ "midian",
    "west": __DIR__ "ximen",
    "east": __DIR__ "xijie2",
  ]));

  set("objects", ([
    //__DIR__"npc/libai" : 1,
  ]));

  set("outdoors", "city");
  setup();
}
