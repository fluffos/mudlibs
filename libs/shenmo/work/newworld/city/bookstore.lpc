#include <ansi.h>
#include "fight.h"
inherit ROOM;

void fullup(object obj);
void create ()
{
set ("short", HIY"京都"NOR HIW"书局"NOR);
set ("long", @LONG

这是一家百年老字号的书店。书架上整整齐齐地放着一些手抄的卷轴。
雕板印刷书到了唐朝已日趋成熟，因此这里的书架上也放着不少印制
精美图文并茂的图书。

LONG);


set("exits",([
	"south" : __DIR__"xijie1",
]));

set("objects",([ //sizeof() == 1
      __DIR__"npc/bookseller" : 1,
]));
set("no_fight",1);
set("no_magic",1);
setup();
}

