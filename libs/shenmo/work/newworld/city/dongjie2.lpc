//大槐国 city

inherit ROOM;
#include <ansi.h>
void create ()
{
set ("short","东城里");
set ("long", @LONG

这里便是都城的商业区，西面就快到城中心了。北边是家大的
镖局，门口人来人往，生意很是兴旺。南边是家新建的杂货店
，往东走就可以出都城了。

LONG);

set("exits", ([ 
	"south" : __DIR__"zahuo",
	"north" : __DIR__"biaoju",
	"west" : __DIR__"dongjie1",
	"east" : __DIR__"dongjie3",
]));

set("objects", ([
	//__DIR__"npc/libai" : 1,
]));

set("outdoors", "city");
setup();
}


