#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "兵器铺");
  set("long", @LONG

刚一进门就看到兵器架上摆着各种兵器，从上百斤重的大刀到轻如芥
子的飞磺石，是应有尽有。女老板是老英雄欧振远的小女儿，也是振
远镖局二当家，巾帼不让须眉。

LONG);

  set("exits", ([
    "north": __DIR__ "dongjie1",
  ]));

  set("objects", ([
    __DIR__ "npc/wuqi-boss": 1,
  ]));
  set("no_clean_up", 0);
  setup();
}
