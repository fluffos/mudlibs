//shoushi.c

#include <ansi.h>
#include "fight.h"
inherit ROOM;

void fullup(object obj);
void create ()
{
set ("short", HIY"首饰店"NOR);
set ("long", @LONG

这儿是京都最热闹的地段，新开了一家首饰店。店内布置精巧，各地
的能工巧匠所制之首饰云集在此。

LONG);


set("exits",([
	"north" : __DIR__"xijie3",
]));

set("objects",([ //sizeof() == 1
      __DIR__"npc/shoushi-boss" : 1,
]));
set("no_fight",1);
set("no_magic",1);
setup();
}

