//changan city

inherit ROOM;
#include <ansi.h>
void create ()
{
  set ("short","西城里");
        set ("long", @LONG

这里便是都城的商业区，东面就快到城中心了。北边是家大的
酒楼，里面唏唏嚷嚷，热闹非凡。而南却是家规模不小的寺院，
往西就快要出城了。

LONG);

set("exits", ([ 
	"south" : __DIR__"banruosi",
	"north" : __DIR__"drink",
	"west" : __DIR__"xijie3",
	"east" : __DIR__"xijie1",
]));

set("objects", ([
	//__DIR__"npc/libai" : 1,
]));

set("outdoors", "city");
setup();
}


