inherit "/d/maze/zizhulin2/room.lpc";

void create() 
{ 
        set("short", "花海入口"); 
  	set ("long", @LONG
放眼望去，但见天地间仿佛已被鲜花充满，但却是无人认得出的花朵，只觉得这些
花无比的鲜艳，无比的美丽。真是令人想不到，荒漠之中，竟有这样的花海。
    北面是一片花海，凝眸仙子开辟的【百花秘境】便在其中，往南去便是回百花谷的路。
LONG);        
        set("exits", ([
                "north" : __DIR__"maze/entry", 
                "south" : sprintf("/d/penglai/baihuagu%s",({ sprintf("%d",random(5)), sprintf("%d",10+random(5)), sprintf("%d",20+random(5)),
        		  sprintf("%d",30+random(5)),sprintf("%d",40+random(5)), })[random(5)] )
        ])); 
        set("objects",([
        	"/d/maze/bhg-baihua/npc/npc" : 1,
        ]));	
        set("virtual_room",1);
        setup();
} 