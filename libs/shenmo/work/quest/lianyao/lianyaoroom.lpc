//Cracked by Roath
//changan city

inherit ROOM;

void create() {
  set("short", "炼药室");
  set("long", @LONG

这是一间炼制各种仙丹妙药的炼药室。在这里可以用您采集的药材炼制各种药品。
炼药室正中摆着一尊大药鼎，地脉之火不断上喷，整个炼药室热气扑面。
要炼制药品请lianzhi 药材。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "south": "/d/city/droproom",
    ]));

  set("objects",
    ([  //sizeof() == 1
    //                __DIR__"npc/bookseller" : 1,
    ]));


  //        set("outdoors", "changan");
  set("no_clean_up", 0);
  setup();
}
