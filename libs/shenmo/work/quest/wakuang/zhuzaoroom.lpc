//Cracked by Roath
//changan city

inherit ROOM;

void create() {
  set("short", "铸造室");
  set("long", @LONG

这是一间铸造各式装备的铸造室。在这里可以用您挖来的矿石随机铸造各种装备。
可以铸造的装备有鞋子、头盔、戒指、腰带、护腕等。
要铸造装备请zhuzao 矿石。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));
  set("no_fight", 1);
  set("no_magic", 1);

  set("exits",
    ([  //sizeof() == 4
    "west": "/d/city/droproom",
    ]));

  set("objects",
    ([  //sizeof() == 1
    "/d/quest/zhuangbei/npc/chai": 1,
    ]));


  //        set("outdoors", "changan");
  set("no_clean_up", 0);
  setup();
}
