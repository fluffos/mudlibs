//Cracked by Roath
//changan city

inherit ROOM;

void create() {
  set("short", "厨房");
  set("long", @LONG

这是一间烹饪各种美食的厨房。在这里可以用您打猎或者钓鱼取得的食材烹饪各种美食。
厨房里装修非常豪华，有一大排厨具、调料摆在桌上，上面还挂着吸力强大的抽油烟机。
要烹饪美食请pengren 食材。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "east": "/d/city/droproom",
    ]));

  set("objects",
    ([  //sizeof() == 1
    //                __DIR__"npc/bookseller" : 1,
    ]));


  //        set("outdoors", "changan");
  set("no_clean_up", 0);
  setup();
}
