#include <ansi.h>
inherit ITEM;
void create(){  
    set_name(HIR"令牌"NOR, ({"lingpai","ling pai","pai"}));  
    set("unit", "个");
    set("long", "一个从强盗那里抢来的灵牌，上面记录了这次抢劫的罪证。\n"); 
    set("value", 10000); 
    set("no_get", 1); 
    set("no_drop", 1); 
    setup();
}

