inherit ROOM;
#include <ansi.h>;
void create()
{
        set("short", "水帘洞内");
         set ("long", @LONG
此处便是水帘洞内，似乎四周已经没有再进入的路了。
LONG);
                     
set("exits", ([
      "out": "/d/dntg/hgs/pubu", 
        ]));

  set("objects", ([ 
     // "/u/xdao/hgs/npc/hgsshanshen.lpc" : 1,
        ]));
  set("no_fight", 1);
  set("no_magic", 1);
  set("freeze", 1);
set("no_clean_up", 1);
set("no_time", 1);
        setup();
}

