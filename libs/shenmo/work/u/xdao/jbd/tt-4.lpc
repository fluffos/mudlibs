inherit ROOM;
#include <ansi.h>;
void create() {
  set("short", "通道");
  set("long", @LONG
这里是通往封印中心的通道，宽阔无比！四周墙壁全是用极品仙玉铸造而成，
晶莹剔透，仙气环绕，前方似乎是封印中心，你不仅加快了脚步。
LONG);
  set("exits", ([
    "south": __DIR__ "tt-3",
    "north": __DIR__ "tt-5",
  ]));
  setup();
}
