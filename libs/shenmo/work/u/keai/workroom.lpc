#include <ansi.h>
inherit ROOM;
void create()
{
string msg;
          set ("short",  HBBLU HIR"乐乐"HBBLU HIC"小屋"NOR);
  //@LONG..LONG格式的，只能在两头加颜色，中间加了无效
  msg = @LONG

  ≈☆≈°．≈★≈ ．·°≈☆≈°∴°．≈★≈°．≈☆≈  

                ☆☆ ★★  ★★ ☆☆☆
               ★★☆☆★★★★☆☆★★ 
              ☆☆★☆★☆★☆★☆★☆☆
               ★★☆☆★☆★☆★☆★★
                ☆☆★★☆☆★★☆☆☆
                  ★★☆☆★★☆★★
                    ☆☆★☆★☆☆
                      ★★☆★★
                         ☆☆

   ≈☆≈°．≈★≈ ．·°≈☆≈°∴°．≈★≈°．≈☆≈  
LONG;
    msg = replace_string(msg,"☆",NOR BLINK HIG"☆"NOR);
    msg = replace_string(msg,"★",NOR BLINK HIR"★"NOR);
  msg = replace_string(msg,"≈",NOR YEL"≈"NOR);
  set("long",msg);
          set("exits", 
     ([ //sizeof() == 4
                   "kz" : "/d/city/kezhan",
   "wiz" : "/d/wiz/wizroom",
                          ]));


           set("no_time", 1);
      set("chat_room",1);
          set("no_fight", 1);
    set("no_magic", 1);
          set("freeze",1);
    set("resource", ([ /* sizeof() == 1 */
    "water" : 1,
  ]));
    set("valid_startroom", 1);
    setup();
  set("sleep_room",1);
          set("objects", ([ /* sizeof() == 2 */
  ]));
          set("no_clean_up", 0);
          setup();
  }

