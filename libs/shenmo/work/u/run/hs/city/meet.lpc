inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "魔幻工会" NOR);
  set("long", @LONG

这里是城中的魔幻工会，基本是处理城市的日常运作。传闻与血族
卡玛利拉（Camarilla）为抵制共同的敌人魔宴（The Sabbat）而结有
盟约。

LONG);
  set("exits", ([
    "south": __DIR__ "jie-e1",
  ]));
  set("objects", ([
    //        __DIR__"npc/"     : 1,
  ]));
  set("no_fight", 1);
  set("no_magic", 1);
  setup();
}
