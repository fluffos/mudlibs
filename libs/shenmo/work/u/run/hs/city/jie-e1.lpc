#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIR "火之大街" NOR);
  set("long", @LONG

你走在一条宽阔的石板大街上，西面就快到城中心了，可以看到钟楼
耸立于前。北面是本城的魔幻士工会。

LONG);
  set("exits", ([
    "north": __DIR__ "meet",
    "west": __DIR__ "center",
    "east": __DIR__ "jie-e2",
  ]));
  setup();
}
