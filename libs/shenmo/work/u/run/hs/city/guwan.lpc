inherit ROOM;
#include <ansi.h>

void create ()
{
set("short","古玩店");
set("long", @LONG

这里是城中的古玩店，专门出售一些希奇古怪的东西，如果
你有兴趣，可以在这里耐心的寻找，说不定有意想不到的收获。

LONG);
set("exits", ([ 
        "east" : __DIR__"jie-n1",
]));
set("objects",([ 
        __DIR__"npc/boss/guwan"  : 1,
]));
set("no_fight", 1);
set("no_magic", 1);
setup();
}

