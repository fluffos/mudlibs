#include <ansi.h>
inherit ROOM;

void create ()
{
set("short",HIR"火の大街"NOR);
set("long", @LONG
你走在一条宽阔的石板大街上，西面就快到城中心了，可以看到钟楼
耸立于前。南面便是魔甲加工店。
LONG);
set("exits",([
        "south" : __DIR__"robit",
        "west" : __DIR__"jie-e1",
        "east" : __DIR__"gate-e",
]));
setup();
}

