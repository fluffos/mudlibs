inherit ROOM;
#include <ansi.h>

void create ()
{
set("short",HIG"水の扉"NOR);
set("long", @LONG

这里便是出西郊的唯一通道了。
由于魔幻术的兴起，整个大陆也陆续出现了一些不可思义的事件。
所以，如果没有魔幻工会的手札，守兵是不会随便让人出城的。

LONG);
set("exits", ([ 
        "east" : __DIR__"jie-w2",
]));
set("objects",([ 
        __DIR__"npc/guard-shui"  : 1,
]));
setup();
}

