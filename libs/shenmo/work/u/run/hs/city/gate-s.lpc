inherit ROOM;
#include <ansi.h>

void create ()
{
set("short",HIC"风の扉"NOR);
set("long", @LONG

这里便是出南郊的唯一通道了。
由于魔幻术的兴起，整个大陆也陆续出现了一些不可思义的事件。
所以，如果没有魔幻工会的手札，守兵是不会随便让人出城的。

LONG);
set("exits", ([ 
        "north" : __DIR__"jie-s1",
        "south" : __DIR__"feng/road1",
]));
set("objects",([ 
        __DIR__"npc/guard-feng"  : 1,
]));
setup();
}

