#include <ansi.h>
inherit NPC;
int do_huan();
void create()
{
	set_name("幻.镜", ({"huanjing"}));
	set("title", HIR"一砂一世界"NOR);
	set("gender", "男性");
	set("age", 25);
	set("kee", 350); 
	set("max_kee", 350);
   set("combat_exp",10000);
   set("daoxing",10000);
	set("sen", 200);
	set("max_sen", 200);	
	set("combat_exp", 10000);
	set("attitude", "friendly");
	set("env/wimpy", 50);
	set("chat_chance", 10);

	set_skill("unarmed", 40);
	set_skill("dodge", 40);
        set("inquiry", ([
"何为幻镜":"幻镜就是所谓的副本",
"幻境介绍":(:do_huan:),
"进入幻境":(:do_huan:),
                             ]) );

	setup();
      //  carry_object("/d/obj/cloth/choupao")->wear();
	
}
int do_huan()
{
    command("say 幻镜系统开发当中\n");
    return 1;
}

