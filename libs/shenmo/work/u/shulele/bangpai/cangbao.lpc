//Cracked by Roath

inherit ROOM;
inherit "/u/weilai/bangpai/jzfw.lpc";
int do_jianzao(string name);
void create() {
  set("short", "藏宝室");
  set("long", @LONG

  这里是帮派辛苦搜刮的财宝所在地
LONG);
  set("exits", ([

  ]));
  set("objects", ([
    "/u/weilai/bangpai/npc/ck": 1,
    "/u/weilai/bangpai/npc/hw2": 1,
    "/u/weilai/bangpai/npc/hw3": 1,

  ]));
  setup();
}
void init() {
  add_action("do_jianzao", "jianzao");
}
int do_jianzao(string name) {
  object me;
  me = this_player();
  if (!me->query("jianf")) {
    write("未经批准，不准建房");
    return 1;
  }
  if (!name)
    write("输入");
  if (!jfzl(name))
    return 1;
  zaofan("jian", name);
  return 1;
}
