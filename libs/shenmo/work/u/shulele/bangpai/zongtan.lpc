//changan city

inherit ROOM;
#include <ansi.h>
void create() {
  set("short", "聚义厅");
  set("long", @LONG
        这是帮派豪杰开会的地方
        LONG);

        //set("item_desc", ([" *** " : " *** ", ]));

        set("exits", 
        ([ //sizeof() == 4
                "west" : __DIR__"zhuque-s2",
        ]));

        set("objects", 
        ([ //sizeof() == 1
                __DIR__"npc/yang" : 1,
               "/d/obj/misc/tables" : 1,
               "/d/obj/misc/chairs" : 2,
        ]));

//        set("outdoors", "changan");
	set("no_clean_up", 0);
        setup();
	replace_program(ROOM);
}


