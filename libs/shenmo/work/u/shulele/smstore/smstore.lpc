#include <ansi.h>
inherit ROOM;
void create() {
  set("short", "黑市");
  set("long", @LONG
这里就是传说中的黑市，在这个地方，贩卖着各种各样的违禁物品。
同时，这里也是三教九流的聚集地，所以要特别小心。
LONG
  );


  set("exits", ([
    "out": "/d/city/kezhan",

  ]));
  set("objects", ([
    "/u/shulele/smstore/npc/hslb": 1,
  ]));
  set("no_fight", 1);
  set("no_exercise", 1);
  set("no_respirate", 1);
  set("no_rideto", 1);
  set("no_exert", 1);
  setup();

}
