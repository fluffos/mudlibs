//Cracked by Roath
//changan city

inherit ROOM;

void create() {
  set("short", "出师之地");
  set("long", @LONG

这里是专门处理出师事宜的地方。盛传江湖上的高手，大多都从这里
走向三界之巅的。不过，从满地干过的血迹可以看出，这个地方埋葬
了很多所谓的武林高手，因此建议你轻易不要进来比试。否则只能同
地上干过的血迹一样，成为这圣地，不容侵犯的标志！
LONG);


  set("exits",
    ([  //sizeof() == 4
    "down": "/d/city/clubbar",
    ]));

  set("objects",
    ([  //sizeof() == 1
    "/u/fang/saint/expellmaster": 1,
    ]));


  setup();
  set("no_clean_up", 0);
}
