//take make it for CS ...1/30/2002.
#include <ansi.h>

inherit ROOM;

void create ()
{
  set ("short", HIR"CS等待室"NOR);
  set ("long", @long
  
  这里是ｃｓ大赛的等待室。

long );

        set("exits", ([
        "out"    :       "/d/city/kezhan",
        ]));
 set("no_fight",1);
 set("outdoors", 1);

  setup();
}
