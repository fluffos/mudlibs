// liaotian.c

inherit ROOM;

int is_chat_room() { return 1; }

void create()
{
        set("short", "神魔寄售行");
        set("long", @LONG
这里是长安城的寄售行，听说这里是黑白两道交易奇珍异宝之所在，
许多无所事事的人在这里聊天，店小二穿梭在众多客人中间，忙个
不停。旁边有一个小门，隐隐有一些花香传进来，沁人心肺。
LONG );
        set("no_fight", 1);
		    set("no_clean_up",1);
        set("no_sleep_room", 1);
        set("exits", ([
              "east" : "/d/wiz/entrance3",
              //  "out"   : __DIR__"xiaohuayuan",
        ]));
	    	set("objects", ([
                        "/u/wuxin/npc/shen": 1,

	    	]));

        setup();
}
int clean_up()
{
      return 0;
}

