// nancheng_b.c
#include <ansi.h>
inherit BULLETIN_BOARD;

void create() {
  set_name(HIW "小龙龙的留言板" NOR, ({ "board" }));
  set("location", "/u/snowtu/room1");
  set("board_id", "snowtu_b");
  set("long", "这是一个供人留言记事的留言板。\n");
  setup();
  set("capacity", 200);
  replace_program(BULLETIN_BOARD);
}
