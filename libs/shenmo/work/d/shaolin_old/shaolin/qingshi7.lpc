inherit ROOM;
#include <ansi.h>
#include <room.h>

void create() {
  set("short", "青石路");
  set("long", @LONG
一条大青石铺成的路，夹杂着青草，微风轻抚，振振竹叶声，
令人尘心得到了清明。少林寺的后门就在眼前。
LONG);
  set("objects", ([   //NPC，空着，我以后加
  ]));
  set("outdoors", "shaolin");
  set("exits", ([
    "north": __DIR__ "houmen",
    "south": __DIR__ "qingshi5",
  ]));
  setup();
}
