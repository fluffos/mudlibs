inherit ROOM;
#include <ansi.h>
#include <room.h>

string look_zi();

void create() {
  set("short", "文殊殿 ");
  set("long", @LONG
少林寺文殊殿，得相传慈笃禅师圆寂火化时，红色火光在空中
凝结成文殊菩萨像，久久不散。大家认为慈笃是文殊菩萨的化身，
从此此楼改为为文殊院。文殊院香火兴盛。历任方丈都在这里开
坛传戒，并办有佛学苑、传习所，培养僧才。
LONG);
  set("objects", ([   //NPC，空着，我以后加
  ]));
  set("exits", ([
    "southwest": __DIR__ "qingshi6",
  ]));
  setup();
}
