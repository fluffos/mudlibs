inherit ROOM;
#include <ansi.h>
#include <room.h>

void create() {
  set("short", "石板路");
  set("long", @LONG
这是少林寺的一条通往后院的石板路，路边林树茂密，阳光的
照射下，走起路来格外的舒服。悠扬的鼓声，使你禁不住止步
静立，体味着清幽的山林古刹。西边是禅房，一些和尚正在做
早课，东边是藏经阁。
LONG);
  set("objects", ([   //NPC，空着，我以后加
  ]));
  set("outdoors", "shaolin");
  set("exits", ([
    "north": __DIR__ "xym",
    "south": __DIR__ "shiban15",
    "west": __DIR__ "chan",
    "east": __DIR__ "cjing",
  ]));
  setup();
}
