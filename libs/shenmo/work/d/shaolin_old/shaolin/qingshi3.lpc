inherit ROOM;
#include <ansi.h>
#include <room.h>

void create() {
  set("short", "青石路");
  set("long", @LONG
一条大青石铺成的路，夹杂着青草，东边的方丈室有僧敲木鱼
声，西边正是少林寺的西门，微风轻抚，振振竹叶声，令人尘心得
到了清明。
LONG);
  set("objects", ([   //NPC，空着，我以后加
  ]));
  set("outdoors", "shaolin");
  set("exits", ([
    "east": __DIR__ "qingshi1",
    //        "east"  : __DIR__"qingshi1",
    "west": __DIR__ "ximen",
  ]));
  setup();
}
