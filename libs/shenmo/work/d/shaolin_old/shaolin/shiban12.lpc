inherit ROOM;
#include <ansi.h>
#include <room.h>

void create ()
{
set ("short", "石板路");
set ("long", @LONG
这是少林寺的一条通往钟楼的石板路，路边林树茂密，阳光
照射下的石板路，走起路来格外的舒服，伴随着耳边清幽的古钟声
附近门禁森严的武器库，显得格外的神秘。
LONG);
set("objects", ([   //NPC，空着，我以后加
]));
set("outdoors", "shaolin");
set("exits", ([ 
        "west"  : __DIR__"shiban10",
        "east"  : __DIR__"shiban14",
        "north"  : __DIR__"bing",
]));
        create_door("north","木门","south",DOOR_CLOSED);
setup();
}
