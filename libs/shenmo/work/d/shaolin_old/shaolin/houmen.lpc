inherit ROOM;
#include <ansi.h>
#include <room.h>

void create() {
  set("short", "后门");
  set("long", @LONG
这里是少林寺后门。出了后门就是一片山林，郁郁葱
葱的森林，犹如一副美丽的画绢！微风吹过树林，又如波
涛汹涌，令人震惊，不禁赞叹大自然的伟大。
LONG);
  set("objects", ([   //NPC，空着，我以后加
  ]));
  set("outdoors", "shaolin");
  set("exits", ([
    "west": __DIR__ "wall35",
    "east": __DIR__ "wall38",
    "south": __DIR__ "qingshi7",
    "north": __FILE__,
  ]));
  create_door("north", "后门", "south", DOOR_CLOSED);
  set("valid_startroom", 1);
  setup();
}
