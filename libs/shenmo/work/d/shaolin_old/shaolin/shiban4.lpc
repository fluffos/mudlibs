inherit ROOM;
#include <ansi.h>
#include <room.h>

void create() {
  set("short", "石板路");
  set("long", @LONG
这是少林寺的一条通往练武场的石板路，路边林树茂密，阳光
照射下的石板路，走起路来格外的舒服，伴随着耳边清幽的古钟声
加上远处练武场传来少林弟子的练功声，显得格外的神秘。
LONG);
  set("objects", ([   //NPC，空着，我以后加
  ]));
  set("outdoors", "shaolin");
  set("exits", ([
    "west": __DIR__ "lianwu-e",
    "east": __DIR__ "ciyun",
  ]));
  setup();
}
