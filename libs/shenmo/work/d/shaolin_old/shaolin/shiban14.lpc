inherit ROOM;
#include <ansi.h>
#include <room.h>

void create() {
  set("short", "石板路");
  set("long", @LONG
这是少林寺的一条通往钟楼的石板路，路边林树茂密，阳光
照射下的石板路，走起路来格外的舒服，耳边响起清幽的古钟声。
门禁森严的武器库就在北边，南边是少林僧人休息的地方。
LONG);
  set("objects", ([   //NPC，空着，我以后加
  ]));
  set("outdoors", "shaolin");
  set("exits", ([
    "west": __DIR__ "shiban12",
    "east": __DIR__ "zhong",
    "south": __DIR__ "seng2",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "shaolin");
  setup();
}
