inherit ROOM;
#include <ansi.h>
#include <room.h>

void create ()
{
set ("short", "东门");
set ("long", @LONG
这里是少林寺东门。出了东门就是一片山林，郁郁葱
葱的森林，犹如一副美丽的画绢！微风吹过树林，又如波
涛汹涌，令人震惊，不禁赞叹大自然的伟大。
LONG);
set("objects", ([   //NPC，空着，我以后加
]));
set("outdoors", "shaolin");
set("exits", ([ 
        "north"  : __DIR__"wall28",
        "south"  : __DIR__"wall26",
        "west"  : __DIR__"qingshi4",
    "east" : __FILE__,
]));
create_door("east", "东门", "west", DOOR_CLOSED);
set("valid_startroom", 1);
setup();
}

