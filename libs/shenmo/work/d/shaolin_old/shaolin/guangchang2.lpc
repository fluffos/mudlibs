inherit ROOM;
#include <ansi.h>
#include <room.h>

void create ()
{
set ("short", "广场");
set ("long", @LONG
过了天王殿，来到后院，又有三道平行的青石台阶引正前方
的一个大广场。台阶上刻的是诸天众佛，五百罗汉以及三千伽蓝
的坐像。上了平台，正中方也有个丈许高的大香炉，香炉前的香
台上，红烛高烧，香烟飘绕，几位香客正对大殿虔诚叩拜。左右
各通向一个小院，两座高塔高耸天际。正前方就是大雄宝殿。  
LONG);
set("objects", ([   //NPC，空着，我以后加
]));
set("outdoors", "shaolin");  //是空地就加，是房间就不写这行
set("exits", ([ 
        "northup" : __DIR__"dian",
        "west"  : __DIR__"shiban7",
        "east"  : __DIR__"shiban8",
]));
setup();
}

