inherit ROOM;
#include <ansi.h>
#include <room.h>

void create() {
  set("short", "院墙");
  set("long", @LONG
这里是少林寺的后院院墙，黄墙碧瓦，飞檐翘檩，这里人迹罕
至，惟闻足底叩击路面，有僧敲木鱼声；微风吹拂竹叶，又如簌簌
禅唱。令人尘心为之一涤，真是绝佳的禅修所在。几个小僧在打扫
吹落的竹叶。
LONG);
  set("objects", ([   //NPC，空着，我以后加
  ]));
  set("outdoors", "shaolin");
  set("exits", ([
    "east": __DIR__ "xym",
    "west": __DIR__ "wall27",
  ]));
  setup();
}
