inherit ROOM;
#include <ansi.h>
#include <room.h>

void create() {
  set("short", "青石路");
  set("long", @LONG
一条大青石铺成的路，夹杂着青草，微风轻抚，振振竹叶声，
令人尘心得到了清明。千佛殿和戒律院殿分别在两边。寂静的后
院显得格外的庄重.
LONG);
  set("objects", ([   //NPC，空着，我以后加
  ]));
  set("outdoors", "shaolin");
  set("exits", ([
    "south": __DIR__ "qingshi6",
    "northeast": __DIR__ "jielu",
    "northwest": __DIR__ "qianfo",
  ]));
  setup();
}
