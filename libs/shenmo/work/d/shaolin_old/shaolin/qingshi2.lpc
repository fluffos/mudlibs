inherit ROOM;
#include <ansi.h>
#include <room.h>

void create ()
{
set ("short", "青石路");
set ("long", @LONG
一条大青石铺成的路，夹杂着青草，微风轻抚，振振竹叶声，
令人尘心得到了清明。西边的方丈室有僧敲木鱼声，西边正是少
林寺的东门。
LONG);
set("objects", ([   //NPC，空着，我以后加
]));
set("outdoors", "shaolin");
set("exits", ([ 
        "north"  : __DIR__"qingshi6",
        "south"  : __DIR__"dym",
        "east"  : __DIR__"qingshi4",
        "west"  : __DIR__"qingshi9",
]));
create_door("south", "东院门", "north", DOOR_CLOSED);
setup();
}

