inherit ROOM;
#include <ansi.h>
#include <room.h>

void create ()
{
set ("short", "僧舍");
set ("long", @LONG
这是少林寺僧人休息的地方，靠墙一排土炕，被子叠的整整
齐齐，南边的窗下几条方桌，上面放着几本佛经，定是那个僧人
拿回来读的。
LONG);
set("objects", ([   //NPC，空着，我以后加
]));
set("exits", ([ 
        "south"  : __DIR__"shiban11",
]));
setup();
}

