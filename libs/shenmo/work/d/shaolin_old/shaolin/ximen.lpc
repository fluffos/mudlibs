inherit ROOM;
#include <ansi.h>
#include <room.h>

void create ()
{
set ("short", "西门");
set ("long", @LONG
这里是少林寺西门。出了西门就是一片山林，郁郁葱
葱的森林，犹如一副美丽的画绢！微风吹过树林，又如波
涛汹涌，令人震惊，不禁赞叹大自然的伟大。
LONG);
set("objects", ([   //NPC，空着，我以后加
]));
set("outdoors", "shaolin");
set("exits", ([ 
        "north"  : __DIR__"wall29",
        "south"  : __DIR__"wall27",
        "east"  : __DIR__"qingshi3",
        "west"  : __FILE__,
]));
create_door("west", "西门", "east", DOOR_CLOSED);
set("valid_startroom", 1);
setup();
}

