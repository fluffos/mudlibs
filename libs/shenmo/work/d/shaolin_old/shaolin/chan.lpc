inherit ROOM;
#include <ansi.h>
#include <room.h>

void create ()
{
set ("short", "禅房");
set ("long", @LONG
这里是少林寺的禅房。房间中央放着观音大师，第三摆
放着大小的禅座，一个老方丈正在给下面的僧人讲佛。这里
是僧人们每天做早课的地方。
LONG);
set("objects", ([   //NPC，空着，我以后加
]));
set("exits", ([ 
        "east"  : __DIR__"shiban17",
]));
setup();
}

