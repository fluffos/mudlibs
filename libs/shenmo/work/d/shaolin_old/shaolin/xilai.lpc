inherit ROOM;
#include <ansi.h>
#include <room.h>

void create ()
{
set ("short", "西来堂");
set ("long", @LONG
这是少林寺西来堂，里面供奉着14座泥塑的武僧，个个栩栩如
生，来到本堂大多是拜佛, 参加法事, 或者抄写般若心经, 写完
了后要求愿, 并将经文供奉给佛祖......，大多善男信女都喜欢
来到这里拜佛许愿。
LONG);
set("objects", ([   //NPC，空着，我以后加
       __DIR__"npc/seng" : random(4),
]));
set("exits", ([ 
        "east"  : __DIR__"shiban3",
]));
setup();
}

