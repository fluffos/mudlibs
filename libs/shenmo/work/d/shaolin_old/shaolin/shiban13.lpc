inherit ROOM;
#include <ansi.h>
#include <room.h>

void create() {
  set("short", "石板路");
  set("long", @LONG
这是少林寺的一条通往鼓楼的石板路，路边林树茂密，阳光的
照射下，走起路来格外的舒服。北边的药王堂里传出浓浓的药味，
南边的僧舍格外的安静，悠扬的鼓声，使你禁不住止步静立，体味
着清幽的山林古刹。
LONG);
  set("objects", ([   //NPC，空着，我以后加
  ]));
  set("outdoors", "shaolin");
  set("exits", ([
    "north": __DIR__ "yao",
    "south": __DIR__ "seng1",
    "west": __DIR__ "gu",
    "east": __DIR__ "shiban11",
  ]));
  setup();
}
