inherit ROOM;
#include <ansi.h>
#include <room.h>

void create() {
  set("short", "石板路");
  set("long", @LONG
这是少林寺的一条通往后院的石板路，路边林树茂密，阳光
照射下的石板路，走起路来格外的舒服，耳边响起清幽的古钟声。
罗汉堂就在附近。
LONG);
  set("objects", ([   //NPC，空着，我以后加
  ]));
  set("outdoors", "shaolin");
  set("exits", ([
    "north": __DIR__ "dym",
    "south": __DIR__ "shiban16",
    "east": __DIR__ "luohan",
  ]));
  setup();
}
