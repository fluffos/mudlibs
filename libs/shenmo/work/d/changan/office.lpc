// Room: /u/wuliao/housingoffice
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "房管所");
  set("long", @LONG
泾水河边的居民逐渐增多，因此特别在此设立房管所，临时由
一个不知名的老头负责。
LONG);
  set("item_desc", ([ /* sizeof() == 1 */
    "hole": "洞中似乎是个机关，能放(insert)下什么东西。\n",
  ]));

  set("exits",
    ([  //sizeof() == 4
    "east": __DIR__ "ph",
      "west": __DIR__ "westseastore",
    ]));

  set("objects",
    ([  //sizeof() == 1
    __DIR__ "officer": 1,
    ]));

  set("no_clean_up", 0);
  set("no_fight", 1);
  set("no_magic", 1);
  set("outdoors", 1);
  setup();
  //      replace_program(ROOM);
}
