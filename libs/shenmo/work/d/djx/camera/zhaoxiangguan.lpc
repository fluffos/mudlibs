//by djx 2003/7/20

inherit ROOM;
int block_cmd(string);
void create() {
  set("short", "长安照相馆");
  set("long", @LONG

这里是这个世界里最前卫的地方了。墙壁四周挂满了人们的
照片，形态各异。摄影师坐在旁边，招呼着客人。但是他好
象现在已经不为人照相了,只是把收藏的各式相机用来出售。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "east": "/u/yhq/meirongshi",
  ]));
  set("objects",
    ([  //sizeof() == 3
    "/d/djx/camera/npc/sheyingshi": 1
    ]));
  set("no_clean_up", 0);
  set("no_fight", 1);
  set("no_magic", 1);
  setup();
}

int block_cmd(string args) {
  string verb;
  verb = query_verb();

  //  if (verb == "bian") return 1; 
  if (verb == "xiudao") return 1;
  if (verb == "exert") return 1;

  return 0;
}
