inherit __DIR__"room.lpc";

#include <ansi.h>;

void create()
{
    set("short", "烈日外门");
    set("long", 
"这里是日出城的南城门，只见城门上方是三个大字。"HIR"

                    烈  日  门
\n"NOR
"近年来倭寇屡次侵犯日出城，故这儿把守相当严密，一些官兵
们正在盘查要离日出城的人。两边是很大的草地。\n"
 );

    set("exits", ([
        "east"   : __DIR__"caodi4",
        "west"   : __DIR__"caodi5",
        "south"  : __DIR__"caodi6",
        "north"  : __DIR__"southgate1",
    ]));

    set("coor/x",110);
    set("coor/y",-300);
    set("coor/z",-30);

    ::create();
}

