#include <room.h>
inherit __DIR__"room.lpc";

void create()
{
    set("short", "马厩");
    set("long", @LONG
这里是日出城内唯一一家车马行。你可花钱租辆马车到各
个地方去，能节约不少体力。近来因为明月正在组织人力
四处搜集防城材料，马夫积极响应，免费提供推车来运
送各种防城材料。雨檐下钉着块木牌(paizi)。
LONG);

    set("exits", ([
        "south"  : __DIR__"northroad2",
    ]));

    set("coor/x",120);
    set("coor/y",-200);
    set("coor/z",-30);

    ::create();
}

