inherit __DIR__"room.lpc";

#include <ansi.h>;

void create()
{
    set("short", "落日外门");
    set("long", 
"这里是日出城的西城门，只见城门上方是三个大字。"HIY"

                    落  日  门
\n"NOR
"近年来倭寇屡次侵犯日出城，故这儿把守相当严密，一些官兵
们正在盘查要离日出城的人。两边是很大的草地。\n"
 );

    set("exits", ([
        "east"  : __DIR__"westgate1",
        "west"  : __DIR__"guandao_1",
    ]));

    set("coor/x",70);
    set("coor/y",-240);
    set("coor/z",-30);

    ::create();
}

