inherit __DIR__"room.lpc";

void create()
{
    set("short", "北城头");
    set("long", @LONG
这里是日出城的北城头。一些士兵正在城头巡逻，一个牙将
正在这儿负责接受各种守城材料，一些弓箭手正拈弓搭箭紧盯
着城下的动静。
LONG );

    set("exits", ([
        "southeast" : __DIR__"walle8",
        "westdown"  : __DIR__"walln5",
    ]));

    set("coor/x",150);
    set("coor/y",-190);
    set("coor/z",10);

    ::create();
}

