inherit __DIR__"room.lpc";

void create()
{
    set("short", "石阶");
    set("long", @LONG
这是登向城头的石阶，因为城墙很高，所以很陡。石阶两
侧有可供拉车上下的斜坡道。
LONG );

    set("exits", ([
        "northup"   : __DIR__"walle6",
        "southdown" : __DIR__"walle2",
    ]));

    set("coor/x",160);
    set("coor/y",-220);
    set("coor/z",-10);

    ::create();
}
