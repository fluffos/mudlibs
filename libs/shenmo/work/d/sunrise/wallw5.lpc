inherit __DIR__"room.lpc";

void create()
{
    set("short", "石阶");
    set("long", @LONG
这是登向城头的石阶，因为城墙很高，所以很陡。石阶两
侧有可供拉车上下的斜坡道。
LONG );

    set("exits", ([
        "northup"   : __DIR__"wallw7",
        "southdown" : __DIR__"wallw3",
    ]));

    set("coor/x",60);
    set("coor/y",-210);
    set("coor/z",0);

    ::create();
}

