inherit __DIR__"room.lpc";
#include <ansi.h>;

void create()
{
    set("short", "草地");
    set("long", @LONG
这是日出城城外的草地。倭寇进犯以来，这里的百姓全进
城了，倭寇驰骋来去，尘土飞扬，甚是嚣张。
LONG );

    set("exits", ([
        "west"  : __DIR__"northgate2",
    ]));

    set("coor/x",120);
    set("coor/y",-180);
    set("coor/z",-30);

    ::create();
}

