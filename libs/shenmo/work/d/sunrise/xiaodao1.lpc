inherit __DIR__"room.lpc";

void create()
{
    set("short", "大官道");
    set("long", @LONG
这是一条弯弯曲曲的小道，前放不远出是一片树林，不知
道通向何处。
LONG );

    set("exits", ([
        "north" : __DIR__"shulin",
        "south" : __DIR__"guandao_2",
    ]));

    set("coor/x",60);
    set("coor/y",-220);
    set("coor/z",-30);

    ::create();
}

