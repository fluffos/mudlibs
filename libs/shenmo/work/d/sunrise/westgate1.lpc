inherit __DIR__"room.lpc";

#include <ansi.h>;

void create()
{
    set("short", "落日内门");
    set("long", 
"这里是日出城的西城门的内城门，只见城门上方是三个大
字。"HIY"

                    落  日  门
\n"NOR
"近年来倭寇屡次侵犯日出城，故这儿把守相当严密，一些官兵
们正在盘查要离日出城的人。两边是上城头的石阶，向东进入
城区。\n"
 );

    set("exits", ([
        "southup" : __DIR__"wallw2",
        "northup" : __DIR__"wallw1",
        "east"    : __DIR__"westjie3",
        "west"    : __DIR__"westgate2",
    ]));

    set("coor/x",80);
    set("coor/y",-240);
    set("coor/z",-30);

    ::create();
}

