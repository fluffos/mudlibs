inherit __DIR__"room.lpc";

void create()
{
    set("short", "胡同");
    set("long", @LONG
这里是日出城内老百姓的居住区。只见胡同两边是有些小
店面，有米行、小杂货铺等。一些居民从这里进进出出。
LONG );

    set("exits", ([
        "east" : __DIR__"jiekou1",
    ]));

    set("coor/x",80);
    set("coor/y",-210);
    set("coor/z",-30);

    ::create();
}

