inherit __DIR__"room.lpc";

void create()
{
    set("short", "安抚使衙门");
    set("long", @LONG
这是安抚使衙门的大门，铜皮包门似乎极为沉重，上面有
两个明晃晃的铜环；大门紧闭。
LONG );

    set("no_clean_up", 0);

    set("exits", ([
        "south" : __DIR__"anfupailou",
    ]));

    set("coor/x",120);
    set("coor/y",-220);
    set("coor/z",-30);

    ::create();
}

