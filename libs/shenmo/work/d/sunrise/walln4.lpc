inherit __DIR__"room.lpc";

void create()
{
    set("short", "石阶");
    set("long", @LONG
这是登向城头的石阶，因为城墙很高，所以很陡。石阶两
侧有可供拉车上下的斜坡道。
LONG );

    set("exits", ([
        "westup"   : __DIR__"walln6",
        "eastdown" : __DIR__"walln2",
    ]));

    set("coor/x",90);
    set("coor/y",-190);
    set("coor/z",-10);

    ::create();
}
