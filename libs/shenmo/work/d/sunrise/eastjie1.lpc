inherit __DIR__"room.lpc";

void create()
{
    set("short", "东大街");
    set("long", @LONG
这是一条宽阔的青石板街道，向东西两头延伸。东边是一
个大十字街口。西面是日出城的中央广场，北边是日出城守城大将
明月的府邸大门，只见那儿守备森严，闲杂人等一般是不能接
近的。南面飘来一缕缕的药香，噢，原来是一家药铺。
LONG );

    set("no_clean_up", 0);

    set("exits", ([
        "east"  : __DIR__"eastjie2",
        "west"  : __DIR__"guangchang",
        "south" : __DIR__"yaopu",
        "north" : __DIR__"jiangjungate",
    ]));

    set("coor/x",130);
    set("coor/y",-240);
    set("coor/z",-30);

    ::create();
}

