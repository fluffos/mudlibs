inherit __DIR__ "room.lpc";

void create() {
  set("short", "西城头");
  set("long", @LONG
这里是日出城的西城头。一些士兵正在城头巡逻，一个牙将
正在这儿负责接受各种守城材料，一些弓箭手正拈弓搭箭紧盯
着城下的动静。
LONG);

  set("exits", ([
    "northeast": __DIR__ "walln8",
    "southdown": __DIR__ "wallw5",
  ]));

  set("coor/x", 60);
  set("coor/y", -200);
  set("coor/z", 10);

  ::create();
}
