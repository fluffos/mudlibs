inherit __DIR__ "room.lpc";
#include <ansi.h>;

void create() {
  set("short", "草地");
  set("long", @LONG
这是日出城城外的草地。倭寇进犯以来，这里的百姓全进
城了，这里几十里都没了人烟。
LONG);

  set("exits", ([
    "west": __DIR__ "southgate2",
  ]));

  set("coor/x", 120);
  set("coor/y", -300);
  set("coor/z", -30);

  ::create();
}
