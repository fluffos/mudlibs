inherit __DIR__"room.lpc";
#include <ansi.h>;

void create()
{
    set("short", "草地");
    set("long", @LONG
这是日出城城外的草地。倭寇进犯以来，这里的百姓全进
城了，虽然倭寇没来到南门，但肃杀氛围丝毫不减。
LONG );

    set("no_clean_up", 0);

    set("exits", ([
        "north"  : __DIR__"southgate2",
    ]));

    set("coor/x",110);
    set("coor/y",-310);
    set("coor/z",-30);

    ::create();
}

