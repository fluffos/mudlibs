inherit __DIR__ "room.lpc";

void create() {
  set("short", "胡同");
  set("long", @LONG
这里是日出城内老百姓的居住区。只见小弄两边是有些小
店面，有米行、小杂货铺等。一些居民从这里进进出出。
LONG);

  set("exits", ([
    "south": __DIR__ "jiekou2",
  ]));

  set("coor/x", 130);
  set("coor/y", -200);
  set("coor/z", -30);

  ::create();
}
