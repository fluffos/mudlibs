#include <ansi.h>
inherit ROOM;
#include "/d/shashou/migong.h"

void create()
{
set ("short", "灵州大街");
set ("long", @LONG

灵州的大街上人山人海，因西夏公主准备选亲，
作为一品堂第一高手的四大恶人，也在这里忙个
不停维护秩序。

LONG);
/*
  set("objects", 
([
  
  " /d/shashou/npc/duan": 1,
  " /d/shashou/npc/ye": 1,
  " /d/shashou/npc/er" : 1,
  " /d/shashou/npc/yun" : 1,
  ]));
*/
set("no_fight", 1);
set("no_magic", 1);
setup();
}

void init()
{
if ( time()<query("migong")+3600 )
        return ;
this_object()->set("migong",time()); //迷宫开启
home=("/d/shashou/migong/");
exit=("/d/shashou/damen.lpc");
entry=("/d/shashou/dajie.lpc");
remove_call_out("r_map");
call_out("r_map",1);
}

void reset()
{
return;
}

