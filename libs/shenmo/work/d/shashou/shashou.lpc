#include <ansi.h>  
inherit ROOM;

void create ()
{
  set ("short", "杀手楼");
  set ("long", @LONG

西夏一品堂招兵买马，成立杀手楼，目的在于成立自己的
杀手组织，暗杀中原武林高手。
LONG);

  set("objects", ([ /* sizeof() == 2 */
         "/d/shashou/npc/helian": 1 ,
]));
  set("exits", ([ /* sizeof() == 3 */
"east" : "/d/shashou/chanfang4",
"west" : "/d/shashou/chanfang5",
"north" : "/d/shashou/chanfang6",
]));
  set("no_fight", 1);
  set("no_magic", 1);
  set("no_magic", 1);
  setup();
}      