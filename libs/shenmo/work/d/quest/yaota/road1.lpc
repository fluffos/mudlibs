//Cracked by Roath
inherit ROOM;
#include "/d/9sky/guzhanchang/road.h" 
#include <room.h>

void create ()
{
  set ("short", "空地");
  set ("long", @LONG

小小的一片空地，没什么好的地方
LONG);
  set("exits", ([ /* sizeof() == 3 */
  "up": __DIR__"tower2",
  
]));
set("objects", ([
   "/d/quest/yaota/npc/guai": 2]));
        set("no_clean_up", 0);
 set("alternative_die",1); 
        set("outdoors", 1);
        setup();


}

int valid_leave(object me, string dir)
{       
        object a;
        if( wizardp(me)) return 1;

        if (dir == "up") {
                if(objectp(a=present("yeji guai", environment(me))) && living(a) )
                return notify_fail("小怪好象不愿让你过去！\n");
        return ::valid_leave(me, dir);
        }

        return 1;
}


