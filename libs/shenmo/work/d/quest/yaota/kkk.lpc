

inherit MAGIC_ROOM;
#include <ansi.h>
int do_none();

void create ()
{
        set ("short", "混沌之初" );
        set ("long", @LONG

黑糊糊的一片,慢慢的你看到一个人影。
LONG);

     

        set("exits", 
        ([ 
              
        ]));

    set("no_fight",1);
set("no_magic",1); 
        set("objects", 
        ([ //sizeof() == 1
                __DIR__"npc/kkkk" : 1,
        ]));

	set("no_clean_up", 0);
        setup();
	replace_program(ROOM);
}


void init()
{
        add_action("do_none","bian");
}

int do_none()
{
  object me = this_player();
        write("你不能在这里用这条命令。\n");  
//        return 0;    //by snowtu return 0的结果是此action之后会继续调用cmds
        return 1;  
}
