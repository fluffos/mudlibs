inherit COMBINED_ITEM;

void setup() {}

void init() {
  add_action("do_eat", "eat");
}

void create() {
  set_name("混元丹", ({ "hunyuan dan", "dan", "hunyuan" }));
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("base_unit", "包");
    set("unit", "些");
    set("long", "这是一包治伤的混元丹。\n");
    set("value", 100);
    set("drug_type", "良药");
  }
  set_amount(1);
  setup();
}

int do_eat(string arg) {
  if (!arg || !id(arg))
    return 0;
  if (this_player()->query("eff_sen") == this_player()->query("max_sen")) {
    write("你现在不需要用混元丹。\n");
    return 1;
  }
  this_player()->receive_curing("sen", 120);
  message_vision("$N吃下一颗混元丹，脸色看起来好多了。\n", this_player());
  if (query_amount() > 1)
    add_amount(-1);
  else destruct(this_object());
  return 1;
}
