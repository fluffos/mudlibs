#include <ansi.h>
inherit ITEM;
void create()
{
    set_name("乌风草", ({ "wufeng cao" }) );
    set_weight(1);
    if( clonep() )
            set_default_object(__FILE__);
    else {
            set("unit", "颗");
            set("long", "一颗乌风草\n");
          }

    setup();
}
