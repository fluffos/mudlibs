#include <ansi.h>
inherit ITEM;
void create() {
  set_name(HIG "绿玉佛" NOR, ({ "lvyu fo" }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "只");
    set("long", "一只绿玉佛\n");
  }

  setup();
}
