#include <ansi.h>
inherit ITEM;
void create()
{
    set_name("千毒圭", ({ "qiandu gui" }) );
    set_weight(1);
    if( clonep() )
            set_default_object(__FILE__);
    else {
            set("unit", "只");
            set("long", "一只千毒圭\n");
          }

    setup();
}
