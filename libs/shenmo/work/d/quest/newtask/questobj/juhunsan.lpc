#include <ansi.h>
inherit ITEM;
void create()
{
    set_name("子午拘魂散", ({ "juhunsan" }) );
    set_weight(1);
    if( clonep() )
            set_default_object(__FILE__);
    else {
            set("unit", "瓶");
            set("long", "一瓶子午拘魂散\n");
          }

    setup();
}
