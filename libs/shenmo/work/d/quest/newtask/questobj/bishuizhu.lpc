#include <ansi.h>
inherit ITEM;
void create() {
  set_name("避水珠", ({ "bishui zhu" }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "颗");
    set("long", "一颗避水珠\n");
  }

  setup();
}
