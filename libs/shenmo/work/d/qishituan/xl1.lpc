// by seawind
inherit ROOM;
void create() {
  set("short", "暹罗");
  set("long", @LONG
    暹罗，属于自由都市同盟里的中级城市，经济力普通，虽然同时有水陆交会，
却因为先天上的障碍，难有什么大发展，唯一所有的特色，即是其独树一帜的南国风情。
LONG);
  set("exits", ([
    //"east" : __FILE__,
    //"west" : __FILE__,

    //"southup" : __DIR__"hmy7",
    // "northdown" : __DIR__"hmy8",
  ]));
  set("objects", ([
    __DIR__ "npc/girl": 1,

  ]));
  set("no_clean_up", 0);
  setup();
}
