#include <ansi.h>
#include <combat.h>
inherit ITEM;
void init();
void create() {
  set_name(HIR "有限示" NOR, ({ "youxian shi", "shi", "wuxian" }));
  if (clonep()) set_default_object(__FILE__);
  else {
    set_weight(5000);
    set("long", HIM "
神匠欧冶子当年参透百家武器奥妙所打造的神兵，用十七块奇形怪状
的玄金属连缀而成。你仔细看看它，发现里面另有玄机：你可以将它
驳(huan)成\n
刀(blade)剑(sword)锤(hammer)斧(axe)鞭(whip)锏(mace)
枪(spear)叉(fork)棍(stick)杖(staff)拳套(hand)钉耙(rake)。\n
" NOR);
    set("unit", "塊");
    set("value", 500000);
    set("material", "steel");
  }
  setup();
}

void init() {
  set("no_give", HIW "此事还是作罢，不要再想了吧...... \n" NOR);
  set("no_drop", HIW "丢不得！丢不得！丢不得！丢不得！丢不得！\n" NOR);
  set("no_get", HIW "好东西你看看就得了，还想拿？小心我打你PP哦...... \n" NOR);
  set("no_sell", HIW "此事还是作罢，不要再想了吧...... \n" NOR);
  add_action("do_huan", "huan");
}

int do_huan(string arg) {
  object me = this_player();
  object ob = this_object();
  object wuqi;
  if (!present(ob, me) || !(arg)) return notify_fail("什么？\n");
  switch (arg) {
    case "sword": wuqi = new(__DIR__ "sword1");
    break;

    case "hammer": wuqi = new(__DIR__ "hammer1");
    break;

    case "stick": wuqi = new(__DIR__ "stick1");
    break;

    case "staff": wuqi = new(__DIR__ "staff1");
    break;

    case "mace": wuqi = new(__DIR__ "mace1");
    break;

    case "axe": wuqi = new(__DIR__ "axe1");
    break;

    case "spear": wuqi = new(__DIR__ "spear1");
    break;

    case "whip": wuqi = new(__DIR__ "whip1");
    break;

    case "blade": wuqi = new(__DIR__ "blade1");
    break;

    case "fork": wuqi = new(__DIR__ "fork1");
    break;

    case "hand": wuqi = new(__DIR__ "hand1");
    break;

    case "rake": wuqi = new(__DIR__ "rake1");
    break;

    default: return notify_fail("你想要的超出我的想象。。。\n");
    break;
  }
  wuqi->move(me);
  message_vision("
$N口中念念有词，$n一阵光华过后变作了一" + wuqi->query("unit") + wuqi->name() + "！\n" NOR, me, ob);
  destruct(ob);
  return 1;
}
