#include <ansi.h>
inherit __DIR__ "room.lpc";

void create() {
  seteuid(geteuid());
  setup();
  set("hs_world", 1);
}

void valid_move(object who) {
  if (!userp(who) && living(who)) {
    who->move(this_object());
    message_vision(HIB "\n蓝光一闪，$N" HIB "从中现出身来。\n\n" NOR, who);
  }
}
