
inherit ROOM;

void create()
{
        set("short", "毗卢居");
        set("long", @LONG
这里是碧游宫七仙之毗卢仙的居室。这房间窗明几净，
清净淡泊，的确是清修的好地方。
LONG);
        set("exits", ([
                "north" : __DIR__"nairongdian",
        ]));

        set("objects", ([
                __DIR__"npc/pilu" : 1,
        ]));

        setup();
        replace_program(ROOM);
}

