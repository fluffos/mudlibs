//Cracked by Roath
#include <room.h>

inherit ROOM;

void create() {
  set("short", "石矶门");
  set("long", @LONG

这里是峨嵋山九老洞内最隐秘的一个山洞，洞口青松叠翠，绿草
氤氲，一条溪水从门前流过，叮咚声响。门边有个大的石碣，写
了“峨嵋山罗浮洞”六个大字。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "south": "/d/emei/jiulaodong",
    "north": __DIR__ "qujing",
  ]));

  set("objects", ([ /* sizeof() == 1*/
    __DIR__ "npc/shiji": 1,
  ]));



  set("outdoors", 1);
  create_door("north", "石门", "south", DOOR_CLOSED);


  setup();
}
