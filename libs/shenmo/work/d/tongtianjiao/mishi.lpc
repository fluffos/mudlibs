
inherit ROOM;

void create()
{
        set("short", "密室");
        set("long", @LONG
这里是通天教弟子为了直达长安修建的密室，这里的光线
非常昏暗，也许已经身处地下了。
LONG);
        set("exits", ([
                "up" : __DIR__"diziju",
                "westup" : "/d/city/center",
        ]));
        set("objects", ([
                __DIR__"npc/kong" : 1,
        ]));
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

