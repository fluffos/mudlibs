//Room: luofudong.c 罗浮洞
// windy

inherit ROOM;

void create() {
  set("short", "罗浮洞");
  set("long", @LONG
这里就是峨嵋山最清幽的罗浮洞了，身处洞内，听不到外界的任
何声响，清净澄明，的确是仙家修炼的最佳地点。赵公明就是在
此处修炼得道。再往北走，就是三霄洞了。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "qujing",
    "north": __DIR__ "sanxiaodong",
  ]));
  set("objects", ([
    __DIR__ "npc/zhao": 1,
  ]));
  set("no_clean_up", 0);
  setup();
}
