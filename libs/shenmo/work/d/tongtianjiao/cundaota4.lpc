#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "通天塔顶");
  set("long", @LONG
这里是通天塔的塔顶，塔梯到这里就到了尽头。墙壁上开
着几个窗户(window)，几个中年道人正捧着书苦苦研读。
再往上走就到达天宫了。
LONG);
  set("exits", ([
    "eastdown": __DIR__ "cundaota3",
    "up": "/d/dntg/sky/dadao4"
  ]));
  set("objects", ([
    __DIR__ "npc/daoren": 1,
  ]));

  set("item_desc", ([
    "window": HIW "\n窗外白云飘过，似乎你一伸手就可以抓住一片。\n" NOR,
  ]));

  set("count", 1);
  setup();
}
