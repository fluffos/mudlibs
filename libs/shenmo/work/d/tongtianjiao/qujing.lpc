//Cracked by Roath
#include <room.h>

inherit ROOM;

void create() {
  set("short", "曲径");
  set("long", @LONG

一条弯弯曲曲的小路，蜿蜒通向罗浮洞内。虽然越走离九老洞越
远，但是身后仍然不时地传来猿猴的啼叫。南面是一扇石门。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "shijimen",
    "north": __DIR__ "luofudong",
  ]));
  set("objects", ([ /* sizeof() == 1*/
    __DIR__ "npc/wen": 1,
  ]));


  create_door("south", "石门", "north", DOOR_CLOSED);


  setup();
}
