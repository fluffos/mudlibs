inherit ROOM;

void create() {
  set("short", "定光堂");
  set("long", @LONG
这里是碧游宫七仙之长耳仙的居所。房间不大，收拾的素
雅整洁，墙上挂着一萧一琴和一些字画，小几上摆放着宣纸和
徽墨狼毫，不知道主人是要练字还是绘画。
LONG);
  set("exits", ([
    "south": __DIR__ "huizhentang",
  ]));
  set("objects", ([
    __DIR__ "npc/changer": 1,
  ]));

  setup();
  replace_program(ROOM);
}
