//By yhq 2004-10-28
//部分优化

inherit ROOM;

void create() {
  set("short", "后院");
  set("long", @LONG
这里是后院，北面是大殿，南面是一个圆形的门洞，有一条小路通往
后山。
LONG);
  set("exits", ([
    "southup": __DIR__ "xiaolu1",
    "north": __DIR__ "dadian",
  ]));
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
