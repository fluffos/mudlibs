//by yhq 2004-10-28
#include <room.h>
inherit ROOM;

void create()
{
        set("short", "练药房");
        set("long", @LONG
这是后山的一座小院，二公主正在这里专心研制本门秘密毒药。
LONG );
        set("exits", ([
                "north" : __DIR__"xiaolu1",
        ]));
        set("objects", ([
                __DIR__"npc/lianxing" : 1,
                      __DIR__"npc/shinu" : 3, 
        ]));
//        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}
