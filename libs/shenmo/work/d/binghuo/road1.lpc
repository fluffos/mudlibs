inherit ROOM;

void create() {
  set("short", "小路");
  set("long", @LONG
冰天雪地里隐隐可以看到一行行的脚印。
LONG);
  set("exits", ([
    "east": __DIR__ "road2",
  ]));
  set("outdoors", "taohua");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
