inherit ROOM;

void create() {
  set("short", "冰洞");
  set("long", @LONG
不知不觉中你来到了岛上一个小洞。
LONG);
  set("exits", ([
    "out": __DIR__ "xuedi5",
  ]));
  set("objects", ([
    "/d/mingjiao/npc/xiexun": 1,
  ]));

  set_light(0);
  //    set("no_clean_up", 0);
  set("sleep_room", "1");
  setup();
}
