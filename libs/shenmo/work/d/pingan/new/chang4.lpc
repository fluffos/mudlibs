// George
// 2005-10-10
// 文件类型  房间  

inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "新人训练场四" NOR);
  set("long", @LONG
这里就是新人入门训练的地方了，周边站着几个练功用的木人。
LONG);

  set("exits", ([
    "west": __DIR__ "chang3",
    "east": __DIR__ "chang5",

  ]));

  set("objects", ([
    __DIR__ "npc/muren.lpc": 3,
  ]));

  set("pingan", 1);


  setup();
  replace_program(ROOM);
}
