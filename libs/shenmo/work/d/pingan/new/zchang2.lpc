// George
// 2005-10-10
// 文件类型  房间  

inherit ROOM;
#include <ansi.h>

void create ()
  {
set("short", "中级训练场二"NOR);
set("long", @LONG
这里就是新人入门训练的地方了，周边站着几个练功用的铜人。
LONG);

 set("exits", ([
    "south" : __DIR__"zchang1",
    "north" : __DIR__"zchang3",

]));
 
    set("objects", ([
     __DIR__"npc/tongren.lpc" : 3 ,
      ]));

     set("pingan", 1);


setup();
replace_program(ROOM);
   }
