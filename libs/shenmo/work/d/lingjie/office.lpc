inherit ROOM;

void create() {
  set("short", "阴曹地府轮回司");
  set("long", @LONG
这里是阴曹地府的轮回司，轮回司主王方平正在大殿正中央端坐，
他可是阎罗王手下的得力干将。
LONG
  );
  set("exits", ([
    "down": __DIR__ "rukoujob",
  ]));

  set("objects", ([
    "/d/quest/cike/sima": 1,
    "/d/quest/xywjob/xyw": 1,
  ]));
  set("no_magic", 1);
  set("no_fight", 1);
  set("outdoors", __DIR__);
  setup();
}
