#include <ansi.h>
inherit __DIR__"r.lpc";

void create_room()
{
        set("short","大门");
        set("long",@LONG
只见那洞门质地颇类珊瑚，比火还红，映得上面的两个
大木环笼罩在一片红光之中，此刻双扉紧闭。
LONG);  
        set("exits",([
                "south" : __DIR__"zhudong-s",
        ]));    
}

void init()
{
        add_action("do_open","open");
}

int do_open(string arg)
{
        return err_msg("你死命的推了推门，结果大门纹丝不动。\n");
}       
        
