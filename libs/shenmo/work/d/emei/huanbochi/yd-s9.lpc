#include <ansi.h>
inherit __DIR__"r.lpc";

void create_room()
{
        set("short","甬道");
        set("long",@LONG
一条孔道，里面酷寒阴森，黑沉沉的，只是一片浓影。
LONG);  
        set("exits",([
                "south" : __DIR__"yd-s10",
                "north" : __DIR__"yd-s8",
        ]));    
}

