#include <ansi.h>
inherit __DIR__"r.lpc";

void create_room()
{
        set("short","甬道");
        set("long",@LONG
这一带甬路本是又高又窄，忽然接连两个转折过去，
地势突往上高起了数十丈。
LONG);  
        set("exits",([
                "up"    : __DIR__"yd-s11",
                "north" : __DIR__"yd-s9",
        ]));    
}

