#include <ansi.h>
#include <room.h>
inherit __DIR__ "r.lpc";

void create_room() {
  set("short", "甬道");
  set("long", @LONG
这是一个狭长的的曲折甬道。四壁颜色渐渐开始变得
由金转赤，隐约间有股热气在脚下翻腾。    
LONG);
  set("exits", ([
    "east": __DIR__ "yd-s3",
    "west": __DIR__ "yd-s1",
  ]));
}
