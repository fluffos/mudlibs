#include <ansi.h>
inherit __DIR__"r.lpc";

void create_room()
{
        set("short","寝室");
        set("long",@LONG
此间空空，只以五色暗寓五行，未设别的法物，更看不
出一点异状和行法的痕迹。
LONG);  
        set("exits",([
                "west" : __DIR__"changtang",
                "east" : __DIR__"qinshi-2",
        ]));    
}

