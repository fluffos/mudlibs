#include <ansi.h>
inherit __DIR__"r.lpc";

void create_room()
{
        set("short","甬道");
        set("long",@LONG
一条狭长的甬道，地势逐渐向下，头顶开始出现一些大
小不一的石乳。
LONG);  
        set("exits",([
                "down"  : __DIR__"yd-b3",
                "up"    : __DIR__"yd-b1",
        ]));    
}

