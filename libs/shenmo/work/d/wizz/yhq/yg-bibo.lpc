inherit __DIR__ "guai.lpc";
#include "guai.h"

void set_skills(int max_value, int level) {
  int guai_value;
  ::set_skills(max_value, level);

  if (level == 1) guai_value = max_value + max_value / 8;
  else if (level == 2) guai_value = max_value + max_value / 6;
  else guai_value = max_value + max_value / 4;


  set_skill("chuanyun-tui", guai_value + random(16));
  set_skill("yulong-steps", guai_value + random(16));
  set_skill("blade", guai_value + random(16));
  set_skill("huaxue-blade", guai_value + random(16));
  set_skill("bihai-spells", guai_value + random(16));
  set_skill("unarmed", guai_value + random(16));
  set_skill("zhuanlong-force", guai_value + 20 + random(16));
  map_skill("spells", "bihai-spells");
  set("class", "yao");
  map_skill("force", "zhuanlong-force");
  map_skill("unarmed", "chuanyun-tui");
  map_skill("blade", "huaxue-blade");
  map_skill("parry", "huaxue-blade");
  map_skill("dodge", "yulong-steps");
  set("force_factor", query_skill("force", 1));
  set("mana_factor", 100);
  set("family/family_name", "乱石山碧波潭");
  /*
       set("chat_chance_combat", 60);
        set("chat_msg_combat", ({
                (: perform_action, "blade","piaoxiang" :)

               }) );                       
*/
  carry_object("/d/obj/armor/jinjia.lpc")->wear();
  carry_object("/d/obj/weapon/blade/jindao")->wield();
}

void init() {
  add_action("do_none", "touxiang");
  add_action("do_none", "surrender");
}
int do_none() {
  object me = this_object();
  message_vision("想耍赖皮？\n", me);
  return 1;
}
