#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "魔甲工厂");
  set("long", @LONG

这里是城中著名的魔甲加工作坊。在这里你可以叫老板替你
度身打造你种意的魔幻装甲，自然价格也是根据装甲的性能而定的。

LONG);
  set("exits", ([
    "south": __DIR__ "jie-w1",
  ]));
  set("objects", ([
    __DIR__ "npc/boss/armor": 1,
  ]));
  set("hs_world", 1);
  setup();
}
