#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "魔甲加工作坊");
  set("hs_world", 1);
  set("long", @LONG

这里便是四象城赫赫有名的魔甲加工作坊了，只要集齐了魔幻装甲，便可以
在此让老板加工成最终的魔幻机甲。当然价格自然是不菲的。

LONG);
  set("exits", ([
    "north": __DIR__ "jie-e2",
  ]));
  set("objects", ([
    __DIR__ "npc/boss/robit": 1,
  ]));
  setup();
}
