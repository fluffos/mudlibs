inherit ROOM;

void create() {
  set("short", "厢房");
  set("long", @LONG

这里是一间布置得相当雅致的厢房，正中墙上大笔书写着一个
“狂”字，想必这就是灵剑门狂剑子唐秋闪的厢房。
LONG);
  set("objects", ([ __DIR__ "npc/tang": 1,
  ]));


  set("exits", ([
    "east": __DIR__ "houyuan",
  ]));
  setup();
}
