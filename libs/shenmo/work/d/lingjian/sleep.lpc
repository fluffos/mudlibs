inherit ROOM;

void create() {
  set("short", "休息室");
  set("long", @LONG

这是弟子们休息、睡觉的房间。
LONG);
  set("exits", ([
    "west": __DIR__ "lianwu",
  ]));

  set("no_clean_up", 0);
  set("sleep_room", 1);
  set("if_bed", 1);

  setup();
  replace_program(ROOM);
}
