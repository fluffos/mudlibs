// by seawind
inherit ROOM;
void create() {
  set("short", "山郊野岭");
  set("long", @LONG
你趋步往前，不料一阵风响，草丛中窜出了一只白蛇！
LONG);
  set("exits", ([
    //"east" : __FILE__,
    //"west" : __FILE__,

    "south": __DIR__ "hmy6",
    "northdown": __DIR__ "hmy8",
  ]));
  set("objects", ([
    __DIR__ "npc/tiger": 1,

  ]));
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
