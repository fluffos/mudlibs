

#include <room.h>
inherit ROOM;

void create ()
{
  set ("short", "下邳桥下");
  set ("long", @LONG





到处都是脏兮兮的污泥，哪里去找什么鞋子,试着钻到桥洞里头去看看吧!
LONG);

//  set("objects", ([ /* sizeof() == 1*/ 
//  __DIR__"npc/erlang" : 1,
//]));
//  set("outdoors", 1);
  
  set("position", "桥6");
  set("exits", ([ /* sizeof() == 2 */
  "northwest" : __DIR__"qx5",
  "southwest" : __DIR__"qx7",
]));
//      
        set("water", 1);
//  
  setup();
}

void init()
{
        add_action("do_zuan", "zuan");
}

int do_zuan(string arg)
{       
        object me;
        me=this_player();message_vision("$N缓缓的钻进桥洞，mmd,,鞋子怎么会掉这里头了???\n",me);
                me->move("/d/bwbj/qiaodong");
                me->set_temp("enter_position", "桥6");
                tell_room( environment(me), "可怜的你，狼狈不堪的钻进了桥底小洞。\n", ({me}));
                return 1;
}

