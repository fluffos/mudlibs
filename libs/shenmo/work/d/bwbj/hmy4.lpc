//Cracked by Roath
// by snowcat

inherit ROOM;

void create ()
{
  set ("short", "无名酒楼");
  set ("long", @LONG

山郊野岭的居然有一个酒楼,酒楼红梁绿瓦，外面挂着一匾大招牌：“无名酒楼”。楼里面摆着大
红木桌子，从里面不时地散发出一阵阵饭菜香酒香。

LONG);

  set("exits", ([
    "west" : __DIR__"hmy3",
 "north" : __DIR__"hmy5", ]));
  set("objects", ([ /* sizeof() == 2 */
    __DIR__"npc/hai" : 1,
  ]));
  set("resource", ([ /* sizeof() == 1 */
    "water" : 1,
  ]));
  setup();
}

