// fangjian1.c
// by lestat/2001.1.31
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "树林");
  set("long", @LONG
阴森森的什么也看不见，只看见一个若有若无的身影。
LONG);
  set("exits", ([
    "south": __DIR__ "fangjian1",
  ]));
  set("no_clean_up", 0);
  setup();

}
