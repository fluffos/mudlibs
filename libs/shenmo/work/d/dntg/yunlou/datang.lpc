//kunlun
inherit ROOM;

void create ()
{
        set ("short", "李府大堂");
        set ("long", @LONG

这里就是大元帅李靖府上了，只见正中坐着一位
花枝招展的姑娘，看来她就是李府的女主人了。
LONG);

        set("exits", 
  ([ //sizeof() == 4
                "north" : __DIR__"taizifu",

        ]));
        set("objects", ([
              "/u/yhq/fazhou/npc/pipa" : 1,
              

   ]));
       setup();
	replace_program(ROOM);
}

