#include <ansi.h>
inherit BULLETIN_BOARD;

void create() {
  set_name("记事石", ({ "jishi shi", "board" }));
  set("location", "/c/playroom/wuxia");
  set("board_id", "player_bbs_wuxia");
  set("long", "一块留言板。\n");
  setup();
  set("capacity", 500);
  replace_program(BULLETIN_BOARD);
}
