#include "/u/snowtu/weapon.h"  
#include <ansi.h>  
inherit SHOUTAO;  

void create() { 
set_name( MAG"狼牙"NOR, ({"quan tao", "hand" }));  
set_weight(8000);  
if (clonep())   set_default_object(__FILE__);  
else {  
      set("unit", "双");  
      set("long",  HBWHT+MAG"用一百○八个；狼牙串接起来的格斗手套。\n"NOR);  
      set("material", "steel"); 
           set("value",0);
      init_shoutao(100);  
     }  
setup( ); 
 } 

