// Room: /d/mojie/shanfang.c
inherit ROOM;

void create()
{
  set ("short", "密洞");
  set ("long", @LONG
这是魔界的密洞，无天佛祖经常在这里闭关修炼。
LONG);
   set("exits", ([ /* sizeof() == 1 */
 // "west" : __DIR__"guodao4",
]));
        set("objects", 
        ([ //sizeof() == 1
  __DIR__"npc/wu tian" :1,
 
        ]));
  setup();
  replace_program(ROOM);
}
