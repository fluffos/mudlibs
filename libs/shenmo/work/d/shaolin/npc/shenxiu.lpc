
//gunseng.c
#include <ansi.h>

int ask_name(); 

inherit NPC;
inherit F_MASTER;
void create()
{

set_name("神秀", ({
                "shen xiu",
                "shenxiu",
                "xiu",
                "shen",
        }));
        set("gender", "男性" );
        create_family("少林寺", 3, "弟子");
        set("title",HIY"大通禅师"NOR );
        set("age", 70);
set("long",
                "他就是号称“两京法主，三帝国师”的神秀禅师。禅宗北宗之祖。\n"
                "他身材瘦高，脸上满布皱纹，手臂处青筋绽露，似乎久经风霜。\n"
        );
        set("class", "human");
        set("attitude", "heroism");

        set("max_force", 3000);
        set("force_factor", 50);
        set("mana", 2000);
        set("max_mana", 3000);
        set("mana_factor", 20);
        set("combat_exp", 1800000);
        set("daoxing", 2500000);

        set_skill("yijinjing", 300);
        set_skill("chanzong", 300);
        set_skill("spells", 300);
        set_skill("force", 300);
        set_skill("dodge", 280);
        set_skill("shaolin-shenfa", 280);
        set_skill("stick", 300);
        set_skill("zui-gun", 300);
        set_skill("parry", 280);
        set_skill("unarmed", 280);
        set_skill("literate", 280);
        set_skill("suixing-tui", 300);
        map_skill("stick", "zui-gun");
        map_skill("unarmed", "suixing-tui");
        map_skill("dodge", "shaolin-shenfa");
        map_skill("parry", "zui-gun");
        map_skill("force", "yijinjing");
        map_skill("spells", "chanzong");

        set("max_kee", 3300);
        set("max_sen", 3300);
        setup();
        carry_object("/d/shaolin/obj/qimeigun")->wield();
        carry_object("/d/shaolin/obj/xuan-cloth")->wear();


}
void attempt_apprentice(object ob)
{
        object me; 
        mapping ob_fam, my_fam;
        string name, new_name;

        me = this_object();
        my_fam  = me->query("family");
        name = ob->query("name");

        if( (string)ob->query("gender") != "男性" )
        {
                command ("say 阿弥陀佛！女施主呀，贫僧可不敢开这等玩笑啊。");
                return;
        }

          if( (int)ob->query_skill("chanzong",1) < 180)
        {
        command("say " + RANK_D->query_respect(ob) + "，你禅宗佛法修为不够，不能越级拜师。");
                return;
        }

        if (!(ob_fam = ob->query("family")) || ob_fam["family_name"] != "少林寺")
        {
                command("say " + RANK_D->query_respect(ob) + "与本派素无来往，不知此话从何谈起？");
                return;
        }

        if( (string)ob->query("human/class")!="human" )
        {
                command ("say 阿弥陀佛！贫僧不收『俗家弟子』。。。");
                return;
        }

        command("say 阿弥陀佛，善哉！善哉！");
        command("recruit " + ob->query("id"));
        ob->set("title", HIY"嵩山少林寺罗汉堂弟子"NOR);
}

