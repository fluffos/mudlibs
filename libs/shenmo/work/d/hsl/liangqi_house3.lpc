// 作者：tonii@飘渺水云间，2003-5-2于浙大，修改请保留此行。
// ROOM: /d/hsl/liangqi_house3

inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "两栖族营帐");
  set("long", @LONG

这就是两栖族赖以遮风挡雨的营帐，营帐仅仅只有丈半见方，
但却不显得拥挤，因为屋内几乎是一贫如洗，除了床、桌子
等必备用品外，其他东西基本无法看到，更别提值钱的了。
LONG);

  set("exits",
    ([
      "out": __DIR__ "liangqi_street2",
    ]));

  /*  set("objects", 
  ([ 
   "/d/hsl/npc/liangqi_man2" : 1,
  ]));
*/

  set("no_clean_up", 0);

  setup();

}
