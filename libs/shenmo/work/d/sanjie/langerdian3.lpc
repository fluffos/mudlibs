#include <ansi.h>
inherit ROOM;

void create ()
{
        set ("short", "浪儿店二楼");
        set ("long", @LONG
一家小店，提供了基本的服务。店里只有一个老人家打理，
不过还是停干净的，有回到家的感觉。

LONG);
        set("exits", ([
        ]));
        set("no_time", 1);
        set("no_fight", 1);
        set("no_magic", 1);
        set("chat_room",1);
        set("outdoors", 0);
        set("no_bian",1);
        set("valid_startroom", 1);
        setup();
}
void init()
{
    this_player()->move("/d/sanjie/langerdian2");
   return;
}
