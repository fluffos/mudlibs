inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "结界");
  set("long", @LONG

一个奇怪的地方，周围一片黑暗，唯有前方隐隐透出点光亮。

LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "out": "/d/sfighter/jiaowai",
  ]));
  set("objects", ([ /* sizeof() == 1 */
  ]));
  set("no_time", 1);
  set("no_fight", 1);
  set("no_magic", 1);
  setup();
}
