#include <ansi.h>
inherit ROOM;
inherit F_MASTER; 

void create()
{
  set ("short","酒吧");
	set("long", @LONG
天啊，这里面的东西不知道被谁砸得惨不忍睹，看来是有人来捣乱过。
一个满脸衰气的人在那高声破骂。
LONG
	);
set("exits", ([ /* sizeof() == 1 */
  "west" : "/d/sfighter/road-n2",
]));
  set("objects", ([ /* sizeof() == 1 */
     "/d/sfighter/npc/drink-boss" : 1,
]));
	setup();
}


 

