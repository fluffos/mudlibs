inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "瀑布");
  set("long", @LONG
前路已绝，奔腾的河水挟着万千之势望断崖直
窜而下，配以气势磅礴的冲击声响，俨然一副
千军万马出征图。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": "/d/sfighter/river2",
    "east": "/d/sfighter/river4",
  ]));
  set("objects", ([ /* sizeof() == 1 */

  ]));
  setup();
}
