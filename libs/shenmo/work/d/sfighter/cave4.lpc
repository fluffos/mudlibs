#include <ansi.h>
#include <room.h>
inherit ROOM;
void create()
{
 set("short", "洞内");
 set("long", @LONG
这里是洞里唯一没被紫气入侵的地方。在隐蔽处似
乎有一个人，定睛一看，啊！那人的筋肉竟然与石
头合为一体了。
LONG
 );
 set("exits", ([
  "east" : "/d/sfighter/cave3",
]));
        set("objects", ([
      "/d/sfighter/npc/long":1,
       ]) );
 setup();
}
