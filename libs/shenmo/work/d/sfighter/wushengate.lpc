inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "武神流大门");
  set("long", @LONG
青石做成的大门，边上的柱子镏金雕龙，气势不凡。远望去
可以看到远处有一座大山，但这山被奇异的紫气笼罩，那就
是传说中的 紫气山，你可从那感受到一股非常强大的力量。
LONG);
  set("exits", ([ /* sizeof() == 1 */
    "northeast": "/d/sfighter/road-s1",
    "southup": "/d/sfighter/wushenroad1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/d/sfighter/npc/menzhong": 1,
    "/d/sfighter/npc/adon": 1,
  ]));
  setup();
}
