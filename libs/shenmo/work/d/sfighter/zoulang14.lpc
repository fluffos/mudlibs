inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "走廊");
  set("long", @LONG
走廊。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": "/d/sfighter/zoulang15",
    "southeast": "/d/sfighter/zoulang13",
  ]));
  set("objects", ([ /* sizeof() == 1 */
  ]));
  setup();
}
