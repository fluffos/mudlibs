inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "监狱");
  set("long", @LONG
这里是武神流关押重犯的地方，把守森严。每个牢房
都有一道通电铁门，铁门紧闭，但其中一个牢房却无
人把守，铁门也未关闭，偷偷探视一下，里面似乎也
有犯人，不知是什么人。
LONG);
  set("exits", ([ /* sizeof() == 1 */
    "enter": "/d/sfighter/laofang",
    "south": "/d/sfighter/zoulang5",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/d/sfighter/npc/sishi": 2,
  ]));
  setup();
}
