
inherit ROOM;
#include <ansi.h>

void create()
{
  set ("short","房间");
	set("long", @LONG
一个身穿武斗服的女孩正目不转睛的盯着一个会
发光的东西，时不时发出一阵银铃般的笑声。
LONG);
set("exits", ([ /* sizeof() == 1 */
  "out" : "/d/sfighter/pianting",
]));
  set("objects", ([ /* sizeof() == 1 */
   "/d/sfighter/npc/sakura":1,
]));
	setup();
}


