inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "正厅");
  set("long", @LONG
武神流正厅，迎面的龙头黄金椅上做着一个气势逼人
的壮年男子，见你进来，两眼逼视着你。这份威势使
得你差点跪了下来。正厅两旁各有一个门，不知通往何
处。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "out": "/d/sfighter/lianwuchang",
    "west": "/d/sfighter/pianting",
    "east": "/d/sfighter/zoulang1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/d/sfighter/npc/wuliuyun": 1,
    // "/d/sfighter/npc/zongguan":1,
  ]));
  setup();
}
