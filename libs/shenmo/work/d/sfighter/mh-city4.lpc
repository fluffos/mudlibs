inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "梦幻都市--外城城墙");
  set("long", @LONG
这里是梦幻都市外城的城墙。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": "/d/sfighter/mh-city1",
  ]));

  set("objects", ([ /* sizeof() == 1 */
  ]));
  setup();
}
