inherit ROOM;
#include <ansi.h>

void create()
{
  set ("short","羊肠小道");
	set("long", @LONG
前面就是一片茂密的森林，进去只怕走不出来了。

LONG);
set("exits", ([ /* sizeof() == 1 */
  "southeast" : "/d/sfighter/xiaodao3",
  "north" : "/d/sfighter/forst0",
]));
  set("objects", ([ /* sizeof() == 1 */
]));
	setup();
}

