inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "东门");
  set("long", @LONG
一个奇特的巨大无比的门，奇怪的是竟然没有半个守卫。在这里就可
听到市区当中嘈杂喧哗，似乎热闹非常。前面有个小树林，隐隐可以
看到树林中似乎有栋房子。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": "/d/sfighter/road-e1",
    "northeast": "/d/sfighter/shulin1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
  ]));
  setup();
}
