
inherit ROOM;
#include <ansi.h>

void create()
{
  set ("short","山路");
	set("long", @LONG
狭窄难行的山路弯弯曲曲。眺目远望，
可见其延绵不断，直通往瀑布之下。
LONG
	);
set("exits", ([ /* sizeof() == 1 */
  "west" : "/d/sfighter/river3",
  "southdown" : "/d/sfighter/river5",
]));
  set("objects", ([ /* sizeof() == 1 */

]));
	setup();
}


