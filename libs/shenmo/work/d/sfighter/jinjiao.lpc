
inherit ROOM;
#include <ansi.h>

void create()
{
  set ("short","近郊");
	set("long", @LONG
沿着大路再往前走，不远处就是一个大城市了。

LONG
	);
set("exits", ([ /* sizeof() == 1 */
  "south" : "/d/sfighter/gate-n",
  "northeast" : "/d/sfighter/jiaowai",
]));
  set("objects", ([ /* sizeof() == 1 */
]));
	setup();
}


