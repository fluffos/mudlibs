inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "湖泊");
  set("long", @LONG
茂密的树林中竟然藏着这么一个美丽的湖。湖面碧波
粼粼，微风带着一股清新的气息迎面而来，与人一种
说不出的舒畅。湖边有个小石坡，坡下似乎有个人工
造成的小山洞，不知是哪位高人隐居于此。
LONG);
  set("exits", ([ /* sizeof() == 1 */
    "south": "/d/sfighter/forst0",
    "enter": "/d/sfighter/sagat_room",
  ]));
  set("objects", ([ /* sizeof() == 1 */
  ]));
  setup();
}
