
inherit ROOM;
#include <ansi.h>

void create()
{
  set ("short","山路");
	set("long", @LONG
再往前走就要走到山上了，刚好可以看看那巨大的黑影是什么？
LONG
	);
set("exits", ([ /* sizeof() == 1 */
  "southup" : "/d/sfighter/shanlu3",
  "north" : "/d/sfighter/gate-s",
]));

  set("objects", ([ /* sizeof() == 1 */
]));
	setup();
}

