inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "山洞");
  set("long", @LONG
你走近一看，果然是人工早成的山洞，不知道何人用外力所造。
一入洞内，一个浑身伤痕的老者正在行功，对你不理不睬。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "out": "/d/sfighter/lake",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    "/d/sfighter/npc/sagat": 1,
  ]));
  setup();
}
