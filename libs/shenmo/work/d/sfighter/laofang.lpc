inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "牢房");
  set("long", @LONG
武神流关押重犯的地方。窗口站着一个身穿囚服的
男人，此时正望着远方。
LONG);
  set("exits", ([ /* sizeof() == 1 */
    "out": "/d/sfighter/zoulang6",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/d/sfighter/npc/powerstone": 1
  ]));
  setup();
}
