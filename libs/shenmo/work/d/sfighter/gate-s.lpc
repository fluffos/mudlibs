
inherit ROOM;
#include <ansi.h>

void create()
{
  set ("short","南门");
	set("long", @LONG
一个奇特的巨大无比的门，奇怪的是竟然没有半个守卫。在这里就可
听到市区当中嘈杂喧哗，似乎热闹非常。
LONG
	);
set("exits", ([ /* sizeof() == 1 */
  "north" : "/d/sfighter/road-s2",
  "south" : "/d/sfighter/shanlu1",
]));
  set("objects", ([ /* sizeof() == 1 */
]));
	setup();
}


