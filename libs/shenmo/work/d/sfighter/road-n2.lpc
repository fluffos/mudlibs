inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "北大街");
  set("long", @LONG
大街上满是奇装异服的人，有的来往匆匆，有的高声谈笑，
旁边还有些奇怪的房子，应该是卖东西的吧。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": "/d/sfighter/road-n3",
    "south": "/d/sfighter/road-n1",
    "east": "/d/sfighter/drink",
  ]));
  set("objects", ([ /* sizeof() == 1 */
  ]));
  setup();
}
