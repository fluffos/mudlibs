inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "山路");
  set("long", @LONG
狭窄难行的山路弯弯曲曲。眺目远望，
可见其延绵不断，直通往瀑布之下。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "northup": "/d/sfighter/river5",
    "southdown": "/d/sfighter/river7",
  ]));
  set("objects", ([ /* sizeof() == 1 */

  ]));
  setup();
}
