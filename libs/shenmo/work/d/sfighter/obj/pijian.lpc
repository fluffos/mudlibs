#include <armor.h>

inherit SURCOAT;

void create() {
  set_name("披肩", ({ "pijian" }));
  set_weight(3000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("material", "cloth");
    set("unit", "件");
    set("value", 0);
    set("armor_prop/armor", 10);

  }
  setup();
}
