inherit ROOM;
#include <ansi.h>

void create()
{
  set ("short","森林");
	set("long", @LONG
这里人迹罕至，茂密丛生的树木形成了一道天然的迷宫。
甫一进入，你顿心旷神怡，长舒了一口气，定神四望时，
却已迷失了方向。

LONG);
set("exits", ([ /* sizeof() == 1 */
   "north"  :       __DIR__"forst"+sprintf("%d",random(8)),
   "east"  :       __DIR__"forst"+sprintf("%d",random(8)),
   "south"  :       __DIR__"forst"+sprintf("%d",random(8)),
   "west"  :       __DIR__"forst"+sprintf("%d",random(8)),
         ]));
  set("objects", ([ /* sizeof() == 1 */
]));
	setup();
}

