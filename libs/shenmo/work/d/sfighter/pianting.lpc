inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "偏厅");
  set("long", @LONG
偏厅。旁边有个门，不时传来一阵女孩的笑声。
LONG);
  set("exits", ([ /* sizeof() == 1 */
    "east": "/d/sfighter/zhengting",
    "enter": "/d/sfighter/wushenroom",
    "north": "/d/sfighter/zoulang11",
  ]));
  set("objects", ([ /* sizeof() == 1 */
  ]));
  setup();
  "/obj/board/sfighter_b"->foo();
}
