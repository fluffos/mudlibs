inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "走廊");
  set("long", @LONG
走廊。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "northwest": "/d/sfighter/zoulang14",
    "southwest": "/d/sfighter/zoulang12",
  ]));
  set("objects", ([ /* sizeof() == 1 */
  ]));
  setup();
}
