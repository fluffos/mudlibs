inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "走廊");
  set("long", @LONG
走廊。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "northeast": "/d/sfighter/zoulang13",
    "south": "/d/sfighter/zoulang11",
  ]));
  set("objects", ([ /* sizeof() == 1 */
  ]));
  setup();
}
