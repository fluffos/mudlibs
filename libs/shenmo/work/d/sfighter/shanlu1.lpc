inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "山路");
  set("long", @LONG
往北走就到了城市中了，远远就能听到嘈杂声，似乎
非常热闹。往南可以到达一座高山，山上面似乎有个
巨大的黑影不知道是什么。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": "/d/sfighter/shanlu2",
    "north": "/d/sfighter/gate-s",
  ]));

  set("objects", ([ /* sizeof() == 1 */
  ]));
  setup();
}
