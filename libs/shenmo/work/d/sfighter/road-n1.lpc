inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "北大街");
  set("long", @LONG
再往南走就是市中心了，一大堆人围在那不知道在看什么东西，去凑凑
热闹吧。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": "/d/sfighter/road-n2",
    "south": "/d/sfighter/center",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/d/sfighter/npc/woman": 1,
  ]));
  setup();
}
