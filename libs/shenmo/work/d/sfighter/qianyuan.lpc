
inherit ROOM;
#include <ansi.h>

void create()
{
  set ("short","前院");
	set("long", @LONG
大院子,对面是房子的入口。
LONG
	);
set("exits", ([ /* sizeof() == 1 */
  "north" : "/d/sfighter/vigagate",
  "enter" : "/d/sfighter/vigaroom",
]));
  set("objects", ([ /* sizeof() == 1 */
]));
	setup();
}


