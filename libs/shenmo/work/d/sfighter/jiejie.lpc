inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "结界");
  set("long", @LONG

一个奇怪的地方，周围一片黑暗，唯有前方隐隐透出点光亮。
在昏暗的光线下，周围似乎站着一个奇怪的人。

LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": "/d/swordman/northseashore",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    "/d/sfighter/npc/rose": 1,
  ]));
  set("no_time", 1);
  set("no_fight", 1);
  set("no_magic", 1);
  setup();
}
