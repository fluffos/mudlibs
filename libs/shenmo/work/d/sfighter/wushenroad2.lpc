inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "石阶");
  set("long", @LONG
长长的石阶，通往武神流练武场。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "southup": "/d/sfighter/wushenroad3",
    "northdown": "/d/sfighter/wushenroad1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
  ]));
  setup();
}
