inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "梦幻都市--城门");
  set("long", @LONG
好一个梦幻都市，不亏为当今世上最先进最富饶
的地方，到处金碧辉煌，堪称为人间中的天堂。
城门是用世界上最硬的东西--超合金所制，要想
攻破真是难比登天。现在城门紧闭，无法入内。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": "/d/sfighter/mh-city1",
  ]));

  set("objects", ([ /* sizeof() == 1 */
  ]));
  setup();
}
