inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "大厅");
  set("long", @LONG
你刚一进门，就觉得迎面扑来一阵杀气。有个俊秀却极为冷酷的
青年坐在一张龙椅上，身后有一位美貌非凡的女孩。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "out": "/d/sfighter/qianyuan",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    "/d/sfighter/npc/viga": 1,
    "/d/sfighter/npc/julier": 1,
  ]));
  setup();
}
