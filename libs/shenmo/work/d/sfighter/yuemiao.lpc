
inherit ROOM;
#include <ansi.h>

void create()
{
  set ("short","岳王庙");
	set("long", @LONG
    这里是岳王庙的正殿，内有岳飞像，像上方悬挂岳飞手书“还我河山”的
横匾。殿两侧壁上嵌着“精忠报国”四个大字。武人到此，都放下武器，毕恭毕敬
地上香礼拜。
LONG);
set("exits", ([ /* sizeof() == 1 */
  "east" : "/d/sfighter/road-w2",
]));
  set("objects", ([ /* sizeof() == 1 */
        __DIR__"npc/yue":     1,
]));
	setup();
}


