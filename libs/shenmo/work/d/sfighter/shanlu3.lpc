
inherit ROOM;
#include <ansi.h>

void create()
{
  set ("short","山路");
	set("long", @LONG
再往前走就要走到山顶上了，刚好可以看看那巨大的黑影是什么？
LONG
	);
set("exits", ([ /* sizeof() == 1 */
  "northdown" : "/d/sfighter/shanlu2",
  "southup" : "/d/sfighter/shanlu4",
]));

  set("objects", ([ /* sizeof() == 1 */
]));
	setup();
}

