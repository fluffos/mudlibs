inherit ROOM;
#include <ansi.h>

void create()
{
  set ("short","西大街");
	set("long", @LONG
西边是一座古朴的庙宇，香火缭绕，那就是天下人所共同敬
仰的岳王庙了。南边是一栋豪华别墅，不知到底是何人住在
里面。西北面为一羊肠小道，不知通向何处。
LONG);
set("exits", ([ /* sizeof() == 1 */
  "east" : "/d/sfighter/road-w1",
  "west" : "/d/sfighter/yuemiao",
  "south" : "/d/sfighter/vigagate",
  "northwest" : "/d/sfighter/xiaodao1",
]));
  set("objects", ([ /* sizeof() == 1 */
]));
	setup();
}

