inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "白鹿洞");
  set("long", @LONG

此地为白鹿书院的大门，自先祖南唐命国子监九经李善道始，此处即为李氏
后人解书传道之所。
在这个百废待兴的时代，李氏后代依旧不忘祖训，在此传授世人文理之道，
儒家之髓。
如今现在大门紧闭，尚是休憩时间。

LONG);

  set("exits", ([
    "down": __DIR__ "bailudong",
  ]));
  set("objects", ([
    //	__DIR__"npc/shusheng" : random(3),
  ]));
  set("no_magic", 1);
  set("no_fight", 1);
  setup();
}
