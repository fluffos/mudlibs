inherit ROOM;
#include <ansi.h>

void create ()
{
set ("short", "山路");
set ("long", @LONG

与世隔绝的地方，树木耸立，虫鸣鼓噪，在未来世界的空间，
这里让你想起了异地的故乡。
风吹过洒洒之声不断，流水潺潺，时闻苍狼嗥叫，时闻饿虎
咆哮。
这里因为与世隔绝的缘故，也成为了受辐射污染的大地少见
的一片净土。但是因为有众多的隐世高人蛰居于此，所以三
大势力对此处也是暂时不敢得罪。

LONG);

set("exits", ([
	"down"    : __DIR__"shanlu1",
	"up"       : __DIR__"shanlu3",
]));
set("objects",([
//	__DIR__"npc/animal" : random(3),
]));
set("outdoors",__DIR__"");
setup();
}

