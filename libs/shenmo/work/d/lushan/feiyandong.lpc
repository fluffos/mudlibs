inherit ROOM;
#include <ansi.h>

void create ()
{
set ("short", "飞燕洞");
set ("long", @LONG

洞内种乳林立，千姿百态，洞的深处是一个天然的地下湖，
流光溢影，映得人满面彩霞一般。

LONG);

set("exits", ([
	"out"   : __DIR__"feiyanbi",
]));

set("item_desc", ([  //暂时不开放
	"湖" : @TEXT
深不可测的天然地下湖，仿佛蕴藏着天地间不可预测的秘密。
TEXT]));

set("objects",([
      __DIR__"npc/chunli999" : 1,
]));
setup();
}
