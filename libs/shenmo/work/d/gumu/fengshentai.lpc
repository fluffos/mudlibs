//轩辕古墓
inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIW "封神台" NOR);
  set("long", @LONG

      殷商灭亡之后，为化解上古时代各路神仙妖魔的恩怨。姜子牙奉
      原始天尊的法旨，在此修建封神台，分封神位，招引
      各路神仙归位。

LONG
  );

  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/jiang": 1,
  ]));

  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "tandim",
    "east": __DIR__ "tandim",
    "down": __DIR__ "beiji.lpc",
  ]));
  set("indoors", "1");
  set("no_fight", 1);
  set("no_magic", 1);
  set("no_clean_up", 0);

  setup();
}
