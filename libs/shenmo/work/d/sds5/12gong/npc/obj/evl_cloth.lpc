// by snowtu@XMxyj2000

#include <armor.h>
#include <ansi.h>
#include "chk_owner2.lpc"
inherit ARMOR;

void create() {
  set_name(HIB "冥衣" NOR, ({ "evl cloth" }));
  set_weight(3000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "件");
    set("material", "steel");
    set("armor_prop/armor", 650);
  }
  setup();
}
