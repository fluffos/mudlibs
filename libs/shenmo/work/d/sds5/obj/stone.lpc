#include <ansi.h>

inherit ITEM;

void create() {
  set_name(YEL "碎石" NOR, ({ "sui shi", "shi", "stone" }));
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "些");
    set("long", "一块大石头。");
  }
  setup();
}
