#include <ansi.h>
inherit ROOM;
#include "road.h"

void create ()
{
set("short",WHT"爱因斯坦东街"NOR);
set("long", @LONG
你走在一条宽阔的石板大街上，西面就快到城中心了，可以看到钟楼
耸立于前。北面是一家酒吧。
LONG);
set("exits",([
        "north" : __DIR__"kezhan",
        "west" : __DIR__"center",
        "east" : __DIR__"jie-e2",
]));
set("objects",([
        __DIR__"npc/jumin2":     random(2),
        __DIR__"npc/jumin4":     random(2),
]));
setup();
}

