#include <ansi.h>
inherit ITEM; 
        
void create() 
{ 
     set_name(HIG"偷窥之眼"NOR, ({ "eye","yan" }) );
     set_weight(1);
           set("long","携带者可以破除一切屏障，发现隐藏之中的道路。\n"); 
           set("value", 1); 
           set("unit", "颗"); 
     setup();
} 

