// Room: /d/jjg/gate.c

inherit ROOM;

void create() {
  set("short", "神策军");
  set("long", @LONG

这里是大唐神策军的驻地了，神策军是大唐王朝的最后一道防线，驻地及其机密。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "front_yard.lpc",
    "out": "/d/huanggong/baiyujie",
  ]));
  set("outdoors", __DIR__ "");
  set("objects", ([ /* sizeof() == 1 */

  ]));

  set("objects", ([
    __DIR__ "npc/cutnpc": 1,
  ]));

  setup();
}
