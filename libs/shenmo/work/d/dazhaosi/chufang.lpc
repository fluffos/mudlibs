inherit ROOM;

void create() {
  set("short", "厨房");
  set("long", @LONG
这里是大昭寺的厨房，新来的僧人可以在此打杂。房中醒
目的位置有一块牌子，但已经破旧不堪，字迹模糊了。
LONG);
  set("exits", ([
    "south": __DIR__ "xiaoyuan",
  ]));

  set("objects", ([
    __DIR__ "npc/xlama": 2 + random(2),
  ]));

  setup();
}
