
inherit ROOM;

void create()
{
        set("short", "库房");
        set("long", @LONG
这里是练武场旁边的小库房，房里密不通风，只有一丝光
线从屋檐小缝中投射进来。房中有些备用的武器，都是密宗弟
子日常练功需要用到的兵器。
LONG);
        set("exits", ([
                "east" : __DIR__"chang",
        ]));
        set("objects", ([
                __DIR__"obj/senggun" : random(3),
                __DIR__"obj/gangchu" : random(3),
                "/d/obj/weapon/sword/changjian" : random(2),
        ]));
        setup();
}

