//Cracked by Roath
#include <room.h>
#include <ansi.h>
#include <mudlib.h>
inherit ROOM;
void create() {
  set("short", HIR "[" + HIY + "麻将馆" + HIR + "]" NOR);
  set("long", @LONG
三教九流的人全聚集在这里，他们目无旁人，专心在自己
赌博事业上。～～～～详细的规则请
看看布告栏（ｂｏａｒｄ）
LONG
  );
  set("light", 1);
  set("no_fight", 1);
  set("no_magic", 1);
  set("freeze", 1);
  set("no_spells", 1);
  set("exits", ([
    "south": "/d/sfighter/road-w1",
  ]));
  set("objects", ([
    "/d/sfighter/npc/mjlady": 1,
  ]));
  set("item_desc", ([
    "board": "
 一般基本的操作：：  每种牌都有其代码　例：一万－＞1w 七索－＞7s
 加入牌局   ：" HIY "addin" NOR "        开始牌局   ：" HIY "start" NOR " 
 摸牌       ：" HIY "mo" NOR "           重开牌局   ：" HIY "restart" NOR " 
 看玩家1的牌：" HIY ".1" NOR "           看玩家2的牌：" HIY ".2" NOR " 
 看玩家3的牌：" HIY ".3 " NOR "          看玩家4的牌：" HIY ".4" NOR " 
 看自己的牌 ：" HIY "." NOR "
 丢牌       ：" HIY "dump xx" NOR "      吃         ：" HIY "eat xx xx " NOR "        
 碰         ：" HIY "pon" NOR "          杠         ：" HIY "gon " NOR "              
 胡         ：" HIY "hu" NOR "           听         ：" HIY "tin xx  " NOR "          
 xx为代码 例：" HIY "eat 2w 3w" NOR " 
",
  ]));
  setup();
}
