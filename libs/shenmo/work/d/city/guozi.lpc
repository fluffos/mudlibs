//Cracked by Roath
//changan city

#include <room.h>
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "国子监");
  set("long", @LONG

这里是国子监的正堂，也就是秀才们平时上课的地方。高高低低的桌
子排了两溜。东边的一排窗子都开着，把屋中照的通亮。正面一桌一
椅，上有文房四宝。一位老先生正闭目养神。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "west": __DIR__ "guozijian",
      // "east" : __DIR__"leitai/bbb",
    ]));

  set("objects",
    ([  //sizeof() == 1
    //                __DIR__"npc/zhangsunwuji" : 1,
    __DIR__ "npc/wuji": 1,
      __DIR__ "npc/xiucai4": 1,
      "/d/quest/dajiu/jiangziya": 1,
    ]));


  //        set("outdoors", "changan");
  set("no_fight", 1);
  set("no_magic", 1);
  set("no_clean_up", 0);
  setup();
  //       replace_program(ROOM);
}

void init() {
  add_action("do_none", "cast");
  add_action("do_none", "kill");
  add_action("do_none", "ji");
  add_action("do_none", "bian");
  add_action("do_none", "exert");
}

int do_none() {
  object me = this_player();
  tell_object(me, HIR "无心大人说了：禁止搞破坏!\n" NOR);
  return 1;
}
