//changan city

inherit ROOM;
#include <ansi.h>
void create() {
  set("short", "搏饼屋外");
  set("long", @LONG

搏饼屋外，你随便选择一个方向就可以进入搏饼室了。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "west": "/d/city/guozi",
      "east": "/d/city/leitai/bobing3",
      "north": "/d/city/leitai/bobing2",
      "south": "/d/city/leitai/bobing1",
    ]));

  set("objects",
    ([  //sizeof() == 1
    //                __DIR__"npc/libai" : 1,
    ]));


  set("outdoors", "changan");
  set("no_clean_up", 0);
  setup();
}
