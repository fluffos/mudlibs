//changan city

inherit ROOM;
#include <ansi.h>
void create ()
{
        set ("short","药铺内厅" );
        set ("long", @LONG

这是长安城一家老字号的药铺的内厅．药铺杨掌柜的亲属
居住在这．
LONG);

        set("exits",
       ([ //sizeof() == 4
        "west" : __DIR__"yaofang", 
        ]));

        set("objects", 
        ([ //sizeof() == 1
               "/d/city/npc/lala" : 1,
        ]));


        set("no_fight", 1);
        set("no_magic", 1);
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}
