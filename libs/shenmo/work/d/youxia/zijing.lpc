// created 17-06-2001 海上飘(piao)

inherit ROOM;
void create()
{
 set("short", "紫禁之巅");
        set("long", @LONG
	从这儿望去就是赫赫有名的紫禁城，历来的高手都以能在这儿比武，
	为最高荣誉，当年号称“剑圣”的叶孤城和号称“剑神”的西门吹雪
	在此一战，惊天地，泣鬼神！
	      月圆之夜，紫荆之巅，一剑袭来，天外飞仙
LONG
        );
        set("exits", ([ /* sizeof() == 4 */


]));
        set("objects", ([
                __DIR__"npc/ximenchuixue" :1,
                        ]) );

        set("coor/x",180);
	set("coor/y",-400);
	set("coor/z",30);
	setup();
        replace_program(ROOM);
}
void init()
{
        add_action("do_none","cast");
        add_action("do_none","ji");
}

int do_none()
{
object me=this_player();
message_vision("这儿是紫禁之巅，岂能你乱来",me);
return 1;
}
