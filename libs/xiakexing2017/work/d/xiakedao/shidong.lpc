// Date: YZC 96/01/19

inherit ROOM;

void create()
{
	set("short", "石洞");
	set("long", "这里是侠客岛石洞，墙壁（wall）上原本刻着绝世武功\n的秘籍图解，但现在已被龙岛主抹去了。\n");

	
    set("item_desc", ([
        "wall" : "墙壁早已四分五裂，没什么好看的了。\n",
        	    ]));
    set("exits", ([
		"out" : __DIR__"qianting",
	]));
	set("objects",([
	  "/d/shenlong/npc/dushe": 1,
	  	]));
	setup();
	replace_program(ROOM);
}