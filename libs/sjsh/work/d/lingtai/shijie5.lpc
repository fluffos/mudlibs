// changan lingtai
// shijie5.c

inherit ROOM;

void create()
{
  set ("short", "石阶);
  set ("long", @LONG

经过大殿，走入这里，风景仿如仙境，与山前的石路简直是两个
截然不同的感觉。抬头忘去，一座高耸如云的峭壁顶天而立。
LONG);
set("exits", ([ /* sizeof() == 4 */
"northup": __DIR__"shijie6",
"south": __DIR__"room1",
]));
        set("no_clean_up", 0);
        set("outdoors", 1);
        setup();
}

