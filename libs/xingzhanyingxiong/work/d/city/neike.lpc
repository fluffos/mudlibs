inherit ROOM;

void create()
{
        set("short", "内科");
        set("long",@LONG
这是治疗内伤的地方，几个大夫站在仪器前正在研究一张病人的
X光照片，时不时的私语几句，一个护士坐在门前的椅子上翻看
着一本厚厚的资料。
LONG);
        set("exits", ([
                "east" : __DIR__"hospital",
        ]));
        set("objects", ([  
          ]));
        setup();
}
