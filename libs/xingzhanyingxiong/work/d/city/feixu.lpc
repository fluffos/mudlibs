// feixu.c 

#include <room.h>
inherit ROOM;

void create() {
  set("short", "废墟");
  set("long", @LONG
这里是个废旧的仓库，尽是一些破破烂烂的东西扔在地上。
你总觉得这里不太对劲，但是具体又说不上来。东侧有个
破烂的石门(men)。
LONG);
  set("item_desc", ([
    "men": "这扇门似乎通向一间密室。\n",
  ]));
  set("exits", ([
    "east": __DIR__ "dong",
    "up": __DIR__ "shuidao3",
  ]));
  set("objects", ([
    __DIR__ "npc/laoshu": 1,
  ]));
  //      set("no_clean_up", 0);
  setup();
}
