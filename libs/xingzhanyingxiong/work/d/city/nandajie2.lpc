// Room: /city/nandajie2.c
// zurn 1995/12/23 

inherit ROOM;
void create()
{
        set("short", "皇后大道");
        set("long", @LONG 
这就是中心城的主干道了，街道两旁都是些十分重要的机构
从这里一直往南就是中心城政府所在了。街上的人并不多
你不禁收起了对中心城的轻视之心。东面是中央银行。西面是
一座豪华的官邸。
LONG);
        set("objects", ([
        ]));
        set("exits", ([
                "south" : __DIR__"nandajie3",
                "north" : __DIR__"nandajie1",
//       "east"  : __DIR__"bank",
//      "west"  : __DIR__"guandi",
        ]));
      setup();
}
