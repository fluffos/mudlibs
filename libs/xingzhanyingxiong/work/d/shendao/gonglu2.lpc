#include <room.h>

inherit ROOM;

void create()
{
	set("short", "公路");
	set("long", @LONG
过了交叉路口,这里是进入圣岛的唯一一条公路了,路上的车辆仍是很多
但是多了几位运用轻身术在离地十几米高的地方,缓缓飘行的白袍人物.
LONG );
	set("exits", ([
		"east" : __DIR__"gonglu3",
		"south"  : __DIR__"gonglu1",
	]));
        set("objects", ([
//                "/d/shendian/npc/zhoubingung" : 1,
        ]));
	set("outdoors", "shendao");
	setup();
}
