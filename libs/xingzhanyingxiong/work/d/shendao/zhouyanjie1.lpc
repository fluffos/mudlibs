#include <room.h>

inherit ROOM;

void create()
{
	set("short", "中央街");
	set("long", @LONG
这里靠近中央广场的,所以也十分繁荣,街的西边是圣岛是大的一栋
宾馆,一些漂亮的小骄车出出进进,站在门口的行宾小姐笑得分外的甜.
LONG );
	set("exits", ([
		"north" : __DIR__"guangchang",
		"south"  : __DIR__"zhouyanjie",
		"west"  : __DIR__"huadu",
	]));
        set("objects", ([
//                "/d/shendian/npc/zhoubingung" : 1,
        ]));
	set("outdoors", "shendao");
	setup();
}
