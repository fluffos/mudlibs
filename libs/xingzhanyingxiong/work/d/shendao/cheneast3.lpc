#include <room.h>

inherit ROOM;

void create()
{
	set("short", "城东路");
	set("long", @LONG
这里已经快接近圣殿了,一些圣殿武士分散各处守门着.几
个不名所谓的人,不时向那些身着白袍的武士投去慕的目光.
LONG );
	set("exits", ([
		"west"  : __DIR__"cheneast2",
		"east"  : __DIR__"cheneast4",
	]));
        set("objects", ([
//                "/d/shendian/npc/zhoubingung" : 1,
        ]));
	set("outdoors", "shendao");
	setup();
}
