#include <room.h>

inherit ROOM;

void create()
{
	set("short", "交叉路");
	set("long", @LONG
这里是进圣岛的第一个交叉路口，一些车辆都选择这里为临时停放
处，在向前走，就能看到圣岛的中心，往西的方向是圣岛有名的风景区
“紫艳”高山。
LONG );
	set("exits", ([
		"north" : __DIR__"gonglu1",
		"south"  : __DIR__"gonglu",
		"west"  : __DIR__"gaoshan",
	]));
        set("objects", ([
//                "/d/shendian/npc/zhoubingung" : 1,
        ]));
//	set("outdoors", "shendao");
	setup();
}
