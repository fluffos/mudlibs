inherit ROOM;
void create() {
  set("short", "地底");
  set("long", @LONG 
这里就是凤凰星底的地下岩洞。只见边上到处是
嶙嶙巨石，还不时有怪叫声从地下传出。
LONG);
  set("objects", ([
    "/d/miti/budui/npc/bei": 1,
  ]));
  setup();
}
