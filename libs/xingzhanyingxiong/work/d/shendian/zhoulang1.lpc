#include <room.h>

inherit ROOM;

void create()
{
	set("short", "走廊");
	set("long", @LONG
这里是由广场到大殿的必经之路，走廊的分为两个叉口，
北就能直接到大殿，向东方走就能看到圣岛招待贵宾的场所
——笃庞楼，听说笃庞楼中只有有一定威望的人才能去住哦。
LONG );
	set("exits", ([
		"northwest" : __DIR__"guangchang",
"east" : __DIR__"zhoulang2",
"north" : __DIR__"dadiang",

	]));
        set("objects", ([
                "/d/shendian/npc/lishenming" : 1,
        ]));
	set("outdoors", "shendian");
	setup();
}
