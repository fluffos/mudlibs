inherit ROOM;

void create()
{
        set("short", "承恩塔");
        set("long", @LONG
不知不觉中你已经走到了承恩塔的四楼楼,光线比以下两层都要暗,
几乎已经看不见什么东西了.突然间你发现房间内一个白色的身影正在
走来走去.
LONG );
        set("exits", ([
//      "up" : __DIR__"ta3",
       "down" : __DIR__"ta2",
        ]));
        set("objects", ([
               "/d/shendian/npc/bai" : 1,
        ]));
        set("outdoors", "shendian");
        setup();
}

