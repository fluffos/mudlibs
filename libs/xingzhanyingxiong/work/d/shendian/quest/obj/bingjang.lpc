#include <ansi.h>
inherit ITEM;

void create()
{
        set_name(HIR"贝斯威身上的饼干"NOR, ({"binggang"}));
        set("long",
               "这是做任务用的工具。\n");
        set("unit", "个");
        set("weight", 5);

        setup();
}

