#include <ansi.h>
inherit ITEM;

void create()
{
        set_name(HIR"李仁民的香烟盒"NOR, ({"xyh"}));
        set("long",
               "这是做任务用的工具。\n");
        set("unit", "个");
        set("weight", 5);

        setup();
}

