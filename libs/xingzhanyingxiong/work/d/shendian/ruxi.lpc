#include <room.h>

inherit ROOM;

void create()
{
	set("short", "教室");
	set("long", @LONG
这是圣岛武士练内息的场所，室内已经有几名武士盘坐在那
里开始修练内息了，你看到靠里的方向似乎还没人，如似乎
你向那里走了过去。
LONG );
	set("exits", ([
		"north" : __DIR__"lianwuchang",
      "east" : __DIR__"lianwu",
  "northwest" : __DIR__"xiulian",
 "west" : __DIR__"npc/wuchang2",
	]));
        set("objects", ([
              "/d/shendian/npc/wushi" : 5,
        ]));
	set("outdoors", "shendian");
	setup();
}
