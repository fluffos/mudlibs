inherit ROOM;
void create()
{
        set("short", "山谷上");
        set("long", @LONG
你站在山谷上面望下看去，云雾茫茫什么都看不清。不时
听到几声怪啸声，令你毛骨悚然。山壁上好象有个洞。
LONG );
        set("exits", ([ /* sizeof() == 2 */
                "north" : __DIR__"xiaolu2",
                "enter"     : __DIR__"hole",
        ]));
        set("no_clean_up", 0);
        set("outdoors", "huaang" );
        setup();
        replace_program(ROOM);
}
 
void init()
{
        object me = this_player();
        return;
}

