#include <ansi.h>

inherit ROOM;

void create()
{
    set("short", "石阶");
    set("long", @LONG
这是一条一望看不到尽头的汉白玉石阶，这也是去皇宫的唯一
的路，石阶两旁站满了杀气腾腾的皇族勇士。
LONG );

    set("exits", ([
        "south" : __DIR__"yanwuchang",
        "north" : __DIR__"huangong",
        "east" : __DIR__"xiaolu",
    ]));
    set("outdoors", "shenlong");
 set("objects", ([   "/kungfu/class/huang/yongshi": 2,        ]));
    setup();
    replace_program(ROOM); 
}


