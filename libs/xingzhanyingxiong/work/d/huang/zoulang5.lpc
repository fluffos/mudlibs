#include <ansi.h>

inherit ROOM;

void create()
{
    set("short", "走廊");
    set("long", @LONG
这是一条走廊,两边都是汉白玉柱子, 看起来十分壮观,往东
就是皇族第子休息的地方了。
LONG );

    set("exits", ([
        "north" : __DIR__"zoulang2",
        "east" : __DIR__"xiuxishi",

    ]));
   
 set("objects", ([   "/kungfu/class/huang/weishi": 2,        ]));
    set("outdoors", "huang");
    setup();
    replace_program(ROOM); 
}


