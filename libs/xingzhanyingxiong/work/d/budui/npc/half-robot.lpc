inherit NPC;

void create() {
  set_name("完成了一半的机器人", ({ "robot", "robot" }));
  set("gender", "男性");
  set("age", 10);
  set("long",
    "这是个做了一半的机器人，他现在还不能使用。\n");
  set("combat_exp", 0);
  set("attitude", "friendly");

  setup();
}
