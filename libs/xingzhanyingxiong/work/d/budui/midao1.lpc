inherit ROOM;

#include <ansi.h>


void create()
{
	set("short", HIY"监狱密道"NOR);
	set("long", @LONG
这里就是特种监狱的密道了。你也没什么时间来欣赏这里的环境了，快逃吧！！！！
LONG
	);
        set("exits",(["north" : __DIR__"midao2",]));

	set("outdoors", "shaolin");
	setup();
	replace_program(ROOM);
}

