inherit ROOM;

void create()
{
	set("short", "人名路");
	set("long", @LONG
这是一条十分宽阔的大街，上面行人稀少，路的两旁只有一些机器人在走来走去。
不过这里的附近有特种部队的警署，和特种医疗院。
LONG
	);

	
	
	set("exits", ([
		"west" : __DIR__"renminglu2",
                "south"  : __DIR__"jingshu",
	        "north"  : __DIR__"yiyuan",
                "east" : __DIR__"bdguangchang2",]));
	
	setup();
	replace_program(ROOM);
}