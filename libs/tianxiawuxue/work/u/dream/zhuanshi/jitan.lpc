inherit ROOM; 
#include <ansi.h>
int num_of_killer;
string call_killer();

void create() 
{ 
        set("short", RED"祭 坛"NOR);
        set("long",HIB @LONG

四处弥漫着浓厚的烟雾，你什么也看不清楚，空中浮动着若
隐若现的鬼火，周围似乎有一双双眼睛盯着你看，你走了几
步。突然发现在不远处有一祭坛，祭坛上有一老者在做法超
度亡灵。你心中一喜：到了，终于到了！
LONG
  NOR );
       set("objects", ([
                __DIR__"jiang"    : 1,
        ]));

       set("exits", ([ /* sizeof() == 2 */
         "east" : "/u/dream/zhuanshi/gui3",
]));
       
       setup();
        reset();
}

