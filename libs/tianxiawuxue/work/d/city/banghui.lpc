// Room: /city/dongxiang.c
// YZC 1995/12/04 
#include <room.h>
inherit ROOM;
void create() {
  set("short", "帮会申请处");
  set("long", @LONG
	这里就是天下无雪的帮会申请处,如果你有能力成立
帮会,就到这里来申请吧。
LONG
  );
  set("exits", ([
    "up": __DIR__ "wumiao",
  ]));
  set("objects", ([
    __DIR__ "npc/bangpai": 1,
  ]));
  setup();
  replace_program(ROOM);
}
