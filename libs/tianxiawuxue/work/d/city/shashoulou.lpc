// Room: /d/city/shashoulou
inherit ROOM;

void create ()
{
  set ("short", "杀手楼");
  set ("long", @LONG
这里就是杀手楼的总部。正面挂着一块匾。
威风凛凛的镶着“杀 手 楼”几个硕大的金字
金牌匾的下方挂着两幅字，“阎王要你三更死”
"谁敢留人至五更”。
LONG);

  set("no_fight", 1);
  set("exits", ([ /* sizeof() == 1 */
"west" : "/d/city/kedian",
]));
  set("no_magic", 1);
  set("objects", ([ /* sizeof() == 1 */
  "/quest/fyue" : 1,
]));

  setup();
  replace_program(ROOM);
}
