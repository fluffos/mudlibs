// jiebai.c
//@ txwx 
//by hhm 05/06/10 

inherit ROOM;

void create ()
{
        set ("short", "城隍庙");
        set ("long", @LONG

这里始终是香火缭绕，台上供奉着一尊威严的神像。然来
当地居民特地将保护本地区的门派掌门人的神像供奉在这
里,以祈求百姓的安居乐业。
LONG);

         set("no_fight",1);
        set("exits", 
        ([ //sizeof() == 4
		"north" :"/d/city/dongmen",
        ]));

        set("objects", 
        ([ //sizeof() == 1
            
                __DIR__"npc/guan" : 1,
        ]));

	set("no_clean_up", 0);
        setup();
	replace_program(ROOM);
}



