#include <room.h>
inherit ROOM;
void create()
{
         set("short","临江亭");
         set ("long",@LONG        
这是个很小的亭子，但是能够看到几乎全部的景色，远处有就是汉水。
江中几条大船在碧绿的江面上往来穿梭，江中大雾弥漫.
LONG
);
         set("exits",([
             "east" : __DIR__"hanshui1",
]));
         set("objects",([
             "/d/city/npc/ladeng" : 1,
]));
         setup();
         replace_program(ROOM);
}
