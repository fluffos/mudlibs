// Room: /d/city2/ximenwai.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create()
{
	set("short", "西门外");
	set("long", @LONG
北京西郊，路上行人匆匆,而名满江湖的六扇门就在这里附近。
LONG
);
	set("exits", ([ /* sizeof() == 2 */
  "west" : "/d/gaochang/room1",
   "north" : "/d/lsm/road",
  "east" : __DIR__"ximen",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
