#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", HIG "【走廊】" NOR);
        set("long", NOR @LONG
你来到这里，发现这里这里就是人间仙境。眼前这一条走廊直接通向神候府大厅。
LONG NOR );
        set("exits", ([                
                "north" : __DIR__"dating",
                "south" : __DIR__"road",             
                
]));
        set("sleep_room", 1);
        setup();
}