// Room: /lsm/lengxue
// @TXWX hhm 2005/4/17
#include <room.h>
inherit ROOM;
void create()
{
	set("short", HIY "【铁手居】" NOR);
	set("long", @LONG
	这里是六扇门"四大名捕"的老三铁手的房间,房间陈设简单.除
了一张床，几只箱子，墙上挂着一幅画,画的是一个年轻的女子,不过时间
过于久远,渐渐有点模糊不清了。
LONG
	);
	set("no_sleep_room",1);     
        set("no_get", 1);

	set("exits", ([
		 "west" : __DIR__"dating",
	]));
	set("objects", ([
		__DIR__"npc/tieshou": 1,
	]));
	setup();
	replace_program(ROOM);
}
