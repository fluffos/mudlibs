// Room: /lsm/lengxue
// @TXWX hhm 2005/4/17
#include <room.h>
inherit ROOM;
void create() {
  set("short", HIY "【追命居】" NOR);
  set("long", @LONG
	这里是六扇门"四大名捕"的老二追命的房间,房间陈设简单.
除了一张床，一张椅子,就什么也没有了.谁也不会相信,这里就是名
捕追命的卧室.
LONG
  );
  set("no_sleep_room", 1);
  set("no_get", 1);

  set("exits", ([
    "out": __DIR__ "dating",
  ]));
  set("objects", ([
    __DIR__ "npc/zhuiming": 1,
  ]));
  setup();
}
