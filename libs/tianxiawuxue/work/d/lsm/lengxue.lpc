// Room: /lsm/lengxue
// @TXWX hhm 2005/4/17
#include <room.h>
inherit ROOM;
void create() {
  set("short", HIY "【冷血居】" NOR);
  set("long", @LONG
	这里是六扇门"四大名捕"的老四冷血的房间,房间陈设简单.除了一张床，
几只箱子，墙上挂着一柄长剑.一看主人就是一个剑术高手。
LONG
  );
  set("no_sleep_room", 1);
  set("no_get", 1);

  set("exits", ([
    "east": __DIR__ "dating",
  ]));
  set("objects", ([
    __DIR__ "npc/lengxue": 1,
  ]));
  setup();
  replace_program(ROOM);
}
