// Room: /lsm/lengxue
// @TXWX hhm 2005/4/17
#include <room.h>
inherit ROOM;
void create() {
  set("short", HIY "【无情居】" NOR);
  set("long", @LONG
	这里是六扇门"四大名捕"的老大无情的房间,房间陈设简单.
除了一张床，几只箱子，墙角有张化妆台，只有靠墙一张红木书桌还
显得略有气派。一个年轻人端座在轮椅上.
LONG
  );
  set("no_sleep_room", 1);
  set("no_get", 1);

  set("exits", ([
    "down": __DIR__ "dating",
  ]));
  set("objects", ([
    __DIR__ "npc/wuqing": 1,
  ]));
  setup();
}
