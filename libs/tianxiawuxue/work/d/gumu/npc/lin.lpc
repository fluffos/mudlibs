// ----/d/gumu/npc/lin.c ----
// lin.c 林朝英

#include <ansi.h>

inherit NPC;
inherit F_MASTER;

string ask_yunv();

void create()
{
        set_name("林朝英", ({"lin chaoying", "lin"}));
        set("gender", "女性");
        set("age", 42);
        set("long",
                "她就是古墓派的开山祖师林朝英，虽然已经是四十许人，望之却\n"
                "还如同三十出头。当年她与全真教主王重阳本是一对痴心爱侣，\n"
                "只可惜有缘无份，只得独自在这古墓上幽居。\n");
    set("attitude", "peaceful");

        set("per", 130);
        set("str", 137);
        set("int", 142);
        set("con", 145);
        set("dex", 142);

        set("chat_chance_combat", 6);
        set("chat_msg_combat", ({
                (: exert_function, "recover" :),
		  (: exert_function, "regenerate" :),
        }) );
        set("chat_chance", 1);

        set("qi", 6000);
        set("max_qi", 6000);
        set("jing", 2400);
        set("max_jing", 2400);
        set("neili", 90000);
        set("max_neili", 90000);
        set("jiali", 1000);

        set("combat_exp", 220000000);

        set_skill("force", 1200);
	set_skill("yinsuo-jinling", 1200);
	set_skill("yunu-xinjing", 1200);
	set_skill("throwing", 1200);
	set_skill("qufeng", 1200);
	set_skill("strike", 1200);
	set_skill("quanzhen-jianfa", 1200);
	set_skill("hubo", 1000);
//	set_skill("", 1200);
//	set_skill("", 1200);
        set_skill("yunv-xinfa", 1200);    //玉女心法
        set_skill("sword", 1200);
        set_skill("yunv-jian", 1000);     //玉女剑
        set_skill("dodge", 1200);
        set_skill("yunu-shenfa", 1200);   //玉女身法
        set_skill("parry", 1200);
        set_skill("unarmed",1200);
        set_skill("meinu-quanfa", 1200);    //美女拳法
        set_skill("literate",1200);
        set_skill("taoism",500);

        map_skill("force", "yunv-xinfa");
        map_skill("sword", "yunv-jian");
        map_skill("dodge", "yunu-shenfa");
        map_skill("parry", "meinu-quanfa");
        map_skill("unarmed", "meinv-quan");

        create_family("古墓派", 1, "开山祖师");

        set("book_count",1);
        set("inquiry", ([
                "王重阳" :  "大胆后辈小子，也敢直呼重阳祖师之名？\n",
                "重阳祖师" : "重阳是全真教的掌教，自号“活死人”。\n",
                "古墓派" :  "是啊，这里就是我一手创下的古墓派\n",
                "玉女心经" : (: ask_yunv :),
        ]) );

        setup();

        carry_object("/clone/weapon/changjian")->wield();
        carry_object("/d/gumu/obj/qun1")->wear();

}

void attempt_apprentice(object ob)
{
        if (ob->query_int() < 30) {
                command("say 我古墓派的功夫最讲一个悟字，你的资质不够。");
                return;
        }
        if ((int)ob->query_skill("yunv-xinfa",1) < 550 ) {
                command("say 你的玉女心法火候不足,难以领略更高深的武功。");
                return;
        }
        command("say 好吧，看你也是性情中人，我就收下你这个徒弟了。");
        command("recruit " + ob->query("id"));
        ob->set("title",HIR "古墓派林朝英闭门弟子"NOR);
        ob->set("class","sworder");

}

string ask_yunv()
{
        mapping fam;
        object ob;
        int lvl_yunv;

        if (!(fam = this_player()->query("family"))
            || fam["family_name"] != "古墓派")
                return RANK_D->query_respect(this_player()) +
                "与本派毫无瓜葛，何以问起本派的心经？";
        if (!(lvl_yunv = this_player()->query_skill("yunv-xinfa",1))
            || lvl_yunv < 55)
                return RANK_D->query_respect(this_player()) +
                "的玉女心法火候不足，我不能将玉女心经交给你";
        if (query("book_count") < 1)
                return "你来晚了，本派的玉女心经已经被人取走了。";
        add("book_count", -1);
        ob = new(__DIR__"obj/yunvjing2");
        ob->move(this_player());
        return "好吧，这本「玉女心经」你拿回去好好研读。";
}