// paper.c
inherit ITEM;
void create() {
  set_name("草纸", ({ "paper" }));
  set_weight(50);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "张");
    set("long", "一张草纸。\n");
  }
}
