// Room: /d/huanggon/d/jiao3.c

inherit ROOM;

void create()
{
	set("short", "城角");
	set("long", @LONG
这里是内城的一角,头上的天空只能见方方的一角.上面有座漂亮的
角楼.
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
  "south" : __DIR__"dao7",
  "up" : __DIR__"lou4",
  "east" : __DIR__"dao6",
]));
	set("no_clean_up", 0);

	set("objects", ([
	"/d/huanggon/npc/yulin2"  : 2,
	]));

	setup();
	replace_program(ROOM);
}
