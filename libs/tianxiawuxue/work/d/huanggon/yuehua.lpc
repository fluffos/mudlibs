// Room: /u/lei/d/yuehua.c

inherit ROOM;

void create()
{
	set("short", "月华门");
	set("long", @LONG
月华门北边是批事处, 南边是奏事房,是皇帝经常出行坐轿的地方.
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"qianqingmen",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
