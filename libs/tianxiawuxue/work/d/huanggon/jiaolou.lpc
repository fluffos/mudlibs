// Room: /d/huanggon/d/jiaolou.c

inherit ROOM;

void create() {
  set("short", "角楼");
  set("long", @LONG
这座六角亭阁式的小楼,造型优美,飞檐峭墙,煞是好看.其实,这是
用作守卫了望的.它紧扼内城一角,能控制四下,内城四角各有一座这样
的角楼,若有风吹草动,都可一清二楚.
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "down": __DIR__ "chengjiao",
  ]));
  set("no_clean_up", 0);

  setup();
}
