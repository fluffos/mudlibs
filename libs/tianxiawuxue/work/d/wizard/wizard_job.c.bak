// Room: /d/wizard/wizard_room.c
inherit ROOM;
void create()
{
    set("short","天下无雪新闻发布室");
    set("long", @LONG
这里就是「天下无雪 」巫师的新闻发布室，四周乱
七八糟地摆着几张凳子。中间桌子的案台上压着一本巫师工作计
划表。
***********************************************************
****************发布新闻命令(broadcast)**********************
*****************删除新闻命令(shanchu)***********************
************************************************************

LONG );
    set("exits", ([ /* sizeof() == 1 */
    "down" : __DIR__"wizard_room" ]));
    set("valid_startroom", 1);
	set("objects", ([
//    "/clone/misc/player":1,
	]));
    set("no_fight", "1");
    set("no_clean_up", 0);
    setup();
    replace_program(ROOM);
}
