#include "/doc/help.h"
#include "ansi.h"
inherit F_CLEAN_UP;
int main(object me, string arg) {
  message("write", "test", this_player());
  message("write", "test", me);

  return 1;
}
int help() {
  write(@TEXT
指令格式：full
这个指令让你加满精气神。
TEXT
  );
  return 1;
}
