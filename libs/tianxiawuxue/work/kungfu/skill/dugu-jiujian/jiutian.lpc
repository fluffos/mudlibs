#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
inherit F_SSERVER;
 
int perform(object me, object target)
{
    object weapon, ob;
    string msg;
  int i,skill;
    skill=me->query_skill("dugu-jiujian",1);
 
    if( !me->is_fighting() )
        return notify_fail("〖剑啸九天〗只能对战斗中的对手使用。\n");
    if (me->query_temp("perform")) return notify_fail("你上一招未使完！\n");
    if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "sword")
    if (!objectp(weapon = me->query_temp("secondary_weapon"))
    || (string)weapon->query("skill_type") != "sword")
        return notify_fail("你使用的武器不对。\n");
    else me->set_temp("secondly_weapon",1);
    if( (int)me->query("neili") < 20000 )
        return notify_fail("你的真气不够！\n");
    if( (int)me->query_skill("dugu-jiujian",1) < 280 ||
        me->query_skill_mapped("sword") != "dugu-jiujian")
        return notify_fail("你的独孤九剑还不到家，无法使用〖剑啸九天〗！\n");
msg = HIY "$N使出独孤九剑之秘"+HIR+"〖剑啸九天〗"+HIY+"幻化出无数剑影！，\n 
$N手中的剑直冲云宵，风云为之变色。\n"+NOR ;
    message_vision(msg, me, target);
    me->clean_up_enemy();
    ob = me->select_opponent();
    me->add("neili", -150);
    me->set_temp("perform",1);
    me->set_temp("perform_dugu",1);
    me->add_temp("apply/attack", skill);
    for(i = 0; i < 9; i++)
        if (me->is_fighting(ob) && ob->is_fighting(me) && ob->query("eff_qi")>0){
              me->set_temp("action_msg","紧接着");
                if (!weapon->query("equipped")) break;
              COMBAT_D->do_attack(me, ob,weapon, 0);
            }else break;
    me->delete_temp("perform_dugu");
    me->delete_temp("secondly_weapon");
     me->add_temp("apply/attack", -skill);
    me->start_call_out( (: call_other, __FILE__, "del_perform", me :), 3);;
    return 1;
}

void del_perform(object me)
{
   if (!me) return;
   me->delete_temp("perform");
}


