// a skeleton for user rooms
inherit ROOM;
void create()
{
set("short", "剑门兵器室");
set("long", @LONG
这里就是专门给剑门弟子用来打造神兵利器的地方。里面夹杂着乒乒乓乓的打造声。

LONG
);
        set("exits", ([
"south" : "/p/residence/rubylucky1118197910.c",
]));
set("objects", ([
	"/clone/weapon/changjian":1,
       ]) );
	set("owner", "rubylucky");
	set("class", "bigwiz7001");
	set("no_fight",1);

setup();
}
