// Room: /p/residence/papa1129024077.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create()
{
	set("short", "流逝时空");
	set("long", @LONG
这里是啪啪的休息室，里面什么都没有只有一张床，一个枕头。啪啪的老婆经常买好吃的给啪啪吃。

LONG
);
	set("sleep_room", "1");
	set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"papa1129006367.lpc",
]));
	set("no_fight", 1);
	set("owner", "papa");
	set("objects", ([ ]));
	set("class", "蜀山派");

	setup();
	replace_program(ROOM);
}
