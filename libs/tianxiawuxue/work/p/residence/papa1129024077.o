// a skeleton for user rooms
inherit ROOM;
void create()
{
set("short", "流逝时空");
set("long", @LONG
这里是啪啪的休息室，里面什么都没有只有一张床，一个枕头。啪啪的老婆经常买好吃的给啪啪吃。

LONG
);
        set("exits", ([
"east" : "/p/residence/papa1129006367.c",
]));
set("objects", ([
       ]) );
	set("owner", "papa");
	set("class", "蜀山派");
	set("no_fight",1);
	set("sleep_room", "1");

setup();
}
