// a skeleton for user rooms
inherit ROOM;
void create()
{
set("short", "机器人休息室");
set("long", @LONG
这里是机器人休息的地方，挂机挂长时间了，来这里休息一下也不错啊
。

LONG
);
        set("exits", ([
"west" : "/p/residence/lwt1128000462.c",
]));
set("objects", ([
       ]) );
	set("owner", "lwt");
	set("class", "逍遥派");
	set("no_fight",1);
	set("sleep_room", "1");

setup();
}
