// a skeleton for user rooms
inherit ROOM;
void create()
{
set("short", "剑门休息室");
set("long", @LONG
这里就是专门给剑门弟子闭关所用的休息室。凡是来打扰的人绝对不得好死。 杀白你一百次。 嘿嘿。

LONG
);
        set("exits", ([
"north" : "/p/residence/rubylucky1118198449",
"east" : "/p/residence/rubylucky1118197584.lpc",
]));
set("objects", ([
       ]) );
	set("owner", "rubylucky");
	set("class", "bigwiz7001");
	set("no_fight",1);
	set("sleep_room", "1");

setup();
}
