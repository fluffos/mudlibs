// Room: /p/residence/papa1129006367.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create()
{
	set("short", "流逝总舵");
	set("long", @LONG
这里是啪啪的专属房间，谁喜欢进来都可以
这里是啪啪的专属房间，谁喜欢进来都可以
这里是啪啪的专属房间，谁喜欢进来都可以
这里是啪啪的专属房间，谁喜欢进来都可以
这里是啪啪的专属房间，谁喜欢进来都可以

LONG
);
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"papa1129024077",
  "east" : "/p/guangchang.lpc",
]));
	set("valid_startroom", 1);
	set("no_fight", 1);
	set("owner", "papa");
	set("class", "逍遥派");
	set("objects", ([ ]));

	setup();
	replace_program(ROOM);
}
