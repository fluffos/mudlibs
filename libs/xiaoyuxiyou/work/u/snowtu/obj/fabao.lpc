inherit "/obj/fabao.lpc";
#include <ansi.h>

void create() {
  set_name("青枚凰羽", ({ "qing mei yu", "yu", "mao", "fabao" }));
  set("unit", "根");
  set("no_put", 1);
  set("no_get", 1);
  set("no_give", 1);
  set("armor_prop", ([
    "max_kee": 200,
    "max_sen": 200,
    "max_force": 50,
    "max_mana": 50,
  ]));
  set("fabao", ([
    "max_attack_qi": 8,
    "max_attack_shen": 7,
    "max_defense_qi": 8,
    "max_defense_shen": 7,
    "combat_damage": 100,
    "combat_def": 80,
    "spells_damage": 100,
    "spells_def": 80,
  ]));
  set("ji_time", 6);
  set("fabao_type", ({ "attack", "def", "cure" }));
  setup();
}
