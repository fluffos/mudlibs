// JJGOD@VRHF
#include <ansi.h>
inherit ROOM;
void create() {
  set("short", "高三阿鼻狱");
  set("long", @LONG
又名高三穿鼻狱，来到这里的学生下世绝无考上大学的可能，这里
的学生都象牛一样，鼻孔被穿，无数语文，数学老师牵之来回行走，不时
以铜锤击身，钢鞭敲打，使其时刻警醒。再下面便是最后一层秤杆狱。
LONG
  );
  set("exits", ([
    "up": __DIR__ "xuechi",
    "down": __DIR__ "banggan",
  ]));
  set("coor/x", -1020);
  set("coor/y", -70);
  set("coor/z", -270);
  setup();
}
