//JJGOD@VRHF
#include <ansi.h>
inherit ROOM;
void create() {
  set("short", "冥府政治司");
  set("long", @LONG
这里就是高三冥府的政治司，俨然一座检察院的样式，
屋里当中坐着的是政治检查委员会委员长关羽同志。
LONG
  );
  set("exits", ([
    "northwest": __DIR__ "naihe2",
  ]));
  set("objects", ([
    __DIR__ "npc/guan": 1,
  ]));
  set("coor/x", -1010);
  set("coor/y", -30);
  set("coor/z", -100);
  setup();
  replace_program(ROOM);
}
