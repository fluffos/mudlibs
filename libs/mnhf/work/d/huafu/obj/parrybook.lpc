inherit ITEM;

void create() {
  set_name("初中招架读本", ({ "parrybooka", "book" }));
  set_weight(600);
  set("unit", "本");
  set("long", "这是一本介绍招架之术的书。\n");
  set("value", 20);
  set("skill", ([
    "name": "parry",
    "exp_required": 0,
    "sen_cost": 10,
    "difficulty": 20,
    "max_skill": 100
  ]));
}
