inherit ITEM;

void create() {
  set_name("高中刀法读本", ({ "bladebookb", "book" }));
  set_weight(600);
  set("unit", "本");
  set("long", "这是一本介绍刀法的书。\n");
  set("value", 2000);
  set("skill", ([
    "name": "blade",
    "exp_required": 200000,
    "sen_cost": 35,
    "difficulty": 20,
    "max_skill": 100
  ]));
}
