inherit ITEM;

void create() {
  set_name("初中闪躲读本", ({ "dodgebooka", "book" }));
  set_weight(600);
  set("unit", "本");
  set("long", "这是一本介绍闪躲之术的书。\n");
  set("value", 20);
  set("skill", ([
    "name": "dodge",
    "exp_required": 0,
    "sen_cost": 10,
    "difficulty": 20,
    "max_skill": 100
  ]));
}
