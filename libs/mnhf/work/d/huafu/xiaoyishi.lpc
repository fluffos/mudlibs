inherit ROOM;
void create() {
  set("short", "校医室");
  set("long", @LONG
校医室是同学们打针吃药体检的地方，始终关闭的门再加上茶色玻璃的窗和厚厚
的窗帘，使得校医室显得阴暗可怕，一般同学们若不是身体有病或是脑子有病，谁都
不会去这个地方。屋子里挂着一块不知谁送的锦旗： [32m
        		    妙手回春
[37m
LONG
  );
  set("valid_startroom", 1);
  set("exits", ([
    "north": __DIR__ "elang",
  ]));
  set("objects", ([
    __DIR__ "npc/xiaoyi": 1,

  ]));
  set("coor/x", 20);
  set("coor/y", 10);
  set("coor/z", 0);
  setup();
  replace_program(ROOM);

}
