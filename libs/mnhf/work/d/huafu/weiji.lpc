inherit ROOM;
#include <ansi.h>
#include <room.h>
void create()
{
        set("short", "生化楼");
        set("long", @LONG
这里是华师附中的生化楼，就是供学生做化学和生物实验的地方，各处都是实验
室，生化楼是比较老的楼了，大厅显得阴暗潮湿，给人一种恐怖的感觉，还有各种药
品的气味，令每一个呼吸系统顺畅的同学都觉得无法忍受。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"juqing",
  "south" : __DIR__"playgrnd1",
  "east" : __DIR__"ejieti",
  "west" : __DIR__"wjieti",
  "up" :   __DIR__"weiji2",
]));
	set("valid_startroom",1);
        set("objects", ([
        __DIR__"npc/teacher" : 1,
                        ]) );
	 set("coor/x",0);
	set("coor/y",-20);
	set("coor/z",0);
	setup();
	create_door("north", "生化楼大门", "south", DOOR_CLOSED);
}
