// Room:/d/huafu/eroad2.c

inherit ROOM;
void create()
{
        set("short", "东校道");
        set("long", @LONG
瓷砖铺制的宽阔路面，因为这里四通八达，时时都有不少人在这儿来往穿梭，
这里是学校里行人最多的地方。东边通向21层教工宿舍，西边通向东阶梯。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "east" : __DIR__"21lou",
  "west" : __DIR__"ejieti",
 ]));
	set("valid_startroom",1);
        set("objects", ([
        __DIR__"npc/student" : 1,
			]) );

        set("outdoors", "华师附中");
        set("coor/x",40);
	set("coor/y",-20);
	set("coor/z",0);
	setup();
        replace_program(ROOM);
}

