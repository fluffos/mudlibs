// shadai.c 沙袋

inherit ITEM;

void create()
{
	set_name("沙袋", ({"shadai","dai"}));
	set_weight(random(2000));
	if (clonep())
		set_default_object(__FILE__);
	else {
		set("long", "一个用来跳沙池和绑着跑步的沙袋。\n");
		set("unit", "个");
		set("value", 500);
	}
}