inherit ITEM;
void create() {
  set_name("冥币", ({ "mingbi", "冥币" }));
  set_weight(50);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "串");
    set("long", "一串几可乱真的冥币。\n");
    set("value", 1000);
  }
}
