// football.c 足球

inherit ITEM;

void create() {
  set_name("足球", ({ "football", "ball" }));
  set_weight(300);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一个普通的足球。\n");
    set("unit", "个");
    set("value", 1000);
  }
}
