// basketball.c 篮球

inherit ITEM;

void create() {
  set_name("篮球", ({ "basketball", "ball" }));
  set_weight(300);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一个普通的篮球。\n");
    set("unit", "个");
    set("value", 1000);
  }
}
