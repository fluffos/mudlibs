// 本意是用来建房的，但buildroom还没搞掂

inherit ROOM;
void create()
{
  set("short","教工宿舍群");
  set("long",@LONG
这里就是华师附中的教工宿舍群了，虽然这里的宿舍视挺早以前建的，可是
外墙因为翻修过，倒不显得陈旧。在这里，只要你有钱，也可以建自己的房子。
你不信？LOOK！
LONG
  );
	set("valid_startroom",1);
  set("exits",([
         "north" :__DIR__"21lou",
               ]));
	set("owner","public");
	set("outdoors", "华师附中");
  	set("coor/x",60);
	set("coor/y",-40);
	set("coor/z",0);
	setup();
  replace_program(ROOM);
}

