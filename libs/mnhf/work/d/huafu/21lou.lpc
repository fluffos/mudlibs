// Room:/d/huafu/21lou.c 21层教工宿舍
// 有关电梯谜题
inherit ROOM;
void create()
{
        set("short", "21层教工宿舍");
        set("long", @LONG
这里就是华师附中新建的（如果还能算新建的话）21层教工宿舍，在楼底下放眼望
上去，上边每层都挂满了洗了的衣服、分体空调机、花盆、咸鱼、防盗网等等等等不一
而足，对校容市容简直是极大破坏。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "west" : __DIR__"eroad2",
  "north" : __DIR__"ricerm",
  "south" : __DIR__"jgsushe",
]));
        set("objects", ([
        __DIR__"npc/guanliyuan" : 1,
                        ]) );
	set("valid_startroom",1);
	set("coor/x",60);
	set("coor/y",-20);
	set("coor/z",0);
	setup();
}
