inherit ROOM;
void create()
{
        set("short", "西校道");
        set("long", @LONG
瓷砖铺制的宽阔路面，西边就是高中所在的西教学楼了，因为这里四通八达，时时
都有不少人在这儿来往穿梭，这里是学校里行人最多的地方。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "east" : __DIR__"juqing",
  "west" : __DIR__"wlou1",
 ]));
	set("valid_startroom",1);
        set("outdoors", "华师附中");
        set("coor/x",-20);
	set("coor/y",0);
	set("coor/z",0);
	setup();
        replace_program(ROOM);
}

