// Room: /d/huafu/elou2.c
// 教室，随手放了几个叮当，拿来练功。

#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "东教学楼教室");
	set("long", @LONG
这里是东教学楼的教室，同学们常在这里上课，切磋学业，偌大的教室
摆满了桌椅，讲台前是投影仪，左上方是电视机，东边一排是壁柜。后边还
有一个阳台。
LONG
	);
        set("exits", ([ /* sizeof() == 3 */
  "down" : __DIR__"elou1",
  "southwest" : __DIR__"weiji2",
  "west" : __DIR__"wulilab",
]));
	set("valid_startroom",1);
	set("objects", ([
		__DIR__"npc/dingdang":3 ]) );
	set("coor/x",40);
	set("coor/y",0);
	set("coor/z",20);
	setup();
}
