// Room: /d/wiz/courthouse.c

#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "西教学楼教室");
	set("long", @LONG
这里是西教学楼的教室，同学们常在这里上课，切磋学业，偌大的教室
摆满了桌椅，讲台前是投影仪，右上方是电视机，西边一排是壁柜。后边还
有一个阳台。
LONG
	);
        set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"wlou2",
  "east" : __DIR__"wulilab",
]));
	set("valid_startroom",1);
	set("objects", ([
		__DIR__"npc/dingdang":3 ]) );

	set("coor/x",-40);
	set("coor/y",20);
	set("coor/z",20);
	setup();
}
