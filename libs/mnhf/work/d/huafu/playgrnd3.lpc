inherit ROOM;
void create()
{
        set("short", "后操场");
        set("long", @LONG
这里就是华师附中的大操场了，操场外侧是一圈400米的胶塑跑道，有不少
人在跑道上奔跑，内侧是一个巨大的足球场，不过为了维护场地，常常不让学生
进场踢球，操场两边各有一个沙池，当然是用来跳远或跳高的。操场北角是一些
单杠双杠，也有不少人在练引体向上什么的。操场正前方是一座主席台。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "east" : __DIR__"hgardenm/entry",
  "westup" : __DIR__"zhuxitai",
  "west" : __DIR__"pdindoor",
  "south" : __DIR__"hnroad",
]));
        set("valid_startroom",1);
        set("objects", ([
        __DIR__"npc/runner" : 2,
                        ]) );
                 set("outdoors", "华师附中");
	set("coor/x",20);
	set("coor/y",-60);
	set("coor/z",0);
	setup();
}