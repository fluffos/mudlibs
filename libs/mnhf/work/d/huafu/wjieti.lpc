#include <ansi.h>
void test_me(object me);
inherit ROOM;

void create() {
  set("short", "西阶梯教室");
  set("long", @LONG
这里是华附装修一新的西阶梯教室，教室分两层，可是每层都坐不了多少人，为了
多坐些人，金碧辉煌的阶梯教室显得拥挤不堪，两排之间塞得紧紧的，窄得连特别瘦的
人都走不过去，在楼上那一层，楼梯小得如果失火了生还的决不会超过三个，在上面就
是伸长了脖子拼命往下望也看不到讲话的人的头皮……
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "west": __DIR__ "wroad2",
    "east": __DIR__ "weiji",
  ]));
  set("valid_startroom", 1);
  set("objects", ([
    __DIR__ "npc/teacher": 1 ]));

  set("coor/x", -20);
  set("coor/y", -20);
  set("coor/z", 0);
  setup();
}
