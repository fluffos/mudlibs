inherit ROOM;
void create() {
  set("short", "后门北路");
  set("long", @LONG
这条路一直通向后门，所以每天上学放学后这里的人也不少，路边就可以
看到一排排的教工宿舍群，至于南边，就是后门了。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "hgate",
    "north": __DIR__ "playgrnd3",
  ]));
  set("valid_startroom", 1);
  set("objects", ([
    __DIR__ "npc/student": 1,
  ]));

  set("outdoors", "华师附中");
  set("coor/x", 20);
  set("coor/y", -80);
  set("coor/z", 0);
  setup();
  replace_program(ROOM);
}
