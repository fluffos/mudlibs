inherit ROOM;
void create() {
  set("short", "东校道");
  set("long", @LONG
瓷砖铺制的宽阔路面，这条路从大操场通向后门花园，因为这里四通
八达，时时都有不少人在这儿来往穿梭，所以这里常常是学校里行人最多
的地方。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "playgrnd1",
  ]));
  set("valid_startroom", 1);
  set("objects", ([
    __DIR__ "npc/student": 1,
  ]));

  set("outdoors", "华师附中");
  set("coor/x", 20);
  set("coor/y", 0);
  set("coor/z", 0);
  setup();
}
