inherit BULLETIN_BOARD;

void create() {
  set_name("留言板", ({ "board" }));
  set("location", "/u/tmy/workroom");
  set("board_id", "tmy_b");
  set("long", "想说就说，但别罗嗦!\n");
  setup();
  set("capacity", 3000);
  replace_program(BULLETIN_BOARD);
}
