// THIS IS PLAYER'S OWN SKILL (write by maxim@nju_fengxue)
// zzll
// 赵无暇
// 白驼山派第三代弟子
// 101295747
// 男性
// sword
// 赵家剑法





inherit SKILL;

////////////////////////////////

mapping *action = ({

  // ZHAOSHI :0

  ([
    "action": "l",
    "force": 50,
    "dodge": 20,
    "damage_type": "刺伤",
    "lvl": 0,
    "skill_name": "百步穿杨"
  ]),
  // ZHAOSHI :1

  ([
    "action": "cha",
    "force": 80,
    "dodge": 10,
    "damage_type": "刺伤",
    "lvl": 12,
    "skill_name": "满月星空"
  ]),
  // ZHAOSHI :2

  ([
    "action": "meskills sword zhaojia 赵家剑法 4",
    "force": 100,
    "dodge": 10,
    "damage_type": "刺伤",
    "lvl": 20,
    "skill_name": "一剑冲天"
  ]),
  // ZHAOSHI :3

  ([
    "action": "meskills sword zhaojia 赵家剑法 5",
    "force": 130,
    "dodge": 5,
    "damage_type": "刺伤",
    "lvl": 30,
    "skill_name": "朱雀展翅"
  ]),
  // ZHAOSHI :4

  ([
    "action": "meskills sword zhaojia 赵家剑法 6",
    "force": 150,
    "dodge": -5,
    "damage_type": "刺伤",
    "lvl": 40,
    "skill_name": "单风解甲"
  ]),
  // ZHAOSHI :5

  ([
    "action": "cha",
    "force": 180,
    "dodge": -5,
    "damage_type": "刺伤",
    "lvl": 50,
    "skill_name": "青龙云屏"
  ]),
  // ZHAOSHI :6

  ([
    "action": "meskills sword zhaojia 赵家剑法 8",
    "force": 200,
    "dodge": -10,
    "damage_type": "刺伤",
    "lvl": 60,
    "skill_name": "无相如来"
  ]),
  // ZHAOSHI :7

  ([
    "action": "$N纵身一跃，手中$w一招「飞雪连天」对准$n的$l斜斜刺出一剑",
    "force": 220,
    "dodge": -10,
    "damage_type": "刺伤",
    "lvl": 80,
    "skill_name": "飞雪连天"
  ]),
});
// ZHAOSHI :8
int valid_learn(object me) {
  object weapon; if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "sword") return notify_fail("你使用的武器不对。\n");
  if ((int)me->query("max_neili") < 50)
    return notify_fail("你的内力太弱，无法练" + "赵家剑法" + "。\n");
  return 1;
}
int valid_enable(string usage) { return usage == "sword" || usage == "parry"; }
string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}
mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("zhaojia-sword", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level > action[i - 1]["lvl"])
      return action[NewRandom(i, 20, level / 5)];
}
int practice_skill(object me) {
  object weapon; if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "sword") return notify_fail("你使用的武器不对。\n"); if ((int)me->query("qi") < 25)
    return notify_fail("你的体力不够了，休息一下再练吧。\n");
  if ((int)me->query("neili") < 3)
    return notify_fail("你的内力不够了，休息一下再练吧。\n");
  me->receive_damage("qi", 25);
  me->add("neili", -3);
  return 1;
}  //total 34 lines!
