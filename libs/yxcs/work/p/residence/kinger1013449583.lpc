// a skeleton for user rooms
inherit "/inherit/room/city";
void create() {
  set("short", "梦想之城");
  set("save_name", "kinger1013449583");
  reload();
  set("long", @LONG
梦幻城历来就是风尘人士汇集之所。这间酒馆处在南来北往的人流中心。就
更加热闹有加。小二托着酒壶，穿梭在桌椅之间。猜拳行令之声、笑骂声、
叱喝声响成一片。也分不清那些是良家百姓、哪些是响马强人。这里真说
得上是龙蛇混杂。成为雇佣士兵的好去处。

LONG
  );
  set("exits", ([
    "northwest": "/d/kaifeng/eastx.lpc",
  ]));
  set("objects", ([
  ]));
  set("class", "bigwiz7001");
  set("no_fight", 1);
  set("valid_startroom", 1);
  set("sleep_room", "1");
  set("no_clean_up", 1);

  setup();
}
int clean_up() {
  return 0;
}
