// a skeleton for user rooms
inherit "/inherit/room/city";
void create() {
  set("short", "楼兰古都");
  set("save_name", "pxjf1018163051");
  reload();
  set("long", @LONG
在前边不远处就是充满了传奇色彩的楼兰古国

LONG
  );
  set("exits", ([
    "southwest": "/d/kaifeng/eastx.lpc",
  ]));
  set("objects", ([
  ]));
  set("class", "bigwiz7001");
  set("no_fight", 1);
  set("valid_startroom", 1);
  set("sleep_room", "1");
  set("no_clean_up", 1);

  setup();
}
int clean_up() {
  return 0;
}
