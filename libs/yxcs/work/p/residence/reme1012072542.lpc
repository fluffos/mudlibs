// a skeleton for user rooms
inherit "/inherit/room/city";
void create() {
  set("short", "天地一心书城");
  set("save_name", "reme1012072542");
  reload();
  set("long", @LONG
这是天地一心读书的地方，你可可以成为书籍的朋友。书是一生的梦想。

LONG
  );
  set("exits", ([
    "west": "/p/residence/reme1012072464.lpc",
  ]));
  set("objects", ([
  ]));
  set("class", "bigwiz7001");
  set("no_fight", 1);
  set("valid_startroom", 1);
  set("sleep_room", "1");
  set("no_clean_up", 1);

  setup();
}
int clean_up() {
  return 0;
}
