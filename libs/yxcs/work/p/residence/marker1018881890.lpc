// a skeleton for user rooms
inherit ROOM;
void create() {
  set("short", "洪兴社大道");
  set("long", @LONG
这里就是通向“洪兴社”的大路了。两边繁华街道，表现了这个社区繁荣景象。
听说里面是个花花世界，令人神往。不过外面的人对里面不是很了解。

LONG
  );
  set("exits", ([
    "west": "/p/residence/marker1018882264",
    "east": "/p/residence/twfh1013317753.lpc",
  ]));
  set("objects", ([
  ]));
  set("owner", "bigtie7001");
  set("banghui", "洪兴社");

  setup();
}

void init() {
  object ob = this_player();
  if ((string)ob->query("banghui") != environment(ob)->query("banghui")) {
    message(
      "vision",
      HIY "〖帮派总坛〗" NOR + ":" + HIY " " + ob->query("name") + "于今日" + NATURE_D->game_time() + "企图闯入" + environment(ob)->query("banghui") + "总坛,被赶了出来!\n" NOR,
      users()
    );
    message_vision(HIR "门卫突然从里面冲了出来给$N一脚,$N惨叫一声,被踢了出来。" NOR, this_player());
    ob->move("/p/guangchang");
  } else return;
}
