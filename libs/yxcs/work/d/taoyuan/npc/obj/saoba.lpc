// saoba.c

#include <ansi.h>
#include <weapon.h>

inherit ITEM;

void create() {
  set_name("扫把", ({ "sao ba" }));
  set_weight(50);

  if (clonep())
    set_default_object(__FILE__);
  set("unit", "枝");
  set("long", "一枝很普通的扫把。\n");
  set("value", 1000);
  setup();
}
