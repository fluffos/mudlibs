#pragma save_binary
// Room: /u/k/karrimor/temple/inneryard.c

inherit ROOM;

void create() {
  set("coor", ({ 1200, 4940, 35 }));
  set("short", "天井");
  set("long",
    "你正站在灵心观的天井里,这里种植著一些花草树木,在西北角落\n"
    "里有著一口水井。北边就是供俸茅山派历代掌门人的地方,南边\n"
    "则是一面墙,墙的另一侧就是大殿。\n"
  );
  set("exits", ([ /* sizeof() == 3 */
    "north": __DIR__ "corridor5",
    "west": __DIR__ "corridor6",
    "east": __DIR__ "corridor7",
  ]));

  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/little_taoist2": 1,
  ]));
  set("outdoors", "temple");

  setup();
  replace_program(ROOM);
}
