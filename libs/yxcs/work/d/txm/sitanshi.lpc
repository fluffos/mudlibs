inherit ROOM;

void create() {
  set("short", "四坛室 ");
  set("long", @LONG
  转过屏风，你来到一间宽敞的屋子，门檐匾额上书「呈坛」两个
大字，原来这里是铁血盟四分坛坛主祭坛并议事的地方。平时各坛主
行走江湖，在各地分坛主持事务，但每月初五必回盟址述职，所以这
里也打扫得非常干净，屋子里呈一派庄严气氛。
LONG);

  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "juxieting",
    //       ]));
    //      set("objects", ([ /* sizeof() == 1 */
    //              __DIR__"npc/xuncha" : 1,
  ]));
  set("no_clean_up", 0);
  set("outdoors", "txm");


  setup();
}
