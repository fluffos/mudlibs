/*
 * Some constants relevant to the login procedure.
 *
 * Just include the proper definitions.
 *
 * FluffOS PORT NOTE: originally forwarded to /d/Genesis/login/login.h --
 * this repo ships no /d/Genesis domain at all (upstream's own README:
 * "This repo does not contain a standard domain which will need to be
 * implemented to use the mud"), so that target never existed here.
 * Left empty rather than restoring a domain-specific race header this
 * project doesn't have content for; if secure/login.lpc ever turns out
 * to need a specific symbol from it, that'll surface as a distinct
 * "Undefined function/identifier" to fix on its own.
 */
