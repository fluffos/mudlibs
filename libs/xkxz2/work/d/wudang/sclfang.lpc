// sclfang.c 南间操练房

inherit ROOM;

void create() {
  set("short", "南间操练房");
  set("long", @LONG
这里摆满了大刀长剑、沙袋绑腿等，是武当弟子日常练功的地方。
LONG
  );
  set("exits", ([
    "north": __DIR__ "clfang",
  ]));

  set("objects", ([
    "/clone/misc/mu-ren": 4,
  ]));

  setup();
}
