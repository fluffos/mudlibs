// Room: /u/xdd/gumu/gmmishi1.c

#include <ansi.h>;
inherit ROOM;

void create() {
  set("short", BLU "密室" NOR);
  set("long", @LONG
借着火烛的光亮，你四下一看，不由打了个寒噤，空旷旷的大厅里只并
排放着五具石棺。凝神细看，见两具石棺棺盖已密密盖着，另外三具的棺盖
却只推上一半，也不知其中有无尸体。
LONG);
  set("exits", ([
    "out": __DIR__ "ss4",
  ]));
  setup();
}
