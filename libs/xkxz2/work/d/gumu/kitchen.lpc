// Room: /u/xdd/gumu/kitchen.c
#include <ansi.h>

#include <room.h>
inherit ROOM;

void create() {
  set("short", "厨房");
  set("long", @LONG
这就是古墓派弟子用斋的石室, 屋中陈设简陋，但洁净异常，堂上只一桌一几，
几把石椅，此外便无别物。一位满脸生满鸡皮疙瘩的老妇正在为古墓派弟子准备茶
(water)点(food)。
LONG
  );
  set("exits", ([
    "south": __DIR__ "gmzt",
  ]));

  set("objects", ([
    __DIR__ "npc/oldlady": 1,
  ]));

  setup();
  replace_program(ROOM);

}
