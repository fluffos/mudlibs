// /u/dubei/fenglei.c

inherit ROOM;

void create() {
  set("short", "风雷堂");
  set("long", @LONG
这里是风雷堂。堂门口站着几名教众把守。进门是一个大厅，两旁排着
兵器。此时正有两个青年在厅中间过招，正位上站着一个老人，正是威名赫
赫的风雷堂堂主童百熊。
LONG
  );
  set("exits", ([
    "east": __DIR__ "zongtan",

  ]));

  set("objects", ([
    //                __DIR__"npc/ " : 1,
  ]));
  setup();
}
