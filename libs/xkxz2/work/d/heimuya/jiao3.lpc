// /u/dubei/jiao3.c

inherit ROOM;

void create() {
  set("short", "绞索三");
  set("long", @LONG
跨出竹篓，只见一片片轻云从头顶飘过，你发现这里原来是崖中间，原
来黑木崖太高，须用绞索接力才能将人和物品绞到崖顶。
LONG
  );
  set("outdoors", "黑木崖");

  set("exits", ([
    "east": __DIR__ "pailou",
    "northdown": __DIR__ "jiao2",
  ]));

  set("objects", ([
    //                __DIR__"npc/ " : 1,
  ]));
  setup();
  replace_program(ROOM);
}
