// Room: /dali/zahuopu.c
// bbb 1997/06/12
inherit ROOM;

void create() {
  set("short", "杂货铺");
  set("long", @LONG
这是一家小小的杂货铺，别看其小，却收拾得非常整齐，因为对面是城
中富人的积居地，所以卖的货物都是特地从产地长途贩来的。有一两个家人
模样正在和老板讨价还价。左侧立着一块招牌(sign)。
LONG
  );

  set("item_desc", ([
    "sign": "请用 \"list\" 列出当前货物表，\"buy\" 向老板购物。\n",
  ]));
  set("exits", ([
    "west": __DIR__ "nanjie3",
  ]));
  set("objects", ([
    __DIR__ "npc/yang": 1,
  ]));

  setup();
  replace_program(ROOM);
}
