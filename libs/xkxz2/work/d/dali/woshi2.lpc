inherit ROOM;

void create() {
  set("short", "卧室");
  set("long", @LONG
这里是镇南王之子段誉的卧室，房子不大，床上被子叠的整整
齐齐，段公子好久没在王府了，屋子里落了些灰尘，显得旧旧的。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "lang6",
  ]));

  set("no_fight", 1);
  set("sleep_room", 1);
  setup();
}
