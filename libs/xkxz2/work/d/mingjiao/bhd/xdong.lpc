inherit ROOM;
void create() {
  set("short", "石洞");
  set("long", @LONG
此处为一小山洞，中间透入一线天光，宛如天窗一般。这里陶碗、土灶
、石床、粗具等一应俱全。
LONG);
  set("exits", ([
    "out": __DIR__ "shishan1",
  ]));
  set("objects", ([
    "/d/mingjiao/npc/xie": 1,
  ]));
  setup();
  replace_program(ROOM);
}
