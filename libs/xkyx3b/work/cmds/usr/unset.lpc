// unset.c

inherit F_CLEAN_UP;

int help();

int main(object me, string arg) {
  if (!arg || arg == "") return help();
  me->delete("env/" + arg);
  write("完成。\n");
  return 1;
}

int help() {
  write(@TEXT
指令格式：unset <变数名>        删除环境变数的设定

TEXT
  );

  return 1;
}
