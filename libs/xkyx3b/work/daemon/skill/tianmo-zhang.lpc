// tianmo-zhang - 天魔掌法

inherit SKILL;

mapping *action = ({
  ([ "action": "只见$N错步上前，一招「唯我独尊」，击向$n的$l要害",
    "force": 100,
    "dodge": 10,
    "parry": 10,
    "damage": 10,
    "lvl": 0,
    "skill_name": "唯我独尊",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N向前跃上一步，一招「万里独行」，$w划出一个大弧，平平地向$n的$l挥去",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 20,
    "lvl": 10,
    "skill_name": "万里独行",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N身形绕$n一转，一招「索命无常」向$n$l接连出掌",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 30,
    "lvl": 20,
    "skill_name": "索命无常",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N身形拔起，在半空一招「血流成河」右掌猛击$n的$l",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 40,
    "lvl": 30,
    "skill_name": "血流成河",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N使一招「君临天下」，纵起丈余直击$n的$l",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 50,
    "lvl": 40,
    "skill_name": "君临天下",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一声大喝使出「天火燎燃」，幻出满天掌影，掌掌不离$n的$l",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 60,
    "lvl": 50,
    "skill_name": "天火燎燃",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N双掌一错, 一招「穷途末路」，幻出满天掌影，掌掌不离$n的$l",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 60,
    "skill_name": "穷途末路",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「群魔乱舞」飘忽若神，双掌一前一后，猛地拍向$n的胸口",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 70,
    "skill_name": "群魔乱舞",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N向前踏上一步，一招「千秋万载」，迅捷无比地劈向$n的$l要害！",
    "force": 260,
    "dodge": 80,
    "parry": 80,
    "damage": 120,
    "lvl": 80,
    "skill_name": "千秋万载",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一声大喝使出「一统江湖」，幻出满天掌影，掌掌不离$n的$l",
    "force": 280,
    "dodge": 90,
    "parry": 90,
    "damage": 140,
    "lvl": 100,
    "skill_name": "一统江湖",
    "damage_type": "瘀伤"
  ]),
});

int valid_enable(string usage) { return (usage == "unarmed") || (usage == "parry"); }

int valid_learn(object me) {
  object ob;
  int level;

  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练天魔掌法必须空手。\n");
  if ((int)me->query("max_force") < 200)
    return notify_fail("你的内力不够。\n");
  level = me->query_skill("tianmo-zhang");
  if (-level * level * level / 10 < me->query("SEN"))
    return notify_fail("修练天魔掌法，要心狠手辣，奸恶歹毒。你好象做得不够呀！\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("tianmo-zhang", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  object weapon;

  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练天魔掌法必须空手。\n");
  if ((int)me->query("kee") < 50)
    return notify_fail("你的体力不够练天魔掌法。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍天魔掌法。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "tianmo-zhang/" + action;
}

int enable_factor() { return 100; }

int learn_factor(int n) {
  if (n < 100) return 110;
  else return 90;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 1; }

int practice_factor() { return 100; }
int practice_bonus() { return 1; }
