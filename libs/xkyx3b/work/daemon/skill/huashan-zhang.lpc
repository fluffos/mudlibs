// huashan-zhang.c - 华山掌法

inherit SKILL;

mapping *action = ({
  ([ "action": "只见$N一招「有凤来仪」，双手虚虚一抱，疾向$n的$l击去",
    "force": 50,
    "dodge": -10,
    "parry": -10,
    "damage": 5,
    "lvl": 0,
    "skill_name": "有凤来仪",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N错步向前，一招「金燕横空」，双手运拳如风，向$n的$l击去",
    "force": 80,
    "dodge": -5,
    "parry": -5,
    "damage": 10,
    "lvl": 10,
    "skill_name": "金燕横空",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "只见$N一招「无边落木」，身子骤然拔高数尺，自上而下向$n的$l击去",
    "force": 100,
    "dodge": 1,
    "parry": 1,
    "damage": 20,
    "lvl": 20,
    "skill_name": "无边落木",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N大喝一声，中宫直进，一招「青山隐隐」，一拳平平地击向$n的$l",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 30,
    "lvl": 30,
    "skill_name": "青山隐隐",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N向前踏上一步，身形一顿，闲雅清隽，一招「苍松迎客」，对准$n的$l斜斜击出一掌",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 40,
    "lvl": 40,
    "skill_name": "苍松迎客",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N说声：“好！”，双足轻点，灵如初燕，一招「天坤倒悬」击向$n的$l",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 50,
    "lvl": 50,
    "skill_name": "天坤倒悬",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N滴溜溜的一转，一招「白云出蚰」，双拳一振，一股力道排山倒海般向$n的$l推了过去",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 60,
    "lvl": 60,
    "skill_name": "白云出蚰",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N错步上前，一招「流星赶月」，气凝丹田，右手握掌成拳缓缓推向$n的$l",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 70,
    "lvl": 70,
    "skill_name": "流星赶月",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N拔步挺腰，一招「回风拂柳」，$n只觉得眼前一花，全身被笼罩在一片掌风之中",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 80,
    "skill_name": "回风拂柳",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N潜运真力，一招「无双无对」，掌风不定，招招击向$n的$l要害",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 100,
    "skill_name": "无双无对",
    "damage_type": "瘀伤"
  ]),
});

int valid_enable(string usage) { return (usage == "unarmed") || (usage == "parry"); }

int valid_learn(object me) {
  int level;

  if ((int)me->query("max_force") < 100)
    return notify_fail("你的内力不够。\n");
  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练华山掌法必须空手。\n");
  level = me->query_skill("huashan-zhang");
  if (level * level * level / 10 > me->query("SEN"))
    return notify_fail("修练华山掌法，要除暴安良，行侠仗义。你好象做得不够呀！\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("huashan-zhang", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练华山掌法必须空手。\n");
  if ((int)me->query("kee") < 40)
    return notify_fail("你的体力不够练华山掌法。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍华山掌法。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "huashan-zhang/" + action;
}

int enable_factor() { return 100; }

int learn_factor(int n) {
  if (n < 100) return 120;
  else return 100;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 3; }

int practice_factor() { return 110; }
int practice_bonus() { return 1; }
