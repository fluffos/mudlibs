// tianshi-zhang.c - 天师掌法

inherit SKILL;

mapping *action = ({
  ([ "action": "$N一招「天师掌法」，气凝丹田，右手握掌成拳缓缓推向$n的$l",
    "force": 80,
    "dodge": 5,
    "parry": 5,
    "damage": 5,
    "lvl": 0,
    "skill_name": "天师掌法",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N施出「天师掌法」，双拳一振，一股力道排山倒海般向$n的$l推了过去",
    "force": 100,
    "dodge": 5,
    "parry": 5,
    "damage": 10,
    "lvl": 10,
    "skill_name": "天师掌法",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「天师掌法」，大吼一声，一拳平平地击向$n的$l",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 20,
    "lvl": 20,
    "skill_name": "天师掌法",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N施出「天师掌法」，左手护胸，右拳直直地击向$n的$l",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 30,
    "lvl": 30,
    "skill_name": "损心伤肺摧肝肠",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N纵身一跃，一招「天师掌法」，幻成一条疾光击向$n的$l",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 40,
    "lvl": 40,
    "skill_name": "天师掌法",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「天师掌法」，掌势如风，双手直击$n的$l",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 50,
    "lvl": 50,
    "skill_name": "天师掌法",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N身形一转，一招「天师掌法」，掌风不定，招招击向$n的$l要害",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 60,
    "lvl": 60,
    "skill_name": "天师掌法",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「天师掌法」，$n只觉得眼前一花，全身被笼罩在一团掌风之中",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 70,
    "skill_name": "天师掌法",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N错步上前，一招「天师掌法」，拳意若有若无，淡淡地向$n的$l击去",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 80,
    "skill_name": "天师掌法",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「天师掌法」，纵身飘开数尺，运发真气，双手中遥摇指向$n的$l",
    "force": 260,
    "dodge": 80,
    "parry": 80,
    "damage": 120,
    "lvl": 100,
    "skill_name": "天师掌法",
    "damage_type": "瘀伤"
  ]),
});

int valid_enable(string usage) { return (usage == "unarmed") || (usage == "parry"); }

int valid_learn(object me) {
  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练天师掌法必须空手。\n");
  if ((int)me->query("max_force") < 150)
    return notify_fail("你的内力不够。\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("tianshi-zhang", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  object weapon;

  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练天师掌法必须空手。\n");
  if ((int)me->query("kee") < 40)
    return notify_fail("你的体力不够练天师掌法。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍天师掌法。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "tianshi-zhang/" + action;
}

int enable_factor() { return 150; }

int learn_factor(int n) {
  if (n < 100) return 115;
  else return 95;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 2; }

int practice_factor() { return 105; }
int practice_bonus() { return 1; }
