// ruyi-blade.c - 如意刀法

inherit SKILL;

mapping *action = ({
  ([ "action": "$N一磕刀柄，一招「若往若返」，猛地一个转身，手中$w直向$n斩去",
    "force": 80,
    "dodge": 5,
    "parry": 5,
    "damage": 5,
    "lvl": 0,
    "skill_name": "若往若返",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N把手中的$w一扬，一招「峰回路转」，手中的刀一个急转，直砍向$n的$l",
    "force": 100,
    "dodge": 5,
    "parry": 5,
    "damage": 10,
    "lvl": 10,
    "skill_name": "峰回路转",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N身形陡然滑出数尺，一招「御风而行」，刀势如风，手中$w斩向$n的$l",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 20,
    "lvl": 20,
    "skill_name": "御风而行",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N一招「云雾萦绕」，身子骤然拔高数尺，手中$w一个平推向$n的$l砍去。",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 30,
    "lvl": 30,
    "skill_name": "云雾萦绕",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N纵身一跃，手中$w一招「金光泻地」轻飘飘地向$n的$l要害掠去",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 40,
    "lvl": 40,
    "skill_name": "金光泻地",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N手中$w向外一分，轻轻颤动，一招「柳暗花明」反手向$n$l掠去",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 50,
    "lvl": 50,
    "skill_name": "柳暗花明",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N横刀上前，身形一转，手中$w使一招「空谷秋虹」画出一道光弧斩向$n的$l",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 60,
    "lvl": 60,
    "skill_name": "空谷秋虹",
    "damage_type": "割伤"
  ]),
  ([ "action": "$n只觉眼前一花，$N一招「紫气东来」，$w挟呼呼风声迅猛砍向$n的$l",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 70,
    "skill_name": "紫气东来",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N把$w划了一圈，一招「神光离合」，$n只觉得全身被笼罩在一团刀气之中。",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 80,
    "skill_name": "神光离合",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N身形一转，一招「逍遥自在」，$w刀光不定，招招砍向$n的$l要害",
    "force": 260,
    "dodge": 80,
    "parry": 80,
    "damage": 120,
    "lvl": 100,
    "skill_name": "逍遥自在",
    "damage_type": "割伤"
  ]),
});

int valid_enable(string usage) { return (usage == "blade") || (usage == "parry"); }

int valid_learn(object me) {
  object ob;
  int level;

  if ((int)me->query("max_force") < 150)
    return notify_fail("你的内力不够。\n");
  if (!(ob = me->query_temp("weapon"))
    || (string)ob->query("skill_type") != "blade")
    return notify_fail("你必须先找一把刀才能练如意刀法。\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("ruyi-blade", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  object weapon;

  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "blade")
    return notify_fail("你使用的武器不对。\n");
  if ((int)me->query("kee") < 40)
    return notify_fail("你的体力不够练如意刀法。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍如意刀法。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "ruyi-blade/" + action;
}

int enable_factor() { return 80; }

int learn_factor(int n) {
  if (n < 100) return 115;
  else return 95;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 2; }

int practice_factor() { return 105; }
int practice_bonus() { return 1; }
