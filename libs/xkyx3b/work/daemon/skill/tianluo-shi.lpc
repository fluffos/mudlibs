// tianluo-shi.c - 天罗地网势

inherit SKILL;

mapping *action = ({
  ([ "action": "$N使一招「天罗地网势」，双手轻飘飘地箍向$n$l",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 20,
    "lvl": 0,
    "skill_name": "天罗地网势",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N左掌一平，右掌一伸，一招「天罗地网势」直拍$n的$l",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 20,
    "lvl": 10,
    "skill_name": "天罗地网势",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N身形绕$n一转，双手上撩，一招「天罗地网势」击向$n$l",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 30,
    "lvl": 20,
    "skill_name": "天罗地网势",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N身形绕$n一转，双手上撩，一招「天罗地网势」击向$n$l",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 40,
    "lvl": 30,
    "skill_name": "天罗地网势",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N左掌一按，右掌一挥，一招「天罗地网势」拍向$n的$l",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 60,
    "lvl": 40,
    "skill_name": "天罗地网势",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N左掌一按，右掌一挥，一招「天罗地网势」拍向$n的$l",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 50,
    "skill_name": "天罗地网势",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N使一招「天罗地网势」，$n的$l已围在$N的重重掌影之下",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 60,
    "skill_name": "天罗地网势",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N使一招「天罗地网势」，$n的$l已围在$N的重重掌影之下",
    "force": 260,
    "dodge": 80,
    "parry": 80,
    "damage": 120,
    "lvl": 70,
    "skill_name": "天罗地网势",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N神色一敛，使出「天罗地网势」，围绕$n的$l接连出掌",
    "force": 280,
    "dodge": 90,
    "parry": 90,
    "damage": 140,
    "lvl": 80,
    "skill_name": "天罗地网势",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N神色一敛，使出「天罗地网势」，围绕$n的$l接连出掌",
    "force": 300,
    "dodge": 100,
    "parry": 100,
    "damage": 160,
    "lvl": 100,
    "skill_name": "天罗地网势",
    "damage_type": "瘀伤"
  ]),
});


int valid_enable(string usage) { return (usage == "unarmed") || (usage == "parry"); }

int valid_learn(object me) {
  object ob;
  int level;

  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练天罗地网势必须空手。\n");
  if ((int)me->query("max_force") < 200)
    return notify_fail("你的内力不够。\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("tianluo-shi", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练天罗地网势必须空手。\n");
  if ((int)me->query("kee") < 50)
    return notify_fail("你的体力不够练天罗地网势。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍天罗地网势。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "tianluo-shi/" + action;
}

int enable_factor() { return 120; }

int learn_factor(int n) {
  if (n < 100) return 105;
  else return 85;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 1; }

int practice_factor() { return 95; }
int practice_bonus() { return 1; }
