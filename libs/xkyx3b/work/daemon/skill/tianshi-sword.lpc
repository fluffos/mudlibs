// tianshi-sword.c - 天师伏魔剑

inherit SKILL;

mapping *action = ({
  ([ "action": "只见$N身形一转，一招「烈火腾云」，手中$w轻飘飘地向$n的$l要害击去",
    "force": 80,
    "dodge": 5,
    "parry": 5,
    "damage": 5,
    "lvl": 0,
    "skill_name": "烈火腾云",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N向前踏上一步，一招「风满长空」，手中$w如风一般卷向$n的$l",
    "force": 100,
    "dodge": 5,
    "parry": 5,
    "damage": 10,
    "lvl": 10,
    "skill_name": "风满长空",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N双足轻点，一招「草深雾泽」，手中$w狂风骤雨般地向$n的$l击去",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 20,
    "lvl": 20,
    "skill_name": "草深雾泽",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N身形一转，一招「雷震四方」，$w如风一般击向$n的$l要害",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 30,
    "lvl": 30,
    "skill_name": "雷震四方",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N向前踏上一步，一招「狂风骤雨」，中宫直进，手中$w猛地击向$n的$l",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 40,
    "lvl": 40,
    "skill_name": "狂风骤雨",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N将身一幌，一招「雪花翩翩」，手中$w直出，绵绵不断，连击$n几处要害",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 50,
    "lvl": 50,
    "skill_name": "雪花翩翩",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N腾空而起，手中$w一抖，使出一招「天目昭辉」，如山轮影，疾疾压向$n",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 60,
    "lvl": 60,
    "skill_name": "天目昭辉",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N身形一转，一招「水到渠成」，$n只觉得全身被笼罩在一团轮影之中",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 70,
    "skill_name": "水到渠成",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N向前踏上一步，手中$w一转，一招「山高林密」缓缓击向$n的$l",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 80,
    "skill_name": "山高林密",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N潜运真力，一招「金刚伏魔」，轮意若有若无，狂风骤起，笼罩天地",
    "force": 260,
    "dodge": 80,
    "parry": 80,
    "damage": 120,
    "lvl": 100,
    "skill_name": "金刚伏魔",
    "damage_type": "挫伤"
  ]),
});

int valid_enable(string usage) { return (usage == "sword") || (usage == "parry"); }

int valid_learn(object me) {
  object ob;

  if ((int)me->query("max_force") < 150)
    return notify_fail("你的内力不够。\n");
  if (!(ob = me->query_temp("weapon"))
    || (string)ob->query("skill_type") != "sword")
    return notify_fail("你必须先找一把剑才能练天师伏魔剑。\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("tianshi-sword", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  object weapon;

  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "sword")
    return notify_fail("你使用的武器不对。\n");
  if ((int)me->query("kee") < 40)
    return notify_fail("你的体力不够练天师伏魔剑。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍天师伏魔剑。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "tianshi-sword/" + action;
}

int enable_factor() { return 150; }

int learn_factor(int n) {
  if (n < 100) return 115;
  else return 95;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 2; }

int practice_factor() { return 105; }
int practice_bonus() { return 1; }
