// qishier-yi.c - 七十二般绝艺

inherit SKILL;
/*
罗汉堂首座苦慧禅师（远走西域）
罗汉堂首座苦智禅师（死于火工头陀掌底）
武功：神掌八打（烈心掌）、分解掌、大缠丝
火工头陀：大缠丝、少林外功（金刚伏魔神通）、金刚指刀、般若金刚
须弥山掌）（虎爪功）武功：韦陀伏魔剑
*/
mapping *action = ({
  ([ "action": "$N拔步挺腰，一招「神拳绝艺」，双手如风，疾向$n的$l击去",
    "force": 100,
    "dodge": 10,
    "parry": 10,
    "damage": 10,
    "lvl": 0,
    "skill_name": "神拳绝艺",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一声大喝，一招「弹指绝艺」，无声无息，疾向$n的$l击去",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 20,
    "lvl": 10,
    "skill_name": "弹指绝艺",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「剑法绝艺」，穿插翻飞，迅捷无比地向$n的$l击去",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 30,
    "lvl": 20,
    "skill_name": "剑法绝艺",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N左右穿插不定，一招「刀法绝艺」，双手如风，疾向$n的$l击去",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 40,
    "lvl": 30,
    "skill_name": "刀法绝艺",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N向前踏上一步，一招「棒法绝艺」，中宫直进，如风一般击向$n的$l",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 50,
    "lvl": 40,
    "skill_name": "棒法绝艺",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「棍法绝艺」，穿插不定，绵绵不断，如风一般扫向$n的$l",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 60,
    "lvl": 50,
    "skill_name": "棍法绝艺",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N虚步提腰，一招「暗器绝艺」，幻出满天影子，招招不离$n的$l",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 60,
    "skill_name": "暗器绝艺",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「鞭法绝艺」，飘忽若神，一前一后，猛地击向$n的胸口",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 70,
    "skill_name": "鞭法绝艺",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N向前踏上一步，一招「硬功绝艺」，迅捷无比地击向$n的$l要害！",
    "force": 260,
    "dodge": 80,
    "parry": 80,
    "damage": 120,
    "lvl": 80,
    "skill_name": "硬功绝艺",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N腾空而起，轻灵飘逸，一招「内功绝艺」将$n笼罩在一片杀气之中",
    "force": 280,
    "dodge": 90,
    "parry": 90,
    "damage": 140,
    "lvl": 100,
    "skill_name": "内功绝艺",
    "damage_type": "瘀伤"
  ]),
});

int valid_enable(string usage) {
  return (usage == "sword") || (usage == "blade") || (usage == "qimen") || (usage == "stick") || (usage == "unarmed") || (usage == "parry");
}

int valid_learn(object me) {
  object ob;
  int level;

  if ((int)me->query("max_force") < 200)
    return notify_fail("你的内力不够。\n");
  if ((int)me->query_skill("buddhism") < (int)me->query_skill("qishier-yi") / 2)
    return notify_fail("佛门每种绝技都要有慈悲佛法为之化解，你好象学得不够呀！\n");
  level = me->query_skill("qishier-yi");
  if (level * level * level / 10 > me->query("SEN"))
    return notify_fail("修练七十二般绝艺，要除暴安良，行侠仗义。你好象做得不够呀！\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("qishier-yi", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  object weapon;

  if ((int)me->query("kee") < 50)
    return notify_fail("你的体力不够练七十二般绝艺。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍七十二般绝艺。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "qishier-yi/" + action;
}

int enable_factor() { return 120; }

int learn_factor(int n) {
  if (n < 100) return 110;
  else return 90;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 1; }

int practice_factor() { return 100; }
int practice_bonus() { return 1; }
