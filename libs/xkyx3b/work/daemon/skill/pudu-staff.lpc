// pudu-staff.c - 普渡杖法

inherit SKILL;

mapping *action = ({
  ([ "action": "$N使一招「黄牛转角」，手中$w自下而上，沉猛无比地向$n的小腹挑去。",
    "force": 50,
    "dodge": -10,
    "parry": -10,
    "damage": 5,
    "lvl": 0,
    "skill_name": "黄牛转角",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N快步跨出，一招「野马追风」，左手平托$w，右掌猛推杖端，顶向$n的胸口。",
    "force": 80,
    "dodge": -5,
    "parry": -5,
    "damage": 10,
    "lvl": 10,
    "skill_name": "野马追风",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N高举$w，一招「猛虎跳涧」，全身跃起，手中$w搂头盖顶地向$n击去。",
    "force": 100,
    "dodge": 1,
    "parry": 1,
    "damage": 20,
    "lvl": 20,
    "skill_name": "猛虎跳涧",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N一招「狮子摇头」，双手持杖如橹，对准$n猛地一搅，如同平地刮起一阵旋风。",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 30,
    "lvl": 30,
    "skill_name": "狮子摇头",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N将身一幌，手中$w一挥，使出一招「风卷残云」，忽左忽右扫向$n的$l",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 40,
    "lvl": 40,
    "skill_name": "风卷残云",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N横持$w，一招「苍龙摆尾」，杖端化出无数个圆圈，凝滞沉重，把$n缠在其中。",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 50,
    "lvl": 50,
    "skill_name": "苍龙摆尾",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N全身滚倒，$w盘地横飞，突出一招「大蟒翻身」，杖影把$n裹了起来",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 60,
    "lvl": 60,
    "skill_name": "大蟒翻身",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N双手和十，躬身一招「如来托钵」，$w自肘弯飞出，拦腰向$n撞去。",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 70,
    "lvl": 70,
    "skill_name": "如来托钵",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N一招「慈航普渡」，$w如飞龙般自掌中跃出，直向$n的胸口穿入。",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 80,
    "skill_name": "慈航普渡",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N腾空而起，手中$w一抖，使出一招「穿云裂石」，如山杖影，疾疾压向$n",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 100,
    "skill_name": "穿云裂石",
    "damage_type": "挫伤"
  ]),
});

int valid_enable(string usage) { return (usage == "stick") || (usage == "parry"); }

int valid_learn(object me) {
  object ob;
  int level;

  if ((int)me->query("max_force") < 100)
    return notify_fail("你的内力不够。\n");
  if (!(ob = me->query_temp("weapon"))
    || (string)ob->query("skill_type") != "staff")
    return notify_fail("你必须先找一根杖才能练普渡杖法。\n");
  if ((int)me->query_skill("buddhism") < (int)me->query_skill("pudu-staff") / 2)
    return notify_fail("佛门每种绝技都要有慈悲佛法为之化解，你好象学得不够呀！\n");
  level = me->query_skill("pudu-staff");
  if (level * level * level / 10 > me->query("SEN"))
    return notify_fail("修练普渡杖法，要除暴安良，行侠仗义。你好象做得不够呀！\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("pudu-staff", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  object weapon;

  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "staff")
    return notify_fail("你使用的武器不对。\n");
  if ((int)me->query("kee") < 40)
    return notify_fail("你的体力不够练普渡杖法。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍普渡杖法。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "pudu-staff/" + action;
}

int enable_factor() { return 100; }

int learn_factor(int n) {
  if (n < 100) return 120;
  else return 100;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 3; }

int practice_factor() { return 110; }
int practice_bonus() { return 1; }
