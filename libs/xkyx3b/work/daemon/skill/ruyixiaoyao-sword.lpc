// ruyixiaoyao-sword.c - 如意逍遥剑

inherit SKILL;

mapping *action = ({
  ([ "action": "只见$N一招「鸟语花香」，$w化作点点寒星，向$n的$l连刺几剑",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 20,
    "lvl": 0,
    "skill_name": "鸟语花香",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N一招「两小无猜」，左手虚抱，右手$w斜斜地划向$n的$l要害",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 20,
    "lvl": 10,
    "skill_name": "两小无猜",
    "damage_type": "割伤"
  ]),
  ([ "action": "只见$N一招「情意缠绵」，手中$w轻飘飘地向$n的$l要害掠去",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 30,
    "lvl": 20,
    "skill_name": "情意缠绵",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N身形一转，一招「逍遥自在」，$w剑光不定，刺向$n的$l要害",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 40,
    "lvl": 30,
    "skill_name": "逍遥自在",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N把$w划了一圈，一招「共醉温柔」，$n只觉得全身被笼罩在一团剑气之中。",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 60,
    "lvl": 40,
    "skill_name": "共醉温柔",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N一招「春花多媚」，剑锋平指，剑势如风，手中$w直刺$n的$l",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 50,
    "skill_name": "春花多媚",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N向前跃上一步，左手剑诀，右手$w使出一式「春鸟多哀」直刺$n的$l",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 60,
    "skill_name": "春鸟多哀",
    "damage_type": "刺伤"
  ]),
  ([ "action": "只见$N一招「春风多情」，手中$w轻灵飘逸，如风一般卷向$n的$l",
    "force": 260,
    "dodge": 80,
    "parry": 80,
    "damage": 120,
    "lvl": 70,
    "skill_name": "春风多情",
    "damage_type": "刺伤"
  ]),
  ([ "action": "但见$N手中$w翩跹不定，一招「吹我罗裳」对着$n的$l一连刺出七剑",
    "force": 280,
    "dodge": 90,
    "parry": 90,
    "damage": 140,
    "lvl": 80,
    "skill_name": "吹我罗裳",
    "damage_type": "刺伤"
  ]),
  ([ "action": "只见$N的$w凭空一指，闲雅清隽，一招「衣带渐宽」缓缓刺向$n的$l",
    "force": 300,
    "dodge": 100,
    "parry": 100,
    "damage": 160,
    "lvl": 100,
    "skill_name": "衣带渐宽",
    "damage_type": "刺伤"
  ]),
});

int valid_enable(string usage) { return (usage == "sword") || (usage == "parry"); }

int valid_learn(object me) {
  object ob;

  if ((int)me->query("max_force") < 200)
    return notify_fail("你的内力不够。\n");
  if (!(ob = me->query_temp("weapon"))
    || (string)ob->query("skill_type") != "sword")
    return notify_fail("你必须先找一把剑才能练如意逍遥剑法。\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("ruyixiaoyao-sword", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  object weapon;

  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "sword")
    return notify_fail("你使用的武器不对。\n");
  if ((int)me->query("kee") < 50)
    return notify_fail("你的体力不够练如意逍遥剑法。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍如意逍遥剑法。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "ruyixiaoyao-sword/" + action;
}

int enable_factor() { return 100; }

int learn_factor(int n) {
  if (n < 100) return 105;
  else return 85;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 1; }

int practice_factor() { return 95; }
int practice_bonus() { return 1; }
