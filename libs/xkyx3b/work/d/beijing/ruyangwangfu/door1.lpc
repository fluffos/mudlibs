// door1.c
//王府南门。

#include <room.h>

inherit ROOM;

void create() {
  set("short", "王府正门");
  set("long", @LONG
王府的正门气派的确是不同的。一对红漆大门把王府和外面的世界关起来了。
门口两边是一对石狮子。
LONG
  );
  set("exits", ([
    "south": __DIR__ "road5",
    "north": __DIR__ "mudroad"
  ]));

  set("outdoors", "beijing");
  set("item_desc", ([
    "door": (: look_door, "north" :),
  ]));
  /*	set("objects", ([
              "/d/npc/sungoku" : 1,
		"/d/snow/npc/waiter" : 1 ]) );
*/
  create_door("north", "红漆大门", "south", DOOR_CLOSED);


  setup();
}
