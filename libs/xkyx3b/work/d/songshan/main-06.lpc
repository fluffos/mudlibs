// Room: /d/songshan/main-06.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create() {
  set("short", "芦崖瀑布");
  set("long", @LONG
								
    这里是嵩山的芦崖瀑布。
LONG
  );

  /*
	set("objects", ([ /* sizeof() == 1 */
  "/d/quanzhou/npc/fighter-a" : 1,
  ]
)
);
* / set("outdoors", "songshan");
set("exits", ([ /* sizeof() == 2 */
  "southdown": __DIR__ "main-05",
  "northwest": __DIR__ "main-07",
]));
set("no_clean_up", 0);

setup();
replace_program(ROOM);
}
