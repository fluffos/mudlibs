inherit ITEM;

void create() {
  set_name("粗瓷茶壶", ({ "ci pot", "茶壶" }));
  set_weight(100);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "只");
    set("long", "一只手工非常粗糙的粗瓷茶壶。\n");
    set("value", 100);
  }
}
