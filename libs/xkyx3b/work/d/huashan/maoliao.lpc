inherit ROOM;

void create() {
  set("short", "茅屋");
  set("long", @LONG
    这里是一间很简陋的茅屋，里面除了一张简单的木床之外，没有任何东西。
但是让人看了有一种很整洁的感觉，应该是经常打扫的那种。一个老头正坐在门
口，一副无精打采的样子。
LONG
  );
  set("exits", ([
    "south": "/d/huashan/qingcao",
  ]));
  set("objects", ([
    __DIR__ "npc/master-feng": 1,
  ]));
  setup();
}
