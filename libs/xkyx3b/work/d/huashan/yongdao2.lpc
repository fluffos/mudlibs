inherit ROOM;

void create() {
  set("short", "甬 道");
  set("long", @LONG
    借著微弱的光亮，你看到一条黑漆漆的甬道向下延伸，不知是通向何
处地方。但是你摸了摸石壁，觉得这条甬道绝对不是天然形成的，应该是
人力所为。但是对于前方的未知，是生是死，只能听天由命LONG
        );
        set("exits", ([ 
	  "southup"    : __DIR__"yongdao1",
	  "northdown"    : __DIR__"hold",
]));
        set("objects", ([
                __DIR__"obj/stick1" : 1,
       ]) );
        setup();
}
