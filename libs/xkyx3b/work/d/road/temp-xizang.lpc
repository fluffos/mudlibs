// temp-xizang.c - 这里是还没有建好的西藏城

inherit ROOM;

string look_sign(object me);

void create() {
  set("short", "西藏城");
  set("long", @LONG

    这里是还没有建好的西藏城。
LONG
  );

  set("exits", ([
    "north": __DIR__ "xi-n01",
  ]));

  set("objects", ([
    "/d/xizang/npc/master-jing": 1,
    "/d/xizang/npc/master-jiu": 1,
  ]));

  setup();

}
