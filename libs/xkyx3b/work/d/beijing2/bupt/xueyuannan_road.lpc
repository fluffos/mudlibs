// xueyuannan_road.c -  学院南路

inherit ROOM;

void create() {
  set("short", "学院南路");
  set("long", @LONG
路上除了飞驰而过的“卟卟”声响的怪物以外，只有几个人在悠闲的闲逛。 
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "mingguangcun",
    "east": "/d/beijing2/nw_road5",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "beijing");
  setup();
  replace_program(ROOM);
}
