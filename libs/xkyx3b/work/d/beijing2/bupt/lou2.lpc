// zhuxi.c -  北邮

inherit ROOM;

void create() {
  set("short", "教二楼");
  set("long", @LONG
无线系主要的实验室都在教二楼。大鱼有 10 %的时间是在这里度过的，另外 90 %,xixi
宿舍和食堂嘛。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "up": __DIR__ "lou2-up2",
    "north": __DIR__ "s-road1",
  ]));
  set("no_clean_up", 0);
  //        set("outdoors", "beijing");
  setup();
  replace_program(ROOM);
}
