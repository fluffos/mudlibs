// nw_road2.c- 西单北大街

inherit ROOM;

void create() {
  set("short", "西单北大街");
  set("long", @LONG
这是北京城南北方向的主要街道，连接西单和新街口，也是北京的主要商业区。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "nw_road3",
    "south": __DIR__ "nw_road1",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "beijing");
  setup();
  replace_program(ROOM);
}
