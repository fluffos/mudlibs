#include "/players/qclxxiv/myroom.h"
#include "/players/qclxxiv/castle.loc"

init() {
  object obj;
  ::init();
}

exit(tp) {
  if (tp->query_chairsoul()) {
    tp->force_me("stand up");
  }
}

#ifdef TESTSITE
move(string str) {
#else
  int move(string str) {
#endif
    if (this_player()->query_chairsoul()) {
      this_player()->force_me("stand up");
      return 1;
    }
    return ::move(str);
  }
