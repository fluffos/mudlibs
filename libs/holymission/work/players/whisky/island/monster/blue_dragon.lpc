inherit "/players/whisky/obj/std_dragon";
#include "../island.h"

void reset(int flag) {
  ::reset(flag);
  if (flag == 0) {
    set_colour("blue");
    set_age(2 + random(7));
    clone_object(OBJ + "blue_star")->move_object(this_object());
  }
}
