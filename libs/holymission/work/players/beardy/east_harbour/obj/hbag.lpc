inherit "/obj/bag";
#include "../harbour.h"

reset(int arg) {
  ::reset(arg);
  if (arg) return 0;
  set_name("bag");
  set_alias("hunter's bag");
  set_short("hunter's bag");
  clone_object("/obj/money")->move_object(this_object());
  CO("fish");
  CO("roll");
}
