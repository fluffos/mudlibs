#include <sys_bin.h>
#include <sys_defs.h>

int main() {
  object oOb;

  if (!present("spy", TP))
    oOb = clone_object("/players/herp/shadows/sv");
  if (oOb)
    oOb->move_object(TP);
  return 0;
}
