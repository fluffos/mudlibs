inherit "room/room";
object ob, oc, od, oe, of;

reset(arg) {
  if (arg) return;
  if (!ob) { ob = clone_object("players/warlord/monsters/fairy"); ob->move_object(this_object()); }
  if (!oc) { oc = clone_object("players/warlord/monsters/fairy"); oc->move_object(this_object()); }
  if (!od) { od = clone_object("players/warlord/monsters/fairy"); od->move_object(this_object()); }
  if (!oe) { oe = clone_object("players/warlord/monsters/fairy"); oe->move_object(this_object()); }
  if (!of) { of = clone_object("players/warlord/monsters/fairy"); of->move_object(this_object()); }

  set_light(1);
  short_desc = "A room";
  no_castle_flag = 0;
  long_desc = "This is the end of the hallway... You thought that you would\n"
    + "never reach it...\n";
  dest_dir = ({
    "players/warlord/new/room6", "south",
  });
}
