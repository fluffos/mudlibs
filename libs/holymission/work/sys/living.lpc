// /sys/living -- the canonical "living" base class path (this is what
// players/kryll/tmp/sys/player.h's own `inherit "/sys/living";` and
// every #include "/sys/living.h" in this mudlib point at). Found
// completely EMPTY (0 bytes) in the preserved archive; the real
// implementation survives at doc/lib/living.lpc (3610 lines -- see that
// file and NOTES.md for the full explanation). Not lost content:
// recovered by inheriting it directly through this canonical redirect.
inherit "/doc/lib/living";
