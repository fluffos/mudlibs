// /sys/player -- the default player-character body object (cloned by
// secure/login.c for every new/returning login, see PLAYER_OB/DEFAULT).
// This file was found completely EMPTY (0 bytes) in the preserved
// archive -- but the real, substantial (4290-line) implementation
// still exists in this same archive at doc/lib/player.lpc: a genuine,
// standalone player class (its own top-level `inherit "secure/valid"`,
// full stat/combat/movement/social-command wiring), not a fragment.
// Not lost content: recovered by inheriting it directly, matching the
// exact same situation as obj/monster.c/obj/mon.c and their
// doc/lib/living.lpc base (see those files and NOTES.md).
inherit "/doc/lib/player";
