###############################################################################
#              Customizable runtime config file for MudOS v22                 #
###############################################################################

# name of this mud
name : Holy Mission

# port number to accept users on
port number : 40260

# absolute pathname of mudlib
mudlib directory : /home/sunyc/src/mudlib/libs/holymission/work

# debug.log and author/domain stats are stored here
log directory : /log

# the directories which are searched by #include <...>
include directories : /include:/std:/obj:/sys:/sys/include:/room:/guild:/race:/spells:/abilities:/religion:/boards

# The global include file is included automatically before every compile.
# This archive's own real auto-include file (originally referenced as
# /include/include.h -- see NOTES.md).
global include file : "/include/include.h"

# Directory to save binaries in.  (if BINARIES is defined)
save binaries directory : /binaries

# the file which defines the master object (real archive content --
# secure/master.c, an LPMud-3.0-era master, see NOTES.md)
master file : /secure/master

# the file where all global simulated efuns are defined (real archive
# content -- secure/simul_efun.c)
simulated efun file : /secure/simul_efun

# alternate debug.log file name (assumed to be in specified 'log directory')
debug log file : debug.log

time to clean up : 1200
time to swap : 0
time to reset : 1800
maximum bits in a bitfield : 1200
maximum local variables : 35
maximum evaluation cost : 5000000
maximum array size : 64000
maximum buffer size : 400000
maximum mapping size : 15000
inherit chain size : 30
maximum string length : 200000
maximum read file size : 200000
maximum byte transfer : 10000
hash table size : 7001
object table size : 4001
default fail message : What?
default error message : Something is wrong.

maximum users : 40
evaluator stack size : 1000
compiler stack size : 200
maximum call depth : 30
living hash table size : 100
