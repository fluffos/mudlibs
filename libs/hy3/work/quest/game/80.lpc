#include <ansi.h>

inherit WQA_ROOM;

void create()
{
	set("short", "现  实");
 	set("long", @LONG
								
    这里是现实时空。嘿嘿嘿....
LONG
	);
// Key: 1999-03-02 add
	set("objects", ([
		__DIR__"npc/master-wqa": 1,
	]) );
// end
        ::create();
}

/*
// Key: 1999-03-02 改为让wqa控制转换
void greeting(object ob)
{
        int i, n;

        if( !ob || environment(ob) != this_object() ) return;

        message_vision( HIW "\n梦灵对$N说道：欢迎来到现实世界。\n" NOR, ob);
        message_vision( HIW "梦灵对$N说道：只有在这里，你才能得到自己真正想要的东西。\n\n" NOR, ob);

        i = ob->query_temp("wqa's room/point");
        if( i <= 0 ) i = 1;
        n = i / 2 + random( i / 2 + 1 );

        tell_object( ob, HIW "梦灵对你说：你现在总共有" 
                + chinese_number(i) + "点社会声望。\n" NOR );
        tell_object( ob, HIW "梦灵对你说：它可以转化为" 
                + chinese_number(n) + "点社会经验。\n" NOR );
        tell_object( ob, HIW "梦灵对你说：而且我再送你" 
                + chinese_number(i/2+1) + "点社会声望。\n" NOR );

        ob->add("combat_exp", n);
        ob->set_temp("wqa's room/point", i / 2 + 1);
}
*/
