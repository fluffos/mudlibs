// THIS IS PLAYER'S OWN SKILL (write by maxim@nju_fengxue)
// lian
// 狂热子
// 星宿派第二代弟子
// 4300067
// 男性
// finger
// 无极金刚指





inherit SKILL;

////////////////////////////////

mapping *action = ({

  // ZHAOSHI :0

  ([
    "action": "$N手指直次向$n",
    "force": 50,
    "damage": 300,
    "dodge": 10,
    "damage_type": "刺伤",
    "lvl": 0,
    "skill_name": "直刺"
  ]),
  // ZHAOSHI :1

  ([
    "action": "$N右手食指暗暗运劲,然后轻轻一弹,一道金光横扫向$n的腰部",
    "force": 80,
    "dodge": -10,
    "damage_type": "刺伤",
    "damage": 500,
    "lvl": 12,
    "skill_name": "横扫千军"
  ]),
  // ZHAOSHI :2

  ([
    "action": "$N气凝丹田,一股热气冲小指间射出,一式当葡n的覃中大穴",
    "force": 100,
    "dodge": -20,
    "damage_type": "瘀伤",
    "lvl": 20,
    "skill_name": "敌我同归"
  ]),
  // ZHAOSHI :3

  ([
    "action": "$N双手齐出,十道剑气一齐射向$n,顿时,剑气横飞,把$n的前,后,左,右都包围起来",
    "force": 130,
    "dodge": -30,
    "damage_type": "瘀伤",
    "lvl": 30,
    "skill_name": "杀人式"
  ]),
  // ZHAOSHI :4

  ([
    "action": "$N食指对天,一道剑气射向空中,化成数千道,纷纷落想地面,突然间转向$n",
    "force": 150,
    "dodge": -50,
    "damage_type": "瘀伤",
    "lvl": 40,
    "skill_name": "天地共灭"
  ]),
});
// ZHAOSHI :5
int valid_learn(object me) {
  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练" + "无极金刚指" + "必须空手。\n");
  if ((int)me->query("max_neili") < 50)
    return notify_fail("你的内力太弱，无法练" + "无极金刚指" + "。\n");
  return 1;
}
int valid_enable(string usage) { return usage == "finger" || usage == "parry"; }
string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}
mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("wuji-finger", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level > action[i - 1]["lvl"])
      return action[NewRandom(i, 20, level / 5)];
}
int practice_skill(object me) {
  if ((int)me->query("qi") < 25)
    return notify_fail("你的体力不够了，休息一下再练吧。\n");
  if ((int)me->query("neili") < 3)
    return notify_fail("你的内力不够了，休息一下再练吧。\n");
  me->receive_damage("qi", 25);
  me->add("neili", -3);
  return 1;
}  //total 34 lines!
