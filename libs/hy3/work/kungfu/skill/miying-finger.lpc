// THIS IS PLAYER'S OWN SKILL (write by maxim@nju_fengxue)
// manooo
// 牙神幻十郎
// [1;36m全真教重阳真人传人[2;37;0m
// 2885182
// 男性
// finger
// 梅莺指法





inherit SKILL;

////////////////////////////////

mapping *action = ({

// ZHAOSHI :0

([
"action" :"$N左手防住攻击，右手以手代刀，劈出「梅莺斩」，直向$n的头顶劈去。",
"force" :50,
"dodge" :10,
"damage_type": "瘀伤",
"lvl" : 0,
"skill_name" : "梅莺斩"
]),
// ZHAOSHI :1

([
"action" :"$N突然掌化为指，一式「梅莺乱」，指间放出万道气剑，分别攻向$n全身的十八死穴。",
"force" :80,
"dodge" :-10,
"damage_type": "瘀伤",
"lvl" : 12,
"skill_name" : "梅莺乱"
]),
// ZHAOSHI :2

([
"action" :"$N纵身一跃，一式「梅莺断肠」，指尖的一丝罡气竟脱手而出，只指$n的腰间。",
"force" :100,
"dodge" :-20,
"damage_type": "瘀伤",
"lvl" : 20,
"skill_name" : "梅莺断肠"
]),
 });
// ZHAOSHI :3
int valid_learn(object me)
{
	if( me->query_temp("weapon") || me->query_temp("secondary_weapon") )
		return notify_fail("练"+"梅莺指法"+"必须空手。\n");
	if( (int)me->query("max_neili") < 50 )
		return notify_fail("你的内力太弱，无法练"+"梅莺指法"+"。\n");
	return 1;
}
int valid_enable(string usage) { return usage== "finger" || usage=="parry"; }
string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}
mapping query_action(object me, object weapon)
{
        int i, level;
    level = (int)me->query_skill("miying-finger",1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
}
int practice_skill(object me)
{
	if( (int)me->query("qi") < 25 )
		return notify_fail("你的体力不够了，休息一下再练吧。\n");
	if( (int)me->query("neili") < 3 )
		return notify_fail("你的内力不够了，休息一下再练吧。\n");
	me->receive_damage("qi", 25);
	me->add("neili", -3);
	return 1;
} //total 34 lines!
