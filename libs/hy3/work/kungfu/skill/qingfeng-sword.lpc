// THIS IS PLAYER'S OWN SKILL (write by maxim@nju_fengxue)
// mooner
// 月儿
// 以彼之道 还施彼身
// 2845766
// 女性
// sword
// 轻风细雨剑





inherit SKILL;

////////////////////////////////

mapping *action = ({

// ZHAOSHI :0

([
"action" :"$N轻身而起,拂剑低吟,手中$w如轻风拂面,飘忽无定.在$n恍惚间,已向他的$l斜斜刺出.",
"force" :50,
"dodge" :20,
"damage_type": "刺伤",
"lvl" : 0,
"skill_name" : "轻风无尽处"
]),
// ZHAOSHI :1

([
"action" :"$N泪眼迷离,回剑向自己的颈间,在小鱼错愕间,$w一摆,一式「细雨润无声」已悄然递出.",
"force" :80,
"dodge" :10,
"damage_type": "刺伤",
"lvl" : 12,
"skill_name" : "细雨润无声"
]),
// ZHAOSHI :2

([
"action" :"$N黯然一叹,眉头轻躇,仿佛想起心上人的远离,手中$w一式「风雨夜归人」对准$n的$l飘然一刺.",
"force" :100,
"dodge" :10,
"damage_type": "刺伤",
"lvl" : 20,
"skill_name" : "风雨夜归人"
]),
// ZHAOSHI :3

([
"action" :"$N举首向天,对着明月长叹,寸寸相思愁华年.$n不由一呆,$w已疾然送出.",
"force" :130,
"dodge" :5,
"damage_type": "刺伤",
"lvl" : 30,
"skill_name" : "相思明月中"
]),
// ZHAOSHI :4

([
"action" :"$N飘然后退,举剑向天,希望长空皓月可以将$N的心上人找回,手中$w一式「明月照人还」已然如狂风暴雨般刺出.",
"force" :150,
"dodge" :-5,
"damage_type": "刺伤",
"lvl" : 40,
"skill_name" : "明月照人还"
]),
 });
// ZHAOSHI :5
int valid_learn(object me)
{
object weapon; if (!objectp(weapon = me->query_temp("weapon"))
     || (string)weapon->query("skill_type") != "sword") return notify_fail("你使用的武器不对。\n");
	if( (int)me->query("max_neili") < 50 )
		return notify_fail("你的内力太弱，无法练"+"轻风细雨剑"+"。\n");
	return 1;
}
int valid_enable(string usage) { return usage=="sword" || usage=="parry"; }
string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}
mapping query_action(object me, object weapon)
{
        int i, level;
    level = (int)me->query_skill("qingfeng-sword",1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
}
int practice_skill(object me)
{
object weapon; if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "sword") return notify_fail("你使用的武器不对。\n");	if( (int)me->query("qi") < 25 )
		return notify_fail("你的体力不够了，休息一下再练吧。\n");
	if( (int)me->query("neili") < 3 )
		return notify_fail("你的内力不够了，休息一下再练吧。\n");
	me->receive_damage("qi", 25);
	me->add("neili", -3);
	return 1;
} //total 34 lines!
