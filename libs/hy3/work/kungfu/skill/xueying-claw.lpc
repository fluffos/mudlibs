// THIS IS PLAYER'S OWN SKILL (write by maxim@nju_fengxue)
// wrong
// 无过子
// [1;36m第十九代武林盟主[2;37;0m
// 3650640
// 男性
// claw
// 邪影神爪





inherit SKILL;

////////////////////////////////

mapping *action = ({

  // ZHAOSHI :0

  ([
    "action": "$N大吼一声,无过救我,一式「无过转世」,只见无过邪影闪过,$n以被死死缠住,攻向$n死穴!",
    "force": 50,
    "dodge": 20,
    "damage_type": "瘀伤",
    "lvl": 0,
    "skill_name": "无过转世"
  ]),
  // ZHAOSHI :1

  ([
    "action": "$N住足停步,使出一式「过行九天」,天空中闪过一道邪影,$n眼前一黑,$N双爪锁向$n的咽喉.",
    "force": 80,
    "dodge": 10,
    "damage_type": "瘀伤",
    "lvl": 12,
    "skill_name": "过行九天"
  ]),
  // ZHAOSHI :2

  ([
    "action": "$N停步住足 ,双爪齐出,一使「过行九天」,只见一道邪影闪过,好似一个魔影,$n眼前一花,$N趁势功向$n各处死穴.",
    "force": 100,
    "dodge": 10,
    "damage_type": "瘀伤",
    "lvl": 20,
    "skill_name": "过行九天"
  ]),
  // ZHAOSHI :3

  ([
    "action": "$N纵身一跃,双爪若有若无,一式「归真反璞」,爪中加着两道邪光,攻向$n,$n以笼罩在$N的爪影之下!",
    "force": 130,
    "dodge": 5,
    "damage_type": "瘀伤",
    "lvl": 30,
    "skill_name": "归真反璞"
  ]),
  // ZHAOSHI :4

  ([
    "action": "$N对$n看都不看,两道邪光直冲向$n,正是一式「目中无人」,只见$n目瞪口呆,不知所措!",
    "force": 150,
    "dodge": -5,
    "damage_type": "瘀伤",
    "lvl": 40,
    "skill_name": "目中无人"
  ]),
});
// ZHAOSHI :5
int valid_learn(object me) {
  //line number must be same!!!
  //so it is done
  if ((int)me->query("max_neili") < 50)
    return notify_fail("你的内力太弱，无法练" + "邪影神爪" + "。\n");
  return 1;
}
int valid_enable(string usage) { return usage == "claw" || usage == "parry"; }
string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}
mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("xueying-claw", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level > action[i - 1]["lvl"])
      return action[NewRandom(i, 20, level / 5)];
}
int practice_skill(object me) {
  if ((int)me->query("qi") < 25)
    return notify_fail("你的体力不够了，休息一下再练吧。\n");
  if ((int)me->query("neili") < 3)
    return notify_fail("你的内力不够了，休息一下再练吧。\n");
  me->receive_damage("qi", 25);
  me->add("neili", -3);
  return 1;
}  //total 34 lines!
