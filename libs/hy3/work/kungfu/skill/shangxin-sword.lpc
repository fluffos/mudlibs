// THIS IS PLAYER'S OWN SKILL (write by maxim@nju_fengxue)
// linchu
// 林楚风
// 桃花岛第二代弟子
// 3025277
// 女性
// sword
// 伤心断肠剑





inherit SKILL;

////////////////////////////////

mapping *action = ({

  // ZHAOSHI :0

  ([
    "action": "$N单手掩面,泪眼若泣,$n顿起怜香惜玉之心,手下不由一缓,$w一式「相思无尽处」已悄然递出.",
    "force": 50,
    "dodge": 20,
    "damage_type": "刺伤",
    "lvl": 0,
    "skill_name": "相思无尽处"
  ]),
  // ZHAOSHI :1

  ([
    "action": "$N神色黯然,伤心欲绝.$w一招「伤心天地间」夹着萧瑟之气,对准$n的$l缓缓刺出.",
    "force": 80,
    "dodge": 10,
    "damage_type": "刺伤",
    "lvl": 12,
    "skill_name": "伤心天地间"
  ]),
  // ZHAOSHI :2

  ([
    "action": "$N仰首向天,对月浩叹.心中断肠之痛借手中$w之威,一式「断肠明月中」带着足以令天地动容之力,向$n疾然击出.",
    "force": 100,
    "dodge": 10,
    "damage_type": "刺伤",
    "lvl": 20,
    "skill_name": "断肠明月中"
  ]),
  // ZHAOSHI :3

  ([
    "action": "$N痛哭失声,仿佛想起以远离的心上人,一招「阴阳殊途」充满哀怨之气,$w对准$n的$l疯狂疾刺.",
    "force": 130,
    "dodge": 5,
    "damage_type": "刺伤",
    "lvl": 30,
    "skill_name": "阴阳殊途"
  ]),
  // ZHAOSHI :4

  ([
    "action": "$N回剑自戮,望能与心上任永世不分,$n顿时呆住,$w一式「天人永隔」突然转向刺出,天地间霎时弥漫萧杀之气.",
    "force": 150,
    "dodge": -5,
    "damage_type": "刺伤",
    "lvl": 40,
    "skill_name": "天人永隔"
  ]),
  // ZHAOSHI :5

  ([
    "action": "$N一脸茫然,凝视剑尖.有意无意间,$w一式「谁知销魂意」对准$n的$l飘然刺出.",
    "force": 180,
    "dodge": -5,
    "damage_type": "刺伤",
    "lvl": 50,
    "skill_name": "伤心断肠剑"
  ]),
});
// ZHAOSHI :6
int valid_learn(object me) {
  object weapon; if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "sword") return notify_fail("你使用的武器不对。\n");
  if ((int)me->query("max_neili") < 50)
    return notify_fail("你的内力太弱，无法练" + "伤心断肠剑" + "。\n");
  return 1;
}
int valid_enable(string usage) { return usage == "sword" || usage == "parry"; }
string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}
mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("shangxin-sword", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level > action[i - 1]["lvl"])
      return action[NewRandom(i, 20, level / 5)];
}
int practice_skill(object me) {
  object weapon; if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "sword") return notify_fail("你使用的武器不对。\n"); if ((int)me->query("qi") < 25)
    return notify_fail("你的体力不够了，休息一下再练吧。\n");
  if ((int)me->query("neili") < 3)
    return notify_fail("你的内力不够了，休息一下再练吧。\n");
  me->receive_damage("qi", 25);
  me->add("neili", -3);
  return 1;
}  //total 34 lines!
