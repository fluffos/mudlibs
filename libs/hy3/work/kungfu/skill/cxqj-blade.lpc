// THIS IS PLAYER'S OWN SKILL (write by maxim@nju_fengxue)
// emjwh
// 叶知秋
// 浣花剑派第四代弟子
// 2839449
// 女性
// blade
// 长笑七击





inherit SKILL;

////////////////////////////////

mapping *action = ({

  // ZHAOSHI :0

  ([
    "action": "$N双掌长空一抡，双手忽然发出一淡若紫金的奇光，平滑加刀，在一旁的高手瞧在眼里，脱口讶道：“西藏紫金手！”",
    "force": 50,
    "dodge": 20,
    "damage_type": "割伤",
    "lvl": 0,
    "skill_name": "紫金手"
  ]),
  // ZHAOSHI :1

  ([
    "action": "$N倏然抽手，变拳，竟是“达摩秘拳”.达摩秘拳”真正能练成的人，只是南北二宗少林寺掌门二有而已，而$N所使的竟然就是“达摩秘拳”！",
    "force": 80,
    "dodge": 10,
    "damage_type": "割伤",
    "lvl": 12,
    "skill_name": "达摩秘拳"
  ]),
  // ZHAOSHI :2

  ([
    "action": "好个叶知秋，竟不慌不忙，一个后弯腰：“柳丝折”，但$n尖急随伏下，$N一扬袖，“长笑七击”之弟三击“东海水云袖”一遮，剑划在袖上，居然发出“丝丝”之声，铁剑竟刺不破软袖！    ",
    "force": 100,
    "dodge": 10,
    "damage_type": "割伤",
    "lvl": 20,
    "skill_name": "东海水云袖"
  ]),
  // ZHAOSHI :3

  ([
    "action": "$n一呆，$N已然弹起，长空冲起，十指连弹，一进之间，漫空布满“丝丝”之声，直袭$n！",
    "force": 130,
    "dodge": 5,
    "damage_type": "割伤",
    "lvl": 30,
    "skill_name": "长天神指"
  ]),
  // ZHAOSHI :4

  ([
    "action": "$N怪叫一声，如鹰击长空，一沉一起，双掌一拍，竟是“大石神功”！翻过$n“大石神功”一出，刺向$N的剑锋给狂飓激偏了之头顶",
    "force": 150,
    "dodge": -5,
    "damage_type": "割伤",
    "lvl": 40,
    "skill_name": "大石神功"
  ]),
});
// ZHAOSHI :5
int valid_learn(object me) {
  object weapon; if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "blade") return notify_fail("你使用的武器不对。\n");
  if ((int)me->query("max_neili") < 50)
    return notify_fail("你的内力太弱，无法练" + "长笑七击" + "。\n");
  return 1;
}
int valid_enable(string usage) { return usage == "blade" || usage == "parry"; }
string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}
mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("cxqj-blade", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level > action[i - 1]["lvl"])
      return action[NewRandom(i, 20, level / 5)];
}
int practice_skill(object me) {
  object weapon; if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "blade") return notify_fail("你使用的武器不对。\n"); if ((int)me->query("qi") < 25)
    return notify_fail("你的体力不够了，休息一下再练吧。\n");
  if ((int)me->query("neili") < 3)
    return notify_fail("你的内力不够了，休息一下再练吧。\n");
  me->receive_damage("qi", 25);
  me->add("neili", -3);
  return 1;
}  //total 34 lines!
