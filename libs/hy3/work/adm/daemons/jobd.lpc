#include <ansi.h>
#include <command.h>
#pragma save_binary

string *roomlines = ({
  "/d/baituo/bridge",
  "/d/baituo/cao1",
  "/d/baituo/cao2",
  "/d/baituo/caomeide",
  "/d/baituo/caoping",
  "/d/baituo/cave",
  "/d/baituo/cave1",
  "/d/baituo/cedong",
  "/d/baituo/chaifang",
  "/d/baituo/changlang",
  "/d/baituo/chufang",
  "/d/quanzhou/qzroad3",
  "/d/quanzhou/qzroad4",
  "/d/quanzhou/qzroad5",
  "/d/quanzhou/seascape",
  "/d/quanzhou/tieqiang",
  "/d/quanzhou/well",
  "/d/quanzhou/well1",
  "/d/quanzhou/xijie",
  "/d/quanzhou/yanyu",
  "/d/quanzhou/yucun",
  "/d/quanzhou/yucun1",
  "/d/quanzhou/zahuopu",
  "/d/quanzhou/zhongxin",
  "/d/quanzhou/zhulin",
  "/d/baituo/damen",
  "/d/baituo/datiepu",
  "/d/baituo/dating",
  "/d/baituo/dongjie",
  "/d/baituo/dongkou",
  "/d/wudang/chaotian",
  "/d/wudang/chashi",
  "/d/wudang/donglang1",
  "/d/wudang/donglang2",
  "/d/wudang/feng.h",
  "/d/wudang/fuzhen1",
  "/d/wudang/fuzhen2",
  "/d/baituo/dongnei",
  "/d/baituo/fang",
  "/d/baituo/fende",
  "/d/baituo/gebi",
  "/d/baituo/guangchang",
  "/d/baituo/houmen",
  "/d/baituo/dating",
  "/d/baituo/dongjie",
  "/d/baituo/dongkou",
  "/d/baituo/dongnei",
  "/d/baituo/fang",
  "/d/baituo/fende",
  "/d/baituo/gebi",
  "/d/baituo/guangchang",
  "/d/baituo/houmen",
  "/d/baituo/houyuan",
  "/d/quanzhen/baoquan",
  "/d/quanzhen/baoziyan",
  "/d/quanzhen/baxiandian",
  "/d/quanzhen/bingdong",
  "/d/quanzhen/bwc",
  "/d/quanzhen/cangjingge",
  "/d/quanzhen/cangjingge1",
  "/d/quanzhen/cangjingge2",
  "/d/quanzhen/chexiangtan",
  "/d/quanzhen/chufang",
  "/d/quanzhen/chufang2",
  "/d/baituo/huayuan",
  "/d/baituo/jiudian",
  "/d/baituo/liangong",
  "/d/baituo/menlang",
  "/d/baituo/midao",
  "/d/baituo/mumen",
  "/d/baituo/neishi",
  "/d/baituo/nongshe",
  "/d/baituo/ouyangfeng",
  "/d/baituo/restroom",
  "/d/baituo/room1",
  "/d/baituo/room2",
  "/d/baituo/room3",
  "/d/baituo/shanlu",
  "/d/baituo/shanlu1",
  "/d/baituo/shijie",
  "/d/baituo/shulin",
  "/d/baituo/shulin1",
  "/d/baituo/storeroom",
  "/d/baituo/tuyuan",
  "/d/baituo/shanlu",
  "/d/baituo/shanlu1",
  "/d/baituo/shijie",
  "/d/baituo/shulin",
  "/d/baituo/shulin1",
  "/d/baituo/storeroom",
  "/d/baituo/tuyuan",
  "/d/baituo/wuqiku",
  "/d/baituo/xiaolu1",
  "/d/baituo/xiaolu2",
  "/d/city2/aobai8",
  "/d/city2/aobai9",
  "/d/city2/bingqiku",
  "/d/city2/bingyin1",
  "/d/city2/bingyin2",
  "/d/city2/bridge",
  "/d/city2/bridge2",
  "/d/city2/bridge3",
  "/d/city2/ceting",
  "/d/city2/chaguan",
  "/d/city2/chaoyan1",
  "/d/city2/chaoyan2",
  "/d/city2/chaoyang",
  "/d/city2/dangpu",
  "/d/baituo/xiaolu3",
  "/d/baituo/xiaolu4",
  "/d/baituo/xijie",
  "/d/baituo/ximen",
  "/d/baituo/yaofang",
  "/d/baituo/yuanzi",
  "/d/baituo/zhailuo",
  "/d/baituo/zhulin",
  "/d/baituo/zhuyuan",
  "/d/city/bingyin",
  "/d/city/bingyindamen",
  "/d/city/bite",
  "/d/city/caizhu",
  "/d/city/cangku",
  "/d/city/chaguan",
  "/d/city/chat-dc1",
  "/d/city/chat-dc2",
  "/d/city/chat-dc3",
  "/d/city/chat-dc4",
  "/d/city/chat-dc5",
  "/d/city/chatroom",
  "/d/city/chatroom2",
});

string *quests = ({
  "/d/obj/quest/tieluohan",
  "tie luohan",
  "/d/obj/quest/kelanjing",
  "kelan ji",
  "/d/obj/quest/shihuifen",
  "shihui fen",
  "/d/obj/quest/qianjinqiu",
  "qianjin qiu",
  "/d/obj/quest/luomei",
  "luo mei",
  "/d/obj/quest/baijiafan",
  "baijia fan",
  "/d/obj/quest/datiechui",
  "datie chui",
  "/d/obj/quest/qixinhaitang",
  "qixin haitang",
  "/d/obj/quest/qingxinzhou",
  "qingxin zhou",
  "/d/obj/quest/yanyuqin",
  "yanyu qin",
  "/d/obj/quest/yuanyangdao",
  "yuanyang dao",
  "/d/obj/quest/liqiwan",
  "li qiwan",
  "/d/obj/quest/fenghuang",
  "biyu fenghuang",
  "/d/obj/quest/huamijiu",
  "huami jiu",
  "/d/obj/quest/gutengbei",
  "guteng bei",
  "/d/obj/quest/zhangben",
  "renpi zhangben",
  "/d/obj/quest/zhubi",
  "zhu bi",
  "/d/obj/quest/zhen",
  "xiuhua zhen",
  "/d/obj/quest/zhang",
  "ganmian zhang",
  "/d/obj/quest/yandou",
  "yan dou",
  "/d/obj/quest/bingdai",
  "bing dai",
  "/d/obj/quest/nang",
  "san zijing",
  "/d/obj/quest/pai",
  "zhao pai",
  "/d/obj/quest/qunzi",
  "hua qunzi",
  "/d/obj/quest/shamao",
  "mao zi",
  "/d/obj/quest/sheng",
  "suoming sheng",
  "/d/obj/quest/shuihu",
  "shui hu",
  "/d/obj/quest/tanzhu",
  "tan zhu",
  "/d/obj/quest/taojian",
  "mutao jian",
  "/d/obj/quest/wang",
  "yu wang",
});

mapping maps = ([
  "d/wudang": "武当山",
  "d/baituo": "白驼山",
  "d/quanzhen": "全真",
  "d/guanwai": "关外",
  "d/city": "扬州",
  "d/city2": "北京城",
  "d/quanzhou": "泉州",
]);

varargs void init_dynamic_quest(int hard);
varargs int spread_quest(string one_quest, int hard);
object find_env(object ob);
varargs int already_spreaded(string str, int hard);
int quest_reward(object me, object who, object quest_item);
int locate_quest(object me, string arg);
string dyn_quest_list();
string query_city(object loc);

void create() {
  CRON_D->set_last_hard_dis();
  init_dynamic_quest(1);
  seteuid(getuid());
}

int quest_reward(object me, object who, object quest_item) {
  int exp, pot, scor, score;

  if (!objectp(who) || !objectp(quest_item)) return 0;

  if (who->query("name") != quest_item->query("owner_name")) return 0;
  exp = random(100) + ((me->query("int")) * random(10));

  pot = exp / 5;
  scor = random(exp) / 2 + 30;
  me->add("combat_exp", exp);
  me->add("potential", pot);
  me->add("score", scor);
  tell_object(
    me,
    sprintf(
      "你获得了" HIR "%s" NOR "的经验值 ," HIY "%s" NOR "点潜能," HIY "%s" NOR "点综合评价\n" NOR,
      chinese_number(exp),
      chinese_number(pot),
      chinese_number(scor)
    )
  );
  CHANNEL_D->do_channel(
    this_object(),
    "rumor",
    sprintf(HIG + me->name() + NOR "将寻到的" HIW + quest_item->name() + NOR "送给" + who->name() + "获得" HIR + chinese_number(exp) + NOR "的经验，" HIY + chinese_number(pot) + NOR "点潜能" HIY + chinese_number(scor) + "点综合评价\n" NOR)
  );
  score = me->query_temp("dynamic_find");
  if (score + 1 == 10) {
    me->delete_temp("dynamic_find");
    me->add("combat_exp", 100);
    tell_object(me, "你真行，你得到了" + chinese_number(100) + "点武学经验的附加奖励！\n");
  } else me->set_temp("dynamic_find", score + 1);

  return 1;
}

varargs void init_dynamic_quest(int hard) {
  int i;
  for (i = 0; i < sizeof(quests) / 2; i++)
    spread_quest(quests[i * 2], hard);
}

varargs int spread_quest(string quest, int hard) {
  object obj0, obj1, obj2, obj3;
  object cur_obj;
  object next_obj;
  object tar;
  object *inv;
  object *target = ({});
  int i;
  string location;
  if (already_spreaded(quest, hard)) return 0;
  reset_eval_cost();
  location = roomlines[random(sizeof(roomlines))];
  obj0 = find_object(location);
  if (obj0)
    obj0->reset();
  else
    obj0 = load_object(location);
  cur_obj = obj0;
  if (cur_obj) {
    inv = all_inventory(cur_obj);
    for (i = 0; i < sizeof(inv); i++) {
      if (inv[i]->is_character() && !userp(inv[i]))
        target += ({ inv[i] });
      if (inv[i]->is_container()) target += ({ inv[i] });
    }
  }
  if (sizeof(target)) cur_obj = target[random(sizeof(target))];
  if (cur_obj) {
    tar = new(quest);
    tar->set("value", 0);
    tar->move(cur_obj);
  }
  return 1;
}

string dyn_quest_list() {
  string output;
  object item;
  int i;

  output = "";
  for (i = 0; i < sizeof(quests) / 2; i++) {
    if (!objectp(item = find_object(quests[i * 2])))
      item = load_object(quests[i * 2]);
    if (already_spreaded(quests[i * 2]))
      if (random(2)) output += sprintf(
        HIG "%10s" NOR "的" HIW "%10s" NOR "（%s）\n",
        item->query("owner_name"),
        item->query("name"),
        item->query("id")
      );
    else output = sprintf(
      HIG "%10s" NOR "的" HIW "%10s" NOR "（%s）\n",
      item->query("owner_name"),
      item->query("name"),
      item->query("id")
    ) + output;
    else if (random(2)) output += sprintf(
      HIG "%10s" NOR "的" HIY "%10s" NOR "（%s） [1;31m【  [32m已完成[31m 】[m\n",
      item->query("owner_name"),
      item->query("name"),
      item->query("id")
    );
    else output = sprintf(
      HIG "%10s" NOR "的" HIY "%10s" NOR "（%s）[1;31m 【 [32m 已完成[31m  】[m\n",
      item->query("owner_name"),
      item->query("name"),
      item->query("id")
    ) + output;
  }
  output = "[1;32m     ※[31m火云新手使命榜[32m※[m\n" +
    "━━━━━━━━━━━━━━━━━━━━━━━━━━━\n" + output +
    "━━━━━━━━━━━━━━━━━━━━━━━━━━━\n\n";

  return output;
}

varargs int already_spreaded(string str, int hard) {
  object ob, *ob_list;
  int i;
  if (!str) return 0;
  if (hard) {
    ob_list = children(str);
    for (i = 0; i < sizeof(ob_list); i++) {
      ob = find_env(ob_list[i]);
      if (ob) {
        ob_list[i]->move(VOID_OB);
        destruct(ob_list[i]);
      }
    }
    return 0;
  } else {
    if (!str) return 0;
    ob_list = children(str);
    for (i = 0; i < sizeof(ob_list); i++) {
      ob = find_env(ob_list[i]);
      if (ob)
        return 1;
    }
    return 0;
  }
}

object find_env(object ob) {
  while (ob) {
    if (ob->query("exits")) return ob;
    else ob = environment(ob);
  }
  return ob;
}

int locate_quest(object me, string arg) {
  object loc;
  string qloc, msg, quest;
  string *exlst;
  object *itemlist;
  mapping exits;
  int index, i;

  if (!arg)
    return notify_fail("zhao <物品id>\n");
  if (present(arg, me))
    return notify_fail("它不正在你的身上吗？\n");

  if ((index = member_array(arg, quests, 0)) > 0) quest = quests[index - 1];
  else return 1;

  if (!already_spreaded(quest)) {
    tell_object(me, HIW "藏宝图" NOR "一点反应也没有，仿佛它根本不存在这个世界上。\n");
    return 0;
  }

  itemlist = children(quest);
  for (index = 0; index < sizeof(itemlist); index++) {
    if (objectp(loc = find_env(itemlist[index]))) {
      qloc = query_city(loc);
      exits = loc->query("exits");
      exlst = keys(exits);
      msg = HIW + itemlist[index]->query("name") + NOR "仿佛在" HIG + qloc + NOR "一处有";
      if (sizeof(exlst)) {
        for (i = 0; i < sizeof(exlst) - 1; i++) msg += exlst[i] + "、";
        msg += exlst[sizeof(exlst) - 1] + "出口的地方。\n";
        tell_object(me, HIW "藏宝图" NOR "显示：\n" + msg);
        return 1;
      }
    }
  }
  tell_object(me, HIW "藏宝图" NOR "一点反应也没有。\n");
  return 1;
}

string query_city(object loc) {
  string filename, filename1;
  int len;

  filename = file_name(loc);
  len = strlen(filename);
  if (len < 2) return "???";

  len--;
  while (filename[len] != '/' && len > 0) {
    len--;
  }
  if (len < 2) return "???";

  filename1 = filename[1..(len - 1)];
  return maps[filename1];
}
