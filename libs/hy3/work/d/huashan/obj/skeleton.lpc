inherit ITEM;
void create() {
  set_name("干枯的尸骸", ({ "skeleton" }));
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "这是一具干枯的尸骸，也不知道是何人留在这里的。\n");
    set("unit", "具");
    set("value", 0);
    set("no_get", 1);
  }

  setup();
}
