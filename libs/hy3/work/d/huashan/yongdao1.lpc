inherit ROOM;

void create()
{
        set("short", " 甬道入口");
        set("long", @LONG
    小洞里面是一条狭窄的天生甬道，其实是山腹内的一条裂
缝。七拐八折地不知道通到哪里。
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
                "out" : __DIR__"yongdao",
                "northup" : __DIR__"yongdao2",
        ]));
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

