inherit ROOM;

void create() {
  set("short", "山洞");
  set("long", @LONG
一个腥臭的山洞，抬目望去，一只斑斓大猛虎向你扑过来，山洞的侧面
有一个小洞，仿佛有着更凶猛的野兽在低啸，还是不要进去的好。
LONG
  );
  set("objects", ([
    "/d/dali/npc/laohu": 2,
  ]));

  set("exits", ([ /* sizeof() == 1 */
    "enter": "/d/dali/gaolishan2",
    "out": __DIR__ "shanyao",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
