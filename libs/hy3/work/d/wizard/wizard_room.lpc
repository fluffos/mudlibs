// Room: /d/wizard/wizard_room.c
// Data: 小子(ran)
// Time: 公元：2000年10月14日，北京时间：16时48分15秒。

inherit ROOM;

void create()
{
        set("short", "[1;36m巫师休息室[2;37;0m");
        set("long", @LONG
这里就是「[1;33m火云[2;37;0m」巫师的休息所在，刚粉刷过的四周墙壁上光秃秃
的什么都没有，屋子的正中央放着一张楠木桌，上面摆着一本巫师留言簿
(board)。
LONG
        );
        set("valid_startroom", 1);
        set("coor", ([ /* sizeof() == 3 */
  "y" : 30,
  "x" : -20,
  "z" : 10,
]));
        set("no_fight", "1");
        set("exits", ([ /* sizeof() == 5 */
  "down" : __DIR__"guest_room",
  "east" : __DIR__"fyting",
   "south" : __DIR__"news_room",
  "up" : __DIR__"arch_room",
]));
        set("no_clean_up", 0);

        setup();

          call_other("/clone/board/wiz_b", "???");
}

void init()
{
        object ob=this_player();
        if ( !wizardp(ob) && wizhood(ob) !="(immortal)" )
        {
                ob->move(__DIR__"guest_room");
                return;
        }
}

