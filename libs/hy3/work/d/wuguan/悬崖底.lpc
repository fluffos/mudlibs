// Room: /u/xyx/震风武馆/悬崖底.c

inherit ROOM;

void create()
{
	set("short", "悬崖底");
	set("long", @LONG
周围是一片茂密的树林，树林密不透风你看不到很远。地下有几堆白骨，
看来是掉下来摔死的。你算幸运的了！
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"密林1",
]));

	setup();
	replace_program(ROOM);
}
