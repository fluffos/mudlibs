// Room: /u/xyx/震风武馆/偏厅2.c

inherit ROOM;

void create() {
  set("short", "偏厅");
  set("long", @LONG
这里是震风武馆偏厅，张昱风爱女张小纤的卧室，装饰有些普通，但看
起来很很清爽，但让你吃惊的是左边的墙上挂着一把宝剑。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "大殿",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
