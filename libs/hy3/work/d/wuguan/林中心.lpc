// Room: /u/xyx/震风武馆/林中心.c

inherit ROOM;

void create()
{
	set("short", "林中心");
	set("long", @LONG
周围还一片茂密的树林，看来没身特别的。
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 4 */
  "west" : __DIR__"密林4",
  "east" : __DIR__"密林6",
  "north" : __DIR__"密林1",
  "south" : __DIR__"密林3",
]));
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/feng" : 1,
]));

	setup();
	replace_program(ROOM);
}
