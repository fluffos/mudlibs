// Room: /d/wuguan/书房.c

inherit ROOM;

void create()
{
        set("short", "书房");
        set("long", @LONG
这里是张昱风的书房，房里都是一些普通的家具也没有什么特殊的装饰
品，窗边是一张红木的书桌和椅子，后面是一个书架。书架上面稀稀拉拉放
着几本书，看上去你也许在这儿得到点什么。
LONG
        );
        set("no_clean_up", 0);
        set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"后花园",
]));
        set("objects", ([ /* sizeof() == 1 */
  "/clone/book/sword_book" : 1,
]));

        setup();
        replace_program(ROOM);
}
