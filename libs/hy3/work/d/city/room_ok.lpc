// Room: room_ok.c
// Apollo make
// Date: Sep.20 1999
inherit ROOM;
#include <ansi.h>
void create()
{
set("short",BLINK HIY"卡拉OK包厢"NOR);
set("long",GRN @LONG
这里是人们练功累了的时候来放松一下的娱乐场所。
人们可以在这里尽情地唱歌，享受一下当歌星的乐趣。
点歌操作方法请help mac。
LONG 
NOR);
              set("exits",([ /* sizeof() == 1 */
                  "down":"/d/city/guangchang"]));
              set("no_fight", "1");
              set("no_clean_up", 0);
              set("objects", ([
                                "/clone/music/mac":1
]));
setup();
//call_other("/clone/board/apollo_ok.lpc", "???");
}
