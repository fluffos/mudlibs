inherit ITEM;

void create() {
  set_name("长索", ({ "rope" }));
  set_weight(1000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "这是一条普通的长索，长达数十丈。\n");
    set("unit", "支");
    set("value", 5000);
  }
}
