// Room: /city/qianzhuang.c
// YZC 1995/12/04 

inherit ROOM;

void create()
{
	set("short", "钱庄");
	set("long", @LONG
	这是一家老字号的钱庄，已有几百年的历史，在全国各地都有分店。它发
行的银票信誉非常好，通行全国。门前有块拍子(paizi)。
LONG
	);

	set("exits", ([
		"east" : __DIR__"beidajie1",
	]));
        set("item_desc", ([ /* sizeof() == 1 */
  "paizi" : "你可以使用以下命令：
查帐　：　chazhang
存钱　：　cun 数量 单位
取钱　：　qu 数量 单位
兑换　：　duihuan 数量 单位 to 单位
",
]));

        set("objects", ([
                __DIR__"npc/qian" : 1
        ]));

	set("coor/x",-10);
	set("coor/y",10);
	set("coor/z",0);
       set("terrain", "shop");
	setup();
	replace_program(ROOM);
}

