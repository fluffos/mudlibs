inherit ROOM;

void create() {
  set("short", "湖底");
  set("long", @LONG

到了这里，你才明白要“出淤泥而不染”是何其难。
LONG);
  set("exits", ([ /* sizeof() == 3 */
    "up": __DIR__ "hubian",
  ]));
  setup();
}


int valid_leave(object me, string dir) {
  message_vision("$N拼命挣扎着爬上岸，肚子里灌满了水．\n", me);
  me->set("water", (int)me->max_water_capacity());
  return ::valid_leave(me, dir);
}
