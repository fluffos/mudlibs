#include <ansi.h>
inherit ITEM;
void create()
{
        set_name(HIC"三级矿"NOR, ({"kuang"}));
        if (clonep())
                set_default_object(__FILE__);
        else {
                set("unit", "块");
                set("long", "这是一块三等矿！\n");
              set("value", 30000);
        }
        setup();
}

