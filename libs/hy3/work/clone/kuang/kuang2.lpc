#include <ansi.h>
inherit ITEM;
void create()
{
        set_name(HIC"二级矿"NOR, ({"kuang"}));
        if (clonep())
                set_default_object(__FILE__);
        else {
                set("unit", "块");
                set("long", "这是一块二等矿！\n");
              set("value", 50000);
        }
        setup();
}

