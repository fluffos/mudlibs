#include <net/daemons.h>
inherit F_CLEAN_UP;

int main(object me, string arg) {
  if (find_object(DNS_MASTER)) {
    DNS_MASTER->send_shutdown();
    write("关闭网际互连，打开互连请 update dns_master.c\n");
  } else write("dns_master not loaded\n");
  return 1;
}
