
#include <ansi.h>
#include <armor.h>
inherit CLOTH;
void create()
{
        set_name( "布甲碎片"NOR, ({ "bujia"}) );
        set_weight(1000);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("unit", "件");
               set("cloth_type",1);
                set("material", "leather");               
                set("armor_prop/armor", 1);
        }
        setup();
}
