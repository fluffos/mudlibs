// a skeleton for user rooms
inherit ROOM;
void create()
{
set("short", "SHORT_TITLE");
set("long", @LONG
LONG_TITLE
LONG
);
        set("exits", ([
]));
set("objects", ([
       "/clone/npc/banker":1,
       ]) );
        //set("owner", "bigtie7001");
        set("banghui", "bigwiz7001");
        set("no_fight",1);
        set("sleep_room", "1");

setup();
}

