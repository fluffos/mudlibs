// Room: /d/city/yaopu.c
// This is a room made by roommaker. write by Fan. :)
inherit ROOM;
void create() {
  set("short", "药房");
  set("long", @LONG
	这是武官药房，一股浓浓的药味让你几欲窒息，那是从药柜上的几百个小
抽屉里散发出来的。神医侄儿平孝心坐在茶几旁，独自喝着茶，看也不看你一眼。一名
小伙计站在柜台后招呼着武官学徒。柜台上贴着一张发黄的告示(gaoshi)。
LONG
  );
  set("no_clean_up", 0);
  set("no_fight", 1);
  set("no_get", 1);
  set("item_desc", ([ /* sizeof() == 1 */
    "gaoshi": "你可以在这里ask ping about work经验潜能多多哦!",
  ]));
  set("objects", ([ /* sizeof() == 3 */
    __DIR__ "npc/ping": 1,
    __DIR__ "npc/xunbu": 1,
    __DIR__ "npc/huoji2": 1,
    //   __DIR__"npc/huoji" : 1,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "zoulang2",
    "north": __DIR__ "yaopuls",
  ]));
  setup();
  replace_program(ROOM);
}
