#include <ansi.h>
inherit ROOM;


void create()
{
        set("short", "饭厅");
        set("long", @LONG
这里是武馆内用餐的地方。屋里布置很简单, 只摆着几张桌椅
几个人在一声不响地吃着。一个厨子在不停的忙碌着，你可以向他
要食物和水。
LONG
        );

        set("exits", ([
                "east" :__DIR__"shilu-2",
                "south" :__DIR__"lang3",
                "north" :__DIR__"shilu-3",


        ]));

             set("objects", ([
   //         __DIR__"npc/wang" : 1,
       ]));

       
        set("no_fight", 1);

        setup();
}

