// Room: /city/damen.c    by king
// YZC 1995/12/04 
inherit ROOM;
void create()
{
        set("short", "风啸楼大门");
        set("long", @LONG
  这里有是风啸楼的大门，进入这里，你就可以拜师学习风啸楼的绝学了，
不过，风啸楼是有规矩的。你先看看招生广告(sign).
LONG
        );
        set("item_desc", ([
                "sign": "入我风啸楼者，必须忠于楼主。\n",
        ]));
        set("exits", ([
                 "down" : "/d/city/dongmen",
                "north":__DIR__"datang",
        ]));
        set("objects", ([
        __DIR__"npc/xianren" : 1,
        __DIR__"npc/luao" :1
]));
        setup(); 
        replace_program(ROOM);
}

