//m_weapon .....tianjianshi.c
//xuanbing.c

inherit ROOM;

void create()
{
	set("short", "冶炼厂办公室");
	set("long",
	     "这里是冶炼厂的办公室，里面的铸造车间传来阵阵的机器捶打声。似乎在铸造着什么。隐隐\n"
	     "的透着一股萧杀的气息。\n"

	);
 set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/laoban" : 1,
]));

	set("exits", ([
		"enter" : __DIR__"zhujian_room",
              "down":"/d/city/shijian",
	]));
	set("coor/x",-150);
	set("coor/y",10);
	set("coor/z",0);
       set("no_fight",0);
	setup();
	replace_program(ROOM);
}
