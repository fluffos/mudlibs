#pragma save_binary
// Room: /d/green/secretroom.c

inherit ROOM;

void create()
{
	set("short", "刀剑室");
	set("long",
"这是一间不大的密室，室内一尘不染。靠墙的地上放着两件不起\n"
"眼的兵刃。\n"
);
	set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"fortgate",
  "north" : __DIR__"wstorage",
  "up" : __DIR__"work1",
]));
	set("objects", ([ /* sizeof() == 2 */
  __DIR__"obj/heavensword" : 1,
  __DIR__"obj/dragonblade" : 1,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
