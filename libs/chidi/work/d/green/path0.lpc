#pragma save_binary
// Room: /d/green/path0.c

inherit ROOM;

void create() {
  set("coor", ({ 65, 4930, 10 }));
  set("short", "小路的尽头");
  set("long",
    "小路到了这里以后往北变得很不明显,往南还可以依稀看到村子\n"
    "的入口,路的四周长满了杂草和小树之类的,看来相当的荒凉.\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": "/d/green/cave0",
    "south": "/d/green/path1",
  ]));
  set("outdoors", "green");

  setup();
}
