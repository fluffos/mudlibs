#pragma save_binary
// Room: /open/tony/jjf/jjf_2.c
inherit ROOM;

void create() {
  set("coor", ({ 110, 2930, 0 }));
  set("short", "东书房");
  set("long", @LONG
此处有近十个大书柜，每个书柜都有好多书籍,大将军在百忙之中
任坚持在此天天看一个时辰的书.
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "jjf_3",
  ]));
  set("objects", ([ /* sizeof() == 0 */

    __DIR__ "jin_wei": 0,
  ]));
  set("no_clean_up", 0);

  set_temp("light", 1);
  setup();
  replace_program(ROOM);
}
