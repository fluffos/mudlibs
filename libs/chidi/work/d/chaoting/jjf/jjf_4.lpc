#pragma save_binary
// Room: /open/tony/jjf/jjf_2.c
inherit ROOM;

void create()
{
    set("coor",({100,2940,0}));
	set("short", "后院走廊");     
        set("long", @LONG
这儿也不似其他官宦之家,那样雕梁画栋.往北是将军的确卧室.东西
方各有一厢房.
LONG
        );
set("exits", ([ /* sizeof() == 4 */
"south" : __DIR__"jjf_3",
"north" : __DIR__"jjf_5",
"east" : __DIR__"jjf_4e",
"west" : __DIR__"jjf_4w",
]));
        set("objects", ([ /* sizeof() == 0 */
                       
   __DIR__"npc/guan_jia":  1,
]));
        set("no_clean_up", 0);

set_temp("light",1);
        setup();
}

int valid_leave(object me, string dir)
{        if( dir == "west"
&& objectp(present("guan jia", environment(me))))
     return                    notify_fail("大管家喝道:大将军有令,任何人不得入内!\n");
        return ::valid_leave(me, dir);
}
