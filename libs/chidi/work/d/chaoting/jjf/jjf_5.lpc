#pragma save_binary
// Room: /open/tony/jjf/jjf_2.c
inherit ROOM;

void create()
{
    set("coor",({100,2950,0}));
	set("short", "将军的卧室");   
        set("long", @LONG
在屋内就只有床和一张桌子，两把椅子．将军府没有后花园，所以从
这儿透过窗往北只能看到一堵墙,往南则是走廊．
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
"south" : __DIR__"jjf_4",
]));
        set("objects", ([ /* sizeof() == 0 */
                       
          __DIR__"jin_wei":  0,
]));
        set("no_clean_up", 0);

set_temp("light",1);
        setup();
        replace_program(ROOM);
}

