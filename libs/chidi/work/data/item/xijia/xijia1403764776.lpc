/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit CLOTH;

void create()
{
	object me = this_player();

	set_name("[1;36m旋风[1;31m甲"NOR, ({"xijia cloth", "hecheng", "cloth"}) );
	set("hcloth/name", "[1;36m旋风[1;31m甲"NOR);
	set("hcloth/id", "xijia cloth");
	set("long", "这是由旋风(xijia)合成的[1;36m旋风[1;31m甲" + NOR + "。\n");
	set("armor_type", "cloth");
	set_weight(5000);
	set("unit", "件");
	set("no_get", 1);
	set("no_give", 1);
	set("no_sell", 1);
	set("no_steal", 1);
	set("no_beg", 1);
	set("no_drop", 1);
	set("armor_prop/armor", 285);
	set("armor_prop/constitution", 791);
	set("armor_prop/dexerity", 666);
	set("armor_prop/intelligence", 731);
	set("armor_prop/strength", 702);
	set("wear_msg", me->name() + NOR "装备" + name () + NOR "！\n");
	set("unwear_msg", me->name() + NOR "脱下" + name () + NOR "！\n");

   setup();
}

