/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create()
{
	object me = this_player();

	set_name("[1;37m真武鞋子"NOR, ({"jimy boots", "hecheng", "boots"}) );
	set("hcloth/name", "[1;37m真武鞋子"NOR);
	set("hcloth/id", "jimy boots");
	set("long", "这是由吉米(jimy)合成的[1;37m真武鞋子" + NOR + "。\n");
	set("armor_type", "boots");
	set_weight(5000);
	set("unit", "双");
	set("no_get", 1);
	set("no_give", 1);
	set("no_sell", 1);
	set("no_steal", 1);
	set("no_beg", 1);
	set("no_drop", 1);
	set("armor_prop/armor", 177);
	set("armor_prop/constitution", 748);
	set("armor_prop/dexerity", 649);
	set("armor_prop/intelligence", 689);
	set("armor_prop/strength", 718);
	set("wear_msg", me->name() + NOR "装备" + name () + NOR "！\n");
	set("unwear_msg", me->name() + NOR "脱下" + name () + NOR "！\n");

   setup();
}

