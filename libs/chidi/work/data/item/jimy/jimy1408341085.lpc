/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create() {
  object me = this_player();

  set_name("[1;37m真武项链" NOR, ({ "jimy neck", "hecheng", "neck" }));
  set("hcloth/name", "[1;37m真武项链" NOR);
  set("hcloth/id", "jimy neck");
  set("long", "这是由吉米(jimy)合成的[1;37m真武项链" + NOR + "。\n");
  set("armor_type", "neck");
  set_weight(5000);
  set("unit", "个");
  set("no_get", 1);
  set("no_give", 1);
  set("no_sell", 1);
  set("no_steal", 1);
  set("no_beg", 1);
  set("no_drop", 1);
  set("armor_prop/armor", 396);
  set("armor_prop/constitution", 690);
  set("armor_prop/dexerity", 733);
  set("armor_prop/intelligence", 825);
  set("armor_prop/strength", 728);
  set("wear_msg", me->name() + NOR "装备" + name() + NOR "！\n");
  set("unwear_msg", me->name() + NOR "脱下" + name() + NOR "！\n");

  setup();
}
