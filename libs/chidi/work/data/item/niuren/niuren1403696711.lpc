/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create()
{
	object me = this_player();

	set_name("[1;35m英雄披风"NOR, ({"niuren pifeng", "hecheng", "pifeng"}) );
	set("hcloth/name", "[1;35m英雄披风"NOR);
	set("hcloth/id", "niuren pifeng");
	set("long", "这是由必须的必(niuren)合成的[1;35m英雄披风" + NOR + "。\n");
	set("armor_type", "pifeng");
	set_weight(5000);
	set("unit", "件");
	set("no_get", 1);
	set("no_give", 1);
	set("no_sell", 1);
	set("no_steal", 1);
	set("no_beg", 1);
	set("no_drop", 1);
	set("armor_prop/armor", 322);
	set("armor_prop/constitution", 692);
	set("armor_prop/dexerity", 717);
	set("armor_prop/intelligence", 676);
	set("armor_prop/strength", 716);
	set("wear_msg", me->name() + NOR "装备" + name () + NOR "！\n");
	set("unwear_msg", me->name() + NOR "脱下" + name () + NOR "！\n");

   setup();
}

