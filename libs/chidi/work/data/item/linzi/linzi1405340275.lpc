/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create() {
  object me = this_player();

  set_name("子项链" NOR, ({ "linzi neck", "hecheng", "neck" }));
  set("hcloth/name", "子项链" NOR);
  set("hcloth/id", "linzi neck");
  set("long", "这是由林子(linzi)合成的子项链" + NOR + "。\n");
  set("armor_type", "neck");
  set_weight(5000);
  set("unit", "个");
  set("no_get", 1);
  set("no_give", 1);
  set("no_sell", 1);
  set("no_steal", 1);
  set("no_beg", 1);
  set("no_drop", 1);
  set("armor_prop/armor", 345);
  set("armor_prop/constitution", 746);
  set("armor_prop/dexerity", 615);
  set("armor_prop/intelligence", 805);
  set("armor_prop/strength", 773);
  set("wear_msg", me->name() + NOR "装备" + name() + NOR "！\n");
  set("unwear_msg", me->name() + NOR "脱下" + name() + NOR "！\n");

  setup();
}
