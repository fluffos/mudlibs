/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create()
{
	object me = this_player();

	set_name("[1;35m天使[1;32m头盔"NOR, ({"rain head", "hecheng", "head"}) );
	set("hcloth/name", "[1;35m天使[1;32m头盔"NOR);
	set("hcloth/id", "rain head");
	set("long", "这是由小雨点(rain)合成的[1;35m天使[1;32m头盔" + NOR + "。\n");
	set("armor_type", "head");
	set_weight(5000);
	set("unit", "双");
	set("no_get", 1);
	set("no_give", 1);
	set("no_sell", 1);
	set("no_steal", 1);
	set("no_beg", 1);
	set("no_drop", 1);
	set("armor_prop/armor", 401);
	set("armor_prop/constitution", 806);
	set("armor_prop/dexerity", 792);
	set("armor_prop/intelligence", 658);
	set("armor_prop/strength", 738);
	set("wear_msg", me->name() + NOR "装备" + name () + NOR "！\n");
	set("unwear_msg", me->name() + NOR "脱下" + name () + NOR "！\n");

   setup();
}

