/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create()
{
	object me = this_player();

	set_name("[1;35m天使[1;32m指套"NOR, ({"rain finger", "hecheng", "finger"}) );
	set("hcloth/name", "[1;35m天使[1;32m指套"NOR);
	set("hcloth/id", "rain finger");
	set("long", "这是由小雨点(rain)合成的[1;35m天使[1;32m指套" + NOR + "。\n");
	set("armor_type", "finger");
	set_weight(5000);
	set("unit", "双");
	set("no_get", 1);
	set("no_give", 1);
	set("no_sell", 1);
	set("no_steal", 1);
	set("no_beg", 1);
	set("no_drop", 1);
	set("armor_prop/armor", 355);
	set("armor_prop/constitution", 772);
	set("armor_prop/dexerity", 792);
	set("armor_prop/intelligence", 722);
	set("armor_prop/strength", 861);
	set("wear_msg", me->name() + NOR "装备" + name () + NOR "！\n");
	set("unwear_msg", me->name() + NOR "脱下" + name () + NOR "！\n");

   setup();
}

