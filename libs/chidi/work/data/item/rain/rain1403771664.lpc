/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create()
{
	object me = this_player();

	set_name("[1;35m天使[1;32m项链"NOR, ({"rain neck", "hecheng", "neck"}) );
	set("hcloth/name", "[1;35m天使[1;32m项链"NOR);
	set("hcloth/id", "rain neck");
	set("long", "这是由小雨点(rain)合成的[1;35m天使[1;32m项链" + NOR + "。\n");
	set("armor_type", "neck");
	set_weight(5000);
	set("unit", "个");
	set("no_get", 1);
	set("no_give", 1);
	set("no_sell", 1);
	set("no_steal", 1);
	set("no_beg", 1);
	set("no_drop", 1);
	set("armor_prop/armor", 342);
	set("armor_prop/constitution", 756);
	set("armor_prop/dexerity", 832);
	set("armor_prop/intelligence", 683);
	set("armor_prop/strength", 753);
	set("wear_msg", me->name() + NOR "装备" + name () + NOR "！\n");
	set("unwear_msg", me->name() + NOR "脱下" + name () + NOR "！\n");

   setup();
}

