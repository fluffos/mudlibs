/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create()
{
	object me = this_player();

	set_name("项链"NOR, ({"benben neck", "hecheng", "neck"}) );
	set("hcloth/name", "项链"NOR);
	set("hcloth/id", "benben neck");
	set("long", "这是由本本(benben)合成的项链" + NOR + "。\n");
	set("armor_type", "neck");
	set_weight(5000);
	set("unit", "个");
	set("no_get", 1);
	set("no_give", 1);
	set("no_sell", 1);
	set("no_steal", 1);
	set("no_beg", 1);
	set("no_drop", 1);
	set("armor_prop/armor", 225);
	set("armor_prop/constitution", 741);
	set("armor_prop/dexerity", 686);
	set("armor_prop/intelligence", 774);
	set("armor_prop/strength", 709);
	set("wear_msg", me->name() + NOR "装备" + name () + NOR "！\n");
	set("unwear_msg", me->name() + NOR "脱下" + name () + NOR "！\n");

   setup();
}

