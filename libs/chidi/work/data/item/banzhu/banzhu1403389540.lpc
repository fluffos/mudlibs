/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create()
{
	object me = this_player();

	set_name("[1;31m烈焰[1;36m永恒"NOR, ({"banzhu hands", "hecheng", "hands"}) );
	set("hcloth/name", "[1;31m烈焰[1;36m永恒"NOR);
	set("hcloth/id", "banzhu hands");
	set("long", "这是由扮猪(banzhu)合成的[1;31m烈焰[1;36m永恒" + NOR + "。\n");
	set("armor_type", "hands");
	set_weight(5000);
	set("unit", "双");
	set("no_get", 1);
	set("no_give", 1);
	set("no_sell", 1);
	set("no_steal", 1);
	set("no_beg", 1);
	set("no_drop", 1);
	set("armor_prop/armor", 272);
	set("armor_prop/constitution", 648);
	set("armor_prop/dexerity", 640);
	set("armor_prop/intelligence", 660);
	set("armor_prop/strength", 661);
	set("wear_msg", me->name() + NOR "装备" + name () + NOR "！\n");
	set("unwear_msg", me->name() + NOR "脱下" + name () + NOR "！\n");

   setup();
}

