/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create() {
  object me = this_player();

  set_name("[1;36m龙神御风靴" NOR, ({ "banzhu boots", "hecheng", "boots" }));
  set("hcloth/name", "[1;36m龙神御风靴" NOR);
  set("hcloth/id", "banzhu boots");
  set("long", "这是由扮猪(banzhu)合成的[1;36m龙神御风靴" + NOR + "。\n");
  set("armor_type", "boots");
  set_weight(5000);
  set("unit", "双");
  set("no_get", 1);
  set("no_give", 1);
  set("no_sell", 1);
  set("no_steal", 1);
  set("no_beg", 1);
  set("no_drop", 1);
  set("armor_prop/armor", 265);
  set("armor_prop/constitution", 607);
  set("armor_prop/dexerity", 706);
  set("armor_prop/intelligence", 683);
  set("armor_prop/strength", 706);
  set("wear_msg", me->name() + NOR "装备" + name() + NOR "！\n");
  set("unwear_msg", me->name() + NOR "脱下" + name() + NOR "！\n");

  setup();
}
