/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create() {
  object me = this_player();

  set_name("[1;31m猪[1;32m肘" NOR, ({ "aoxa hands", "hecheng", "hands" }));
  set("hcloth/name", "[1;31m猪[1;32m肘" NOR);
  set("hcloth/id", "aoxa hands");
  set("long", "这是由猪头(aoxa)合成的[1;31m猪[1;32m肘" + NOR + "。\n");
  set("armor_type", "hands");
  set_weight(5000);
  set("unit", "双");
  set("no_get", 1);
  set("no_give", 1);
  set("no_sell", 1);
  set("no_steal", 1);
  set("no_beg", 1);
  set("no_drop", 1);
  set("armor_prop/armor", 288);
  set("armor_prop/constitution", 791);
  set("armor_prop/dexerity", 710);
  set("armor_prop/intelligence", 684);
  set("armor_prop/strength", 693);
  set("wear_msg", me->name() + NOR "装备" + name() + NOR "！\n");
  set("unwear_msg", me->name() + NOR "脱下" + name() + NOR "！\n");

  setup();
}
