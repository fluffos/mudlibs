/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create()
{
	object me = this_player();

	set_name("[1;31m猪[1;32m头"NOR, ({"aoxa head", "hecheng", "head"}) );
	set("hcloth/name", "[1;31m猪[1;32m头"NOR);
	set("hcloth/id", "aoxa head");
	set("long", "这是由猪头(aoxa)合成的[1;31m猪[1;32m头" + NOR + "。\n");
	set("armor_type", "head");
	set_weight(5000);
	set("unit", "双");
	set("no_get", 1);
	set("no_give", 1);
	set("no_sell", 1);
	set("no_steal", 1);
	set("no_beg", 1);
	set("no_drop", 1);
	set("armor_prop/armor", 323);
	set("armor_prop/constitution", 796);
	set("armor_prop/dexerity", 778);
	set("armor_prop/intelligence", 716);
	set("armor_prop/strength", 770);
	set("wear_msg", me->name() + NOR "装备" + name () + NOR "！\n");
	set("unwear_msg", me->name() + NOR "脱下" + name () + NOR "！\n");

   setup();
}

