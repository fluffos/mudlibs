/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create()
{
	object me = this_player();

	set_name("布帽"NOR, ({"aimi head", "hecheng", "head"}) );
	set("hcloth/name", "布帽"NOR);
	set("hcloth/id", "aimi head");
	set("long", "这是由艾米(aimi)合成的布帽" + NOR + "。\n");
	set("armor_type", "head");
	set_weight(5000);
	set("unit", "双");
	set("no_get", 1);
	set("no_give", 1);
	set("no_sell", 1);
	set("no_steal", 1);
	set("no_beg", 1);
	set("no_drop", 1);
	set("armor_prop/armor", 195);
	set("armor_prop/constitution", 588);
	set("armor_prop/dexerity", 676);
	set("armor_prop/intelligence", 592);
	set("armor_prop/strength", 648);
	set("wear_msg", me->name() + NOR "装备" + name () + NOR "！\n");
	set("unwear_msg", me->name() + NOR "脱下" + name () + NOR "！\n");

   setup();
}

