/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit CLOTH;

void create()
{
	object me = this_player();

	set_name("布依"NOR, ({"aimi cloth", "hecheng", "cloth"}) );
	set("hcloth/name", "布依"NOR);
	set("hcloth/id", "aimi cloth");
	set("long", "这是由艾米(aimi)合成的布依" + NOR + "。\n");
	set("armor_type", "cloth");
	set_weight(5000);
	set("unit", "件");
	set("no_get", 1);
	set("no_give", 1);
	set("no_sell", 1);
	set("no_steal", 1);
	set("no_beg", 1);
	set("no_drop", 1);
	set("armor_prop/armor", 504);
	set("armor_prop/constitution", 851);
	set("armor_prop/dexerity", 757);
	set("armor_prop/intelligence", 831);
	set("armor_prop/strength", 807);
	set("wear_msg", me->name() + NOR "装备" + name () + NOR "！\n");
	set("unwear_msg", me->name() + NOR "脱下" + name () + NOR "！\n");

   setup();
}

