/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create() {
  object me = this_player();

  set_name("中央杏黄旗" NOR, ({ "helper boots", "hecheng", "boots" }));
  set("hcloth/name", "中央杏黄旗" NOR);
  set("hcloth/id", "helper boots");
  set("long", "这是由飞雪连天(helper)合成的中央杏黄旗" + NOR + "。\n");
  set("armor_type", "boots");
  set_weight(5000);
  set("unit", "双");
  set("no_get", 1);
  set("no_give", 1);
  set("no_sell", 1);
  set("no_steal", 1);
  set("no_beg", 1);
  set("no_drop", 1);
  set("armor_prop/armor", 1071);
  set("armor_prop/constitution", 1404);
  set("armor_prop/dexerity", 847);
  set("armor_prop/intelligence", 6319);
  set("armor_prop/strength", 1211);
  set("wear_msg", me->name() + NOR "装备" + name() + NOR "！\n");
  set("unwear_msg", me->name() + NOR "脱下" + name() + NOR "！\n");

  setup();
}
