/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit NECK;

void create() {
  object me = this_player();

  set_name("太极图" NOR, ({ "helper waist", "hecheng", "waist" }));
  set("hcloth/name", "太极图" NOR);
  set("hcloth/id", "helper waist");
  set("long", "这是由飞雪连天(helper)合成的太极图" + NOR + "。\n");
  set("armor_type", "waist");
  set_weight(5000);
  set("unit", "条");
  set("no_get", 1);
  set("no_give", 1);
  set("no_sell", 1);
  set("no_steal", 1);
  set("no_beg", 1);
  set("no_drop", 1);
  set("armor_prop/armor", 1470);
  set("armor_prop/constitution", 938);
  set("armor_prop/dexerity", 1276);
  set("armor_prop/intelligence", 6095);
  set("armor_prop/strength", 1104);
  set("wear_msg", me->name() + NOR "装备" + name() + NOR "！\n");
  set("unwear_msg", me->name() + NOR "脱下" + name() + NOR "！\n");

  setup();
}
