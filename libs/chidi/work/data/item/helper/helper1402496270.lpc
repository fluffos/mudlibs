/**本文件由合成系统自动生成**/

#include <ansi.h>
#include <armor.h>

inherit CLOTH;

void create()
{
	object me = this_player();

	set_name("东方宝莲旗"NOR, ({"helper cloth", "hecheng", "cloth"}) );
	set("hcloth/name", "东方宝莲旗"NOR);
	set("hcloth/id", "helper cloth");
	set("long", "这是由飞雪连天(helper)合成的东方宝莲旗" + NOR + "。\n");
	set("armor_type", "cloth");
	set_weight(5000);
	set("unit", "件");
	set("no_get", 1);
	set("no_give", 1);
	set("no_sell", 1);
	set("no_steal", 1);
	set("no_beg", 1);
	set("no_drop", 1);
	set("armor_prop/armor", 856);
	set("armor_prop/constitution", 1143);
	set("armor_prop/dexerity", 1257);
	set("armor_prop/intelligence", 6143);
	set("armor_prop/strength", 900);
	set("wear_msg", me->name() + NOR "装备" + name () + NOR "！\n");
	set("unwear_msg", me->name() + NOR "脱下" + name () + NOR "！\n");

   setup();
}

