#include <ansi.h>
inherit "/d/maze/lingzhu-shui/room.lpc";

nosave string color = ({HIC,HIW,HIB,HIM,HIR,HIG,HIY,CYN,WHT,MAG,BLU,RED,GRN,YEL})[random(14)];

void create()
{
        set("short","水下");
        set("channel_id","水灵珠副本");
        set("long", @LONG
水底到处都是些水草，还有些小虾米，鱼儿穿来穿去，偶尔还能看到
几具骸骨。

LONG );
        set("alternative_die",1);
        set("virtual_room", 1); 
        set("exits", ([
                "south" : __DIR__"maze/entry", 
        ])); 
        set("objects",([
//        	"/d/maze/lingzhu-tu/npc/yao" : 1,
        ]));	
        setup();
}