#include <ansi.h>
inherit "/d/maze/qingling-3/room.lpc";

void create() {
  set("short", "始皇陵三层");
  set("channel_id", "始皇陵三层副本");
  set("virtual_room", 1);
  set("exits", ([
    "south": __DIR__ "maze/entry",
  ]));
  set("alternative_die", 1);
  set("objects", ([
    "/d/maze/qingling-3/npc/npc": 1,
  ]));
  setup();
}
