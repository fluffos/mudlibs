#include <ansi.h>
inherit "/d/maze/lingzhu-lei/room.lpc";

nosave string color = ({HIC,HIW,HIB,HIM,HIR,HIG,HIY,CYN,WHT,MAG,BLU,RED,GRN,YEL})[random(14)];

void create()
{
        set("short","树林");
        set("channel_id","雷灵珠副本");
        set("long", @LONG
这是一片茂密的树林，好象被人设计了一种阵，一走进来，你仿佛迷失了方向。
LONG );
        set("alternative_die",1);
        set("virtual_room", 1); 
        set("exits", ([
                "south" : __DIR__"maze/entry", 
        ])); 
        set("objects",([
        	"/d/maze/lingzhu-lei/npc/lei" : 1,
        ]));	
        setup();
}