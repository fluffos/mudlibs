// edit by god 2004-7-4
#include <ansi.h>
inherit ROOM;

void create ()
{
	set ("short", "女娲补天处");
    set ("long", @LONG
这里是一片开阔地。地上铺满了一堆又一堆奇形怪状色彩斑
斓的残岩碎石，在雪色映照下溢彩流光。边上一块大石上刻着几
行大字：
LONG);
	set("long2",@LONG
	昆仑绝处
     	女娲补天
     	彩石未尽
     	遗福後人

LONG);
    set("exits", ([
		"east" : __DIR__"fabao-road2",
 		]));
	set("objects", ([
  	]));
	set("no_fight", 1);
   	set("no_magic", 1);
	setup();
}