inherit ITEM;

void create()
{
	set_name("青竹筒(空)", ({ "qing zhutong", "zhu tong","tong" }) );
  	set_weight(300);
  	if( clonep() )
    		set_default_object(__FILE__);
  	else 
  	{
    		set("long", "这是一根尺把长的三节蟠虬雕花青竹筒。\n");
    		set("unit", "根");
    		set("value", 120);
  	}
  	setup();
}
