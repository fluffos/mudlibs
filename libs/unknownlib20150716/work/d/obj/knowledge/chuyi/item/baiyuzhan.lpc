inherit ITEM;

void create() {
  set_name("白玉盏(空)", ({ "baiyu zhan", "zhan", "cup" }));
  set_weight(300);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一个淡白玉的小酒盏。\n");
    set("unit", "个");
    set("value", 500);
  }
}
