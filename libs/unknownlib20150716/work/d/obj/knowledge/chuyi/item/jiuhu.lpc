inherit ITEM;

void create() {
  set_name("酒壶(空)", ({ "jiu hu", "jiuhu", "hu" }));
  set_weight(100);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一个空空的酒壶。\n");
    set("unit", "个");
    set("value", 40);
  }
  setup();
}
