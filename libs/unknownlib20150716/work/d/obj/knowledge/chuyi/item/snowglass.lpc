inherit ITEM;

void create() {
  set_name("雪饮杯(空)", ({ "snowglass", "glass" }));
  set_weight(600);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "杯体碧绿通透，玲珑可爱．\n");
    set("unit", "个");
    set("value", 1200);
  }
  setup();
}
