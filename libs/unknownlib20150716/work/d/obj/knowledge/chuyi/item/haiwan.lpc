inherit ITEM;

void create() {
  set_name("大海碗(空)", ({ "hai wan", "wan" }));
  set_weight(100);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一个大海碗。\n");
    set("value", 30);
  }
  setup();
}
