inherit ITEM;

void create() {
  set_name("醋坛子(空)", ({ "tan zi", "tanzi" }));
  set_weight(500);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一个装醋的大坛子。\n");
    set("unit", "个");
    set("value", 50);
  }
  setup();
}
