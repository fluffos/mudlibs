inherit ITEM;

void create()
{
	set_name("牛皮酒袋(空)", ({"niupi jiudai", "wineskin", "skin"}));
	set_weight(700);
	if (clonep())
		set_default_object(__FILE__);
	else {
		set("long", "一个用来装米酒的大酒袋。\n");
		set("unit", "个");
		set("value", 50);
	}
	setup();
}

