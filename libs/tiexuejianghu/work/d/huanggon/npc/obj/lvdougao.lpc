inherit ITEM;
inherit F_FOOD;

void create() {
  set_name("玫瑰绿豆糕", ({ "gao" }));
  set_weight(900);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "块");
    set("value", 100);
    set("food_remaining", 4);
    set("food_supply", 20);
  }
}
