// Room: /u/xinyi/huichang.c 
 
inherit ROOM; 
 
void create() 
{ 
	set("short", "天下第一比武会场");
	set("long", @LONG
这里是比武会场,在这里给了人们一个公平的比较功夫的地方.
并能给你天下第一的美名.在比武会场的门口有一块石碑(bei)上面写着有关的事项. 

[1;37m          天  下  第  一  比  武  大  会 
[2;37;0m
LONG
	);
	set("no_clean_up", 0);
	set("no_sleep", 0);
	set("NONPC", 1);
	set("no_magic", 1);
	set("no_kill", 0);
	set("item_desc", ([ /* sizeof() == 1 */
  "bei" : " 
     在比不能使用 kill 命令 
     只能使用 bihua ; fight ; bi ; halt 
    在开比武大会时玩家能用 bihua  
",
]));
	set("objects", ([ /* sizeof() == 2 */
        "/u/xinyi/npc/referee" : 1,
]));
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"xiuxishi.lpc",
  "northeast" : "/d/city/guangchang.lpc",
]));

	setup();
	replace_program(ROOM);
}
