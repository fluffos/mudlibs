 
#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", "万兽谷");
        set("long", @LONG 
下来后的视野一下开阔起来,里面遍地是绿幽幽的野草,到处
充满了春天的气息.不远处有一块石壁,上面有几个苍劲有力
的大字,不知道是那位神仙写的,上书:"万兽谷".
LONG);
       set("exits", ([
    "northup"  : __DIR__"luxingchu",
    "southup"  : __DIR__"shijie1",
    "west"  : __DIR__"zahuopu",
    "east"  : __DIR__"bank",
        ]));
      setup();
}
