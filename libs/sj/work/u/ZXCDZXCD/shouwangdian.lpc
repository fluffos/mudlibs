#include <ansi.h>
inherit ROOM;

void create()
{
    set("short", "兽王殿");
    set("long", @LONG
这里就是兽族之总统帅"神兽王"之所在.整个神殿灯火通明,
四根用神山古木做成的柱子上刻着四种颜色的盘龙,霸气十足
好一派皇者风范,让你不自觉的后退了几步.
LONG);
    set("exits", (
     "northwest" : __DIR__"shenjiangdian1",
     "northeast" : __DIR__"shenjiangdian2",
     "east" : __DIR__"bingqiku",
     "west" : __DIR__"qingong",
    ]));
 set("objects", ([   "/kungfu/class/shouzu/shenshouwang": 1       ]));
    setup();
    replace_program(ROOM); 
}
