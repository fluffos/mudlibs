// Room: practice 练功房
// Date: Sep.6 1997

#include <room.h>

inherit ROOM;

void create() {
  set("short", "练功房");
  set("long", @LONG
这里摆满了刀枪剑戟、斧矛棍棒、鞭锏链锤、弓戈牌扒、沙袋绑腿，
普天下的暗器兵刃，一一具陈。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "happy",
  ]));
  set("no_clean_up", 0);
  set("objects", ([
    "/d/shaolin/obj/mu-ren": 10,
  ]));
  setup();
}
