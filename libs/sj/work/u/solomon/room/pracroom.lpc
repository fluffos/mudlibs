// Room: pracroom.c

inherit ROOM;

void create()
{
      set("short", "铁人巷");
      set("long", @LONG
这里是本山的铁人巷，里面暗淡无光，不知有多大，只可见几个铁人
站在你面前...
LONG
      );

      set("objects", ([
               "/d/npc/tie-ren": 10,
      ]));

      set("exits", ([
            "west"   : __DIR__"bamboo1",
      ]));

      setup();
      replace_program(ROOM);
}
