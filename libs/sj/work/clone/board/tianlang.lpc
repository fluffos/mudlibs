// /clone/board/wiz_b.c

inherit W_BOARD;

void create()
{
            set_name("天狼星留言版", ({ "board" }) );
          set("location", "/d/tianlang/street");
          set("board_id", "player_b");
         set("long", "这是一个专供玩家使用的留言板。\n" );
        setup();
        set("capacity", 30);
        replace_program(W_BOARD);
}

