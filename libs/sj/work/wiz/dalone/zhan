#include <ansi.h>
#include <armor.h>

inherit CLOTH;

void create()
{
        set_name(HIR "炎龙战袍" NOR, ({ "long pao", "pao" }) );
        set_weight(5000);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("unit", "件");
                set("no_drop","宝物是扔/卖不了的，也不能给别人。");
                set("value", 2250000);
                set("material", "gold");
      set("armor_prop/armor", 3000);
        }
        setup();
}
int query_autoload()
{
        return 1;
}