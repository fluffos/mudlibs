//room: liangong.c

#include <ansi.h>

inherit ROOM;

void create()
{
  set("short",HIR "戏凤阁" NOR);
  set("long", HIW @LONG
这里是莉娜练功的地方。
LONG
NOR      );

  set("exits",([
      "east" : __DIR__"workroom",
     ]));

 set("objects",([
          "/wiz/dalone/mu-ren.lpc" : 10 ]));
  setup();
 replace_program(ROOM);
}
