// Room: /wiz/kuangf/workroom.c

inherit ROOM;

void create() {
  set("short", "狂风会客室");
  set("long", @LONG
这就是狂风大人的会见客人的地方
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "wiz": "/d/wizard/bugroom",
  ]));

  setup();
}
