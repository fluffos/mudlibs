inherit ROOM;
#include <ansi.h>

void create() {
  set("short", NOR + CYN "蜡笔小新的工作室" HIG);
  set("long", @LONG       
 这是蜡笔小新的工作室，里面的摆设很简单，
看来蜡笔小新是一个很扑素的人。在这里有几个出口。
KEDIAN是通往扬州客店的,
LT是到扬州擂台的。
LONG);
  set("valid_startroom", 1) set("sleep_room", "1");
  set("no_steal", "1");
  set("exits", ([
    "wiz": "/d/wizard/wizard_room",
  ]));

  setup();

  {}
}
