#include <room.h>

inherit ROOM;

void create()
{
        set("short", "监狱通道");
        set("long", @LONG
你现在身处圣殿监狱中,圣殿犯过错误的武士都要被关在这里
反省.监狱里空空荡荡,没什么东西.墙角的角落里一位满面胡须
的人凶巴巴的看着你.
LONG );
        set("exits", ([
                "north" : __DIR__"jinqu1",
         "southeast" : __DIR__"jinqu3",
        ]));
        set("objects", ([
  "/d/shendian/npc/liumang" : 1,
        ]));
        set("outdoors", "shendian");
        setup();
}

