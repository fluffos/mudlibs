#include <room.h>

inherit ROOM;

void create()
{
	set("short", "过道");
	set("long", @LONG
这里是进入圣殿的必经之路，雪白地墙壁，看不到一丝的污点，使人油
然升起一种圣洁的感觉，
LONG );
	set("exits", ([
		"south" : __DIR__"beidajie2",
		"north" : "/d/shaolin/yidao",
		"west"  : "/d/huanghe/caodi1",
	]));
        set("objects", ([
        ]));
	set("outdoors", "shendian");
	setup();
}
