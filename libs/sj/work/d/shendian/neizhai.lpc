inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIY "电信大楼" NOR);
  set("long", @LONG
这里是圣殿电信大楼，你可以在这里买到基本的通信工具，手机和无线电。
LONG
  );



  set("exits", ([
    "east": __DIR__ "shangjie1",

  ]));
  set("objects", ([
    __DIR__ "npc/dxlaoban": 1,
  ]));

  setup();
}
