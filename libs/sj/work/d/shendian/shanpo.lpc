#include <room.h>

inherit ROOM;

void create()
{
	set("short", "山坡");
	set("long", @LONG
这是圣岛靠东的一个小山坡，向前望去可以看到微微突起的高地。
LONG );
	set("exits", ([
		"east" : __DIR__"gaodi",
		"west"  : __DIR__"luyindao",
          "north"  : __DIR__"xiulisi",
         "south"  : __DIR__"xiulisi1",
	]));
        set("objects", ([
        ]));
	set("outdoors", "shendian");
	setup();
}
