#include <room.h>

inherit ROOM;

void create() {
  set("short", "绿荫道");
  set("long", @LONG
这是一条并不十分宽敞的小道，道路的两旁的栽着两排
四季常青的树木，树的树叶整整齐齐地，显然是被修剪过。
LONG);
  set("exits", ([
    "east": __DIR__ "shanpo",
    "west": __DIR__ "guangchang",
  ]));
  set("objects", ([
  ]));
  set("outdoors", "shendian");
  setup();
}
