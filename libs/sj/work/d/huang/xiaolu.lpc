// Room: /city/caizhu.c
// wuliao 1999/12/04 

inherit ROOM;

void create()
{
        set("short", "小路");
        set("long", @LONG
这是一条弯弯曲曲的小路，前面不远的地方好象可以看到水光，
隐隐约约有个人影。
LONG);
        set("exits", ([
                "west" : __DIR__"shijie",
                "north" : __DIR__"shatan",
        ]));
//      set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

