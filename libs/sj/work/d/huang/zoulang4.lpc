#include <ansi.h>

inherit ROOM;

void create()
{
    set("short", "走廊");
    set("long", @LONG
这是一条走廊,两边都是汉白玉柱子, 看起来十分壮观,往西
就是皇族的大厅，里面透出一股强烈的霸者之气。
LONG );
    set("exits", ([
        "south" : __DIR__"zoulang2",
        "west" : __DIR__"dating",
        "north" : __DIR__"huayuan",

    ]));
 set("objects", ([   "/kungfu/class/huang/weishi": 2,        ]));
    set("outdoors", "huang");
    setup();
    replace_program(ROOM);     replace_program(ROOM); 
}
