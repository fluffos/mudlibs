#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "王府");
  set("long", @LONG
这里就是皇族坐第二把交椅的南角王的居所。四壁都是一幅
幅的地图，看来皇族军师就是南角王了。
LONG
  );

  set("exits", ([
    "east": __DIR__ "huangong",

  ]));
  set("outdoors", "huang");
  set("objects", ([ "/kungfu/class/huang/wang": 1, ]));
  setup();
}
