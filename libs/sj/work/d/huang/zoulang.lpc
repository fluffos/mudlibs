#include <ansi.h>

inherit ROOM;

void create()
{
    set("short", "走廊");
    set("long", @LONG
这是一条走廊,两边都是汉白玉柱子, 看起来十分壮观,东面是
演武场，西面还是一条幽深的走廊。
LONG );

    set("exits", ([
        "east" : __DIR__"yanwuchang",
        "west" : __DIR__"zoulang2",
                
    ]));
   
 set("objects", ([   "/kungfu/class/huang/weishi": 4,        ]));
    set("outdoors", "huang");
    setup();
    replace_program(ROOM); 
}

