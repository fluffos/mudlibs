// Room: /city/caizhu.c
// wuliao 1999/12/04 

inherit ROOM;

void create()
{
        set("short", "杂货铺");
        set("long", @LONG
这是皇族的杂货铺，里面有一条黑龙负责卖货，你瞧了一下，
立刻被它的威慑力给镇住了，还是不惹事的好。
LONG);
set("objects", ([   "/d/huang/npc/boss": 1, 
                 ]));
        set("exits", ([
                "east" : __DIR__"road2",
        ]));
//      set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

