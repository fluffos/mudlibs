#include <room.h>

inherit ROOM;

void create()
{
	set("short", "车房");
	set("long", @LONG
车站西北的停车场,每条停车位都有白\黄两种颜色划好,显得井然有
序,但现在你却看不到一辆车子,可以都出去了吧.
LONG );
	set("exits", ([
		"southeast"  : __DIR__"chezhan",
	]));
        set("objects", ([
//                "/d/shendian/npc/zhoubingung" : 1,
        ]));
	set("outdoors", "shendao");
	setup();
}
