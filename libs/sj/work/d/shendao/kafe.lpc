#include <room.h>

inherit ROOM;

void create()
{
	set("short", "咖啡屋");
	set("long", @LONG
一间不大的咖啡屋,装饰并不十分漂亮,但来这喝咖啡的人却并不少.
一位中等年龄的女老板,站在巴台里,一双老鼠眼睛,四下张望着.
LONG );
	set("exits", ([
		"south"  : __DIR__"cheneast",
		"enter"  : __DIR__"baoxian",
	]));
        set("objects", ([
//                "/d/shendian/npc/zhoubingung" : 1,
        ]));
	set("outdoors", "shendao");
	setup();
}
