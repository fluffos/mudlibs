#include <room.h>

inherit ROOM;

void create()
{
	set("short", "华都宾馆");
	set("long", @LONG
圣殿最大的一家宾馆,能来这里的都是些很在体面的人物,但是
现在只见站口钉着一张大的版子:

             [1m[1;31m 内  部  装  修

              暂  停  营  业

                                  [1m[1;36m 董事长:lsg[2;37;0m
LONG );
	set("exits", ([
		"east" : __DIR__"zhouyanjie1",
	]));
        set("objects", ([
//                "/d/shendian/npc/zhoubingung" : 1,
        ]));
//	set("outdoors", "shendao");
	setup();
}
