#include <ansi.h>

#include <room.h>
inherit ROOM;

void create() {
  set("short", "厨房");
  set("long", @LONG
这里是紫阳宫的人用餐的地方，屋子里传出阵阵香气，新烤好的蛋糕出炉了，你
饿了的话，就可以来要点食物(food)和水(water).
LONG
  );
  set("exits", ([
    "east": __DIR__ "zoulang1",
  ]));

  set("objects", ([
    __DIR__ "npc/madem": 1,
  ]));

  setup();
  replace_program(ROOM);

}
