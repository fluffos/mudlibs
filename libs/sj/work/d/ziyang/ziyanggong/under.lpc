

inherit ROOM;

void create()
{
    set("short", "地下宫殿入口");
	set("long", @LONG
这里竟然有一处地下宫殿，原来，紫阳宫的后人发现了这里的旧址后，
因为地上面辐射仍很强烈，所以在这里修了一座地下宫殿。
LONG
	);

	set("exits", ([
       “up" : __DIR__"shanmen",
	]));
	setup();
}
