//houtang.c
//date:12.19
inherit ROOM;

void create() {
  set("short", "圣殿后堂");
  set("long", @LONG
这里是圣殿的后堂，正中坐着一人红光满面，似乎就是
这座圣殿的圣主，旁边有两间房不知通向何处。
LONG);
  set("exits", ([
    "north": __DIR__ "houyuan",
    "south": __DIR__ "zhengting",
    "east": __DIR__ "zhanglaodian",
    "west": __DIR__ "cedian",
  ]));

  setup();
  replace_program(ROOM);
}
