//caroom.c
//date:12.17
inherit ROOM;

void create() {
  set("short", "停车房");
  set("long", @LONG
这里是天宾馆专给客人停车的地方，只见你的正前
方真是什么车都有，让你看得目瞪口呆。
LONG);
  set("exits", ([
    "west": __DIR__ "hotel",
  ]));

  setup();
  replace_program(ROOM);
}
