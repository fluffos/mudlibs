//hotel.c
//date:12.17
inherit ROOM;

void create()
{
    set("short","天宾馆");
    set("long",@LONG
这是圣殿里最负盛名的天宾馆，就单从外表来看，就
已经显示出了那不同凡响的气派，进出的人都显的更
有身份。
LONG);
    set("exits",([
        "west":__DIR__"road1",
        "up":__DIR__"floor1",
        "east":__DIR__"caroom",
    ]));

    set("outdoors","earth");
    setup();
    replace_program(ROOM);
}
