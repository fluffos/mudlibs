// didao1.c 地道1
inherit ROOM;
#include <ansi.h>

void create()
{
      set("short","地道");
      set("long",@LONG
这里是特种部队军事防御的地下第一层，边上堆满了
激光枪、激光炮。几个合成人正在周围警戒，不时发
出叫喊声。
LONG);
        set("exits",([ /* sizeof() == 1 */
          "up"     : __DIR__"rukou",
 "down"     : __DIR__"didao2",
          "east"     : __DIR__"xunlianshi",
              ]));
      setup();
}
