inherit ROOM;
#include <ansi.h>

void create()
{
      set("short","地道");
      set("long",@LONG
这里是特种部队军事防御的地下第二层，边上堆满了
激光枪、激光炮。几个合成人正在周围警戒，不时发
出叫喊声。
LONG);
        set("exits",([ /* sizeof() == 1 */
          "up"     : __DIR__"didao1",
          "down"     : __DIR__"didao3",
 "east"     : __DIR__"xunlianshi1",
              ]));
      setup();
}

