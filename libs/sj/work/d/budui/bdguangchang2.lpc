inherit ROOM;

void create() {
  set("short", "特种部队联盟镇第三层");
  set("long", @LONG
这里是特种部队联盟镇的第三层，这一层是特种部队联盟的办公区。
里面高科技如云，如果你想买什么高级货，来这一层准没错。这一层
看上去，显然没有第二层热闹，但是周围弥漫着浓郁的高科技的气息！
LONG
  );

  set("valid_startroom", 1);

  set("exits", ([
    "down": __DIR__ "bdguangchang1",
    "east": __DIR__ "luxingchu",
    "north": __DIR__ "tiantailu1",
    "west": __DIR__ "renminglu1",

    "south": __DIR__ "changjianglu1",
  ]));

  set("objects", ([ __DIR__ "npc/tezhongbing": 3, ]));
  setup();
  replace_program(ROOM);
}
