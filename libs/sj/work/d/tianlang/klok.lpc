// Room: /d/tianlang/street.c

inherit ROOM;
void create() {
  set("short", "卡啦OK厅");
  set("long", @LONG 
这里是一个卡啦OK厅，这里有很多陪唱女在这里唱歌，
不过我们这里不提供色情服务。
LONG
  );
  set("objects", ([
    "/d/tianlang/npc/genv": 1,
    "/d/tianlang/npc/xietingfeng": 1,
  ]));
  set("exits", ([
    "south": __DIR__ "street4",
  ]));
  setup();
  replace_program(ROOM);
}
