inherit ROOM;

void create()
{
        set("short", "书院");
        set("long", @LONG
这里是一间宽敞的书院，虽然房子看起来很老旧了，但是打扫得很
整洁，墙壁上挂著一幅山水画，意境颇为不俗。
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
                "west" : __DIR__"street6",
]));

        set("objects", ([
                __DIR__"npc/teacher": 1 ]) );
        setup();

}

