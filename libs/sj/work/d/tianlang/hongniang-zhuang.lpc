// Room: /u/yz/marryhouse.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create()
{
        set("short", "红娘庄");
        set("long", @LONG
你现在正站在红娘庄里，这里是专门替人缔结婚约和解除婚约的地方，
城里所有的夫妇都是在这里喜结良缘的，就连附近镇子也有许多慕名而来的。
    靠近门口的地方有一块乌木雕成的招牌(sign)。
    媒婆身旁，摆着一张鲜红色的请帖(qingtie)。
LONG
);
        set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/mei_po" : 1,
]));
        set("item_desc", ([ /* sizeof() == 2 */
  "qingtie" : "这是玩家结婚送给媒婆的请贴，输入check可以查询结婚记录。
",
  "sign" : "缔结或解除婚约。
",
]));
        set("no_clean_up", 0);
        set("exits", ([ /* sizeof() == 1 */
"north" : __DIR__"street5",
]));

        setup();
        replace_program(ROOM);
}
