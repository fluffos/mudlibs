// Room: /d/changan/post_office.c

inherit ROOM;

void create() {
  set("short", "电子邮局");
  set("long", @LONG
这里是负责长安城官府文书跟军令往来的驿站，驿站大门口上
悬著一块木牌(sign)，上面写了些字。旁边有一份使用说明书(note)。
LONG
  );
  set("no_clean_up", 0);
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/post_officer": 1,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "street2",
  ]));
  set("item_desc", ([ /* sizeof() == 1 */
    "sign": "牌子上写道：
    奉令尹令喻，本驿著日起收发事宜扩及官员百姓上下人等
通办，官员及兵部该管军士、官府衙役免费，一般良民百姓书信收
费每通十文，收信免费，物品收费依份量大小另计，贵重细软倘有
遗失退费加五成赔偿，不得异议。
",
    "note":
    @TEXT
  本驿可以玩家之间互寄信件，具体用法:
  ask officer about mail
  ask officer about 寄信
TEXT,
  ]));

  setup();
  replace_program(ROOM);
}
