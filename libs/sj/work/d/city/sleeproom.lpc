inherit ROOM;
void create() {
  set("short", "休息室");
  set("long", @LONG
这里就是修理厂的休息室了，这里显得十分的安静，大家都在睡觉
你小心的走了进来，找了个空位，睡了起来。
LONG);
  set("exits", ([
    "out": __DIR__ "repireroom",
  ]));
  set("sleep_room", 1);
  set("no_fight", 1);
  set("no_steal", 1);
  setup();
}
