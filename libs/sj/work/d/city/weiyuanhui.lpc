// Room: /city/weiyuanhui.c
// zurn 1995/12/23 

inherit ROOM;
void create()
{
        set("short", "居民管理委员会");
        set("long", @LONG 
这里是中心城的居民管理委员会，你在这里可以办理购房，
结婚，申请户口等手续，这里是使你成为中心城一员的重
要机构。
LONG);
        set("objects", ([
        ]));
        set("exits", ([
                "north" : __DIR__"xidajie3",
        ]));
      setup();
}
