// dong.c 

#include <room.h>
inherit ROOM;

void create() {
  set("short", "废弃的防空洞");
  set("long", @LONG
你一进来就只听见身后一声巨响，‘嘭’你心里一惊，心想完了。
回头一看，果然那扇门竟然关了。
LONG);
  set("objects", ([
    __DIR__ "npc/laoshu": 1,
  ]));
  //      set("no_clean_up", 0);
  setup();
}
