// throwing.c

inherit SKILL;

mapping *action = ({
  ([
    "action": "$N将$w对准$n的$l射了过去",
    "damage": 10,
    "damage_type": "刺伤",
  ])
});

mapping query_action() {
  return action[random(sizeof(action))];
}
int learn_bonus() {
  return 0;
}
int practice_bonus() {
  return 0;
}
int black_white_ness() {
  return 0;
}
