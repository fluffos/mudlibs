#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", RED"我的休息室"NOR);
        set("long", @LONG

丁当的休息室，外人不得入内。真不知道你这个家伙是怎么闯进来的。
LONG
        );

        set("sleep_room",1);
        set("if_bed",1);

 	setup();
}
