// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */
 
//standroom.c used by weiqi...others may hate this format:D
//wroad1.c

inherit ROOM;

void create ()
{
   set ("short", "[37m冰崖底[m");
   set ("long", @LONG

看样子前面是过不去了。往西的路被一块大牌子堵死。牌子上用
醒目的大字写着：“冰谷禁地，擅闯者死！”。在这可以学习造
冰刀的技术(ask xunshi about 冰刀).
LONG);

   //set("item_desc", ([" *** " : " *** ", ]));
   //for look something.

   set("exits", 
   ([ //sizeof() == 4
     "east" : __DIR__"binggu",
   ]));
   
set("objects", ([
__DIR__"npc/xunshi2": 1,
]));
   set("outdoors", "xuesan");

   setup();
}
