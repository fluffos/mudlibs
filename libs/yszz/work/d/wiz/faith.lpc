inherit ROOM;

void create ()
{
        set ("short", "[1;35m关于忠诚[m");
        set ("long", @LONG

        忠诚度与学习技能有很大关系，请help faith.

LONG);
        set("exits",([
                "south" : __DIR__"faith2",
                 "west":__DIR__"enter",                         
        ]));
   set("no_clean_up", 0);
        setup();
   replace_program(ROOM);
}
