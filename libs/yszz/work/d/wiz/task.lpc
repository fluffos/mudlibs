inherit ROOM;
#include <ansi.h>
void create ()
{
        set ("short", HIR"关于TASK"NOR);
        set ("long", @LONG

        如果您想让经验值涨的快点，help task

LONG);
        set("exits",([
                "north" : __DIR__"renwu",
                "east" : __DIR__"faith2",]));
   set("no_clean_up", 0);
        setup();
  replace_program(ROOM);
 }
