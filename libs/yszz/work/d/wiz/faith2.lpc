
inherit ROOM;

void create ()
{
        set ("short", "[1;36m最新变化[m");
        set ("long", @LONG

        关注最新变化，可以让您与游戏同步，help news

LONG);
        set("exits",([
                "west" : __DIR__"task",
                "north" : __DIR__"faith",]));
   set("no_clean_up", 0);
        setup();
  replace_program(ROOM);
}
