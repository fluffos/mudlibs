inherit ROOM;
#include <ansi.h>
void create ()
{
        set ("short", HIY"灭妖与斗佛"NOR);
        set ("long", @LONG

        中等玩家必修课 help renwu

LONG);
        set("exits",([
                "east" :__DIR__"fabao",
                "south" : __DIR__"task",
]));
   set("no_clean_up", 0);
        setup();
  replace_program(ROOM);
  }
