// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */
 
// created by snowcat.c 5/23/1997
// room: /d/pantao/yaob.c

inherit ROOM;


void create ()
{
  set ("short", "飞霞殿前殿");
  set ("long", @LONG
     这就是西王母的飞霞殿了.不知道怎么回事,这回儿殿上静悄悄的,
虽然两旁列满了诸路神仙,但他们都面容肃穆,大气都不敢透一口,可能是西
王母正在发脾气.
LONG);

  set("exits", ([
        "south" : __DIR__"gate",
        "northup" : __DIR__"zhendian",
      ]));
  set("objects", ([
  __DIR__"npc/xuannu": 1,
  __DIR__"npc/longnu": 1,
]));
  setup();
}
  int valid_leave(object me, string dir)
{
        object a;
        if( wizardp(me)) return 1;

        if (dir == "northup") {
             if(objectp(a=present("xuan nu", environment(me))) && living(a))
               return notify_fail("九天玄女道:娘娘现下不想见外客.\n");
              if(objectp(a=present("long nu", environment(me))) && living(a))
                return notify_fail("南海龙女道:娘娘心情不好,请回吧!\n");
return ::valid_leave(me, dir);
        }

        return 1;
}

