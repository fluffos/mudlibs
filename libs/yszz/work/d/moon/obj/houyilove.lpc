inherit ITEM;

void create() {
  set_name("后羿的回信", ({ "houyilove" }));
  set_weight(80);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "后羿写给嫦娥的情谊绵绵的回信.\n");
    set("unit", "封");
  }
}
