inherit ITEM;
inherit F_FOOD;

void create() {
  set_name("嫦娥的情书", ({ "paper" }));
  set_weight(80);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一封信，是月宫仙子写给后羿的情书.\n");
    set("unit", "封");
  }
}
