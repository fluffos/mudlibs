// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */

// Room: /moon/neartop.c
//cglaem...12/13/96.

inherit ROOM;

void create() {
  set("short", "[37m逍遥谷[m");
  set("long", @LONG

这里是逍遥谷底,靠近玉女峰顶。虽说是冰雪遍地，你并不觉得太冷。四周冰缝中长着
一些洁白的雪莲。
LONG);

  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "obj/xuelian": 2,
  ]));
  set("outdoors", 1);
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "neartop",
  ]));

  setup();
}
