// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */
 
// name.c

#include <ansi.h>
#include <dbase.h>

nosave string *my_id;

string *query_my_id()
{
  return my_id;
}

void set_name(string name, string *id)
{
  set("name", name);
  set("id", id[0]);
  my_id = id;
}

int id(string str)
{
  string *applied_id;

  if( this_player() && !this_player()->visible(this_object()) ) return 0;

  if( pointerp(applied_id = query_temp("apply/id")) 
  &&  sizeof(applied_id) ) 
   if( member_array(str, applied_id)!=-1 )
    return 1;
   else
    return 0;
    
  // If apply/id exists, this object is "pretending" something, don't
  // recognize original id to prevent breaking the pretending with "id"
  // command.

  if( pointerp(my_id) && member_array(str, my_id)!=-1 )
   return 1;
  else
   return 0;
}

string *parse_command_id_list()
{
  string *applied_id;

  if( pointerp(applied_id = query_temp("apply/id")) 
  &&  sizeof(applied_id) )
   return applied_id;
  else
   return my_id;
}

varargs string name(int raw)
{
  string str, *mask;
  
  if( !raw && sizeof(mask = query_temp("apply/name")) )
   return mask[sizeof(mask)-1];
  else {
   if( stringp(str = query("name")) )
    return str;
   else
    return file_name(this_object());
  }
}

varargs string short(int raw)
{
  string title, nick, str, *mask, degree ;
  
  if( !stringp(str = query("short")) )
   str = name(raw) + "(" + capitalize(query("id")) + ")";

  if( !this_object()->is_character() ) return str;
       
  if (!raw) {
                if ((int)query_temp("pending/exercise") != 0)
                        return name() + "[32m 修炼内力中.....[m";
                else if ((int)query_temp("pending/respirate") != 0)
                        return name() + "[35m 修炼法力中.....[m";
        }

  if( !raw && sizeof(mask = query_temp("apply/short")) )
   str = (string)mask[sizeof(mask)-1];
  else {
   if( stringp(nick = query("nickname")) )
    str = sprintf("「%s」%s", nick, str);

        if( !raw && sizeof(mask = query_temp("apply/title")) )
            title = (string)mask[sizeof(mask)-1];
        else 
          title = query("title");
        
   if( stringp(title) )
    str = sprintf("%s%s%s",title,(nick?"":" "), str);
   
  if( stringp(degree = query("degree")) )
    str = sprintf("『%s』%s%s%s", degree,(title?"":" "),(nick?"":" "), str);
      
  }

  if( !raw ) {
   if( this_object()->is_ghost() ) 
        return name() + "[34m 现在是一个鬼......[m";
   if( query_temp("netdead") ) 
        return name() + "[35m 断线了...[m";
   if( in_input() ) 
        return name() + "[32m 现在在写字....[m";
   if( in_edit() ) 
        return name() + "[1;33m 在工作.....[m";
   if( interactive(this_object())
   &&  query_idle( this_object() ) > 240 ) 
        return name() + "[33m 现在是个呆子.....[m";
   if( !living(this_object()) ) 
        return name() + "[35m 昏了过去......[m";
  }
return str;
}

varargs string long(int raw)
{
  string str, extra, *mask;
  
  if( !raw && sizeof(mask = query_temp("apply/long")) )
   str = mask[sizeof(mask)-1];
  else if( !stringp(str = query("long")) )
   str = short(raw) + "。\n";

  if( stringp(extra = this_object()->extra_long()) )
   str += extra;

  return str;
}

