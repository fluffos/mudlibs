// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */

//changan city

inherit ROOM;

void create() {
  set("short", "社会活动处");
  set("long", @LONG
这是一些初级完家挣钱的地方,为了完mud累一点也无所谓
有的却只好干些体力活 .欢迎大家来 （ｗｏｒｋ）！！！
在桌子上放了一个怪怪的东西(guaiwu),你不禁想看看是什么.
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "north": "/d/wiz/entrance",
      "west": "/u/city/qishi",
    ]));

  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/taitai": 1,
      "/clone/misc/vcd": 1,
    ]));
  set("outdoors", "changan");
  //   set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
