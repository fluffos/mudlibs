		类ＢＢＳ留言板ＲＥＡＤＭＥ文件 
		   (by ammy@yszz 12/08/2000)


㈠设计思想
①申明:这个程序很大程度上借鉴烛光泪影发布与bbs的文章！
②设计思想
	1)玩家在quit时，提供了4种选择：不留言，给玩家，给巫师，返回世界
	2)玩家在新上线时能看到自己的留言以及新的留言。
	3)文件的存取方式是存成许多文件，而不是一个。
③文件改动说明
	由于系统在发呆时间过长，或者键入相通的命令过多会简单的command("quit")，
	而在新的改动中会总在那里等待玩家选择，所以相应的程序需要改动。
④使用说明
	由于留言存取成多个文件，这些文件是按数字排序命名，所以删除留言的时候不
	能简单的删除一两个文件，因为那样的话，会造成以后的存取出错。
	如果要清除留言的话，必须把两个目录下的文件都rm，然后保证在没有玩家留言
	的情况下面重起，以清除玩家的留言查看纪录。这时存在一个弊端，就是，如果
	有玩家不是在重起时马上重新登陆的，这是又有玩家写了留言，那么它将看不到
	这些文章！
	想说的一点是，如果条件允许，不在万不得以，最好不要删两个数据目录下面的
	东西！
	
㈡相关文件:

/adm/daemons/bbsfiled.c
/adm/daemons/dis_bbs_msg.c
/adm/deamons/quitd.c
/cmds/usr/quit.c

㈢修改文件:

①
/include/globals.h
加入
#define BBSLEAVE_DIR            "/data/bbs/leavemsg_player/" 
	//定义给玩家留言的存放目录
#define BBSLEAVE_TOWIZ_DIR      "/data/bbs/leavemsg_towiz/" 
	//定义存放给巫师留言的目录
#define DISBBSMSG_D "/adm/daemons/dis_bbs_msgd"　
	// 跟显示留言相关的一些函数
#define BBSFILE_D "/adm/daemons/bbsfiled" 
	// 留言时需要调用
#define QUIT_D "/adm/daemons/quitd" 
　　　// 包含原来/cmds/usr/quit.c 的基本功能用这个文件中的leave_world()来实现
　　　// 如果以后要用到强制玩家直接退线的话　也得用QUIT_D->leave_world()

②
/adm/daemons/logind.c
在所有enter_world();后面加入
DISBBSMSG->msg_player();
DISBBSMSG->msg_wizard();
当然，如果只是想在正常进入的时候能够查看留言板，也可以只在一处加入这两行，如:
void get_passwd()
{
	......
        enter_world(ob, user);
        DISBBSMSG_D->msg_player();
        DISBBSMSG_D->msg_wizard();
        return;
 	.....
}	


③
/std/user/user.c(ES2)的改动
将所有的command("quit")
换成     QUIT_D->leave_world(this_object());

④
/feature/alias.c的改动
将所有的command("quit")
换成     QUIT_D->leave_world(this_object());
