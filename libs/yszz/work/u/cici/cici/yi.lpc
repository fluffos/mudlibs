// Room: /u/cici/house.c

#include <room.h>
.#include<room.h>
inherit ROOM;

void create()
{
	set("short", "工作室");
	set("long", @LONG
工作地点LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "up" : "/u/shenxian/rubbish",
  "down" : "/u/shenxian/workroom",
]));
	set("no_clean_up", 0);
//set("up", "/u/shenxian/rubbish.lpc");

	setup();
//replace_program(ROOM);
}
