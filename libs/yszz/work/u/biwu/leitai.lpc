// Room: /u/biwu/leitai.c
// Date: bye linxue([1;37m林雪[2;37;0m) Thu Apr 29 18:48:28 1999
inherit ROOM;

void create()
{
	set("short", "擂台上");
	set("long", @LONG
这里是一个露天擂台下面，是武林人物比武的地方，也是江湖女
子举行比武招亲的较技场所。边上有一个兵器架(jia)，你可以取用合
手的兵器和防具。如果一切就绪，可以输入(ok)，认输是(giveup)，
如果没有对手了可以用（quit）下去。

LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "down" : __DIR__"biwu",
]));
	set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}
