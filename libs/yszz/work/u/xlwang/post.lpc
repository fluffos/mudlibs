inherit ROOM;
#include <ansi.h>
void create() {

  set("short", "修罗王接待室");
  set("long", @LONG


这里主要是为了这次系统大变动设的post专区,
对perform的强弱有什么意见请在此post

LONG);
  set("exits", ([

    "up": "/u/city/center",
  ]));
  set("no_fight", 1);
  set("no_magic", 1);
  setup();
  call_other("/obj/board/xlwang_post_b", "???");
}
