#include "playerroom.h"
inherit ROOM;
void create() {
  set("short", "泰南二区");
  set("long", @LONG
你已来到了清净整洁的居民小区，西面就是宽阔的石板大街上，街面是由碗
口大的彩色石块铺成，直通城中心。规模不小的宅院竖立在周围。
LONG
  );
  set("owner", "public");
  set("exits", ([
    "linfeng": __PLAYERROOM__ "room/l/linfeng/linfeng927045026", /* sizeof() == 2 */
    "east": __DIR__ "sroad",
    "yiping": "/u/playerroom/room/y/yiping/yiping921143558",
  ]));
  set("no_clean_up", 0);
  set("coor", ([ /* sizeof() == 3 */
    "x": 10,
    "y": 2000,
    "z": 0,
  ]));
  setup();
}
