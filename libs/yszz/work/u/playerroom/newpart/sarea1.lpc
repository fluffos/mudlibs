#include "playerroom.h"
inherit ROOM;
void create() {
  set("short", "泰南一区");
  set("long", @LONG
你已来到了清净整洁的居民小区，东面就是宽阔的石板大街上，街面是由碗
口大的彩色石块铺成，直通城中心。规模不小的宅院竖立在周围。
LONG
  );
  set("exits", ([
    "wly": __PLAYERROOM__ "room/w/wly/wly926135401",
    "west": __DIR__ "sroad",
  ]));
  set("coor/x", 10);
  set("coor/y", 2000);
  set("coor/z", 0);
  set("owner", "public");
  setup();
  replace_program(ROOM);
}
