inherit ROOM;
void create()
{
        set("short", "舜耕小区空地");
        set("long", @LONG
这里是一块空地，可以建造自己的家。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "east" : __DIR__"sroad1",
]));
        set("outdoors", "playerroom");
        set("owner","public");
        set("coor/x",0);
        set("coor/y",-50);
        set("coor/z",0);
        set("coor/x",0);
        set("coor/y",-50);
        set("coor/z",0);
        setup();
        replace_program(ROOM);
}

