inherit ROOM;
void create()
{
        set("short", "舜耕小区广场");
        set("long", @LONG
这里是舜耕小区的中心广场，来来往往的人很多。中间一片绿色的大草坪，
还有一座全身雕像，高高的耸立在花岗岩的基座上，面目则始终看不清，给人一
种无法言喻的神秘感。广场的东北面是一座高耸入云的摩天大楼，就像一把锋利
的宝剑直插云霄，它就是本ＭＵＤ中巫师的大本营——接天楼。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"sroad1",
  "south" : "/d/qianfo/road1",
]));
        set("outdoors", "playerroom");
        set("coor/x",10);
        set("coor/y",2010);
        set("coor/z",0);
        set("coor/x",10);
        set("coor/y",2010);
        set("coor/z",0);
        setup();
}

int valid_leave(object me,string dir)
{
  if ( (dir=="north") && (!wizardp(me)) )
    return notify_fail("大天神正在构筑玩家新区,尚未完成,严禁通行!\n");
  return 1;
}
