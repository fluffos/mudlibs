inherit ROOM;
#include <ansi.h>
int block_cmd(string);
void create ()
{
  set ("short", HBRED"飞雪"HIY"小屋"NOR);
  set ("long", @LONG

这里是wizard 飞雪特地为各位上仙专开的雅房，
大家对Skills 有什么意见 好的见解以及各种bug，请在这里Post !

LONG);
  set("exits", ([ 
        "down" : "/d/kaifeng/tieta",
]));
set("objects", ([ /* sizeof() == 2 */
//               "/u/fly/npc/zhifu":1,
]));
  set("no_fight", 1);
  set("no_magic", 1);
  set("resource", ([ /* sizeof() == 1 */
  "water" : 1,
]));
  set("valid_startroom", 1);
  setup();
  call_other("/obj/board/fly_post_b", "???");
}
void init()
{
    add_action("block_cmd", "", 1);
    add_action("do_look","look");
    add_action("do_look","l");
}

int block_cmd(string args)
{
string verb;
verb = query_verb();

    if (verb == "bian") return 1;
    if (verb == "xiudao") return 1;
    if (verb == "exert") return 1;
    if (verb == "cast") return 1; //by cjl
    if (verb == "fly") return 1; //by cjl
        return 0;
}

