//happ
inherit ROOM;

void create() {
  set("short", "[32m[m");
  set("long", @LONG

        这里面可全部都是水帘洞里面的宝贝哦！
LONG);

  set("exits", ([
    "east": __DIR__ "out",
  ]));
  set("objects", ([
    //                __DIR__"obj/gun":1,
    "/d/obj/drug/xiantao": 2,
    "/d/obj/drug/fake-tao": 2,
  ]));
  replace_program(ROOM);

  setup();
}
