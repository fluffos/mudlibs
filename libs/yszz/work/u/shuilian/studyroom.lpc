
//happ
inherit ROOM;

void create()
{
        set("short","[0;32m学堂[m");
        set("long", @LONG
     别看这里住的是一窝猴，可是这里也有上知天文
下知地理的聪明猴子，水帘洞里除了文猴就是武猴，所以
想学习啊，还是要找这位猴先生试试吧。

LONG);


    set("exits",([
        "north":__DIR__"cave3",
"south":__DIR__"shuyuan",
]));
       set("objects",([
                __DIR__"npc/teacher" : 1,
                __DIR__"npc/saodihou": 1,


]));
replace_program(ROOM);

setup();
}
