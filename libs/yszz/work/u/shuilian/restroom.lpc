//happ@xyj
inherit ROOM;

void create() {
  set("short", "[0;36m休息室[m");
  set("long", @LONG
  这是一间休息室，当顽皮的猴子们玩的累的时候，他们
经常来到这个房间里睡上一觉，特别的是，这里面的床是用
石头做的。
LONG);
  set("exits", ([
    "south": __DIR__ "cave2",
  ]));
  set("sleep_room", 1);
  set("no_fight", 1);
  set("no_steal", 1);
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
