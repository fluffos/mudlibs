// Room:   /u/shuilian/uptree.c

inherit ROOM;

void create()
{
        set("short","[1;32m树上[m");
        set("long", @LONG

[33;1m………… 这里到处都是水果 ……… [m，旁边还有一棵树，你可以试
着跳过去。
LONG);
        set("objects",([
                "/clone/food/fruit": 3,
]));
        set("exits", ([
                "down": __DIR__"shuyuan",
                     ]));
        setup();
}
