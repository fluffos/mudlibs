//happ
inherit ROOM;

void create()
{
  set("short","[0;33m洞内[m");
  set("long", @LONG

        洞内是一片石器的世界，石碗，石桌，石凳，石床。
  石锅，石盆。中间一块石碣上：水帘洞洞天。

LONG);
        set("exits",([
                "out": __DIR__"dongkou",
                "west":__DIR__"cave2",
]));
setup();
}
int valid_leave(object me, string dir)
{
object map;

     if( dir == "out" && !(map=present("raincoat", me)) ) {
        return notify_fail("这么大的水，你是怎么进来的？\n");
        }
        return ::valid_leave(me, dir);
}
