#include <mudlib.h>

inherit ARMOR;

string C_NAME="[37;1m破神甲[37;0m";

void create()
{
	set_name( "The plate of antilordus", C_NAME );
	add( "id", ({ "plate", "platemail" }) );
	set_short( C_NAME );
	set_long(@LONG
这是一套闪闪发亮的战袍，传说是屠龙道人斩杀了长河恶蛟後
剥了它的皮制成的。一片片闪著辉光的鳞甲层层叠叠的覆满整套盔
甲，不仅能够提供严密的保护，更不会妨碍行动。
LONG
	);
	set( "unit", "套" );
	set( "type", "body" );
	set( "material", "swordman");
	set( "armor_class", 32 );
	set( "defence_bonus",4);
	set( "weight", 200 );
	set( "value", ({ 30, "gold" }) );
	set( "no_sale",1 );
}
