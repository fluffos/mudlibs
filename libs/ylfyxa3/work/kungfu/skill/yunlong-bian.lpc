// yunlong-bian.c 云龙鞭法	下乘武学100~300

#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>

inherit SKILL;
string *order = ({
  "" HIY "",
  "" HIG "",
  "" RED "",
  "" MAG "",
  "" YEL "",
  "" HIC "",
  "" HIW "",
  "" HIR "",
  "" HIB "",
  "" CYN "",
  "" WHT "",
  "" HIM "",
  "" BLU ""
});
string *xue_name = ({
  "劳宫穴", "膻中穴", "曲池穴", "关元穴", "曲骨穴", "中极穴", "承浆穴", "天突穴", "百会穴",
  "幽门穴", "章门穴", "大横穴", "紫宫穴", "冷渊穴", "天井穴", "极泉穴", "清灵穴", "至阳穴",
});

mapping *action = ({
  ([ "action": "$N将身一纵，跃在半空，一式" + (order[random(13)]) + "「彩凤栖梧」" NOR "，手中$w盘旋而下，鞭势灵动之至，击向$n$l",
    "attack": 100,
    "parry": 100,
    "dodge": 100,
    "damage": 100,
    "force": 100,
    "lvl": 5,
    "skill_name": "彩凤栖梧",
    "damage_type": "拉伤"
  ]),
  ([ "action": "$N沉肩滑步，手中$w一抖，一式" + (order[random(13)]) + "「凤凰展翅」" NOR "，迅捷无比地分打左右两侧，$n顿时左右支绌，慌了手脚",
    "attack": 100,
    "parry": 100,
    "dodge": 100,
    "damage": 100,
    "force": 100,
    "lvl": 25,
    "skill_name": "凤凰展翅",
    "damage_type": "拉伤"
  ]),
  ([ "action": "$N将内力注入$w，蓦地使出一式" + (order[random(13)]) + "「蛟龙戏凤」" NOR "，$w矫夭飞舞，直如神龙破空一般抽向$n",
    "attack": 100,
    "parry": 100,
    "dodge": 100,
    "damage": 100,
    "force": 100,
    "lvl": 45,
    "skill_name": "蛟龙戏凤",
    "damage_type": "拉伤"
  ]),
  ([ "action": "$N一声清啸，手中$w一招" + (order[random(13)]) + "「龙飞凤舞」" NOR "，划出漫天鞭影铺天盖地地向$n卷来，势道猛烈之极",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "lvl": 65,
    "skill_name": "龙飞凤舞",
    "damage_type": "拉伤"
  ]),
  ([ "action": "$N面露微笑跨前一步，右手$w轻扬，缓缓使出一式" + (order[random(13)]) + "「龙凤呈祥」" NOR "，鞭势平和中正，不带丝毫霸气",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "lvl": 85,
    "skill_name": "龙凤呈祥",
    "damage_type": "拉伤"
  ]),
  ([ "action": "$N单手一扬，一招" + (order[random(13)]) + "「开天辟地」" NOR "，手中$w抖得笔直，对准$n当头罩下",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "lvl": 0,
    "skill_name": "开天辟地",
    "damage_type": "劈伤"
  ]),
  ([ "action": "$N身形一转，一招" + (order[random(13)]) + "「龙腾四海」" NOR "，手中$w如矫龙般腾空一卷，猛地击向$n太阳穴",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "lvl": 10,
    "skill_name": "龙腾四海",
    "damage_type": "劈伤"
  ]),
  ([ "action": "$N唰的一抖长鞭，一招" + (order[random(13)]) + "「矫龙出水」" NOR "，手中$w抖得笔直，刺向$n双眼",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "lvl": 20,
    "skill_name": "矫龙出水",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N力贯鞭梢，一招" + (order[random(13)]) + "「破云见日」" NOR "，手中$w舞出满天鞭影，排山倒海般扫向$n全身",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "lvl": 30,
    "skill_name": "破云见日",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N运气于腕，一招" + (order[random(13)]) + "「开山裂石」" NOR "，手中$w向一根铜棍般直击向$n",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "lvl": 40,
    "skill_name": "开山裂石",
    "damage_type": "内伤"
  ]),
  ([ "action": "$N单臂一挥，一招" + (order[random(13)]) + "「玉带围腰」" NOR "，手中$w直击向$n腰肋",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "lvl": 50,
    "skill_name": "玉带围腰",
    "damage_type": "内伤"
  ]),
  ([ "action": "$N高高跃起，一招" + (order[random(13)]) + "「大漠孤烟」" NOR "，手中$w笔直向$n当头罩下",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "lvl": 60,
    "skill_name": "大漠孤烟",
    "damage_type": "内伤"
  ])
});

int valid_enable(string usage) {
  return (usage == "whip") || (usage == "parry");
}
int practice_level() {
  return 60;
}
int valid_learn(object me) {
  object weapon;
  //		if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
  if ((int)me->query("max_neili") < 10000)
    return notify_fail("你的内力不足，没有办法练云龙鞭法, 多练些内力再来吧。\n");
  if (me->query("family/family_name") != "云龙门")
    return notify_fail("不加入云龙门，学不了本门技能。\n");
  if ((int)me->query_skill("yunlong-shengong", 1) < 100)
    return notify_fail("你的云龙神功火候不够，无法学习本门技能。\n");
  if ((int)me->query_skill("whip", 1) < 100)
    return notify_fail("你的鞭法基础太浅。\n");
  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "whip")
    return notify_fail("你必须先找一条鞭子才能练鞭法。\n");
  if (me->query_skill("whip", 1) <= me->query_skill("yunlong-bian", 1))
    return notify_fail("你的基础不够，无法领会更高深的技巧。\n");
  return 1;
}

int practice_skill(object me) {
  object weapon;
  //		if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "whip")
    if (!objectp(weapon = me->query_temp("secondary_weapon"))
      || (string)weapon->query("skill_type") != "whip")
      return notify_fail("你使用的武器不对。\n");
  if (me->query("family/family_name") != "云龙门")
    return notify_fail("不加入云龙门，练习不了本门技能。\n");
  if ((int)me->query("qi") < 50)
    return notify_fail("你的体力不够练云龙鞭法。\n");
  me->receive_damage("qi", 5);
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  return action[random(sizeof(action))];  //2017.9.4阿飞改，将招式简单点
}

string perform_action_file(string action) {
  return __DIR__ "yunlong-bian/" + action;
}

mixed hit_ob(object me, object victim, int damage_bonus, int factor) {
  int damage2, ap, dp;
  object weap, weap1;
  weap = victim->query_temp("weapon");
  weap1 = me->query_temp("weapon");
  damage2 = (int)me->query_skill("yunlong-bian", 1);
  ap = me->query_skill("yunlong-bian", 1);
  dp = victim->query_skill("dodge", 1);
  if (damage_bonus < 100) return 0;
  if (me->query_skill("yunlong-shengong", 1) < 100) return 0;
  if (me->query("jiali") < 1 || me->query("neili") < 100 || me->query_skill_mapped("force") != "yunlong-shengong") return 0;
  if (damage2 < 10) damage2 = 10;
  if ((me->query_skill("yunlong-bian") > 150)
    && (me->query("neili") > 300)
    && random(ap) > dp / 10
    && random(6) == 2
    && me->query_skill_mapped("force") == "yunlong-shengong"
    && me->query("jiali") > 0) {
    message_vision(HIY "只见$N急速挥舞" + weap1->name() + "，转出无数个大小圈子，以阴柔之劲缠向对方！\n" NOR, victim, me);
    //		if (!victim->is_busy()) victim->start_busy(3);
    victim->receive_wound("qi", damage2 + damage_bonus / 2, me);
    me->add("neili", -100);
    if (victim->query_temp("weapon")) {
      message_vision(
        HIY "$n气运手臂用力一拽，" + weap->name() + "却无法从" + weap1->name() + "的缠绕中脱开，情急之下只好放弃了手中的兵刃。\n" NOR,
        victim,
        me
      );
      weap->move(environment(me));
      victim->reset_action();
    }
  }
  return 1;

}
