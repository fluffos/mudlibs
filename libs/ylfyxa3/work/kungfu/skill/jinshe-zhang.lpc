// jinshe-zhang.c	金蛇掌	下乘武学100~300
#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
inherit SKILL;
mapping *action = ({
  ([ "name": "千蛇出洞",
    "action": "$N双掌一错，一招「千蛇出洞」幻出漫天掌影拢向$n的$l",
    "attack": 100,
    "parry": 100,
    "dodge": 100,
    "damage": 100,
    "force": 100,
    "damage_type": "瘀伤"
  ]),
  ([ "name": "魔吼天地",
    "action": "$N暴喝一声，双掌连环推出，一招「大沼龙蛇」强劲的掌风直扑$n的$l",
    "attack": 100,
    "parry": 100,
    "dodge": 100,
    "damage": 100,
    "force": 100,
    "damage_type": "瘀伤"
  ]),
  ([ "name": "双蛇抢珠",
    "action": "$N双掌纷飞，一招「双蛇抢珠」直取$n的$l",
    "attack": 100,
    "parry": 100,
    "dodge": 100,
    "damage": 100,
    "force": 100,
    "damage_type": "瘀伤"
  ]),
  ([ "name": "游走式",
    "action": "$N提气缠身游走，一招「游走式」，森森掌风无孔不入般地击向$n的$l",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "damage_type": "瘀伤"
  ]),
  ([ "name": "盘身式",
    "action": "$N盘身错步，双掌平推，凝神聚气，一招「盘身式」拍向$n的$l",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "damage_type": "瘀伤"
  ]),
  ([ "name": "金蛇吐衅",
    "action": "$N左掌立于胸前，右掌推出，一招「金蛇吐衅」击向$n$l",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "damage_type": "瘀伤"
  ]),
  ([ "name": "金蛇翻身咬",
    "action": "$N使出「金蛇翻身咬」，身形凌空飞起，从空中当头向$n的$l出掌攻击",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "damage_type": "瘀伤"
  ]),
  ([ "name": "杯弓蛇影",
    "action": "$N使出一招「杯弓蛇影」，左掌化虚为实击向$n的$l",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "damage_type": "瘀伤"
  ]),
  ([ "name": "金蛇缠丝手",
    "action": "$N左掌画了个圈圈，右掌推出，一招「金蛇缠丝手」击向$n$l",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "damage_type": "瘀伤"
  ]),
  ([ "name": "灵蛇游八方",
    "action": "$N使出「灵蛇游八方」，身形散作八处同时向$n的$l出掌攻击",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "damage_type": "瘀伤"
  ]),
  ([ "name": "金蛇探头",
    "action": "$N使出金蛇游身掌法「金蛇探头」，如鬼魅般欺至$n身前，一掌拍向$n的$l",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "damage_type": "瘀伤"
  ]),
  ([ "name": "金龙升天",
    "action": "$N内气上提，全身拔起，一招「金龙升天」，双掌凌空拍下，$n的全身都被笼罩在掌力之下",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "damage_type": "瘀伤"
  ]),
});

int valid_learn(object me) {
  return notify_fail("金蛇游身掌没法教授。\n");
}

int valid_enable(string usage) { return usage == "unarmed" || usage == "parry"; }

mapping query_action(object me, object weapon) {
  return action[random(sizeof(action))];
}

int practice_skill(object me) {
  //		if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
  if ((int)me->query("qi") < 30)
    return notify_fail("你的体力不够了，休息一下再练吧。\n");
  if ((int)me->query("neili") < 5)
    return notify_fail("你的内力不够了，休息一下再练吧。\n");
  //	if ((int)me->query_skill("taixuan-gong", 1) < 100 )
  //		return notify_fail("你的太玄功火候不够，无法练习金蛇掌。\n");				
  me->receive_damage("qi", 30);
  me->add("neili", -5);
  return 1;
}
int effective_level() { return 10; }

string *parry_msg = ({
  "$n使出「粘」字诀，双掌一划，引偏了$N的$w。\n",
});

string *unarmed_parry_msg = ({
  "$n施展出金蛇游身掌，轻描淡写的化解了$N的攻势。\n",
});

string query_parry_msg(object weapon) {
  if (weapon)
    return parry_msg[random(sizeof(parry_msg))];
  else
    return unarmed_parry_msg[random(sizeof(unarmed_parry_msg))];
}

string perform_action_file(string action) {
  return __DIR__ "jinshe-zhang/" + action;
}

mixed hit_ob(object me, object victim, int damage_bonus) {
  int ap, dp, damage2;
  ap = me->query_skill("unarmed", 1) + me->query_skill("jinshe-zhang", 1);
  dp = victim->query_skill("dodge", 1) + victim->query_skill("parry", 1);
  damage2 = (int)me->query_skill("jinshe-zhang", 1);
  if (me->query("jiali") < 1 || me->query("neili") < 100 || damage_bonus < 100) return 0;
  if (me->query("jiali") > 0
    && random(ap) > dp / 8
    && random(6) == 1) {
    victim->receive_damage("qi", damage2 + damage_bonus / 2, me);
    victim->receive_wound("qi", damage2 + damage_bonus / 2, me);
    //		if (!victim->is_busy()) victim->start_busy(1);
    //	if (!me->is_busy()) me->start_busy(1);
    me->add("neili", -100);
    return HIM "$N双臂如蛇一般，柔弱无骨，瞬间摸遍$n全身！$n顿时浑身发凉，鸡皮疙瘩掉了一地！\n" NOR;

  }
}
