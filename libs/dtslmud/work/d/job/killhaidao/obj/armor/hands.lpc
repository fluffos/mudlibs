#include <armor.h>

inherit HANDS;

void create() {
  set_name("护手", ({ "hands" }));
  set_weight(900);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("material", "铁");
    set("unit", "双");
    set("armor_prop/armor", 2);
  }
  setup();
}
