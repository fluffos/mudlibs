--- a/adm/daemons/logind.lpc
+++ b/adm/daemons/logind.lpc
@@ -101,7 +101,9 @@
 	}
 
 */
-set_encoding("gbk");
+// Catalog WASM may lack GBK ICU data; never hard-error before login.
+  if (catch(set_encoding("gbk")))
+    set_encoding("utf-8");
   if (BAN_D->is_banned(query_ip_number(ob)) ||
     BAN_D->is_banned(query_ip_number(ob))) {
     write("对不起，您的地址被拒绝连线。
@@ -119,22 +121,35 @@
   int i, wiz_cnt, ppl_cnt, login_cnt;
   int log_num, extra_num;
 
+  // Catalog WASM FluffOS ships ICU without GBK converter data
+  // (ucnv_open("gbk") → U_FILE_ACCESS_ERROR). Native telnet still
+  // gets the bilingual GBK/UTF-8 menu; when GBK is unavailable the
+  // browser path is UTF-8-only, so skip the prompt and continue.
   if(arg == "0" || arg == "０") {
-	 	 set_encoding("gbk");
-	 	ob->set_temp("char_encoding", "gbk");
-	 } else if(arg == "1" || arg == "１") {
-	 	 set_encoding("utf-8");
-	 	ob->set_temp("char_encoding", "utf-8");
-	 } else {
-	      set_encoding("gbk");
-	 	write("    0) 国标码 (gbk) \n");
-	 	 set_encoding("utf-8");
-	 	write("    1) utf编码 (utf-8) \n");
-	      set_encoding("gbk");
-	      write("   请选择您使用的内码:\n");
-	 	input_to((: get_encoding :), ob);
-	 	return;
-	 }
+                if (catch(set_encoding("gbk"))) {
+                        set_encoding("utf-8");
+                        ob->set_temp("char_encoding", "utf-8");
+                } else {
+                        ob->set_temp("char_encoding", "gbk");
+                }
+         } else if(arg == "1" || arg == "１") {
+                 set_encoding("utf-8");
+                ob->set_temp("char_encoding", "utf-8");
+         } else {
+              if (catch(set_encoding("gbk"))) {
+                        set_encoding("utf-8");
+                        ob->set_temp("char_encoding", "utf-8");
+                        // fall through into post-choice login path
+              } else {
+                        write("    0) 国标码 (gbk) \n");
+                         set_encoding("utf-8");
+                        write("    1) utf编码 (utf-8) \n");
+                      set_encoding("gbk");
+                      write("   请选择您使用的内码:\n");
+                        input_to((: get_encoding :), ob);
+                        return;
+              }
+         }
   write("\n\n");
   UPTIME_CMD->main();
   usr = users();
