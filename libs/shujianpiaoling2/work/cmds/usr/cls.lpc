// more /cmds/usr/cls
#include <ansi.h>
inherit F_CLEAN_UP;
int main(object me, string arg)
{
        write(CLR);
        return 1;
}

int help(object me)
{
     write(@HELP
指令格式: cls

这个指令可以将屏幕上所有的文字消除。

HELP
    );
    return 1;
}
