// created by baiwt@sjpl
#include <ansi.h>
inherit ROOM;

//int do_quit();

void create() {
  set("short", "");
  set("long", @LONG

欢迎下次继续你在《书剑飘零》里的旅程。请再键入一次“quit”指令，谢谢！
LONG
  );
  set("no_study", 1);
  set("no_magic", 1);
  set("no_fight", 1);

  setup();
}

/*
void init()
{
	if (!wizardp(this_player())){ 
	     add_action("do_quit","doquit");
	     call_out("force_quit",1,this_player());
	     //use delayed quit to avoid the error message
	     //in debug.log due to this_player is destructed
	     //before the move() is finished.
	     // mon 8/17/97
	}
}

int do_quit()
{
    this_player()->force_me("quit");
    return 1;
}

void force_quit(object me)
{
   if(me) me->force_me("quit");
}
*/
