//changan city

inherit ROOM;

void create() {
  set("short", "午门");
  set("long", @LONG
这里是朝廷重犯开刀问斩的地方。中央高高竖起一根盘龙柱，
柱前有断头台，台上端端正正安放着一口大铡刀。
LONG);

  set("NONPC", 1);
  set("no_fight", 1);
  set("no_magic", 1);
  set("objects",
    ([  //sizeof() == 1
    "/u/xiyu/npc/army_1": 1,
    ]));
  setup();
  replace_program(ROOM);
}
