// Room: /u/fuzhou/nwroad1.c

inherit ROOM;

void create()
{
	set("short", "北市场");
	set("long", @LONG
这里是福州城的北市场，再向北可到城外。
    一股茶香与腥膻相混合的气味扑鼻而来，西首铺子外挂着个牛头;
东面是家二层的茶楼，金字招牌十分晃眼。
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
//  __DIR__"npc/jianke1" : 1,
]));
	set("outdoors", "cloud");
	set("exits", ([ /* sizeof() == 4 */
  "south" : __DIR__"nwroad2",
  "north" : __DIR__"entrance",
  "west" : __DIR__"butchery",
  "east" : __DIR__"tearoom",
]));
	set("no_clean_up", 0);
        set("objects", 
        ([ //sizeof() == 1
		"/obj/npc/shangren" : 2,
        ]));
	set("hell", 1);

	setup();
	replace_program(ROOM);
}
