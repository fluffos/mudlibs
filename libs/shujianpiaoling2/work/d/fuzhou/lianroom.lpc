// Room: /u/fuzhou/lianroom.c

inherit ROOM;

void create()
{
	set("short", "练功房");
	set("long", @LONG
这里是镖师门平时练功的地方.
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"biaoju",
]));
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/newfighter" : 2,
]));
	set("no_clean_up", 0);
	set("hell", 1);

	setup();
	replace_program(ROOM);
}
