inherit ROOM;
#include <room.h>
void create()
{
        set("short", "振远镖局大厅");
        set("long", @LONG
大红地毯，珍奇古玩，高悬字画，表明这里的主人不但性情豪爽，广交朋友，
且非大字不识，目中无人之辈。上首一狮皮交椅，左右各明烛闪烁。一古木书案
横于椅前，上面整齐的堆着一卷卷镖局压镖的货票。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "east" : __DIR__"biaoju6",
  "south" : __DIR__"biaoju7",
  "north" : __DIR__"biaoju2",
]));
        set("objects", ([
        __DIR__"npc/biaomaster" : 1,
                        ]) );
	set("hell", 1);

	setup();
        create_door("east", "铁门", "west", DOOR_CLOSED);

}
