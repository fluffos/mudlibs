
inherit ROOM;

void create()
{
	set("short", "山路");
	set("long", @LONG
这是上山的碎石大道，盘旋通往山上，路的左边是高高的石壁，
右边是陡峭的悬崖。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "eastdown" : __DIR__"npath2",
  "southup" :  __DIR__"npath4",
]));
	set("no_clean_up", 0);
	set("outdoors", "shiwan");

	setup();
	replace_program(ROOM);
}
