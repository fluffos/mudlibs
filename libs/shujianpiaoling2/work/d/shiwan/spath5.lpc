// Room: /u/yandang/spath5.c

inherit ROOM;

void create() {
  set("short", "黄土路");
  set("long", @LONG
这是一段长长的黄土大道，走在路上很容易使人忘记了路程的
远近，这里已经深入南诏境内，往南走可以到大理城。
LONG
  );
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/dizi-1": 2,
  ]));
  set("outdoors", "shiwan");
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "spath3",
    //  "south" : "/d/fuzhou/entrance",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
