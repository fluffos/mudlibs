inherit ROOM;

void create() {
  set("short", "树林");
  set("long", @LONG
这是一个大树林，山路较为平坦，北高南低，两旁绿树成荫，白天
也只有一丝的光线从树叶缝中透下来，显得有点阴深可怕。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "northup": __DIR__ "npath4",
    "southwest": __DIR__ "spath2",
  ]));
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
