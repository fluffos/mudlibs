
inherit ROOM;

void create()
{
	set("short", "万兽山庄");
	set("long", @LONG
这里是万兽山庄的大门，往西面上山就是万兽庄的所在地。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "eastdown" :  __DIR__"npath4",
  "westup"    :  __DIR__"wpath2",
]));
	set("no_clean_up", 0);
	set("objects", ([ /* sizeof() == 1 */
       __DIR__"npc/bear" : 1,
       __DIR__"npc/xiniu" : 1,
]));
       set("valid_startroom", 1);
	set("outdoors", "shiwan");

	setup();
	replace_program(ROOM);
}
