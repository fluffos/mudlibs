inherit ROOM;

void create() {
  set("short", "青石官道");
  set("long", @LONG
这是一条宽敞坚实的青石大道，向东通往扬州。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "ximen",
    "west": "/u/wudang/wdroad1",
  ]));
  set("outdoors", "yangzhou");
  set("objects", ([
    //"/u/lark/fnpc/dog":1,
  ]));
  setup();
  //        replace_program(ROOM);
}
void init() {
  add_action("do_go", "go");
}

int do_go() {
  if (random(3)) {
    this_player()->move(this_object());
    return 1;
  }
}
