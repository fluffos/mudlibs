// Room: /new/yangzhou/jiyuan.c

inherit ROOM;

void create()
{
	set("short", "丽春院");
	set("long", @LONG
这家妓院刚开张，正招聘妓女。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"street4",
]));
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/guipo" : 1,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
