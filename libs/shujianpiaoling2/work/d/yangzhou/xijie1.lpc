//yangzhou  nanjie.c
inherit ROOM;

void create()
{
        set("short", "西大街");
        set("long", @LONG
这里是扬州城的西大街，大街上人来人往，北面好象有一家药店，
南面好象是衙门。
LONG
        );
        set("exits", ([/* sizeof() == 4 */
           "east" : __DIR__"zhongxin",
           "west" : __DIR__"xijie2",
           "south" : __DIR__"yamen",
           "north" : __DIR__"yaodian",

]));
     set("outdoors", "changan");

set("objects",([
"/u/lark/fnpc/liumang":2,
]));

        setup();
        replace_program(ROOM);
}
