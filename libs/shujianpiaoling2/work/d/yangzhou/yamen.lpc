//yangzhou  yamen.c
inherit ROOM;

void create()
{
        set("short", "衙门");
        set("long", @LONG
这里就是扬州城的衙门了。衙门口摆着一对威武的石狮。朱红的大门
紧紧的关闭着，门上碗大的铜钉闪闪发光。在衙门口有两个兵丁一动不动
的站立着，没是你还是不要来这里吧。
LONG
        );
        set("exits", ([/* sizeof() == 4 */
           "south" : __DIR__"yamen2",
           "north" : __DIR__"xijie1",

]));
        set("objects", 
        ([ //sizeof() == 1
              __DIR__"npc/yayi1" : 2,
        ]));

        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}