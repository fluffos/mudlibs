// Room: /u/shandong/ta-road7.c

inherit ROOM;

void create() {
  set("short", "泰安城里");
  set("long", @LONG
这里是泰安城东的一个丁字路口。北面传来阵阵敲打的声音告诉你
那是一家武器店，门口还设有一个武器架上面摆满了各种各样的兵器，
共武林人士任意挑选、检验质量。南面则是“何财迷”开的当铺，从门
面上看去这家当铺到是已经有不少年月了。写着“当”字的布幌子在风
中懒散的来回飘荡。
    往北走是家武器店，
    往南走是家当铺，
    往西走则直通西门。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "east": __DIR__ "minzhai2",
    "west": __DIR__ "ta-road6",
    "north": __DIR__ "ta-wuqidian",
    "south": __DIR__ "ta-dangpu",
  ]));
  set("outdoors", "shandong");
  set("objects",
    ([  //sizeof() == 1
    "/obj/npc/girl": 1,
    ]));
  set("hell", 1);

  setup();
  replace_program(ROOM);
}
