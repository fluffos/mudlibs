inherit ROOM;

void create() {
  set("short", "青石官道");
  set("long", @LONG
这是一条宽敞坚实的青石大道，向西延伸。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "guanroad4",
    "east": __DIR__ "ta-ximen",
  ]));
  set("outdoors", "shandong");
  set("hell", 1);

  setup();
  //        replace_program(ROOM);
}
void init() {
  add_action("do_go", "go");

}

int do_go() {
  if (random(3)) {
    this_player()->move(this_object());
    return 1;
  }
}
