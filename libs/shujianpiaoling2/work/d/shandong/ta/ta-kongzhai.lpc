// Room: /u/shandong/ta-kongzhai.c

inherit ROOM;

void create()
{
        set("short", "空宅");
        set("long", @LONG
这里是一座空宅。也不知多长时间没人住过了，到处杂草丛生，散
发着一阵阵腐臭的气味，真叫人恶心。现在成了乞丐群居的地方了。
    往北走是一条东西走向的小街。
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"ta-road6",
]));
        set("objects", 
        ([ //sizeof() == 1
                __DIR__"npc/guituozi" : 1,
		"/d/fuzhou/npc/beggar" : 2,
        ]));
	set("hell", 1);

        setup();
        replace_program(ROOM);
}
