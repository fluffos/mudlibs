// Room: /u/wuhan/dianjia/wu1.c

inherit ROOM;

void create()
{
	set("short", "武场");
	set("long", @LONG
这里有几个女子正在互相切磋剑法。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"wu1",
]));
	set("objects", ([
	__DIR__"npc/trainer" : 2,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
