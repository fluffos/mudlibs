// yuzhu.c


inherit ITEM;

void create() {
  set_name("武昌鱼之珠", ({ "perle" }));
  set("long",
    "一颗黄稀稀的小珠子\n");

  set_weight(10);
  if (clonep())
    set_default_object(__FILE__);
  else
    set("unit", "颗");
  setup();
}
