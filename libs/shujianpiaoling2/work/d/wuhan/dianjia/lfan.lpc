// Room: /u/wuhan/dianjia/lfan.c

inherit ROOM;

void create()
{
	set("short", "点龙帆的家");
	set("long", @LONG
点龙帆是点家庄这一代最有学问的人, 在江湖中也有不少朋友。
这间房子里摆满了书和朋友们送的物什。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"street3",
  "north" : __DIR__"lfann",
]));
        set("objects", ([
	    __DIR__"npc/longfan" : 1,
]) );
	setup();
	replace_program(ROOM);
}
