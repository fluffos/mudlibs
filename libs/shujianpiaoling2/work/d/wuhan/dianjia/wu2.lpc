// Room: /u/wuhan/dianjia/wu2.c

inherit ROOM;

void create()
{
	set("short", "武场");
	set("long", @LONG
这里是年轻一代练功的地方。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"lianwuchang",
]));
	set("objects", ([
	__DIR__"npc/trainee" : 2,
	]) );
	setup();
	replace_program(ROOM);
}
