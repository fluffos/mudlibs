// Room: /u/wuhan/dianjia/yingu.c

inherit ROOM;

void create()
{
	set("short", "点音姑的家");
	set("long", @LONG
这间房子里充满了药味, 角角落落都堆满了瓶瓶罐罐。正中间有
一个小丹炉, 女主人正看着火候。
LONG
	);
        set("itme_desc", ([
	"瓶瓶罐罐" : "那里面装的是大大小小的药丸\n",
]) );
	set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"street3",
]));
        set("objects", ([
	__DIR__"npc/yingu" : 1,
]) );
	setup();
}
