// Room: /u/wuhan/dianjia/sankou.c

inherit ROOM;

void create() {
  set("short", "山口");
  set("long", @LONG
这是一间什麽也没有的空房间。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "southwest": __DIR__ "daotian",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
