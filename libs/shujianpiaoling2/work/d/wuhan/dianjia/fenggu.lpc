// Room: /u/wuhan/dianjia/fenggu.c

inherit ROOM;

void create()
{
	set("short", "点凤姑的家");
	set("long", @LONG
屋子不大, 因为收拾得很干净, 也不显得小。从打开的窗子向外
望去, 是一片云雾缭绕的山峦。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"street2",
]));
        set("objects", ([
	__DIR__"npc/fenggu" : 1,
]));
	setup();
}
