// Room: /u/wuhan/wsroad3.c

inherit ROOM;

void create()
{
	set("short", "城外大道");
	set("long", @LONG
这是武昌城外的一条大道, 两旁时见荒芜的稻田。走着走着
东边出现了一个很大的湖, 问问行人才知道那是南湖。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
	"north" :  __DIR__"wroad3",
  "south" : __DIR__"wsroad4",
]));
	set("no_clean_up", 0);
	set("outdoors", "jinling");

	setup();
	replace_program(ROOM);
}
