// Room: sroad1.c

inherit ROOM;

void create() {
  set("short", "船坞");
  set("long", @LONG
这里是长江两岸最大的一个船坞，四处可见各类船只的模型，还有
一些正在建造中的木船。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "jiangb1",
  ]));
  set("outdoors", "wuhan");
  set("objects", ([
    __DIR__ "npc/chuanwu_boss": 1,
  ]));
  set("hell", 1);

  setup();
  replace_program(ROOM);
}
