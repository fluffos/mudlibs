// dragon_wuding1.c

inherit ROOM;

void create()
{
	set("short", "屋顶");
	set("long", @LONG
你落在一个屋顶上. 四面望过去全是一浪浪的红瓦, 你不觉
惊叹道: 海鲸帮好大的架势!
    往四周屋顶的路还没修好, 过些天再来看看!
LONG );
	set("exits", ([
	"down" : __DIR__"dragon_changlang"
]) );
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
