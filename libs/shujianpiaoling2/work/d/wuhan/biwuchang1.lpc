// Room: /u/wuhan/biwuchang1.c

inherit ROOM;

void create() {
  set("short", "比武场");
  set("long", @LONG
武汉人喜欢打架，但是由于法律的限制，不敢放肆。不过在这里
打，打死人，被打死都是没关系的。所以来这里打架的人特别多。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "wnroad3",
  ]));
  //	set("no_dead", 1);
  setup();
}

int valid_leave(object me, string dir) {
  if (me->is_fighting()) {
    me->remove_all_killer();
    all_inventory(environment(me))->remove_killer(me);
  }
  return 1;
}
