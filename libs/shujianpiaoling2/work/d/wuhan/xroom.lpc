// Room: /u/wuhan/xroom.c

inherit ROOM;

void create() {
  set("short", "萧通家");
  set("long", @LONG
这里四周都是木架，放着些先古留下来的东西。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "xyard",
  ]));
  set("objects", ([
    __DIR__ "npc/xiaotong": 1,
  ]));

  setup();
}
