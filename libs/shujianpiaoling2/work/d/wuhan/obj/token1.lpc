// token1.c


inherit ITEM;

void create()
{
	set_name("铜牌",({ "token" }) );
	set("long",
		"一块锈迹斑斑的铜牌\n");
	set_weight(3000);
	if( clonep() )
		set_default_object(__FILE__);
	else 
		set("unit", "块");
	setup();
}
