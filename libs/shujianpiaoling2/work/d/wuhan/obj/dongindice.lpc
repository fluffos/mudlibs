// dongindice.c

inherit ITEM;

void create()
{
	set_name("纸片", ({ "zhipian"}));
	set_weight(0);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "页");
		set("long",
"这显然是从某本书里撕下来的, 但是上面的字你实在认不出!\n");
	}
}


