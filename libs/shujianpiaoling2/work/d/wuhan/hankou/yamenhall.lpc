// Room: /u/wuhan/hankou/yamenhall.c

inherit ROOM;

void create()
{
	set("short", "大堂");
	set("long", @LONG
你第一眼便看见大堂正中间高悬的金匾 明镜高悬 和匾下正襟威坐的知
府大人. 大堂两边放着些刑具, 两排衙役口中低喧道 威武 . 
    大人身后立着面东海日出的白玉屏风. 案桌上摆放着知府大印和一桶竹
签. 笔架上挂着数支狼毫, 其中一支红的显然是用来判决生死的.
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"yamenn",
  "east" : __DIR__"yamen",
]));
        set("objects", ([
	__DIR__"npc/zhifu" : 1,
	 __DIR__"npc/dyayi" : 2,
]) );
	setup();
	replace_program(ROOM);
}
