// Room: /u/wuhan/hankou/yamenll.c

inherit ROOM;

void create()
{
	set("short", "大牢走廊");
	set("long", @LONG
这是一条阴暗的走廊, 左边是一面墙是用一尺见方的花岗岩砌成的, 整
面墙没有一扇窗户. 右边是一间间的小牢房, 都用碗口粗的铁栅栏隔开. 走
廊里隔两丈才点盏小油灯, 显得格外阴森恐怖.
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"yamenk",
  "east" : __DIR__"yamenl",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
