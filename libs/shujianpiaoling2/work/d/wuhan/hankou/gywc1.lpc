// Room: /u/wuhan/hankou/gywc1.c

inherit ROOM;

void create()
{
	set("short", "小广场");
	set("long", @LONG
这是一个广场。正中间摆着一个一人高的铜鼎，里面高高低低地插
着数不清的焚香。虔诚的香客们毕恭毕敬地磕着头。
LONG
	);
	set("exits", ([ /* sizeof() == 4 */
  "south" : __DIR__"gywc2",
  "north" : __DIR__"gync1",
  "west" : __DIR__"gyenter",
  "east" : __DIR__"gyluohan",
]));
	set("objects", ([
	__DIR__"npc/gyheshang" : 1,
	]) );
        set("movedirection", "north");
	set("outdoors", "hankou");
	setup();
	replace_program(ROOM);
}
