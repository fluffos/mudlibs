// wjyazuo.c
#include <room.h>
inherit ROOM;

create()
{
    set("short", "雅座");
    set("long", @LONG
非常安静的所在
LONG );
    set("exits", ([
        "down"   : __DIR__"wjiulou"
]) );
    create_door("down", "珠帘", "up", DOOR_CLOSED);
    setup();
}

