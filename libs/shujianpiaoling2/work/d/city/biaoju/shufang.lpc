//biaoju shufang.c
inherit ROOM;

void create() {
  set("short", "书房");
  set("long", @LONG
但见房中一排排都是书架，架上都摆满了书，也不知有几千几万本书。
靠窗有一张极大的紫檀木的书桌．只见桌上摊着一本书，左首放着的砚台
笔筒也都雕刻精致。书桌右首是一只青铜古鼎，烧着檀香，鼎盖的兽头口
中袅袅吐出一楼楼青烟。
LONG);


  set("exits",
    ([  //sizeof() == 4
    "south": __DIR__ "wushi",
    ]));

  set("objects",
    ([  //sizeof() == 1
    ]));


  set("outdoors", "biaoju");
  set("no_clean_up", 0);
  setup();
}
