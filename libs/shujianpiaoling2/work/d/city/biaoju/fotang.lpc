//biaoju fotang.c
inherit ROOM;

void create ()
{
        set ("short", "佛堂");
        set ("long", @LONG
这里是一间小小的佛堂。仅供有一尊观音像。像前香火缭绕。由于局主
常年走镖在外，夫人一心向佛，长在佛像前祈祷出镖的丈夫一路平安。
LONG);

       
        set("exits", 
        ([ //sizeof() == 4
              "south" : __DIR__"furen",
        ]));

        set("objects", 
        ([ //sizeof() == 1
        ]));


        set("outdoors", "biaoju");
	set("no_clean_up", 0);
	        setup();
}


