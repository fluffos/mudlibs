// Room: /u/yanzi/huashan/road1.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"上山路"NOR);
	set("long", @LONG
这是一段曲曲折折的上山路，往西望去，有着一大片的山岭，其
样子酷似一只卧着的巨大的石狮，便是华山有名的狮子岭了。
LONG
	);
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "eastdown" : __DIR__"qingke",
  "westup" : __DIR__"shizi",
]));

	setup();

}
#include "/u/yanzi/huashan/westup.h";