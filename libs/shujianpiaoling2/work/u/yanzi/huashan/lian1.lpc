// Room: /u/yanzi/huashan/lian1.c

inherit ROOM;

void create()
{
	set("short", "[1;36m练功房[2;37;0m");
	set("long", @LONG
这里是供华山弟子练功习武之用，几个华山弟子正在练功，非常
是热闹，房中站着几个陪练弟子，还有个专门指导武术的老师。
LONG
	);
	set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/zhaotl" : 1,
  __DIR__"npc/peilian" : 4,
]));
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "southwest" : __DIR__"xiaoj2",
  "east" : __DIR__"lian2",
]));

	setup();
	replace_program(ROOM);
}
