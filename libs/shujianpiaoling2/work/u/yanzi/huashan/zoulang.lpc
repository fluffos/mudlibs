// Room: /u/yanzi/huashan/zoulang.c

inherit ROOM;

void create() {
  set("short", "[1;36m走廊[2;37;0m");
  set("long", @LONG
一条长长地走廊，这里比较冷清，因为一般华山弟子都不允许来
到这里，走廊西面直通华山大侠令狐冲的以前卧室，南面则是莫天雄
的书房，一个华山弟子在此看守着。
LONG
  );
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/thekid": 1,
  ]));
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 3 */
    "south": __DIR__ "nsf",
    "west": __DIR__ "woshi",
    "east": __DIR__ "xiaoj4",
  ]));

  setup();

}
int valid_leave(object me, string dir) {
  object ob;
  if (dir == "south" && present("xu da", environment(me)))
    return notify_fail("徐达一手拦住，厉声喝道：“未经许可，不可进入内书房！”\n");
  return ::valid_leave(me, dir);
}
