// Room: /u/yanzi/huashan/jiaozi.c

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIC "轿子" NOR);
  set("long", @LONG
一个用竹子做成的轿子，你正坐在上面。
LONG
  );
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
