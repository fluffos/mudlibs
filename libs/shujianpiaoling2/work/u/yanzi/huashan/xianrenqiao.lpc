// Room: /u/yanzi/huashan/xianrenqiao.c

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIC "仙人桥" NOR);
  set("long", @LONG
这里就是仙人桥了，说是桥，其实只是几条铁链子，上面铺上几
块木版，在山风的吹拂下，摇摇晃晃的，令人不寒而栗，一不小心就
会掉入桥下万丈深渊，因此许多游客即使过得了千尺幢，百尺峡也都
在此止步。
LONG
  );
  set("outdoors", "huashan");
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "baichi",
    "north": __DIR__ "laojun",
  ]));

  setup();
  replace_program(ROOM);
}
