// Room: /u/yanzi/huashan/jintian.c

inherit ROOM;

void create()
{
	set("short", "[1;36m金天宫[2;37;0m");
	set("long", @LONG
走进这里，你眼睛差点都花了，这里就是闻名的金天宫是因为他
的宫顶全部为纯金打制，及尽奢华，看上去确实就象有一个金色的天
空一样，这里平时也是华山弟子们切磋比武的地方。西面乃是传说太
上老君炼丹的炼丹宫。
LONG
	);
	set("outdoors", "huashan");
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/bailiyun" : 1,
]));
	set("no_clean_up", 0);
	set("no_dead", 1);
	set("exits", ([ /* sizeof() == 3 */
  "east" : __DIR__"ziqi",
  "northup" : __DIR__"nanfeng",
  "west" : __DIR__"lu",
]));

	setup();
	replace_program(ROOM);
}
