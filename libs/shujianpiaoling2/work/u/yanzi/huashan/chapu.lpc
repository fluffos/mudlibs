// Room: /u/yanzi/huashan/chapu.c

inherit ROOM;

void create()
{
	set("short", "[1;36m凉茶铺[2;37;0m");
	set("long", @LONG
这里是华山之上的一个小小的凉茶铺，专门供游人歇脚喝茶之用
，除此之外，也向游客提供各种登山用的用具，铺子前着有一面大旗
，上书：
			沈记凉茶。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"zhyue",
]));
	set("no_clean_up", 0);
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/laoshen" : 1,
]));

	setup();
	replace_program(ROOM);
}
