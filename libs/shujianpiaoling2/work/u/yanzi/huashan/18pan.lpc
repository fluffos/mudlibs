// Room: /u/yanzi/huashan/18pan.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"十八盘"NOR);
	set("long", @LONG
这里是一段七拐八拐的崎岖山路，因为沿路道路盘旋，因而被人
们称为「十八盘」。走在上面，你只觉得头都快晕了。
    往东上山就是青柯坪了。
LONG
	);
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "eastup" : __DIR__"qingke",
  "northdown" : __DIR__"shaluo",
]));

	setup();

}
#include "/u/yanzi/huashan/eastup.h";