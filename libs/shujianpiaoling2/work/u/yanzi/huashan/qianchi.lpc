// Room: /u/yanzi/huashan/qianchi.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"千尺幢"NOR);
	set("long", @LONG
这里是华山的咽喉要地「千尺幢」，这是一处在山崖极陡处开出
的一条小路，共三百七十多磴石级，石级的宽度只能容纳一个人上下
，两旁挂着铁索，人们手攀铁索，一步步地向上登，往上看，只见一
线天开；往下看，就像站在深井上。千尺幢的顶端，就像井口一样。
    崖壁(bi)上似乎刻着一些字。
LONG
	);
	set("item_desc", ([ /* sizeof() == 1 */
  "bi" : "[1;31m		“太华咽喉”、“气吞东瀛”
[2;37;0m",
]));
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "southdown" : __DIR__"huixin",
  "eastup" : __DIR__"baichi",
]));

	setup();

}
#include "/u/yanzi/huashan/eastup.h";