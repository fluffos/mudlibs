// Room: /u/yanzi/huashan/xianzhang.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"仙掌崖"NOR);
	set("long", @LONG
这里已是华山东峰的地方了，这里有着是五根擎天而起的大石柱
，其中中指最高。似一只巨大的手掌，故名仙掌崖。
LONG
	);
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"moon",
  "westup" : __DIR__"zhfeng",
]));

	setup();

}
#include "/u/yanzi/huashan/westup.h";