// Room: /u/yanzi/huashan/tianti.c

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIC "上天梯" NOR);
  set("long", @LONG
山路逐渐变得越来越陡，到这里更是达到了极点，就象一把巨大
的梯子，直同云霄，你在胆战心惊地走着这段山路的同时也不得不赞
叹大自然的鬼斧神工。
LONG
  );
  set("outdoors", "huashan");
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "southup": __DIR__ "chuangwang",
    "northdown": __DIR__ "xianren",
  ]));

  setup();
  replace_program(ROOM);
}
