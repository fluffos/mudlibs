#include <ansi.h>

inherit ROOM;
void init();
int do_step(string);
void create() {
  set("short", "怡然居");
  set("long", @LONG
这里是怡然的房间.
LONG
  );
  set("valid_startroom", 1);
  set("exits", ([
    "down": "/d/city/ciensi",

  ]));
  setup();

}
