// Room: /u/qingcheng/menf2.c

inherit ROOM;

void create()
{
	set("short", "南厢房");
	set("long", @LONG
这里是供来客休息的地方。屋内陈设虽然只有一张雕花木床、八
仙桌子、几把椅子，但是异常干净。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"ro1",
]));
	set("no_clean_up", 0);
	set("sleep_room", 1);

	setup();
	replace_program(ROOM);
}
