inherit ROOM;

void create() {
  set("short", "观日亭");
  set("long", @LONG
这里已是青城山主峰的峰顶，四望脚下，只见云迷雾封，如临太虚
幻境。崖边有一石亭，在此可以观看日出奇景，所以被称为观日亭。
LONG
  );
  set("exits", ([
    "southeast": __DIR__ "xiaolu27",
  ]));
  set("objects", ([
    __DIR__ "npc/yuqingzi": 1,
  ]));
  setup();
  replace_program(ROOM);
}
