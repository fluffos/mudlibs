// Room: /u/qingcheng/hole1.c

inherit ROOM;

void create()
{
	set("short", "黑洞");
	set("long", @LONG
这是一个黑漆漆的山洞，伸手不见五指。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "out" : __DIR__"qiaobi",
]));
	set("no_magic", 1);
        set("sleep_room",1);
	setup();
	replace_program(ROOM);
}
