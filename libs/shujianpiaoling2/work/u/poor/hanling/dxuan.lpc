// Room: /beggar/door.c
#include <ansi.h>;

inherit ROOM;

void create() {
  set("short", "牢房");
  set("long", @LONG
你走进了一间牢房，在牢房的一角锁着个僧人，
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "out": __DIR__ "droad5",
  ]));
  set("objects", ([
    __DIR__ "npc/xuannan": 1,
  ]));
  setup();
}
