// yanzhi

inherit ITEM;
#include <ansi.h>

void create()
{
    set_name(BLU"水形令牌"NOR, ({ "lingpai" }) );
    set_weight(500);
    set("long", @LONG 
这是一个水形状的令牌。
LONG
  );
    set("unit", "个");
    set("material", "teeth");
       set("value", 0);
}

