// Room: /city/nandajie2.c

inherit ROOM;

void create()
{
	set("short", "后院");
	set("long", @LONG
你走进了热窝的后院，在院子中有两棵大树，东边是热窝接
待客人的地方。北边和南边的屋门都反锁着。
LONG
	);
	set("exits", ([
		"east" : __DIR__"rewo",
	]));
       set("no_clean_up", 0);

	set("outdoors","zhang");

	setup();
	replace_program(ROOM);
}
