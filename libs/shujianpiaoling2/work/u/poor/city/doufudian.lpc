// Room: /city/nandajie2.c

inherit ROOM;

void create()
{
	set("short", "豆腐店");
	set("long", @LONG
这是一家豆腐店，在这里经常卖一些豆腐脑，豆浆等可口
小吃。店中的主人早年丧夫，人都都叫她何寡妇。
LONG
	);
	set("exits", ([
		"east" : __DIR__"lizi1",
	]));
	set("objects", ([
	__DIR__"npc/df_seller":1,
	]));
       set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
