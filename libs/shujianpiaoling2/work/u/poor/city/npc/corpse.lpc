// corpse.c

inherit ITEM;

void create()
{
       string *order = ({"店小二", "酒客", "男孩", "妇女", "小女孩", "流氓", "店老板", "官兵", "乞丐", "老头"});
     set_name((order[random(10)])+"的尸体", ({ "corpse" }) );
    set_weight(100000);
    set("long", @LONG 
这是一具刚死不久的尸体，在死人的胸前有一个利器插入过的洞。
LONG
  );
    set("value", 0);
    set("unit", "具");
    set("material", "wood");
    set("no_get","拿尸体？你不恶心吗？\n");
}

