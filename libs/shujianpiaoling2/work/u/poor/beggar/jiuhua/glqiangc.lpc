//guangchang
inherit ROOM;

void create()
{
        set("short", "前广场");
        set("long", @LONG
这里是「甘露寺」的前广场，几长见方，大块的青石铺成。四角种
着几棵树，还有几个香客在树下阴凉处休息。
    北面是北广场，南边有一个小院子，东边是大成殿。
LONG
        );
        set("exits", ([/* sizeof() == 4 */
           "east"  : __DIR__"gldcdian",
           "south" : __DIR__"glyuan",
           "west"  : __DIR__"gldoor",
           "north" : __DIR__"glbeigc",
]));

        set("objects", ([
           __DIR__"npc/wuseng": 3 ]) );

        set("no_clean_up", 0);
	set("outdoors","gaibang");

        setup();
        replace_program(ROOM);
}