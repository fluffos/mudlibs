// /u/poor/gaibang/hj
inherit ROOM;

void create()
{
        set("short", "世外桃源");
        set("long", @LONG
你穿过茂密的红叶林来到这个美妙的地方。三面都是悬崖
峭壁，只有这一面可以进来。在右侧有一个水潭，可以清楚看
到潭水底部的碎石。左侧有几株芙蓉树，树间有几座蜂巢。在
正中有两间依山而建的小屋。
LONG
        );
        set("exits", ([/* sizeof() == 1 */
           "south" : __DIR__"shulin1",
]));
       set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/yang" : 1,
  __DIR__"npc/bee" : 2,
]));

        set("no_clean_up", 0);
	set("outdoors","gaibang");

        setup();
	replace_program(ROOM);
}
