//fzshi 方丈室
inherit ROOM;

void create()
{
        set("short", "方丈室");
        set("long", @LONG
这里就是「甘露寺」方丈空相大师的起居室，空相大师平时在
此地修行，在正中是个蒲团，是空相大师修行的地方。屋中并无其
他物品。
LONG
        );
        set("exits", ([/* sizeof() == 1 */
           "south"  : __DIR__"glbeigc",
]));
	set("objects",([
	__DIR__"npc/kongxiang" : 1,
	]));

	set("item_desc",([
	"蒲团":"一个很旧的蒲团。\n",
	]));

        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}