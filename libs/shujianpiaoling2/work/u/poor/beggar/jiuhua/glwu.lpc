//fzshi 方丈室
inherit ROOM;

void create() {
  set("short", "小屋");
  set("long", @LONG
你走进了这个简陋的小屋，在屋子里只有一个蒲团。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "glyuan",
  ]));
  set("objects", ([
    __DIR__ "npc/wubian": 1,
  ]));

  set("item_desc", ([
    "蒲团": "一个很旧的蒲团。\n",
  ]));
  setup();
  replace_program(ROOM);
}
