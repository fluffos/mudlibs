// Room: /u/poor/gaibang/zhulin3.c

inherit ROOM;

void create() {
  set("short", "草舍");
  set("long", @LONG
你走进了一间不起眼的草舍之中，屋子中的陈设很简单，在
屋子的右边摆放着一张木床，床上做着一个老和尚。在屋子的左
边的墙上写着一行小字。
LONG
  );
  set("item_desc", ([ /* sizeof() == 1 */
    "bed": "石床的一角放着一柄翡翠匕首。  
",
    "小字": "  
                莫   朝   心   身
                使   朝   如   是
                惹   勤   明   菩
      神        尘   拂   镜   提
      光        埃   拭   台   树
      书
",
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "lroad1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/u/poor/beggar/npc/wuyi/shenguang": 1,
  ]));

  setup();
  replace_program(ROOM);
}
