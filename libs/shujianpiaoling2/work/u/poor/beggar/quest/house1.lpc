// Room: /u/poor/gaibang/zhulin3.c

inherit ROOM;

void create()
{
	set("short", "茅屋");
	set("long", @LONG
你走进了一间不起眼的茅屋，屋子的陈设很简单，在屋子的
有边站着一个胖胖的白发老者。他的身边放着各种各样的食物。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
    "south" : __DIR__"songlin4",
]));
       set("objects", ([ /* sizeof() == 1 */
  __DIR__"fooder" : 1,
]));

	setup();
	replace_program(ROOM);
}
