// Room: /u/poor/gaibang/zhulin2.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIB"石室"NOR);
	set("long", @LONG
你穿过地道来到了这间石室里，这里的光线很差。在屋子
的左边是一张石床，石床的旁边有两个石凳，在床的对面居然
有个通向地上的出口。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
    "north" : __DIR__"ddroad1",
    "up" : "/u/poor/beggar/xiaoduo/xliu",
]));
       set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/beggar" : 2,
]));
        set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
