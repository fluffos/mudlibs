// Room: /u/poor/gaibang/zhulin2.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIB"石梯"NOR);
	set("long", @LONG
这里是进入或离开丐帮地道的地方，正北边是几十级的石
梯。从那里可以进入到丐帮总舵之中。南边是黑暗的地道。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
    "up" : "/u/poor/beggar/zongduo/zdrukou",
    "south" : __DIR__"ddroad17",
]));
       set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/beggar" : 2,
]));
        set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
