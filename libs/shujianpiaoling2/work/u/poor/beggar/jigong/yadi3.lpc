// /u/poor/gaibang/hj
inherit ROOM;

void create()
{
        set("short", "山洞中");
        set("long", @LONG
你钻进了一个黑黑的山洞，这里阴森森的，不时能听到“嘀
嗒、嘀嗒”的流水声音，在山洞壁上有行小字。
LONG
        );
	set("item_desc", ([ /* sizeof() == 1 */
  "小字" : "施楠大命不死，但又落奸人之手，特留神功于此，珍重！。
",
]));
        set("exits", ([/* sizeof() == 1 */
           "out" : __DIR__"yadi2",
]));
	set("objects", ([ /* sizeof() == 1 */
  "/u/poor/obj/book/pytw-book" : 1,
]));
        set("no_clean_up", 0);

        setup();
	replace_program(ROOM);
}

