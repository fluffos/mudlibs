// stone.c

inherit ITEM;
#include <ansi.h>
void create()
{
    set_name( HIY "金砖" NOR, ({ "jinzhuan" }) );
    set_weight(50000);
    set("long", @LONG 
这是一块黄澄澄的金子。
LONG
  );
    set("value", 10000);
    set("unit", "块");
    set("material", "gold");
}

