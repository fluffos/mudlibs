// Room: /u/poor/gaibang/zhulin2.c

inherit ROOM;

void create()
{
	set("short", "大门");
	set("long", @LONG
你来到了丐帮的大仁分舵外，北边是扇木头制成的大门，
但是因为年久失修早已经破烂不堪。两边的院墙是用泥土堆积
而成，只有一人高。你从这里能够听见院子中的声音。
LONG
	);

	set("exits", ([ /* sizeof() == 2 */
    "west" : __DIR__"renyzi1",
    "east" : "/u/poor/city/byidao2",
]));
       set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/shengt" : 1,
]));
        set("no_clean_up", 0);
	set("outdoors","gaibang");

	setup();
	replace_program(ROOM);
}

