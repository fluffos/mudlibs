// Room: /u/poor/gaibang/zhulin2.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIB"书房"NOR);
	set("long", @LONG
这里是丐帮的书房，四周并没有多少书籍。只有在地上散
落着几本破破烂烂的书。北边是个大院子。
LONG
	);

	set("exits", ([ /* sizeof() == 2 */
    "northeast" : __DIR__"renyzi2",
]));
       set("objects", ([ /* sizeof() == 1 */
  "/u/poor/obj/book/whip-book" : 1,
  "/u/poor/obj/book/force-book" : 1,
]));
        set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
