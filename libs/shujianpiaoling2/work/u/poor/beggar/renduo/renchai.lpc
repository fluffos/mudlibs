// Room: /u/poor/gaibang/zhulin2.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIB"柴房"NOR);
	set("long", @LONG
这是一间柴房。里面堆满了大捆的柴禾，有一个老乞丐躺
在屋子的角落里。
LONG
	);

	set("exits", ([ /* sizeof() == 2 */
    "south" : __DIR__"renyzi3",
]));
       set("objects", ([ /* sizeof() == 1 */
  "/u/poor/beggar/npc/wuyi/changfan" : 1,
]));
        set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
