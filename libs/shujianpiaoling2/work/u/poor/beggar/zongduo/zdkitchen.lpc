// Room: /u/poor/gaibang/gbwuch2

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIB"食堂"NOR );
	set("long", @LONG
这里香气扑鼻，不想就知道这里是丐帮的食堂，由于丐帮比较穷
困，所以只有地位较高的弟子可以来这里吃饭。也可以在这里休息。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"zdzoul1",
]));
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"obj/rou" : 3,
  __DIR__"npc/sunge" : 1,
]));
       set("sleep_room",1);	
	set("outdoors","gaibang");
        set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
