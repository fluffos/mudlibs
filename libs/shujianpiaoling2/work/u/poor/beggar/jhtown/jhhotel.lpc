// Room: /poor/dian.c

inherit ROOM;

void create()
{
	set("short", "小酒馆");
	set("long", @LONG
这是鸡华村的小酒馆。正中放着几张桌子和椅子，靠门口
的地方有个台子是，后面放着几坛子酒。常常有客人就睡在店
里的椅子上。现在店里的生意很冷清。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"jhw1",
]));
	set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/jhwaiter" : 1,
  __DIR__"npc/oldman" : 1,
]));
	set("no_clean_up", 0);
	set("valid_startroom", 1);
       set("sleep_room",1);	

	setup();
}
