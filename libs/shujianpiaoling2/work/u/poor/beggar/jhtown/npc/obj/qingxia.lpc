// fish

inherit ITEM;
inherit F_FOOD;

void create()
{
	set_name("青虾", ({ "qingxia" }) );
	set_weight(80);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("long", "一只刚死不久的青虾。\n");
		set("unit", "只");
		set("food_remaining", 2);
		set("food_supply", 5);
	}
}
