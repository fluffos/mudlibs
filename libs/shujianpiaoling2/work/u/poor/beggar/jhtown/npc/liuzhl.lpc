// corpse.c

inherit ITEM;

void create() {
  set_name("刘长老的尸体", ({ "corpse" }));
  set_weight(100000);
  set("long", @LONG 
这是一具刚死不久的尸体，在死人的脖子上带这一条长命锁，
在死人的胸前有一个利器插入过的洞。
LONG
  );
  set("value", 0);
  set("unit", "具");
  set("material", "wood");
  set("no_get", "拿尸体？你不恶心吗？\n");
}
