// /u/poor/gaibang/hj
inherit ROOM;

void create()
{
        set("short", "鸡华村东口");
        set("long", @LONG
你来到了鸡华村的东口，街道上来来往往的行人很少，街道两
旁是错落有秩的民局，北边有个大宅子是村长家，向东就村子了。
LONG
        );
        set("exits", ([/* sizeof() == 3 */
           "north" : __DIR__"jhboss",
           "west" : __DIR__"jhzx",
           "east" : __DIR__"jhe2",
           "south" : __DIR__"jhcanku",
]));
        set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/beggar" : 2,
]));
        set("no_clean_up", 0);
	set("outdoors","gaibang");

        setup();
        replace_program(ROOM);
}