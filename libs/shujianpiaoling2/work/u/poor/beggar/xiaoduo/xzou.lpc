// Room: /beggar/door.c
#include <ansi.h>;

inherit ROOM;

void create()
{
	set("short", HIB"大树上"NOR);
	set("long", @LONG
你爬到了大树之上，在右边的两个树杈间坐着一个人。从这
里能看见四周茂密的树林。
      这里没有明显的出口。
LONG
	);
       set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/bird" : 1,
  __DIR__"npc/zou" : 1,
]));
	set("outdoors","gaibang");
        set("no_clean_up", 0);

	setup();
}

void init()
{
	add_action("do_climb", "climb" );
}

int do_climb(string arg)
{
	if( arg=="down" || arg=="树") {
	    if(this_player()->query_skill("dodge", 1)<=20) {
		     write("你向下看了一眼，不由得天旋地转。\n");
                  this_player()->unconcious();
			 return 1;
		}
		message_vision("$N慢慢向树下爬去，不一会儿，爬下了大树。\n", this_player());
		this_player()->move(__DIR__"xshulin4");
		message("vision", this_player()->name() + "爬下了大树。\n",
			environment(this_player()), this_player());
		return 1;
	}
	else return 0;
}
