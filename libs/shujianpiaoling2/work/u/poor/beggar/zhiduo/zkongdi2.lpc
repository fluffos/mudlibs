// Room: /gaibang/gbwuch1.c

inherit ROOM;
#include <ansi.h>;

create()
{
     	set("short", HIB"林中空地" NOR);
	set("long", @LONG
这里是丐帮智舵弟子练功的地方，有不少丐帮的弟子在此修习
武功。南边是智舵的大门，北边又进入黑松林了。
LONG
	);
	set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/beggar" : 2,
]));
       set("sleep_room",1);	
	set("exits", ([ /* sizeof() == 3 */
  "northeast" : __DIR__"zkongdi3",
  "south" : __DIR__"zkongdi1",
  "northwest" : __DIR__"zsonglin4",
]));
	set("outdoors","gaibang");
        set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}

