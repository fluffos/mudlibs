// Room: /u/poor/gaibang/gbwuch2

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIB "武器库" NOR);
  set("long", @LONG
这里是丐帮弟子领取武器的地方，其实说是武器，不过是一些
木棍、木棒。你可以随意拿几件武器来使用。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "zkongdi1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/u/poor/beggar/npc/wuyi/zhangdk": 1,
    "/u/poor/obj/weapon/bamboo_stick": 2,
    "/u/poor/obj/weapon/wood_hammer": 3,

  ]));
  set("sleep_room", 1);
  set("outdoors", "gaibang");
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
