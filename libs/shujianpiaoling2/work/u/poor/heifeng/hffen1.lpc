// Room: /u/poor/yidao.c

inherit ROOM;

void create()
{
        set("short", "坟坑");
        set("long", @LONG
你跳下了坟坑之中，在坑的一角是头陀的尸骨。旁边的
土里有一个指环和本册子。
LONG
        );
        set("exits", ([
                "up" : __DIR__"hffen",
]));
       set("objects", ([ /* sizeof() == 1 */
  "/u/poor/obj/book/sanhua-book" : 1,
  "/u/poor/obj/weapon/lg-finger" : 1,
]));
	set("outdoors","zhang");
        setup();
}
