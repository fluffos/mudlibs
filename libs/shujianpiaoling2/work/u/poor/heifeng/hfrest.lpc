// Room: /u/poor/yidao.c

inherit ROOM;

void create()
{
        set("short", "休息室");
        set("long", @LONG
这里是黑风寨弟子休息的地方。有几个弟子在这里赌钱。
LONG
        );

        set("exits", ([
                "west" : __DIR__"hfhy",
        ]));
       set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/louluo" : 1,
  __DIR__"npc/louluo1" : 1,
  __DIR__"npc/jialong" : 1,
]));
        setup();
	replace_program(ROOM);
}
