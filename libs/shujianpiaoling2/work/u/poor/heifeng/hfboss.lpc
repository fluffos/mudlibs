// Room: /u/poor/yidao.c

inherit ROOM;

void create()
{
        set("short", "卧室");
        set("long", @LONG
你走进了寨主的卧室，在右边是个木床，床边有张桌子。
寨主正坐在桌子边的椅子上。
LONG
        );
        set("exits", ([
                "out" : __DIR__"hfhy",
]));
       set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/boss" : 1,
]));
        setup();
        replace_program(ROOM);
}
