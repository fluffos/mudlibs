// Room: /u/poor/yidao.c

inherit ROOM;

void create()
{
        set("short", "池塘底");
        set("long", @LONG
你跳进了池塘，游到了池塘的底下。发现池底好象有什么。
LONG
        );
        set("exits", ([
                "up" : __DIR__"hfhy",
]));
       set("objects", ([ /* sizeof() == 1 */
  __DIR__"obj/youbao" : 1,
]));
	set("outdoors","zhang");
        setup();
}