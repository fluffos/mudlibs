#include <room.h>
inherit ROOM;

void create() {
  set("short", "隙底");
  set("long", @LONG
隙底宽极了，纵横足有半里。地面全为赤色石底。四周十丈之处似有许多
厐然大物慢慢的向你靠来。地上都是人骨，足有十几堆，旁边散落这各式各样
的兵器。在不远处不时传来水流声。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "hebian",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "全真教");

  setup();
}
