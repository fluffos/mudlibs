
#include <room.h>
inherit ROOM;

void create()
{
        set("short", "山路");
        set("long", @LONG
山路弯弯曲曲盘旋起伏。路面坎坷凸凹不平。路边早已是乱草丛生，杂石
竖横。
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
  "northeast" : __DIR__"shanlu6",
  "southup" : __DIR__"baoquan",
 ]));
        set("no_clean_up", 0);
        set("outdoors", "quanzhen");

        setup();
  //      replace_program(ROOM);
}
