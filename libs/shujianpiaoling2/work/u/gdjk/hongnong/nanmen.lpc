// Room: /open/gdjk/nanmen.c

inherit ROOM;

void create() {
  set("short", "弘农南门");
  set("long", @LONG
这里是军事重镇——弘农——的南城门，虽然鲜有居民来往，但是部队的马蹄
声却不绝于耳。突然，你觉得好象有人在注视着你，不由的打了个激灵，赶忙抬起
头来，只见几个军兵正虎视耽耽的盯着你。你只觉得腿有些发软。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "nanjie1",
    "south": __DIR__ "road4",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/bing": 4,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
