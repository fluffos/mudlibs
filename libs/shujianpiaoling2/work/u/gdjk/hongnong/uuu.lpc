#include <ansi.h>
inherit ROOM;

void create()
{
        set("short",HIY "毒谷庄园"NOR);
        set("long", HIG@LONG
这是钱庄老板毒谷新近买下的房产，据说准备建成美丽
的庄园。地上已经堆放了不少的木料、大理石等建筑材料。
LONG NOR
        );
        set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"jie4",
]));
        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}

