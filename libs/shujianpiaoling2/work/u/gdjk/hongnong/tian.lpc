// Room: /open/gdjk/tian.c

inherit ROOM;

void create() {
  set("short", "田庄");
  set("long", @LONG
这是一间十分普通的田庄，房子里只有一些破烂的家具。房子里住着
一户农民夫妇，还有一个小孩子。
LONG
  );
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/nong": 1,
    __DIR__ "npc/fu": 1,
  ]));
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "jie4",
  ]));

  setup();
  replace_program(ROOM);
}
