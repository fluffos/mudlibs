// Room: /open/gdjk/jie4.c

inherit ROOM;

void create() {
  set("short", "弘农广场");
  set("long", @LONG
这里是弘农的中心，各种重要的活动都在这里举行。如今这里却成了乞丐
汇聚的地方。在广场的中央有个石碑，上面的字已经模糊不清。石碑下边正做
着几个乞丐，旁边有几个衣衫褴褛的顽童在玩耍。
LONG
  );
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/qigai": 2,
    __DIR__ "npc/xingren": 1,
  ]));
  set("item_desc", ([ /* sizeof() == 1 */
    "石碑": "上面的字已经模糊不清，你隐隐约约的看到几个字：弘农重镇...
",
  ]));
  set("no_clean_up", 0);
  set("valid_startroom", 1);
  set("exits", ([ /* sizeof() == 5 */
    "north": __DIR__ "duyuan",
    "southeast": __DIR__ "nanjie",
    "west": __DIR__ "jie-3",
    "northeast": __DIR__ "dgate",
    "east": __DIR__ "tian",
  ]));

  setup();

}
