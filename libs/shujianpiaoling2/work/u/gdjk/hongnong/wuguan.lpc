// Room: /open/gdjk/wuguan.c

inherit ROOM;

void create()
{
        set("short", "武馆");
        set("long", @LONG
这是弘农唯一的一家武馆。只见武馆的四周墙面上画着各种图形，
有的使枪，有的弄棒，栩栩如生。武馆靠北的地方有一个兵器架，上面
有各种武器。但奇怪的是，武馆里一个人也没有。你从门缝看到的是什
么？难道是鬼吗？
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"jie2",
]));
        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}

