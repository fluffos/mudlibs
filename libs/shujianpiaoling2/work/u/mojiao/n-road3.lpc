// n-road3.c  written by john darks
inherit ROOM;
void create() {
  set("short", "大路");
  set("long", @LONG
这是一条大路，路是由黑黑的泥土铺的，上面撒了些细纱。两旁是无边的
稻田，偶尔看见远处有湖水，不过像这样的大路却行人寥寥。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "southeast": __DIR__ "n-road2",
    "northwest": "/u/maoshan/sroad",
  ]));
  set("objects",
    ([
      "/npc/nknpc": 1,
    ]));
  set("outdoors", "tianzhu");
  setup();
  replace_program(ROOM);
}
