//n-road1.c written by john darks
inherit ROOM;
void create()
{
	set("short", "大路");
	set("long", @LONG
这是一条大路，路是由黑黑的泥土铺的，上面撒了些细纱。路一直往北面
延伸，远处依稀出现了一排排杨柳。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  	"north" : __DIR__"n-road2",
  	"east" : __DIR__"town1",
  	"southup" : "/u/kugu/sroad1",
]));
	set("outdoors","tianzhu");
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
