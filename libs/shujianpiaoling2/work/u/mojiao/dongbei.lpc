#include <room.h>
inherit ROOM;
void create()
{
  set("short","东碑廊");
  set("long",@LONG
此为天魔宫大事所记的碑廊。
LONG
  );
  set("exits",([
         "west":__DIR__"zhengyuan.lpc",
               ]));
        set("objects", ([
                __DIR__"npc/tian" : 1,
       ]) );
  setup();
  replace_program(ROOM);
}
