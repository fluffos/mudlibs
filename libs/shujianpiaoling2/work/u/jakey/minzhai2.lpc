inherit ROOM;

void create() {
  set("short", "民宅");
  set("long", @LONG
一间很普通的民宅，青色的砖，灰黑色的瓦。摆设很简单，
没有什么很特别的东西。两张桌子，几个凳子。别的房间的
门都是关着的，看不见里面的摆设了。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "xx2",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/laohe": 1,
  ]));
  setup();
  replace_program(ROOM);
}
