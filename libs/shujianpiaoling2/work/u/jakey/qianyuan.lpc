inherit ROOM;

void create()
{
	set("short", "山庄前院");
	set("long", @LONG
这是个挺大但打扫的很干净的院子，左右种着两株槐树。槐
树已长的比较粗壮了，树冠几乎把整个院子遮住了。东西分别是
落月山庄的财院和律院，往北通往落月山庄的大厅--卧龙厅。
LONG
	);
	set("exits", ([ /* sizeof() == 4 */
	"north" : __DIR__"zhuting",
	"south" : __DIR__"menting",
	"east" : __DIR__"caiyuan",
	"west" : __DIR__"lvyuan",
	]));
	set("no_clean_up", 0);
        set("outdoors", "jakey");
	setup();
}

int valid_leave(object me,string dir)
{
	if (dir=="west")
	me->start_busy(3);
	return 1;
}
