inherit ROOM;

void create() {
  set("short", "水潭");
  set("long", @LONG
这里是水潭的潭底，潭水冰冷。潭底是细小的沙，有几处泉
眼在咕咕嘟嘟的往外涌着水。这里还有一些小虾，小鱼。你时常
可以看到一种银色的小鱼在一些小石块间徘徊。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "up": __DIR__ "yadi2",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/fish": 2,
  ]));
  set("no_clean_up", 0);
  setup();
}
