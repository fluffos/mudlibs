inherit ROOM;

void create()
{
	set("short", "黄土小路");
	set("long", @LONG
这是一条黄土铺就的小路，路上几乎没有什么行人，向北通
往再来镇，向南是一条青石小路。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	"north" : __DIR__"htpath2",
	"south" : __DIR__"qspath2",
	]));
        set("objects", 
        ([
              "/npc/nknpc" : 1,
        ]));
	set("no_clean_up", 0);
        set("outdoors", "jakey");
	setup();
	replace_program(ROOM);
}
