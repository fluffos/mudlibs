inherit ROOM;

void create()
{
	set("short", "厢房");
	set("long", @LONG
这里是落月山庄的厢房，这里住的都是些江湖豪杰。房内摆
放着两张单床和一些家具。除此之外没有什么别的长物了。这里
朴素的很，但是给人一种很亲切的感觉。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	"south" : __DIR__"zl3",
	]));
	set("no_clean_up", 0);
	set("objects", ([ 
	__DIR__"npc/zhuangke" : 2,
	]));
        set("sleep_room",1);
	setup();
	replace_program(ROOM);
}
