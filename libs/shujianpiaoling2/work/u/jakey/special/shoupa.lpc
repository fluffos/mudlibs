#include <armor.h>

inherit ITEM;

void create() {
  set_name("手帕", ({ "shou pa" }));
  set_weight(700);
  set("long",
    "一块丝绢手帕，在手帕的左下角绣着一个兰色的“玉”字。\n");
  set("unit", "块");
  set("value", 100);
}
