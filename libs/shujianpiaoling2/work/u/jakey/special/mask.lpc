// face off.c

#include <armor.h>

inherit HEAD;

void create() {
  set_name("布衣", ({ "cloth" }));
  set_weight(3000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "件");
    //		set("long",	"一张用人皮制作的男孩面具。\n");
    set("material", "cloth");
    set("armor_prop/armor", 3);
    //		set("armor_prop/id", ({"boy"}) );
    //		set("armor_prop/name", ({"男孩"}) );
    //		set("armor_prop/short", ({"男孩(Boy)"}) );
    //		set("armor_prop/long", ({
    //			"一个小男孩。\n"
    //		}) );
  }
  setup();
}
