inherit ROOM;

void create() {
  set("short", "门房");
  set("long", @LONG
这里是门房，不大的地方里摆着两张桌子和几个长条凳子。
书桌上摆着一本书，被风吹得摊开着。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "damen1",
  ]));
  set("objects", ([
    __DIR__ "npc/hantian": 1,
  ]));
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
