inherit ROOM;

void create()
{
	set("short", "小酒馆");
	set("long", @LONG
一间小酒馆，不知道开了多长时间了，门口的酒幌已经脏的
瞧不出底色了。可能是好酒的人不多，只有一个人在喝酒，一个
小酒保靠着酒柜闲坐着打发时间。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
	"west" : __DIR__"lpath3",
	]));
	set("objects", ([ /* sizeof() == 2 */
        __DIR__"npc/zuihan" : 1,
	__DIR__"npc/jiubao" : 1,
	]));
	set("no_clean_up", 0);       	
	set("valid_startroom",1);
	setup();
	replace_program(ROOM);
	call_other( "/obj/board/luoyue_b", "???" );
}
