inherit ROOM;

void create()
{
	set("short", "黄土街道");
	set("long", @LONG
这是再来镇的主要街道，路西是一家铁铺，门口挂着铁斧，
铁锨，铁犁之类的农家用具。路东是一间药店，你要是有点头疼
脑热，可以在这里买点药吃。
LONG
	);
	set("exits", ([ /* sizeof() == 4 */
	"east" : __DIR__"yaodian",
	"west" : __DIR__"tiepu",
	"south" : __DIR__"cross",
	"north" : __DIR__"htpath4",
	]));
	set("no_clean_up", 0);
        set("outdoors", "jakey");
	setup();
	replace_program(ROOM);
}
