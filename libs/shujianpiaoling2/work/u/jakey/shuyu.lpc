inherit ROOM;

void create()
{
	set("short", "书房");
	set("long", @LONG
这里是书房，房间里的四面墙壁都摆着书架，上面摆放着许
多书，房间里很明亮，阳光透过窗户照在室内。一个叼着烟袋的
老先生正躺在椅子上面闭目养神。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
	"south" : __DIR__"huiyou",
	]));
	set("objects",([
	__DIR__"npc/teacher" : 1,
	]));
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
