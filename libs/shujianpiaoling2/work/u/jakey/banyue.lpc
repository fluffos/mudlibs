inherit ROOM;

void create()
{
	set("short", "伴月亭");
	set("long", @LONG
这里是落月山庄的著名景点了。在这里观月那可是最清楚的
地方了。这里面一到每月十五成家上上下下都在此看月。现在这
里一个人也没有。整个亭子空空荡荡的。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	"southwest" : __DIR__"hspath",
	]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
