inherit ROOM;

void create()
{
	set("short", "马宅");
	set("long", @LONG
这里是镇上酒馆马老板的家，家里布置的倒也朴素。不过，
在墙上挂着一些不知名的字画，给人的感觉是一个“文化之家”。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
	"south" : __DIR__"xx2",
	]));
	set("objects", ([ /* sizeof() == 1 */
	__DIR__"npc/maboss" : 1,
	]));
	set("no_clean_up", 0);       
	setup();
	replace_program(ROOM);
}
