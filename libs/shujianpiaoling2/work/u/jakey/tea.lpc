inherit ROOM;

void create() {
  set("short", "茶馆");
  set("long", @LONG
这里是再来镇的茶馆，乌黑的房瓦，木料的柱子，门口挂着
个幡子，上面写着个大大的“茶”字。里面有几张桌子和几条长
凳，因为是农忙时间，这里面没有多少人。一个小二在门口坐着。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "lpath3",
  ]));
  set("objects", ([
    __DIR__ "npc/boshi": 1,
  ]));
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
