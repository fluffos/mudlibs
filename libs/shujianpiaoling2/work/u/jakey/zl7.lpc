inherit ROOM;

void create()
{
	set("short", "走廊");
	set("long", @LONG
这里是一条安静的走廊，两旁来来往往的家仆却没人出一声
，显得很有这规矩。北间屋子是个香堂，再往内就是李员外的卧
室了。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	"north" : __DIR__"xiang",
	"west" : __DIR__"woshi1",
	"east" : __DIR__"qianting",
	]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
