inherit ROOM;

void create()
{
	set("short", "内洞");
	set("long", @LONG
这里面不算特别阴暗，倒是潮的很。原来在石壁上有一条由
地下暗泉冲出来的小石沟，看来可以在此饮水，而在这里长着几
株矮小的果树，上面的果实又青又小，可能是因为没有得到阳光
的充分照射吧。
LONG
	);
	set("exits", ([ /* sizeof() == x */
	"west" : __DIR__"hole2",
	]));
	set("no_clean_up", 0);       
	setup();
	replace_program(ROOM);
}
