inherit ROOM;

void create() {
  set("short", "桂楼二层");
  set("long", @LONG
这里面的摆设简单的很，无论是床、椅、桌、柜都是简简单
单，没有一点奢华的样子，这里原先是间空闲的房间，一直没有
人居住，后来因为成小姐生了怪病一直不好，成夫人为了看护女
儿搬到这里住了下来。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "down": __DIR__ "gui2",
  ]));
  set("no_clean_up", 0);
  setup();
}
