inherit ROOM;

void create() {
  set("short", "桂楼外");
  set("long", @LONG
这里是落月山庄前任庄主最爱的桂楼。这是一栋五角小红楼
。楼的五角吊着金丝盘龙扣，每条龙口中扣着一个夜明珠。桂楼
的门开着半扇，现在这里是成夫人住的地方。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "garden",
    "enter": __DIR__ "gui2",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "jakey");
  setup();
  replace_program(ROOM);
}
