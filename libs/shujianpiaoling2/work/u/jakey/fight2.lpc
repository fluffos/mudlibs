inherit ROOM;

void create() {
  set("short", "练功房");
  set("long", @LONG
这是一间普通的练功房，里面尘土飞扬，有几个年轻弟子正
在吆喝着切磋武艺。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "fight1",
  ]));
  set("objects", ([
    __DIR__ "npc/fighter": 3,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
