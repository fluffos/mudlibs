inherit ROOM;

void create()
{
	set("short", "小径");
	set("long", @LONG
四周都是过胸的杂草，阻碍着你的道路，又好像要将整个世界
都包起来似的。在你的脚下是一条小径，若隐若现的延伸着。
LONG
	);
	set("exits", ([ /* sizeof() == x */
	"north" : "/u/jakey/home/home1",
	"south"	: __DIR__"xiaojing",
	]));
	set("no_clean_up", 0);       
	set("outdoors", "jakey");
	setup();
	replace_program(ROOM);
}
