// Room: /u/wudang/zmroom.c

inherit ROOM;

void create()
{
        set("short", "掌门人卧室");
        set("long", @LONG
这是掌门人的卧室，布置简简单单，除了一床、一桌、一椅、一蒲团，
再也没有别的什物。比较引人注目的是墙上挂着一把剑。
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"hyuan",
]));
	 set("objects", ([
		__DIR__"npc/master1": 1,
]));
        setup();
        replace_program(ROOM);
}


