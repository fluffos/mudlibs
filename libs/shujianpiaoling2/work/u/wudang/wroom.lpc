// Room: /u/wudang/wroom.c

inherit ROOM;

void create()
{
	set("short", "兵器库");
	set("long", @LONG
这是武当的兵器库，存放着各类武器，几个道童正在整理兵器。
西面是后院。
LONG
	);
	set("objects", ([ /* sizeof() == 2 */
  "/obj/weapon/longsword" : 2,
  "/obj/weapon/bamboo_sword" : 4,
]));
	set("outdoors", "wudang");
	set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"hyuan",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
