// Room: /u/wudang/hroad2.c

inherit ROOM;

void create()
{
	set("short", "小院子");
	set("long", @LONG
这是一个小院子，柔软的草皮，四周种着一些鲜花，一个满面慈祥的
师太静静地坐在那里闭目养神。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"hroad1",
]));
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/master2" : 1,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
