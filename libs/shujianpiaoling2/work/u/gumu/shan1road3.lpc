inherit ROOM;

void create() {
  set("short", "山腰");
  set("long", @LONG
你走在山腰的山路中，耳朵里传来阵阵清脆的鸟鸣声，煞是
动听。前面的路直上山顶。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "southdown": __DIR__ "shan1road2",
    "northwest": __DIR__ "shan1road4",
    "east": __DIR__ "shan4road1",
  ]));

  set("outdoors", "city");

  setup();
  replace_program(ROOM);
}
