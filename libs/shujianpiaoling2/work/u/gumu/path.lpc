// Room: /open/hurt/oldtomb/path.c

inherit ROOM;

void create() {
  set("short", "小径");
  set("long", @LONG
走在山洞里的小径上，你步子渐渐加快。路好象很长，也不知道
尽头是出口还是死路。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "path1",
    "west": __DIR__ "corridor",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
