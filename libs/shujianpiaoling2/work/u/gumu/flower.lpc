// Room: /open/hurt/oldtomb/flower.c

inherit ROOM;

void create()
{
	set("short", "花廊");
	set("long", @LONG
周围种了许多紫花，能在这么一个地方生存的花，一定不同寻常，
花的颜色和形状令人浮想翩翩。在石洞里竟有这么一个地方，令你感到
莫名惊诧。这时你耳朵里传来嗡嗡声。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "northeast" : __DIR__"kitchen",
  "west" : __DIR__"beehouse",
]));
	set("objects", ([ /* sizeof() == 1 */
//  __DIR__"obj/guancai" : 1,
//  __DIR__"npc/shanshan" : 1,
]));
	set("no_clean_up", 0);
set("sleep_room", 1);

	setup();
	replace_program(ROOM);
}
