#include <room.h>

inherit ROOM;

void create()
{
   set("short", "蜂房");
	set("long", @LONG
四周挂着一个个麻袋，噢！原来是蜂巢！嗡嗡声越来越响，
你不禁问自己：难道这就是令人闻风丧胆的杀人蜂？
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
    "east" :__DIR__"flower" ,
      ]));

      set("objects", ([
//         __DIR__"npc/ruyu" : 1,
     __DIR__"npc/bee" : 4,
     __DIR__"obj/guancai2" : 1,

   ]) );
 	set("no_clean_up", 0);


       setup();
       replace_program(ROOM);
    }