#include <room.h>

inherit ROOM;

void create()
{
   set("short", "小径的尽头");
	set("long", @LONG
原来是死路，这里就是尽头，四周的石壁又冷又硬，地上
的石子坑坑洼洼的，令你非常不舒服。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
    "south" :__DIR__"path2",
      ]));
       setup();
       replace_program(ROOM);
    }