// Room: /d/waterfog/honggu.c

inherit ROOM;

void create()
{
	set("short", "虹谷断崖");
	set("long", @LONG
这里就是著名的虹谷断崖，山壁在这里好象被劈开了一样，西面是万丈深
渊。东边是一条弯曲的小径，断崖边上建了一间不起眼的茅屋。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "eastdown" : __DIR__"wpath2",
]));
	set("no_clean_up", 0);

	setup();
}
/*
void init()
{
         add_action("do_enter", ({"enter"}) );
}


int do_enter(string arg)
{
	 object ob;
         string my_id = this_player()->query("id");		
	 if(my_id=="lmn" || my_id=="yanzi" ||
	    my_id=="swallow" ||my_id=="yaner") {
	        this_player()->move(__DIR__"lmn-yanzi");
		message_vision("$N走进了茅屋。\n",this_player());
		return 1;
	 }
         if(arg=="茅屋")
                 return notify_fail( "门是锁着的，你进不去。\n ");
         return 0;
}
*/
