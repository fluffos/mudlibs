// Room: /d/waterfog/wpath2.c

inherit ROOM;

void create()
{
	set("short", "山坡小径");
	set("long", @LONG
你现在正走在一条山坡小径上，东边不远处可以望见达天阁琉璃色
的屋瓦，小径东北是一道白石阶梯通往山巅的虹台，往西则是「崆峒十
景」之中最有名的虹谷断崖，小径旁立著一个七、八尺高的石碑(stone
)。
LONG
	);
	set("item_desc", ([ /* sizeof() == 1 */
  "stone" : "石碑上面几个苍劲古朴的字写道：虹谷石台，乙丑年达天阁第六代
阁主方玉琪立。
你仔细一看，这几个字迹丝毫没有斧凿痕迹，竟像是题字的人直接
用手指在岩石上写出来一般。
",
]));
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/xiaofan" : 1,
]));
	set("outdoors", "waterfog");
	set("exits", ([ /* sizeof() == 3 */
  "northeast" : __DIR__"wpath3",
  "westup" : __DIR__"honggu",
  "east" : __DIR__"wpath1",
]));
	set("no_clean_up", 0);

	setup();

}
