//ROOM:/u/changan/bingbu/ymei.c

inherit ROOM;

void create() {
  set("short", "文香书院");
  set("long", @LONG
这里是洛阳第一才子薛文潜开设的书院，只要交付一定的金额你
就可以在这里学习文学。四周的书桌旁坐满摇头晃脑的学生么，
朗诵的声音不禁有些嘈杂
LONG
  );
  set("outdoors", "changan");
  set("exits", ([
    "east": __DIR__ "lyn2",

  ]));
  set("objects", ([
    __DIR__ "npc/teacher": 1,
  ]));
  setup();

}

int valid_leave(object me, string dir) {
  this_player()->delete("薛文潜/learn");
  return 1;
}
