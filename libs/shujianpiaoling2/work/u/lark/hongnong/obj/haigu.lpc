// token1.c


inherit ITEM;

void create() {
  set_name("骸骨", ({ "bone" }));
  set("long",
    "不知道死了多久了血肉的已经风干了.\n"
    "只剩下一些零落的白骨\n");
  set_weight(20);
  if (clonep())
    set_default_object(__FILE__);
  else
    set("unit", "堆");
  setup();
}
