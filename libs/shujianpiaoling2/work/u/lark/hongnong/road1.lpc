//changan city

inherit ROOM;

void create ()
{
        set ("short", "官道");
        set ("long", @LONG
这里是一条尘土飞扬的黄土路,一直通向东南方向,
路上几乎没有什么行人,只有偶尔几匹快马飞驰而过.
LONG);

        //set("item_desc", ([" *** " : " *** ", ]));

        set("exits", 
        ([ //sizeof() == 4
                "northeast" : __DIR__"road2",
                "southwest" : __DIR__"road",
        ]));

//        set("objects", 
//        ([ //sizeof() == 1
//                "/u/lark/fnpc/xiaofan" : 1,
                
//        ]));


        set("outdoors", "changan");
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

