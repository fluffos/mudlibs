// xy2.c

inherit ROOM;

void create()
{
        set("short", "小场");
        set("long", @LONG
这里是山坡上一片不大的空地，是逍遥派弟子日常练武的地方，
一些弟子正在考较着武艺。西面的斜顶草房里传出丁当的铁器
撞击声。东面是是一个平静的院落。
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"player",
  "west" : __DIR__"bingqi",
  "northup": __DIR__"xy3",
  "eastdown":__DIR__"ground",
]));
        set("objects", ([ /* sizeof() == 1 */
       __DIR__"npc/dizi3" : 4,
]));
        set("no_clean_up", 0);
        set("valid_startroom", 1);

        setup();
        replace_program(ROOM);
}

