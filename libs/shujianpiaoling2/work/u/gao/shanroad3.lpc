// shanlu.c

inherit ROOM;

void create()
{
  set ("short", "太室山路");
  set ("long", @LONG

好不容易走出了田埂，迈上了一条山间小路。越走树林越密，树缝
中洒下来的阳光也变得斑斑点点。四周青山翠绿，鸟啭莺啼。太室山和
少室山遥遥相对，在这里隐约可以望见对面山头的重重寺庙。
LONG);
set("exits", ([ /* sizeof() == 4 */
"southdown" : __DIR__"shanroad2",
"northup" : __DIR__"gate",
]));

        set("outdoors", 1);
        setup();
}

