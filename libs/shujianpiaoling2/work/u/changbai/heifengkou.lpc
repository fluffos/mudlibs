#include <ansi.h>
inherit ROOM;
void create()
{
        set("short", WHT "黑风口" NOR);
        set("long", @LONG
这里长年多风且多是大风。当山顶刮到五级风时，此处狂风呼啸，
飞砂走石，游人们根本站不起来，只能匍匐着爬到风口。但在风口又
可以饱览长白瀑布的全貌，所以游人们又难以割舍，真是“无限风光
在险峰”了。
LONG);
        set("exits", ([ 
     "southdown" : __DIR__"cbshanlu4",
	 "eastup" : __DIR__"zixiafeng",
   ]));
	set ("oldarea",06);
	set("outdoors","changbai");
	setup();
 }