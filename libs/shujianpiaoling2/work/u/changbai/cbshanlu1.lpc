#include <ansi.h>
inherit ROOM;
void create()
{
        set("short", WHT "长白山上" NOR);
        set("long", @LONG
这里已经是雪线以上了，遍地的白雪覆盖着整个你的视野，
一篇白莽莽中，有几排凌乱的脚印，看来刚有人从这里经过。
LONG);
        set("exits", ([ 
//  "north" : __DIR__"shanxia",
   ]));
	set ("oldarea",14);
	set("outdoors","changbai");
	setup();
 }