#include <ansi.h>
inherit ROOM;
void create() {
  set("short", WHT "紫霞峰" NOR);
  set("long", @LONG
紫霞峰由紫色沙石堆积，远观之烟云缭绕，宛若西天落霞，
紫气生辉，古香古色，因而得名。
LONG);
  set("exits", ([
    "west": __DIR__ "zixiafeng",
  ]));
  set("oldarea", 09);
  set("outdoors", "changbai");
  setup();
}
