#include <ansi.h>
inherit ROOM;
void create()
{
        set("short", WHT "白头峰" NOR);
        set("long", @LONG
主峰白头山多白色浮石与积雪而得名，素有“千年积雪为年松，
直上人间第一峰”的美誉。
LONG);
        set("exits", ([ 
        "west" : __DIR__"baitoufeng",
	  
   ]));
	set ("oldarea",05);
	set("outdoors","changbai");
	setup();
 }