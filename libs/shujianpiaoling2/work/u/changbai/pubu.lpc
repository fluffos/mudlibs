#include <ansi.h>
inherit ROOM;
void create()
{
        set("short", WHT "长白瀑布" NOR);
        set("long", @LONG
天池的水从一个小缺口上溢出来，流出约１０００多米，从悬崖上往
下泻，就成著名的长白山大瀑布。大瀑布高达６０余米，很壮观，距布
２００米远可以听到它的轰鸣声。
LONG);
        set("exits", ([ 
    "south" : __DIR__"shangu6",
   ]));
	set ("oldarea",03);
	set("outdoors","changbai");
	setup();
 }