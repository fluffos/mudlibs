inherit ROOM;

void create()
{
        set("short", "聚宝斋");
        set("long", @LONG

需进一步的描述
LONG
        );
        set("exits", ([/* sizeof() == 1 */
            "south" : __DIR__"qsmlu14",		/*青石马路*/
]));

        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}
