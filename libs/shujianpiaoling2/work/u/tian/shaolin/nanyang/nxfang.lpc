inherit ROOM;

void create()
{
        set("short", "南厢房");
        set("long", @LONG

需进一步的描述
LONG
        );
        set("exits", ([/* sizeof() == 1 */
            "north" : __DIR__"bsbju",		/*百胜镖局*/
]));

        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}
