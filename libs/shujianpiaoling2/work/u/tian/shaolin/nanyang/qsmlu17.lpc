inherit ROOM;

void create()
{
        set("short", "青石马路");
        set("long", @LONG

需进一步的描述
LONG
        );
        set("exits", ([/* sizeof() == 4 */
            "east"  : __DIR__"cross2",		/*十字路口*/
            "south" : __DIR__"xingchang",	/*刑场*/
            "west"  : __DIR__"xiaolu14",	/*小路*/
            "north" : __DIR__"yamen",		/*衙门*/
]));

        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}
