// letter1.c 推荐信

//#include <armor.h>

inherit ITEM;

void create()
{
	set_name("推荐信",({ "letter" }) );
	set_weight(20);
	if( clonep() )
		set_default_object(__FILE__);
	else 
		set("unit", "封");
		set("long",
		"一只牛皮纸信封，装着师傅写的推荐信，上面有你的名字。\n");
	setup();
}

