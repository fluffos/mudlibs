// maofang 茅房

#include <room.h>
inherit ROOM;

void create()
{
	set("short", "茅房");
	set("long", @LONG
这里是少林弟子出恭的地方。出家人讲究清洁，连这里也打扫的
干干净净。墙角放着几个马桶。
LONG
	);

	set("exits", ([
            "east": __DIR__"zoulang1",
	]));
	set("objects", ([
	__DIR__"obj/matong": 1 ,
]) );
        set("no_sleep", 1);
	
	setup();
	replace_program(ROOM);
}
