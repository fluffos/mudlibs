inherit ROOM;

void create()
{
        set("short", "小路");
        set("long", @LONG
一条山间的羊肠小道，崎岖不平，只能容一人行走，真难想象会
通到哪里。
LONG
       );
        set("exits", ([/* sizeof() == 2 */
            "east" : __DIR__"shanlu2",
            "northwest" : __DIR__"nongjia",

]));

        set("no_clean_up", 0);
	set("outdoors","shaolin");

        setup();
        replace_program(ROOM);
}