inherit ROOM;

void create() {
  set("short", "小路");
  set("long", @LONG
你幽闲地走在小路上。周围树林越来越密,光线也越来越暗,不知道
前面是什么地方,还是回头吧.
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "xiaoqiao",
    "northeast": __DIR__ "xiaolu4",
  ]));

  set("no_clean_up", 0);
  set("outdoors", "shaolin");

  setup();
  replace_program(ROOM);
}
