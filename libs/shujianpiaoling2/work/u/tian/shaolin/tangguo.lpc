inherit ROOM;
void create()
{
        set("short", "糖果店");
        set("long", @LONG
这是一家糖果店，店铺里琳琅满目地放着各式各样好吃的糖果
，是孩子们经常光顾的地方。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "east"  : __DIR__"town1", 
]));
	 set("objects", ([
		__DIR__"npc/tang_boss": 1,
]));
        set("outdoors", "shaolin");
        setup();
        replace_program(ROOM);
}
