inherit ROOM;

void create() {
  set("short", "林间小路");
  set("long", @LONG
你走在一条小径上，两旁长满了小树，修篁森森，绿荫满地，除了
树叶哗哗声和鸟鸣声，听不到别的动静。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "shulin1",
    "northwest": __DIR__ "xiaolu1",
  ]));

  set("no_clean_up", 0);
  set("outdoors", "shaolin");

  setup();
  replace_program(ROOM);
}
