// tiekey.c 

inherit ITEM;

void create()
{
	set_name("铁钥匙", ({ "tie key","key" }) );
	set_weight(100);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "把");
		set("long", "这是一把铁钥匙，黑黑的，看起来很普通。\n");
		set("value", 0);
	}
}

