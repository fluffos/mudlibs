// shimabu.c 

inherit ITEM;

void create()
{
	set_name("湿抹布", ({ "shi mabu","mabu" }) );
	set_weight(100);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "块");
		set("long", "这是一块湿淋淋的抹布。\n");
		set("value", 0);
	}
}

