// mabu.c 

inherit ITEM;

void create()
{
	set_name("抹布", ({ "ma bu" }) );
	set_weight(100);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "块");
		set("long", "这是一块抹布，洗的干干净净的。\n");
		set("value", 0);
	}
}

