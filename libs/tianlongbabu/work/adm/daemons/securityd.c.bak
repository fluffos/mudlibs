/*
 * TLBB MUDLIB
 * $Header: /home/mud/cvsroot/tlbbmud/tlbb/tlbb_mudlib/adm/daemons/securityd.c,v 1.8 2000/10/30 05:30:05 mud Exp $
 */
 
// securityd.c
// last update by cbra Apr-18-1998

#include <login.h>

/// this line by cbra Jun-23-1998
#define PROMOTE_CMD "/cmds/arch/promote"
/// end

#define DEBUG

inherit F_DBASE;

// By Annihilator@Eastern.Stories (11/25/94)

// This is the "effective" access status compare to the "status" property
// in login object that this mapping determine wizards' access rights
// directly. It is important to make the "status" property matches this
// mapping for wizards higher than regular wizard such as sage, arch and
// admin. For thoese users whose name doesnot appear in this mapping, they
// are divided into three groups (wizard),(player) and (object) using
// wizardp() and interactive() efun.
// The parenthesis is nessessary to prevend players naming themself "admin"
// to hack the security system.
// 
// 01/14/95 Annihilator - wiz_status are defined outside this daemon in the
//                        WIZLIST file.

// Note: This mapping need be initialized before any file access to let
//       valid_read() and valid_write() works correctly on boot time.
private mapping wiz_status = ([]);

private string *wiz_levels = ({
	"(player)",
	"(immortal)",
	"(apprentice)",
	"(wizard)",
	"(arch)",
	"(admin)"
});

// A valid write attempt must pass 2 checks: your uid or status must not be
// "excluded" and must be "trusted" on that directory in order to write in 
// that directory. The directories containing the file is searched backward
// from the current directory to root. And exclude is checked prior than
// trusted.

private mapping trusted_write = ([
        "/":            ({ "Root", "(admin)", "(arch)" }),
        "daemon":       ({ "(arch)" }),
        "open":         ({ "(wizard)", "(apprentice)" }),
        "doc/help":         ({ "(wizard)" , "(apprentice)" }),
        "t":            ({ "(admin)", "(arch)" }),
        "t/tianshan":   ({ "kelvinx" }),
        "t/xiaoyao":   ({ "kelvinx" }),
        "t/shaolin":	({ "puff" }),
	"t/qinzhai":	({ "auteur", "buyi"}),
	"t/yunzhou":	({ "auteur", "buyi" }),
	"t/ly":		({ "buyi" }),
	"t/death":	({ "buyi" }),
	"t/wiz":	({ "buyi" }),
	"t/dengfeng":	({ "patrick" }),
	"t/juxianzhuang":	({ "patrick" }),
]);

private mapping special_write = ([
        "/":            ({ "windf", "hdk" }),
        "www":		({ "may" }),
        "u":		({ "may" }),
	"obj/board":	({ "buyi" }),
        "daemon/skill":	({ "puff", "kelvinx", "buyi", "patrick" }),
        "daemon/class/haoke":	({ "auteur" }),
	"daemon/class/beggar":   ({ "buyi" }),
	"daemon/class":   ({ "patrick" }),
	"daemon/condition": ({ "patrick" }),
]);

private mapping exclude_write = ([
	"adm":		({ "(admin)" }),
	"data":		({ "(admin)" }),
	"log":		({ "(admin)" }),
	"u":		({ "(arch)" }),
	"include":	({ "(admin)" }),
	"secure":	({ "(admin)" }),
	"www":		({ "(admin)" }),
	"binaries":	({ "(admin)" }),
	"adm/daemons/network":	({ "(admin)" }),
	"adm/obj":	({ "(admin)" }),
	"adm/etc":	({ "(admin)" }),
	"adm/simul_efun":	({ "(admin)" }),
	"adm/daemons/securityd.c":	({ "(admin)" }),
]);

private mapping trusted_read = ([
	"/": 		({	"Root","Mudlib",
					"(admin)", "(arch)", "(wizard)", "(apprentice)" }),
//	"t":		({ "Domain" }),
	"daemon":	({ "Domain", "Mudlib" }),
	"obj":		({ "Domain" }),
	"ftp":		({ "anonymous" }),
]);


private mapping exclude_read = ([
	"adm":		({ "(wizard)", "(apprentice)" }),
	"data":		({ "(wizard)", "(apprentice)" }),
	"cmds":		({ "(wizard)", "(apprentice)" }),
	"feature":	({ "(wizard)", "(apprentice)" }),
	"t/back":	({ "(wizard)", "(apprentice)" }),
//	"t/dali":	({ "(wizard)", "(apprentice)" }),
	"t/dali_king":	({ "(wizard)", "(apprentice)" }),
	"t/dali_wangfu":	({ "(wizard)", "(apprentice)" }),
	"t/kaifeng":	({ "(wizard)", "(apprentice)" }),
	"t/erengu":	({ "(wizard)", "(apprentice)" }),
	"t/jianhu":	({ "(wizard)", "(apprentice)" }),
	"t/murong":	({ "(wizard)", "(apprentice)" }),
	"t/shaolin":	({ "(wizard)", "(apprentice)" }),
	"t/nianhuasi":	({ "(wizard)", "(apprentice)" }),
	"t/tianshan":	({ "(wizard)", "(apprentice)" }),
	"t/wuxi":	({ "(wizard)", "(apprentice)" }),
	"t/xiaoyao":	({ "(wizard)", "(apprentice)" }),
	"t/xingxiu":	({ "(wizard)", "(apprentice)" }),
	"t/yubigong":	({ "(wizard)", "(apprentice)" }),
	"t/yuxuguan":	({ "(wizard)", "(apprentice)" }),
	"t/yipin":	({ "(wizard)", "(apprentice)" }),
	"t/wanjiegu":	({ "(wizard)", "(apprentice)" }),
	"t/xueshan":	({ "(wizard)", "(apprentice)" }),
	"t/suzhou":	({ "(wizard)", "(apprentice)" }),
	"t/wuxi":	({ "(wizard)", "(apprentice)" }),
	"t/xiaojing":	({ "(wizard)", "(apprentice)" }),
	"t/yanziwu":	({ "(wizard)", "(apprentice)" }),
	"t/ly":		({ "(wizard)", "(apprentice)" }),
	"secure":	({ "(wizard)", "(apprentice)" }),
	"u":		({ /*"(wizard)",*/ /*"(apprentice)"*/ "(immortal)" }),
	"log":		({ "(admin)", "(arch)", "(wizard)", "(apprentice)" }),
	"www":		({ "(admin)", "(arch)", "(wizard)", "(apprentice)" }),
	"binaries":	({ "(admin)", "(arch)", "(wizard)", "(apprentice)" }),
	"include/net":	({ "(admin)", "(arch)", "(wizard)", "(apprentice)" }),
	"adm/daemons/network":	({ "(admin)", "(arch)", "(wizard)", "(apprentice)" }),
	"adm/obj":	({ "(admin)", "(arch)", "(wizard)", "(apprentice)" }),
	"adm/etc":	({ "(admin)", "(arch)", "(wizard)", "(apprentice)" }),
	"adm/simul_efun":	({ "(admin)", "(arch)", "(wizard)", "(apprentice)" }),
	"adm/daemons/securityd.c":	({ "(admin)", "(arch)", "(wizard)", "(apprentice)" }),
]);

private mapping special_read = ([
        "/":		({ "windf", "hdk" }),
        "www":		({ "may" }),
        "u":		({ "may" }),
        "log":		({ "may" }),
        "t/shaolin":	({ "puff" }),
	"t/dengfeng":	({ "puff","patrick" }),
	"t/juxianzhuang":	({ "patrick" }),
        "t/tianshan":	({ "kelvinx" }),
        "t/xiaoyao":	({ "kelvinx" }),
        "t/dali":	({ "kelvinx" }),
	"t/ly":		({ "buyi" }),
	"u/oven":	({ "buyi"}),
	"feature":	({ "buyi" }),
	"cmds":		({ "buyi" })
]);

void create()
{
	string *wizlist, wiz_name, wiz_level;
	int i;

	seteuid(getuid());
	set("channel_id", "╰参");
	wizlist = explode(read_file(WIZLIST), "\n");
	wiz_status = allocate_mapping(sizeof(wizlist));
	for(i=0; i<sizeof(wizlist); i++) {
		if( wizlist[i][0]=='#' 
		|| sscanf(wizlist[i], "%s %s", wiz_name, wiz_level)!=2 ) continue;
		wiz_status[wiz_name] = wiz_level;
	}
}

string *query_wizlist() { return keys(wiz_status); }

// This function returns the status of an uid.
string get_status(mixed ob)
{
	string euid;
	
	if( objectp(ob) ) {
//	This cause some tools lost access write.
//		if( clonep(ob) && !userp(ob) ) return "(player)";
		euid = geteuid(ob);
		if( !euid ) euid = getuid(ob);
	}
	else if( stringp(ob) ) euid = ob;

	if( member_array(euid, wiz_levels)!=-1 ) return euid;

	if( !undefinedp(wiz_status[euid]) ) return wiz_status[euid];
	else return "(player)";
}

int get_wiz_level(object ob)
{
	return member_array(get_status(ob), wiz_levels);
}

int set_status(mixed ob, string status)
{
	string uid, *wiz, wizlist, old_status;
	int i;

	if( !this_player(1) || member_array(status, wiz_levels) < 0 ) return 0;
	if( geteuid(previous_object())!= ROOT_UID ) return 0;

	if( objectp(ob) )			uid = getuid(ob);
	else if(stringp(ob))		uid = ob;
	else return 0;

//// by cbra Jun-28-1998
	if ( file_name(previous_object()) != PROMOTE_CMD ) {
		log_file( "static/security.log", capitalize(geteuid(this_player(1)))
		+ " Try to promote " + capitalize(uid) + " to "
		+ status + " on " + ctime(time()) + " by "
		+ file_name(previous_object()) + "\n" );
		return 0;
	}
/// end by cbra

	old_status = wiz_status[uid];
	if( !old_status ) old_status = "(player)";
	if( status == old_status ) return 0;
	
	if( status == "(player)" )	map_delete(wiz_status, uid);
	else						wiz_status[uid] = status;

	wiz = keys(wiz_status);
	for(wizlist = "", i=0; i<sizeof(wiz); i++)
		wizlist += wiz[i] + " " + wiz_status[wiz[i]] + "\n";
	rm(WIZLIST);
	write_file(WIZLIST, wizlist);

	log_file( "static/promotion", capitalize(geteuid(this_player(1)))
		+ " promotes " + capitalize(uid) + " from " + old_status + " to "
		+ status + " on " + ctime(time()) + "\n" );


	return 1;
}

string *get_wizlist() { return keys(wiz_status); }

// valid_write - called by the master object to determine whether if an user
//               is allowed to write on a certain file.
// NOTE:
//     The argument user should be an object according to the source code
// of driver. However, it is a mixed type in the released docs. We assume
// it is an object in normal case here and issue an error if it was not
// an object. 												- Annihilator

int valid_write(string file, mixed user, string func)
{
	string euid, status, *path, dir;
	int i;

#ifdef DEBUG
        if((!sscanf(file,"/adm/daemons/network/%*s")) && 
		(sscanf(file,"/cmds/%*s") || sscanf(file,"/adm/%*s") || sscanf(file,"/feature/%*s")) )
	CHANNEL_D->do_channel(this_object(), "sys",
		sprintf("valid_write: %s, %O, %s, %O", file, user, func, previous_object(-1)));
#endif

/*
	if( func=="write_file" && sscanf(file, LOG_DIR + "%*s") ) return 1;
        if(sscanf(file,"/adm/%*s") k ) {
                log_file( "driver/driver.log.bak", sprintf("valid_write:%s, %O, %s, %s from %s at %s",
                        file,user,func,geteuid(user), query_ip_number(user),ctime(time()) ));
        }
*/

	if( !objectp(user) ) return 0;

	// Get the euid and status of the user.
	if( !(euid = geteuid(user)) ) return 0;
	euid=geteuid(user);
// by cbra
//	CHANNEL_D->do_channel(this_object(), "sys",
//		sprintf("valid_write: %s, %O, %s, %s", file, user, func,euid));
// by cbra
	status = get_status(user);

	if( euid==ROOT_UID ) return 1;
	if((geteuid(previous_object())== ROOT_UID) && (func == "mkdir")) return 1;


	// The following are some special cases.
	path = explode(file, "/");

	if( func=="write_file" && sscanf(file, LOG_DIR + "%*s") ) return 1;

	if( func=="write_file" && sscanf(file, MAPDATA + "%*s") ) return 1;

/* by cbra	if( func=="save_object" && sscanf(file, DATA_DIR + "%*s") ) {
	// by cbra	if( euid == MUDLIB_UID ) return 1;
		return 1;
	}
*/
        // Let user save their save file
        if(func == "save_object")
        {
                if(sscanf(base_name(user), "/obj/%*s")
                && sscanf(file, "/data/%*s")
		&& file == (string)user->query_save_file() + __SAVE_EXTENSION__)
                        return 1;
        }

        if(func == "write_file")
        {
                if(sscanf(base_name(user), "/obj/%*s")
                && sscanf(file, "/data/%*s")
		&& file == (string)user->query_save_text())
                        return 1;
        }
// end

// by cbra Mar-15-1998	if( sscanf(file, "/u/" + euid[0..0] + "/" + euid + "/%*s") )
	if( sscanf(file, "/u/" + euid + "/%*s") )
		return 1;

/// add by cbra Feb-24-1998

	// And then check if we are trusted in one of the SPECIAL directories containing
	// the file.
	if( member_array(euid, special_write["/"])!=-1 ) return 1;
	if( member_array(status, special_write["/"])!=-1 ) return 1;
	for(i=sizeof(path)-1; i>=0; i--) {
		dir = implode(path[0..i], "/");
		if( undefinedp(special_write[dir]) ) continue;
		if( member_array(euid, special_write[dir])!=-1 ) return 1;
		if( member_array(status, special_write[dir])!=-1 ) return 1;
	}
// end

	// Check if we are excluded in one of the directories containing the file.
	for(i=sizeof(path)-1; i>=0; i--) {
		dir = implode(path[0..i], "/");
		if( undefinedp(exclude_write[dir]) ) continue;
		if( (member_array(euid, exclude_write[dir])!=-1)
		||	(member_array(status, exclude_write[dir])!=-1) )
			return userp(user) ? 0: valid_write(file, this_player(1), func);
	}

	// And then check if we are trusted in one of the directories containing
	// the file.
	if( member_array(euid, trusted_write["/"])!=-1 ) return 1;
	if( member_array(status, trusted_write["/"])!=-1 ) return 1;
	for(i=sizeof(path)-1; i>=0; i--) {
		dir = implode(path[0..i], "/");
		if( undefinedp(trusted_write[dir]) ) continue;
		if( member_array(euid, trusted_write[dir])!=-1 ) return 1;
		if( member_array(status, trusted_write[dir])!=-1 ) return 1;
	}

	return 0;
}

int valid_read(string file, mixed user, string func)
{
	string euid, status, *path, dir;
	int i;
	int cross=0; // add by cbra Mar-16-1996

#ifdef DEBUG
        if((!sscanf(file,"/adm/daemons/network/%*s")) && 
		(sscanf(file,"/cmds/%*s") || sscanf(file,"/adm/%*s") || sscanf(file,"/feature/%*s")) )
	CHANNEL_D->do_channel(this_object(), "sys",
		sprintf("valid_read: %s, %O, %s, %O", file, user, func, previous_object(-1)));
#endif
/*
        if(objectp(user) && userp(user)) {
                log_file( "driver/driver.log.bak", sprintf("valid_read:%s, %O, %s, %s from %s at %s",
                        file,user,func,geteuid(user), query_ip_number(user),ctime(time()) ));
        }
        else {  
                log_file( "driver/driver.log.bak", sprintf("valid_read:%s, %O, %s, at %s",
                        file,user,func,ctime(time()) ));
        }
*/

	if( !objectp(user) ) return 0;

	// Let user save their save file
	if( func=="restore_object" ) {
		if( sscanf(base_name(user), "/obj/%*s")
		&&	sscanf(file, "/data/%*s")
		&&	file == (string)user->query_save_file() + __SAVE_EXTENSION__ )
			return 1;
	}
	//by cbra
	if (func=="file_size") return 1;
//	if( sscanf(base_name(user), "/cmds/%*s"))
//			return 1;
	//end

	// Get the euid and status of the user.
	if( !(euid = geteuid(user)) ) return 0;
	status = get_status(user);

	if( euid==ROOT_UID ) return 1;

// by cbra Mar-15-1998	if( sscanf(file, "/u/" + euid[0..0] + "/" + euid + "%*s") )
	if( sscanf(file, "/u/%*s") ) {
		if ( sscanf(file, "/u/" + euid + "%*s") ) {
			return 1;
		}
		else {
			if ((file=="/u/") || (file == "/u")) return 1;
			cross = 1;
		}
	}

	if( func=="read_file" && sscanf(file, MAPDATA + "%*s") ) return 1;

	path = explode(file, "/");


/// add by cbra Feb-24-1998

	// And then check if we are trusted in one of the directories containing
	// the file.
	if( member_array(euid, special_read["/"])!=-1 ) return 1;
	if( member_array(status, special_read["/"])!=-1 ) return 1;
	for(i=sizeof(path)-1; i>=0; i--) {
		dir = implode(path[0..i], "/");
		if( undefinedp(special_read[dir]) ) continue;
		if( member_array(euid, special_read[dir])!=-1 ) return 1;
		if( member_array(status, special_read[dir])!=-1 ) return 1;
	}
	if (cross) return 0;
/// end

	// Check if we are excluded in one of the directories containing the file.
	for(i=sizeof(path)-1; i>=0; i--) {
		dir = implode(path[0..i], "/");
		if( undefinedp(exclude_read[dir]) ) continue;
		if( (member_array(euid, exclude_read[dir])!=-1)
		||	(member_array(status, exclude_read[dir])!=-1) )
			return userp(user) ? 0: valid_read(file, this_player(1), func);
	}
	// And then check if we are trusted in one of the directories containing
	// the file.
	if( member_array(euid, trusted_read["/"])!=-1 ) return 1;
	if( member_array(status, trusted_read["/"])!=-1 ) return 1;
	for(i=sizeof(path)-1; i>=0; i--) {
		dir = implode(path[0..i], "/");
		if( undefinedp(trusted_read[dir]) ) continue;
		if( member_array(euid, trusted_read[dir])!=-1 ) return 1;
		if( member_array(status, trusted_read[dir])!=-1 ) return 1;
	}
//	return 1;
	return 0;
}

int valid_seteuid( object ob, string uid )
{
	if( uid==0 ) return 1;
	if( uid==getuid(ob) ) return 1;

	if( getuid(ob)==ROOT_UID ) return 1;
// by cbra Jun-26-1998	if( getuid(ob)==MUDLIB_UID || getuid(ob)==DOMAIN_UID )
// by cbra Jun-26-1998		return (uid[0]>='a' && uid[0]<='z');
// by cbra Jun-26-1998	if( wiz_level(ob) >= wiz_level(uid) )  return 1;

	return 0;
}

