/*
 * TLBB MUDLIB
 * $Header: /home/mud/cvsroot/tlbbmud/tlbb/tlbb_mudlib/t/homes/obj/apple.c,v 1.1 2000/03/05 20:24:39 mud Exp $
 */
 
// food.c

#include <ansi.h>

inherit ITEM;
inherit F_FOOD;

void create()
{
        set_name("大苹果", ({ "apple" }) );
        set_weight(80);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("long", "一个红彤彤的大苹果。\n");
                set("unit", "个");
                set("value", 0);
                set("food_remaining", 20);
                set("food_supply", 10);
        }
}

void init()
{
        add_action("do_eat", "eat");
}

int do_eat(string arg)
{
        object me = this_player();
	int shiwu;

        if( !arg ) return notify_fail("你要吃什麽？\n");
        if ( !(arg == "apple") ) return notify_fail("你要吃什麽？\n");
	if( me->query("food") >= me->max_food_capacity() )
		return notify_fail("你吃得太饱了，想撑死自己啊！\n");

	shiwu = me->max_food_capacity();
        me->set("food", shiwu);
	switch ( random(2) ) {
	case 0:
            message_vision("$N拿起一个大苹果拼命地吃呀吃，觉得从来没吃过这么好的东西。\n", me);
		break;
	case 1:
	    message_vision("$N拿起一个大苹果咬了一口，啊呀！牙都酸掉了！！\n",me);
		break;
	}
        destruct(this_object());
        return 1;
}

