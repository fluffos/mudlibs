/*
 * TLBB MUDLIB
 * $Header: /home/mud/cvsroot/tlbbmud/tlbb/tlbb_mudlib/t/kaifeng/drugstore.c,v 1.1 2000/03/05 20:28:38 mud Exp $
 */
 
// Room: /d/kaifeng/yaopu.c

inherit ROOM;

void create()
{
	set("short", "益民堂");
	set("long", @LONG
这里是一间规模不小的药铺，奇怪的是，在这里你闻不到一丝药铺
应有的药味，有的只是一股淡淡的檀香，令人觉得心旷神怡，药铺的柜
台就在店堂靠北的一面，柜台後是一个巨大的药柜，上面的小抽屉只怕
有七、八百个之多。
LONG
	);
	set("exits", ([
		"east" : __DIR__"swroad",
	]));

	set("objects", ([ /* sizeof() == 2 */
		__DIR__"npc/herbalist" : 1,
	]));
	set("no_clean_up", 0);

	setup();
}
