/*
 * TLBB MUDLIB
 * $Header: /home/mud/cvsroot/tlbbmud/tlbb/tlbb_mudlib/t/kaifeng/npc/inn-waiter.c,v 1.1 2000/03/05 20:32:46 mud Exp $
 */
 
// waiter.c

inherit NPC;
inherit F_VENDOR;

void create()
{
	set_name("伙计", ({ "waiter" }));
	set("gender", "男性" );
	set("age", 22);
	set("long",
		"这位伙计正笑咪咪地忙着接待客人。\n");
	set("combat_exp", 5);
	set("attitude", "friendly");
	set("rank_info/respect", "小二哥");
	set("vendor_goods", ([
		"老酒": "/obj/example/wineskin",
		"包子": "/obj/example/dumpling",
		"鸡腿": "/obj/example/chicken_leg",
		"月饼":	"/obj/example/mooncake",
		"天龙八部排行榜": "/obj/ntopten.lpc",
	]) );
	set("combat_exp", 200);
	set_skill("unarmed", 20);
	
	setup();

        carry_object("/obj/cloth")->wear();
        add_money("silver", 2);
}

void init()
{	
	object ob;

	::init();
	if( interactive(ob = this_player()) && !is_fighting() ) {
		remove_call_out("greeting");
		call_out("greeting", 1, ob);
	}
	add_action("do_vendor_list", "list");
}

void greeting(object ob)
{
	if( !ob || environment(ob) != environment() ) return;
	switch( random(3) ) {
		case 0:
			say( "伙计客气地说道：这位" + RANK_D->query_respect(ob)
				+ "，要住店吗？\n");
			break;
		case 1:
			say( "伙计笑咪咪地说道：这位" 
				+ RANK_D->query_respect(ob)
				+ "，请进请进。\n");
			break;
/*
		case 2:
			say( "伙计说道：这位" + RANK_D->query_respect(ob)
				+ "，本店新开封的老酒，要不要尝尝。\n");
			break;
*/
		case 2:
			say( "伙计说道：这位" + RANK_D->query_respect(ob)
				+ "，本店新到的中秋月饼，要不要尝尝。\n");
			break;
	}
}
