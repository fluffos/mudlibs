/*
 * TLBB MUDLIB
 * $Header: /home/mud/cvsroot/tlbbmud/tlbb/tlbb_mudlib/t/juxianzhuang/secret-9.c,v 1.2 2000/05/31 05:30:33 mud Exp $
 */
 
// Room: /u/patrick/juxianzhuang/secret-9.c
#include <ansi.h> 
inherit ROOM;

void create()
{
set("short", "小路");
set("long", @LONG
这里是小路的尽头。你抬头一看，前面赫然是一座构筑宏伟的大
庙，庙前匾额写著“敕建悯忠寺”五个大字。四周是无法形容地冷，
就连庙顶都结了一层霜。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"secret-8.lpc",
  "enter" : __DIR__"temple.lpc",
]));
set("item_desc", ([
                "庙顶": @TEXT

结了一层霜的庙顶！
    
TEXT
        ]) );

	set("outdoors", 1);
	set("no_clean_up", 0);

	setup();
}

