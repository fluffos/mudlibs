/*
 * TLBB MUDLIB
 * $Header: /home/mud/cvsroot/tlbbmud/tlbb/tlbb_mudlib/t/juxianzhuang/temple.c,v 1.1 2000/03/05 20:28:17 mud Exp $
 */
 
// Room: /u/patrick/juxianzhuang/temple.c

inherit ROOM;

void create()
{
	set("short", "悯忠寺");
	set("long", @LONG
这里是庙中的一处，由此能听到庙中钟磬木鱼及诵经之声此起，
这里的矮胖和尚见你进来，先是吓了一跳，便向你发动攻击！
LONG
	);
	set("no_clean_up", 0);
	set("indoors", 1);
	set("exits", ([ /* sizeof() == 1 */
  "out" : __DIR__"mountain.lpc",
]));
set("objects", ([
	 __DIR__"npc/monk" : 1,
                 __DIR__"npc/worm" : 1,
	 ]) );
	setup();
	replace_program(ROOM);
}
