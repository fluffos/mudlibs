// Stub: TLBB overlay named this skill but the archive had no daemon/skill/.
inherit SKILL;

int valid_enable(string usage)
{
	return usage == "unarmed" || usage == "parry";
}

int valid_learn(object me) { return 1; }
