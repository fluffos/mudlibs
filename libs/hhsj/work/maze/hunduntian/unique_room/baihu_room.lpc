#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIY "混沌森林" NOR);
  set("long", @LONG
混沌森林
LONG);

  set("maze", 1);
  set("no_magic", 1);
  set("virtual_room", 1);
  set("no_clean_up", 1);
  set("outdoors", "hunduntian");

  set("objects", ([
    "/maze/hunduntian/npc/king2": 1,
  ]));
  setup();
  // replace_program(ROOM);
}
