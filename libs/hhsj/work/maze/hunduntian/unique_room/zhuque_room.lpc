#include <ansi.h> 
inherit ROOM;

void create()
{
        set("short", HIB"混沌火海"NOR);
        set("long", @LONG
镇压封印处！
LONG);
        
        set("maze", 1);
        set("no_magic", 1);
        set("virtual_room",1);
        set("no_clean_up", 1);
        set("outdoors", "hunduntian");
        
        set("objects",([
                "/maze/hunduntian/npc/king":1,
        ]));
        setup();
        // replace_program(ROOM);
}
