#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIC "混沌风暴" NOR);
  set("long", @LONG
三千神魔之一罗喉封印之地
LONG);

  set("maze", 1);
  set("no_magic", 1);
  set("virtual_room", 1);
  set("no_clean_up", 1);
  set("outdoors", "hunduntian");

  set("objects", ([
    "/maze/hunduntian/npc/king4": 1,
  ]));
  setup();
  // replace_program(ROOM);
}
