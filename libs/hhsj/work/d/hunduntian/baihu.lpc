#include <ansi.h> 
inherit ROOM;

void create()
{
        set("short", HIY"????"NOR);
        set("long", @LONG
???????????
LONG);
        
        set("maze", 1);
        set("no_magic", 1);
        set("virtual_room",1);
        set("no_clean_up", 1);
        set("outdoors", "hunduntian");
		
	   set("exits", ([
                 "up" : "/d/city/guangchang",
         ]));
        
        set("objects",([
                "/maze/hunduntian/npc/baihu": 1,  
        ]));
        setup();
        // replace_program(ROOM);
}
