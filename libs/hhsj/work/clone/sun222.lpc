// This program is a part of NT MudLIB
// sun.c 日魂

#include <ansi.h>

inherit TESSERA;

void create() {
  set_name(RED "日魂-天下第二" NOR, ({ "sun soul", "sun" }));
  set_weight(1);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    set("long", RED "太阳真火凝聚而成，乌金色，能破邪怯魔，千军辟易。。\n" NOR);
    set("yuanbao", 10);
    set("unit", "块");
    set("can_enchant", "weapon");
    set("can_be_enchased", 1);
    set("magic/type", "magic");
    set("magic/power", 500);
    set("auto_load", 1);
  }
  set("enchase/type", "all");
  set("enchase", ([ /* sizeof() == 5 */
    "weapon_prop": ([ /* sizeof() == 3 */
      "derive_effect": 20,
      "research_times": 500,
      "research_effect": 20,
      "practice_effect": 20,
      "study_times": 500,
      "learn_times": 20,
      "derive_times": 500,
      "practice_times": 500,
      "max_experience": 3000000,
    ]),
    "type": "all",
    "SN": 9,
  ]));

  set("enchant", 1);
  set("set_data", 1);
  setup();
}
