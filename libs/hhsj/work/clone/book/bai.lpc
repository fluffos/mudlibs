// jiuyang.c

inherit BOOK;
inherit F_UNIQUE;
 
void create()
{
        set_name("白金剑气", ({ "j q", "j", "q" }));
        set_weight(500);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
                set("unit", "本");
                set("long",
"这是一本薄薄的经书，。\n",
        );
                set("value", 1);
                set("no_sell", 1);
                set("material", "paper");
                set("skill", ([
                        "name"        : "baijin-jianqi",
				        //"family_name" : "明教",
                        "exp_required":  100000,
                        "jing_cost"   :  120,
                        "difficulty"  :  40,
                        "max_skill"   :  180,
                        // "min_skill"   :  30,
                ]) );
        }
}

int query_autoload() { return 1; }
