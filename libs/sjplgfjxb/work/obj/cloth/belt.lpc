#include <armor.h>
inherit WAIST;
void create()
{
	set_name("腰带", ({ "belt" }) );
	set_weight(1000);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "条");
            set("value",10);
		set("material", "cloth");
		set("armor_prop/armor", 1);
	}
	setup();
}
