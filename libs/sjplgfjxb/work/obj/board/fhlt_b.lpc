// common_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("《烽火连天》留言板", ({ "board" }) );
	set("location", "/slg/center/changan");
	set("board_id", "fhlt_b");
	set("long",	"这是一个供人留言记事的留言板。\n" );
	setup();
	set("capacity", 90);
	replace_program(BULLETIN_BOARD);
}
