// Room: nroad2.c

inherit ROOM;

void create() {
  set("short", "南市场");
  set("long", @LONG
这里是福州城的南市场，西面是杂货铺，向东便是一家粮店。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "zaihuoy",
    "east": __DIR__ "liangdian",
    "north": __DIR__ "nroad1",
    "south": __DIR__ "cross",
  ]));
  set("outdoors", "fuzhou");
  set("objects",
    ([  //sizeof() == 1
    "/npc/newnpc/huangyuzhen": 1,
    ]));
  set("hell", 1);

  setup();
  replace_program(ROOM);
}
