// Room: eroad4.c

inherit ROOM;

void create() {
  set("short", "茶场");
  set("long", @LONG
这里是福州城的茶场，福州茶名扬天下，因此这里有不少茶工在忙着
采茶。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "northwest": __DIR__ "eroad3",
  ]));

  set("objects", ([
    __DIR__ "npc/worker": 4,
  ]));

  set("outdoors", "fuzhou");
  set("hell", 1);

  setup();
  replace_program(ROOM);
}
