// Room: /u/fuzhou/eroad1.c

inherit ROOM;

void create() {
  set("short", "福州城街道");
  set("long", @LONG
这里是十字路口的东侧，两边房子里都颇为吵闹。北面似乎是家春
楼，不时可听到风尘女子的卖笑声。南面却是些三教九流的人物在吆三
喝五，象是家赌场。
LONG
  );
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/eba": 1,
  ]));
  set("outdoors", "fuzhou");
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "duchang",
    "north": __DIR__ "jiyuan",
    "west": __DIR__ "cross",
    "east": __DIR__ "eroad2",
  ]));
  set("objects",
    ([  //sizeof() == 1
    "/npc/newnpc/ouyang": 1,
    ]));
  set("hell", 1);
  setup();
  replace_program(ROOM);
}
