// Room: /u/shandong/hl-shulin3.c

inherit ROOM;

void create() {
  set("short", "黑龙潭的树林");
  set("long", @LONG
这里又是一片树林，高大的树木把你的视线挡得严严实实的。那奇
怪的响声却更大了。树林中有一条小路向南通去。
    往南还是往北你自己拿主意吧。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "hl-tingzi1",
    "south": __DIR__ "hl-shitai",
  ]));

  set("outdoors", "shandong");

  set("hell", 1);

  setup();
}
