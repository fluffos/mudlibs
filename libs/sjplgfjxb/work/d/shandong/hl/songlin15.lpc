// Room: /u/shandong/songlin1.c

inherit ROOM;

void create() {
  set("short", "黑龙潭的松树林");
  set("long", @LONG
你走进一片松树林。抬头望去，满树都是松果。如此大的松果你却
是从没见过。想必那松子一定好吃的很（你不禁流口水了）。你想试试
能不能爬上去。可是树太高了爬是不大可能。还是踢两脚吧，定不定掉
下几个来也是好的。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "songlin10",
    "east": __DIR__ "songlin16",
    "north": __DIR__ "songlin14",
  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/yang": 2,
    ]));

  set("outdoors", "shandong");

  set("hell", 1);

  setup();
  replace_program(ROOM);
}
