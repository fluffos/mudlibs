// Room: /u/shandong/hl-tingzi1.c

inherit ROOM;

void create()
{
	set("short", "黑龙潭的小亭子");
	set("long", @LONG
这里是一个小亭子。与其说是小亭子，不如说是茅屋。几根高低不
一的松木顶着一个茅草盖。（你实在想不出词把它说的再好听些了。）
这是从远处传来一阵奇怪的响声。难道是那黑蛇、黑龙？你的心又跑到
喉咙里了。
    往南走是一片树林；
    往西北走是一个三岔路口。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"hl-shulin3",
  "northwest" : __DIR__"hl-xiaojing3",
]));

       set("outdoors","shandong");
        set("objects", 
        ([ //sizeof() == 1
                "/npc/newnpc/mochouren" : 1,
        ]));

	set("hell", 1);

	setup();
       replace_program(ROOM);
}
