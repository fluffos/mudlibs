// Room: /u/shandong/hl-xiaojing4.c

inherit ROOM;

void create()
{
	set("short", "黑龙潭的小径");
	set("long", @LONG
又是一条小径，和先前走过的也没啥两样。你无心观赏，还是找路
继续走吧。望东面瞧去是一片树林，也不知里面有啥。
    往东走是一片树林；
    往西南走是一个三岔路口。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */

  "east" : __DIR__"hl-shulin2",
  "southwest" : __DIR__"hl-xiaojing3",
]));

       set("outdoors","shandong");

	set("hell", 1);

	setup();
       replace_program(ROOM);
}
