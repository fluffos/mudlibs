// Room: /u/shandong/ta-road2.c

inherit ROOM;

void create() {
  set("short", "泰安城里");
  set("long", @LONG
这里是泰安城里的一条小街。南面有一家杂货铺，门外的布幌子已
经很旧了，但上面的“杂”字还看的清。北面的二层红楼是泰安城最大
的酒店“宏泰酒楼”。这里游客很多所以“宏泰酒楼”的买卖很好，有
时竟然会没有座位。另外，这“宏泰酒楼”的娄老板原是泰山派的俗家
弟子在江湖上有些小名气，所以武林中人路过此地一般也来捧捧场。
    往东走有泰安城的中心，
    往西走一直通向泰安城的西门。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "ta-road1",
    "east": __DIR__ "ta-road3",
    "north": __DIR__ "ta-inn1",
    "south": __DIR__ "ta-zahuopu",
  ]));
  set("outdoors", "shandong");
  set("objects",
    ([  //sizeof() == 1
    "/obj/npc/fatman": 1,
    ]));
  set("hell", 1);

  setup();
  replace_program(ROOM);
}
