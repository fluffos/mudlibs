// Room: /u/shandong/ta-shuju.c

inherit ROOM;

void create() {
  set("short", "魏家书局");
  set("long", @LONG
这里是魏家书局。屋里点着天竺贩卖来的名香，清幽淡雅沁人心扉。
这里没有柜台，四周摆着几个书架。书架上都是各种各样的书籍。魏柄
林魏老先生乃是当朝探花的授业恩师，学富五车远近学子多来求教。魏
老先生闲来无事在此开了家书局，出卖各种书籍。如果你与老先生有源
或许魏老先生也会指点你一些。墙上挂着一个牌子。(sign)
    往南走是一条东西走向的小街。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "ta-road1",
  ]));
  set("item_desc", ([ /* sizeof() == 1 */
    "sign": "最新书讯\n"
  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/wei": 1,
    ]));
  set("hell", 1);
  set("NONPC", 1);
  set("no_study", 1);
  set("no_fight", 1);
  set("no_steal", 1);
  set("no_magic", 1);

  setup();
}
