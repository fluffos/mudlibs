// Room: /u/shandong/ta-zhaojia.c

inherit ROOM;

void create() {
  set("short", "赵员外家");
  set("long", @LONG
这里是赵员外家里。赵员外是方圆百里内有名的富豪。独子
赵宏业因得名师魏柄林的指点，得中探花现在朝中作官。老两口
在家中悠闲自在，只盼能早日抱上孙子。见你胡乱闯了近来，脸
上表情十分不悦。
    由于老两口并不好客东、西、南三面的门都关着。
    往北走是一条东西走向的小街。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "ta-road1",
  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/yuanwai": 1,
    ]));
  set("hell", 1);
  setup();
}
