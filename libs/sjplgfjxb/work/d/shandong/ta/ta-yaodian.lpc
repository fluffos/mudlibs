// Room: /u/shandong/ta-yaodian.c

inherit ROOM;

void create() {
  set("short", "济世堂");
  set("long", @LONG
这里是一家规模不小的药店。由于泰山上药材资源极为丰富，
这家药店的货自是又好又全。何掌柜乃是农民出身，穷人看病分
文不取，在这一带口碑甚好。他经常亲自给病人配药，如果病人
家远不便来取，他就叫伙计送药上门。柜台上摆着几付给穷人免
费取用的药包和一个牌子(sign)。
    往西走是一个丁字路口。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "ta-road5",
  ]));
  set("item_desc", ([
    "sign": "              祖传医术  包治百病\n"
  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/yaopu_boss": 1,
    ]));
  set("hell", 1);
  setup();
  //        replace_program(ROOM);
}
