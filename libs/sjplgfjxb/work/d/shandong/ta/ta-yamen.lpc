// Room: /u/shandong/ta-yamen.c

inherit ROOM;

void create() {
  set("short", "衙门");
  set("long", @LONG
这里是泰安城的衙门。这里收拾得很干净，两边立着一些诸
如回避、肃静一类的牌子。透过半开的堂门可以看到一个稍显肥
胖的知府悠闲的坐在堂中。由于人少事少，这里十分清静。衙役
们整齐的站在两旁。而大堂里面师爷正不知和知府嘀咕些什么。
    往东走是一条南北走向的小街。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "ta-road4",
  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/yayi": 6,
    ]));
  set("hell", 1);

  setup();
  replace_program(ROOM);
}
