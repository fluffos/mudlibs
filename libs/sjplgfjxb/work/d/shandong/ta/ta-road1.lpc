// Room: /u/shandong/ta-road1.c

inherit ROOM;

void create() {
  set("short", "泰安城里");
  set("long", @LONG
这里是在泰安城里的一条小街。南面有一座红墙碧瓦的宅院
打听得知是城里的富豪赵员外的私宅，门口的老仆躲在阴凉里扇
着扇子乘凉。北面的店铺外挂着一个大牌子上书一字“书”显然
是一家书局，门口贴了一副对子“万般皆下品，唯有读书高”。
    往东走有泰安城最大的酒店，
    往西走是泰安城的西门。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "ta-ximen",
    "east": __DIR__ "ta-road2",
    "north": __DIR__ "ta-shuju",
    "south": __DIR__ "ta-zhaojia",
  ]));
  set("outdoors", "shandong");
  set("objects",
    ([  //sizeof() == 1
    "/npc/newnpc/muzhu": 1,
    ]));
  set("hell", 1);

  setup();
}
