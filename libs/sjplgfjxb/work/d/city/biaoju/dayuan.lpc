//biaoju dayuan.c
inherit ROOM;

void create() {
  set("short", "大院");
  set("long", @LONG
这里是红旗镖局的大院，地方不小，有几个镖伙正忙着整理大车，看上
去就要出发的样子，人声有些吵杂．在大院的南边就是镖局的议事大厅了。

LONG);


  set("exits",
    ([  //sizeof() == 4
    "east": __DIR__ "damen",
      "south": __DIR__ "yishiting",
      "west": __DIR__ "changlang3",

    ]));

  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/zhoubiqi": 1,
      __DIR__ "npc/biaoshi": 1,
      __DIR__ "npc/tangzishou": 3,
    ]));
  set("no_clean_up", 0);
  setup();
}
