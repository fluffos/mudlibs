inherit ITEM;
inherit F_FOOD;

void create()
{
	set_name("白砂糖", ({ "tang" }) );
	set_weight(20);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("long", "一包烧菜用的白砂糖。\n");
		set("unit", "包");
		set("value", 90);
		set("food_remaining", 1);
		set("food_supply", 1);
	}
}
