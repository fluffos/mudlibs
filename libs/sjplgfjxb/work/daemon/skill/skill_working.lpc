// 劳动能力

inherit SKILL;

string *dodge_msg = ({
	"你身先士卒，带头苦干，大伙儿的干劲也上来了。\n",
	"你指挥自如，调度有方，工作井然有序地进行着。。。\n",
	"你大声叫道：大伙儿不要偷懒，争取早日完工！\n",
	"你望着满脸灰尘、汗流浃背的人们，不禁对完成任务充满了信心。\n"
});

int valid_enable(string usage)
{
	return (usage=="dodge") || (usage=="move");
}

int valid_learn(object me)
{
	if( (int)me->query("max_force") < 0 )
		return notify_fail("你的力气不够，没有办法锻炼。\n");

	return 1;
}

string query_dodge_msg(string limb)
{
	return dodge_msg[random(sizeof(dodge_msg))];
}

int effective_level() { return 1;}

string perform_action_file(string action)
{
        return CLASS_D("guanfu") + "/other_skills/" + action;
}

