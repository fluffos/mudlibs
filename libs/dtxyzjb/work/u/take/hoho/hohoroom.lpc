//take make it for adm hoho ...9/27/2001.
#include <ansi.h>

inherit ROOM;

void create ()
{
  set ("short", HIY"伊甸园"NOR);
  set("exits", ([ /* sizeof() == 2 */
       "back" : "/d/city/kezhan",

]));
        set("objects", 
        ([ //sizeof() == 1
                "/u/take/hohodashi": 1,
        ]));

 set("outdoors", 1);

  setup();
}

