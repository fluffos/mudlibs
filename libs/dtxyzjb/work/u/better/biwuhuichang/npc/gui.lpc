
#include <dbase.h>

inherit  NPC_SAVE;

void greeting(object me);

void create()
{
    reload("leitaiscore");
        set_name("龟仙人", ({"gui","gui","gui"}));
        set("long","一个威风凛凛的老人，专门负责维持会场秩序。\n");

        set("gender", "男性");
        set("age", 500);
        set("per",30);
        set("kee", 800); 
        set("max_kee", 800);
        set("sen", 200);
        set("max_sen", 200);
        set("combat_exp", 50000);
        set("attitude", "friendly");
        setup();
  }

