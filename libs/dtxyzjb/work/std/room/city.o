// a skeleton for user rooms
//Last Modified By PKYOU@DHXY 2002/05/14
inherit "/std/room/city";
void create()
{
set("short", "SHORT_TITLE");
set("save_name","SAVE_NAME");
reload();
set("long", @LONG
LONG_TITLE
LONG
);
        set("exits", ([
]));
set("objects", ([
       ]) );
        set("class", "bigwiz7001");
        set("no_fight",1);
        set("valid_startroom",1);
        set("sleep_room", "1");
        set("no_clean_up",1);

setup();
}
int clean_up()
{return 0;}

