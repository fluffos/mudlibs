// Room: /d/tianmo/bdoor
inherit ROOM;

void create()
{
  set ("short", "绿色小门");
  set ("long", @LONG
这是一扇淡绿色的小门，门后面你隐隐约约听见好象有人在弹琴
音色悠扬，你好象都可以在琴音中睡去。
LONG);

  set("exits", ([ /* sizeof() == 3 */
  "north" : __DIR__"qinshi.lpc",
   "west" : __DIR__"lroad7.lpc",
   "east" : __DIR__"rroad7.lpc",
]));
  setup();
  replace_program(ROOM);
}
