inherit ROOM;

void create ()
{
  set ("short", "异形基地");
  set ("long", @LONG

这就是异形基地啦，想要得到里面的宝物，那么就杀掉八神庵吧。
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "out" : __DIR__"kongjian.lpc",
]));
  set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/laotou" : 1,
  __DIR__"npc/iori" : 1,
]));
  set("light_up", 1);

  setup();
}

