//changan city

inherit ROOM;

void create ()
{
        set ("short", "青龙大街");
        set ("long", @LONG

你走在一条宽阔的石板大街上，西面就快到城中心了，可以看到钟楼
耸立于前。南面是长安武馆，门匾上四个金字闪闪发光。北面是一家
兵器铺子，是城内的振远镖局开的，专卖一些兵器。
LONG);

        set("exits", 
        ([ //sizeof() == 4
                "north" : __DIR__"bingqipu",
                "south" : "/d/wuguan/wuguangate",
                "west" : __DIR__"center",
                "east" : __DIR__"qinglong-e2",
        ]));


        set("outdoors", "city");

        setup();
	replace_program(ROOM);
}

