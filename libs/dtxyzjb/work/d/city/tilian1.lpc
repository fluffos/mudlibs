inherit ROOM;

void create ()
{
  set ("short", "提炼房");
  set ("long", @LONG

这里是提炼房，房子中间摆放着一尊宝石提炼台，你可以用它来提炼各种宝石。\n

LONG);

  set("objects", ([ /* sizeof() == 2 */
  "/d/npc/vr_room/obj/tai" : 1,
  ]));
/*set("no_fight", 1);
set("no_magic", 1);
set("no_steal", 1);*/
  set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"paimaiwu",
]));
	set("action_list", ([
		HIC"租用"NOR:"init",
		HIG"放宝石"NOR:"add baoshi in tai",
		HIY"加温"NOR:"jiawen",
		HIW"降温"NOR:"jiangwen",
		GRN"测温"NOR:"tem",
		HIM"结束"NOR:"finish",
	]));
  setup();
}

