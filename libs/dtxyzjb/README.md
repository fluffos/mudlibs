# 大唐西游指间版 (dtxyzjb)

2009「指间版」大唐西游 mudlib。RAR 注释里的密码是 `676036579`。端口 40267。

浏览器/WASM 第一行就是英文 id（手机指间客户端仍走 `ver1.0` 密文握手）。
管理员 `fluffos` / 云游，登录 `Play2026x`，巫师专用 `Wiz@2026x`，
落地南城客栈。WASM `look`/`score`/`quit` 可用。房间输出带指间标记。
