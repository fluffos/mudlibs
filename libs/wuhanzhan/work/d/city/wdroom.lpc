// Room: /d/city/wdroom.c

inherit ROOM;

void create() {
  set("short", "密码报失处");
  set("long", @LONG

这是一间类似于监狱一样的屋子,屋子里有几个彪行大汉,玩家可
以在这里报失自己的id,但需要有5个以上的非同一玩家的id 证
明,如果发现有做伪证的现象,一律严惩!这里的留言版不可以写
与主题无关的内容,否则被关大牢1小时!
LONG
  );
  set("no_clean_up", 0);
  set("no_time", 1);
  set("no_magic", 1);
  set("no_fight", 1);
  set("exits", ([ /* sizeof() == 1 */
    "down": "/d/obj/shop/changan_shop",
  ]));

  call_other("/obj/board/pass_b", "???");
  setup();
  replace_program(ROOM);
}
