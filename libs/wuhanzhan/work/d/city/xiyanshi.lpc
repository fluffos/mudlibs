//changan city

inherit ROOM;

void create() {
  set("short", "吸烟室");
  set("long", @LONG

这里是长安唯一可以吸烟的地方，憋坏了的烟鬼们，一有机会就瞒着老婆孩子
到这里来过过瘾。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "north": __DIR__ "kezhan",
    ]));



  set("can_smoke", 1);
  set("no_clean_up", 0);
  setup();
}
