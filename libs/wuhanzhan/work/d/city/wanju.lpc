//by sadboy
//changan city

inherit ROOM;

void create() {
  set("short", "玩具坊");
  set("long", @LONG

这里是玩具坊,一些充满爱心的大米的休息,聊天场所.你可以找
漂亮的店主雪绒花要些买不到的玩具,饿了还可以找她要吃的,当然,
要用钱买的.
LONG);


  set("exits",
    ([  //sizeof() == 4
    "north": "/d/city/dian",
    ]));

  set("objects",
    ([  //sizeof() == 1
    "/u/sadboy/xrh": 1,
      // "/u/vir/npc/gift_lady" : 1,
      //  "/u/skyhawk/npc/gift" : 1,
    ]));


  set("no_fight", 1);
  set("no_cast", 1);
  set("no_magic", 1);
  set("no_exert", 1);
  set("no_clean_up", 0);
  setup();
  call_other("/obj/board/bet_b", "???");
  replace_program(ROOM);
}
