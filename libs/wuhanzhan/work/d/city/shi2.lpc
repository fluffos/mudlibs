#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIY "麻将室" NOR);
  set("no_fight", 1);
  set("no_magic", 1);
  set("long", @LONG
这是一间麻将室，就差一个脚了。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "out": __DIR__ "sleep", ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "east": 1,
      __DIR__ "west": 1,
      __DIR__ "south": 1,
      __DIR__ "new-mj": 1,

    ]));
  setup();
}
