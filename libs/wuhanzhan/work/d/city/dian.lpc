// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */
//by qqzqq 12/2/2001
//changan city

#include <login.h>
#include <room.h>
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "宠物店");
  set("long", @LONG

这里是长安有名的宠物店，城里的太太小姐都喜欢来这里领养一些小猫，
小狗。也有一些成名的英雄来这里挑选自已的座骑。这里的老板就是最
美丽的看不清楚了。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "north": "/d/obj/shop/changan_shop",
      "south": "/d/city/wanju",
    ]));

  set("objects",
    ([  //sizeof() == 1
    "/d/city/npc/petboss": 1,
      "/u/skyhawk/npc/skyhawk": 1,
      "/u/vir/npc/new": 1,
    ]));


  //        set("outdoors", "changan");
  set("no_fight", 1);
  set("no_cast", 1);
  set("no_magic", 1);
  set("no_exert", 1);
  set("no_clean_up", 0);
  setup();
  call_other("/obj/board/sldh_b", "???");
}

void init() {
  add_action("do_dadad", "bian");
}
int do_dadad(string arg) {
  object who = this_player();
  object me = this_object();
  message_vision("想变出去变！”\n", who);
  return 1;
}
