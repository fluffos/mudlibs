// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */

//changan city

inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "含秀轩");
  set("long", @LONG
这里包子铺后面的一个小别院，据说数年前被一位西域来客租下当作
书斋，只见屋内除了桌椅各一张外，就只剩下书架了。整个房间里面
散发着淡淡的书香，加上时不时自前面传来的的包子香。恩，真是一
个读书的好地方。
LONG);

  set("exits",
    ([  //sizeof() == 4
    "east": "/u/lol/workroom",
      "up": "/u/lol/room/shuxiang",
    ]));

  set("objects",
    ([  //sizeof() == 1
    "/u/lol/npc/ayani": 1,
    ]));

  set("no_clean_up", 0);
  setup();

}
