inherit ROOM;

void create ()
{
  set ("short", "壁斗崖底");
  set ("long", @LONG

你从头顶黑雾中的壁斗崖上跳了下来，才发现其实崖并不高。
崖壁笔直如削，崖下地面倒是很平，只是黑雾依然很浓，面对
面都看不清人。你忽然听到一阵凄惨地哭声………
LONG);
  set("hell", 1);
  set("objects", ([ /* sizeof() == 1 */
//   __DIR__"npc/laotou" : 1,
]));

  setup();
}

