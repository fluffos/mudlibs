//    cmds/usr/news.c
#include <ansi.h>
inherit ROOM;
void create() {
  set("short", HIY "新闻发布中心" NOR);
  set("long", @LONG

    这里是武汉站的新闻发布中心，有什么新闻都可以在这里发布(post)，
然后系统自动随机通知所有在线玩家。如果那位巫师需要发布新闻而
新闻版不在的话，请先"news all"。


LONG
  );

  set("exits", ([ /* sizeof() == 2 */
    "down": __DIR__ "wizroom",
  ]));
  set("objects", ([ /* sizeof() == 2 */
  ]));
  setup();
}
