
inherit ROOM;

int block_cmd();
void create ()
{
  set ("short", "号子");
  set ("long", @LONG

俗称“号子”其实就是拘留所了，黑漆漆的一间小屋，看样子不老实点是出不去了
谁叫你骂人呢，自找苦吃。
LONG);


  set("exits", ([ /* sizeof() == 2 */
]));
  set("objects", ([ /* sizeof() == 1 */
]));
        set("valid_startroom", 1);
       set("wiz_board",1);
  setup();
  call_other("/obj/board/wizard_b", "???");
}

void init()
{
        if (!wizardp(this_player()))
          add_action("block_cmd","",1);   
}

int block_cmd()
{
       string verb = query_verb();

       if (verb=="say") return 0;
       if (verb=="look") return 0;
       return 1;
}
