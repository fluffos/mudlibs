// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */

//changan city

inherit ROOM;

void create() {
  set("short", "水廉洞");
  set("long", @LONG

盘丝洞是孙悟空的住所。开始盘丝大仙的发现。后被孙悟空改为水廉洞
。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "south": __DIR__ "up2",
      "east": __DIR__ "sleeproom",
      "west": "/d/obj/qlj/room/froom",
    ]));

  set("objects",
    ([  //sizeof() == 1
    "/d/4world/npc/xiang": 1,
    ]));


  set("outdoors", "changan");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
