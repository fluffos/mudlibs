// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */
 
    // Room: /u/emaile/room/tianwangfu.c

inherit ROOM;

void create()
{
   set("short", "天王府");
   set("long", @LONG
这里就是天宫的护卫拜师学艺的地方，慕名来的人很多。大门两侧
有两个高大威猛的卫兵。院子里看起来金碧辉煌的。南边是个空场，
大概是天王府操练的地方。
LONG
   );
   set("exits", ([ /* sizeof() == 2 */
    "north" : "/d/sky/yunlougate",
    "south" : __DIR__"fight",
]));
   set("out_doors", 1);
   set("objects", ([ /* sizeof() == 1 */
]));
   set("no_clean_up", 0);

   setup();
}

int valid_leave(object me, string dir)
{
    if (dir == "south" ) {
     return ::valid_leave(me, dir);
           }
       return 1;
   }

