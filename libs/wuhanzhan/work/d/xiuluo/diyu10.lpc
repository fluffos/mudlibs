
#include <ansi.h>
#include <room.h>
inherit ROOM;
void create()
{
  set ("short", HIW"寒冰狱"NOR);
  set ("long", @LONG

**************************************
**               寒冰狱             **
**************************************

LONG);
set("exits", ([ /* sizeof() == 1 */
"down" : "/d/xiuluo/diyu11",
]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__"npc/18gui" : 1,
]));
        setup();
}
int valid_leave(object me, string dir)
{       if (dir == "down") {
        if (objectp(present("gui", environment(me))))
        return notify_fail("鬼怪叫道：“你给老子找死吗？竟敢来我十八层地狱！\n");
        }

        return ::valid_leave(me, dir);
        }

