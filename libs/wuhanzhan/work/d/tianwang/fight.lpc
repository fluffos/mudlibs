// fight.c

inherit ROOM;

void create() {
  set("short", "练武场");
  set("long", @LONG
空阔的场地上铺满了细细的黄土，正好适合练武。有几个天兵
打扮模样的弟子正练的满头大汗，拳风霍霍，吆喝阵阵。好
一派士气高昂的景象。
LONG);
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "gate",
    "east": __DIR__ "sleeproom",
    "south": __DIR__ "tianwangtang",
  ]));
  set("objects", ([ /* sizeof() == 3 */
  ]));
  setup();
}
