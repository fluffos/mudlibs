// dating.c

inherit ROOM;

void create() {
  set("short", "天王厅");
  set("long", @LONG
天王厅内庄严肃穆，金壁辉煌，大厅里金光闪烁瑞霭如云紫气环绕，
大厅中是雕龙绘凤祥云朵朵，一位威严的天王手执巨斧立在厅内，
一群天王府的弟子正在聆听天王的教诲。

LONG);
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "tianwangtang",
  ]));
  set("objects", ([ /* sizeof() == 3 */
    __DIR__ "npc/gm-tianwang": 1,
  ]));
  setup();
}
