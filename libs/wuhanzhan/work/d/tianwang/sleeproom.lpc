// sleeproom.c
inherit ROOM;

void create()
{
  set ("short", "卧室");
  set ("long", @LONG
天王府把他们的弟子照顾的周到无比，屋子四周香炉
里传来一阵阵清香。一张宽大柔软的床上铺着被褥，
让人不觉昏昏欲睡。

LONG);
set("exits", ([ /* sizeof() == 4 */
"west":  __DIR__"fight",
]));

        set("no_clean_up", 0);
        set("sleep_room",1);
        setup();
        replace_program(ROOM);
}
