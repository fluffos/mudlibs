inherit ROOM;
void create() {
  set("short", "药房");
  set("long", @LONG

         这里是天上人间的药房,四周排满了一排排药柜,空气中弥漫着一股清香的药味
     令人心旷神怡,这里是门主收集了天下的灵丹妙药,陈列在这里,供门下弟子选
     购(list).
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "bihai2",
  ]));

  set("objects", ([
    //                     __DIR__"npc/xxx" : 1,
  ]));

  setup();
}
