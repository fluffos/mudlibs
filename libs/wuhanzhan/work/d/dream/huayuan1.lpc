inherit ROOM;
void create() {
  set("short", "花苑");
  set("long", @LONG

       这里是天上人间的花苑,园中景色优美,林木柔绿,碧水环绕,各式各样的花儿
      在争芳斗艳.
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "east": __DIR__ "xxx",
  ]));

  set("outdoors", 1);

  setup();
  replace_program(ROOM);
}
