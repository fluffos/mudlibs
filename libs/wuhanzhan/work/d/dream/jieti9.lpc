#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "白色阶梯");
  set("long", @LONG

    白色的阶梯到这里就没有了，你面前是一座美丽的宫殿。
这里没有雾了，宫殿里传来美妙的乐曲,一阵阵淡雅的清香从
里面传了出来。这里真是世外桃园啊。
LONG
  );

  set("exits", ([
    "south": __DIR__ "jieti8",
    "north": __DIR__ "dream-dian",
  ]));
  set("no_magic", 1);
  set("objects", ([

    //       __DIR__"npc/huwai" : 2,

  ]));
  setup();
  replace_program(ROOM);
}
