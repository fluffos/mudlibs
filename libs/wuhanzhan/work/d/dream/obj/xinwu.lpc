inherit ITEM;

void create() {
  set_name("信物", ({ "xinwu" }));
  set_weight(100);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一枚天上人间的信物。\n");
    set("unit", "张");
    set("value", 100);
  }
}
