// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */

// Room: /d/qujing/pansi/dongtian.c

inherit ROOM;

void create() {
  set("short", "别洞天");
  set("long", @LONG

好一个福地洞天！这是盘丝洞与外界的交接处,
外面阴森恐怖，里面去别有洞天。世外桃园。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "rukou.lpc",
    "north": __DIR__ "shuidao.lpc",
    "east": __DIR__ "chunge2.lpc",
    "west": __DIR__ "chunge.lpc",
  ]));
  set("objects", ([  /* sizeof() == 1 */
    "/d/qujing/pansi/npc/zhangmen": 1,
  ]));
  set("outdoors", 0);

  setup();
}
void init() {
  if (!wizardp(this_player()))
    add_action("do_get", "get");
}
int do_get() { return 1; }
