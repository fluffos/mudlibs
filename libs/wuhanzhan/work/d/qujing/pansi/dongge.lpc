// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */

// Room: /d/qujing/wudidong/dongge.c

inherit ROOM;

void create() {
  set("short", "冬阁");
  set("long", @LONG

这里是紫霞仙子的住所[冬阁]，你眼前忽然一亮，这里布置得华丽中
不乏清雅，平实中透着高贵。楠木方桌，大太师椅，以及墙上的字画都
透着一股非凡之气。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "baodao.lpc",
    "up": __DIR__ "mishi.lpc",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/zixia": 1,
  ]));
  set("outdoors", 0);

  setup();
}
