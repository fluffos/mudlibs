// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */
 
// Room: /d/qujing/wudidong/chunge.c

inherit ROOM;

void create ()
{
  set ("short", "春阁");
  set ("long", @LONG

这里是春夏秋冬四阁的第一阁——春阁，果然与众不同。花草，字
画都仿佛对人说明，这里是春的世界。的确，淡淡的花香，行如游
龙的书法，使来人留念忘返。
LONG);

  set("exits", ([ /* sizeof() == 1 */
     "east" : __DIR__"dongtian.lpc",
]));
  set("objects", ([ /* sizeof() == 1 */
        __DIR__"npc/dao" : 1,
]));
  set("outdoors", 0);

  setup(); 
}

