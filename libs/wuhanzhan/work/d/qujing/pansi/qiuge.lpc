// qiuge.c
// by lestat/2001.1.31

inherit ROOM;

void create() {
  set("short", "秋阁");
  set("long", @LONG

秋阁的布局和其他不同，房间色调十分明亮，使人感受到浓
浓的秋意。墙壁上的字画，有菊花，有月季，有杜鹃，周围
摆放着青霞仙子特意收集来的的花卉，散发出令人陶醉的香
气。仙子正坐在梳妆台前。见你走进来，不由得瞥了你一眼。
LONG);
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "sleeproom.lpc",
    "east": __DIR__ "shuidao3.lpc",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/qingxia": 1,
  ]));
  setup();
}
