inherit ROOM;

void create()
{
   set("short", "神魔堂");
   set("long", @LONG

这里一个小妖都没有，大堂正中端坐着一个慈眉善目的的老者，见到
你进来了，他和蔼地笑了起来。

LONG
   );
   set("exits", ([ /* sizeof() == 2 */
  "down" : __DIR__"houdong",
  ]));
  set("objects", ([ /* sizeof() == 1 */
      __DIR__"npc/shu" : 1,
  ]));
 
   setup();
   replace_program(ROOM);
}
