#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "塞亚星球");
  set("long", @LONG

    风摇曳着树叶发出的沙沙声，天就一直是阴沉沉的。时时有干冷的风轻轻打着旋。。。

LONG);

  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "syroom",
    "east": __DIR__ "syroom4",
    "up": __DIR__ "tree",
  ]));
  set("outdoors", 1);

  setup();
}
