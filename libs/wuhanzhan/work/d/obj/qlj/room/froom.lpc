inherit ROOM;


void create() {
  set("short", "潋情轩");
  set("long", @LONG

               形影单只，寻寻觅觅，何时蝶成双？ 

               星砂本是一颗孤星，却有伴星相随.....

               恍忽中，抓住一点精灵的絮语......


LONG);

  set("mai_corpse_able", 1);
  set("exits", ([ /* sizeof() == 1 */
    "east": "/d/4world/dong",
  ]));
  set("objects",
    ([  //sizeof() == 1
    "/d/obj/qlj/npc/first": 1,
    ]));
  setup();
}

void init() {
  object me = this_player();

  message_vision("恍惚中，$N仿佛听到远处有人轻轻的叹息。。。\n", me);
}
