//skyhawk.2004.1.22
inherit SHOP;

void create() {
  set("short", "龙凤庄");
  set("long", @LONG
龙凤庄这里的东西都相当不错，城里的人都来这里买东西。逢年过节来
光顾的人就更多了。
LONG);
  set("open_long", @LONG
龙凤庄这里的东西都相当不错，城里的人都来这里买东西。逢年过节来
光顾的人就更多了。
LONG);
  set("no_fight", 1);
  set("no_magic", 1);
  set("owner", "VOID_SHOP");
  set("exits", ([
    "south": "/d/qujing/baoxiang/xi2",
  ]));
  set("no_steal", 1);
  set("objects", ([
    __DIR__ "waiter": 1,
  ]));
  set("no_clean_up", 0);
  set("shop_type", 0);

  setup();
}
