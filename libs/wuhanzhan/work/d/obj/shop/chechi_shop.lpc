//skyhawk.2004.1.22
inherit SHOP;

void create() {
  set("short", "英雄楼");
  set("long", @LONG
这里就是天下英雄集地的地方，远道而来的客人受到邀请可以在这
里休息。
LONG);
  set("open_long", @LONG
这里就是天下英雄集地的地方，远道而来的客人受到邀请可以在这
里休息。
LONG);
  set("no_fight", 1);
  set("no_magic", 1);
  set("owner", "VOID_SHOP");
  set("exits", ([
    "west": "/d/qujing/chechi/jieshi7",
  ]));
  set("no_steal", 1);
  set("objects", ([
    __DIR__ "waiter": 1,
  ]));
  set("no_clean_up", 0);
  set("shop_type", 0);

  setup();
}
