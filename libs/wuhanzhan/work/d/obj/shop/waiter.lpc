#include <ansi.h>
inherit WAITER;

void create() {
  set_name("伙计", ({ "huo ji", "huo", "ji" }));
  set("title", HIY "富甲天下" NOR);
  set("long", "他就是这家店铺的伙计。\n");
  set("gender", "男性");
  set("age", random(10) + 20);

  setup();
  carry_object("/d/obj/cloth/linen")->wear();

}
