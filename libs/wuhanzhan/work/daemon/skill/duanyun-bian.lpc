// duanyun-bian.c 断云鞭法

#include <ansi.h>

inherit SKILL;

mapping *action = ({
(["action":"$N一招"HIR" 聚火 "NOR",手扬鞭起，手中$w将$n团团围住，内力一吐，鞭圈匀速缩小。",
        "parry":        10,
        "dodge":        -10,
        "damage":       20,
        "name" : "聚火",
        "damage_type":  "内伤"
]),
(["action":"$N左手虚幻，右手$w一招"HIC" 起光 "NOR",手中$w由似一条青龙迅速击向$n。",
        "parry":        10,
        "dodge":        5,
        "damage":       23,
        "name" : "起光",
        "damage_type":  "刺伤"
]),
(["action":"$N腾空而起，手中$w舞成圆环，一招"HIB" 招雨 "NOR"压向$n,令$n无处可躲。",
        "parry":        10,
        "dodge":        10,
        "damage":       26,
        "name" : "招雨",
        "damage_type":  "内伤"
]),
(["action":"$N力贯鞭梢，一招"HIR" 动雷 "NOR"，手中$w舞出满天鞭影，排山倒海般扫向$n全身",
        "parry":        10,
        "dodge":        -5,
        "damage":       29,
        "name" : "动雷",
        "damage_type":  "淤伤"
]),
(["action":"$N往前一纵, 但见$N身形一转，一招"HIC" 移电 "NOR"，手中$w如鬼魅一般，神不知鬼不觉地袭向$n的$l！",
        "parry":        10,
        "dodge":        -10,
        "damage":       32,
        "name" : "移电",
        "damage_type":  "刺伤"
]),
(["action":"$N手持$w,力贯鞭稍，一招"HIR" 剪虹 "NOR"手中$w犹如彩虹般击向$n,已将$n笼罩于鞭下。",
        "parry":        10,
        "dodge":        -20,
        "damage":       35,
        "name":  "剪虹",
        "damage_type":  "刺伤"
]),
(["action":"$N左手虚晃，右脚突然向前迈出，一招"CYN" 止风 "NOR"，右手$w如同鬼魅一般无声无息的刺向$n!",
        "parry":        10,
        "dodge":        0,
        "damage":       38,
        "name" : "止风",
        "damage_type":  "刺伤"
]),
(["action":"$N向前纵出，身体飞过$n头顶，一招"HIW" 断云 "NOR"手中$w击向$n的$l",
        "parry":        10,
        "dodge":        10,
        "damage":       41,
        "name" : "断云",
        "damage_type":  "内伤"
]),
(["action":"$N手中$w晃出满天鞭影，一招"HIB" 遮月 "NOR"虚虚实实的鞭影，铺天盖地的击向$n",
        "parry":        10,
        "dodge":        50,
        "damage":       44,
        "name" : "遮月",
        "damage_type":  "淤伤"
]),
(["action":"$N一招"YEL" 掩日 "NOR"手中$w形似蛟龙又似飞蝗$w密不透风的罩向$n！",
        "parry":        10,
        "dodge":        40,
        "damage":       47,
        "name" : "掩日",
        "damage_type":  "淤伤"
]),
});

string *parry_msg = ({
        "$n一招"YEL" 掩日 "NOR"罩向$N，$N不得不向后急退一尺，
所有攻势都被封住了。\n",
        "$n一招"HIB" 遮月 "NOR"罩向$N，$N不得不向后急退一尺，
所有攻势都被封住了。\n",
        "$n一招"HIW" 断云 "NOR"罩向$N，$N不得不向后急退一尺，
所有攻势都被封住了。\n",
        "$n一招"CYN" 止风 "NOR"罩向$N，$N不得不向后急退一尺，
所有攻势都被封住了。\n",
        "$n一招"HIR" 剪虹 "NOR"罩向$N，$N不得不向后急退一尺，
所有攻势都被封住了。\n",
        "$n一招"HIC" 移电 "NOR"罩向$N，$N不得不向后急退一尺，
所有攻势都被封住了。\n",
        "$n一招"HIB" 招雨 "NOR"罩向$N，$N不得不向后急退一尺，
所有攻势都被封住了。\n",
        "$n一招"HIR" 动雷 "NOR"罩向$N，$N不得不向后急退一尺，
所有攻势都被封住了。\n",
        "$n一招"HIC" 起光 "NOR"罩向$N，$N不得不向后急退一尺，
所有攻势都被封住了。\n",
        "$n一招"HIR" 聚火 "NOR"罩向$N，$N不得不向后急退一尺，
所有攻势都被封住了。\n",  
});

int valid_enable(string usage) { return (usage == "whip") || (usage == "parry"); }

int valid_learn(object me)
{
        object weapon;

        if( (int)me->query("max_force") < 300 )
                return notify_fail("你的内力不足，没有办法练断云鞭法, 多练些内力再来吧。\n");

        if ((int)me->query_skill("boro-gong", 1) < 101)
                return notify_fail("你的大般若功火候太浅。\n");

        if ( !objectp(weapon = me->query_temp("weapon"))
        || ( string)weapon->query("skill_type") != "whip" )
                return notify_fail("你必须先找一条鞭子才能练鞭法。\n");

        return 1;
}

mapping query_action(object me, object weapon)
{
        return action[random(sizeof(action))];
}

  string query_parry_msg(object me, object weapon)
{
        return parry_msg[random(sizeof(parry_msg))];
}

int practice_skill(object me)
{
        object weapon;

        if (!objectp(weapon = me->query_temp("weapon"))
        || (string)weapon->query("skill_type") != "whip")
                return notify_fail("你使用的武器不对。\n");
                
        if ((int)me->query("kee") < 50)
                return notify_fail("你的体力不够练断云鞭法。\n");
        if ((int)me->query("force") < 50)
                return notify_fail("你的内力不够练断云鞭法。\n");
        me->add("kee", -30);
        me->add("force", -30);
        return 1;
}
mixed hit_ob(object me, object victim, int damage_bonus)
{
        if( damage_bonus < 100 ) return 0;

        if( random(damage_bonus/2) > victim->query_str() ) {
                victim->receive_wound("kee", (damage_bonus - 100));
                return HIR "你听到「喀啦」一声轻响，$n的$l的骨头尽数折断！\n" NOR;
        }
}

string perform_action_file(string action)
{
        return CLASS_D("tianwang") + "/duanyun-bian/" + action;
}
