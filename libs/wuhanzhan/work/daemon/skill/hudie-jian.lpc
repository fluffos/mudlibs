inherit SKILL;
#include <ansi.h>

mapping *action = ({
   ([   "action":
"$N步伐轻盈，掠风而过，一招"MAG"〖蕊香引蝶〗"NOR"，剑走偏锋，在空中划过半个弧形，一剑刺向$n的$l"NOR,
     "dodge":      25,
     "parry":     -20,
     "damage":     10,
     "damage_type":   "刺伤"
   ]),
   ([   "action":
"只见$N身行急转，使出"MAG"〖彩蝶飞舞〗"NOR"，于空中挽出数个剑花，朵朵剑花直逼$n的$l"NOR,
     "dodge":      35,
     "parry":      -25,
     "damage":     15,
     "damage_type":   "刺伤"
   ]),
   ([   "action":
"$N浅浅一笑，无限妩媚中一式"MAG"〖漫天蝶影〗"NOR"，$n只感万道剑光袭来，一个不慎，露出了破绽"NOR,
     "dodge":      10,
     "parry":     -15,
     "damage":     20,
     "damage_type":   "刺伤"
   ]),
   ([   "action":
"$N一式"MAG"〖香里寻蝶〗"NOR"，转身$w斜刺一剑，正好指向$n的$l"NOR,
     "dodge":      30,
     "parry":     -20,
     "damage":     25,
     "damage_type":   "刺伤"
   ]),
   ([   "action":
"$N身形飞转，百忙之中一招"MAG"〖蝶起蝶落〗"NOR"，这分明是一段优美的舞姿，可$n却感到了一阵恐惧"NOR,
     "dodge":     -10,
     "parry":       0,
     "damage":     30,
     "damage_type":   "刺伤"
   ]),
   ([   "action":
"$N娇态百露，侧身滑步前冲一剑，$w"NOR"不偏不倚地击向了$n的$l，
好一招"MAG"〖蝶影翩翩〗"NOR"。"NOR,
     "dodge":     10,
     "parry":      15,
     "damage":     30,
     "damage_type":   "刺伤"
   ]),
   ([   "action":
"$N暗中运起轻功，身法陡然加快，两道幻影一左一右同时闪向$n,好一招"MAG"〖化蝶双飞〗"NOR"。"NOR,
     "dodge":       10,
     "parry":        0,
     "damage":     35,
     "damage_type":   "刺伤"
   ]),
});

int valid_learn(object me)
{
   object ob;
   if( (string)me->query("gender",1) != "女性")
     return notify_fail("你对如此柔弱的剑法似毫不能领悟。\n");
   if( (int)me->query("married") != 1)
     return notify_fail("你未成家，尚不能体会这套剑法的精妙。\n");
   if( (int)me->query("max_force") < 1500 )
     return notify_fail("你的内力不够，没有办法练"MAG"〖蝴蝶剑法〗\n"NOR);
   if( (int)me->query_skill("sword") < 150 )
     return notify_fail("你的剑法不够纯熟，没有办法将"MAG"〖蝴蝶剑法〗"NOR"招数发挥淋漓尽致。\n"NOR);
   if( (int)me->query_skill("dodge") < 150 )
     return notify_fail("你的轻功不够，没有办法领悟"MAG"〖蝴蝶剑法〗"NOR"中的舞蹈技巧。\n"NOR);
   if( !(ob = me->query_temp("weapon")) || (string)ob->query("skill_type") != "sword" )
     return notify_fail("你必须先找一把剑才能练剑法。\n");

   return 1;
}

int valid_enable(string usage)
{
   return usage=="sword" || usage=="parry" || usage=="dodge";
}
 
mapping query_action(object me, object weapon)
{
   return action[random(sizeof(action))];
}

int practice_skill(object me)
{
   if( (int)me->query_skill("hudie-jian",1) < 200)
     return notify_fail("你对"MAG"〖蝴蝶剑法〗"NOR"的了解不足以靠练习来提高。\n");
   
   if( (int)me->query("kee") < 200
   ||   (int)me->query("force") < 300 )
     return notify_fail("你的内力或气不够，没有办法练习这套剑法。\n");
   me->receive_damage("kee", 30);
   me->add("force", -3);
   write("你持剑踏着步子按口诀舞动一番，看来有些领悟。\n");
   return 1;
}

string perform_action_file(string func)
{
return CLASS_D("others") + "/hudie-jian/" + func;
}

