inherit SKILL;
#include <ansi.h>

mapping *action = ({
   ([   "action":     "$N使出一招"+BLINK""+HIY"「虚空」"NOR+"，剑光闪烁不定，若有若无地刺向$n$l",
     "dodge":     0,
     "parry":     0,
     "damage":     25,
     "force":     70,
     "damage_type":   "刺伤"
   ]),
   ([   "action":     "$N身形转个不停，突然一招"+BLINK""+HIR"「长虹」"NOR+"使出，手中$w划出一道银弧，径刺$n$l",
     "dodge":     -3,
     "parry":     -3,
     "damage":     25,
     "force":     80,
     "damage_type":   "刺伤"
   ]),
   ([   "action":     "$N脚下一转，一招"+BLINK""+CYN"「裂地」"NOR+"手中$w势若飞瀑向$n脚下一斩",
     "dodge":     -6,
     "parry":     -6,
     "damage":     25,
     "force":     90,
     "damage_type":   "劈伤"
   ]),
   ([   "action":     "但见$N连退几步，忽然间纵身跃起，手中$w一式"+BLINK""+HIR"「爆裂」"NOR+"对准$n的$l凌空一斩",
     "dodge":     -9,
     "parry":     -9,
     "damage":     25,
     "force":     100,
     "damage_type":   "劈伤"
   ]),
   ([   "action":     "$N轻轻一跃，使了个"+BLINK""+WHT"「混沌」"NOR+"，却在转身之际对准$n$l斜斜挥出一剑",
     "dodge":     -12,
     "parry":     -12,
     "damage":     25,
     "force":     120,
     "damage_type":   "刺伤"
   ]),
   ([   "action":  "$N狂喝一声，使出一招"+BLINK""+HIW"「无极」"NOR+"，双手紧握$w带着逼人的杀气，刺向$n的$l",
     "dodge":     -15,
     "parry":     -15,
     "damage":     25,
     "force":     140,
     "damage_type":   "刺伤"
   ]),
   ([   "action":     "$N神态悠闲，手中$w凭空一指，一招"+BLINK""+HIG"「九天」"NOR+"轻妙地划向$n的$l",
     "dodge":     -18,
     "parry":     -18,
     "damage":     25,
     "force":     160,
     "damage_type":   "刺伤"
   ]),
   ([   "action":     "$N手中$w向外一分，使一招"+BLINK""+HIR"「乾坤」"NOR+"反手对准$n$l一剑刺去",
     "dodge":     -25,
     "parry":     -25,
     "damage":     25,
     "force":     180,
     "damage_type":   "刺伤"
   ]),
});

int valid_learn(object me)
{
   object ob;

   if( (int)me->query("max_force") < 500 )
     return notify_fail("你的内力不够，没有办法练霸邪剑法。\n");
   if( !(ob = me->query_temp("weapon"))
   ||   (string)ob->query("skill_type") != "sword" )
     return notify_fail("你必须先找一把剑才能练剑法。\n");
   return 1;
}
int valid_enable(string usage)
{
   return usage=="sword" || usage=="parry";
}
mapping query_action(object me, object weapon)
{
   return action[random(sizeof(action))];
}
int practice_skill(object me)
{
   if( (int)me->query("kee") < 30
   ||   (int)me->query("force") < 3 )
     return notify_fail("你的内力或气不够，没有办法练习霸邪剑法。\n");
   me->receive_damage("kee", 30);
   me->add("force", -3);
   write("你按着所学练了一遍霸邪剑法。\n");
   return 1;
}
string perform_action_file( string action)
{
return CLASS_D("sword") + "/baxie-jianfa/" + action;
}
