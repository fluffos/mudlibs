#include <ansi.h>
inherit ITEM;
void create() {
  set_name(HIY "铜币" NOR, ({ "tong bi", "tongbi" }));
  set_weight(30000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "枚");
    set("long", "铜币啊，灿烂的铜币啊。\n");
    set("value", 300);
    set("material", "gold");
  }
}
