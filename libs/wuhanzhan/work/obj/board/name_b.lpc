inherit BULLETIN_BOARD;

void create() {
  set_name("本站有奖征名收集版", ({ "board" }));
  set("location", "/d/city/center");
  set("board_id", "name_b");
  set("long", "欢迎将你心仪的名字留下。\n");
  setup();
  set("capacity", 200);
  replace_program(BULLETIN_BOARD);
}
