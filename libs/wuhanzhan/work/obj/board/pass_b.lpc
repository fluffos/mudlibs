inherit BULLETIN_BOARD;

void create()
{
        set_name("证明人留言处", ({ "board" }) );
              set("location", "/d/city/wdroom");
                      set("board_id", "pass_b");
                              set("long",     "密码报失处留言板。\n" );
                              set("capacity", 500);
                                      setup();
                                              replace_program(BULLETIN_BOARD);
                                              }
