
inherit BULLETIN_BOARD;

void create()
{
           set_name("工作笔记", ({ "board" }) );
           set("location", "/u/cibear/workroom");
           set("board_id", "cibear_b");
     set("long",        "cibear的笔记本。\n" );
     setup();
          set("capacity", 100);
 replace_program(BULLETIN_BOARD);
}
