
inherit BULLETIN_BOARD;

void create()
{
           set_name("涂鸦版", ({ "board" }) );
           set("location", "/d/lingjian/gate");
           set("board_id", "lingjian_b");
     set("long",        "一个专门征集心情故事和好的emote的版块。\n" );
     setup();
          set("capacity", 100);
 replace_program(BULLETIN_BOARD);
}
