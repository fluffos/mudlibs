inherit BULLETIN_BOARD;

void create()
{
           set_name("工作笔记", ({ "board" }) );
           set("location", "/u/vir/workroom");
           set("board_id", "vir_b");
     set("long",        "vir的备忘录。\n" );
     setup();
          set("capacity", 100);
 replace_program(BULLETIN_BOARD);
}

