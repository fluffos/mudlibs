// Room: /u/cibear/workroom.c

inherit ROOM;

void create()

{
  set("short", "发呆的荒原");
  set("long", @LONG
这是一片无边无际的草原，你可以在这里随便发呆
LONG
  );
  set("no_clean_up", 0);
  set("no_time", 1);
  set("exits", ([ /* sizeof() == 2 */
    "north": "/d/wiz/newsroom",
    "kz": "/d/city/kezhan",
  ]));
  call_other("/obj/board/cibear_b.lpc", "???");
  setup();
  replace_program(ROOM);
}
