//forsky's chatroom

#include <ansi.h>
inherit ROOM;
void create() {
  set("short", HIB "梦归居" NOR);
  set("long", @LONG

          )\       _
  .--._  ,' `\_.-~~/'
  `\    \'_    __ ( _ _ 
    `\   (_)  |__|/~ ~~=~\    风
      )_____.---~~ \>\~-./'
     /'    //===  /==(        尘
    (     /' __\ ( __\) 
    ( /~\(   o   |_o_(        自
    (( (         _____)
     \\_/     ,      )        画
      \ `\   ~-.._  /
     ._)/ \        /          像
      `/   \     ./
      /     `~/~'
    '~~-.__./      
这里是风尘休息的地方，墙上挂着一副巨大的自画像。
LONG);
  set("no_fight", 1);
  set("no_magic", 1);
  set("if_bed", 1);

  set("sleep_room", 1);
  set("exits", ([ /* sizeof() == 4 */
    "southdown": "/u/forsky/flowerroom",
  ]));
  setup();
  replace_program(ROOM);
}
