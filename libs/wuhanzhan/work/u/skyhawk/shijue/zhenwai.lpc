#include <room.h>
#include <ansi.h>
inherit ROOM;
void create() {
  set("short", HIY "天雷阵外" NOR);
  set("long", @LONG

地上好象有什么东西。

LONG);
  set("exits", ([ /* sizeof() == 1 */
    "up": "/u/skyhawk/shijue/gate2",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/u/skyhawk/obj/leizhu": 1,
  ]));
  set("no_clean_up", 1);
  setup();
}
