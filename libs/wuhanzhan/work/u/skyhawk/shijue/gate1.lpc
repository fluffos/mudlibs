inherit ROOM;

void create ()
{
  set ("short", "空地");   
  set ("long", @LONG   
总算出了十绝阵，你眼前一亮，居然有一块空地，上面站着个人。
LONG);

    set("exits", ([ 
               "south" : __DIR__"gate",
               "north" : __DIR__"gate2",
                 ]));
    set("objects",([
                    __DIR__"npc/ystz" : 1,
                 ]));
  set("outdoors", __DIR__);
    setup();
}
