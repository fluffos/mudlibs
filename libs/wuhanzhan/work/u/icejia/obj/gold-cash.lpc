
#include <ansi.h>

inherit MONEY;

void create()
{
        set_name( HIY "一百两金票" NOR, ({"gold-cash", "gold-cash_money", "cash"}));
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("money_id", "thousand-cash");
                set("long", "一张面额值一百两金子的银票。\n");
		set("unit", "叠");
                set("base_value", 1000000);
		set("base_unit", "张");
		set("base_weight", 5);
	}
	set_amount(1);
}

