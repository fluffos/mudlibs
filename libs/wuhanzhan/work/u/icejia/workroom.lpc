// Room: /u/cici/workroom.c
inherit ROOM;

void create() {
  set("short", "血洒舞池");
  set("long", @LONG
这是一间什么也没有的空房间。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "up": "/u/icejia/home.lpc",
    //  "wiz" : "/d/wiz/wizroom.lpc",
    //  "down" : "/d/city/kezhan.lpc",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
