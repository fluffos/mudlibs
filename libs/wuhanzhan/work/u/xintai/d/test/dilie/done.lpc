#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIW "阵外" NOR);
  set("long", @LONG
终于出了地烈阵，你长吁了一口气，往东就是下一阵的入口了，在这里调整一下状态吧。
LONG);
  set("exits", ([ /* sizeof() == 1 */
    "east": "/u/xintai/d/test/fenghou/zhenkou.lpc",
  ]));
  setup();
}
