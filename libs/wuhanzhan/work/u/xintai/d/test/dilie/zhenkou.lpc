#include <ansi.h>
inherit ROOM;
void create() {
  set("short", HIG "阵口" NOR);
  set("long", @LONG
这里就是地烈阵的入口。
LONG);
  set("exits/west", "/u/xintai/d/test/tianjue/done");
  set("exits/east", __DIR__ "dilie" + (string)random(16));
  setup();
}
