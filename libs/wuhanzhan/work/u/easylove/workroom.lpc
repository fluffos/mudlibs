#include <ansi.h>
inherit ROOM;

void create() {
  set("short", BLINK + HIB "休息室" NOR);
  set("long", @LONG

  这里是简爱工作学习修建的工作室

LONG);
  set("exits",
    ([  //sizeof() == 2
    "wiz": "/d/wiz/wizroom",
      "kz": "/d/city/kezhan",

    ]));
  set("objects",
    ([
    ]));

  set("outdoors", "1");
  set("no_time", 1);
  set("if_bed", 1);
  set("sleep_room", 1);
  set("no_upgrade_age", 1);
  set("valid_startroom", 1);
  set("valid_startroom", 1);
  setup();
}
