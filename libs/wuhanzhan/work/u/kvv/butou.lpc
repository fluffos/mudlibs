// xunbu.c
int waiting(object me,object dest);
int checking(object me, object dest);
int do_hit(object me, object dest);
string ask_me(object me);
nosave int time;
int count;

void create()
{
        set_name("巡捕", ({ "xunbu" }) );
        set("title", "长安差役" );
        set("gender", "男性" );
        set("shen_type", 1);
        set("age", 30);
        set("str", 30);        set("con", 30);        set("int", 30);== 未完继续 8% == (ENTER 继续下一页，q 离开，b 前一页)
        set("str", 30);
        set("con", 30);
        set("int", 30);
        set("dex", 30);
        set("no_clean_up",0);

        set("long",
