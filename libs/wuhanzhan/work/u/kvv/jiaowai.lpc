inherit ROOM;

void create()
{
    set("short", "小树林");
    set("long", @LONG;这是扬州郊外的一片小树林，落英缤纷，蜂歌蝶舞，朝露夕光，沁人;心脾。呼朋唤友开怀欢乐正其时。周围小树颗颗健美，上有无数的小鸟小;雀在树梢上欢歌，一点也不怕人。这正是个打鸟（da）的好机会。;LONG );
    set("no_clean_up", 0);
    set_temp("wayao1",1);
    set_temp("wayao1",1);
    set("outdoors", "yangzhou");
    setup();
}

void init()
{
        add_action("do_da", "da");
}


int do_da()
{
        object me;
        int i, kar, cps, bb,mm;
        me  this_player();
