inherit ROOM;

void create() {
  set("short", "后院");
  set("long", @LONG

这里是一个后院，从布局来看很巧妙，但是
从这里你可以看出来，这里的男主人和
女主人生活是很清苦的。墙角已经剥落了。
LONG);

  set("exits", ([ /* sizeof() == 3 */
    "north": "/u/rao/houtang",
    "east": "/u/rao/island2",
    "south": "/u/rao/stand",
  ]));

  setup();
}
