inherit ROOM;

void create() {
  set("short", "小径");
  set("long", @LONG

这是一条清幽的小路，不知通往何处。路边盛开着不知名的小花，在风的撩动中，醉人的舞动。
LONG);

  set("objects", ([ /* sizeof() == 1 */
  ]));
  set("exits", ([ /* sizeof() == 2 */
    //  "xx" : "/u/vir/migong/xxx",
  ]));
  set("outdoors", 1);

  setup();
}
