// Room: /u/lxinxin/workroom.c by lxinxin 4.9

inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIW "心心的睡房" NOR);
  set("long", @LONG
没有什么话好说,最近家里很忙!!
LONG);
  set("sleep_room", 1);
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "kz": "/d/city/kezhan",
    "wiz": "/d/wiz/wizroom",
  ]));

  setup();
  replace_program(ROOM);
}
