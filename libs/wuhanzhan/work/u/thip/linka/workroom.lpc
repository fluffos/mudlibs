inherit ROOM;
#include <ansi.h>

void create ()
{
  set ("short", HIG"草棚"NOR);
  set ("long", @LONG

一个破草棚，林卡的家。
LONG);

  set("objects", ([ /* sizeof() == 1 */
]));
  set("exits", ([ /* sizeof() == 2 */
  "kz" : "/d/city/kezhan",
  "wr" : "/d/wiz/wizroom",
]));
  set("valid_startroom", 1);

  setup();
}
