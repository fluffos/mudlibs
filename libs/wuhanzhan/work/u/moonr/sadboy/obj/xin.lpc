#include <ansi.h>

inherit ITEM;

void create() {
  set_name("蜘蛛女王的心", ({ "spiderheart" }));
  set("long",
    HIM "这是一颗血淋淋的蜘蛛女王的心，请不要随意丢，会不见的！\n" NOR);
  set_weight(500);
  set("unit", "颗");
  set("value", 0);
}
