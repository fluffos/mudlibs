#include <ansi.h>
#include <room.h>
inherit ROOM;
void create()
{
     set("short","足球俱乐部");
     set("long","重温那段令人怀念的历史，我们不能忘记为自己的尊严和地位（以及前途和MM）\n\n\n");
  set("exits", ([ /* sizeof() == 3 */
                "kz":"/d/city/kezhan",
                "south":"/u/ken/ken/officer",
                "north":"/d/wiz/wizroom"
]));
set("objects",([ /* sizeof() == 2 */
]));
set("valid_exercise",1);
set("valid_sleep",1);
 setup();
      replace_program(ROOM);
}
