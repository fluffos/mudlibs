#include <ansi.h>
inherit ROOM;
void create()
{
       set("short",HIW "潜龙勿用"NOR);
       set("long", "
一间新建的房子,来的匆忙,空荡荡的
什么都没有。

");
  set("exits", ([ /* sizeof() == 1 */
  "out" : "/d/city/kezhan",
]));
        set("valid_startroom", 1);
        setup();
}
