inherit ROOM; 

void create() 
{
	set("short", "闺房"); 
	set("long",@LONG
这是戚芳的闺房。
LONG);
       
        set("exits",([
        "north" : __DIR__"lianwuchang",
        ]));
		set("objects", ([
			__DIR__"npc/qifang": 1,
			]));
        setup(); 
} 
