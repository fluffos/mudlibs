//mac's houshan.c
#include <room.h>

inherit ROOM;

void create() {
  set("short", "後山平地");
  set("long", @LONG
这里是一块开阔平地，在从山合抱之中，四面松涛环绕，是寺
中众僧健身习武的地方。地上放了许多练功用的器械。
LONG
  );
  set("exits", ([
    "southwest": __DIR__ "chanfang.lpc",
    "southeast": __DIR__ "chanfang2.lpc",
  ]));
  set("objects", ([
    __DIR__ "obj/bigstone": 1,
  ]));
  set("outdoors", "qianfo");
  set("coor/x", 50);
  set("coor/y", 2110);
  set("coor/z", 30);
  setup();
}
