mixed reset() { }

mixed short() { return "One exit: inn"; }

mixed init() {
  add_action("move_out", "inn");
}

mixed move_out() {
  write("Moving..\n");
  this_player()->move_object("/world/city/inn");
  return 1;
}
