mixed short() { return "A shimmering ball of pure white light"; }

mixed long(mixed str) {
        write(  "You see a shimmering ball of pure white light. It shines so\n"+
                "brightly that it burns your eyes. It feels warm and goodness\n"+
                "seems to be radiating from it.\n");
        return;
}

mixed id(mixed str) { return str == "ball" || str == "soul123" || str == "soul"; }

mixed get() {
    return 1;
}

mixed query_weight() {
  return 10;
}
