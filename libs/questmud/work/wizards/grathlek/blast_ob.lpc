mixed id(mixed str) { if (str == "floor") return 1; }

mixed long() { write("the floor"); return 1; }

mixed hit_with_spell(mixed dam, mixed type) {
  if (type == 1) { /* Eli fire dama */
	write("Jees");
	return 1;
  }
}

mixed reset(mixed arg) {
  enable_commands();
}
