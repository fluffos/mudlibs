//To prevent abuse

mapping uses;

mixed reset(mixed arg) {
	if(arg) return;
	uses = ([]);
}

mixed query_use(mixed str) {
	return uses[str];
}

mixed add_use(mixed str) {
	if(!uses[str]) {
		uses += ([ (string)str: (int)1]);
		return 1;
	} else {
		uses[str]++;
	}
	return 1;
}
