inherit "room/room";

mixed reset(mixed arg) {

  if(arg) return;

  add_exit("out", "/wizards/kaappi/drow/vmap/castle1_entrance.lpc");
  add_exit("north", "castle1_2.lpc");

  short_desc = "Castle1";
  long_desc = "A hallway.\n";
              
  set_light(1);
}
