mixed long(mixed str) {
write("You look the bushes closer and discover some hole where you might enter.\n");
return;
}
mixed id(mixed str) { return str == "bushes"; }
mixed get(mixed str) {
 if (str == "bushes") {
 write("You can't take the bushes.\n");
 return 0;
}
return 0;
}     
