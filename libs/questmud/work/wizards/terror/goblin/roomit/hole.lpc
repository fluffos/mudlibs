mixed long(mixed str) {
write("As you look the hole closer you discover some tunel where you could enter.\n");
return;
}
mixed id(mixed str) { return str == "hole"; }
mixed get(mixed str) {
 if (str == "bushes") {
 write("You can't take the hole.\n");
 return 0;
}
return 0;
} 
