#include "room.h"
 
#undef EXTRA_RESET
#define EXTRA_RESET\
        extra_reset();
 
object monster;
 
mixed extra_reset() {
    set_not_out(3);
 
    if (!monster) {
        monster = clone_object("/wizards/walla/area/monsters/secretary");
        monster->move_object(this_object());
    }
}
 
TWO_EXIT("wizards/walla/jericho/rooms/ispep-1", "north",
         "wizards/walla/jericho/rooms/mayor2", "south",
         "Inside the mayor's office",
         "You are inside the newly build mayor's office. The walls are made\n" +
         "of newly cut chalkstone and lights flicker in all the corners. \n", 3)
