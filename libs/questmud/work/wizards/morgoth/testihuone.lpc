#include "room.h"
 
#undef EXTRA_RESET
#define EXTRA_RESET\
    extra_reset();
 
object monsu;
int bucket_found;
 
mixed extra_reset() {
    if (!monsu) {
        monsu = clone_object("/wizards/morgoth/can.lpc");
        monsu->move_object(this_object());
    }
 
ONE_EXIT("world/city/inn", "inn",
"Example room",
"This is an example room. There is something in the water. There is a 
sign.\n", 3)
 
 
 
