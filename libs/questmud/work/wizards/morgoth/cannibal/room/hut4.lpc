#include "room.h"
 
#undef EXTRA_RESET
#define EXTRA_RESET\
    extra_reset();
 
object monsu;
int bucket_found;
 
mixed extra_reset() {
    if (!monsu) {
        monsu = clone_object("/wizards/morgoth/cannibal/npc/cannibal.lpc");
        monsu->move_object(this_object());
    }
}
 
ONE_EXIT("/wizards/morgoth/cannibal/room/path26.lpc", "out",
         "Hut.",
         "This is normal cannibal hut.\n" +
         "This place is really messy.\n", 1)
