inherit "room/room";
object rb1, rb2;
mixed reset (mixed arg) {
if(!present("rabbit 2", this_object())) clone_object("/wizards/neophyte/forest/monst/rabbit")->move_object(this_object());
if(!present("rabbit 2", this_object())) clone_object("/wizards/neophyte/forest/monst/rabbit")->move_object(this_object());
    if(arg) return;
        short_desc = "A field";
        long_desc = read_file("/wizards/neophyte/forest/fieldesc.lpc");
    add_exit("north","/wizards/neophyte/forest/field5");
    add_exit("west","/wizards/neophyte/forest/field3");
    add_exit("east","/wizards/neophyte/forest/field9");
            items = allocate(3);
                    items[0] = "hay";
                    items[1] = "It looks rather old because they are all brown,";

}
