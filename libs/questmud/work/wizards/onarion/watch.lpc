mixed short() { return "A clock showing the time"; }

mixed long() {
    write("It shows:\n");
    write(ctime(time()) + ".\n");
}

mixed get() { return 1; }

mixed id(mixed str) { return str == "watch" || str == "clock"; }
