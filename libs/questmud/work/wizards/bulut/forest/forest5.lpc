#include "room.h"

object wolf;

/*
init() {
    ::init();
add_action("search", "search");
       }
*/

/* search(str) { */
/* if (str=="bushes") { */
/*      write("You discover a secret passage!\n"); */
/*      write(".. and sneak through the bush.\n"); */
/*      this_player()->move_object("/wizards/bulut/forest/beasts.lpc"); */

/* return 1; */
/*                } */
/*            } */

mixed extra_reset() {
        if (!wolf || !living(wolf)) {
wolf = clone_object("/wizards/bulut/forest/wolf.lpc");
        wolf->move_object(this_object());

                                    }
              }




#undef EXTRA_RESET
#define EXTRA_RESET\
        extra_reset();


TWO_EXIT("wizards/bulut/forest/forest6","east",
"wizards/bulut/forest/forest4","south",
"In the forest",
/* "Light is blinking through the northern bushes\n" + */
"Path continues to east and south from here.\n", 1)
