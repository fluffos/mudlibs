#define PATH "/cmds/emotes/"

void init() {
  add_action("pnp","",1);
}

int pnp() {
  string str;
  str = lower_case(query_verb());
  if(file_size(PATH+str+".lpc") < 10) return 0;
  call_other(PATH+str+".lpc","cmd_"+str);
  return 1;
}
