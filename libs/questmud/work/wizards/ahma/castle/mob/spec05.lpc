inherit "obj/monster";

mixed id(mixed str) { return str == "spec"; }

mixed reset(mixed arg) {
  ::reset(arg);
  if(arg) { return; }
  set_block_dir("west");
  set_block_message("You cannot continue further due to the collapsed ceiling.");
}
