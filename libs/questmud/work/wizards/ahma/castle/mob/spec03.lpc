inherit "obj/monster";
string tmp;

mixed id(mixed str) { return str == "wall"; }

mixed catch_tell(mixed str) {
  if(sscanf(str,"%s breaks the wall to the north",tmp) == 1) {
    destruct(this_object());
    return 1;
  }
}