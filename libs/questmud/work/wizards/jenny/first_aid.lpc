mixed get_time() {
    return 1;
}
mixed ep_cost() { return 1; }
mixed query_type() { return "neutral"; }

mixed resolve_skill(mixed target) {
    if(target->query_bleeding()) {
        target->end_bleeding();
}
 }
