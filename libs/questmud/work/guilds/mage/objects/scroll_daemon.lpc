/* Scroll makers can only have max 10 scroll */

mapping makers;
mixed reset(mixed arg) {
	if(arg) return;
	makers = ([ ]);
}

mapping query_makers() {
	return makers;
}

mixed query_maker(string str) {
	return makers[str];
}

mixed add_maker(string str) {
	makers[str] += 1;
}

mixed reduce_maker(string str) {
	makers[str] -= 1;
}
