object user;

mixed id(mixed str) { return str == "first_aid_object"; }
mixed drop() { return 1; }

mixed start(mixed ob) {
  user = ob;
  call_out("end_fa", 300);
}
mixed end_fa() {
 if(user) {
 tell_object(user, "You feel you can be aided again.\n");
}
destruct(this_object());
return 1;
}