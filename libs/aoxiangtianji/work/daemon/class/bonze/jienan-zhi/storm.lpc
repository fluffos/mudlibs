#include <ansi.h>
#include <combat.h>
#define HUO "「" HIR "暴风骤雨" NOR "」"
inherit F_SSERVER;
int valid_pfm(object me, object target)
{
  if(userp(me) && (string)me->query("family/family_name") != "南海普陀山")
    return notify_fail(HUO"是南海普陀山不传之密！\n");
  if (me->is_busy())
    return notify_fail("你正忙着呢！\n");

  if (me->query_skill("jienan-zhi", 1) < 150)
    return notify_fail("你的劫难指修为不够，难以施展" HUO "。\n");
  if (me->query_skill("lotusforce", 1) < 150)
    return notify_fail("你的莲花心法修为不够，难以施展" HUO "。\n");
  if (me->query_skill("buddhism", 1) < 150)
    return notify_fail("你的大乘佛法修为不够，难以施展" HUO "。\n");
  if (me->query_skill_mapped("unarmed") != "jienan-zhi")
    return notify_fail("你没有激发劫难指，难以施展" HUO "。\n");
  if (me->query("force") < 500)
    return notify_fail("你的真气不够，难以施展" HUO "。\n");
  if ((int)me->query("max_force") < 1500)
    return notify_fail("你的内力修为不够，难以施展" HUO "。\n");
  return 1;
}

int perform(object me, object target)
{
  string msg;
  int i;
  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail(HUO "只能空手使用。\n");
  msg = HIC "\n$N" HIC"口中大喝了一声，施展出" + HUO + HIC"，双手十指上下翻飞，"
        "身形前后左右移动，\n$n" HIC"只觉得到处是$N" HIC"的影子，根本不知如何招架，"
        "更不用说躲闪了！\n\n"NOR;
  message_vision(msg, me, target);
  me->add("force", -60);
  for (i = 1; i <=3 ; i++)
  {
    if (!living(target) || !me->is_fighting(target))
      break;
    if (random(2) && !target->is_busy())
      target->start_busy(1);
    COMBAT_D->do_attack(me, target, 0);
  }

  me->start_busy(1 + random(3));
  return 1;
}

