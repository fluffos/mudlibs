#include <ansi.h>
#include <combat.h>
#define PILI "「" HIR "霹雳三打" NOR "」"
inherit F_SSERVER;
int valid_pfm(object me, object target)
{
  if(userp(me) && (string)me->query("family/family_name") != "方寸山三星洞")
    return notify_fail(PILI"是方寸山三星洞不传之密！\n");
  if (me->is_busy())
    return notify_fail("你正忙着呢！\n");

  if (me->query_skill("qitian-stick", 1) < 100)
    return notify_fail("你的齐天棍修为不够，难以施展" PILI "。\n");
  if (me->query_skill_mapped("stick") != "qitian-stick")
    return notify_fail("你没有激发齐天棍，难以施展" PILI "。\n");
  if (me->query("force") < 200)
    return notify_fail("你的真气不够，难以施展" PILI "。\n");
  return 1;
}

int perform(object me, object target)
{
  object weapon;
  string msg;
  int i;

  if (! objectp(weapon = me->query_temp("weapon")) ||
      (string)weapon->query("skill_type") != "stick")
    return notify_fail("你使用的武器不对，难以施展" PILI "。\n");
  msg = HIY "$N"HIY"一个高跳跃至半空，手中" + weapon->name()
          + "霹雳间连攻三招，正是方寸山绝技" + PILI + HIY"，棒影重重砸往$n！\n" NOR;

  message_combatd(msg, me, target);
  for (i=6;i<=8;i++)
  {
    if (!living(target) || environment(me) != environment(target))
      break;
    me->set_temp("QTS_perform",i);
    if (!target->is_busy() && (random(2)))
      target->start_busy(2);
    COMBAT_D->do_attack(me, target, weapon,3);
    me->add("force",-20);
  }

  me->delete_temp("QTS_perform");
  me->start_busy(1+random(3));
  return 1;
}