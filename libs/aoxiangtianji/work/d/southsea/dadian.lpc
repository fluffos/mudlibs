inherit BIGROOM;
inherit WATERROOM;
void create ()
{
  set ("short", "大殿");
  set ("long", @LONG
这里是南海龙王所在的大殿了。前面一张文案后，端坐着南海龙王
敖钦。左右两边，摆放着一些锦绣桌椅，看来是招待客人的。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "northdown"   : __DIR__"yujie3",
  ]));

  set("objects", ([
    "/d/eastsea/npc/aoqin" : 1,
  ]));

  setup();
}