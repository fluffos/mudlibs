inherit BIGROOM;
inherit LANDROOM;
inherit TAOISTROOM;
void create()
{
  set ("short", "掌门室");
  set ("long", @LONG

这里是方寸的掌门房间，房间内陈设比较简单。一个道士装束的
人站立在中间。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "down": __DIR__"inside2",
  ]));
  set("objects", ([
    "/obj/npc/zhangmen/fangcun" : 1,
  ]) );

  set("no_clean_up", 0);
  set("no_npc",1);
  set("zm_limit",1);
  setup();
}

