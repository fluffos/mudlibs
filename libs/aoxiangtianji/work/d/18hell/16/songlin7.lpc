#include <ansi.h>
inherit __DIR__"songlin";
void create ()
{
  set ("short", "松树林");
  set ("long", @LONG

松林拣松涛阵阵，山风及体生寒，松树间相互间隔颇远，但这些
松树显然都有了很多的年头了，浓荫把日光都遮蔽了。

LONG);
  set("exits",
  ([
     "south" : sprintf(__DIR__"songlin%d",1+random(8)),
     "west" : sprintf(__DIR__"songlin%d",9+random(4)),
     "north" : sprintf(__DIR__"songlin%d",9+random(4)),
     "east" : __DIR__"songlin8",
  ]));
  set("number",7);
  setup();
}

