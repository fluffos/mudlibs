#include <ansi.h>
inherit SMALLROOM;
inherit MUDDYROOM;
void create ()
{
  set ("short", "小路");
  set ("long", @LONG

一条泥泞的小路。继续往东而行，可以看到一排排的
黑色房屋。那里应该是火山地狱的鬼魂们聚集的所在
地了。

LONG);
  set("exits",
  ([
     "west" : __DIR__"xiaolu3",
     "east"  : __DIR__"suishi21",
  ]));
  set("objects",
  ([
      __DIR__"npc/heimiangui" : 1,
  ]));
  setup();
}

