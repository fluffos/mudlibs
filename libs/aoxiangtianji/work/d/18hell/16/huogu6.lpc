#include <ansi.h>
inherit __DIR__"huogu";
void create ()
{
  set ("short", "火谷");
  set ("long", @LONG

峡谷内十分炎热，散布的石头都是通红的，到处都有一股股的
黑烟冒出，不时有火从地上直窜了出来。

LONG);
  set("exits",
  ([
     "east" : __DIR__"shanlu5",
     "west" : __DIR__"huogu5",
     "south" : __DIR__"huogu9",
     "north" : __DIR__"huogu3",
  ]));
  setup();
}

int valid_leave(object me, string dir)
{
  if (dir == "east" && !playerp(me))
    return 0;
  return ::valid_leave(me, dir);
}

