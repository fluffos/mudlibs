#include <ansi.h>
inherit __DIR__"songlin";
void create ()
{
  set ("short", "松树林");
  set ("long", @LONG

以走入这片松树林，发现这松林几乎看不到什么明显的出路。
未有南面有条东西横贯的小山路。松林拣松涛阵阵，山风及
体生寒，松树间相互间隔颇远，但这些松树显然都有了很多
的年头了，浓荫把日光都遮蔽了。

LONG);
  set("exits",
  ([
     "southup" : __DIR__"shanlin3",
     "west" : __DIR__"songlin2",
     "north" : sprintf(__DIR__"songlin%d",1+random(4)),
     "east" : sprintf(__DIR__"songlin%d",1+random(4)),
  ]));
  set("number",1);
  setup();
}

