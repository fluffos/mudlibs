#include <ansi.h>
inherit SMALLROOM;
inherit MUDDYROOM;
void create ()
{
  set ("short", "小路");
  set ("long", @LONG

这条小路到了这里，西北和北面都是崎岖的山路了。
继续往东行，泥泞的小路不知道通往何方。

LONG);
  set("exits",
  ([
     "northwest" : __DIR__"shanlin4",
     "north" : __DIR__"shanlu1",
     "east"  : __DIR__"xiaolu2",
  ]));
  setup();
}

