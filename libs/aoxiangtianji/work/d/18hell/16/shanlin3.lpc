#include <ansi.h>
inherit SMALLROOM;
inherit WOODROOM;
void create ()
{
  set ("short", "山林");
  set ("long", @LONG

山林的小路曲折难行。小路在这里分叉了，往东继续
下山，往西翻过这片山林，是刀锯地狱的所在。北面
的下山路上，松树茂密，显然是片松树林。

LONG);
  set("exits",
  ([
     "eastdown" : __DIR__"shanlin4",
     "westup" : __DIR__"shanlin2",
     "northdown" : __DIR__"songlin1",
  ]));
  setup();
}

