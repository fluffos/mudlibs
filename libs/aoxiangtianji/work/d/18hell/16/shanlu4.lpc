#include <ansi.h>
inherit SMALLROOM;
inherit MUDDYROOM;
void create ()
{
  set ("short", "山路");
  set ("long", @LONG

越来越热了。往前看，一股股的黑烟在远处天空飘
散。地上的花草已经很少了，泥土渐渐的为岩石所
替代。

LONG);
  set("exits",
  ([
     "south" : __DIR__"shanlu3",
     "northup" : __DIR__"shanlu5",
  ]));
  setup();
}

