#include <ansi.h>
inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "青石路");
  set ("long", @LONG

这是村东的青石路。不时有些面目不清，或身形模糊
的鬼魂在身边经过。
LONG);
  set("exits",
  ([
     "west" : __DIR__"e-qingshi1",
  ]));
  set("objects",
  ([
      __DIR__"npc/daojugui" : 1,
  ]));
  setup();
}

