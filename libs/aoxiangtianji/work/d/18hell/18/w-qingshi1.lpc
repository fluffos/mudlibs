#include <ansi.h>
inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "青石路");
  set ("long", @LONG

这是村西的青石路。不时有些面目不清，或身形模糊
的鬼魂在身边经过。
LONG);
  set("exits",
  ([
     "east" : __DIR__"center",
     "west" :  __DIR__"w-qingshi2",
  ]));
  set("objects",
  ([
      __DIR__"npc/daojugui" : 1,
  ]));
  setup();
}

