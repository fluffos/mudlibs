#include <ansi.h>
inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "青石路");
  set ("long", @LONG

这青石路并不长，转眼就到头了。往西看去，荒草一片，
阴风阵阵。北面有间小屋，静悄悄的，仔细看去，似乎象个
书馆。
LONG);
  set("exits",
  ([
     "east" : __DIR__"w-qingshi1",
     "west" :  __DIR__"huangcao",
     "north" : __DIR__"shuguan",
  ]));
  setup();
}

