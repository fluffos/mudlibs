#include <ansi.h>
inherit NPC;
string *name = ({
  "刀锯鬼",
  "偷工鬼",
  "减料鬼",
  "欺上鬼",
  "瞒下鬼",
});

void create()
{
  int i = random(sizeof(name));
  set_name(name[i],({"ghost","gui"}));
  if (random(2))
    set("gender", "男性" );
  else
    set("gender", "女性" );
  set("age",100 + random(100));
  set("long","这是个影影绰绰的鬼，从头开始，一道裂痕把身体分成了两份，看起来有点吓人。\n");
  set("combat_exp",3000000 + random(2000000));
  set("daoxing",3000000 + random(2000000));
  set("con",30 + random(20));
  set("spi",30 + random(20));
  set("dex",30 + random(20));
  set("str",30 + random(20));
  set("int",30 + random(20));
  set("cor",30 + random(20));
  set("cps",30 + random(20));
  set("kar",30 + random(20));
  set("per",30 + random(20));
  set("attitude", "peaful");
  set("max_kee",5000);
  set("max_sen",5000);
  set("max_force",3500);
  set("force",7000);
  set("max_mana",3500);
  set("mana",7000);
  set_skill("force",280+random(100));
  set_skill("spells",280+random(100));
  set_skill("dodge",280 + random(100));
  set_skill("unarmed",280+random(100));
  set_skill("parry",280+random(100));

  set_skill("tonsillit",280+random(100));
  set_skill("gouhunshu",280+random(100));
  set_skill("ghost-steps",280 + random(100));
  set_skill("jinghun-zhang",280+random(100));

  map_skill("force","tonsillit");
  map_skill("spells","gouhunshu");
  map_skill("dodge","ghost-steps");
  map_skill("unarmed","jinghun-zhang");

  set("chat_chance", 40);
  set("chat_msg", ({
     CYN + name[i] + "道：前世做了坏事，现在就要受苦！唉！！\n" NOR,
     HIW + name[i] + "叹道：所以做人要做好人啊，否则拿刀劈你，分做两份，唉！\n" NOR,
     (: random_move :),
  }) );
  set("chat_chance_combat",20);
  set("chat_msg_combat", ({
     (: cast_spell, "gouhun" :),
  }) );
  setup();
  carry_object("/obj/cloth/cloth")->wear();
}

void init ()
{
  object ob = this_player();
  ::init();
  if( playerp(ob) && !is_fighting() )
  {
    remove_call_out("greeting");
    call_out("greeting", 1, ob);
  }

}

void greeting(object ob)
{
  if( !ob || environment(ob) != environment() )
    return;

  command("say 你是新来的吗？怎么身上有股阳气？莫非还没有死透？\n");
  command("say 等你被刀锯了，就知道做坏事的下场了！呜呜，痛啊！\n");
  message_vision("$N一裂嘴，身体仿佛分开了一般，裂成了两半，把$n吓了一跳！\n",this_object(),ob);
}

