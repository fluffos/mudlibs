#include <ansi.h>
inherit SMALLROOM;
inherit WOODROOM;
void create ()
{
  set ("short", "树林");
  set ("long", @LONG

进入了这片树林，顿时没有了方向。这里看来桃树比较多，断落
的树枝散落在地上，踩上去“吱吱”作响。这里拿把斧头，看看桃柴还
不错。
LONG);
  set("exits",
  ([
     "east" : sprintf(__DIR__"shulin%d",13+random(6)),
     "east" : sprintf(__DIR__"shulin%d",13+random(6)),
     "south" : __DIR__"shulin16",
     "north" : sprintf(__DIR__"shulin%d",13+random(6)),
  ]));
  set("resource/chai","桃柴");
  setup();
}

