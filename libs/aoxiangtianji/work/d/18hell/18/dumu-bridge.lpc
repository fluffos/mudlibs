#include <ansi.h>
inherit BIGROOM;
inherit WATERROOM;
void create ()
{
  set ("short", "独木桥");
  set ("long", @LONG

眼前突然出现了一座独木桥，桥身不宽，桥下看去，
黑色的浓雾看不清，却可以听见水流动的声音。过了这桥，
就是真正的地狱所在了。在桥头，一个石头上赫然刻有：
  刀锯地狱
几个血红的字样。
LONG);
  set("exits",
  ([
     "west" : __DIR__"pingyang",
     "east" : __DIR__"nining1",
  ]));
  setup();
}

