#include <ansi.h>
inherit SMALLROOM;
inherit SANDROOM;
void create ()
{
  set ("short", "碎石路");
  set ("long", @LONG

一条碎石小路，往北是个小村的中心了，继续向南而下。
眼看就出了小村了。
LONG);
  set("exits",
  ([
     "north" : __DIR__"center",
     "south" : __DIR__"suishi5",
  ]));
  set("objects",
  ([
      __DIR__"npc/ghost2" : 1,
  ]));
  setup();
}

