#include <ansi.h>
inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "小村中心");
  set ("long", @LONG

这是小村的中心，很宽敞，地面整理的也非常干净。
这里显然是各个方向的集汇点，交通四通八达。
LONG);
  set("exits",
  ([
     "north" : __DIR__"suishi3",
     "east" : __DIR__"e-qingshi1",
     "south" : __DIR__"suishi4",
     "west" :  __DIR__"w-qingshi1",
  ]));
  set("objects",
  ([
      __DIR__"npc/daojugui" : 1,
  ]));
  setup();
}

