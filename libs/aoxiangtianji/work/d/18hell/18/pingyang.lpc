#include <ansi.h>
inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "平阳路");
  set ("long", @LONG

一条宽阔的大路，路面似乎是白色的。四周看去，黑雾
弥漫，除了这条白色的大路，孤零零的。
LONG);
  set("exits",
  ([
     "west" : __DIR__"entrance",
     "east" : __DIR__"dumu-bridge",
  ]));
  setup();
}

