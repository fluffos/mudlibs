#include <ansi.h>
inherit SMALLROOM;
inherit HILLROOM;
void create ()
{
  set ("short", "山路");
  set ("long", @LONG

慢慢的走入了山的深处，山间寂静，山泉叮咚。继续往山里
去，就要步入另外一个地狱了。在路边，不知何人何时竖立了
一个木牌：τ
$HIW$大鬼小鬼禁行：$NOR$τ
$HIW$刀锯地狱$NOR$：$HIM$南$NOR$行－－$HIR$火山地狱$NOR$τ
$HIR$火山地狱$NOR$：$HIM$北$NOR$往－－$HIW$刀锯地狱$NOR$τ

LONG);

  set("exits",
  ([
     "northwest" : __DIR__"shanlu4",
     "south" : "/d/18hell/16/shanlu6",
  ]));
  setup();
}

int valid_leave(object me, string dir)
{
  if (dir == "south" && !playerp(me))
    return 0;
  return ::valid_leave(me, dir);
}

