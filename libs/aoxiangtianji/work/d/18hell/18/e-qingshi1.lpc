#include <ansi.h>
inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "青石路");
  set ("long", @LONG

这是村东的青石路。不时有些面目不清，或身形模糊
的鬼魂在身边经过。
LONG);
  set("exits",
  ([
     "west" : __DIR__"center",
     "east" :  __DIR__"e-qingshi2",
  ]));
  set("objects",
  ([
      __DIR__"npc/ghost1" : 1,
  ]));
  setup();
}

