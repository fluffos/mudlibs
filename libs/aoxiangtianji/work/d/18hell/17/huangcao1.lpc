#include <ansi.h>
inherit BIGROOM;
inherit WOODROOM;
void create ()
{
  set ("short", "荒草地");
  set ("long", @LONG

一片枯黄的荒草地了。据说这里有恶鬼出没，这些恶鬼因为前世
作孽太多，到了地狱，接受的惩罚也最严厉，同时也无法在群鬼
中找到真正的朋友，也只能在平时躲藏在这荒草地。避免和恰鬼
的接触。但是该受的刑法确实怎么也逃脱不了的。

LONG);
  set("exits",
  ([
     "west" : sprintf(__DIR__"huangcao%d",1+random(4)),
     "east" : __DIR__"w-shiban3",
     "south" : __DIR__"huangcao2",
     "north" : sprintf(__DIR__"huangcao%d",1+random(4)),
  ]));
  setup();
}

