#include <ansi.h>
inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "小屋");
  set ("long", @LONG

一间阴森森的小屋。一走进来就仿佛看到有无数的鬼影子
在晃动。凝神细听，似乎有无数的哭号声音，隐隐约约，
并不真切。

LONG);
  set("exits",
  ([
     "south" : __DIR__"w-shiban1",
  ]));
  setup();
}

