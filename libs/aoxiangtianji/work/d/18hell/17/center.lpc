#include <ansi.h>
inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "镇中心");
  set ("long", @LONG

这里是个小镇的中心，显得颇为宽敞。这里影影绰绰的有些
鬼在来来往往。

LONG);
  set("exits",
  ([
     "north" : __DIR__"tulu2",
     "west" : __DIR__"w-shiban1",
     "east" : __DIR__"e-shiban1",
     "south" : __DIR__"s-shiban1",
  ]));
  set("objects",
  ([
      __DIR__"npc/shimogui" : 1,
  ]));
  setup();
}

