#include <ansi.h>
inherit NPC;
void create()
{
  set_name(HIM"绿毛恶鬼"NOR,({"lvmao e gui","e gui","gui", "ghost"}));
  set("gender", "男性" );
  set("age",100 + random(100));
  set("long","这是个气势汹汹的鬼，前世冤孽太重，只得躲在这山林里。\n");
  set("combat_exp",3000000 + random(2000000));
  set("daoxing",3000000 + random(2000000));
  set("con",30 + random(20));
  set("spi",30 + random(20));
  set("dex",30 + random(20));
  set("str",30 + random(20));
  set("int",30 + random(20));
  set("cor",30 + random(20));
  set("cps",30 + random(20));
  set("kar",30 + random(20));
  set("per",30 + random(20));
  set("attitude", "peaful");
  set("max_kee",5000);
  set("max_sen",5000);
  set("max_force",4200);
  set("force",8000);
  set("max_mana",4200);
  set("mana",8000);
  set("force_factor",200);
  set("mana_factor",200);
  set_skill("force",280+random(100));
  set_skill("spells",280+random(100));
  set_skill("dodge",280 + random(100));
  set_skill("unarmed",280+random(100));
  set_skill("parry",280+random(100));
  set_skill("whip",280+random(100));

  set_skill("tonsillit",280+random(100));
  set_skill("gouhunshu",280+random(100));
  set_skill("ghost-steps",280 + random(100));
  set_skill("jinghun-zhang",280+random(100));
  set_skill("hellfire-whip",280+random(100));

  map_skill("force","tonsillit");
  map_skill("spells","gouhunshu");
  map_skill("dodge","ghost-steps");
  map_skill("unarmed","jinghun-zhang");
  map_skill("whip","hellfire-whip");
  map_skill("parry","hellfire-whip");
  set("chat_chance", 40);
  set("chat_msg", ({
     HIY"绿毛鬼走了过来，恶狠狠的看着你！\n"NOR,
     HIY"绿毛鬼裂嘴一笑，好象要活吞了你似的！\n"NOR,
     (: random_move :),
  }) );

  set("chat_chance_combat",20);
  set("chat_msg_combat", ({
     (: perform_action, "whip.biluo" :),
     (: perform_action, "whip.duohun" :),
     (: perform_action, "whip.three" :),
     (: cast_spell, "gouhun" :),
     (: cast_spell, "ldlh" :),
  }) );
  setup();
  carry_object("/obj/cloth/cloth")->wear();
  carry_object("/d/obj/weapon/whip/tielian")->wield();
}

void init ()
{
  object ob = this_player();
  ::init();
  if( playerp(ob) && !is_fighting() )
  {
    remove_call_out("greeting");
    call_out("greeting", 1, ob);
  }

}

void greeting(object ob)
{
  if( !ob || environment(ob) != environment() )
    return;

  if (!ob->query_temp("hell-17/daozeigui1"))
  {
    command("grin " + ob->query("id"));
    message_vision ("$N道：送上门来了，识相的，把值钱的留下，快滚！\n",this_object());
    command("sing");
  }

  else
  {
    message_vision ("$N道：哈哈，居然找上了门。。。\n",this_object());
  }

  this_object()->kill_ob(ob);
}

int accept_fight (object ob)
{
  kill_ob (ob);
  return 1;
}

void kill_ob (object ob)
{
  set_temp("no_return",1);
  set_temp("my_killer",ob);
  command("exert powerup");
  ::kill_ob(ob);
}

void die()
{
  object ob = query_temp("my_killer");
  object me = this_object();

  object where;

  where =environment(me);

  if (ob &&
      ob == me->query_last_damage_from() &&
      ob->query_temp("hell-17/daozeigui1"))
  {
    tell_object(ob,"你帮助" HIG"盗贼鬼"NOR + "杀了绿毛鬼！\n");
    ob->set_temp("hell-17/daozeigui2",1);
  }

  message_vision ("\n$N惨叫一声，化成一堆骷髅，散落在地上。\n",me);
  destruct(me);
  return;
}

void unconcious()
{
  die();
}