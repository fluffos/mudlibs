#include <ansi.h>
inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "石板路");
  set ("long", @LONG

西面的石板路到头了。在继续往西而去，是连鬼都不愿轻易千万的
荒草地了。

LONG);
  set("exits",
  ([
     "east" : __DIR__"w-shiban2",
     "west" : __DIR__"huangcao1",
  ]));
  set("objects",
  ([
      __DIR__"npc/shimogui" : 1,
  ]));
  setup();
}

