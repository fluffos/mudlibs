#include <ansi.h>
inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "石板路");
  set ("long", @LONG

这里离镇中心很近。石板路面显得颇为整洁。往西看去，
就是这个小镇的中心了，往东看，镇外一片山林。

LONG);
  set("exits",
  ([
     "east" : __DIR__"e-shiban2",
     "west" : __DIR__"center",
  ]));
  set("objects",
  ([
      __DIR__"npc/shimogui" : 1,
  ]));
  setup();
}

