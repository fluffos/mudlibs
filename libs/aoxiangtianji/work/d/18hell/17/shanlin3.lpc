#include <ansi.h>
inherit SMALLROOM;
inherit WOODROOM;
void create ()
{
  set ("short", "山林");
  set ("long", @LONG

渐渐的走人了山林的深处。这里的山路分出了岔路。山路
崎岖，路两边树木虽低矮，却很繁茂。

LONG);
  set("exits",
  ([
     "eastup" : __DIR__"shanlin4",
     "westdown" : __DIR__"shanlin2",
     "southup" : __DIR__"wuxing1",
  ]));
  setup();
}

