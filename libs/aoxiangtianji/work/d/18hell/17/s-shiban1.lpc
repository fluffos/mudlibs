#include <ansi.h>
inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "石板路");
  set ("long", @LONG

这里是小镇中心南面的一条石板路。路面很安静，一路行来
看到的鬼影在这里几乎都见不到了。

LONG);
  set("exits",
  ([
     "north" : __DIR__"center",
     "south" : __DIR__"s-shiban2",
  ]));
  setup();
}

