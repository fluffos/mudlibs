#include <ansi.h>
inherit SMALLROOM;
inherit WOODROOM;
void create ()
{
  set ("short", "山林");
  set ("long", @LONG

渐渐的走人了山林的深处。从这里折往东北方向，可以到达
火山地狱，而往西，沿山路而下，是刀锯地狱所在。

LONG);
  set("exits",
  ([
     "northeast" : __DIR__"shanlin5",
     "westdown" : __DIR__"shanlin3",
  ]));
  setup();
}

