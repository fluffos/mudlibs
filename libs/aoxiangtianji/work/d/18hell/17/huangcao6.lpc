#include <ansi.h>
inherit BIGROOM;
inherit WOODROOM;
void create ()
{
  set ("short", "荒草地");
  set ("long", @LONG

一片枯黄的荒草地了。据说这里有恶鬼出没，看来要十分
当心了。

LONG);
  set("exits",
  ([
     "west" : sprintf(__DIR__"huangcao%d",5+random(4)),
     "east" : __DIR__"huangcao7",
     "south" : sprintf(__DIR__"huangcao%d",1+random(8)),
     "south" : sprintf(__DIR__"huangcao%d",1+random(8)),
  ]));
  setup();
}

