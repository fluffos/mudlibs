#include <ansi.h>
inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "石板路");
  set ("long", @LONG

石板小路在这里到头了。回头北望，小镇的中心就在北面不远的地方。

LONG);
  set("exits",
  ([
     "north" : __DIR__"s-shiban2",
  ]));
  setup();
}

