#include <ansi.h>
inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "石板路");
  set ("long", @LONG

眼看就要出了小镇了。石板路面显得颇为整洁。往东行不久
就出了小镇，那是一片山林。

LONG);
  set("exits",
  ([
     "east" : __DIR__"shanlin1",
     "west" : __DIR__"e-shiban2",
  ]));
  set("objects",
  ([
      __DIR__"npc/huiguo" : 1,
  ]));
  setup();
}

