#include <ansi.h>
inherit SMALLROOM;
inherit MUDDYROOM;
void create ()
{
  set ("short", "土路");
  set ("long", @LONG

一条黄土铺就的小路，路面显得整洁了很多。走在路上，
似乎总有影子在晃动，想仔细看去，却什么都没有。
南面看到了一些房屋，看来离镇中心不远了。

LONG);
  set("exits",
  ([
     "north" : __DIR__"xiaolu3",
     "south" : __DIR__"tulu2",
  ]));
  set("objects",
  ([
      __DIR__"npc/nongfu" : 1,
  ]));
  setup();
}

