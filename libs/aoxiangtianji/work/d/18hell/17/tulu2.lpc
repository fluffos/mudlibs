#include <ansi.h>
inherit SMALLROOM;
inherit MUDDYROOM;
void create ()
{
  set ("short", "土路");
  set ("long", @LONG

一条黄土铺就的小路，路面显得整洁了很多。走在路上，
似乎总有影子在晃动，想仔细看去，却什么都没有。
南面就是镇中心了。

LONG);
  set("exits",
  ([
     "north" : __DIR__"tulu1",
     "south" : __DIR__"center",
  ]));
  setup();
}

