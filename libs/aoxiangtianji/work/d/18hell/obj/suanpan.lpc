inherit ITEM;
void create()
{
  set_name("黑算盘", ({"18hell hei suanpan", "hei suanpan","suanpan"}));
  set_weight(80);
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "一件黑色的算盘，怎么拨拉都是害别人，利自己！\n");
    set("unit", "件");
  }

  setup();
}

