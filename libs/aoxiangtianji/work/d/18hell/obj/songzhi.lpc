inherit ITEM;
void create()
{
  set_name("干枯的松枝", ({"18hell song zhi", "branch"}));
  set_weight(80);
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "一截落在地上的干枯了的松枝，闻闻还有股松脂的味道！\n");
    set("unit", "截");
  }

  setup();
}

