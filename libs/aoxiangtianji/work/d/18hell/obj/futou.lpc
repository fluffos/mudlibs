inherit ITEM;
void create()
{
  set_name("一把坏的砍柴斧", ({"18hell futou", "futou","axe"}));
  set_weight(80);
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "一把已经坏了的砍柴斧头！\n");
    set("unit", "把");
    set("need_repair",1);
  }

  setup();
}

