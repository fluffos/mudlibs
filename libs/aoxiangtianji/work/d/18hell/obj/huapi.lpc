inherit ITEM;
void create()
{
  set_name("画皮", ({"18hell hua pi", "pi"}));
  set_weight(80);
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "一件描画得非常仔细的人皮，"
                "正面看真是眉目清秀动人，可惜背面看，却鲜血淋漓，腐臭不堪！\n");
    set("unit", "件");
  }

  setup();
}

