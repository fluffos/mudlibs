inherit ITEM;
void create()
{
  set_name("香肉", ({"18hell rou", "rou"}));
  set_weight(80);
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "一块香喷喷的肉！\n");
    set("unit", "块");
  }

  setup();
}

