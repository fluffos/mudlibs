inherit ITEM;
void create()
{
  set_name("油桶", ({"18hell you tong", "youtong", "tong"}));
  set_weight(80);
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "一个装东西的桶，里面油腻腻的，看来是装油的！\n");
    set("unit", "个");
  }

  setup();
}

