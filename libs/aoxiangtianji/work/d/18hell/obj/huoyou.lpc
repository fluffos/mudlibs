inherit ITEM;
void create()
{
  set_name("火油", ({"18hell huoyou", "huoyou", "oil"}));
  set_weight(80);
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "一桶火油！\n");
    set("unit", "桶");
  }

  setup();
}

