inherit ITEM;
void create()
{
  set_name("法尺", ({"18hell law rule", "rule"}));
  set_weight(80);
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "一件正面是黑色，背面是白色的法尺！\n");
    set("unit", "件");
  }

  setup();
}

