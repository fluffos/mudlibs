inherit ITEM;
void create()
{
  set_name("白酒", ({"18hell tanzang wine", "wine"}));
  set_weight(80);
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "一坛白酒，闻闻真香！\n");
    set("unit", "坛");
  }

  setup();
}

