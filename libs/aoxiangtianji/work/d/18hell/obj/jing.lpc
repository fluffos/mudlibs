inherit ITEM;
void create()
{
  set_name("经书", ({"18hell jing", "jing", "book"}));
  set_weight(80);
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "一本满是尘土的经书！\n");
    set("unit", "本");
  }

  setup();
}

