inherit ITEM;
void create()
{
  set_name("红粉骷髅", ({"18hell hongfen kulou", "kulou"}));
  set_weight(80);
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "一个红粉骷髅，看起来别提多难看了！\n");
    set("unit", "个");
  }

  setup();
}

