inherit ITEM;
void create()
{
  set_name("黄金", ({"18hell false gold", "gold"}));
  set_weight(80);
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "黄澄澄的金子！\n");
    set("unit", "件");
  }

  setup();
}

