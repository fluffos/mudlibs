inherit ITEM;
void create()
{
  set_name("松脂火把", ({"18hell huoba", "huoba", "tourch"}));
  set_weight(80);
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "一个用松脂和松枝做成的火把！\n");
    set("unit", "个");
  }

  setup();
}

