// Room: /d/wiz/hall.c
inherit WIZROOM;
inherit SAFTYROOM;
#include <ansi.h>
int block_cmd();
void create ()
{
  set ("short","巫师大殿");
  set ("long", @LONG
这里是巫师日常处理事务的地方，如果巫师们要开会的话，这里也是可供选择的地方。

LONG);
  set("exits", ([ /* sizeof() == 2 */
  "west" : "/d/wiz/wizroom",
 ]));
  setup();
  call_other("/obj/board/post_b", "???");
}

void init()
{
  if (!wizardp(this_player()))
    add_action("block_cmd","",1);
}

int block_cmd()
{
  string verb = query_verb();
  if (verb=="say") return 0; //allow say
  if (verb=="chat") return 1;
  if (verb=="quit") return 1;
  if (verb=="look") return 1;
  return 1;
}

