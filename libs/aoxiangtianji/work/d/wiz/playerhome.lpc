inherit BIGROOM;
inherit LANDROOM;
inherit SAFTYROOM;
#include <ansi.h>
void create ()
{
  set ("short","玩家之家"HIW"门派讨论室"NOR);
  set ("long", @LONG
这里为玩家提供了BBS类的留言板。希望玩家在这里交流各个门派的游戏心得。
如果觉得需要增加话题板块，可以找巫师协商。
LONG);
  set("exits", ([
    "up": "/d/city/skezhan",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    "/d/wiz/npc/giftman" : 1,
    "/adm/npc/notebook" : 1,
  ]));
  set("no_time", 1);
  set("no_bian",1);
  set("no_quit",1);
  setup();
  call_other( "/obj/board/menpai_p_bbs", "???" );
}

