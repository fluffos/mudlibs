inherit SMALLROOM;
inherit LANDROOM;
inherit SAFTYROOM;
void create()
{
  set ("short","系统公告室");
  set("long", @LONG
这是一间很普通的房间，房内贴着一些有关这个世界的资讯。
LONG
);
  set("item_desc" , ([ /* sizeof() == 1 */
    "east": "房子里云雾缭绕，什么也看不清楚。\n",
  ]));
  set("exits", ([ /* sizeof() == 2 */
    "east" : "/d/wiz/wizroom",
    "west" : "/d/wiz/entrance",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    "/d/wiz/gift/liwu-jilu" : 1,
    "/d/wiz/gift/giftman" : 1,
  ]));
  set("limit_post",1);
  setup();
  call_other("/obj/board/system_b", "???");
}

