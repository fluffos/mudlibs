inherit BIGROOM;
inherit MUDDYROOM;
inherit GHOSTROOM;
void create ()
{
  set ("short", "阴山殿");
  set ("long", @LONG
四周的黑暗到了这里，突然如潮水般消退了。一束光亮
晃了过来。仔细查看四周，不知觉间，竟处身于一座大殿
之上，一个身披黑龙袍的鬼王正目不转睛的盯住你。
LONG);
  set("exits", ([ /* sizeof() == 1 */
    "south" : __DIR__"yinshan2",
  ]));

  set("objects", ([ /* sizeof() == 1 */
    __DIR__"npc/youmingwang" : 1,
  ]));
  setup();
}

