inherit BIGROOM;
inherit MUDDYROOM;
inherit GHOSTROOM;
void create ()
{
  set ("short", "荒郊外");
  set ("long", @LONG
你好象走出了荒郊野外的草野之地。面前陡然的出现了
一座高山，仿佛这山是突然从地下冒了出来一样。一眼
看去，怎么也找不到上山的路，似乎这山只是为了阻拦
你继续往前一般。
LONG);
  set("exits", ([ /* sizeof() == 3 */
    "west" : sprintf(__DIR__"caoye%d", 7+random(3)),
  ]));
  set("objects", ([
    __DIR__"npc/zhandian" : 1,
  ]));
  setup();
}

