#include <ansi.h>
inherit ITEM;
void create()
{
  set_name(YEL "文殊菩萨神像" NOR, ({ "wenshu xiang", "xiang" }) );
  set_weight(500000);
  set_max_encumbrance(800);
  if (clonep())
    set_default_object(__FILE__);
  else
  {
    set("unit", "座");
    set("value", 50);
  }

}