inherit MIDDLEROOM;
inherit HILLROOM;
void create()
{
   set("short", "山路" );
   set("long", @LONG
一条不算宽阔的弯曲山路，路的两面。绿树
成荫。这里向东直上，就是中台翠石峰。
LONG);
  set("exits", ([
    "eastup" : __DIR__"cuishifeng",
    "southwest" : __DIR__"c-shanlu1",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "wutai");
  setup();
}

