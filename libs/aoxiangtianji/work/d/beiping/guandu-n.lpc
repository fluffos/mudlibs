#include <ansi.h>
inherit RIVER;
void create()
{
  set("short", "官渡北岸");
  set("long", @LONG
这里是官渡的北岸。三国时期，袁绍和曹操在这里
展开了著名的“官渡之战”。黄河从西向东流去，
河上(river)有一叶小舟。
LONG);
  set("arrive_room", "/d/beiping/guandu-s");
  set("exits", ([
    "north" : __DIR__"s-dadao3",
  ]));
  set("resource/fish", ({ "/obj/fish/liyu",
                          "/obj/fish/jiyu",
                          "/obj/fish/qingyu",
                          "/obj/fish/caoyu", }));
  set("outdoors", "huanghe");
  setup();
}

