#include <ansi.h>
inherit MIDDLEROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "装备店");
  set ("long", @LONG

这里是装备店。店里到处银光闪闪，让人眼花缭乱。装备
店老板也是慕容世家的传人。
LONG);
  set("exits",
  ([ //sizeof() == 1
     "south" : __DIR__"qixiang6",
  ]));

  set("objects",
  ([ //sizeof() == 1
      __DIR__"npc/murong-yin" : 1,
  ]));
  set("no_clean_up", 0);
  setup();
}

