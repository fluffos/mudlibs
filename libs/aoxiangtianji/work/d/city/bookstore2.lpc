inherit SMALLROOM;
inherit LANDROOM;
void create ()
{
        set ("short", "西城书屋");
        set ("long", @LONG
这是一家老字号的书局。书架上整整齐齐地放着一些手抄的卷轴。
雕板印刷书到了唐朝已日趋成熟，因此这里的书架上也放着不少印制
精美图文并茂的图书。
LONG);
  set("exits",
   ([ //sizeof() == 4
       "west" : __DIR__"baihu3",
   ]));
  set("objects",
    ([ //sizeof() == 1
         __DIR__"npc/bookseller2" : 1,
    ]));
  set("no_clean_up", 0);
  setup();
}

