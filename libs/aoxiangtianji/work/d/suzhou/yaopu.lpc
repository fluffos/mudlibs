inherit SMALLROOM;
inherit LANDROOM;
inherit SAFTYROOM;
void create()
{
  set("short", "立春堂");
  set("long", @LONG
这是一家药铺，一股浓浓的药味让你几欲窒息，那是从药
柜上的几百个小抽屉里散发出来的。药房先生却不在店内。一
名小伙计站在柜台后招呼着顾客。
LONG );
  set("no_clean_up", 0);
  set("exits", ([
    "south"  : __DIR__"dongdajie2",
  ]));
  set("objects", ([
     "/obj/npc/yaopu-huoji" : 1,
  ]));
  setup();
}

