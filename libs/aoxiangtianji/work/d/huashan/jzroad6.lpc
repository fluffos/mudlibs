inherit BIGROOM;
inherit WOODROOM;
#include "/obj/quarry/snareroom.lpc"
void create()
{
  set("short", "树林外");
  set("long", @LONG
在阴森森的林子边上，耳边只能听到风的呼号，和野兽凄厉的
鸣叫。西上是登山的路，东边就进入森林了。
LONG );
  set("exits", ([ /* sizeof() == 4 */
    "enter"   : __DIR__"jzroad5",
    "westup"  : __DIR__"jzroad7",
  ]));
  set("quarrys", ([
   "tu"  : 100000,
   "hou"   : 80000,
   "qingshe" : 20000,
   "hu2"  : 5000,
  ]));
  setup();
}

