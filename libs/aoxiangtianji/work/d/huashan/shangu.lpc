inherit BIGROOM;
inherit WOODROOM;
void create()
{
  set("short", "山谷");
  set("long", @LONG
这里是中条山的一个隐秘山谷。四周是高可及天的山峰，云深林
密，一径小溪潺潺流过，溅入坡下塘中。
LONG );
  set("exits", ([ /* sizeof() == 4 */
    "northdown" : __DIR__"jzroad7",
  ]));
  set("outdoors", "huashan");
  set("no_clean_up", 0);
  setup();
}

