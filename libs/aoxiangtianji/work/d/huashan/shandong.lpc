inherit SMALLROOM;
inherit MUDDYROOM;
void create()
{
  set("short", "山洞");
  set("long",
"这里是一个不大的山洞，奔腾的瀑布成了它的门帘，阳光轻易就"
"可以的透过水帘射进来，因此这里白天并不阴暗。");
  set("exits", ([ /* sizeof() == 2 */
    "out"  : __DIR__"shanhongpb",
  ]));
  setup();
}