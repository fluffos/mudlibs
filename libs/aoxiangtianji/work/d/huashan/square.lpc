inherit BIGROOM;
inherit LANDROOM;
void create()
{
  set("short", "广场");
  set("long", @LONG
这里华山山顶开辟的一块平台，虽然不大却很平整，人来人往，
热闹非凡。西面有个小店。通常有许多华山派弟子在此买些物品。
西北方似乎有条小道通往山上。
LONG );
  set("exits", ([ /* sizeof() == 2 */
    "northwest" : __DIR__"chaopath1",
    "south"     : __DIR__"yunu",
    "west"      : __DIR__"shop",
  ]));
  set("outdoors", "huashan" );
  setup();
}