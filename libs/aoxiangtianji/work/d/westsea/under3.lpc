inherit BIGROOM;
inherit WATERROOM;
void create ()
{
  set ("short", "海底");
  set ("long", @LONG
沿着海底的这条小径，踏着轻柔的海波，一路行来
真是奇异景象数不胜数。眼前霞光突的射了出来，定目
观看，却见前面矗立着一座庞大的宫殿。
LONG);

  set("exits", ([ /* sizeof() == 2 */
    "south" : __DIR__"under2",
    "west" : __DIR__"shuijingong",
  ]));

  setup();
}