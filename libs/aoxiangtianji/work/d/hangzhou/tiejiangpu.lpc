inherit SMALLROOM;
inherit LANDROOM;
void create()
{
  set("short", "铁匠铺");
  set("long", @LONG
这是一家不小的打铁铺，店里摆了几座火炉，一位铁匠正在火炉
旁满头大汗地打造东西。一位老者坐在边上。火炉边放着不少打好了
的铁器。
LONG);
  set("exits", ([ /* sizeof() == 2 */
    "west" : __DIR__"road11",
  ]));
  set("objects", ([ /* sizeof() == 1 */
     __DIR__"npc/smith" : 1,
  ]));
  set("no_clean_up", 0);
  setup();
}