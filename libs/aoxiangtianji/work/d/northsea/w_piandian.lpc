inherit BIGROOM;
inherit WATERROOM;
void create ()
{
  set ("short", "西偏殿");
  set ("long", @LONG
西偏殿有几个宫娥正在打闹，突然看见你走了进来，
都不禁吃了一惊。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "east"  : __DIR__"dadian",
  ]));

  setup();
}