inherit SMALLROOM;
inherit WATERROOM;
void create ()
{
  set ("short", "餐房");
  set ("long", @LONG
龙宫内的一间餐房，一般只是宫中弟子使用。锅里沽嘟嘟不知煮着什么东
西，一个大箩筐里放着好些包子。
LONG);
  set("objects", ([ /* sizeof() == 3 */
    __DIR__"npc/long6.lpc" : 1,
    __DIR__"obj/food" : 3,
    "/d/obj/food/niupi-jiudai.lpc" : 1,
  ]));
  set("resource", ([
    "water" : 1,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "west" : __DIR__"mulang1.lpc",
  ]));

  set("room_size","small");
  set("terrain","water");
  setup();
}

