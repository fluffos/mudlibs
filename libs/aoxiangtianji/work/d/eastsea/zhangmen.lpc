inherit BIGROOM;
inherit WATERROOM;
void create()
{
  set ("short", "掌门室");
  set ("long", @LONG

这里是龙宫的掌门房间，房间内陈设比较简单。一个龙宫弟子
站立在中间。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "down": __DIR__"inside1",
  ]));
  set("objects", ([
    "/obj/npc/zhangmen/longgong" : 1,
  ]) );

  set("no_clean_up", 0);
  set("no_npc",1);
  set("zm_limit",1);
  setup();
}

