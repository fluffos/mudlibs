#include <ansi.h>
inherit SMALLROOM;
inherit HILLROOM;
inherit SAFTYROOM;
void create()
{
  set("short", "内厅");
  set("long", @LONG
这里是一间小小的房间，没什么特别的地方，你看见书桌旁边有一位头发
花白的老道正在阅读经书，他的前方有一扇半开的铁门，不停的从里面冒出滚
滚浓烈的药味.
LONG );
  set("objects", ([
     __DIR__"npc/liandan_yaochun"     : 1,
  ]));
  set("exits", ([
     "south" : __DIR__"liandan_fang",
     "north": __DIR__"xiaowu",
  ]));
  setup();
}