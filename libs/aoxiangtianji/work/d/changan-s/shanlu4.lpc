inherit SMALLROOM;
inherit WOODROOM;
void create ()
{
  set ("short", "山路");
  set ("long", @LONG
小路上荒草凄凄，直没膝盖处，荆棘也挂扯着，不让
前行。这里似乎看不出明显的方向了，向南继续，似乎小
路要容易些。
LONG);
  set("exits", ([ /* sizeof() == 2 */
    "north" : __DIR__"shanjian2",
    "south" : __DIR__"shanlu5",
  ]));
  set("outdoors", 1);
  setup();
}