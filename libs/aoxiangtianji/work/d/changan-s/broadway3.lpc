inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "大官道");
  set ("long", @LONG
这是一条宽阔的官道，足可容得下十马并驰。依绵延
的山势，道路不在笔直。往南通向汉水，往北则直达
大唐国都长安城。
LONG);
  set("exits", ([ /* sizeof() == 4 */
  "southwest" : __DIR__"broadway4",
  "northwest" : __DIR__"broadway2",
]));
  set("outdoors", 1);
  if(random(2))
    set("objects", ([ "/d/obj/weapon/throwing/shi.lpc" : 1, ]));
  else
    set("objects", ([ "/d/obj/weapon/throwing/smallshi.lpc" : 1, ]));
  setup();
}

