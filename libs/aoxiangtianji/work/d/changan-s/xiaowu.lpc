#include <ansi.h>
inherit SMALLROOM;
inherit LANDROOM;
inherit SAFTYROOM;
void create()
{
  set("short", "小屋内");
  set("long", @LONG
你一走进这里，就闻到了一股浓烈的药草味道。
一个小童正在这里埋着头整理药材。屋内的小桌子
上，也堆放着各种说不出名字的药草。
LONG );
  set("objects", ([
     __DIR__"npc/liandan_tongzi"     : 1,
  ]));
  set("exits", ([
     "northwest" : __DIR__"shanlu5",
     "south": __DIR__"liandan_yaoshi",
  ]));
  setup();
}