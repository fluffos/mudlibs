inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "大官道");
  set ("long", @LONG
这是一条宽阔笔直的官道，足可容得下十马并驰。往北
则直达大唐国都长安城。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "east" : __DIR__"fendui.lpc",
    "south" : __DIR__"zhongnan",
    "north" : __DIR__"sbridge",
  ]));
  set("can_build_home",1);
  set("outdoors", "changan");
  if(random(2))
    set("objects", ([ "/d/obj/weapon/throwing/shi.lpc" : 1, ]));
  else
    set("objects", ([ "/d/obj/weapon/throwing/smallshi.lpc" : 1, ]));
  setup();
}

