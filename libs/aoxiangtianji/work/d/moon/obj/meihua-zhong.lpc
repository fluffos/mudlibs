inherit ITEM;
void create()
{
  set_name("梅花种子", ({"meihua zhongzi", "zhongzi"}));
  set_weight(40);
  if (clonep())
    set_default_object(__FILE__);
  else
  {
    set("long", "一袋花种。\n");
    set("unit", "袋");
    set("value", 0);
  }

  setup();
}

