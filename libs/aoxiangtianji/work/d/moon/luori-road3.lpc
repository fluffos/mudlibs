inherit SMALLROOM;
inherit SNOWROOM;
void create ()
{
  set ("short", "落日峰");
  set ("long", @LONG
一条上山峰的小路，迂回盘绕。路两边的积雪慢慢的显得
单薄了。两边的树林渐渐的茂密起来。
LONG);
  set("exits",
  ([
     "northdown" : __DIR__"luori-road2",
     "southup" : __DIR__"luori-maze1",
  ]));
  set("outdoors", "moon");
  setup();
}

