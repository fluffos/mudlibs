inherit BIGROOM;
inherit SNOWROOM;
void create()
{
  set ("short", "掌门室");
  set ("long", @LONG

这里是将军府的掌门房间，房间内陈设比较简单。一个将军装扮的人
站立在中间。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "down": __DIR__"keting",
  ]));
  set("objects", ([
    "/obj/npc/zhangmen/jjf" : 1,
  ]) );

  set("no_clean_up", 0);
  set("no_npc",1);
  set("zm_limit",1);
  setup();
}

