void init()
{
  object me;

  me = this_player();

  if ((int)me->query("level") > 12 ||
      (int)me->query("PKS") >= 2)
  {
    tell_object(me, "这里不适合你呆，出去吧！\n");
    me->move(__DIR__"northgate");
    message_vision("$N糊里糊涂的被扔到了这里！\n",me);
  }
}