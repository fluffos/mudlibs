inherit SMALLROOM;
inherit MUDDYROOM;
void create ()
{
  set ("short", "斜巷尽头");
  set ("long", @LONG
一屡光线射了进来，四周可以模模糊糊的看清了。
回头看看过来的路，不禁吓了一跳，那一片依然黑糊糊
的不见尽头！
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "south" : __DIR__"xiaolu1",
    "north" : __DIR__"andao10",
  ]));
  setup();
}