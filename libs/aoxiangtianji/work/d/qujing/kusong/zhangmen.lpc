inherit BIGROOM;
inherit LANDROOM;
void create()
{
  set ("short", "掌门室");
  set ("long", @LONG

这里是火云洞的掌门房间，房间内陈设比较简单。一个怪模怪样的
人站立在中间。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "down": __DIR__"huoyun",
  ]));
  set("objects", ([
    "/obj/npc/zhangmen/hyd" : 1,
  ]) );

  set("no_clean_up", 0);
  set("no_npc",1);
  set("zm_limit",1);
  setup();
}

