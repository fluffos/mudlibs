inherit SMALLROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "土戊洞");
  set ("long", @LONG
一个黑漆漆的小室，象是来储藏东西用的。
LONG);
  set("exits", ([
    "west" : __DIR__"huoyun",
  ]));

  set("objects", ([
    __DIR__"obj/che" : 1,
  ]));
  setup();
}