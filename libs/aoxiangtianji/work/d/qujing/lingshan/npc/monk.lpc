#include <ansi.h>
inherit "/d/qujing/lingshan/npc/biqiu";
void create()
{
  ::create();
  if (random(2))
    set_name(HIW"阿罗"NOR, ({ "a luo", "a", "luo" }));
  else
    set_name(HIG"揭谛"NOR, ({ "jie di", "jie", "di" }));
  set("gender", "男性");
  set("age", 100);
}