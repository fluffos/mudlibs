inherit ITEM;
void create()
{
  set_name("书帖", ({"shu tie", "tie"}));
  set_weight(100);
  if( clonep() )
    set_default_object(__FILE__);
  else {
    set("unit", "封");
    set("long", "一封奇怪的绿色书帖。\n");
  }

  setup();
}