inherit BIGROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "后宰门");
  set ("long", @LONG
这里是王宫的偏门，看上去冷冷清清的，只有几个老太监无所事事
地在那儿打瞌睡。
LONG);
  set("exits", ([ /* sizeof() == 2 */
    "east" : __DIR__"jxt",
    "south" : __DIR__"wall.lpc",
  ]));
  set("objects", ([
    "/d/qujing/npc/taijian" : 2,
  ]));
  setup();
}