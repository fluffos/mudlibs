#include <ansi.h>
inherit "/d/qujing/npc/yaojing";
string *names = ({
  HIY"兔精"NOR,
  HIM"狐精"NOR,
  HIG"獐怪"NOR,
  HIC"鹿精"NOR,
});
void create()
{
  int i;
  ::create();
  i = random(sizeof(names));
  set_name(names[i], ({"yao jing"}));
  setup();
}

