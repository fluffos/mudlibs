inherit SMALLROOM;
inherit LANDROOM;
void create ()
{
  set ("short", "厨房");
  set ("long", @LONG
厨房里支起了数口大锅，灶下烈火熊熊，整日不息。蒸笼里传出的香
气却甚是古怪，看来这包子里大有文章。墙上有张告示，油渍斑斑，字迹
颇为模糊。
LONG);
  set("valid_startroom", 1);
  set("exits", ([
    "east" : __DIR__"dong2",
  ]));
  set("resource", ([
    "water" : 1,
  ]));
  set("objects", ([
    __DIR__"npc/yanshu": 1,
    __DIR__"obj/baozi": 1,
  ]) );
  setup();
  call_other("/obj/board/wudidong_b", "???");
}

