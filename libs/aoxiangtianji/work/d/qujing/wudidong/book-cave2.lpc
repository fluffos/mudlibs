inherit SMALLROOM;
inherit MUDDYROOM;
void create ()
{
  set ("short", "洞穴");
  set ("long", @LONG
这里的洞穴突然大了许多。四壁插着几支牛油蜡烛，发出点若明若暗
的光。洞穴里空阔的很，除了北边靠墙处一张巨大的石桌外，什么都
没有。
LONG);
  set("exits", ([
    "southup" : __DIR__"book-cave1",
  ]));

  set("objects", ([
    __DIR__"npc/kulou1" : 1,
  ]));

  setup();
}