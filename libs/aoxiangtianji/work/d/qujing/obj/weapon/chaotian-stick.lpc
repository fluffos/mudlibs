#include <weapon.h>
inherit STICK;
void create()
{
  set_name("朝天棍", ({ "chaotian gun", "gun", "stick" }) );
  set("unit", "根");
  set_weight(500);
  if( clonep() )
     set_default_object(__FILE__);
  else {
    set("value", 150);
    set("material", "iron");
  }

  init_stick(30);
  setup();
}

