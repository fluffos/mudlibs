#include <weapon.h>
inherit SWORD;
void create()
{
  set_name("无头剑", ({ "wutou jian", "jian", "sword" }) );
  set("unit", "把");
  set_weight(800);
  if( clonep() )
     set_default_object(__FILE__);
  else
  {
    set("value", 390);
    set("material", "iron");
  }

  init_sword(35);
  setup();
}

