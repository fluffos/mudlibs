#include <weapon.h>
inherit SWORD;
void create()
{
  set_name("花竺剑", ({ "huazhu jian", "jian", "sword" }) );
  set("unit", "把");
  set_weight(800);
  if( clonep() )
     set_default_object(__FILE__);
  else {
    set("value", 390);
    set("material", "iron");
  }

  init_sword(30);
  setup();
}

