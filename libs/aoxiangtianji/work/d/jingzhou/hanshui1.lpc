#include <ansi.h>
inherit RIVER;
void create()
{
  set("short", "汉津渡");
  set("long", @LONG
这里已经是汉水南岸了。从此渡过汉水，直达
大唐京城长安。而从次南下，这通达重镇荆州。
LONG);
  set("arrive_room", "/d/changan-s/hanshui2");
  set("exits", ([
    "southeast" : __DIR__"nroad2",
  ]));
  set("resource/fish", ({ "/obj/fish/liyu",
                          "/obj/fish/jiyu",
                          "/obj/fish/qingyu",
                          "/obj/fish/caoyu", }));
  set("outdoors", "jingzhou");
  set("room_size","big");
  setup();
}