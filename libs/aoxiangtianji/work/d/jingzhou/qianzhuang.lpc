#include <ansi.h>
inherit BANK;
void create()
{
  set("short", "钱庄");
  set("long", @LONG
这是一家老字号的钱庄，已有几百年的历史，在全国各地
都有分店。它发行的银票信誉非常好，通行全国。
LONG);
  set("no_fight",1);
  set("no_kill",1);
  set("no_magic",1);
  set("room_size","small");
  set("terrain","land");
  set("exits", ([
     "east" : __DIR__"beidajie1",
  ]));

  set("objects", ([
    __DIR__"npc/qian" : 1
  ]));
  set("no_clean_up", 0);
  setup();
}

