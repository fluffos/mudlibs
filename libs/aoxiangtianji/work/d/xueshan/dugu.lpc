#include <ansi.h>
inherit SMALLROOM;
inherit LANDROOM;
void create()
{
  set("short", "毒谷入口");
  set("long", @LONG

一个峡谷显露在了眼前。峡谷内，雾气弥漫。路边立着一个牌
子，牌子上，鲜红的一行字引人注目：擅入此谷，尸骨无存。毒郎中
警示。
LONG);
  set("exits", ([
     "south" : __DIR__"shanlu6",
     "northeast"    : __DIR__"shanlu5",
  ]));

  setup();
}

