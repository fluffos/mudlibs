#include <armor.h>
#include <ansi.h>
inherit CLOTH;
void create()
{
  set_name("熟罗长袍", ({ "cloth" }) );
  set_weight(3000);
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("unit", "件");
    set("value", 80);
    set("material", "cloth");
  }

  init_cloth(8);
  setup();
}

