inherit SMALLROOM;
inherit MUDDYROOM;
void create()
{
  set("short", "衡山水莲洞内");
  set("long", @LONG
这里四周空旷，比起外面来却没有那么潮湿，路到这里却
没有了。
LONG);
  set("exits", ([
    "south" : __DIR__"zigai2",
  ]));

  set("objects", ([
    "/obj/beast/yanjingshe" : 2,
  ]));
  setup();
}

