inherit MIDDLEROOM;
inherit LANDROOM;
void create()
{
  set("short", "回雁楼");
  set("long", @LONG
这里是衡阳回雁楼，是本地最大的酒楼。
LONG);
  set("exits", ([
    "down" : __DIR__"shop",
  ]));
  set("no_clean_up", 0);
  setup();
}

