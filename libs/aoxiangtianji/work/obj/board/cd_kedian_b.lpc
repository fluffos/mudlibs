inherit BULLETIN_BOARD;
void create()
{
  set_name("成都客店留言板", ({ "board" }) );
  set("location", "/d/chengdu/kedian");
  set("board_id", "cd_kedian_b");
  set("long", "有有用的话就往这里说。\n" );
  setup();
  set("capacity", 100);
  replace_program(BULLETIN_BOARD);
}

