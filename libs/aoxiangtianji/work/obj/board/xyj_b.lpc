#include "ansi.h"
inherit BULLETIN_BOARD;
void create()
{
  set_name(HIG"工作调度板"NOR, ({ "board" }) );
  set("location", "/d/wiz/jobroom");
  set("board_id", "xyj_b");
  set("long", "这是一个供各位交流讨论的"+HIG+" BBS "+NOR+"板。\n" );
  setup();
  set("capacity", 500);
}

