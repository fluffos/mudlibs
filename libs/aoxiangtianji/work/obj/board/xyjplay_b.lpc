#include <ansi.h>
inherit BBS_BOARD;
void create()
{
  set_name("翱翔天际"+HIG+" 交流 "+NOR+"谈论板", ({ "board" }) );
  set("short",HIY"翱翔天际交流版"NOR);
  set("location", "/d/wiz/entrance");
  set("board_id", "xyjplay_b");
  set("long", "这是一个供玩家交流讨论的"+HIG+" 留言 "+NOR+"板。\n" );
  setup();
  set("capacity", 200);
}

