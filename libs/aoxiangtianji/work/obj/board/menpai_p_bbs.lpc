#include <ansi.h>
inherit BBS_BOARD;
void create()
{
  set("short","翱翔天际门派讨论BBS");
  set("location", "/d/wiz/playerhome");
  set_name("擎日挽月"HIG"门派"NOR"讨论板", ({ "board" }) );
  set("board_id", "menpai_p_bbs");
  set("long", "这是一个供玩家们讨论门派经验用的新闻板。\n" );
  setup();
 set("capacity", 300);
 replace_program(BBS_BOARD);
}

