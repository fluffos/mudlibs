#include <ansi.h>
inherit "/obj/furniture/bed.lpc";
void create()
{
  set_name("鸳鸯床", ({ "yuanyang chuang","bed"}) );
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "这是一张宽大的鸳鸯床，床褥绣着戏水鸳鸯图案。\n");
    set("value", 50000);
    set("material", "wood");
  }

  setup();
}