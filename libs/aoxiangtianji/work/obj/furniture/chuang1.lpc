#include <ansi.h>
inherit "/obj/furniture/bed.lpc";
void create()
{
  set_name("木板床", ({ "muban chuang","bed"}) );
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "这是一张常见的,普通木板床。\n");
    set("value", 12000);
    set("material", "wood");
  }

  setup();
}