#include <ansi.h>
inherit "/obj/furniture/bed.lpc";
void create()
{
  set_name("丝绒床", ({ "sirong chuang","bed"}) );
  if( clonep() )
    set_default_object(__FILE__);
  else
  {
    set("long", "这是一张柔软的丝绒床。\n");
    set("value", 30000);
    set("material", "wood");
  }

  setup();
}