inherit "/obj/questnpc/tasknpc";
void create()
{
  ::create();
  NPC_D->generate_cn_name(this_object());
  set("long", "一个人。\n");
  set("gender", "男性");
  setup();
  if (clonep())
    keep_heart_beat();
}

