// league command: league
#include <ansi.h>
void create() { seteuid(getuid()); }

int main(object me, string arg)
{
  string msg;

  if (!playerp(me))
    return 0;
  msg = LEAGUE_D->leave_league(me);
  if (!stringp(msg))
    return 0;
  write(msg);
  return 1;
}