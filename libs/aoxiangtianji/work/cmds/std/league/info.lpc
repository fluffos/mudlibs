// league command: info
#include <ansi.h>
void create() { seteuid(getuid()); }

int main(object me, string arg)
{
  string msg;

  if (!playerp(me))
    return 0;
  msg = LEAGUE_D->league_info(me,arg);
  if (!stringp(msg))
    return 0;
  me->start_more(msg);
  return 1;
}