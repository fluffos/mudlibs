inherit F_CLEAN_UP;
#include <ansi.h>;
int main(object me,string arg)
{
  if (!arg)
    SHOP_D->open_all(me);
  else
    SHOP_D->open_shop(me,arg);
  return 1;
}

int help (object me)
{
	write(@HELP
指令格式 : openshop [店铺名称]
如果没有指定店铺名称，则是开放所有店铺。
HELP );
        return 1;
}

