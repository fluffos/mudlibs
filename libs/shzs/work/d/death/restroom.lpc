// Room: /d/wiz/restroom.c

inherit ROOM;

void create() {
  set("short", "地狱门");
  set("long", @LONG
这里是什么也没有的一个小房间，整个房间笼罩在柔和的白光中。

LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "up": "/d/lwe/kedian1",
  ]));
  set("no_clean_up", 0);
  set("no_fight", 1);
  set("move_cost", 1);

  setup();
  replace_program(ROOM);
}
