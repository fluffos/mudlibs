// Room: /d/death/hall.c

#include <ansi.h>

inherit ROOM;

void create() {
  set("short", HIW "地狱" NOR);
  set("long", @LONG
这里白茫茫的一片空间，地狱主管史巴克站在这里。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */

    "out": __DIR__ "gate",
  ]));
  set("objects", ([
    __DIR__ "npc/toilet": 1
  ]));
  set("no_fight", 1);
  set("resource/water", "史巴克");

  setup();
}
