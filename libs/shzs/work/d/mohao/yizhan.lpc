// Room: /d/zhongzhou/yizhan.c

inherit ROOM;

void create() {
  set("short", "魔法传送站");
  set("long", @LONG
这里是魔龙城的魔法传送站，你可以通过这里的魔法阵去到其他的大陆。
LONG
  );

  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "wendingnan4",
  ]));

  setup();
}
