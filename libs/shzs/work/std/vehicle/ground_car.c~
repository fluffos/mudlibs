// ground_car.c

#include <vehicle.h>
#include <dbase.h>

inherit VEHICLE;

void setup()
{
	if( clonep( this_object()) ) return;

	set("vehicle_type", TYPE_GROUND_CAR);
	set("armors", ({"車頭", "車尾", "右側", "左側", "車頂",}));
	set("plain", 1);
	set("hill", 1);
}