// ground_car.c

#include <vehicle.h>
#include <dbase.h>

inherit VEHICLE;

void setup() {
  if (clonep(this_object())) return;

  set("vehicle_type", TYPE_GROUND_CAR);
  set("armors", ({ "ó繷", "óЮ", "凹", "オ凹", "ó郴", }));
  set("plain", 1);
  set("hill", 1);
}
