// pawn_stamp.c

inherit ITEM;

void create() {
  set_name("╄┿舦", ({ "pawn stamp", "stamp" }));
  set_weight(5);
  if (!clonep()) {
    set("unit", "眎");
    set("long", "眎フ╄┿舦\n");
  }
  setup();
}
