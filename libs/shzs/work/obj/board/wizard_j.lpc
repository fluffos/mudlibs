// wizard_j.c

inherit BULLETIN_BOARD;

void create() {
  set_name("ね逼痁璸礶厨狾", ({ "board" }));
  set("board_id", "wizard_j");
  set_weight(20000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "硂琌ㄑね逼痁璸礶痙ē狾.\n");
    set("unit", "");
    set("capacity", 100);
    set("value", 10);
  }
  setup();
}
