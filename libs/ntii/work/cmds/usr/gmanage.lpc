// Cmds:/cmds/usr/gmanage.c

inherit F_CLEAN_UP;

#ifndef GAMBLE_D
#define GAMBLE_D          "/adm/daemons/gambled"
#endif

int main(object me, string arg) {
  GAMBLE_D->start_init(me);
  return 1;
}

int help(object me) {
  write(@HELP
指令格式 : gmanage 

这条指令用于管理博彩系统。

HELP);
  return 1;
}
