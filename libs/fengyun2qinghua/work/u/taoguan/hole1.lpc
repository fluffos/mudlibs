inherit ROOM;

void create() {
  set("short", "洞内");
  set("long", @LONG
洞里黑暗潮湿。你心里的不祥感觉越来越重。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": "/u/taoguan/hole2",
    "south": "/u/taoguan/hole",
  ]));
  setup();
  replace_program(ROOM);
}
