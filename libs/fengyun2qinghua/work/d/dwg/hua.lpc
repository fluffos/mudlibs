inherit ROOM;

void create()
{
        object con, item;
        set("short", "画室");
        set("long", @LONG
这间画室的四壁都挂满了画，其中不乏一些精品，你凑近点仔细
瞧一瞧，竟然有唐伯虎的真迹--八美图，真是让人讶异不已。
LONG
        );
        set("exits", ([ 
  "west"  : __DIR__"shuimu4",
]));
        set("objects",  ([
        __DIR__"npc/gongnu2" : 1,
])  );
        setup();
        replace_program(ROOM);
}
