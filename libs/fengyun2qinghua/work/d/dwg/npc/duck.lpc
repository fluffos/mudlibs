// crazy_dog.c

inherit NPC;

void create()
{
	set_name("野鸭", ({ "duck" }) );
	set("race", "野兽");
	set("age", 2);
	set("long", "一只憨态可掬的野鸭。\n");
	set("attitude", "peaceful");
	
	set("limbs", ({ "头部", "身体", "前脚", "後脚", "尾巴" }) );
	set("verbs", ({ "bite" }) );

	set("combat_exp", 200);

	setup();
}
