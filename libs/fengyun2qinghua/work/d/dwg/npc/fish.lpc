// crazy_dog.c

inherit NPC;

void create()
{
	set_name("鱼", ({ "fish" }) );
	set("race", "野兽");
	set("age", 2);
	set("long", "一条活鱼。\n");
	set("attitude", "peaceful");
	
	set("limbs", ({ "头部", "身体", "尾巴" }) );
	set("verbs", ({ "bite" }) );

	set("combat_exp", 0);

	setup();
}
