// mother.c
inherit NPC;

void create()
{
	set_name("恶魔亡魂", ({ "ghost" }) );
	set("long", @LONG
一个无法投胎重生的恶魔亡魂。
LONG
);
	set("combat_exp", 500);
	set("attitude", "peaceful");

	setup();
    carry_object(__DIR__"obj/staff");
}

int is_ghost() { return 1; }
