inherit ROOM;

void create() {
  set("short", "小白屋");
  set("long", @LONG
在外面看这屋子已经小得可怜了，走进去之后，更象是走进间鸽
子笼。可是鸽子虽小，五脏俱全，这屋子也一样，别人家里有什么，
这屋子里几乎也一样不缺，甚至还有个金漆马桶。
LONG
  );
  set("exits", ([
    "south": __DIR__ "youling1"
  ]));
  set("objects", ([
    __DIR__ "npc/yeling": 1
  ]));
  setup();
  replace_program(ROOM);
}
