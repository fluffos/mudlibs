// token.c


inherit ITEM;

void create() {
  set_name("梅树幼苗", ({ "youmiao" }));
  set_weight(10000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "棵");
    set("long", "一棵梅树幼苗。\n");
    set("material", "wood");
  }
  setup();
}
