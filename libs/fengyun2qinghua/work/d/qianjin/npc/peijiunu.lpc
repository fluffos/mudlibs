// mother.c
#include <ansi.h>
inherit NPC;

void create()
{
	set_name("陪酒女", ({ "peijiunu" }) );
	set("gender", "女性" );
	set("age", 19);
	set("per", 26);
	set("long", @LONG
一个满脸媚笑的陪酒女
LONG
);
    set("title", RED"满脸媚笑"NOR);
	set("combat_exp", 500000);
    set_skill("unarmed", 160);
    set_skill("dodge", 160);
    set_skill("move", 160);
	set("attitude", "friendly");

	setup();
	
	carry_object(__DIR__"obj/shayi")->wear();
	set("score",-10);
}

