// mother.c
#include <ansi.h>
inherit NPC;

void create()
{
	set_name("妙龄少女", ({ "dancer" }) );
	set("gender", "女性" );
	set("age", 19);
	set("per", 26);
	set("long", @LONG
一个纱衣朦胧，狂野性感的妙龄舞女
LONG
);
    set("title", RED"纱衣朦胧"NOR);
	set("combat_exp", 500000);
    set_skill("unarmed", 160);
    set_skill("dodge", 160);
    set_skill("move", 160);
	set("attitude", "friendly");

	setup();
	
	carry_object(__DIR__"obj/shayi")->wear();
	set("score",-10);
}

