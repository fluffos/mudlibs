// token.c


inherit ITEM;

void create() {
  set_name("大黑石", ({ "blackstone" }));
  set_weight(800000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "块");
    set("long", "一块沉重的大黑石。\n");
    set("material", "stone");
    set("value", 1);
  }
  setup();
}
