// Room: /d/snow/crossroad.c

inherit ROOM;

void create()
{
        set("short", "山坳");
        set("long", @LONG
这里是一处山坳，往南就是雪亭镇，一条蜿蜒的小径往东通往另一
个邻近的小山村，一块官府立的告示牌立在路旁，上面写著有关黑松山
土匪出没的警告标志。
LONG
        );
        set("exits", ([ /* sizeof() == 3 */
  "west" : "/u/quicksand/huangyie0",
  "south" : "/d/snow/mstreet4",
  "east" : "/d/green/path6",
]));
        set("no_clean_up", 0);
        set("outdoors", "snow");

        setup();
        replace_program(ROOM);
}
