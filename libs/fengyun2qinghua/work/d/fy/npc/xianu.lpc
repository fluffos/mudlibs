// teacher.c

inherit NPC;

void create()
{
	set_name("裸体女人", ({ "naked girl", "girl" }) );
	set("gender", "女性" );
	set("age", 20 + random(50));

	set("attitude", "peaceful");
    set("combat_exp", 1000);

	setup();
	set("score",5);
}

