// dumpling.c

inherit ITEM;
inherit F_FOOD;

void create()
{
	set_name("椒盐面筋", ({ "jymj" }) );
	set_weight(80);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("long", "一盘香喷喷的椒盐面筋。\n");
		set("unit", "盘");
		set("value", 2000);
		set("food_remaining", 1);
		set("food_supply", 30);
	}
}
