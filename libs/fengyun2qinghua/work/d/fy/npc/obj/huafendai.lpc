// token.c


inherit ITEM;

void create() {
  set_name("花粉袋", ({ "hubag" }));
  set_weight(10);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "袋");
    set("long", "一袋香喷喷的花粉。\n");
    set("material", "cloth");
    set("value", 1000);
  }
  setup();
}
