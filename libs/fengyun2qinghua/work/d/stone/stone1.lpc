inherit ROOM;
void create()
{
        set("short", "阴暗石巷");
        set("long", @LONG
石巷散发着一种奇怪的霉腐味道．又加杂着一丝血腥．和干净宽
敞的东云大路格格不入．一条又脏又臭的小河沟顺着巷侧缓缓流淌．
上面还漂着染满着黑血的绷带．
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"stone2",
  "south" : "/d/fy/ecloud1",
]));
        set("objects", ([
        "/d/snow/npc/scavenger" : 1,
			]) );

        set("outdoors", "fengyun");
        setup();
        replace_program(ROOM);
}
