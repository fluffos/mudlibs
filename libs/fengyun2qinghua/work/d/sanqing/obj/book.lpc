// token.c


inherit ITEM;

void create() {
  set_name("三清道术", ({ "necromancy_book", "book" }));
  set_weight(100);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "本");
    set("long", "一本无字天书。\n");
    set("material", "paper");
    set("value", 0);
  }
  setup();
}
