
inherit ROOM;

void create()
{
	set("short", "山坳");
	set("long", @LONG
这里是一处山坳，往东就是南北大道，一条蜿蜒的小径往西通往
一座山，这座山好象叫做......无名山。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "westup" : __DIR__"road1",
  "eastdown" : "/d/laowu/nroad1",
]));
	set("outdoors", "taoguan");
	setup();
	replace_program(ROOM);
}
