// hat.c

inherit EQUIP;

void create() {
  set_name("订做的", ({ "order" }));
  set_weight(200);
  set("no_drop", 1);
  set("no_get", 1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "条");
    set("long",
      "一条自制的腰带，看上去华丽非常。\n");
    set("material", "cloth");
    set("armor_type", "waist");
    set("armor_prop/armor", 10);
    set("bu_gold", 1);
    set("owner", "qiqi");
  }
}
