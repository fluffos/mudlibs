// /clone/board/towiz_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("任务系统意见留言本",({"board"}) );
	set("location", "/d/jingzhou/guangchang");
	set("board_id", "quest_b");
	set("long","请玩家把对任务系统的意见在这里post出来。\n");
	setup();
	set("capacity", 30);
	replace_program(BULLETIN_BOARD);
}

