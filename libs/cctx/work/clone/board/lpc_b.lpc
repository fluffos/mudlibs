// /clone/board/lpc_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("LPC留言簿", ({ "board" }) );
	set("location", "/d/wizard/lpcroom");
	set("board_id", "lpc_b");
	set("long", "这是一个巫师交流LPC的留言板。\n" );
	setup();
	set("capacity",100);
	replace_program(BULLETIN_BOARD);
}

