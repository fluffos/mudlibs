// /clone/board/quanzhen_b.c

inherit BULLETIN_BOARD;

void create() {
  set_name("全真弟子留言簿", ({ "board" }));
  set("location", "/d/chongyang/sanqingdian");
  set("board_id", "quanzhen_b");
  set("long", "这是一个专供全真弟子使用的留言板。\n");
  setup();
  set("capacity", 30);
  replace_program(BULLETIN_BOARD);
}
