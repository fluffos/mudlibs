// Room: /d/wudu/caofang1.c
// by netkill 98-9-19


#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "休息室");
  set("long", @LONG
这里是五毒弟子们休息的地方，空空的房间摆了一张通铺，
看样子可以睡几十人，几个弟子正在床上熟睡，你不由也有了
阵阵的困意。
LONG
  );
  set("sleep_room", "1");
  set("no_fight", "1");
  set("exits", ([
    "west": __DIR__ "zoulang",
  ]));
  setup();
}
