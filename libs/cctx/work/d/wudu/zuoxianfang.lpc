// by netkill 98-9-19


#include <ansi.h>
inherit ROOM;

void create() {
  set("short", GRN "左厢房" NOR);
  set("long", @LONG
这是一间厢房，一边墙上有一个大书架，中间一张方桌，靠里
有张小床，看上去虽然简陋，却显示着一种威严之气。
LONG
  );
  set("exits", ([
    "east": __DIR__ "jinshetang",
  ]));
  set("objects", ([
    __DIR__ "obj/wuduxinfa": 1,
    __DIR__ "obj/wudubianfa": 1,
  ]));
  setup();
  replace_program(ROOM);
}
