// Room: /d/wudu/caofang1.c
// by netkill 98-9-19


#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", GRN "走廊"NOR );
	set("long", @LONG
这是一很安静的走廊，西边是五毒教第子休息的地方，南边
不时可以听到练武的声音，原来那是间练功房。
LONG
	);
        set("exits", ([
		"east" : __DIR__"xiuxishi",
		"south" :__DIR__"liangongfang",
		"west"  :__DIR__"qianting",
		]));
	setup();
	replace_program(ROOM);
}



	