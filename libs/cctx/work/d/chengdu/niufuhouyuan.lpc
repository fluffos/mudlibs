// chengdu/niufuhouyuan.c
// Written by Doing Lu  1998

inherit ROOM;

void create() {
  set("short", "牛府后院");
  set("long", @LONG
	这里是牛府的后院，东西竟然没有厢房，前院的假山一直连了过来，到
后院也有一个鱼池。
LONG
  );

  set("exits", ([
    "south": __DIR__ "niufuroom3",
  ]));

  set("objects", ([
    __DIR__ "npc/puyi": 1,
  ]));

  setup();
  replace_program(ROOM);
}
