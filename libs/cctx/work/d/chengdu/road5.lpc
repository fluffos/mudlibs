// road5.c
// by netkill

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "官道");
  set("long", @LONG
一条笔直的官道，向东通往成都，西去却只见高山绵绵，不知道哪里
还有出路，路的两边树木茂密，青松翠柏。
LONG
  );
  set("outdoors", "qingzh");

  set("exits", ([
    "east": __DIR__ "ximen",
    "west": __DIR__ "road4",

  ]));

  setup();
}
