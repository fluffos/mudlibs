// chengdu/jinpu.c
// Written by DOing Lu  1998

inherit ROOM;

void create()
{
	set("short", "金铺");
	set("long", @LONG
	这里是销售金银饰品的地方，各种精致的金饰品实在是巧夺天工，不过口
袋里若是没有点家当，最好就不要问津。
LONG
	);

	set("exits", ([
		"west" : __DIR__"beidajie2",
	]));
        set("objects", ([
                 __DIR__"npc/huoji3": 1,
	]));

	setup();
	replace_program(ROOM);
}
