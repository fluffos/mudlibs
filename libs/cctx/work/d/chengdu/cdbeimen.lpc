// Room: /d/chengdu/cdbeimen
// netkill /98/8/16/

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "北门");
  set("long", @LONG
成都北门，新修的城门在阳光下闪闪发亮，上书“东门”，由
于成都地处天俯之地，四面有天险与外隔断，从来就没有发生过什
么战争，所以值岗的官兵懒懒散散，有的还打起了瞌睡。
LONG
  );
  set("exits", ([
    "north": __DIR__ "nroad1",
    "south": __DIR__ "beijie",

  ]));


  set("no_clean_up", 0);
  set("outdoors", "chengdu");

  setup();
  replace_program(ROOM);
}
