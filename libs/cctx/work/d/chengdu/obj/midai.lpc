// midai.c 米袋
// Written by Doing Lu

inherit ITEM;

void create() {
  set_name("米袋", ({ "midai", "dai", "bag" }));
  set_weight(20000);
  set("unit", "只");
  set("long", "一只用麻布织成的袋子，上面写着盛源米行四个字，里面装着大米。\n");
  set("value", 1000);
}
