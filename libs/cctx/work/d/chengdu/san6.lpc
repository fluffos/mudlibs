// Room: /d/chengdu/san6.c
// Date: xws 98/08/30

#include <room.h>
inherit ROOM;

void create()
{
    set("short", "祁山山道");
    set("long", @LONG
山道边是一片松林，面前是一条宽广的石板道，笔直向上通向山前面
就是诸葛堂了，里面传来阵阵香火的味道。
LONG
    );
    set("outdoors", "chengdu");
    set("exits", ([
        "northup" : __DIR__"zugetang",
        "southdown" : __DIR__"san5",
        "eastdown" : __DIR__"san7",

]));

      setup();
    replace_program(ROOM);
}

