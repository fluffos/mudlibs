// chengdu/xiaodian.c
// Written by Doing Lu  1998

inherit ROOM;

void create()
{
	set("short", "小店");
	set("long", @LONG
	这里是成都城西街的一个小店，卖一些日用杂货。
LONG
	);

	set("exits", ([
		"south" : __DIR__"xidajie1",
	]));

	set("objects", ([
		__DIR__"npc/laoban" : 1
	]));

	setup();
	replace_program(ROOM);
}
