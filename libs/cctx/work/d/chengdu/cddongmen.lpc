// Room: /d/chengdu/dongmen
// netkill /98/8/16/

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short",  "东门"  );
        set("long", @LONG
成都东门，新修的城门在阳光下闪闪发亮，上书“东门”，由
于成都地处天俯之地，四面有天险与外隔断，从来就没有发生过什
么战争，所以值岗的官兵懒懒散散，也不对过路行人进行盘问，有
的甚至摆起了龙门，讲些官俯趣闻，引的过路人驻足听闻。
LONG
        );
        set("exits", ([
  "west" :  __DIR__"dongjie",
  "east" : __DIR__"shudao19",
  
   ]));
        

        set("no_clean_up", 0);
        set("outdoors", "chengdu");

        setup();
        replace_program(ROOM);
}

