// Room: /chengdu/chatin.c
// xws 98/09/03 
inherit ROOM;
void create()
{
	set("short", "茶亭");
	set("long", @LONG
这是坐落在祁山山腰上的是一座茶亭，迎面飘来一阵阵茶所特有的清香
一位老者正殷勤地为刚到的游客斟茶水，茶亭的两个柱子上写着一幅对
联（duilian）。
LONG
  );
 	set("item_desc", ([ /* sizeof() == 1 */
  "duilian" : "
                    蒙            扬
                    山            子
                    顶            江
                    上            中
                    茶            水
",
]));
	set("resource/water", 1);
	set("exits", ([
		"eastdown" : __DIR__"san1",
	]));
	set("objects", ([
		__DIR__"npc/daye" : 1,
	]));
	setup();
	replace_program(ROOM);
}

