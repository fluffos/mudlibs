//room: /d/city2/tiantan.c

inherit ROOM;

void create()
{
	set("short","天坛");
	set("long",@LONG
这里是京城的一大名胜，四周是各式各样的雕刻和建筑，据
说这里在修建时势按照五行八卦的方位进行设计的。
LONG								    
	);
 set("outdoors", "city2");	
	set("exits",([
       
	"east" : __DIR__"ttenter",
	]));

 setup();
 replace_program(ROOM);
}
