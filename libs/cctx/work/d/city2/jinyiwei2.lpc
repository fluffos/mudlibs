//room: /d/city2/jinyiwei.c

inherit ROOM;

void create()
{
	set("short","锦衣卫府衙");
	set("long",@LONG
这里就是锦衣卫衙门，墙角放着些兵器之类的东西，这
里的人好像都出去了，四处静得可怕。
LONG								    
	);
	
	set("exits",([
	"east" : __DIR__"jinyiwei",
	]));

 setup();
 replace_program(ROOM);
}
