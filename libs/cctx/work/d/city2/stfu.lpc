inherit ROOM;

void create()
{
	set("short", "顺天府");
	set("long", @LONG
这里就是当年的顺天府旧址，似乎有人在这里居住，
不过也看得出来比较久远了，朱门也已调了大多的漆。
LONG
	);
        set("outdoors", "city2");

	set("exits", ([
	//	"east" : __DIR__"",
	//	"west" : __DIR__"",
		"north" : __DIR__"xbstreet3",
	//	"south" : __DIR__"",
	]));

	setup();
	replace_program(ROOM);
}


