inherit ROOM;

void create() {
  set("short", "苹果园");
  set("long", @LONG
你走进了一片苹果林子里，这里到处都栽满了苹果树，可
惜不是秋收季节，不然可以饱餐一顿了。南边是一条宽广的大
道。
LONG
  );

  set("exits", ([
    "southdown": __DIR__ "wroad3",
  ]));

  set("outdoors", "city2");
  setup();
  replace_program(ROOM);
}
