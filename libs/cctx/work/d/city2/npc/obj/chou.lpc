//chou duan.c 绸缎

inherit ITEM;

void create()
{
	set_name("绸缎", ({"chou duan","silk","chou","duan"}));
	if (clonep())
		set_default_object(__FILE__);
	else {
		set("long", "一匹上好料子的绸缎。\n");
		set("unit", "丈");
	        set("value", 5000);
				
	}
}

