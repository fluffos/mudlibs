#include <ansi.h>
#include <room.h>
inherit ROOM;

void create() {
  set("short", YEL "南广场" NOR);
  set("long", @LONG
这里看起来非常宽广，四处人声鼎沸，十分闹热，不愧是
京都的繁华地段，不是有马车在这里飞驰而过，有些官兵在这
里走来走去，还有些太监在四处走动。
LONG
  );
  set("outdoors", "city2");
  set("exits", ([
    "north": __DIR__ "center",
    "west": __DIR__ "nmdajie2",
    "east": __DIR__ "nanmendajie1",
    "south": __DIR__ "nanmen",
  ]));

  setup();
  replace_program(ROOM);
}
