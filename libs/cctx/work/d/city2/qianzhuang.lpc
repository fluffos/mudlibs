// Room: /city2/qianzhuang.c

inherit ROOM;

void create() {
  set("short", "通吃钱庄");
  set("long", @LONG
这是一家老字号的钱庄，已有几百年的历史，在全国各地
都有分店。它发行的银票信誉非常好，通行全国。不管你走到
哪里，在任何一个本庄分店都可以任意存取。墙上挂着一个牌
子(paizi)。
LONG
  );

  set("item_desc", ([
    "paizi": "\n本庄有利息，至少百分之一。\n",
  ]));
  set("exits", ([
    "north": __DIR__ "enroad2",
  ]));

  set("objects", ([
    __DIR__ "npc/qianlaob": 1
  ]));

  setup();
  replace_program(ROOM);
}
