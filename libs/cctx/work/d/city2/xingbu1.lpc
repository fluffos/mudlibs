//room: /d/city2/xingbu.c

inherit ROOM;

void create() {
  set("short", "刑部大堂");
  set("long", @LONG
这里是刑部大堂。这里可不是能随便乱闯的地方。通常有
大的案件，才会在这里审理。
LONG
  );

  set("exits", ([

    "north": __DIR__ "xingbu",
  ]));

  setup();
  replace_program(ROOM);
}
