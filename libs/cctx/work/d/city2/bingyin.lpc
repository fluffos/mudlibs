#include <room.h>
inherit ROOM;

void create() {
  set("short", "兵营");
  set("long", @LONG
这里是兵营，密密麻麻到处都是官兵，有的在武将的指挥
下列队操练，有的独自在练功，有的坐着、躺着正在休息。还
有些太监在四处走动。
LONG
  );
  set("outdoors", "city2");
  set("exits", ([
    "south": __DIR__ "nmdajie2",
  ]));

  set("objects", ([

    "/d/city/npc/wujiang": 2,
    "/d/city/npc/bing": 5,
  ]));
  setup();
  replace_program(ROOM);
}
