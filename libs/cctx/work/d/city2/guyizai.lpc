inherit ROOM;

void create() {
  set("short", "古意斋");
  set("long", @LONG
这里是京城老字号的古玩店，掌柜是正精明地坐在柜台上
玩赏着一个细花磁瓶。货架上琳琅满目的堆满了各式各样的古
玩玉器。
LONG
  );



  set("exits", ([
    "south": __DIR__ "enroad3",
  ]));
  set("objects", ([
    __DIR__ "npc/sun": 1,
  ]));
  setup();
  replace_program(ROOM);
}
