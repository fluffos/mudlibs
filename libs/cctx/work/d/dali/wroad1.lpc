// /d/dali/wroad1.c
// jackyboy 98/11/23

inherit ROOM;

void create()
{
	set("short",  "青石路" );
	set("long", @LONG
一条青石铺成的路，从洱海流出的西洱河在路旁缓缓向西流去，南
边是一座雄伟的无量山。东边是大理城的西城门。
LONG
);
	set("outdoors", "dali");

	set("exits", ([
		"west" : __DIR__"wroad2",    
		"east" :__DIR__"ximen",
	]));
	setup();
	replace_program(ROOM);
}
