// Room: /d/dali/sroad5.c
// By JackyBoy 98/12/1

inherit ROOM;

void create()
{
	set("short", "山路");
	set("long", @LONG
现在已经走到半山腰了，云雾弥漫，根本看不出十步以外有些什么东西！
LONG
);
	set("outdoors","dali");
	set("exits",([
		"eastdown" : __DIR__"sroad4",
		"southwest" : __DIR__"sroad6",
	]));
	setup();
	replace_program(ROOM);
}
