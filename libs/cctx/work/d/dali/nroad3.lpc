// Room /d/dali/nroad3.c
// By JackyBoy 98.10.19
inherit ROOM;
void create() {
  set("short", "小路");
  set("long", @LONG
这是一条铺满了大理石的小路，沿着洱海边弯弯曲曲地向北延伸，
有时要经过一条条从苍山上流下来的小溪，溪水清澈透底，还带着一种
雪水特有的冰凉。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "southeast": __DIR__ "nroad2",
    "northeast": __DIR__ "qiao1",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "dali");
  setup();
  replace_program(ROOM);
}
