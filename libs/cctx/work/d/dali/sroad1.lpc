// Room: /d/dali/sroad1.c
// By JackyBoy 98.10.27

inherit ROOM;

void create() {
  set("short", "山路");
  set("long", @LONG
这里是大理的南城门外，往北可以看到大理城门上龙飞凤舞地写着
[大理]两个字，再往南过一座桥是一条崎岖的山路，西南边可以看到高耸
的无量山，满山都是出名的思茅松。
LONG
  );
  set("outdoors", "dali");
  set("exits", ([
    "north": __DIR__ "nanmen",
    "south": __DIR__ "xiaoqiao",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
