// /d/dali/board/jiulou_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("酒楼留言板", ({ "board" }) );
	set("location", "/d/dali/jiulou1");
	set("board_id", "jiulou_board");
	set("long", 
	"请别灌水。\n" );
	setup();
	set("capacity", 100);
	replace_program(BULLETIN_BOARD);
}

