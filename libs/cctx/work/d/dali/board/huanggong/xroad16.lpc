// Room: /d/dali/xroad16.c
// By JackyBoy 98/12/6
#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "藤桥下");
	set("long", @LONG
这里是澜沧江上为数不多的渡口之一，旁边立着一快石碑(bei),似乎
写着几个字。往西即可走上藤桥。
LONG
);
	set("outdoors","dali");
	set("item_desc",([
		"bei" : HIY"善人渡"NOR"\n",
	]));
	set("exits",([
		"southeast" : __DIR__"xroad15",
		"westup" : __DIR__"xroad17",
	]));
	setup();
	replace_program(ROOM);
}

