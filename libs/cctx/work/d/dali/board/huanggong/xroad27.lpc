// Room: /d/dali/xroad27.c
// By JackyBoy 98/12/8
#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "碎石路");
	set("long", @LONG
这是一条碎石路，南边是从洱海流出来的西洱河，这条路往东一直
可以通到大理城。
LONG
);
	set("outdoors","dali");
	set("exits",([
		"southwest" : __DIR__"xroad26",
		"north" : __DIR__"xroad28",
	]));
	setup();
	replace_program(ROOM);
}
