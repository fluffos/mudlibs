inherit ROOM;

void create()
{
        set("short", "嘉明殿");
        set("long", @LONG
嘉明殿是大理皇宫皇上用膳之处，于寝宫所在的勤政殿相邻。四
周禁卫亲从，近侍中贵，戒备的甚是森严。
LONG
        );
        set("no_clean_up", 0);
       set("exits", ([
 "south" : __DIR__"qinzheng",
     ]));
        set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/guard" : 1,
  __DIR__"npc/jinyiwei" : 2,
]));
        setup();
}
