// Room: /open/playhouse/huanggong/paifang.c

inherit ROOM;

void create()
{
        set("short", "御药房");
        set("long", @LONG
这里是堆满了从各国进贡来的珍奇药材。一群太监正忙着在整理
。房间正中放着一个丹炉，想必又是在炼什么长生不死的灵药吧！
LONG
        );
        set("no_clean_up", 0);
        set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/yu-yi" : 1,
]));
        set("exits", ([
  "south" : __DIR__"jiamin",
 ]));
       setup();
}
