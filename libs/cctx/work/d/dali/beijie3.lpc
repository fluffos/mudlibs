// Room: /d/dali/beijie3.c
// By JackyBoy 98.10.19
inherit ROOM;

void create() {
  set("short", "北街");
  set("long", @LONG
这是一条以大理石铺就的长街，往南是皇宫和王府之间的北广场，
往被则是大理城的北门。东边是则是大理囤积粮食的仓库，西边的建筑
上几幅旗帜迎风飘扬，是大理皇宫侍卫的兵营。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "beimen",
    "south": __DIR__ "beiguangchang",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "dali");
  setup();
  replace_program(ROOM);
}
