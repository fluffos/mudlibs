// Room: /d/dali/tlsroad1.c
// By JackyBoy 98.11.3 @ cctx
inherit ROOM;

void create()
{
	set("short", "小路");
	set("long", @LONG
这是一条铺满大理石的小路，虽然简陋，但是依然人来人往的，往
西已经可以看到大理崇圣寺，背后就是苍山十九峰中的[应乐峰]不少人
正提着香烛往西走去，东边是碧波荡漾的洱海，可以看到点点帆影。
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
		"westup" : __DIR__"tlsroad2",
		"east" : __DIR__"nroad1",
	]));
	set("no_clean_up", 0);
	set("outdoors","dali");
	setup();
	replace_program(ROOM);
}

