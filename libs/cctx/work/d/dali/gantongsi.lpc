// /d/dali/gantongsi.c 感通寺门外
// jackyboy /98/11/9

inherit ROOM;

void create() {
  set("short", "感通寺门外");
  set("long", @LONG
这里是大理苍山感通寺门外的小院，在苍山上行路的旅者到了这里
都要歇口气。南边有一条小溪喧闹着向山下流去，正是苍山十八溪之一
的莫残溪。
LONG
  );
  set("outdoors", "dali");

  set("exits", ([
    "northup": __DIR__ "feng_shengying",
    "southeast": __DIR__ "xi_mocan",
  ]));
  set("objects", ([
    "/kungfu/class/dali/fu": 1,
  ]));
  setup();
  replace_program(ROOM);
}
