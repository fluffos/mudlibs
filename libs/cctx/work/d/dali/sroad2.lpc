// Room: /d/dali/sroad2.c
// By JackyBoy 98.10.27

inherit ROOM;

void create()
{
	set("short", "山路");
	set("long", @LONG
这里是大理的城外的小山路，往北越过一道小桥就快到大理城门了
，再往南是一条崎岖的山路，往无量山上延伸出去。
LONG
);
	set("outdoors","dali");
	set("exits",([
		"north" : __DIR__"xiaoqiao",
		"southup" : __DIR__"sroad3",
	]));
	set("no_clean_up",0);

	setup();
	replace_program(ROOM);
}


