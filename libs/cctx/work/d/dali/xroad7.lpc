// Room: /d/dali/xroad7.c
// By JackyBoy 98/12/3
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "青石路");
  set("long", @LONG
这是一段由一整块石头铺成的青石路，比上山的路要平缓多了，石
头往西边伸出去了一些。往北是下山的路。
LONG
  );
  set("outdoors", "dali");
  set("exits", ([
    "south": __DIR__ "xroad6",
    "west": __DIR__ "yubi1",
    "northeast": __DIR__ "xroad8",
  ]));
  setup();
  replace_program(ROOM);
}
