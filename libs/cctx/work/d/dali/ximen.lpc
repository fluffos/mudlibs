// /d/dali/ximen
// netkill /98/8/14/
// modify by jackyboy 98/11/23

inherit ROOM;

void create()
{
	set("short",  "大理西门" );
	set("long", @LONG
这是大理西门，城墙上写有楷书“大理”两个大字，卫兵不时在城墙
上来回巡视。由此往西，就进入渺无人烟的山区，很少有人去哪里，所以
你还是小心一点。南去，则进入有名的无量山。
LONG
);
	set("outdoors", "dali");
	set("exits", ([
		//"west" : __DIR__"xroad1", 
		"west" : __DIR__"wroad1",
		"east" :__DIR__"xijie1",    
		//"southwest" :__DIR__"cangshan",
	]));
	setup();
	replace_program(ROOM);
}
