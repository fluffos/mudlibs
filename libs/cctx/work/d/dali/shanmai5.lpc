// /d/dali/shanmai5
// netkill /98/8/14/


inherit ROOM;

void create()
{
        set("short",  "横断山脉" );
        set("long", @LONG
你来到横断山脉，山高路险，山中各种野兽出没，你不由
得加快了步伐，向前赶去。
LONG
        );
        set("outdoors", "dali");

        set("exits", ([
               "north" :__DIR__"shanmai3",
               "west" :__DIR__"shanmai6",
                      ]));
       set("objects", ([
		__DIR__"npc/laohu" : 1,
	]));
             
        setup();
        replace_program(ROOM);
}
