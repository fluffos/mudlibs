// Room: /d/dali/sdong1.c
// By JackyBoy 98/12/2

inherit ROOM;

void create()
{
	set("short", "石洞");
	set("long", @LONG
虽说这里黑黑的，而且石洞外面也裹着大雾，可是不知道为什么仍
然让人感觉到特别暖和，看来这洞里是有点古怪。
LONG
);
	set("outdoors","dali");
	set("exits",([
		"out" : __DIR__"sroad9",
	]));
	setup();
	replace_program(ROOM);
}

int valid_leave(object me,string dir)
{
	me->set("move_dir",dir);
	return ::valid_leave(me, dir);
}