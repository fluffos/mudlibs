// Room /d/dali/qiao1.c
// By JackyBoy 98.10.19
inherit ROOM;
void create() {
  set("short", "小桥");
  set("long", @LONG
这是座用大理石做成的小拱桥，从苍山上流下来的隐仙溪溪水清澈
透底，波浪把阳光反射到你身上，一闪一闪的。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "southwest": __DIR__ "nroad3",
    "northeast": __DIR__ "nroad4",
  ]));
  set("objects", ([
    __DIR__ "npc/sboy": 1,
    __DIR__ "npc/sgirl": 1,
  ]));
  set("no_clean_up", 0);
  set("outdoors", "dali");
  setup();
  replace_program(ROOM);
}
