// Room /d/dali/qiao5.c
// By JackyBoy 98.10.23
inherit ROOM;
void create() {
  set("short", "锦溪");
  set("long", @LONG
这是从苍山上流下来的锦溪，溪水清澈透底，波浪把阳光反射到
你身上，一闪一闪的。双脚泡在水中很是冰凉清爽，舒服极了。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "southwest": __DIR__ "nroad8",
    "north": __DIR__ "nroad9",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "dali");
  setup();
}
