inherit ROOM;

void create()
{
        set("short", "御膳房");
        set("long", @LONG
这里是展中省所属的御厨，厨中烛活通明，几个厨子正忙着为宫
中准备晚餐，看着满屋的美味佳肴，你不由也想叫（serve)一份饭菜
LONG
        );
        set("no_clean_up", 0);
        set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/cuzi" : 1,
]));
        set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"zoulang3",
]));
        setup();
}
