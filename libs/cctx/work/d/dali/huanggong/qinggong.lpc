inherit ROOM;

void create()
{
        set("short", "寝宫");
        set("long", @LONG
这里就是保定帝休息的地方，每个人都想皇帝的寝宫应该是何等
的富丽堂皇，单说翡翠玛瑙墙上最少也该有二，三千颗吧，但这里却
和普通的房间没有什么区别，除了床上的被褥是极为精致的黄色龙凤
呈祥的苏绣外，再也找不出半点特殊的地方。
LONG
        );
        set("no_clean_up", 0);
       set("exits", ([
 "east" : __DIR__"qinzheng", 
     ]));
        set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/huanghou" : 1,
]));
        setup();
}
