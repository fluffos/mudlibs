inherit ROOM;

void create()
{
	set("short", "走廊");
	set("long", @LONG
这是正殿前面的一条长长的走廊，往左通往一个小圆，往右直通
正德殿，这里是朝臣上朝必经之路，因此走廊个侧甲士林立，戒备深
严。
LONG
	);
	set("no_clean_up", 0);
       set("exits", ([
 "east" : __DIR__"zhengde",
 "west" : __DIR__"xiaoyuan",
 "south" : __DIR__"lizheng",
    ]));
        set("objects", ([ /* sizeof() == 3 */
  __DIR__"npc/wushi" : 4,
]));
	setup();
}
