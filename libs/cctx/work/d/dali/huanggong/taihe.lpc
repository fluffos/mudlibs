inherit ROOM;

void create()
{
        set("short", "太和殿");
        set("long", @LONG
这里是每天皇上处理军机大事的地方，每当上朝时文武百官侧立
两旁，齐诵“皇上万岁万岁万万岁”！
LONG
        );
        set("no_clean_up", 0);
       set("exits", ([
 "southdown" : __DIR__"zhengde",
       ]));
        set("objects", ([ /* sizeof() == 3 */
  __DIR__"npc/wushi" : 4,
]));
        setup();
}
