// Room: /d/dali/storage.c
// By JackyBoy 98.11.6

inherit ROOM;

void create()
{
	set("short", "当铺储藏室");
	set("long", @LONG
当铺储藏室。
LONG
);
	set("valid_startroom",1);
	set("no_clean_up",0);
	setup();
	replace_program(ROOM);
}
