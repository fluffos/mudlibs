// /d/dali/wroad14.c
// Jackyboy 98/12/15

inherit ROOM;

void create()
{
	set("short",  "峡谷西" );
	set("long", @LONG
往东是一条大峡谷，不时有几个行人很狼狈地跑了过来，好象有的
还受了伤!难道峡谷里有什么东西在为非作歹？
LONG
);
	set("outdoors", "dali");
	set("exits", ([
		"east" : __DIR__"wroad13",
		"southwest" :__DIR__"xroad29",
	]));

	setup();
	replace_program(ROOM);
}

