// Room: /d/dali/xroad17.c
// By JackyBoy 98/12/6
#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "东桥头");
	set("long", @LONG
这是藤桥的东桥头，有几个石凳着，可能是让行人来到这里后休息
一会而摆设的。
LONG
);
	set("outdoors","dali");
	set("exits",([
		"eastdown" : __DIR__"xroad16",
		"west" : __DIR__"xroad18",
	]));
	setup();
	replace_program(ROOM);
}


