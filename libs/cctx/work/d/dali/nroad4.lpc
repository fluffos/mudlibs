// Room /d/dali/nroad4.c
// By JackyBoy 98.10.19
inherit ROOM;
void create()
{
	set("short","小路");
	set("long",@LONG
这是一条铺满了大理石的小路，沿着洱海边弯弯曲曲地向北延伸，
有时要经过一条条从苍山上流下来的小溪，小溪上架着一座座独木桥。
向南可以通到大理城内，往北则可以达到大理最出名的蝴蝶泉。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"southwest" : __DIR__"qiao1",
		"north" : __DIR__"nroad5",
	]));
	set("outdoors","dali");
	setup();
	replace_program(ROOM);
}
