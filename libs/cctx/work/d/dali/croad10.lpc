// /d/dali/xueshan
// netkill /98/8/14/

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", "官道" );
        set("long", @LONG
一条青石大官道，来往的人很多，尘土飞扬，十分热闹，北面已经清
晰可见成都的南门了，南边看去，只见万亩良田，好一派繁荣的景象，此
情此景，另你感慨不已。
LONG
        );
        set("outdoors", "dali");

        set("exits", ([
               "north"  :"/d/chengdu/nanmen",
		   "south" :__DIR__"croad9",
                      ]));

             
        setup();
       replace_program(ROOM);
}

