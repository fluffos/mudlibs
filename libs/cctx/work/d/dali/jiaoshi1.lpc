// Room /d/dali/jiaoshi1.c
// By JackyBoy 98.12.25
inherit ROOM;
void create() {
  set("short", "礁石");
  set("long", @LONG
这是一块青色的大理石礁石，东边是宽阔的洱海，海风不时吹起阵
阵浪涛，一个扑一个地打向岸上。被礁石挡住的地方形成了一个小水湾，
也许可以在这里吊到鱼也说不定呢！
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "nroad9",
  ]));
  set("objects", ([
    "/kungfu/class/dali/zhuwanli": 1,
  ]));
  set("no_clean_up", 0);
  set("outdoors", "dali");
  setup();
  replace_program(ROOM);
}
