// Room: /d/dali/xroad13.c
// By JackyBoy 98/12/5
#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "平地");
	set("long", @LONG
这是澜沧江边的一快平地，已经可以清晰地听到江水拍打礁石的声
音了。往南是上山的路。
LONG
);
	set("outdoors","dali");
	set("exits",([
		"southeast" : __DIR__"xroad12",
		"north" : __DIR__"xroad14",
	]));
	setup();
	replace_program(ROOM);
}
