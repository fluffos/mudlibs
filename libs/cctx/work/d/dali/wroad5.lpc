// /d/dali/wroad5.c
// netkill /98/8/14/


inherit ROOM;

void create() {
  set("short", "青石路");
  set("long", @LONG
一条青石铺成的路，向东不远，就到大理了。西去，是一片茫茫山
区。放眼看去，只见群山连绵，不知哪里有路。
LONG
  );
  set("outdoors", "dali");
  set("exits", ([
    //"westup" : __DIR__"shanlu",    
    //往西通往澜沧江
    //山路应该从蝴蝶泉以北开始，接在剑湖后
    "southwest": __DIR__ "wroad6",
    "east": __DIR__ "wroad4",
  ]));
  setup();
}
