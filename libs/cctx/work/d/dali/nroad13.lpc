// Room: /d/dali/nroad13.c
// By JackyBoy 98.11.23

inherit ROOM;

void create() {
  set("short", "小路");
  set("long", @LONG
这是一条顺着洱海延伸的小路，西北边就是大理最有名的大理蝴蝶
泉了。向南远远地看去，可以隐约看到大理城的影子。
LONG
  );
  set("exits", ([
    "south": __DIR__ "qiao9",
    "northwest": __DIR__ "hudiequan",
    "north": __DIR__ "nroad14",
  ]));
  set("no_clean_up", 0);
  set("objects", ([
    __DIR__ "npc/hudie": 2,
  ]));
  set("outdoors", "dali");
  setup();
  replace_program(ROOM);
}
