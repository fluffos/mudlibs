// Room: /d/dali/xroad2.c
// By JackyBoy 98/12/3
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "石阶");
  set("long", @LONG
这是一段直接在石头上凿出来的阶梯，一直望西伸下去，数也数不
清楚到底有多少级阶梯。再往东边爬几步就到石阶的尽头了。
LONG
  );
  set("outdoors", "dali");
  set("exits", ([
    "southwest": __DIR__ "xroad3",
    "east": __DIR__ "xroad1",
  ]));
  setup();
}
