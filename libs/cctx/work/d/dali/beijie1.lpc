// Room: /d/dali/beijie1.c
// By JackyBoy 98.10.19
inherit ROOM;

void create()
{
	set("short", "北街");
	set("long", @LONG
往南就是大理城的广场了，不是有几个挑着菜的农民匆匆地向广场
走过去，也许是要占据一个卖菜的好位子吧。往北可以看到大理国的皇
宫了。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"north" : __DIR__"beijie2",
		"south" : __DIR__"guangchang",
	]));
	set("no_clean_up", 0);
	set("outdoors", "dali");

	setup();
	replace_program(ROOM);
}
