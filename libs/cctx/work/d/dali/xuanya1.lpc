// /d/dali/xuanya1
// netkill /98/8/14/

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", HIR "悬崖" NOR );
        set("long", @LONG
你爬在悬崖上，稍不小心就会掉下去，只听的风声呼呼的吹过，你
还是快一点吧。 
LONG
        );
        set("outdoors", "dali");

        set("exits", ([   
                "down" :__DIR__"shanlu1",    
                ]));

         
        setup();
        replace_program(ROOM);
}
