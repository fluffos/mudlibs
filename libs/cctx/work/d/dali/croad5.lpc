// /d/dali/xueshan
// netkill /98/8/14/

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", HIG "平原" NOR );
        set("long", @LONG
一片水土肥沃的平原地带，可以看到农夫们在田里辛勤的劳作，阵阵
的稻香飘来，泌人心肺，历代四川称为天俯之国，看来名不虚传啊，南去
是一座座大山挡住了去路，北边则大路一直通向成都俯。
LONG
        );
        set("outdoors", "dali");

        set("exits", ([
               "north"  :__DIR__"croad6",
		   "south" :__DIR__"croad4",
                      ]));

             
        setup();
       replace_program(ROOM);
}

