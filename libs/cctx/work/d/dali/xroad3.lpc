// Room: /d/dali/xroad3.c
// By JackyBoy 98/12/3
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "石阶");
  set("long", @LONG
这是一段直接在石头上凿出来的阶梯，一直望西伸下去，数也数不
清楚到底有多少级阶梯。往东一直是坡路。
LONG
  );
  set("outdoors", "dali");
  set("exits", ([
    "westdown": __DIR__ "xroad4",
    "northeast": __DIR__ "xroad2",
  ]));
  setup();
  replace_program(ROOM);
}
