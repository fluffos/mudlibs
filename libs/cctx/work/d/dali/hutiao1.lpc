// /d/dali/hutiao1
// netkill /98/8/14/


inherit ROOM;

void create()
{
        set("short",  "山路" );
        set("long", @LONG
一条很陡的山路，西边下去直通谷底，路很不好走，不
时会有大石拦住去路，你不得不绕过去。
LONG
        );
        set("outdoors", "dali");

        set("exits", ([
               "westdown" :__DIR__"hutiao2",
               "eastup"     :__DIR__"shanmai6",
                      ]));

             
        setup();
        replace_program(ROOM);
}
