// /d/dali/hutiao3
// netkill /98/8/14/


inherit ROOM;

void create()
{
        set("short",  "山路" );
        set("long", @LONG
一条山路，向下望去，可见一条大河，水流甚急，发出
巨大的咆哮声。
LONG
        );
        set("outdoors", "dali");

        set("exits", ([
               "westdown" :__DIR__"hutiao4",
               "southup"     :__DIR__"hutiao2",
                      ]));

             
        setup();
        replace_program(ROOM);
}
