// Room: /d/dali/xroad11.c
// By JackyBoy 98/12/5
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "青石路");
  set("long", @LONG
青石路一直往南延伸，不过好象南边就是一个断崖了，不知道还有
没有路。往北是下山的路，已经可以看到山脚的澜沧江了。
LONG
  );
  set("outdoors", "dali");
  set("exits", ([
    "southeast": __DIR__ "xroad10",
    "northdown": __DIR__ "xroad12",
  ]));
  setup();
}
