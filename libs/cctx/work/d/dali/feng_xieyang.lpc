// /d/dali/feng_xieyang.c 斜阳峰
// jackyboy /98/11/8

inherit ROOM;

void create()
{
	set("short",  "斜阳峰" );
	set("long", @LONG
这里是大理苍山十九峰的斜阳峰山顶，南边是一条下山的小山路，
透过丝丝云雾可以看到山脚的西洱河，北边有一条小溪喧闹着向山下流
去，正是苍山十八溪之一的阳南溪。
LONG
);
	set("outdoors", "dali");

	set("exits", ([
		"southdown" : __DIR__"csroad2",
		"northwest" : __DIR__"froad1",
	]));
	setup();
	replace_program(ROOM);
}
