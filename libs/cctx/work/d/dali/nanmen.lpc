// Room: /d/dali/nanmen.c
// By JackyBoy 98.10.27

inherit ROOM;

void create()
{
	set("short", "南门");
	set("long", @LONG
这里是大理的南城门，往北一直走可以到达大理城中心广场了，出
了南门就是一片翠绿的群山。
LONG
);
	set("outdoors","dali");
	set("exits",([
		"north" : __DIR__"nanjie3",
		"south" : __DIR__"sroad1",
	]));
	set("no_clean_up",0);

	setup();
	replace_program(ROOM);
}

