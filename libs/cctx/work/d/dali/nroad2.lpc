// Room: /d/dali/nroad2.c
// Room /d/dali/nroad2.c
// By JackyBoy 98.10.19
inherit ROOM;
void create() {
  set("short", "小路");
  set("long", @LONG
这是一条铺满了大理石的小路，沿着洱海边弯弯曲曲地向北延伸，
不时还可以听到捕鱼人的歌声。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "nroad1",
    "northwest": __DIR__ "nroad3",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "dali");
  setup();
}
