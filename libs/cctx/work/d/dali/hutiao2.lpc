// /d/dali/hutiao3
// netkill /98/8/14/


inherit ROOM;

void create()
{
        set("short",  "山路" );
        set("long", @LONG
一条很陡的山路,路很不好走，不时会有大石拦住去
路，你不得不绕过去。侧耳倾听，谷底传来哗哗的流水声
音，看来下面是一条河。

LONG
        );
        set("outdoors", "dali");

        set("exits", ([
               "northdown" :__DIR__"hutiao3",
               "eastup"     :__DIR__"hutiao1",
                      ]));

             
        setup();
        replace_program(ROOM);
}
