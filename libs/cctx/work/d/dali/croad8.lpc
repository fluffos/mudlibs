// /d/dali/xueshan
// netkill /98/8/14/

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", "官道" );
        set("long", @LONG
一条青石大官道，来往的人很多，尘土飞扬，十分热闹，看来往的人
大多都衣冠鲜亮，似乎会武功的人也不少，看来成都真是个丰衣足食的好
地方。
LONG
        );
        set("outdoors", "dali");

        set("exits", ([
               "north"  :__DIR__"croad9",
		   "south" :__DIR__"croad7",
                      ]));

             
        setup();
       replace_program(ROOM);
}

