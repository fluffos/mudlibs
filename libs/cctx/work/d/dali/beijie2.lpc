// Room: /d/dali/beijie2.c
// By JackyBoy 98.10.19
inherit ROOM;

void create() {
  set("short", "北街");
  set("long", @LONG
再往南走几步就到大理城的中心广场了，往北则是一些富丽堂皇的宫
殿，是大理南诏的皇宫和王府，远远地还可以看到有名的大理三塔稍稍露
了个塔尖出来。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "beiguangchang",
    "south": __DIR__ "beijie1",
  ]));
  set("outdoors", "dali");

  setup();
}
