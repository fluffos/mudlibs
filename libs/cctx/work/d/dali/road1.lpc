// /d/dali/road1
// netkill /98/8/14/


inherit ROOM;

void create()
{
        set("short",  "崎岖山路" );
        set("long", @LONG
你走在一条崎岖的山路上，路沿山势而走，山却越来越险峻，有
的地方路仅能过一人，你不由得小心翼翼，一步一补的走。

LONG
        );
        set("outdoors", "dali");

        set("exits", ([
                "east" : __DIR__"changting", 
                "southwest"  :__DIR__"road2",
                
                
        ]));

             
        setup();
        replace_program(ROOM);
}
