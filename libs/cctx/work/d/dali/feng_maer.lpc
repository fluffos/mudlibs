// /d/dali/feng_maer.c 马耳峰
// jackyboy /98/11/8

inherit ROOM;

void create()
{
	set("short",  "马耳峰" );
	set("long", @LONG
这里是大理苍山十九峰的马耳峰山顶，南北两边各有一条小溪喧闹
着向山下流去，正是苍山十八溪之一的阳南溪和葶溟溪。
LONG
);
	set("outdoors", "dali");

	set("exits", ([
		"southdown" : __DIR__"froad2",
		"northdown" : __DIR__"xi_tingming",
	]));
	setup();
	replace_program(ROOM);
}
