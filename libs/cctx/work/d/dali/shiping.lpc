// Room: /d/dali/shiping.c
// By JackyBoy 98/12/3
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "石坪");
  set("long", @LONG
这是一块平地，整个地面实际就是一块巨大的石头，地面显得特别
光滑。南边立着一块石碑(bei)。西北就是猫头山的山顶了。往西是下
山的小路。
LONG
  );
  set("outdoors", "dali");
  set("exits", ([
    "northwest": __DIR__ "maotoushan",
    "westdown": __DIR__ "xroad1",
  ]));
  set("item_desc", ([
    "bei": HIY + "无量剑派比武场" + NOR + "\n",
  ]));
  setup();
}
