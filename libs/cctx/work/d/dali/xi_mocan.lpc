// /d/dali/xi_mocan.c 莫残溪
// jackyboy /98/11/9

inherit ROOM;

void create()
{
	set("short",  "莫残溪" );
	set("long", @LONG
这里是大理苍山十八溪的葶溟溪，溪水清澈透底，正哗哗地流向山
下注入洱海，南边抬头就是十九峰之一的佛顶峰，北边则是圣应峰，峰
顶下还有一座寺院，不时传出阵阵钟声。
LONG
);
	set("outdoors", "dali");

	set("exits", ([
		"southup" : __DIR__"feng_foding",
		"northwest" : __DIR__"gantongsi",
	]));
	setup();
	replace_program(ROOM);
}

