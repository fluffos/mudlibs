// /d/dali/chouduanpu
// YZC 1995/12/04 

inherit ROOM;

void create() {
  set("short", "绸缎铺");
  set("long", @LONG
这是一家绸缎铺，各种绫罗绸缎挂满了铺面。胖胖的白掌柜正在为
客人们量尺码，这里同时也卖些农家穿的布衣，摊上立着一块招牌(pai)。
LONG
  );

  set("item_desc", ([
    "pai": "请用 \"list\" 列出货物表，\"buy\" 向老板购物。\n",
  ]));
  set("exits", ([
    "south": __DIR__ "dongjie1",
  ]));
  set("objects", ([
    __DIR__ "npc/bai": 1,
  ]));

  setup();
  replace_program(ROOM);
}
