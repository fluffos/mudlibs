// /d/dali/feng_sanyang.c 三阳峰
// jackyboy /98/11/9

inherit ROOM;

void create()
{
	set("short",  "三阳峰" );
	set("long", @LONG
这里是大理苍山十九峰三阳峰的山顶，南北两边各有一条小溪喧闹
着向山下流去，正是苍山十八溪之一的灵泉溪和白石溪。
LONG
);
	set("outdoors", "dali");

	set("exits", ([
		"southdown" : __DIR__"xi_baishi",
		"northdown" : __DIR__"xi_lingquan",
	]));
	setup();
	replace_program(ROOM);
}