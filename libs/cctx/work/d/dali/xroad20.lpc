// Room: /d/dali/xroad20.c
// By JackyBoy 98/12/7
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "藤桥下");
  set("long", @LONG
这里往东过了藤桥就进入无量山区了，路旁的杂草中依稀有一块石
碑(bei)。
LONG
  );
  set("outdoors", "dali");
  set("exits", ([
    "north": __DIR__ "xroad21",
    "eastup": __DIR__ "xroad19",
  ]));
  set("item_desc", ([
    "bei": HIY "善人渡" NOR "\n",
  ]));
  setup();
  replace_program(ROOM);
}
