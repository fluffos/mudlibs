// /d/dali/xueshan
// netkill /98/8/14/

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIG "山路" NOR);
  set("long", @LONG
一条山路，坎坷不平，向上去就是的雪山了，没有十足体力，是翻不
过雪山的，你趁早回头吧，下了山，就进入了四川平原了。
LONG
  );
  set("outdoors", "dali");

  set("exits", ([
    "northdown": __DIR__ "croad2",
    "southup": __DIR__ "xueshan11",
  ]));


  setup();
}
