// Room: /d/dali/xroad19.c
// By JackyBoy 98/12/7
#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "西桥头");
	set("long", @LONG
这是藤桥的西桥头，摆着几个石凳，好让行人在这里歇口气。这里
既能听江水拍打堤岸的响声，又能仰视群山的秀美，竟也是个不错的赏
景地点。
LONG
);
	set("outdoors","dali");
	set("exits",([
		"westdown" : __DIR__"xroad20",
		"east" : __DIR__"xroad18",
	]));
	setup();
	replace_program(ROOM);
}
