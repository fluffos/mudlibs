// Room: /d/dali/hudiequan.c
// By JackyBoy 98.10.19

inherit ROOM;

void create() {
  set("short", "蝴蝶泉");
  set("long", @LONG
这里就是大理闻名天下的蝴蝶泉了，在白族每年的三月街节时，这
里都会聚集很多各种各样的蝴蝶和飞蛾。这里树阴遮天，特别清凉，泉
水也特别爽口，听说经常喝可以延年益寿，所以每天总是有很多白族人
家到这儿来挑水。
LONG
  );
  set("outdoors", "dali");
  set("resource/water", 1);
  set("exits", ([
    "southeast": __DIR__ "nroad6",
  ]));
  set("objects", ([
    __DIR__ "npc/hudie": 12,
  ]));
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
