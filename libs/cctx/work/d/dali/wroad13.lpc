// /d/dali/wroad13.c
// Jackyboy 98/12/15

inherit ROOM;

void create()
{
	set("short",  "峡谷西口" );
	set("long", @LONG
这是一条特别狭窄的峡谷，两边分别是无量山和大理苍山。小路弯
来弯去的，根本无法看到峡谷的两头，再往东些就是劫匪出没的地方，
得小心些!
LONG
);
	set("outdoors", "dali");
	set("exits", ([
		"east" : __DIR__"wroad12",
		"west" :__DIR__"wroad14",
	]));

	setup();
	replace_program(ROOM);
}
