// Room: /d/dali/jiulou1.c

inherit ROOM;

void create()
{
	set("short", "香格里拉酒楼");
	set("long", @LONG
这是一间久负盛名的酒楼，大理城内的名人贵爵都时常来这里饮酒，
由于大理国内民风淳朴，王爷与百姓同乐也是常见的事，所以就不足为
奇怪了。
LONG
	);
	set("valid_startroom", "1");
	set("exits", ([ /* sizeof() == 1 */
		"up" : "/u/jackyboy/guestroom",
		"north" : __DIR__"xijie",
	]));
	set("objects",([
		__DIR__"npc/waiter":1,
	]));
	set("no_clean_up", 0);

	setup();
	call_other(__DIR__"board/jiulou_b", "???");
	replace_program(ROOM);
}
