// Room /d/dali/nroad5.c
// By JackyBoy 98.10.19
inherit ROOM;
void create()
{
	set("short","小路");
	set("long",@LONG
这是一条铺满了大理石的小路，沿着洱海边弯弯曲曲地向北延伸，
再往北走可以到达蝴蝶泉，往南则可以回到大理城内。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"south" : __DIR__"nroad4",
		"north" : __DIR__"qiao2",
	]));
	set("no_clean_up", 0);
	set("outdoors","dali");
	setup();
	replace_program(ROOM);
}
