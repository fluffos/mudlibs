// Room: /d/hangzhou/jiulou
// Date:  netkill    98/08/18

inherit ROOM;

void create() {
  set("short", "酒楼");
  set("long", @LONG
这里酒楼的二楼，几张八仙桌擦的十分干净，许多书生秀才都喜欢
在这里喝酒吟诗，从窗外看去，可以看到西湖的美景，还有什么比这个
更下酒的呢？
LONG
  );


  set("exits", ([
    "down": __DIR__ "jiujia",
  ]));
  set("objects", ([
    __DIR__ "npc/hansi": 1,
    __DIR__ "npc/shusheng": 1,
  ]));
  setup();
  replace_program(ROOM);
}
