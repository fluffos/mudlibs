// Room: /d/hangzhou/jiulou
// Date:  netkill    98/08/18

inherit ROOM;

void create() {
  set("short", "酒楼");
  set("long", @LONG
这里原来是一间比较红火的酒楼，可是如今却门可罗雀，只有
一名店小二不愿离去，在此守着，偷偷的卖些东西，一道楼梯通向
二楼，可是如今却被木板(muban)订的死死的。
LONG
  );


  set("exits", ([
    "west": __DIR__ "nanjie1",
  ]));
  set("objects", ([
    __DIR__ "npc/xiaoer": 1,
  ]));

  set("item_desc", ([
    "muban": "一块大木板,将楼道封的死死的，上面打着杭州知府的封条。\n"
  ]));

  setup();
  replace_program(ROOM);
}
