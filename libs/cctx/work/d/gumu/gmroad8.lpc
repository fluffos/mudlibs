// Room:/d/gumu/gmroad8.c
// Kayin@CuteRabbit 1999/3/12
inherit ROOM; 
 
void create() 
{ 
	set("short", "山涧小路");
	set("long", @LONG
这是一条狭窄的山涧小路，旁边悬崖陡峭，峭壁上采药的人
正从上顶用绳索挂下采药。
LONG
	);

	set("exits",([ 
  "southdown":__DIR__"gmroad7",
  "northup":__DIR__"gmroad9",
]));
	set("outdoors","gumu");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
