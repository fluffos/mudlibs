// Room:/d/gumu/chucanshi.c
// By Kayin @ CuteRabbit Studio 99-4-2 13:37 new 
#include <ansi.h> 
inherit ROOM; 
 
void create() 
{ 
	set("short",BLK "储藏室"NOR);
	set("long", @LONG
这是一间比较昏暗的小屋，里边只有一支烧到了底的蜡烛发出一丝微弱的光线。
地上放着一些乱七八糟的东西。
LONG
	);

	set("exits",([ 
  "east":__DIR__"guodao4",
]));
    set("objects", ([
        __DIR__"obj/dawan"  : 1,
        __DIR__"obj/dami":1,
    ]) );    


	set("no_clean_up", 0);

	setup();

}
