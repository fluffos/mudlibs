// Room: /d/gumu/gmroad5.c 
 
inherit ROOM; 
 
void create() 
{ 
	set("short", "终南山脚");
	set("long", @LONG
这是终南山脚,往西可看到长长的石阶通往赫赫有名的
重阳宫,朝西北小路则可走到终南山后山。
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
  "eastdown" : __DIR__"gmroad4.lpc",
  "northeast" : __DIR__"gmroad6.lpc",
  "westup" : "/d/chongyang/qzroad1.lpc",
]));
	set("outdoors","gumu");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
