// room:/d/gumu/xiaolu5.c
inherit ROOM; 
 
void create() 
{ 
	set("short", "山腰");
	set("long", @LONG
这是终南山的山腰,再往北走就可以到长安了.
LONG
	);

	set("exits",([ 
  "southup":__DIR__"xiaolu7",
  "northdown":__DIR__"xiaolu9",
]));
		
	set("outdoors","gumu");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
