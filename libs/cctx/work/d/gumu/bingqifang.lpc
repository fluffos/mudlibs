// Room:/d/gumu/guodao1.c
// By Kayin @ CuteRabbit Studio 99-4-2 new 
#include <ansi.h> 
inherit ROOM; 
 
void create() 
{ 
	set("short",BLK "兵器房"NOR);
	set("long", @LONG
这是古墓内存放兵器的地方，地上和墙上挂着好多兵器。
LONG
	);
    set("objects", ([
        "/clone/weapon/changjian" : random(2),
        __DIR__"obj/changbian"  : random(2),
        "/clone/weapon/gangdao":random(2),
        __DIR__"obj/xiujian":1,
        __DIR__"obj/xiudao":1,
    ]) );    


	set("exits",([ 
  "east":__DIR__"guodao6",
]));
	setup();

}
