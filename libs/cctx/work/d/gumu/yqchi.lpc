// Room:/d/gumu/yqchi.c
// Kayin @ CuteRabbit 1999.3.25 new
#include <ansi.h>
inherit ROOM; 
string do_zhao();
void create() 
{ 
	set("short", "玉清池");
	set("long", @LONG
这是终南山间的一个水池，因为是地下水，水(shui)可以随时保持清澈明亮。
LONG
	);
        set("item_desc", ([
	"shui":"这水清澈见底,微风吹来水面上泛起层层水波.\n",
	]));

	set("exits",([ 
  "east":__DIR__"shanlu1",
]));
    set("resource/water", 1);
	set("outdoors","gumu");
	set("no_clean_up", 0);

	setup();
}
