// Room :/d/gumu/liangongfang3.c
// By Kayin @ CuteRabbit Studio 99-4-11 13:03 new
inherit ROOM;
#include <ansi.h> 
void create() 
{ 
	set("short", HIC"练功房"NOR);
	set("long", @LONG
这是一间很大的练功房，房中放着几个练功用的木人。
LONG
	);
	set("no_sleep_room",1);

	set("exits",([
  "north":__DIR__"guodao10-1",
]));
	set("objects",([
		__DIR__"npc/muren":8,
]));


	setup();
	replace_program(ROOM);
}