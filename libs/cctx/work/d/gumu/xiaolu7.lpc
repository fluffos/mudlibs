// room:/d/gumu/xiaolu5.c
inherit ROOM; 
 
void create() 
{ 
	set("short", "林间小道");
	set("long", @LONG
你走在丛林深处,周围都是苍天大树把太阳光全遮住了.
你不时可以看到一些小鸟停在枝头.
LONG
	);

	set("exits",([ 
  "south":__DIR__"xiaolu6",
  "northdown":__DIR__"xiaolu8",
]));
		
	set("outdoors","gumu");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
