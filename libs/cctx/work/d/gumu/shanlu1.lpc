// Room:/d/gumu/shanlu1.c
// Kayin @ CuteRabbit 1999/3/12
inherit ROOM; 
 
void create() 
{ 
	set("short", "小山路");
	set("long", @LONG
你费了一番力气爬上了山.在山边有一间小屋,屋门前有一名
妇人正在喂鸡.
LONG
	);

	set("exits",([ 
  "eastdown":__DIR__"gmroad9",
  "northeast":__DIR__"shanlu2",
  "west":__DIR__"yqchi",
]));
	set("objects", ([
		__DIR__"npc/shaofu":1]));

	set("outdoors","gumu");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}

