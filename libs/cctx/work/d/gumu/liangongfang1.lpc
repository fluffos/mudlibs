// Room :/d/gumu/liangongfang1.c
// By Kayin @ CuteRabbit Studio 98-4-11 13:03 new
inherit ROOM;
#include <ansi.h> 
void create() 
{ 
	set("short", HIC"练功房"NOR);
	set("long", @LONG
这是一间很大的练功房，房中放着几个练功用的木人。
LONG
	);
	set("no_sleep",1);

	set("exits",([
  "south":__DIR__"guodao7",
]));
	set("objects",([
		__DIR__"npc/muren":8,
]));


	setup();
}