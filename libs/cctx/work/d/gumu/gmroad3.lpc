// Room: /d/gumu/gmroad3.c 

inherit ROOM;

void create() {
  set("short", "碎石路");
  set("long", @LONG
这是通往终南山的毕经之路。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "southwest": __DIR__ "gmroad4",
    "southeast": __DIR__ "gmroad2.lpc",
  ]));
  set("objects", ([
    CLASS_D("gumu") + "/limochou": 1,
  ]));
  set("outdoors", "gumu");
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
