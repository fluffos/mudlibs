inherit ROOM;

void create() {
  set("short", "峭壁");
  set("long", @LONG
你终于看到了那座先看不清的峭壁了,好大呀, 
你不由得使劲再向前走去, 
LONG
  );

  set("exits", ([
    "west": __DIR__ "road10.lpc",
    "east": __DIR__ "road12.lpc",


  ]));
  // set("objects", ([
  //             "/d/shenlong/npc/xuxuetin": 1,
  // ]));
  set("outdoors", "shenlong");
  setup();
}
