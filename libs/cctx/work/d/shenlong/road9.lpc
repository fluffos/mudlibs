inherit ROOM;

void create()
{
    set("short", "东石板道");
    set("long", @LONG
这已是后岛的石板道,岛上的商业区全在这一片,
由于是远离中土的缘故,这儿的食住行高得离谱,不趁个
千儿八百的,你还是不要去了吧.
LONG
    );

    set("exits", ([
                               "east" : __DIR__"road10",
                             "west" : __DIR__"road8",
   
       ]));
   // set("objects", ([
   //             "/d/shenlong/npc/xuxuetin": 1,
   // ]));
    set("outdoors", "shenlong");
    setup();
   replace_program(ROOM); }