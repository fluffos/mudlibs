inherit ROOM;

void create()
{
    set("short", "东石板道");
    set("long", @LONG
东方远处有座高高的山涯,象座屏风一样,隔住了刮来的海风
所以这儿格外的温暖,从南边吹来了阵阵的菜香,神龙的海鲜可是
有名的哟,你不禁有点肚子饿了,
LONG
    );

    set("exits", ([
                               "east" : __DIR__"road11",
                             "west" : __DIR__"road9",
                                                 "south" : __DIR__"xroad1",
       ]));
   // set("objects", ([
   //             "/d/shenlong/npc/xuxuetin": 1,
   // ]));
    set("outdoors", "shenlong");
    setup();
   replace_program(ROOM); }