inherit ROOM;

void create()
{
    set("short", "走道");
    set("long", @LONG
下面就是精进二阁,虽然这是神龙岛刚修不久的练武场
但是地上铺的竹片已经被踩得有点光滑了,神龙弟子的
呼喝声,跳跃声,隐隐从下面传来
LONG
    );

    set("exits", ([
                "northup" : __DIR__"lgf1",
                "southdown" : __DIR__"lgf2",
    ]));
   // set("objects", ([
   //            "/d/shenlong/npc/xuxuetin": 1,
   // ]));
    set("outdoors", "shenlong");
    setup();
   replace_program(ROOM); }