inherit ROOM;

void create() {
  set("short", "大道");
  set("long", @LONG
这里就要到神龙岛的腹地了,一路上的鸟语花香
突然没有了,从东边过来的人,也看起来有点严肃,空气
真的有点沉重了.
LONG
  );

  set("exits", ([
    "southwest": __DIR__ "road5",
    "east": __DIR__ "road7",
  ]));
  // set("objects", ([
  //             "/d/shenlong/npc/xuxuetin": 1,
  // ]));
  set("outdoors", "shenlong");
  setup();
}
