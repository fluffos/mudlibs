inherit ROOM;

void create() {
  set("short", "小岔街");
  set("long", @LONG
前面不远处就是神龙的内港--小码头了,街面上
到这儿就人流不多了,是不是想到小码头上找找看有没
有赶海回来的渔船呢???东边是家药铺
LONG
  );

  set("exits", ([
    "east": __DIR__ "yaopu",
    "south": __DIR__ "xroad4",
    "north": __DIR__ "xroad2",
  ]));
  // set("objects", ([
  //             "/d/shenlong/npc/xuxuetin": 1,
  // ]));
  set("outdoors", "shenlong");
  setup();
  replace_program(ROOM);
}
