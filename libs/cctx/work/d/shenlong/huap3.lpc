inherit ROOM;

void create()
{
    set("short", "花圃前院");
    set("long", @LONG
你走进这个巨大的花圃,天那,好多花呀,红的,绿的,
各式各样的花朵在争奇斗艳,让你看得眼花缭乱,不知所措
,由于这儿四季有日照,鲜花可开长年,据说只是远在西南
的昆明可以和这儿比哟,
LONG
    );

    set("exits", ([
                "northup" : __DIR__"huap2",
                "southdown" : __DIR__"huapu",
                          
   
       ]));
   // set("objects", ([
   //             "/d/shenlong/npc/xuxuetin": 1,
   // ]));
    set("outdoors", "shenlong");
    setup();
   replace_program(ROOM); }