inherit ROOM;

void create()
{
    set("short", "小码头");
    set("long", @LONG
你刚走进来的时候,有点不适应这儿的黑暗,背街
的小岔街到这儿是尽头了,东面就是一望无际的大海了
天那,原来这儿有很多人,看见你,慢慢的围了上来,不
好,快跑吧.

LONG
    );

    set("exits", ([
                             "west" : __DIR__"xroad4.lpc",
                                    ]));
   // set("objects", ([
   //             "/d/shenlong/npc/xuxuetin": 1,
   // ]));
    set("outdoors", "shenlong");
    setup();
   replace_program(ROOM); }