inherit ROOM;

void create() {
  set("short", "小岔街");
  set("long", @LONG
这儿的住户大多是渔民,按道理应该是比较豪爽的
但却家家门户紧闭,不知道是什么意思,东边一家铺子
的门上挂个块阴丹蓝的布,套在白圈里有个斗大的字
--"当"
LONG
  );

  set("exits", ([
    "east": __DIR__ "dangpu",
    "south": __DIR__ "xroad3",
    "north": __DIR__ "xroad1",
  ]));
  // set("objects", ([
  //             "/d/shenlong/npc/xuxuetin": 1,
  // ]));
  set("outdoors", "shenlong");
  setup();
  replace_program(ROOM);
}
