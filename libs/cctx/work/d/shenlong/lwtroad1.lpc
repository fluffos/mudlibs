inherit ROOM;

void create()
{
        set("short", "沙滩小道");
        set("long", @LONG
这是条建在沙滩上的小道,是用贝壳拼起来的中间有些菊花,太
阳等美丽的图案。 
LONG
        );

        set("exits", ([
                "south" : __DIR__"matou",
                      "northup" : __DIR__"lwtroad2",
]));

       // set("objects", ([
       //       __DIR__"/d/shenlong/npc/flowerg":2,
       //         "/d/shenlong/npc/chuanfu" :1,
       // ]));

        
        setup();
        replace_program(ROOM);
}
