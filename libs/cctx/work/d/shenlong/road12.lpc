inherit ROOM;

void create() {
  set("short", "峭壁");
  set("long", @LONG
你终于来到了这座石屏风下面,首先看到的就是几个大字
<<鹿回头>>,此石高约百丈,顶上影影看得清个小鹿的影
子,连鹿都回头了,你也回头吧
LONG
  );

  set("exits", ([
    "west": __DIR__ "road11.lpc",


  ]));
  // set("objects", ([
  //             "/d/shenlong/npc/xuxuetin": 1,
  // ]));
  set("outdoors", "shenlong");
  setup();
  replace_program(ROOM);
}
