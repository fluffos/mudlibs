inherit ROOM;

void init();
void close_men();
int do_open(string);
int do_close(string);


void create() {
  set("short", "休息室");
  set("long", @LONG
 这就是神龙的来客休息室了,许多刚下船的游客
由于不适应这么颠簸的路途,所以休息一下,不过条件虽
然差点,但是床还是这么大,枕头还是这么软,你不由得打
了个哈欠,阿..............
LONG
  );
  set("exits", ([
    "north": __DIR__ "sleeproad1",

  ]));
  set("sleep_room", 1);
  set("no_fight", 1);

  setup();
}
