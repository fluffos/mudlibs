inherit ROOM;

void create()
{
    set("short", "山间小道");
    set("long", @LONG
这条小道,足下用细竹铺起了一条细径,走起来并不
太费力,但这小道是因地势而起,让人觉得好崎岖.
LONG
    );

    set("exits", ([
                "northup" : __DIR__"hiroad3",
                "southdown" : __DIR__"hiroad1",
                            
   
       ]));
   // set("objects", ([
   //             "/d/shenlong/npc/xuxuetin": 1,
   // ]));
    set("outdoors", "shenlong");
    setup();
   replace_program(ROOM); }