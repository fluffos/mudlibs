inherit ROOM;

void create()
{
    set("short", "大道");
    set("long", @LONG
石板大道,东北方向有个小小的山丘,山上环饶在翠绿的竹子
当中仿佛有几座零星的小屋,你揉揉眼睛,好象有点花了,

LONG
    );

    set("exits", ([
                "northeast" : __DIR__"road6",
                "southwest" : __DIR__"road4",
    ]));
   // set("objects", ([
   //             "/d/shenlong/npc/xuxuetin": 1,
   // ]));
    set("outdoors", "shenlong");
    setup();
   replace_program(ROOM); }