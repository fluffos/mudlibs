inherit ROOM;

void create() {
  set("short", "山间小道");
  set("long", @LONG
这里是上山的小径,其实这也不能算是山,只是海
岛上的土包,围径二百余丈,上面种满了各种奇花异草,
各自竟芳,你发现,好多花你都叫不出来名字.

LONG
  );

  set("exits", ([
    "southdown": __DIR__ "road8",
    "northup": __DIR__ "hiroad2",


  ]));
  // set("objects", ([
  //             "/d/shenlong/npc/xuxuetin": 1,
  // ]));
  set("outdoors", "shenlong");
  setup();
}
