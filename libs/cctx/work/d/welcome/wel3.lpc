// Room: /d/hangzhou/dajiie1
// Date:  netkill    98/08/18


inherit ROOM;

void create()
{
    set("short",  "欢迎光临驰骋天下" );
    set("long", @LONG
如果你想加入某个门派，可以先拜(bai)个师傅，你可以跟他学习(xue)
也可以通过其他方式，如读书(du)(study),也可以向同门的师兄弟讨教。
LONG
    );
	

        set("exits", ([
               	"south":__DIR__"welx",
		"west" :__DIR__"wel1",
        ]));
	set("objects",([
		__DIR__"npc/jieyinshi3":1,
		]));
  	set("no_fight", 1);
	
      setup();
	replace_program(ROOM);
}


