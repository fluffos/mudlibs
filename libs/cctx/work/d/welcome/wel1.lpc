// Room: /d/hangzhou/dajiie1
// Date:  netkill    98/08/18


inherit ROOM;

void create() {
  set("short", "欢迎光临驰骋天下");
  set("long", @LONG
你混混沌沌的来到了一个陌生的世界，你想看看你在哪里(l, look),
你想知道这个世界里都有谁(who),或许你要四处走走，(w,s,n,e,nw,ne,
,sw,se,d,u,sd,nd,wd,ed,wu,nu,eu,su)，不过你得先看看都有那些出口。
    新手请用new加空格，然后再加上要说的话来聊天。
LONG
  );
  set("exits", ([
    "east": __DIR__ "wel2",
    "south": __DIR__ "welx",

  ]));
  set("objects", ([
    __DIR__ "npc/jieyinshi": 1,
  ]));
  set("no_fight", 1);
  set("valid_startroom", 1);
  setup();
}
