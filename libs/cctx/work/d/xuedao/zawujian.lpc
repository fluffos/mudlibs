// damen 血刀门
// by congw
inherit ROOM;
#include <ansi.h>

void create()
{
	set("short",HIW "杂屋间" NOR);
	set("long", 
"这里乱七八糟的放着些木材，钉子，菜刀什么的，是用来放杂物的地方。\n"
	);

	set("exits", ([
		"west" : __DIR__"tzfang",
	]));
			setup();
	replace_program(ROOM);

}

