// damen 血刀门
// by congw
inherit ROOM;
#include <ansi.h>

void create()
{
	set("short",HIW "猪圈" NOR);
	set("long", 
"这里横七竖八的躺着些从山下打来的野猪，不时乱叫两声，满屋散发出难闻\n"
"的猪大便味和霉变的味道。\n"
	);

	set("exits", ([
		"east" : __DIR__"tzfang",
	]));
			setup();
	replace_program(ROOM);

}

