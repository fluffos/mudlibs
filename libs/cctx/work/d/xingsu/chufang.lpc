// Room: /d/xingsu/raod1.c
// by netkill 98/10/16

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", HIG "厨房" NOR);
        set("long", @LONG
这是一间厨房，一位厨子正在为丁春秋做精美的点心，一边放着些羊
肉清水。看来是给弟子们准备的，至于那些好吃的东西，就别奢望了。
LONG
        );
        	set("exits", ([
  		"north" : __DIR__"biwuting",
		"west" : __DIR__"e_changlang2",
			]));
        
		set("no_clean_up", 0);
        //	set("outdoors", "xingxiuhai");
		set("objects", ([
                __DIR__"npc/obj/yangrou.lpc" : 3,
		   __DIR__"obj/hulu"  : 2,

]));


        	setup();
        	replace_program(ROOM);
}
