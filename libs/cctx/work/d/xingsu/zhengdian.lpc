// Room: /d/xingsu/raod1.c
// by netkill 98/10/16

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIG "正殿" NOR);
  set("long", @LONG
这是一间宏伟的正殿，可谓是美奂绝伦，老怪丁春秋平时就在这里接
受星宿弟子门的顶礼膜拜，歌颂之声经常从这里传遍整个星宿派，可谓是
江湖之中，无人能及。
LONG
  );
  set("exits", ([
    "north": __DIR__ "houyuan",
    //"west" : __DIR__"dating",
    //"east" : __DIR__"e_changlang2",
    "south": __DIR__ "guangchang",
  ]));

  set("no_clean_up", 0);
  //	set("outdoors", "xingxiuhai");
  set("objects", ([
    CLASS_D("xingxiu") + "/ding": 1,
    CLASS_D("xingxiu") + "/chuchen": 1,
  ]));
  setup();
  replace_program(ROOM);
}
