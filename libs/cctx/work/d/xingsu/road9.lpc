// Room: /d/xingsu/raod1.c
// by netkill 98/10/16

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "星宿海");
  set("long", @LONG
这里是星宿海边。说是海，其实是片湖泊和沼泽，地形十分险恶。四
面看去，仿佛是一片死寂的天地，没有植物，每没有生物，什么都没有，
前面好象有一个洞。
LONG
  );
  set("exits", ([
    "enter": __DIR__ "xiaoyao",
    "southeast": __DIR__ "road7",
    //	"northwest" :__DIR__"road9",
  ]));

  set("no_clean_up", 0);
  set("outdoors", "xingxiuhai");
  set("objects", ([
    __DIR__ "npc/caihua": 1,
  ]));
  setup();
  replace_program(ROOM);
}
