// Room: /d/xingsu/raod1.c
// by netkill 98/10/16

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIG "休息室" NOR);
  set("long", @LONG
一间普通的休息室，只有中间放了一张大床，看上去虽然是有点脏乱
可安静的环境，昏暗的光线，使你不由的也睡意浓浓。
LONG
  );

  set("no_fight", 1);
  set("sleep_room", 1);

  set("exits", ([
    //"north": __DIR__"xiuxishi",
    "east": __DIR__ "w_changlang3",
    //"west" : __DIR__"dating",
    "south": __DIR__ "liangongfang",
  ]));

  set("no_clean_up", 0);
  //	set("outdoors", "xingxiuhai");

  setup();
  replace_program(ROOM);
}
