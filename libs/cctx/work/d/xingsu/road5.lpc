// Room: /d/xingsu/raod1.c
// by netkill 98/10/16

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "小路");
  set("long", @LONG
一条通向天山的小路，路到了这里，渐渐的不平坦起来，远远看出去
可以见到前面连绵起伏的高山和山顶上皑皑的白雪，在阳光的照耀下发出
七彩的光芒，十分的漂亮。向北而上，那就是飘渺峰了。
LONG
  );
  set("exits", ([
    "northwest": __DIR__ "road6",
    "eastdown": __DIR__ "road4",
    "west": "/d/lingjiu/eexit",
  ]));

  set("no_clean_up", 0);
  set("outdoors", "xingxiuhai");

  setup();
  replace_program(ROOM);
}
