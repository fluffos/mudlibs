// Room: /d/xingsu/raod1.c
// by netkill 98/10/16

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "小路");
  set("long", @LONG
这是一条通向天山的小路，沿途只能见到一些野花草在顽强的生存着
顶着烈日开出一朵朵美丽的小花，天空中万里无云，晒的地上快要冒出火
来，一阵阵热浪袭来，另人发昏。
LONG
  );
  set("exits", ([
    "west": __DIR__ "road2",
    "east": "/d/jyguan/luzhou1",
  ]));

  set("no_clean_up", 0);
  set("outdoors", "xingxiuhai");

  setup();
  replace_program(ROOM);
}
