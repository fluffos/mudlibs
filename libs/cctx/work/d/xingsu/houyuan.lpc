// Room: /d/xingsu/raod1.c
// by netkill 98/10/16

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIG "后花园" NOR);
  set("long", @LONG
在这个荒芜的星宿海，居然有这样一个花园，另人吃惊不已，这些花
草，都是弟子们为了奉承丁春秋而从各地弄来的，虽然不是什么名花异草
却都不是平常的草木，西面有道小门通向天山脚下。
LONG
  );
  set("exits", ([
    //"north": __DIR__"houyuan",
    "west": __DIR__ "tianroad3",
    //"east" : __DIR__"e_changlang2",
    "south": __DIR__ "zhengdian",
  ]));

  set("no_clean_up", 0);
  //	set("outdoors", "xingxiuhai");

  setup();
  replace_program(ROOM);
}
