// Room: /d/xingsu/raod1.c
// by netkill 98/10/16

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", "广场");
        set("long", @LONG
一个很大的广场，看样子是给星宿弟子练功用的，不过并没有什么人
来练。到是有几个人在这里晒太阳，聊天。
LONG
        );
        	set("exits", ([
  		"north": __DIR__"zhengdian",
		"east" : __DIR__"e_changlang3",
		"west" : __DIR__"w_changlang3",
		"south"  : __DIR__"dating",
			]));
        	
		set("no_clean_up", 0);
        	set("outdoors", "xingxiuhai");
		set("objects", ([
		__DIR__"npc/haoshou" : 1,
		__DIR__"npc/boshou"  : 1,
		__DIR__"npc/gushou"  : 1,
		CLASS_D("xingxiu") + "/zhaixing" : 1,
			]));
        	setup();
        	replace_program(ROOM);
}
