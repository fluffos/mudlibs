// Room: /d/xingsu/raod1.c
// by netkill 98/10/16

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIG "练功房" NOR);
  set("long", @LONG
这是一间普通的练功房，什么都没有，几个蒲团放在地上，一两个弟
子在地上打坐，北面是休息室。
LONG
  );
  set("exits", ([
    "north": __DIR__ "xiuxishi",
    //"east" : __DIR__"w_changlang1",
    //"west" : __DIR__"dating",
    "south": __DIR__ "w_changlang2",
  ]));

  set("no_clean_up", 0);
  //	set("outdoors", "xingxiuhai");

  setup();
  replace_program(ROOM);
}
