inherit ROOM;

void create()
{
    set("short", "书房");
    set("long", @LONG
这里是归云庄庄主陆乘风的书房，陆庄主一般也在这里接待来客。墙壁四周
挂了许多名家字画，一看就知道主人在这方面的造诣颇高。
LONG );
    set("exits", ([
    "east"  : __DIR__"houting",
    ]));
    set("objects", ([
                "/kungfu/class/taohua/luchenfeng" : 1,
                __DIR__"npc/puren" : 2,
        ]));
    setup();
    replace_program(ROOM);
}
