// liangongfang.c 练功房
// 山猫 21:04 99-1-19

inherit ROOM;

void create()
{
	set("short", "练功房");
	set("long", @LONG
	这里是练功房，地上摆放着几个练功用的木人，木人制作非常精
巧，象真人一般。几位桃花岛弟子正在这里练功。
LONG
	);
	set("exits", ([
		"west" : __DIR__"qianyuan1",
	]));

	set("objects", ([
          "/clone/npc/muren":5
]));

	setup();
	replace_program(ROOM);
}
