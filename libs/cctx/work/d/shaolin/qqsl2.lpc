// Room: /d/shaolin/qqsl2.c
// Date: YZC 98/04/14

inherit ROOM;




void create()
{
	set("short", "崎岖山路");
	set("long", @LONG
这里的地势已经变得陡峭难行，两边都是峭壁悬崖。高高的耸山笼罩
在云雾中，隐约可见。可是你的注意力都集中在仔细地町着自己的脚下，不
敢抬头多望一眼，惟恐一个不小心，摔了下去，那就。。。嘿嘿。。。！
LONG
	);

	set("exits", ([
		"down" : __DIR__"qqxl",
		"westup" : __DIR__"qqsl1",
	]));

        set("objects",([
                __DIR__"npc/xiong" : 3,
        ]));

	set("outdoors", "shaolin");
	setup();
        replace_program(ROOM);
}

