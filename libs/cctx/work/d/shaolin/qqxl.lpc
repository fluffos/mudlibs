// Room: /d/shaolin/qqxl.c
// Date: YZC 98/04/16

inherit ROOM;




void create() {
  set("short", "崎岖小路");
  set("long", @LONG
你走在一条崎岖难行的小路上，抬头隐约可见云雾笼罩中的耸山。这里
的野草长得差不多有人高了，把路都掩没了，你只有不停的拨开那些野草才
可以再向前走，突然你草丛里一阵乱响，原来惊动了正在睡觉的毒蛇。
LONG
  );

  set("exits", ([
    "up": __DIR__ "qqsl2",
    "northdown": __DIR__ "shanjiao",
  ]));

  set("objects", ([
    __DIR__ "npc/dushe": 5,
  ]));

  set("outdoors", "shaolin");
  setup();
  replace_program(ROOM);
}
