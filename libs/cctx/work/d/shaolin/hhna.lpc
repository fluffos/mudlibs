// Room: /d/shaolin/hhna.c
// Date: Hop 97.06.13

inherit ROOM;




void create() {
  set("short", "黄河南岸");
  set("long", @LONG
这里是黄河的南岸，面前那条水流喘急、汹涌澎湃大河就是闻名
天下的黄河了，岸边有个渡口，一只小船正停泊在那里等候渡客，遥
望北岸，似乎有条大道，但是不知道是通向什么地方。
LONG
  );

  set("exits", ([
    "southwest": __DIR__ "guandu",
  ]));

  set("objects", ([
    __DIR__ "npc/chuanfu1": 1,
  ]));

  set("outdoors", "shaolin");
  setup();
  replace_program(ROOM);
}
