// jiasha.c 黄色袈裟


#include <armor.h>
#include <ansi.h>

inherit CLOTH;

void create() {
  set_name(HIR "袈裟" NOR, ({ "jiasha", "jia sha" }));
  set_weight(5000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "件");
    set("material", "cloth");
    set("armor_prop/armor", 60);
    set("value", 100);
  }
  setup();
}
