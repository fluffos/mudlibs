// Room: /d/shaolin/laofang.c
// by godzilla 99.03.04

#include <room.h>      
inherit ROOM;

void create()
{
	set("short", "树洞底部");
	set("long", @LONG
这是树洞的底部，四周一团漆黑，借助从树外透下的点点亮光
隐约的看见里面坐着一为老人
LONG
	);
	set("exits", ([ 
      "up" : __DIR__"fumoquan",
    ]));
	set("no_clean_up", 0);
	set("objects", ([ 
          __DIR__"npc/xiexun" : 1,
]));
        create_door("up","石板","down",DOOR_CLOSED);
	setup();
	replace_program(ROOM);
}
