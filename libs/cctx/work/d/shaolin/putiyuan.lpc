//Room :/d/nshaolin/putiyuan

inherit ROOM;

void create()
{
  set ("short", "菩提塔");
  set ("long", @LONG
这里是少林菩提塔，虽然塔身已经石迹斑驳，但是仍然耸立挺拔
。塔顶供奉着菩提子，乃少林圣物。塔前有来往僧兵守护着。
LONG);
set("exits", ([ 
              "east": __DIR__"zhulin1",
]));
set("objects", ([
                __DIR__"npc/seng-bing4" :2, 
                __DIR__"npc/huta-seng": 1,

]) );
        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}
