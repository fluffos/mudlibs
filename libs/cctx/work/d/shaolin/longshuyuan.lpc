#include <ansi.h>
inherit ROOM;

void create()
{
        set("short",WHT"龙树院"NOR);
        set("long",@LONG
这是进入武器库的必经之路。往远望去，可见后寺中亭台楼阁规
模宏大，构筑精丽，宅地连云，气势磅礴，从建筑上看也不愧为天下
第一大门派。
LONG
        );
	set("outdoors","shaolin");
        set("exits", ([
		"east": __DIR__"zhulin3",    			
	        "southup" : __DIR__"wuqiku",
		"northup" : __DIR__"fangjuku",
	]));
        set("objects",([
                __DIR__"npc/seng-bing4" : 4,
           ]));
        setup();
        replace_program(ROOM);
}
