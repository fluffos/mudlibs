//Room: /d/nshaolin/celang
inherit ROOM;
#include <room.h>

void create() {

  set("short", "侧廊");
  set("long", @LONG
虽只是一墙之隔，堂前的喧哗却一点也听不见了。左右都是竹林
，微风吹过，林中发出哗哗的响声。廊边的木柱显得相当陈旧，却一
尘不染，也许有人时常打扫。弯弯曲曲的侧廊通向韦驮王殿。

LONG
  );


  set("exits",
    ([
      "north": __DIR__ "piandian1",
      "northeast": __DIR__ "daxiong",
      "west": __DIR__ "weituo",

    ]));

  set("outdoors", "shaolin");

  setup();

  create_door("west", "木门", "east", DOOR_CLOSED);
}
