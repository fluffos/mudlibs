// Room: /d/shaolin/shanjiao.c
// Date: YZC 98/04/16

inherit ROOM;




void create() {
  set("short", "山脚");
  set("long", @LONG
小路仿佛已经到了尽头，原来已经到了少室山的后山山脚。再向前走就
只有一条崎岖难行的山路可以通向山里，一阵一阵的山风扑面而来，使人不
自觉的打了个寒战。
LONG
  );

  set("exits", ([
    "southup": __DIR__ "qqxl",
    "north": __DIR__ "xiaolu1",
  ]));

  set("outdoors", "shaolin");
  setup();
  replace_program(ROOM);
}
