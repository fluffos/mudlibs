// Room: /d/shaolin/shuitan3.c


inherit ROOM;

void create()
{
	set("short", "碧水神潭底");
	set("long", @LONG
这里的水已经开始缓了而且变得明亮起来，你知道终于找到出口
了。
LONG
	);
	set("exits", ([
		"up" : __DIR__"shuitan",
	]));

	set("outdoors", "shaolin");
	setup();
}

