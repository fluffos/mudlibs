// Room: /city/yangliudao
// netkill /98/8/18/

#include <ansi.h>
inherit ROOM;


void create()
{
	set("short", GRN "杨柳道" NOR 	);
	set("long", @LONG
你走在一条杨柳道上，两旁是青青的杨柳，随风摇摆。东望而去
太湖已清楚可见，一路上游山玩水的人来来往往，轻摇蒲扇，慢渡兰
舟，好不逍遥。
LONG
	);
        set("outdoors", "city");

	set("exits", ([
		"southeast" : __DIR__"yangliudao2",
		"west" : __DIR__"yangliudao",
	]));

	
	setup();
	replace_program(ROOM);
}

