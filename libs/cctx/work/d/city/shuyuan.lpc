// Room: /d/city/shuyuan.c

inherit ROOM;

void create() {
  set("short", "书院");
  set("long", @LONG
这里是书院的讲堂，窗明几净，一尘不染。这里曾经有位名震天下大儒
朱熹，多少武林人士都来找他学读书写字，可如今却人去楼空，不见了，听
说是被官俯抓起来了，也不知道是犯了什么罪，好象是什么文字狱之类的吧，
只有那张学规(xuegui)还贴在那里。
LONG
  );
  set("item_desc", ([ /* sizeof() == 1 */
    "xuegui": "日讲经书三起，日看纲目数页。
通晓时务物理，参读古文诗赋。
读书必须过笔，会课按时蚤完。
夜读仍戒晏起，疑误定要力争。


",
  ]));
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "dongdajie1",
  ]));

  set("no_clean_up", 0);

  setup();
}
