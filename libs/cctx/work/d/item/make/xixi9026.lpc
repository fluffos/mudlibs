// ITEM Made by player /d/item/make/xixi9026.c
// Written by ITEMMAKER(Doing Lu 1998/11/2)	Sat Dec 26 00:32:19 1998
#include <ansi.h>

#include <weapon.h>

inherit SWORD;

string query_autoload() { return "1"; }

void autoload(string param) {}

void create() {
  set_name("xixi's[2;37;0m", ({ "君子剑sword" }));
  set_weight(3000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("item_make", 1);
    set("unit", "把");
    set(
      "long",
      "这是由[33m黑珍石[2;37;0m炼制而成的一把剑。\n剑柄上刻着一行小字：笑嘻嘻(xixi)\n这是传说中的情人剑，和淑女剑合壁后，威力极大。[2;37;0m\n"
    );
    set("value", 3750000);
    set("materail", "[33m黑珍石[2;37;0m");
    set("wield_msg", "$N一声清啸，抖出一把xixi's[2;37;0m。\n");
    set("unwield_msg", "$N随手一抹，收起了xixi's[2;37;0m。\n");
  }
  init_sword(50);
  setup();
}
