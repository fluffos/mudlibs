// ITEM Made by player /d/item/make/wus9916.c
// Written by ITEMMAKER(Doing Lu 1998/11/2)	Thu Mar 11 23:50:30 1999
#include <ansi.h>

#include <weapon.h>

inherit SWORD;

string query_autoload() { return "1"; }

void autoload(string param) {}

void create() {
  set_name("黑情剑[2;37;0m", ({ "hqj" }));
  set_weight(3000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("item_make", 1);
    set("unit", "把");
    set(
      "long",
      "这是由[33m黑珍石[2;37;0m辅以[1;33m汞丹母[2;37;0m炼制而成的一把剑\n剑柄上刻着一行小字：无生(wus)\n暗谈无光[2;37;0m\n"
    );
    set("value", 3750000);
    set("materail", "[33m黑珍石[2;37;0m");
    set("wield_msg", "一道电光闪过，助人消灾解难[2;37;0m\n");
    set("unwield_msg", "kickgodzilla[2;37;0m\n");
  }
  init_sword(70);
  setup();
}
