// ITEM Made by player /d/item/make/xfeng6569.c
// Written by ITEMMAKER(Doing Lu 1998/11/2)	Mon Feb 15 12:33:17 1999
#include <ansi.h>

#include <weapon.h>

inherit SWORD;

string query_autoload() { return "1"; }

void autoload(string param) {}

void create() {
  set_name("天龙剑[2;37;0m", ({ "sword" }));
  set_weight(100);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("item_make", 1);
    set("unit", "把");
    set("long", "这是由炼制而成的一把剑。\n剑柄上刻着一行小字：晓风(xfeng)\n\n");
    set("value", 0);
    set("materail", "");
    set("wield_msg", "$N一声清啸，抖出一把天龙剑[2;37;0m。\n");
    set("unwield_msg", "$N随手一抹，收起了天龙剑[2;37;0m。\n");
  }
  init_sword(0);
  setup();
}
