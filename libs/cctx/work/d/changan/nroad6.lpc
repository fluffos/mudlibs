// Room: /d/changan/nroad6.c
// Date: inca 98/08/20

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", "山脚");
        set("long", @LONG
你来到太行山脉的山脚下。
LONG
        );
        set("outdoors", "city2");

        set("exits", ([
    "east"   : "/d/city2/wroad4",
                "southeast" : __DIR__"nroad5",
        ]));

        setup();
        replace_program(ROOM);
}

