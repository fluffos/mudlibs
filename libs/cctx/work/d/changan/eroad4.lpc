// Room: /d/changan/nroad4.c
// Date: inca 98/08/20

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short","大道");
        set("long", @LONG
你走在一条大道上。这是一条普通的大道，周围一马平川，几乎没有行人，
只是偶尔有一匹快马飞驰而过，扬起一片灰尘。从这里向东远远的就可以看见
东边有一座高山，山顶云雾缭绕，漫山遍野都是挺拔的大树。

LONG
       );
        set("outdoors", "changan");

        set("exits", ([
                "eastup"         : __DIR__"eroad5",
                "southwest"      : __DIR__"eroad3",
        ]));


        setup();
        replace_program(ROOM);
}

