// Room: /d/changan/jiudian.c
// data: inca	98/09/26

inherit ROOM;

void create()
{
	set("short", "小酒店");
	set("long", @LONG
这是一家小小的酒店，是一个老头开的已经有二十多年了。这里面积不是很大，
只能放三，四张桌子，坐满了无聊的士兵，看来这里不是一个久留之地，还是赶快
离开的好。
LONG
	);

	set("exits", ([
		"east"    : __DIR__"b-dajie3",
	]));

	set("objects", ([
		__DIR__"npc/bing": 6,
	]));

	setup();
	replace_program(ROOM);
}


