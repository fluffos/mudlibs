// Room: /d/changan/nroad3.c
// Date: inca 98/08/20

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "青石大道");
  set("long", @LONG
你走在一条青石大道上。四周的人不是很多，不时有几个挑担的，经商的匆匆走过。
路的两边种着不少泡桐树，一条青石大道向东西两边延伸出去。
LONG
  );
  set("outdoors", "changan");

  set("exits", ([
    "northeast": __DIR__ "eroad4",
    "southeast": "/d/shaolin/road1",
    "west": __DIR__ "eroad2",
  ]));

  set("objects", ([
    __DIR__ "npc/tiaofu": 1,
  ]));

  setup();
  replace_program(ROOM);
}
