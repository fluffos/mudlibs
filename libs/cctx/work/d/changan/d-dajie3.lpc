// Room: /d/changan/d-dajie3.c
// Date: inca 98/08/30

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", "东大街");
        set("long", @LONG
你走在一条青石大道上，这是长安城的东大街，由于这里靠近城门，所以不论是
进城的还是出城的都匆匆而过，很少在这里停留。北边是裁缝铺，南边是威武镖局，
从这里向东出城就可以去华山了。
LONG
        );
        set("outdoors", "changan");

        set("exits", ([
                "east"  : __DIR__"dongmen",
                "west"  : __DIR__"d-dajie2",
		"north" : __DIR__"caifengpu",
		"south" : __DIR__"biaoju1",
        ]));

        setup();
        replace_program(ROOM);
}

