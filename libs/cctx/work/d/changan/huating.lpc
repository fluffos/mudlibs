// ROOM  /d/changan/huating.c
// DATA  inca   98/09/20

#include <ansi.h>;
inherit ROOM;

void create()
{
	set("short","花厅");
	set("long",@long
这是一间不大的房间，正对门的墙上挂着一幅字，正中摆着一张桌子，
桌子边摆着两把太师椅，两边各放着两把椅子，桌子左边的椅子上坐着一
个中年妇人，她就是常夫人，据说她年轻时也练过武功。
long
	);

	set("exits",([
		"eastdown" : __DIR__"houhuayuan",
		"north"    : __DIR__"shufang",
		"south"    : __DIR__"woshi",
	]));

	set("objects",([
		__DIR__"npc/c-furen" : 1,
		__DIR__"npc/yahuan" : 2,
	]));

	setup();
	replace_program(ROOM);
}
