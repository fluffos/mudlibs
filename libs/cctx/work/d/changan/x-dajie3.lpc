// Room: /d/changan/x-dajie3.c
// Date: inca 98/08/30

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "西大街");
  set("long", @LONG
走在一条青石大道上，这是长安城的东大街，由于这里靠近城门，所以不论是
进城的还是出城的都匆匆而过，很少在这里停留。由于南边的兵器铺和北边的金狮
武馆在武林中都是赫赫有名的，所以来拜师和打造兵器的人络绎不绝。
LONG
  );
  set("outdoors", "changan");

  set("exits", ([
    "east": __DIR__ "x-dajie2",
    "west": __DIR__ "ximen",
    "south": __DIR__ "bingqipu",
    "north": __DIR__ "men1",
  ]));

  setup();
  replace_program(ROOM);
}
