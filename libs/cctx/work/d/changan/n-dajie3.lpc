// Room: /d/changan/n-dajie3.c
// Date: inca 98/08/30

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", "南大街");
        set("long", @LONG
走在一条青石大道上，这是长安城的东大街，由于这里靠近城门，所以不论是
进城的还是出城的都匆匆而过，很少在这里停留。但东边的戏院却吸引了不少长安
的戏迷，西边的花店也因此吸引了不少顾客。
LONG
        );
        set("outdoors", "changan");

        set("exits", ([
                "south"  : __DIR__"nanmen",
                "north"  : __DIR__"n-dajie2",
		"east"   : __DIR__"xiyuan1",
		"west"   : __DIR__"huadian",
        ]));

        setup();
        replace_program(ROOM);
}

