// ROOM  /d/changan/ceting1.c
// DATA  inca   98/09/20

#include <ansi.h>;
inherit ROOM;

void create() {
  set("short", "侧厅");
  set("long", @long
这是一间不大的房子，顺着墙整齐的摆放着一排桌椅，这里是常将军
和手下的将士们讨论军情或处理普通事件的地方。
long
  );

  set("exits", ([
    "south": __DIR__ "dating",
  ]));

  setup();
  replace_program(ROOM);
}
