// Room: /d/changan/nroad6.c
// Date: inca 98/08/20

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "大道");
  set("long", @LONG
你走在一条大道上。这是一条普通的大道，几乎没有行人。周围一马平川，
从这里向东就可以看见高耸华山了，前面不远处有一个村庄，那大概就是有名
的华山村了。

LONG
  );
  set("outdoors", "changan");

  set("exits", ([
    "northeast": "/d/village/sexit",
    "south": "/d/gumu/xiaolu10",
    "northwest": __DIR__ "eroad5",
  ]));


  setup();
  replace_program(ROOM);
}
