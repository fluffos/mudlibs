// Room: /d/changan/nroad3.c
// Date: inca 98/08/20

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIY "黄土高原" NOR);
  set("long", HIY @LONG
你走在一条黄土路上。这里已经进入黄土高原了，举目望去到处都是一片
黄土，很少见到人家，只是偶尔有一个由保镖保护的富商匆匆走过。听说这里
时常有土匪出没，你还是赶快离开这里。
LONG
    NOR);
  set("outdoors", "changan");

  set("exits", ([
    "northeast": __DIR__ "nroad4",
    "southdown": __DIR__ "nroad2",
  ]));



  setup();
  replace_program(ROOM);
}
