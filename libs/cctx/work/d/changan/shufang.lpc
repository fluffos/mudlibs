// ROOM  /d/changan/shufang.c
// DATA  inca   98/09/20

#include <ansi.h>;
inherit ROOM;

void create() {
  set("short", "书房");
  set("long", @long
这里是常将军的书房，由于有时处理公务到很晚，他为了不打扰夫人睡觉
有时也在这里办公。所以正中间的书桌上放着几份公文和一只令箭。
long
  );

  set("exits", ([
    "south": __DIR__ "huating",
  ]));

  setup();
  replace_program(ROOM);
}
