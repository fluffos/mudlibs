// ROOM  /d/changan/bingying1.c
// DATA  inca   98/09/20

#include <ansi.h>;
inherit ROOM;

void create()
{
	set("short","兵营");
	set("long",@long
这里是长安城的兵营，门口站着两个守卫的士兵，里面不时传来阵阵的喊杀声，
看来士兵们士气高昂，随时准备报效祖国。
long
	);

	set("exits",([
		"east" : __DIR__"bingying2",
		"west" : __DIR__"b-dajie3",
	]));

	set("objects",([
		__DIR__"npc/bing" : 2,
	]));

	set("outdoors","changan");

	setup();
	replace_program(ROOM);
}
