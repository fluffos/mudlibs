// Room: /d/changan/nroad5.c
// Date: inca 98/08/20

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short","大道");
        set("long", @LONG
你走在一条大道上。这是一条普通的大道，周围一马平川。不时有骑着
快马的江湖豪客飞驰而过。从这里向东远远的就可以看见华山了，山脚下隐
约的看到有一片房舍。

LONG
       );
        set("outdoors", "changan");

        set("exits", ([
                "southeast"     : __DIR__"eroad6",
                "westdown"      : __DIR__"eroad4",
        ]));

        set("objects", ([
                __DIR__"npc/jian-ke" : 1,
        ]));


        setup();
        replace_program(ROOM);
}

