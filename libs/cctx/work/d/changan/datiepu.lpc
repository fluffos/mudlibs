// ROOM:  /d/changan/datiepu
// DATA:  inca	98/09/02

inherit ROOM;

void create() {
  set("short", "打铁铺");
  set("long", @LONG
这里是兵器铺的后院，也是一个简陋的打铁铺，中心摆着一个火炉，炉火把四周照得
一片通红，你一走进去就感到浑身火热。墙角堆满了已完工和未完工的铁锤、铁棍、匕首、
盔甲等兵器。一个铁匠满头大汗挥舞着铁锤，专心致志地在打铁。
LONG
  );

  set("exits", ([
    "north": __DIR__ "bingqipu",
  ]));
  set("objects", ([
    __DIR__ "npc/tiejiang": 1, ]));

  setup();
}
