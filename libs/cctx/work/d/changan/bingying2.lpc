// ROOM  /d/changan/bingying2.c
// DATA  inca   98/09/20

#include <ansi.h>;
inherit ROOM;

void create() {
  set("short", "兵营");
  set("long", @long
这里是长安城的兵的操场，只见士兵们喊杀声震天，正在将军的带领下认真的
操练着，看着这热火朝天的场面，你不由得涌起了入伍保卫国家的热情。
long
  );

  set("exits", ([
    "west": __DIR__ "bingying1",
  ]));

  set("objects", ([
    __DIR__ "npc/wujiang": 1,
    __DIR__ "npc/bing": 5,
  ]));

  set("outdoors", "changan");

  setup();
  replace_program(ROOM);
}
