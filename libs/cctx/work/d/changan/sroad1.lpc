// ROOM  /d/changan/sroad1.c
// DATA  inca   98/10/09

#define path /wizhome/incahome/changan
#include <ansi.h>;
inherit ROOM;

void create() {
  set("short", "青石大道");
  set("long", @long
你走在一条青石大道上。四周人来人往，赶路的，经商的，挑担子的，
熙熙攘攘，非常热闹。路的两边种着不少泡桐树。一条大路向南北两边延
伸出去，东边是长安城的南城门。
long
  );

  set("exits", ([
    "north": __DIR__ "nanmen",
    "south": __DIR__ "sroad2",
  ]));

  set("outdoors", "changan");

  setup();
  replace_program(ROOM);
}
