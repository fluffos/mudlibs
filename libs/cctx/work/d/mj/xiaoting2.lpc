//room: /d/mingjiao/dadian.c
inherit ROOM;

void init();

void create() {
  set("short", "小厅");
  set("long", @LONG
这里是间收拾整齐的小厅堂，四周是整齐的古木家具，是明教弟子用膳所
在。很多弟子进进出出，繁忙的准备食物。
LONG
  );
  set("no_fight", 1);
  set("exits", ([
    "northwest": __DIR__ "shenshe",
    //            "southeast" : __DIR__"xiaoting2",
    //            "north" : __DIR__"qinglong",
    "east": __DIR__ "eatroom",
  ]));
  set("objects", ([
  ]));
  setup();
  replace_program(ROOM);
}
