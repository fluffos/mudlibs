// Room: /mingjiao/mishi.c
// netkill /98/8/11

#include <room.h>
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", YEL "小石室" NOR);
  set("long", @LONG
这里就是历代明教教主修炼至上武功的密室，许久没人来，依旧
干净,墙边有张石床，几个石凳。
LONG
  );
  set("no_fight", 1);
  set("valid_startroom", 1);
  set("sleep_room", 1);
  set("exits", ([
    "northup": __DIR__ "mishi",
  ]));

  set("objects", ([
    __DIR__ "obj/yangpi": 1,
  ])); setup();

  create_door("northup", "小石门", "southdown", DOOR_CLOSED);

}
