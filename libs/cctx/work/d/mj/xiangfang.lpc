//room: /d/mingjiao/dadian.c
inherit ROOM;

void init();

void create() {
  set("short", "厢房");
  set("long", @LONG
这是间收拾整齐的厢房，墙边一排排的书架整齐的排列着，书架上放
着各种书籍，物内经常有人打扫，书架上一尘不染。静悄悄的。
LONG
  );
  set("no_fight", 1);
  set("exits", ([
    //            "southdown" : __DIR__"gmding",
    //            "north" : __DIR__"llang4",
    "enter": __DIR__ "md17",
    "south": __DIR__ "xiaoting3",
  ]));
  set("objects", ([
  ]));
  setup();
}
