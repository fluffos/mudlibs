// Room: /d/wizhome/incahome/md1-1.c
// Date: inca 98/08/09

inherit ROOM;

void create() {

  set("short", "明教秘道");
  set("long", @LONG
这里就是明教花费无数人力建立起来的密道，也是明教中最大的禁地，
只有教主才能进入。这里光线很暗，你隐隐约约看到两边都是凹凸不
平的石壁，地上铺了厚厚的一层干草。左右两边各有一个出口，黑沉
沉的不知道通到哪儿去。
LONG
  );

  set("exits", ([
    "west": __DIR__ "md1-2",
    "east": __DIR__ "md1-3",
  ]));


  setup();
  replace_program(ROOM);
}
