//HSQTAN2.C

inherit ROOM;

void create() {
  set("short", "潭底");
  set("long", @LONG
这里是瀑布下的寒潭，水中幽暗无比，四周似乎有几处出口，但实在看不清
是通向何处。水中冰冷刺骨，令人难以忍受，你只觉得气力在一点一点消失。
LONG
  );

  set("exits", ([
    "up": __DIR__ "hsqtan1",

  ]));

  set("outdoors", "mingjiao");
  setup();
  replace_program(ROOM);
}
