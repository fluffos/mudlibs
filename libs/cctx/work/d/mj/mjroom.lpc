// Room : /wizhome/rtmhome/mjroom.c
// Rtm 08/08/1998 , Mingjiao Yang Buhui 's room
// Produced by Rtm , ver0.01

#include <room.h>

inherit ROOM;

void create()
{
        set("short","小石室");
        set("long", @LONG
这间卧室光线很好，并不象走廊中的其他房间。不时你闻到丝丝芳香，你左右查看
发现床(bed)头挂着一只香包，香气正是从那里散发。想必这是哪位小姐的闺房。
    西北角垂下一串珠帘，那里还有一间小房。
LONG
        );

        

        set("exits",([                /* sizeof() == 3 */
                     "down": __DIR__"md1-1",
                     "northwest" : __DIR__"mjxiaoshi",
                     "south" : __DIR__"mjpath1",



        ]));

        set("item_desc", ([
                     "bed" : "啊！一张松软舒适的床。\n",
        ])); 

        create_door("south","楠木门","north",DOOR_CLOSED);        

        setup();
        replace_program(ROOM);
}