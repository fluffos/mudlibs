// Room : /wizhome/rtmhome/gomj3.c
// Rtm 08/08/1998 , from GaiBang to Mingjiao ,north , ANDAO
// Produced by Rtm , ver0.01

inherit ROOM;

void create()
{
        set("short","暗道");
        set("long", @LONG
暗道十分陡峭，一不留神就会失足。它尽付了丐帮几代人的心血挖成。
LONG
        );

        set("exits",([                /* sizeof() == 2 */
                     "north" : __DIR__"gomj4",
                     "southeast" : __DIR__"gomj2"
        ]));

        setup();
        replace_program(ROOM);
}
