//room: /d/mingjiao/dadian.c
inherit ROOM;

void init();

void create() {
  set("short", "山路");
  set("long", @LONG
这是一条一窄又陡的山路，这里已见白雪，白雪盖着山路两旁又高又杂
的乱草杂树，极是美丽。你感到阵阵的寒恿上心头。
LONG
  );
  set("outdoors", "mingjiao");
  set("exits", ([
    //            "southdown" : __DIR__"gmding",
    "east": __DIR__ "tingzi",
    "westdown": __DIR__ "road2",
    "north": __DIR__ "zongtan",
  ]));
  set("objects", ([
  ]));
  setup();
  replace_program(ROOM);
}
