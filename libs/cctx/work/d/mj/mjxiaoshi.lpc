// Room : /wizhome/rtmhome/mjxiaoshi.c
// Rtm 08/08/1998 , Mingjiao Yang Buhui 's room nw 
// Produced by Rtm , ver0.01

inherit ROOM;

void create() {
  set("short", "小室");
  set("long", @LONG
这里光线昏暗，好象堆放着写书籍，转身透过门帘可以看到卧室。
LONG
  );

  set("exits", ([                /* sizeof() == 1 */
    "southeast": __DIR__ "mjroom"
  ]));

  setup();
}
