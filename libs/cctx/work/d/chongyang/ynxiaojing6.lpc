// Room:/d/chongyang/ynxiaojing3.c
inherit ROOM; 
 
void create() 
{ 
	set("short", "院内小径");
	set("long", @LONG
一条七弯八拐的小径，西边是通往终南山主峰的小路。
LONG
	);

	set("exits",([ /* sizeof()==3 */
  "south":__DIR__"houyuan",
  "north"   :__DIR__"ynxiaojing7",
	"west":__DIR__"xiaoyuan",
]));
	set("outdoors","chongyang");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
