// Room :/d/chongyang/zoulang2.c
inherit ROOM; 
 
void create() 
{ 
	set("short", "走廊");
	set("long", @LONG
这是一条宽敞的走廊，西边可通往养心殿。东边是长春殿。
LONG
	);

	set("exits",([ /* sizeof()==2 */
  "westdown":__DIR__"taijie1",
  "east":__DIR__"ccdian",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
