// Room:/d/chongyang/liangongfang5.c
inherit ROOM; 
 
void create() 
{ 
	set("short", "练功房");
	set("long", @LONG
这是一间不大的练功房，放着几个木人。
LONG
	);
	set("no_sleep",1);
	set("exits",([ /* sizeof()==1 */
  "west"   :__DIR__"yanwuting",
]));
	set("objects",([
          __DIR__"npc/mu-ren":3,
]));
	setup();
	replace_program(ROOM);
}

