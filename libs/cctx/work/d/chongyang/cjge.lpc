// Room :/d/chongyang/cjge.c
inherit ROOM;

void create() {
  set("short", "藏经阁");
  set("long", @LONG
这是全真教的藏经阁里边堆放着学多到家的书籍,整个阁楼打扫的
一尘不染,四周摆放着许多书架.
LONG
  );

  set("exits", ([ /* sizeof()==2 */
    "south": __DIR__ "ynxiaojing8",
    "up": __DIR__ "cjge1",
  ]));
  set("objects", ([
    CLASS_D("chongyang") + "/wangzhitan": 1 ]));

  set("no_clean_up", 0);

  setup();
}
