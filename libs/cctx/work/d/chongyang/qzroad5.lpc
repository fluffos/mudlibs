// Room: /d/chongyang/qzroad5.c 
 
inherit ROOM; 
#include <ansi.h> 
void create()
{ 
	set("short", HIY"金莲阁"NOR);
	set("long",
"你走了大半个时辰,终于走到了终南山的山腰"HIY"金莲阁"NOR"。\n"
"这里是终南山的山腰，有些全真教的弟子上山就在此歇脚。\n"
"再往上走就是崎岖的山路了。\n"
	);
	set("exits", ([ /* sizeof() == 2 */
  "northup" : __DIR__"qzroad6.lpc",
  "southeast" : __DIR__"qzroad4.lpc",
]));
	set("objects", ([
			__DIR__"npc/dizi1":1,
			__DIR__"npc/xiangke":1]));
	set("outdoors","chongyang");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
