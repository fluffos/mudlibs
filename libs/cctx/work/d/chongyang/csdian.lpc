// Room:/d/chongyang/csdian.c
inherit ROOM;

void create() {
  set("short", "长生殿");
  set("long", @LONG
这是重阳宫长生殿，“长生子”王处一便在此处，这里是王处一
的居所，四周打扫的非常干净。
LONG
  );

  set("exits", ([ /* sizeof()==2 */
    "south": __DIR__ "taijie2",
    "north": __DIR__ "taijie3",
  ]));
  set("objects", ([
    CLASS_D("chongyang") + "/liuchuxuan": 1
  ]));

  set("no_clean_up", 0);

  setup();
}
