// Room: /d/chongyang/qzroad2-1.c 

inherit ROOM;

void create() {
  set("short", "凉棚");
  set("long", @LONG
这是终南山的凉棚,上山的人都会在这里歇脚.
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "qzroad2",
  ]));
  set("objects", ([
    "/d/chongyang/npc/xiaoer": 1 ]));

  set("outdoors", "chongyang");
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
