// Room:/d/chongyang/ynxiaojing7.c
inherit ROOM; 
 
void create() 
{ 
	set("short", "院内小径");
	set("long", @LONG
这是重阳宫中的小径，南边是重阳宫后殿，西边就可通往演武场了。
LONG
	);

	set("exits",([ /* sizeof()==2 */
  "south":__DIR__"ynxiaojing6",
  "east"   :__DIR__"ynxiaojing5",
]));
	set("outdoors","chongyang");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
