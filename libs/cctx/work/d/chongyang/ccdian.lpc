// Room:/d/chongyang/ccdian.c
inherit ROOM; 
 
void create() 
{ 
	set("short", "长春殿");
	set("long", @LONG
这是全真教“长春子”秋处机的屋子，整间屋子打扫的一尘不染，地下
几个蒲团整整齐齐的放着。秋处机正坐在上边打座。
LONG
	);

	set("exits",([ /* sizeof()==4 */
  "southdown":__DIR__"lianwuchang1",
  "northup":__DIR__"taijie2",
  "west":__DIR__"zoulang2",
  "northeast":__DIR__"ynxiaojing3",
]));
	set("objects", ([
		CLASS_D("chongyang") + "/qiuchuji" : 1]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
