//room:/d/chongyang/yuanping.c
inherit ROOM;

void create() {
  set("short", "圆坪");
  set("long", @LONG
你转过了弯弯曲曲的小路走到了重阳宫下的圆坪.你放眼望去这个圆坪
足有半里简直就是一个大比武场。向北就到了赫赫有名的重阳宫了，
而东边好像有一条小路不知通往何处。
LONG
  );

  set("exits", ([ /* sizeof()==3 */
    "south": __DIR__ "damen.lpc",
    "north": __DIR__ "yuqingchi.lpc",
    "northeast": "/d/gumu/xiaolu1",
  ]));
  set("objects", ([
    CLASS_D("chongyang") + "/miaodaoyi": 1,
    __DIR__ "npc/dizi": 2 ]));
  set("outdoors", "chongyang");
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
