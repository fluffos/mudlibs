// Room:/d/chongyang/liandanfang.c
inherit ROOM;

void create() {
  set("short", "炼丹房");
  set("long", @LONG
这是重阳宫的炼丹房,房屋中间放着丹炉(danlu),炉内也不知有什么.真想过去看看.
LONG
  );

  set("exits", ([ /* sizeof()==1 */
    "east": __DIR__ "zuopiandian",
  ]));
  set("item_desc", ([
    "danlu": "这是一个青铜的丹炉,虽然是炼丹的可是擦的很亮.\n" ]));
  set("objects", ([
    CLASS_D("chongyang") + "/zzjing": 1,

    __DIR__ "npc/xiangke": 1 ]));

  setup();
}
