//room:/d/chongyang/guangchang.c
inherit ROOM; 
 
void create() 
{ 
	set("short", "广场");
	set("long", @LONG
这是一个由大石板铺成的广场，是全真弟子学习武功和互相切磋的地点。
周围种满了梧桐树，一到秋天就是满地的落叶。一个年纪轻轻的道童正在打扫。
北边是大厅。东边是左偏殿，右边是右偏殿.
LONG
	);

	set("exits",([ /* sizeof()==4 */
  "north":__DIR__"sanqingdian.lpc",
  "southdown"   :__DIR__"yuqingchi.lpc",
  "west"	:__DIR__"zuopiandian.lpc",
  "east"	:__DIR__"youpiandian.lpc",
]));
	set("objects", ([
		CLASS_D("chongyang") + "/zhangzhixian" : 1,
			__DIR__"npc/dizi":2]));
	set("outdoors","chongyang");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
