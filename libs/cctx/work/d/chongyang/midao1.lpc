inherit ROOM;

void create() {
  set("short", "密道入口");
  set("long", @LONG
你钻进洞以后发现这里原来是一个迷道,也不知通往何处.
墙角处有一块石碑(bei),上边的字已经模糊不清了.
LONG
  );
  set("item_desc", ([
    "bei": "此阵以八卦而布，若不精通此道者请速速离去
万不可乱闯。
切记。切记
	王重阳\n",

  ]));

  set("exits", ([ /* sizeof()==2 */
    "north": __DIR__ "bagua0",
    "southwest": __DIR__ "zawufang",
  ]));

  set("no_clean_up", 0);

  setup();
}
