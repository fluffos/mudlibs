// Room:/d/chongyang/houyuan.c
inherit ROOM;

void create() {
  set("short", "后院");
  set("long", @LONG
这是重阳宫后院，院内种着几株梧桐树，秋风吹来树叶落了一地，
一个全真弟子正在打扫，墙上画着一幅高约三丈的画，原来这是你
在重阳宫后殿看过的哪幅画，只不过这一幅大了不少。
LONG
  );

  set("exits", ([ /* sizeof()==2 */
    "south": __DIR__ "houdian",
    "north": __DIR__ "ynxiaojing6",
  ]));
  set("objects", ([
    __DIR__ "npc/dizi": 1,
  ]));
  set("outdoors", "chongyang");

  setup();
}
