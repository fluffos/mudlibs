// Room:/d/chongyang/jingshi.c
inherit ROOM;

void create() {
  set("short", "静室");
  set("long", @LONG
这是全真教后院的静室，因为比较靠山，而且离大门很远，
在这里你听不到任何声音。
LONG
  );

  set("exits", ([ /* sizeof()==1 */
    "east": __DIR__ "yanwuting",

  ]));
  set("objects", ([
    CLASS_D("chongyang") + "/sunbuer": 1 ]));
  setup();
  replace_program(ROOM);
}
