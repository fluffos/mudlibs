// room:/d/chongyang/shanlu1.c
inherit ROOM; 
 
void create() 
{ 
	set("short", "山路");
	set("long", @LONG
这是上终南山主峰的小路，在这里你可以看到主峰高耸入云。下边是
茂密的森林。你突然之间有种说不出的感觉。在路边似乎有一块小石头，
也不知是什么。
LONG
	);

	set("exits",([ /* sizeof()==2 */
  "southeast":__DIR__"xiaoyuan",
  "northup"   :__DIR__"shanlu2",
]));
	set("outdoors","chongyang");


	setup();
	replace_program(ROOM);
}
