// Room:/d/chongyang/liangongfang2.c
inherit ROOM; 
 
void create() 
{ 
	set("short", "练功房");
	set("long", @LONG
这是一间很大的练功房，中间放着几个木人给全真弟子练功。
LONG
	);
	set("no_sleep",1);

	set("exits",([ /* sizeof()==2 */
  "west":__DIR__"lianwuchang1",
  "north":__DIR__"liangongfang4",
]));
	set("objects",([
          __DIR__"npc/mu-ren":3,
      ]));

	setup();
	replace_program(ROOM);
}
