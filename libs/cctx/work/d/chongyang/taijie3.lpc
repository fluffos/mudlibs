//Room:/d/chongyang/taijie3.c
inherit ROOM;

void create() {
  set("short", "台阶");
  set("long", @LONG
由此上去就是重阳宫后殿了，站在此处隐约可以看到重阳宫主峰。
LONG
  );

  set("exits", ([ /* sizeof()==2 */
    "south": __DIR__ "csdian",
    "northup": __DIR__ "houdian",
  ]));
  set("outdoors", "chongyang");
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
