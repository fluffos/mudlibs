//room:/d/chongyang/qzroad10.c
inherit ROOM; 
 
void create() 
{ 
	set("short", "抱子岩");
	set("long", @LONG
这抱子岩生得甚是奇怪，就如一个妇人抱著孩子一般。
LONG
	);

	set("exits",([ /* sizeof()==2 */
  "westdown":__DIR__"qzroad9.lpc",
  "northup"   :__DIR__"qzroad11.lpc",
]));
	set("outdoors","chongyang");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
