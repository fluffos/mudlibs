// Room: /heichao/guilin4
// netkill /98/8/11
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", RED "鬼林" NOR);
  set("long", @LONG
你走进了一片茂密的森林，这里暗无天日，黑漆漆的不见一点光，
四面的寒风吹来，你不由簌簌发抖。一个声音提醒你，得快离开这里。
LONG
  );

  set("exits", ([
    "west": __DIR__ "guilin4",
    "east": __DIR__ "guilin4",
    "south": __DIR__ "guilin",
    "north": __DIR__ "shijie",
  ]));

  setup();
  replace_program(ROOM);

}
