#include <ansi.h>
inherit ITEM;

void create() {
  set_name(HIW "水晶球" NOR, ({ "shuijing qiu", "crystal", "ball", "qiu" }));
  set_weight(200);
  set("unit", "只");
  set("long", "一只鎏光四溢的水晶球，虽然不能未卜先知，后见之明(detect)倒也马马虎虎。\n");
  set("value", 1000);
  setup();
}
