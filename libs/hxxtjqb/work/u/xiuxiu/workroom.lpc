inherit ROOM;
#include <ansi.h>

void create()
{
   set("short",HIW"熊的世界"NOR);
    set("exits", ([
   "down" : "/d/city/kezhan",
   ]));
  set("objects",([
  "/cmds/usr/news" : 1,
  "/adm/daemons/msgd" : 1,
   ]));
  setup();
}
