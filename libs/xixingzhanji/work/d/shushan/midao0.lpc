//shushan by yushu 2000.11
inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIC "「" HIR "蜀山御天宫" HIC "」" NOR);
  set("long", @LONG

蜀山御天宫是蜀山剑圣修炼的地方，是蜀山派最秘密地方，
一般蜀山派弟子，没有很好练功资质是不知道这里的,
而且蜀山剑圣教徒地极为严格。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "sleeproom",
    "south": __DIR__ "midao",
  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/jiansheng": 1,
    ]));
  set("light_up", 1);

  setup();
}
