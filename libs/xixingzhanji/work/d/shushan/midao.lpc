//shushan by yushu 2000.11
inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIR "「" HIY "密道" HIR "」" NOR);
  set("long", @LONG

这里是蜀山派的密道，这里四面都是云彩，但你看不到天空。
脚下都是雾，如仙境一般。你不知道如何是好，只好往前
继续走。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "midao0",
  ]));
  set("light_up", 1);

  setup();
}
