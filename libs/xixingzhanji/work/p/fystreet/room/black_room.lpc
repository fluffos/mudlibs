//indoor.c zip@hero 2000.1.27 房子内部
#include <ansi.h>
#define ID 101
inherit ROOM;
void create() {
  set("short", "狼巢[2;37;0m");
  set("long", @LONGLONGAGO
 这是一个由巨大石块砌成的房间，充满了野性和力量。
墙壁上挂着各种珍贵奇特的兵器、法宝，
还有许多怪兽的头颅、毛皮，
是主人多年来行侠仗义的战利品和收藏品。

墙脚有一块巨大的寒玉，上面铺了一条巨大无比的白虎皮，看来是主人的床铺。
房间正中铺了一条熊皮，上面有一块四四方方的青石，
看来主人常在这席地而坐，款待宾客。

房顶上挂着一颗碗大的夜明珠，让整个房间没有了黑夜。

看来主人是剑侠神仙一般的人物。

LONGLONGAGO);
  set("exits", ([
    "out": "/p/fystreet/street" + (string)ID,
  ]));
  set("owner", "black");
  set("no_fight", 1);
  set("no_magic", 1);
  set("sleep_room", 1);
  set("if_bed", 1);
  set("NONPC", 1);
  set("no_dazuo", 1);
  set("no_study", 1);
  set("coor/x", 40 + (ID % 2) * 20);
  set("coor/y", 20 + (ID / 100) * 10);
  set("coor/z", 0);
  setup();
  call_other("/p/fystreet/board/black_board", "???");
}
#include "indoor_func.lpc"
