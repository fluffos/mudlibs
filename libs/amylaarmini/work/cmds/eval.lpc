int main(string cmd, string arg) {
  object ob;

  seteuid(getuid());
  ob = find_object("/log/tmpfile");
  if (ob)
    destruct(ob);
  rm("/log/tmpfile.lpc");
  write_file("/log/tmpfile.lpc", "run() { " + arg + "}");
  "/log/tmpfile"->run();
  return 1;
}
