// road for enter shaolin: lin on 06/01/97 02:00AM
// shaoshi_shanyao2.c
#include <ansi.h>
#include <room.h>

inherit ROOM;



void create() {
  set("short", "少室山腰");
  set("long", @LONG
山路在寂静的松树林中穿行，一阵阵松涛声回荡在山谷中，给似乎静止了的
世界增加了许多生命的气息。清新凉爽的空气令人心旷神怡。这里就是嵩山主脉
之一的少室山，在它的东面，太室山雄伟的身影半隐在云雾之中。
LONG);
  set("exits", ([
    "westdown": __DIR__ "shaoshi_shanyao1",
    "northeast": __DIR__ "xiao_shanting",
  ]));
  set("objects", ([
  ]));
  set("detail", ([
  ]));
  set("outdoors", "shaolin");
  setup();
  //place_program(ROOM);
}
