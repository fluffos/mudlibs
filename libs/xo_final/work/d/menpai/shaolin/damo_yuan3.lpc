// Room: /d/shaolin/damo_yuan3.c

inherit ROOM;



void create() {
  set("short", "首座居室");
  set("long", @LONG
这里是达摩院首座的居室。室内空空如也，一张床都没有，只有靠南面的墙
边摆放着一个破旧的蒲团，达摩院的首座玄难大师正坐在蒲团上默默颂经，似已
超脱凡俗，不为身外事所扰。
LONG
  );
  set("exits", ([
    "east": __DIR__ "damo_yuan2.lpc",
  ]));

  set("objects", ([
    __DIR__ "npc3/xuannan": 1,
  ]));

  setup();
  //place_program(ROOM);
}
