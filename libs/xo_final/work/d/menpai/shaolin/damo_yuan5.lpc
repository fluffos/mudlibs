// Room: /d/shaolin/damo_yuan5.c

inherit ROOM;



void create() {
  set("short", "僧舍");
  set("long", @LONG
一间空荡荡的僧舍，除了一张竹床，两个蒲团外什么都没有。这里只是少林
僧人清修和休憩的地方。南面开了一扇小竹门，平时就是敞开着的，从屋里隐约
可见达摩院的一角。
LONG
  );
  set("exits", ([
    "south": __DIR__ "damo_yuan1.lpc",
  ]));

  setup();
  //place_program(ROOM);
}
