//writen by lnwm
//mishi.c 

#include <room.h>

inherit ROOM;

string *things = ({
            "jingwen",
            });



void create()
{
        set("short","罗汉堂秘室");
        set("long",@LONG
这里是罗汉堂的秘室。实际就是屏风后面的一个夹层，但看起来这里什么也
没有，似乎早已闲置不用了很长时间。地上散乱地放着很多的杂物，屋角有一个
旧书架.
LONG);
        set("exits",([
        	"south"       :      __DIR__ "luohan_tang4",
          
        ]));
        
    set("search_things", ([
            "书架"      :       __DIR__ "obj/" + things[random(sizeof(things))],
                        ]) );

    setup();
    //place_program(ROOM);

}