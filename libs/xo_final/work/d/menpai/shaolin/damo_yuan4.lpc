// Room: /d/shaolin/damo_yuan4.c

inherit ROOM;



void create()
{
    set("short", "僧舍");
    set("long", @LONG
一间空荡荡的僧舍，除了一张竹床，两个蒲团外什么都没有。这里只是少林
僧人清修和休憩的地方。北面开了一扇小竹门，平时就是敞开着的，从屋里隐约
可见达摩院的一角。
LONG
    );
    set("exits", ([
        "north"     : __DIR__"damo_yuan1.lpc",
    ]));

    set("objects",([
        __DIR__"npc3/huiduan":1,
    ]) );

    setup();
    //place_program(ROOM);
}
