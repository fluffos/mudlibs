// xiaolu02.c
// Ffox 98-4-13 20:23

#include "kl_r.h"



void create()
{
    set( "short", "小路" );
    set( "long", @LONG
一条小路，通向后花园，正是“小径通幽处”。小路上铺着一些园的卵石，
两旁有几棵果树，小路曲曲折折，在树荫下行走，让人有说不出的悠闲感，饭后
到此走一走的话，真是一种享受。
LONG );
    set("no_clean_up", 0);
    set("exits", ([ /* sizeof() == 2 */
    "west" : __DIR__"langdao02",
    "east" : __DIR__"garden",
]) );
    setup();
    //place_program(ROOM);
}
