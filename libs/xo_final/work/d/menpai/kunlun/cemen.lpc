// cemen.c
// Ffox 98-4-13 20:26
#define KL_DOOR
#include "kl_r.h"



void create() {
  set("short", "侧门");
  set("long", @LONG
一扇不大的小门，将花园和墙外的山林相隔开来，这里一般没有人来，因为
这里常常年都是锁着门的，从门缝里可以看到外面是一条山路，山路就向山蜿蜒
上去了，再远处已为所薄雾笼罩，就看不太清楚了。
LONG);
  set("exits", ([ /* sizeof() == 2 */
    "northwest": __DIR__ "shandao02",
    "south": __DIR__ "garden",
  ]));
  create_door("northwest", "侧门", "southeast", DOOR_CLOSED);
  set("outdoors", "kunlun");
  setup();
}
