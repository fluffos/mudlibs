// suan.c   蒜
// Ffox 98-4-20 20:20

#include "kl_o.h"



void create()
{
    set_name( "蒜", ({ "suan" }) );
    if ( clonep() )
        set_default_object(__FILE__);
    else {
        set( "unit", "把" );
        set( "long", "一把大蒜，用作调味品，亦可入药。\n");
        set( "value", 10);
    }
    setup();
}
