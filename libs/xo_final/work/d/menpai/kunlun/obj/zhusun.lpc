// zhusun.c  竹笋
// Ffox 98-4-20 20:20

#include "kl_o.h"



void create() {
  set_name("竹笋", ({ "zhu sun", "zhusun", "sun" }));
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "棵");
    set("long", "一棵鲜嫩的，白色的竹笋，可以用来作菜。\n");
    set("value", 200);
  }
  setup();
}
