inherit SLEEP_ROOM;

void create()
{
    set("short", "侧洞");
    set("long", @LONG
这里是一个小小的山洞，地上铺了很多茅草，非常的干燥。光线
有些昏暗，只是西面透出一些亮光。你不禁觉得有些疲倦了，一时忍
不住想躺下睡睡。
LONG );

    set("exits", ([
        "west" : __DIR__"shandong3",
    ]));

    set("family", "血刀门");
    set("exit_room", __DIR__"shenghu");

    ::create();
}
