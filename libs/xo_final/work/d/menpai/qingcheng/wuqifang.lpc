//   file  :  wuqifang.c
//   attrib:  room of QingCheng
//   name  :  武器房
//   writer:   xli
//   date  :   1998.10
//   Last Modified: xli 1999.9

inherit ROOM;

#include "/d/menpai/qingcheng/qc_room.h"

void create()
{
    set("short","武器房");
    set("long",@LONG
这里就是青城派的武器房，房里整齐地摆放了几排木柜子，每个柜子又有许
多小抽屉，每个抽屉上面又贴着一张纸条，纸条上写着一个名字。看来这些柜子
就是青城弟子暂时存放武器的地方。
LONG);

    set("exits",([
        "north" : __DIR__"songfeng_guan",
    ]));

    set("objects",([
        __DIR__"npc/laozhang" : 1,
    ]));

    setup();
}

