// doushuai.c cry
inherit ROOM;

void create() {
  set("short", "兜率大士院");
  set("long", @LONG
这是一般参拜者所能到的最远的地方，院中央有一个大香炉，靠院墙的地方
还有一个香台。再往里就是天龙寺的般若台和牟尼堂，隐约中能看见有两个沙弥
守在那里。
LONG
  );
  set("outdoors", "tianlongsi");
  set("exits", ([
    "west": __DIR__ "yuhua",
    "south": __DIR__ "wuwu",
  ]));
  set("objects", ([
    __DIR__ "obj/xianglu": 1,
    __DIR__ "npc/shami": 2,
  ]));
  set("search_things", ([
    "香台": __DIR__ "obj/xiang",
  ]));
  setup();
}
