inherit ROOM;

void create()
{
    set("short","山坳");
    set("long",@LONG
山势到了这里，转了一个弯，北面是绝壁，怪石嶙峋，极是陡峭，向西是一
片平坦的草地，草地过去就是后山了。
LONG
);
    set("exits",
    ([
        "west" : __DIR__"caodi2",
        "southeast" : __DIR__"wadi",
    ]));

    set("outdoors","wuliang");
    setup();
    //place_program( ROOM );
}
