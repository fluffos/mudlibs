inherit ROOM;

void create()
{
    set("short", "阶梯");
    set("long", @LONG
脚下是一条人工开凿的山路，不太整齐的石阶看上去已很古老，想必是许久
以前修造的。岩石都被露水浸的湿漉漉的。西边是万丈悬崖。南边有一个小亭子
建在山道中央。石阶一直向上延伸。
LONG
    );
    set("exits", ([ /* sizeof() == 2 */
        "northwest" : __DIR__"jieti1",
        "southdown" : __DIR__"shanyao1",
    ]));
    set("outdoors","wuliang");
    if( random(10) > 5 )
    {
        set("objects", ([
            MISC_DIR"killer"  : 1,
        ]) );
    }
    setup();
    //place_program(ROOM);
}
