// Room: ../wu_liang/xiepo.c

inherit ROOM;

void create()
{
    set("short", "斜坡");
    set("long", @LONG
你站在一个斜坡底部。斜坡的顶部有个竹亭，坡上花木丛生，花草清气，扑
鼻而来，令人心神具醉。
LONG
    );

    set("no_clean_up", 0);
    set("exits", ([ /* sizeof() == 2 */
        "southeast" : __DIR__"xiaolu",
        "west" :__DIR__"liangting",
    ]));

    set("outdoors","wuliang");
    setup();
    //place_program(ROOM);
}
