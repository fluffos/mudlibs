// Room: /d/menpai/wuliang/west/jianwu.c

inherit ROOM;

void create()
{
    set("short", "剑屋");
    set("long", @LONG
这里是一间小小的剑屋，是西宗存放各种剑器的地方。里头摆着一个架子。
架子分为三层，每层都摆放着几柄剑。每把剑都被拭擦得一尘不染。屋角还摆放
着几把专门用于练功的木剑、竹剑。
LONG
    );
    set("objects", ([ /* sizeof() == 2 */
        __DIR__"npc/chen" : 1,
        __DIR__"obj/zhujian" : 4,
    ]));
    set("exits", ([ /* sizeof() == 1 */
        "north" : __DIR__"lianwu_chang",
    ]));
    set("need_clean", "无量剑派西宗");
    set("no_kill", "west_wuliang");
    setup();
    //place_program(ROOM);
}

