// Room: /d/menpai/wuliang/west/zoulang.c

inherit ROOM;

void create() {
  set("short", "走廊");
  set("long", @LONG
这里是一条短短的走廊。北通内厅，西连厢房。两旁的栏杆上摆着几盆白山
茶，长的欣欣向荣。一位身着青衣的小厮和一个穿着紫衣的丫鬟拱手立于西厢房
门两侧，表情甚为拘敬。
LONG
  );
  set("no_kill", "wuliang");
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/yahuan": 1,
    __DIR__ "npc/xiaosi": 1,
  ]));

  set("exits", ([ /* sizeof() == 3 */
    "north": __DIR__ "neiting",
    "south": __DIR__ "tianjing",
    "west": __DIR__ "xiangfang",
  ]));
  set("no_clean_up", 0);

  setup();
  //place_program(ROOM);
}
