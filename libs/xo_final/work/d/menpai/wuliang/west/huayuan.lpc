// Room: ../wuliang/caiyuan.c

#include <room.h>
inherit ROOM;

void create() {
  set("short", "花园");
  set("long", @LONG
你现在站在一个花园里。园中的茶花长得生气勃勃，散发着阵阵的清香，引
来了几只蝴蝶。西首的假山旁边有一个小亭，亭里背坐着个女弟子，似乎可以听
见几声叹息。东首的青石凳上飘着一片黄叶。
LONG
  );
  set("no_kill", "west_wuliang");
  set("need_clean", "无量剑派西宗");
  set("exits", ([ /* sizeof() == 2 */
    "southeast": __DIR__ "caiyuan",
  ]));
  set("objects", ([
    __DIR__ "npc/xiaoyu": 1,
  ]));

  //    create_door("southeast", "小竹门", "northwest", DOOR_CLOSED);
  setup();
}
