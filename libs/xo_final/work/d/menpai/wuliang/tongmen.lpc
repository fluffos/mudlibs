// filename : /d/wuliang/tongmen.c

//created by qyz at 1996-12-21
#include <room.h>

inherit DOOR_ROOM;

void create() {
  set("short", "铜门");
  set("long", @LONG
这里是一片黑暗，由于不见天光，地下极为潮湿，只觉霉气扑鼻，洞内似乎
已久无人居，前面隐隐约约可以看到一道门，那门似是用铜铸成，甚是沉重，上
面有十馀枚碗大的门钉，从门缝里透出一线光亮，看来铜门后别有洞天。
LONG
  );
  set("exits", ([
    "east": __DIR__ "shishi1",
    "westup": __DIR__ "dongkou",
  ]));
  create_door("east", "铜门", "west", DOOR_CLOSED);
  setup();
  //place_program( ROOM );
}
