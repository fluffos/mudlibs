inherit ROOM;

void create() {
  set("short", "草地");
  set("long", @LONG
这附近一片平坦，到处长满了如茵的野草，许多不知名的五颜六色的野花点
缀于其间，一眼望去，就如一整块锦毯一般，煞是好看。
LONG
  );
  set("exits",
    ([
      "west": __DIR__ "guaishi",
      "east": __DIR__ "caodi2",
    ]));
  set("outdoors", "wuliang");
  setup();
  //place_program(ROOM);
}
