inherit ROOM;

void create() {
  set("short", "兵器库");
  set("long", @LONG
这里整齐地排着一列列架子，架子都被擦得一尘不染，上面陈列着各种护具
和衣服，正对门的那面墙壁上有一排小钩，弟子门练完剑后都把自己的剑擦干净
，再挂到钩子上。
LONG);
  set("exits",
    ([
      "west": __DIR__ "xiuxi_shi",
    ]));
  set("need_clean", "无量剑派东宗");
  set("objects", ([
    __DIR__ "npc/laogao": 1, ]));
  setup();
  //place_program(ROOM);
}
