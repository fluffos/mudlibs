// Room: yongdao4.c

inherit ROOM;

void create()
{
	set("short", "青石甬道");
	set("long", @LONG
这里通向一品堂的后院, 同前院一样, 两侧依然种着各式名贵而奇异的花草
地面依然一尘不染. 但不知为何, 你总觉得有人在暗处监视着你, 可你左右看时
却没有什么异常的事情, 神色肃穆的护卫依然来回巡视着, 连正眼都不看你一眼
.南面有个侧殿. 
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
  		"south" : __DIR__"zhangfang",
        "west" : __DIR__"dating",
	    "east" : __DIR__"yongdao5",
	]));
	set("outdoors","lnwm");
	setup();
}
