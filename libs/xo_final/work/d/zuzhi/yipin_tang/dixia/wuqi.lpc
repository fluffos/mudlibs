// Room: /u/ysg/room/wuqi.c
// (C) ysg 5/9/1997

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "武器房");
  set("long", @LONG
这里是玄云坛的武器库。屋子很大，但是放满了各种各样的武器。靠墙边放
了一具兵器袈，江湖上常见的各种兵器如：刀、枪、剑、戟、棍···等等，在
这儿你都能找得到，还有诸如仙人掌、神龙剑等不常用的兵器，以及江湖上已经
禁用的暴雨犁花针等等暗器。你一进来，禁不住眼花缭乱，不知道该选什么好。
LONG
  );

  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "midao22",
  ]));

  setup();
}
