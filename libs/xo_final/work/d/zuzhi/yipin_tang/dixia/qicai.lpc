// Room: /u/ysg/room/qicai.c
// (C) ysg 5/9/1997

#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "器材库");
	set("long", @LONG
这里是玄云坛的器材库。屋子很大，但是放满了各种各样的武器和防具。在
这儿你能找到江湖上一切常用的兵器如刀、枪、剑、戟、棍···等等，还有其
他的防具如铁护手、铁背心、铁鞋···等等。在屋子的角落里放着一个大箱子
，上面沾满了灰尘，好象许久没人动过似的。
LONG
	);

	set("exits", ([ /* sizeof() == 1 */
            "east" : __DIR__"midao25",
]));      
                 
	set("objects", ([
		__DIR__"npc/weishi3_3":1,
	]));

	setup();
}
