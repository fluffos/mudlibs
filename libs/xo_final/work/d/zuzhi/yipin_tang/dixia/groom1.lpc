// Room: /u/ysg/room/groom1.c
// (C) ysg 5/9/1997
                   
#include <ansi.h>
inherit __DIR__"midao_1";

void create()
{
	set("short", "卫士房");
	set("long", @LONG
这里是一品堂玄阳坛的卫士房，平时坛里的弟子只要没被派出去执行任务时
，一般都待在这里喝酒，练武什么的，所以这里总是很热闹。屋子里满满的坐着
一堆人，却没有什么人大呼小叫，也没人聊天，所以显得很安静。你发现你走进
来的时候，居然没人看你一眼，数银票的接着数银票，檫剑的接着檫剑，显得那
么专注和投入。
LONG
	);       

	set("exits", ([ /* sizeof() == 2 */
		"north" : __DIR__"zhongt",
		"east"  : __DIR__"shenf1",
	]));                                          
	set("objects", ([
		__DIR__"npc/weishi2":2,
	]));

	setup();
}
