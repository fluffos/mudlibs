// Room: /u/ysg/room/zhongt.c
// (C) ysg 5/9/1997

#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "中厅");
	set("long", @LONG
这里是玄阳坛的中厅所在，延着左墙根一溜排开五张太师椅，右墙根下放着
一具兵器架(shelf)，正对着厅门的墙上挂着一幅武圣关云长的画。
LONG
	);       

	set("item_desc", ([ /* sizeof() == 1 */
		"shelf" : "这是一放兵器的木架子。
",
]));
	set("exits", ([ /* sizeof() == 5 */
		"southeast" : __DIR__"shenf1",
		"south"     : __DIR__"groom1",
		"west"      : __DIR__"midao41",
		"north"     : __DIR__"midao39",
		"east"      : __DIR__"xiuxi1",
]));
	set("no_sleep_room", 1);

	setup();
}
