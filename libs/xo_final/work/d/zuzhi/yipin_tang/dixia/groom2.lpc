// Room: /u/ysg/room/groom2.c

inherit ROOM;

void create() {
  set("short", "卫士房");
  set("long", @LONG
这里是一品堂玄才坛的卫士房，平时坛里的弟子只要没被派出去执行任务时
，一般都待在这里。玄才坛的弟子做的都是暗杀和卧底的工作，为了保密和自身
的安全起见，玄才坛的弟子一般都戴着面罩，与人交谈时都尽量不露自己的口音
。卫士房里点着几只蜡烛，四周都蒙着一层黑布，烛光照得一切都模糊不清，只
能隐隐的看见一个轮廓。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "west": __DIR__ "shenf2",
    "north": __DIR__ "luang",
    "east": __DIR__ "midao9",
  ]));
  set("no_clean_up", 0);
  set("objects", ([
    __DIR__ "npc/weishi2": 2,
  ]));
  setup();
}
