// Room: zhangfang.c

inherit ROOM;

void create()
{
	set("short", "帐房");
	set("long", @LONG
屋内一张大桌子, 一把椅子, 桌上摆放着几本厚厚的帐簿. 一只算盘摆放在
桌子上, 你定睛看时, 竟发现那算盘通体由精铁打就. 椅上座着一个瘦骨嶙峋的
帐房先生, 正拨打着算盘在算着什么. 听见你进来, 头也不抬. 
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
		"north" : __DIR__"yongdao4",
	]));
	set("objects", ([
		__DIR__ "npc/zhangfang" : 1,
		]) );
	setup();
}
