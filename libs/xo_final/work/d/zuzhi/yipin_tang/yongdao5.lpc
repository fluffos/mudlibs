// Room: yongdao5.c

inherit ROOM;

void create()
{
	set("short", "甬道");
	set("long", @LONG
这里通向一品堂的后院, 同前院一样, 两侧依然种着各式名贵而奇异的花草
地面依然一尘不染. 但不知为何, 你总觉得有人在暗处监视着你, 可你左右看时          
却没有什么异常的事情, 神色肃穆的护卫依然来回巡视着, 连正眼都不看你一眼          
. 南北两侧各由一座卫士房. 
LONG
	);
	set("exits", ([ /* sizeof() == 4 */
		"south" : __DIR__"weishifang4",
		"north" : __DIR__"weishifang3",
		"west" : __DIR__"yongdao4",
		"east" : __DIR__"houting",
	]));
	set("objects", ([
		__DIR__ "npc/huwei3" : 2,
        __DIR__ "npc/huwei4" : 2,
	]) );
	set("outdoors","lnwm");
	setup();
}
