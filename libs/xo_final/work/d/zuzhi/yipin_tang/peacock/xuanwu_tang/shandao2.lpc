// Room: /u/zodiac/xuanwu/shandao2.c

inherit ROOM;

void create()
{
	set("short", "山道");
	set("long", @LONG
这是一间什麽也没有的空房间。
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
  "southdown" : __DIR__"shandao1",
  "northwest" : __DIR__"migong",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
