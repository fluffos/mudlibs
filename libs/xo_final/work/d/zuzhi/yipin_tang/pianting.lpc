// Room: pianting.c

inherit ROOM;

void create()
{
	set("short", "偏厅");
	set("long", @LONG
这里是一间只有一扇门的侧厅, 也不很大. 不过比起外面空旷的大厅, 这里
倒反而有些拥挤, 几张巨大的书架就几乎充满了整个空间, 除此之外, 地上还整
整齐齐码放着几只巨大的箱子, 也不知道里头装了些什么. 
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
		"south" : __DIR__"ceting1",
	]));
	setup();
}
