// Room: /u/zodiac/xuanwu/menfang.c

inherit ROOM;

void create() {
  set("short", "门房");
  set("long", @LONG
这是一间小小的门房。一根红绸带上系着一串闪闪发光的小铃铛
。每当来人拉动门外的绸带，铃铛们便欢欢快快地闹起来。看门阿三
不免又是睡眼朦胧地推开窗，恶声恶气地问“谁呀，还让不让人睡了
”。看着他眼角那黄黄粘粘的眼屎，总是让人觉得败兴。真是恶人好
杀，小人难缠呀。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "neiyuan_men",
  ]));
  set("objects", ([
    __DIR__ "npc/asan": 1,
  ]));

  set("no_clean_up", 0);

  setup();
}
