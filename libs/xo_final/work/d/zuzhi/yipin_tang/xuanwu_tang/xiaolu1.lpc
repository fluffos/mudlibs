// Room: /u/zodiac/xuanwu/xiaolu1.c

inherit ROOM;

void create() {
  set("short", "小路");
  set("long", @LONG
出内院向西，是一条窄窄的青石板路。经过风雨和人的踩踏磨损，
变得非常光滑。两边是黄色的山坡，坡上开着许多不知名的小花。非
常醒目。西边不远能看见一座小殿，孤零零得建在山坡之上。两边四
面为一圈篱笆所围绕。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "neitang",
    "east": __DIR__ "tingyuan1",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "xuanwu");
  setup();
}
