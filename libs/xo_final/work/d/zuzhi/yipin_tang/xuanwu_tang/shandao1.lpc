// shandan1.c
// by lnwm

inherit ROOM;

void create() {
  set("short", "山道");
  set("long", @LONG
从贺兰山脚一路往上，都是崎岖不平的山路。说是路，其实是山下的农户，
在农闲时上山打猎时采出的通道。接近山脚，路还比较多，越往上走，气候越冷
，人烟越稀少，当走到有雪的地方时，几乎已经没有道路，四面全是巍峨的山脉
和陡峭的悬崖。
LONG
  );
  set("exits", ([
    "southdown": __DIR__ "shanjiao",
    "northup": __DIR__ "shandao2",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "xuanwu");
  setup();
}
