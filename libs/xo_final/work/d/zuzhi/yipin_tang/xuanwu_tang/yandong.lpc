// Room: /u/zodiac/xuanwu/yandong.c

inherit ROOM;

void create()
{
    set("short", "地下岩洞");
    set("long", @LONG
这里是十余丈深的地下，地面上的声音一丝也传不进来。神奇的
造物在这里形成了一个高达六七丈的岩洞，宛如一个大殿。洞穴的一
半被一潭水占据着。深潭尽头处的洞壁逐渐收敛成一个宽近五尺洞穴
，洞顶距离水面也有一人多高。一条地下河顺着岩洞缓缓地注入潭水
中，发出淙淙的水声。一道人工开凿的阶梯沿着洞壁盘旋而上，阶梯
的尽头是一条隧道。
LONG );
    set("exits", ([ /* sizeof() == 2 */
        "up" : __DIR__"midao",
        ]) );
    set("no_clean_up", 0);

    setup();
    if ( load_object( __DIR__"xiaochuan" )->query( "exits/out" ) == __FILE__ )
    {
        set( "exits/in", __DIR__"xiaochuan" );
    }
}
