// Room: /u/zodiac/xuanwu/tingyuan4.c

inherit ROOM;

void create()
{
    set("short", "庭院");
    set("long", @LONG
小小的庭院。走到这里就能听见西边传来喧闹之声，那里是饭厅。
东边一座并不高大的建筑，一条毡布门帘将里外隔开。门帘虽然比较厚
，可挡不住里面传出来的阵阵饭菜香味。
    北面一扇小小的木门紧闭着。
LONG
    );
    set("exits", ([ /* sizeof() == 5 */
        "north" : __DIR__"xiaomen",
        "south" : __DIR__"tingyuan3",
        "west"  : __DIR__"fanting",
        "east"  : __DIR__"chufang",
    ]) );
    set("no_clean_up", 0);
    set("outdoors","xuanwu");
    create_door("north",([
        "name" : "小门",
        "lock" : "玄武堂后山门",
        "status": 3,
        "id"  : ({ "north","小门","door"}),
        "other_side_dir": "south",
    ]) );

    setup();
}
