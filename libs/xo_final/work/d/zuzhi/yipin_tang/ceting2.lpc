// Room: ceting2.c

inherit ROOM;

void create() {
  set("short", "侧厅");
  set("long", @LONG
这里是在侧厅, 是赫连云鹰将军休息的地方. 亭中陈设简单, 只有两排椅子
列在一张长长的木桌两侧. 木桌的尽头也是同样的一张八仙椅, 八仙桌上同样覆
着一张色彩斑斓的虎皮, 有时赫连云鹰就和他的幕僚在这里商议军机大事. 
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "dating",
  ]));
  setup();
}
