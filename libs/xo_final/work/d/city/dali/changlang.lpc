// room:/d/dali/changlang.c  writen by king Jan-21-97

#include <room.h>
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "长廊");
  set("long", @LONG
这是条通向后花园的长廊，，位于内宅的东面，飞檐红柱，大理石铺就的地
面一尘不染，两个衣着光鲜的小家丁认真的在擦洗两边的栏杆和柱子，走廊向东
北拐了个弯通向花园的花厅，西面是内宅。
LONG
  );
  set("exits", ([
    "west": __DIR__ "beilang",
    "northeast": __DIR__ "huayuan",
  ]));

  setup();
  //place_program(ROOM);
}
