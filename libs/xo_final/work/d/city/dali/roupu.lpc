inherit ROOM;
void create()
{
    set("short", "肉铺");
    set("long", @LONG
一进肉铺，就闻到一股浓烈的血腥气，脏稀稀的肉案上放着几块鲜血淋淋的
生肉。一堆绿头苍蝇在肉块边上忙忙碌碌的飞起飞落。肉铺边上有几个生了锈的
铁钩，上面挂着几个刚砍下来的羊头，猪头，和牛头。那屠户正在跟人讨价还价
，争得面红耳赤。
LONG
        );
    set("exits", ([ /* sizeof() == 4 */
              "south" : __DIR__"dongjie",
                ]));
    set("objects", ([
                    __DIR__"npc/tuhu" : 1,
            ]) );

    setup();
    //place_program(ROOM);
}