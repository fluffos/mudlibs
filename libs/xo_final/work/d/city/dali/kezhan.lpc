// room:/d/dali/kezhan.c writen by king Jan-20-97

#include <room.h>
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "如意客栈");
  set("long", @LONG
这是一家高档的车马客栈，建筑相当豪华，很是抢眼，而且由于价钱低廉， 
又是在南门的交通要道上, 三教九流多选择这里落脚，生意相当不错, 你也可以
在这里打听到当地的风土人情。据说客店主人姓张，但从不露面，引起过很多猜
疑！”如此豪华舒适的客栈为何价格这么便宜？“金字招牌上写着“如意客栈”，
门口进进出出的客人很是不少，忙得小二哥团团乱转。
LONG
  );
  set("exits", ([
    "east": __DIR__ "nanjie1",
  ]));

  set("objects", ([
    __DIR__ "npc/biaoshi": 2,
    __DIR__ "npc/waiter": 1,
    //"/d/place/yanziwu/npc/duanyanqing" : 1,
  ]));
  if (random(10) > 7)
    add("objects", ([
      MISC_DIR "killer": 1,
    ]));

  setup();
  //place_program(ROOM);
}
