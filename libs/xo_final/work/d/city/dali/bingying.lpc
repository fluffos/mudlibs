// room:/d/dali/bingying.c writen by king Jan-20-97

#include <room.h>
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "虎威营");
  set("long", @LONG
这里是皇上的最亲信的禁卫军的驻地，位于大理都城大理城东北占地不小，
北面是营房，南面是小校场，东面紧挨着城墙是一排马厩，有西营门和南营门通
向北大街和南大街。虎威营的军士俱是大理军中千挑万选的猛士，平时负责都城
的警戒，皇上出巡时便挑起开道护卫的重责，战时更是一支生力军。由善阐侯高
升泰统领。
LONG
  );
  set("exits", ([
    "west": __DIR__ "beijie",
    "east": __FILE__,
  ]));
  set("objects", ([
    __DIR__ "npc/shibing": 2,
  ]));
  set("blocks", ([
    "east": "shi bing",
  ]));

  set("outdoors", "xinan");
  setup();
  //place_program(ROOM);
}
