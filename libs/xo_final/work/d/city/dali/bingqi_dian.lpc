// room:/d/dali/bingqi_dian.c writen by king Jan-20-97

#include <room.h>
#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "莫邪兵器店");
	set("long", @LONG
这是一家南疆最有名的兵器店，座落在大理南大街的西端，对面是铁面当铺
一走进去你就觉得冷嗖嗖的，四壁挂满了寒光闪闪的十八般兵器和精致的盔甲，
据说都是古代名将使用过的以此来显示该店所制作兵器的精良，老板笑容可掬，
热情地招呼着顾客, 后院里几个身强力壮的男子正围着火炉叮叮当当地敲打着一
把尚未完工的鱼肠宝剑, 但看上去已经是一件上品了。这里的兵器自然不可和风
记铁匠铺的同日而语，不过价格也贵的惊人。听说老板是干将和莫邪的后代，是
真是假就没有人知道了。
LONG
	);
    set("exits", ([ 
	        "east"  : __DIR__"nanjie2",
        ]));
	set("objects",([
		__DIR__"npc/boss" : 1,
		]) );

//	set("objects", ([
//                   __DIR__"npc/waiter.lpc" : 1 ]) );
	
	setup();
	//place_program(ROOM);
}                       

