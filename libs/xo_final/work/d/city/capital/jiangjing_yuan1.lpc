// 汴京   jiangjing_yuan1.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit BROOM;
void create()
{
	set("short","讲经院");
	set("long", @LONG
这是相国寺的讲经院之一的“北讲堂”。经常有各地的云游僧人来此开坛说
法。相国寺的僧人也常在此地讲经，并散发出售刻本佛经，印刷精美，价格不菲。
每当高僧前来开坛说法时，院子里总是人满为患。
LONG
	);
	set("objects", ([ 
         ]));
	set("exits", ([  
	    "north" : __DIR__"jingling_jie1",
         ]));
        set("detail",([
        ]));
        set("outdoors", "capital");
	setup();
}
void init()
{
}
