// 汴京   bianhe_jie1.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"

inherit STREET;

void create()
{
	set("short",HIW "汴河大街" NOR);
	set("long", @LONG
横连新郑门到新宋(朝阳)门的大街是汴京城中最宽的大街(天街除外)，但是
也只有三十步宽。大街沿着汴河的北岸，两边都是临街的大店铺，花红柳绿，车
马喧沸。北面是皇家的左藏库，旁边是一家青鱼行，南面是一家温州人开设的漆
器店铺。东面紧接天街，就到天汉桥了。 
LONG
	);
	set("objects", ([        
         ]));
	set("exits", ([  
	    "west" : __DIR__"yujie4",
	    "east" : __DIR__"bianhe_jie2",
    ]));
    set("detail",([
    ]));
	setup();
}