// 汴京   jiucheng_qianggen.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
	set("short","旧城墙根");
	set("long", @LONG
汴京内城原是唐汴州州城，城墙还算坚固，只是城小街窄，颇失王都之风。
城墙外也有一道护城河，与金水，五丈，汴，蔡等河渠道相通，舟楫可行；一派
江南风光。北边绕城而走，东北角上是新建的城隍庙，南面就是宜秋门了。
LONG
	);
	set("objects", ([        
         ]));
	set("exits", ([  
	    "north" : __DIR__"chenghuang_miao",
	    "south" : __DIR__"yiqiu_men",
        "west"  : __DIR__"shulin1",
    ]));
    set("detail",([
    ]));
	setup();
}
