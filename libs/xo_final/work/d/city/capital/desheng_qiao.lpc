// 汴京  desheng_qiao.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
	set("short", "得胜桥" );
	set("long", @LONG            
桥两边是铁屑楼酒店，皇建院街等地。下桥有一家著名的郑家油饼店，那里
的油饼是全城闻名的。西边的高阳正店，店前也是一带市集，夜市相当热闹。
LONG
	);
	set("objects", ([ 
         ]));
	set("exits", ([    
	    "north" : __DIR__"shizi_lukou3",
	    "south" : __DIR__"taimiao_jie",
         ]));
        set("detail",([
        ]));
	setup();
}
