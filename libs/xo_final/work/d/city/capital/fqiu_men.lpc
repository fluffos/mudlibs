// 汴京   /d/city/capital/fqiu_men.c
#include <room.h>
#include <ansi.h>
#include "bianjing.h"
inherit GATE;
void create() {
  set("short", HIW "新封丘门" NOR);
  set("long", @LONG     
环绕汴京的外城城墙是用从郑州虎牢关取来的土筑成，城墙多曲折，顺着地
势而走，形似卧牛。城外每隔百步即建一军哨，屯驻数十骑禁军官兵，沿着护城
河两岸是一带柳树临水，风景怡然。
LONG
  );
  set("outdoors", "capital");
  set("objects", ([
    __DIR__ "npc/gate_shibing": 2,
    __DIR__ "npc/gate_dutou": 1,
  ]));
  set("exits", ([
    "southeast": __DIR__ "hanh_men",
    "west": __DIR__ "cheq_men",
    "south": __DIR__ "fengqiumen_jie1",
  ]));
  set("detail", ([
  ]));
  setup();
}
void init() {}
