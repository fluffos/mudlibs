// 汴京  tushizi_jie1.c
#include <room.h>
#include <ansi.h>
#include "bianjing.h"
inherit STREET;
void create() {
  set("short", "土市子街");
  set("long", @LONG            
这里的茶坊，店铺十分特别，每天总在五更前就点灯开张，为赶夜路早到的
人作停留之处，或者早起人盘行的地方，买卖衣物，图画，花环，领抹之类的东
西；但是一到天亮时就停业关张了，人持之为「鬼市子」。南北两旁，有高门大
宅，也有酒楼店铺。
LONG
  );
  set("objects", ([
  ]));
  set("exits", ([
    "west": __DIR__ "shizi_lukou3",
    "east": __DIR__ "tushizi_jie2",
  ]));
  set("detail", ([
  ]));
  set("outdoors", "capital");
  setup();
}
