// 汴京  maxing_jie2.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
	set("short",HIW "马行街" NOR);
	set("long", @LONG            
这里的东西各有一家名酒楼：东面的叫杨楼，西边的叫任楼。两家皆是花红
柳绿，各出奇招，招揽顾客：任楼内一条东西主廊长百步，每晚数百名妓女夹道
两旁，钗玲玉珑，直是神仙世界；杨楼则大张旗鼓，在楼上建探头高台，既可饮
酒，又可观景。你来我往，皆是不肯服输。
LONG
	);
	set("objects", ([ 
         ]));
	set("exits", ([    
	    "south" : __DIR__"shizi_lukou3",
	    "north" : __DIR__"maxing_jie1",
         ]));
        set("detail",([
        ]));
        set("outdoors", "capital");
	setup();
}
