// 汴京  mashi.c
#include <room.h>
#include <ansi.h>
#include "bianjing.h"
inherit BROOM;
void create() {
  set("short", "马市");
  set("long", @LONG            
这里是城中驴马牲口的交易市场，一匹好马往往可以卖到十两黄金，但更多
的人是租马作为交通工具，小马头会牵着你租来的马，送你到任何地方。还有供
人乘坐的车驾，以及拉货的大车，也在这里出租。
LONG
  );
  set("objects", ([
  ]));
  set("exits", ([
    "west": __DIR__ "shizi_lukou2",
  ]));
  set("detail", ([
  ]));
  set("outdoors", "capital");
  setup();
}
