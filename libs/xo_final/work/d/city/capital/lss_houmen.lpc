// 汴京   /d/city/capital/lss_houmen.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit BROOM;
void create()
{
	set("short","后门");
	set("long", @LONG
脚下一条碎石小径，从西面花园的假山中引出来，一直通到后门前。眼前是
一个朱红月洞小门儿，两扇门用木栓拴着。
LONG
	);
	set("objects", ([ 
         ]));
	set("exits", ([  
	    "west" : __DIR__"lss_houyuan", 
         ]));     
    set("detail",([  
        "花园" : "走过去不就看见了么？",
    ]));           
    set("outdoors","capital");
	setup(); 
}
void init()
{
}
