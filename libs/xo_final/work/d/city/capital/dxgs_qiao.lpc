// 汴京   dxgs_qiao.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"

inherit STREET;

void create()
{
	set("short",HIW "相国寺桥" NOR);
	set("long", @LONG
相国寺桥与州桥的建筑风格很相象，也是平缓的桥面，青石构造，坚固气派。
桥北正对着雄伟的大相国寺山门，门前信男信女人山人海，香火缭绕，钟声不绝。
南面过了桥有一条街道，通向保康门。
LONG
	);
	set("objects", ([        
         ]));
	set("exits", ([  
	    "northwest" : __DIR__"bianhe_jie2",
	    "east" : __DIR__"bianhe_jie3", 
        "north" : __DIR__"dxgs_simen",
    ]));
    set("detail",([
    ]));
	setup();
}