//汴京 LIQUIDs: yanggao_jiu.c 

inherit ITEM;
inherit F_LIQUID;

#include <ansi.h>

void create() {
  set_name("羊羔酒", ({ "yang gao jiu", "jiu" }));
  set_weight(300);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", @LONG
        一个竹子制成的大酒杯，里面恰好能装一角酒。
LONG);
    set("unit", "杯");
    set("value", 8000);
    set("max_liquid", 3);
    set("drink_msg", "$N端起酒杯喝了一大口羊羔酒。\n");
  }
  set("liquid", ([
    "type": "alcohol",
    "name": "羊羔酒",
    "remaining": 3,
    "drink_apply": 20,
    "drunk_apply": 4,
  ]));
}
void init() {
  add_action("do_drink", "drink");
}

int drink_effect(object who, object this) {}
