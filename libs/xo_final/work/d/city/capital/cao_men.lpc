// 汴京  cao_men.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
	set("short", "曹门" );
	set("long", @LONG            
曹门以里，尽是热闹非凡的市井，酒楼；两边屋宇森然，道中车马不息。门
里两旁，各有一家小有名气的药铺：北边是李生菜小儿药铺，南边是仇防御使的
药铺。一官一民，倒是谁也不输谁。门外朱家瓦子连着南北斜街，中有泰山庙，
两边则全是妓院。
LONG
	);
	set("objects", ([ 
         ]));
	set("exits", ([    
	    "west" : __DIR__"tushizi_jie2",
	    "east" : __DIR__"hanh_men",
         ]));
        set("detail",([
        ]));
	setup();
}
