// 汴京   liu_goldshop.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit SROOM;
void create()
{
	set("short",HIY "刘号金银店" NOR);
	set("long", @LONG
汝南大富刘金泉开设的刘号金店是这里乃至全国最大的商号之一。每天交易
动辄数十万钱，令人咋舌不已。刘金泉在城南有所大宅院，平时很少到这里来。
楼上三层均是客间，用来谈生意的。店后是金库。
LONG
	);
	set("objects", ([ 
         ]));
	set("exits", ([  
	    "east" : __DIR__"jieshen_xiang1",
    ]));
    set("detail",([
    ]));
	setup();
}
void init()
{
}
