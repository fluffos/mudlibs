// 汴京   /d/city/capital/lss_tingyuan.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit DOOR_ROOM;
//inherit BROOM;
void create()
{
        set("short", "庭院曲径" );
        set("long", @LONG
入得门来，迎面是碎石铺就，三尺来宽的曲折小路，两边俱是山石翠嶂，间
或点缀着些红花黄蕊，蝴蝶飞鸟，一弯清流自西北墙角下引来，绕山石弯了几个
圈子，直通向后院去了。
LONG
        );
        set("objects", ([ 
         ]));
        set("exits", ([  
            "west" : __DIR__"lss_yuanmen",
            "north" : __FILE__,
            "east" : __FILE__,
            "south" : __DIR__"lss_ningcui_tang",
         ]));     
    create_door("west",BLK "黑漆院门" NOR,"east",DOOR_CLOSED);
    set("detail",([
    ]));
    set("outdoors", "capital");
        setup(); 
}
void init()
{
}
