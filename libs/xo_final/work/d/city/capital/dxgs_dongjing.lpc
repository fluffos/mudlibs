// 汴京     /d/city/capital/dxgs_dongjing.c
#include <room.h>
#include <ansi.h>
#include "bianjing.h"

inherit SROOM;
void create()
{
    set("short","井亭" );
    set("long",@LONG
一座八角型的木制小亭子，亭子中央有一口水井，井台是白玉石砌就；低头
向内望去，青幽幽的井水映着你的影子，轻轻地晃动。
LONG);
    set("exits",([
        "west"     : __DIR__"dxgs_shengrong_ge",
    ]));
    set("resource/water",1);
    set("watername",GRN "井水" NOR);
    set("fillplace","井");
    set("detail",([
    ]));
    set("objects",([
    ]));
    setup();
}

void init()
{
    ::init();
}
