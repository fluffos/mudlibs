// 汴京    shulin2.c

#include "bianjing.h"

inherit BROOM;

#include <room.h>
#include <ansi.h> 

void create()
{
	set("short",GRN "树林" NOR);
	set("long", @LONG
据说当年太祖登基后，暗地里担心跟随他的将领们会有人谋反，抢夺大权，
于是一天早朝后，他只身请手下的十名最高将领一起来这里打猎，并且说好都不
带随从。休息时，太祖皇帝开诚布公地对大家说：“这里没有别人，如果你们谁
想要我这个位子的话，尽管来杀了我便是。”众将领听了之后，均汗流夹背，一
起跪倒在地，发誓绝不背叛。
LONG);
	set("objects", ([        
         ]));
	set("exits", ([  
	    "south" : __DIR__"kh_lin",
	    "northeast" : __DIR__"shulin1",
    ]));
    set("detail",([
    ]));
	setup();
}
