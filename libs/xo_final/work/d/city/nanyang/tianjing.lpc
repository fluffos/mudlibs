#include <ansi.h>

inherit ROOM;

void create()
{
    set("short", "天井" );
    set("long",@LONG
大厅东侧，便是柯府的内院。小巧精致的一个四方院落，北面是五间青砖瓦
房，红漆廊柱，白是台阶，颇有几分气派；院南是三间书房；东面一个小花厅，
通向后花园。院当中一个小小假山，山上植一棵青松。台阶下廊子里挂着些鸟
雀架子。
LONG);
    set("exits", ([
        "north" : __DIR__"zhengf",
        "south" : __DIR__"shufang",
        "east"  : __DIR__"dxiangf",
        "west"  : __DIR__"dating",
    ]));

    set("need_clean", "伏牛派");
    set("outdoors", "nanyang");
    setup();
}

