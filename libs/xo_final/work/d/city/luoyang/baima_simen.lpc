// rooms:/d/luoyang/baima_simen.c

#include <room.h>

inherit ROOM;

void create()
{
	set("short", "白马寺寺门");
	set("long", @LONG
面前是一座规模宏伟的寺院，进进出出的进香的人很多，香火隆盛。大门上
方的匾上写着三个大大的金字“白马寺”，门外是两匹青石圆雕马，绿树红墙，
庄严肃穆。传闻汉明帝谴使西域求佛法及佛经，“时白马负经而来”，故取名为
“白马寺”。
LONG
	);
        set("exits", ([ 
	        "north" : __DIR__"tianwang_dian",
	        "west"  : __DIR__"beishi",
  
        ]));
        set("objects",([
            __DIR__"npc/jinxiangke":2,
            __DIR__"npc/youke":2
        ]) );
        set("outdoors","luoyang");
	setup();
	//place_program(ROOM);
}                       
