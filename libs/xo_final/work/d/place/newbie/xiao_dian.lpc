#include <ansi.h>
inherit ROOM;

void create() {
  set("short", YEL "小店" NOR);
  set("long", @LONG
这里是一家小店，各种生活用品应有尽有，小二看你近来，赶忙迎了上来。
新手可以在这里练习buy，get，drop，list，eat，drink等指令。
LONG
  );
  set("no_clean_up", 0);
  set("no_kill", "yangzhou");
  set("detail", ([]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/xiaoer.lpc": 1,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "lian_life_cmds",
  ]));

  setup();
}
