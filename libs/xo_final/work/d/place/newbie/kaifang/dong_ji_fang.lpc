// Room: /d/newbie/kaifang/dong_ji_fang.c
#include <ansi.h>

inherit KAIFANG;
void create()
{
    set("short", "东机房");
    set("long", @LONG
这里有120 台同创P133的机器，机子破是破了点，不过还可以凑合着用，如
果不是这里可以上网的话，鬼才愿意到这里来呢。:P
LONG
    );
    set("exits", ([
        "south" : __DIR__"guo_dao2",
    ]));
    setup();
}
