// Room: /d/newbie/kaifang/xi_ji_fang.c
#include <ansi.h>

inherit KAIFANG;
void create()
{
    set("short", "西机房");
    set("long", @LONG
这里有50台同方 PII 266，这些用起来就是爽，不过这里经常被一些莫名奇
妙的人占用。另外这里还有100台破 AST 486/66，两种机器形成鲜明对比。北面
是过道。
LONG
    );
    set("exits", ([ 
        "north" : __DIR__"guo_dao",
    ]));
    setup();
}
