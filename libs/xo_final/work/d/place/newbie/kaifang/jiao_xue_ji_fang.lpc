// Room: /d/newbie/kaifang/jiao_xue_ji_fang.c
#include <ansi.h>
 
inherit KAIFANG;
void create()
{
    set("short", "教学机房");
    set("long", @LONG
这里有80台金长城的p133机器，有光驱和声卡，可惜显示器太破，一会儿就
感到眼睛特别累。所以一般情况下，这里的人也不是太多。西面是过道。
LONG
    );
    set("exits", ([
        "west" : __DIR__"guo_dao2",
    ]));
    setup();
}
