// Room: /d/newbie/kaifang/er_duan.c
#include <ansi.h>

inherit KAIFANG;
void create() {
  set("short", "中央机房二段");
  set("long", @LONG
这里是实验室中央机房二段，这里有100台破破的AST 486/66 。别的机子
有空的话，一般不会有人使用这些破机。所以一般来说，没有太多人，周末上机
的人多时，这里人也会多一些。西南面是中央机房一段，东面有过道通往东机房
和教学机房。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "southwest": __DIR__ "yi_duan",
    "east": __DIR__ "guo_dao2"
  ]));
  setup();
}
