// Room: /d/newbie/kaifang/guo_dao.c
#include <ansi.h>

inherit KAIFANG;
void create()
{
    set("short", "过道");
    set("long", @LONG
这是从划卡处通往西机房的过道。东面是划卡处，北面是工作人员的休息室，
南面是西机房。
LONG
    );
    set("exits", ([ 
        "east"  : __DIR__"hua_ka_chu",
	"north" : __DIR__"xiu_xi_shi",
	"south" : __DIR__"xi_ji_fang",
    ]));
    setup();
}
