// Room: /d/newbie/kaifang/yi_duan.c
#include <ansi.h>

inherit KAIFANG;
void create()
{
    set("short", "中央机房一段");
    set("long", @LONG
这里是实验室中央机房一段，这里有100台同方166。这里的机器都可以出国，
不过出国要额外掏银子的说，xixi。一般来说，这里都有很多人，考前的时间里
人会少一些。东北是中央机房二段，南面是划卡处。
LONG
    );
    set("exits", ([ /* sizeof() == 2 */
        "south" : __DIR__"hua_ka_chu",
	"northeast" : __DIR__"er_duan"
    ]));
    setup();
}
