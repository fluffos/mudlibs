#include <ansi.h>

inherit SHOP;

void create()
{
    set( "short", YEL "当铺兼钱庄" NOR );
    set( "long", @LONG
这里简简单单，就一个柜台，几个活计和帐房先生坐在后面，不时有人在此
存钱，换钱或当、赎东西。
LONG
    );
    set( "exits", ([
        "west" : __DIR__"lian_life_cmds",
    ]) );
    setup();
}