inherit SLEEP_ROOM;

void create()
{
    set("short", "女休息室");
    set("long", @LONG
这里是女弟子休息室，里头被打扫得很干净。门的左右两边各是几张床。床
上的被子叠得整整齐齐，旁边还放着几件干净的衣服。除此以外，就没有什麽东
西了。看来，她们的生活是比较清苦的。
LONG
    );

    set("exits", ([ /* sizeof() == 1 */
        "west" : __DIR__"cundao2",
    ]));

    set("gender", "女性");
    set("exit_room", __DIR__"cundao2");

    ::create();
}

