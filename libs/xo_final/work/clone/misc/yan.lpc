// lvcha.c

inherit ITEM;



void create() {
  set_name("盐", ({ "yan", "salt" }));
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "这是普普通通的一包盐。\n");
    set("unit", "包");
    set("value", 100);
    set_weight(50);
  }
  setup();
}
