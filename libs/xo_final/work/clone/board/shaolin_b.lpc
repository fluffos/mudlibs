#include <ansi.h>
inherit BULLETIN_BOARD;


void create() {
  set_name("少林大事记", ({ "board" }));
  set("location", "/d/menpai/shaolin/fatang");
  set("board_id", "shaolinb");
  setup();
  set("capacity", 150);
}
