int atoi( string str )
{
    int v;

    if ( !stringp( str ) || !sscanf( str, "%d", v ) )
    {
        return 0;
    }

    return v;
}

string to_string(mixed arg)
{
    return stringp(arg) ? arg : "" + arg;
}

