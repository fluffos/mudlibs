寻找一个物件的方式

有些巫师指令会需要一个寻找物件的参数。比如dest,get等，这个参数可以有非
常复杂的格式，以便巫师在整个ＭＵＤ中定位一个物件。
格式详细说明如下：

1.  物件的前缀：
    !obj        obj不是一个NPC，也不是玩家。
    %obj        obj是个玩家。
    $obj        obj是个NPC。
    #num        num是个整数，用来表示第几个。0是第一个，依次类推。
    /str        str是个文件名或物件。
    其他的特殊词：
        me      你自己。
        here    你的环境。
        sefun   Simul Efun Object。

2.  物件的连接表示：
    物件加上他们的前缀用一些特殊的符号连接起来，构成一个表达式。
    @       object@env  在某个env中的object。env也可以是另外的
                        object@env之类的表达式。
    e()     e(object)   表示一个物件的环境。(environment)

3.  举例：
    !sword@%akuma       玩家akuma身上的sword。
    #2@!bag@$orc@here   这里的orc身上的bag里的第3个物品。
                        ( #0 是第一个物品。 )
    $pig@/d/place/inn   巫师会客室里的pig。
    sword#4             第四个剑。// ????怎么就这样了？
    sword#2@akuma
    sword@bag#2@akuma
    
    当然，一般的，你可以忽略这些前缀。直接写成sword@akuma。
    
    另外：!这个前缀好象不能用。