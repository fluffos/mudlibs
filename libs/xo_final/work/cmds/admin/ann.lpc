#include <ansi.h>



inherit F_CLEAN_UP;
int main(object me, string arg) {
  if (!wizardp(me))
    return notify_fail("只有巫师才能发布公告。\n");

  if (!arg) return notify_fail("你想发布什么内容？\n");
  efun::shout("\n\n" + HIW + HBRED + arg + NOR + "\n\n");
  write(HIW + HBRED + arg + NOR + "\n");
  return 1;
}
