// Qyz

#include <family.h>

int family_relation(object who) {
  return FAMILY_D->new_family_relation(this_object(), who);
}

int deal_family_relation(object who) {
  return FAMILY_D->new_deal_family_relation(this_object(), who);
}
