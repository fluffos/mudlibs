#include <weapon.h>

inherit WEAPON;

varargs void init_fan(int damage, int wound, int flag)
{
    init_weapon( damage, wound, flag | EDGED | SECONDARY );
    set_weapon_type( "fan" );
	set( "skill_type", "fan" );
    set( "xiuwei_type", "short");
	set( "verbs", ({ "impale","point" }) );
}

