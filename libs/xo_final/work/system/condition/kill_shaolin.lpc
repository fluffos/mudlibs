// kill_shaolin.c
#include <condition.h>

inherit F_CLEAN_UP;

int update_condition(object me, int duration) {
  if (!duration) {
    write("由于时日长久，少林寺决定对你以前杀了僧众不在追究。\n");
    return 0;
  }

  me->apply_condition("kill_shaolin", duration - 1);
  return CND_CONTINUE;
}
