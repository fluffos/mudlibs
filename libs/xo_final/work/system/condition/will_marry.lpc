// will_marry.c
#include <condition.h>

inherit F_CLEAN_UP;

int update_condition(object me, int duration) {
  if (!duration) {
    write("在这长长的一年中，你终于没有结婚，你的婚约自动取消了。\n");
    return 0;
  }

  me->apply_condition("will_marry", duration - 1);

  return CND_CONTINUE;
}
