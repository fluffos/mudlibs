// kill_yangzhou.c
#include <condition.h>

inherit F_CLEAN_UP;

int update_condition(object me, int duration) {
  if (!duration) {
    write("由于时日长久，扬州官府决定放弃对你的追捕。\n");
    return 0;
  }

  me->apply_condition("kill_yangzhou", duration - 1);
  return CND_CONTINUE;
}
