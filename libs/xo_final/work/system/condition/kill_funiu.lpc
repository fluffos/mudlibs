// kill_funiu.c
#include <condition.h>

inherit F_CLEAN_UP;

int update_condition(object me, int duration) {
  if (!duration) {
    write("由于时日长久,伏牛派决定放弃对你的追杀.\n");
    return 0;
  }

  me->apply_condition("kill_funiu", duration - 1);
  return CND_CONTINUE;
}
