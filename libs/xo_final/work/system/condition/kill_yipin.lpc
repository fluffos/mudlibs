// kill_yipin.c
// by lnwm
#include <condition.h>

inherit F_CLEAN_UP;

int update_condition(object me, int duration) {
  if (!duration) {
    return 0;
  }

  me->apply_condition("kill_yipin", duration - 1);
  return CND_CONTINUE;
}
