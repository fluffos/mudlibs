#include <ansi.h>

inherit FORCE_YIN;

int valid_xiulian( object me )
{
    if ( me->query("max_force") > 5999 && !random(10) )
    {
        message_vision(YEL"这么弱的内功，这么深厚的内力，这样修练下去莫不会走火入魔？你不禁有些担心，陷入思考。\n"NOR,me);
        me->start_busy(5);
    }

    return ::valid_xiulian(me);
}

