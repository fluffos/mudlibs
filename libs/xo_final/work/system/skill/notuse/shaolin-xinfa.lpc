// shaolin-xinfa.c
// 少林心法

inherit FORCE_YANG;

int valid_learn(object me) {
  if (me->query("gender") != "男性")
    return notify_fail("少林心法只有男性才能学。\n");
  if (me->query("max_force") < 400)
    return notify_fail("你的内功修行还不够学习这门内功。\n");
  return 1;
}

int query_level() {
  return 3;
}
