# 江湖风云3 (jhfy3)

游戏内标题为"夕阳再现 之「风云再起Ⅱ」"，是 [jhfy2](../jhfy2/) 的后
续/扩展版本（7977/9332 个 jhfy2 文件按路径重合，jhfy3 在此基础上大
幅扩充了内容），金庸题材 mudlib。虽然文件路径和 `jhfy2` 高度重合，
但这份档案的 `d/city/sj.lpc`（"世界之巅"跳崖场景）实际上和 `jhfy2`
所属的"天涯"家族版本不同，而是和 `xajh4gkb`（笑傲江湖4公开版，同
样打着"夕阳再现"旗号）、`xyzx`（夕阳再现，同一品牌的源头版本之一）
逐字节相同——说明"夕阳再现"是独立于"天涯"的另一个代码血统，这份
档案是站在 `jhfy2` 的地图基础上、换用了"夕阳再现"这一支的核心系统
文件。

## 内容亮点

- 和 `xajh4gkb` 同属"夕阳再现"系列，但地图内容继承自 `jhfy2`（和
  "天涯"家族共享的门派/地图布局），是两个不同代码血统在同一份档
  案里拼接的结果。
- 天赋数值有六项，其中"福缘""容貌"是隐藏属性，和 `jhfy2` 一致。
- 采用管理密码+普通密码的双密码机制，比 `jhfy2` 的单一密码更严格。

## 注册流程

英文名字（3-10 个英文字母）→ 确认建立新角色（y/n）→ 中文名字（直
接输入完整姓名）→ 管理密码（至少 5 位）→ 确认管理密码 → 普通密码
→ 确认普通密码 → 天赋数值（六项属性，其中福缘、容貌为隐藏属性；
输入 0 交由系统随机产生，随后需再输入 y 确认这组数值）→ 电子邮件
地址 → 性别（m/f）→ 进入游戏世界。

**注意**：这份档案采用管理密码+普通密码的双密码机制，和
[jhfy2](../jhfy2/) 的单一密码不同。

## 本次修复的关键 bug

- `adm/daemons/logind.lpc` 的 `logon()`：经典的服务器启动宽限期
  检查 `if (uptime() < 30)`（AGENTS.md §1.3e）——WASM 沙箱每次连线
  都是紧接着驱动启动之后，这个检查每次都必然触发，而且由于 WASM
  的事件驱动机制是由宿主逐笔驱动而非真实系统时钟驱动，靠等待真实
  时间流逝也无法满足这个条件。已按项目标准做法，对
  `127.0.0.1`（本地/WASM 测试连线）放行，保留对其余 IP 的原始检
  查不变。

没有发现 Chinese 姓名判断、宏定义不匹配或指令表相关的 bug。

## 管理员账号 / Admin account

- **id**: `fluffos`
- **管理密码 / admin password**: `Mud2026Adm`
- **普通密码 / login password**: `Mud2026Log`
- **权限 / level**: `(admin)`

管理员名单存储在纯文本文件 `adm/etc/wizlist`（CRLF 换行，与档案里
原有条目格式一致）里；账号本身通过正常注册流程创建，已在游戏内确
认 "★ 您目前权限：(admin)" 显示正确。

> 警告：这是一个公开的默认密码，仅供本地/浏览器试玩。正式对外开服前
> 请务必修改此密码。

## 本地运行

```
cd libs/jhfy3
~/src/fluffos/build-debug/src/driver config.fluffos
```

游戏端口：**40143**。
