inherit ROOM;

void create() {
  set("short", "入口");
  set("long", @LONG
一条昏暗潮湿的地道，地道很长，两边点着两根不太明亮的火把，
而且画得相当难看，你加快脚步，想知道地道的尽头是什么地方。 
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "down": "/u/jerry/workroom",
  ]));
  set("objects", ([
    "/u/jerry/npc/cisco": 1,
  ]));
  set("no_clean_up", 0);

  setup();
}
