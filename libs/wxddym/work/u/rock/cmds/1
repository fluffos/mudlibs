        for(i=1;  i<sizeof(id)+1; i++)
        {
        if ( ob->query("tiaozhan/"+i+"") == ob->query("id") ) {
        ob->delete("tiaozhan/"+i+"");
        break;
        }
        }

        for(i=1;  i<sizeof(id)+1; i++)
        {
        if ( !ob->query("tiaozhan/"+i+"") )
        cost=i;
        if ( cost )
        {
        ob->set("tiaozhan/"+cost+"",""+ob->query("tiaozhan/"+i+"")+"");
        i=cost;
        }
        }

