/*********************************************
by rock&jym 以后这段时间估计没什么时间做开发了把
自己的lpc经验写在这个board上，希望其他巫师能够
学到点新的东西!
*********************************************/

inherit ROOM;

void create() {
  set("short", "巫师学习室");
  set("long", @LONG
这里是《号令天下》主要开发巫师方舟为《号令天下》的新手巫师
写的学习室，新手巫师们可以使用(read new)指令来
查看方舟写的lpc介绍，这些只是方舟的lpc经验，有
些不对的，希望各位巫师post指出!
LONG);
  set("exits", ([ /* sizeof() == 1 */
    "rock": "/u/rock/workroom",
    "wiz": "/d/wizard/wizard_room" ]));

  set("objects", ([
  ]));

  set("valid_startroom", 1);
  set("no_suck", "1");
  set("no_fight", "1");
  set("no_clean_up", 0);
  setup();
  call_other("/u/rock/obj/board2", "???");
}
