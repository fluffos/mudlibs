inherit ROOM;

void create() {
  set("short", "道具商铺");
  set("long", @LONG
这里是方舟为《号令天下》写的宝石商铺,各位大侠们可以到此购买珍宝。
LONG
  );
  set("exits", ([
    "east": "/u/rock/shoufei/room/clothroom",
  ]));
  set("no_fight", 1);
  set("objects", ([
    "/u/rock/shoufei/othershang": 1,
  ]));
  setup();
  replace_program(ROOM);
}
