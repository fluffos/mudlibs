// This is player's own skill (Write by Lonely@nt2)
//Createby慕容西词(Lam101)atFri Aug 24 21:56:38 2018
// 烈火掌(liehuo-strike.c)

#include <ansi.h>
inherit SKILL;

int is_invent_skill() { return 1; }

mapping *action = ({
  // ZHAOSHI : 0
  ([ "action": "《突如其来》",
    "attack": 260,
    "damage": -21,
    "force": -21,
    "dodge": -21,
    "parry": -21,
    "lvl": 100,
    "damage_type": "内伤",
    "skill_name": "突如其来"
  ]),
  // ZHAOSHI : 1
  ([ "action": "《鸟焚其巢》",
    "attack": 260,
    "damage": -21,
    "force": -21,
    "dodge": -21,
    "parry": -21,
    "lvl": 110,
    "damage_type": "内伤",
    "skill_name": "鸟焚其巢"
  ]),
  // ZHAOSHI : 2
  ([ "action": "《我仇有疾》",
    "attack": 260,
    "damage": -21,
    "force": -21,
    "dodge": -21,
    "parry": -21,
    "lvl": 120,
    "damage_type": "内伤",
    "skill_name": "我仇有疾"
  ]),
  // ZHAOSHI : 3
  ([ "action": "《震伐鬼方》",
    "attack": 260,
    "damage": -21,
    "force": -21,
    "dodge": -21,
    "parry": -21,
    "lvl": 130,
    "damage_type": "内伤",
    "skill_name": "震伐鬼方"
  ]),
  // ZHAOSHI : 4
  ([ "action": "《击蒙御寇》",
    "attack": 260,
    "damage": -21,
    "force": -21,
    "dodge": -21,
    "parry": -21,
    "lvl": 140,
    "damage_type": "内伤",
    "skill_name": "击蒙御寇"
  ]),
  // ZHAOSHI : 5
  ([ "action": "《匪夷所思》",
    "attack": 260,
    "damage": -21,
    "force": -21,
    "dodge": -21,
    "parry": -21,
    "lvl": 150,
    "damage_type": "内伤",
    "skill_name": "匪夷所思"
  ]),
  // ZHAOSHI : 6
  ([ "action": "《三褫终朝》",
    "attack": 260,
    "damage": -21,
    "force": -21,
    "dodge": -21,
    "parry": -21,
    "lvl": 160,
    "damage_type": "内伤",
    "skill_name": "三褫终朝"
  ]),
  // ZHAOSHI : 7
  ([ "action": "《伏戎于莽》",
    "attack": 260,
    "damage": -21,
    "force": -21,
    "dodge": -21,
    "parry": -21,
    "lvl": 180,
    "damage_type": "内伤",
    "skill_name": "伏戎于莽"
  ]),
  // ZHAOSHI : 8
});

int valid_learn(object me) {
  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练" + "烈火掌" + "必须空手。\n");
  if ((int)me->query("max_neili") < 50)
    return notify_fail("你的内力太弱，无法练" + "烈火掌" + "。\n");
  return 1;
}

int valid_enable(string usage) {
  return usage == "strike" || usage == "parry";
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("liehuo-strike", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level > action[i - 1]["lvl"])
      return action[NewRandom(i, 20, level / 5)];
}

mixed *query_actions() { return action; }

int query_effect_parry(object attacker, object me) {
  int lvl;

  if (me->query_temp("weapon"))
    return 0;

  lvl = me->query_skill("liehuo-strike", 1);
  if (lvl < 80) return 0;
  if (lvl < 200) return 50;
  if (lvl < 280) return 80;
  if (lvl < 350) return 100;
  return 120;
}

int practice_skill(object me) {
  if ((int)me->query("qi") < 25)
    return notify_fail("你的体力不够了，休息一下再练吧。\n");
  if ((int)me->query("neili") < 3)
    return notify_fail("你的内力不够了，休息一下再练吧。\n");
  me->receive_damage("qi", 25);
  me->add("neili", -3);
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ + "liehuo-strike" + "/" + action;
}
