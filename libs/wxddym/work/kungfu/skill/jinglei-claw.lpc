// This is player's own skill (Write by Lonely@nt2)
//Createby西词(Lam101)atSun Aug 26 13:16:40 2018
// 惊雷爪(jinglei-claw.c)

#include <ansi.h>
inherit SKILL;

int is_invent_skill() { return 1; }

mapping *action = ({
  // ZHAOSHI : 0
  ([ "action": "《震惊百里》",
    "attack": 260,
    "damage": -28,
    "force": -27,
    "dodge": -28,
    "parry": -27,
    "lvl": 100,
    "damage_type": "瘀伤",
    "skill_name": "震惊百里"
  ]),
  // ZHAOSHI : 1
  ([ "action": "《建侯行师》",
    "attack": 260,
    "damage": -28,
    "force": -27,
    "dodge": -28,
    "parry": -27,
    "lvl": 110,
    "damage_type": "瘀伤",
    "skill_name": "建侯行师"
  ]),
  // ZHAOSHI : 2
  ([ "action": "《赦过宥罪》",
    "attack": 260,
    "damage": -28,
    "force": -27,
    "dodge": -28,
    "parry": -27,
    "lvl": 120,
    "damage_type": "瘀伤",
    "skill_name": "赦过宥罪"
  ]),
  // ZHAOSHI : 3
  ([ "action": "《雷风相与》",
    "attack": 260,
    "damage": -28,
    "force": -27,
    "dodge": -28,
    "parry": -27,
    "lvl": 130,
    "damage_type": "瘀伤",
    "skill_name": "雷风相与"
  ]),
  // ZHAOSHI : 4
  ([ "action": "《享于岐山》",
    "attack": 260,
    "damage": -28,
    "force": -27,
    "dodge": -28,
    "parry": -27,
    "lvl": 140,
    "damage_type": "瘀伤",
    "skill_name": "享于岐山"
  ]),
  // ZHAOSHI : 5
  ([ "action": "《井谷射鲋》",
    "attack": 260,
    "damage": -28,
    "force": -27,
    "dodge": -28,
    "parry": -27,
    "lvl": 150,
    "damage_type": "瘀伤",
    "skill_name": "井谷射鲋"
  ]),
  // ZHAOSHI : 6
  ([ "action": "《过涉灭顶》",
    "attack": 260,
    "damage": -28,
    "force": -27,
    "dodge": -28,
    "parry": -27,
    "lvl": 160,
    "damage_type": "瘀伤",
    "skill_name": "过涉灭顶"
  ]),
  // ZHAOSHI : 7
  ([ "action": "《有孚在道》",
    "attack": 260,
    "damage": -28,
    "force": -27,
    "dodge": -28,
    "parry": -27,
    "lvl": 180,
    "damage_type": "瘀伤",
    "skill_name": "有孚在道"
  ]),
  // ZHAOSHI : 8
});

int valid_learn(object me) {
  if ((int)me->query("max_neili") < 50)
    return notify_fail("你的内力太弱，无法练" + "惊雷爪" + "。\n");
  return 1;
}

int valid_enable(string usage) {
  return usage == "claw" || usage == "parry";
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("jinglei-claw", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level > action[i - 1]["lvl"])
      return action[NewRandom(i, 20, level / 5)];
}

mixed *query_actions() { return action; }

int query_effect_parry(object attacker, object me) {
  int lvl;

  if (me->query_temp("weapon"))
    return 0;

  lvl = me->query_skill("jinglei-claw", 1);
  if (lvl < 80) return 0;
  if (lvl < 200) return 50;
  if (lvl < 280) return 80;
  if (lvl < 350) return 100;
  return 120;
}

int practice_skill(object me) {
  if ((int)me->query("qi") < 25)
    return notify_fail("你的体力不够了，休息一下再练吧。\n");
  if ((int)me->query("neili") < 3)
    return notify_fail("你的内力不够了，休息一下再练吧。\n");
  me->receive_damage("qi", 25);
  me->add("neili", -3);
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ + "jinglei-claw" + "/" + action;
}
