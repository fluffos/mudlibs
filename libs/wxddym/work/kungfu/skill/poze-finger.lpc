// This is player's own skill (Write by Lonely@nt2)
//Createby慕容西词(Lam101)atFri Aug 24 22:38:56 2018
// 破泽指(poze-finger.c)

#include <ansi.h>
inherit SKILL;

int is_invent_skill() { return 1; }

mapping *action = ({
// ZHAOSHI : 0
([      "action" : "《商兑未宁》",
	"attack" : 260,
	"damage" : -28,
	"force" : -28,
	"dodge" : -28,
	"parry" : -28,
	"lvl" : 100,
	"damage_type" : "瘀伤",
	"skill_name" : "商兑未宁"
 ]),
// ZHAOSHI : 1
([      "action" : "《幽谷不明》",
	"attack" : 260,
	"damage" : -28,
	"force" : -28,
	"dodge" : -28,
	"parry" : -28,
	"lvl" : 110,
	"damage_type" : "瘀伤",
	"skill_name" : "幽谷不明"
 ]),
// ZHAOSHI : 2
([      "action" : "《一握为笑》",
	"attack" : 260,
	"damage" : -28,
	"force" : -28,
	"dodge" : -28,
	"parry" : -28,
	"lvl" : 120,
	"damage_type" : "瘀伤",
	"skill_name" : "一握为笑"
 ]),
// ZHAOSHI : 3
([      "action" : "《以虚受人》",
	"attack" : 260,
	"damage" : -28,
	"force" : -28,
	"dodge" : -28,
	"parry" : -28,
	"lvl" : 130,
	"damage_type" : "瘀伤",
	"skill_name" : "以虚受人"
 ]),
// ZHAOSHI : 4
([      "action" : "《往蹇来硕》",
	"attack" : 260,
	"damage" : -28,
	"force" : -28,
	"dodge" : -28,
	"parry" : -28,
	"lvl" : 140,
	"damage_type" : "瘀伤",
	"skill_name" : "往蹇来硕"
 ]),
// ZHAOSHI : 5
([      "action" : "《君子有终》",
	"attack" : 260,
	"damage" : -28,
	"force" : -28,
	"dodge" : -28,
	"parry" : -28,
	"lvl" : 150,
	"damage_type" : "瘀伤",
	"skill_name" : "君子有终"
 ]),
// ZHAOSHI : 6
([      "action" : "《密云不雨》",
	"attack" : 260,
	"damage" : -28,
	"force" : -28,
	"dodge" : -28,
	"parry" : -28,
	"lvl" : 160,
	"damage_type" : "瘀伤",
	"skill_name" : "密云不雨"
 ]),
// ZHAOSHI : 7
([      "action" : "《反归以娣》",
	"attack" : 260,
	"damage" : -28,
	"force" : -28,
	"dodge" : -28,
	"parry" : -28,
	"lvl" : 180,
	"damage_type" : "瘀伤",
	"skill_name" : "反归以娣"
 ]),
// ZHAOSHI : 8
});

int valid_learn(object me)
{
        if( me->query_temp("weapon") || me->query_temp("secondary_weapon") )
                return notify_fail("练"+"破泽指"+"必须空手。\n");
        if( (int)me->query("max_neili") < 50 )
                return notify_fail("你的内力太弱，无法练"+"破泽指"+"。\n");
        return 1;
}

int valid_enable(string usage) { 
        return usage == "finger" || 
               usage == "parry"; 
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
        level = (int)me->query_skill("poze-finger",1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
}

mixed *query_actions() { return action; }

int query_effect_parry(object attacker, object me)
{
        int lvl;

        if (me->query_temp("weapon"))
                return 0;

        lvl = me->query_skill("poze-finger", 1);
        if (lvl < 80)  return 0;
        if (lvl < 200) return 50;
        if (lvl < 280) return 80;
        if (lvl < 350) return 100;
        return 120;
}

int practice_skill(object me)
{
        if( (int)me->query("qi") < 25 )
                return notify_fail("你的体力不够了，休息一下再练吧。\n");
        if( (int)me->query("neili") < 3 )
                return notify_fail("你的内力不够了，休息一下再练吧。\n");
        me->receive_damage("qi", 25);
        me->add("neili", -3);
        return 1;
} 

string perform_action_file(string action) 
{
        return __DIR__ + "poze-finger" + "/" + action; 
}
