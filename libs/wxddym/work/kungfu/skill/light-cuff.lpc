// This is player's own skill (Write by Lonely@nt2)
//Createby迪迦奥特曼(Zgznr)atThu Jul 05 11:54:23 2018
// 哉佩利敖光线拳(light-cuff.c)

#include <ansi.h>
inherit SKILL;

int is_invent_skill() { return 1; }

mapping *action = ({
// ZHAOSHI : 0
([      "action" : "$N双手平伸，向两侧拉出一条白线，然后呈T型释放出一道让空气扭曲的光线",
	"attack" : 260,
	"damage" : -24,
	"force" : -24,
	"dodge" : -24,
	"parry" : -24,
	"lvl" : 100,
	"damage_type" : "瘀伤",
	"skill_name" : "哉佩利敖光线"
 ]),
// ZHAOSHI : 1
});

int valid_learn(object me)
{
        if( (int)me->query("max_neili") < 50 )
                return notify_fail("你的内力太弱，无法练"+"哉佩利敖光线拳"+"。\n");
        return 1;
}

int valid_enable(string usage) { 
        return usage == "cuff" || 
               usage == "parry"; 
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
        level = (int)me->query_skill("light-cuff",1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
}

mixed *query_actions() { return action; }

int query_effect_parry(object attacker, object me)
{
        int lvl;

        if (me->query_temp("weapon"))
                return 0;

        lvl = me->query_skill("light-cuff", 1);
        if (lvl < 80)  return 0;
        if (lvl < 200) return 50;
        if (lvl < 280) return 80;
        if (lvl < 350) return 100;
        return 120;
}
 
int practice_skill(object me)
{
        if( (int)me->query("qi") < 25 )
                return notify_fail("你的体力不够了，休息一下再练吧。\n");
        if( (int)me->query("neili") < 3 )
                return notify_fail("你的内力不够了，休息一下再练吧。\n");
        me->receive_damage("qi", 25);
        me->add("neili", -3);
        return 1;
} 

string perform_action_file(string action) 
{
        return __DIR__ + "light-cuff" + "/" + action; 
}

string query_description()
{
	return
 "哉佩利敖光线是迪迦奥特曼的必杀技。双手凝聚等离
子能量从右手手腕处发射出七千摄氏度的超高温光线。" ; 
}
