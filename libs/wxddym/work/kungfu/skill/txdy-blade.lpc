// This is player's own skill (Write by Lonely@nt2)
//Createby谢晓峰(Am2982414)atThu Jan 25 21:19:21 2018
// 天下第一刀(txdy-blade.c)

#include <ansi.h>
inherit SKILL;

int is_invent_skill() { return 1; }

mapping *action = ({
  // ZHAOSHI : 0
  ([ "action": "$N脚步轻浮，手中单刀斜挥，一式「" HIM "花开花落" NOR "」虚虚实实向$n攻出二十三刀",
    "attack": 255,
    "damage": 255,
    "force": 255,
    "dodge": 255,
    "parry": 255,
    "lvl": 100,
    "damage_type": "割伤",
    "skill_name": "花开花落"
  ]),
  // ZHAOSHI : 1
  ([ "action": "$N直冲向$n，接着身子一缩，竟然到了$n的身后，接着一记「" HIG "背车一刀" NOR "」向后砍去",
    "attack": 260,
    "damage": 260,
    "force": 260,
    "dodge": 260,
    "parry": 260,
    "lvl": 110,
    "damage_type": "割伤",
    "skill_name": "背车一刀"
  ]),
  // ZHAOSHI : 2
  ([ "action": "$N长身前扑，手中$w使一招「" HIB "风狂雨暴" NOR "」，只见一片刀光向$n罩下",
    "attack": 260,
    "damage": 260,
    "force": 260,
    "dodge": 260,
    "parry": 260,
    "lvl": 120,
    "damage_type": "割伤",
    "skill_name": "风狂雨暴"
  ]),
  // ZHAOSHI : 3
  ([ "action": "$N脚踩太虚，刀走偏锋，一手「" HIY "轻风微拂" NOR "」从下往上直取$n的喉咙" NOR,
    "attack": 260,
    "damage": 260,
    "force": 260,
    "dodge": 260,
    "parry": 260,
    "lvl": 130,
    "damage_type": "割伤",
    "skill_name": "轻风微拂"
  ]),
  // ZHAOSHI : 4
  ([ "action": "$N急越上半空，使出「" HIB "星云直坠" NOR "」绝招，一连数十刀从天而降，将$n攻个错手不及" NOR,
    "attack": 260,
    "damage": 260,
    "force": 260,
    "dodge": 260,
    "parry": 260,
    "lvl": 140,
    "damage_type": "割伤",
    "skill_name": "星云直坠"
  ]),
  // ZHAOSHI : 5
  ([ "action": "$N脚踩太白虚浮，身形如影，手中刀光忽隐忽现，$n一时不觉，$N手中刀光竟然已扑面而至" NOR,
    "attack": 260,
    "damage": 260,
    "force": 260,
    "dodge": 260,
    "parry": 260,
    "lvl": 150,
    "damage_type": "割伤",
    "skill_name": "天马行空"
  ]),
  // ZHAOSHI : 6
  ([ "action": "$N一招" + HIC "「风刀霜剑」" NOR + "，只见漫天刀光闪烁，重重刀影向$n的全身涌去",
    "attack": 260,
    "damage": 260,
    "force": 260,
    "dodge": 260,
    "parry": 260,
    "lvl": 160,
    "damage_type": "割伤",
    "skill_name": "风刀霜剑"
  ]),
  // ZHAOSHI : 7
  ([ "action": "$N一招" + HIB "「风雨飘摇」" NOR + "，$w刀光闪烁不定，似幻似真地逼$n的$l",
    "attack": 260,
    "damage": 260,
    "force": 260,
    "dodge": 260,
    "parry": 260,
    "lvl": 180,
    "damage_type": "割伤",
    "skill_name": "风雨飘摇"
  ]),
  // ZHAOSHI : 8
  ([ "action": "$N挪步小退，一招" + HIC "「风花雪月」" NOR + "，手中$w轻描淡写地挥出一"
    "刀，随即又“唰唰”连劈出两刀，后发先至，洒向$n的$l",
    "attack": 260,
    "damage": 260,
    "force": 260,
    "dodge": 260,
    "parry": 260,
    "lvl": 200,
    "damage_type": "割伤",
    "skill_name": "风花雪月"
  ]),
  // ZHAOSHI : 9
});

int valid_learn(object me) {
  object weapon;

  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "blade")
    return notify_fail("你使用的武器不对。\n");

  if ((int)me->query("max_neili") < 50)
    return notify_fail("你的内力太弱，无法练" + "天下第一刀" + "。\n");

  return 1;
}

int valid_enable(string usage) {
  return usage == "blade" || usage == "parry";
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("txdy-blade", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level > action[i - 1]["lvl"])
      return action[NewRandom(i, 20, level / 5)];
}

mixed *query_actions() { return action; }

int query_effect_parry(object attacker, object me) {
  int lvl;

  if (me->query_temp("weapon"))
    return 0;

  lvl = me->query_skill("txdy-blade", 1);
  if (lvl < 80) return 0;
  if (lvl < 200) return 50;
  if (lvl < 280) return 80;
  if (lvl < 350) return 100;
  return 120;
}

int practice_skill(object me) {
  object weapon;
  if (!objectp(weapon = me->query_temp("weapon")) ||
    (string)weapon->query("skill_type") != "blade")
    return notify_fail("你使用的武器不对。\n");

  if ((int)me->query("qi") < 25)
    return notify_fail("你的体力不够了，休息一下再练吧。\n");

  if ((int)me->query("neili") < 3)
    return notify_fail("你的内力不够了，休息一下再练吧。\n");

  me->receive_damage("qi", 25);
  me->add("neili", -3);
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ + "txdy-blade" + "/" + action;
}
