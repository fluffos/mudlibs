// This is player's own skill (Write by Lonely@nt2)
//Createby慕容西词(Lam101)atSat Aug 25 07:43:34 2018
// 流水手(liushui-hand.c)

#include <ansi.h>
inherit SKILL;

int is_invent_skill() { return 1; }

mapping *action = ({
  // ZHAOSHI : 0
  ([ "action": "《流水不盈》",
    "attack": 260,
    "damage": -23,
    "force": -23,
    "dodge": -23,
    "parry": -23,
    "lvl": 100,
    "damage_type": "瘀伤",
    "skill_name": "流水不盈"
  ]),
  // ZHAOSHI : 1
  ([ "action": "《不出门庭》",
    "attack": 260,
    "damage": -23,
    "force": -23,
    "dodge": -23,
    "parry": -23,
    "lvl": 110,
    "damage_type": "瘀伤",
    "skill_name": "不出门庭"
  ]),
  // ZHAOSHI : 2
  ([ "action": "《建侯不宁》",
    "attack": 260,
    "damage": -23,
    "force": -23,
    "dodge": -23,
    "parry": -23,
    "lvl": 120,
    "damage_type": "瘀伤",
    "skill_name": "建侯不宁"
  ]),
  // ZHAOSHI : 3
  ([ "action": "《小人勿用》",
    "attack": 260,
    "damage": -23,
    "force": -23,
    "dodge": -23,
    "parry": -23,
    "lvl": 130,
    "damage_type": "瘀伤",
    "skill_name": "小人勿用"
  ]),
  // ZHAOSHI : 4
  ([ "action": "《革言三就》",
    "attack": 260,
    "damage": -23,
    "force": -23,
    "dodge": -23,
    "parry": -23,
    "lvl": 140,
    "damage_type": "瘀伤",
    "skill_name": "革言三就"
  ]),
  // ZHAOSHI : 5
  ([ "action": "《日中见洙》",
    "attack": 260,
    "damage": -23,
    "force": -23,
    "dodge": -23,
    "parry": -23,
    "lvl": 150,
    "damage_type": "瘀伤",
    "skill_name": "日中见洙"
  ]),
  // ZHAOSHI : 6
  ([ "action": "《明入地中》",
    "attack": 260,
    "damage": -23,
    "force": -23,
    "dodge": -23,
    "parry": -23,
    "lvl": 160,
    "damage_type": "瘀伤",
    "skill_name": "明入地中"
  ]),
  // ZHAOSHI : 7
  ([ "action": "《开国承家》",
    "attack": 260,
    "damage": -28,
    "force": -27,
    "dodge": -28,
    "parry": -27,
    "lvl": 180,
    "damage_type": "瘀伤",
    "skill_name": "开国承家"
  ]),
  // ZHAOSHI : 8
});

int valid_learn(object me) {
  if ((int)me->query("max_neili") < 50)
    return notify_fail("你的内力太弱，无法练" + "流水手" + "。\n");
  return 1;
}

int valid_enable(string usage) {
  return usage == "hand" || usage == "parry";
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("liushui-hand", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level > action[i - 1]["lvl"])
      return action[NewRandom(i, 20, level / 5)];
}

mixed *query_actions() { return action; }

int query_effect_parry(object attacker, object me) {
  int lvl;

  if (me->query_temp("weapon"))
    return 0;

  lvl = me->query_skill("liushui-hand", 1);
  if (lvl < 80) return 0;
  if (lvl < 200) return 50;
  if (lvl < 280) return 80;
  if (lvl < 350) return 100;
  return 120;
}

int practice_skill(object me) {
  if ((int)me->query("qi") < 25)
    return notify_fail("你的体力不够了，休息一下再练吧。\n");
  if ((int)me->query("neili") < 3)
    return notify_fail("你的内力不够了，休息一下再练吧。\n");
  me->receive_damage("qi", 25);
  me->add("neili", -3);
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ + "liushui-hand" + "/" + action;
}
