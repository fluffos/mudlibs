#include <ansi.h>
#include "songshan.h"
#define MASTER_D "/adm/daemons/masterd"
#define CANYANG "/clone/lonely/canyang"

inherit NPC;
inherit F_MASTER;
inherit F_QUESTER;
inherit F_COAGENT;

mixed ask_skill1();
mixed ask_skill2();
mixed ask_skill3();
mixed ask_skill4();
mixed ask_skill5();
mixed ask_skill6();
mixed ask_skill7();
mixed ask_skill8();
mixed ask_skill9();
mixed ask_skill10();
mixed ask_skill11();

void create()
{
        object ob;

        set_name("左冷禅", ({ "zuo lengchan", "zuo" }) );
        set("title", HIR"五岳剑派盟主"HIY"嵩山派第十三代掌门"NOR);
        set("gender", "男性");
        set("class", "swordsman");
        set("age", 55);
        set("attitude", "peaceful");
        set("str", 36);
        set("con", 33);
        set("dex", 30);
        set("int", 28);

        set("max_qi",5500);
        set("max_jing",2200);
        set("neili", 5000);
        set("max_neili", 5000);
        set("jiali", 220);
        set("combat_exp", 1850000);
        set("shen_type", 1);

        set_skill("cuff", 240);
        set_skill("strike", 260);
        set_skill("sword", 260);
        set_skill("force", 280);
        set_skill("parry", 240);
        set_skill("dodge", 240);
        set_skill("fuguanglueying", 240);
        set_skill("songshan-jian", 260);
        set_skill("lingyun-jian", 260);
        set_skill("chuanyue-jian", 260);
        set_skill("jiuqu-jian", 260);
        set_skill("poyang-jian", 260);
        set_skill("songshan-xinfa", 280);
        set_skill("hanbing-zhenqi", 280);
        set_skill("hanbing-zhang", 260);
        set_skill("songshan-zhang", 260);
        set_skill("songshan-quan", 240);
        set_skill("literate", 220);
        set_skill("martial-cognize", 200);

        map_skill("force", "hanbing-zhenqi");
        map_skill("sword", "poyang-jian");
        map_skill("parry", "poyang-jian");
        map_skill("strike", "hanbing-zhang");
        map_skill("cuff", "songshan-quan");
        map_skill("dodge", "fuguanglueying");

        prepare_skill("strike", "hanbing-zhang");

        create_family("嵩山派", 13, "掌门");

         set("inquiry", ([
                "寒冰真气"  : (: ask_skill1 :),
                "如梦如幻"  : (: ask_skill2 :),
                "剑气冲霄"  : (: ask_skill3 :),
                "皓月穿空"  : (: ask_skill4 :),
                "九曲十八弯": (: ask_skill5 :),
                "天外玉龙"  : (: ask_skill6 :),
                "神光再现"  : (: ask_skill7 :),
                "极天寒掌"  : (: ask_skill8 :),
                "踏雪无痕"  : (: ask_skill9 :),
                "破山斧"    : (: ask_skill10 :),
                "千斤锤"    : (: ask_skill11 :),
        ]));

        set("chat_chance_combat", 120);
        set("chat_msg_combat", ({
                (: perform_action, "sword.long" :),
                (: perform_action, "sword.xian" :),
                (: perform_action, "strike.heng" :),
                (: perform_action, "strike.han" :),
                (: exert_function, "recover" :),
                (: exert_function, "powerup" :),
                (: exert_function, "freezing" :)
        }));

        set_temp("apply/attack", 100);
        set_temp("apply/damage", 100);
        set_temp("apply/unarmed_damage", 100);
        set_temp("apply/armor", 200);

        set("master_ob", 5);
	setup();

        if (clonep())
        {
                ob = find_object(CANYANG);
                if (! ob) ob = load_object(CANYANG);
                if (! environment(ob))
                {
                        ob->move(this_object());
                        ob->wield();
                } else
                {
                        ob = new("/clone/weapon/changjian");
                        ob->move(this_object());
                        ob->wield();
                }
        }
        carry_object("/clone/cloth/cloth")->wear();
}


void attempt_apprentice(object ob)
{
        if (! permit_recruit(ob))
                return;
/*
        if ((int)ob->query("shen") > -30000)
        {
                command("say 我最看不惯你们这些狭义之士，你还是请回吧！");
                return;
        }
*/
        if (ob->query("combat_exp") < 360000)
	{
		command("say 你现在经验尚浅，还是多在江湖上走走吧。");
		return;
	}

        if ((int)ob->query_skill("songshan-jian", 1) < 160)
	{
		command("say 我嵩山剑派乃五岳剑派之一，最为重视剑法。");
		command("say 你的嵩山剑法还不够熟练，下去练练再来吧。");
        	return;
        }

        if ((int)ob->query_skill("songshan-xinfa", 1) < 160)
	{
		command("say 我嵩山剑派虽善用剑，但内功却是御剑之本。");
		command("say 你是否还应该在嵩山心法上多下点功夫？");
		return;
 	}

	command("haha");
        command("say 孺子可教，孺子可教。今日我就收下你吧。");
        command("say 你以后定要勤练武，扬我嵩山派雄风。");
        command("recruit " + ob->query("id"));
}

mixed ask_skill1()
{
        object me;

        me = this_player();

        if (me->query("can_perform/hanbing-zhenqi/freezing"))
                return "咦？这招你不是已经会了吗？";

        if (me->query("family/family_name") != query("family/family_name"))
                return RANK_D->query_respect(me) + "和本派素无瓜葛，何出此言？";

        if (me->query_skill("hanbing-zhenqi", 1) < 1)
                return "你连寒冰真气都没练，还谈什么绝招可言？";


        if (me->query_skill("force") < 140)
                return "你的内功火候不足，学不了这一招！";

        if (me->query_skill("hanbing-zhenqi", 1) < 140)
                return "你的寒冰真气修为还不够，自己下去练练再来吧。";

        tell_object(me, HIC "你学会了「寒冰真气」。\n" NOR);

        if (me->can_improve_skill("force"))
                me->improve_skill("force", 1500000);
        if (me->can_improve_skill("hanbing-zhenqi"))
                me->improve_skill("hanbing-zhenqi", 1500000);
        me->improve_skill("martial-cognize", 1500000);
        me->set("can_perform/hanbing-zhenqi/freezing", 1);


        return 1;
}
mixed ask_skill2()
{
        object me;

        me = this_player();

        if (me->query("can_perform/songshan-jian/meng"))
                return "咦？这招你不是已经会了吗？";

        if (me->query("family/family_name") != query("family/family_name"))
                return RANK_D->query_respect(me) + "和本派素无瓜葛，何出此言？";

        if (me->query_skill("songshan-jian", 1) < 1)
                return "你连嵩山剑法都没练，还谈什么绝招可言？";


        if (me->query_skill("sword") < 140)
                return "你的剑法火候不足，学不了这一招！";

        if (me->query_skill("hanbing-zhenqi", 1) < 140)
                return "你的嵩山剑法修为还不够，自己下去练练再来吧。";

        tell_object(me, HIC "你学会了「如梦如幻」。\n" NOR);

        if (me->can_improve_skill("sword"))
                me->improve_skill("sword", 1500000);
        if (me->can_improve_skill("songshan-jian"))
                me->improve_skill("songshan-jian", 1500000);
        me->improve_skill("martial-cognize", 1500000);
        me->set("can_perform/songshan-jian/meng", 1);


        return 1;
}
mixed ask_skill3()
{
        object me;

        me = this_player();

        if (me->query("can_perform/lingyun-jian/xiao"))
                return "咦？这招你不是已经会了吗？";

        if (me->query("family/family_name") != query("family/family_name"))
                return RANK_D->query_respect(me) + "和本派素无瓜葛，何出此言？";

        if (me->query_skill("lingyun-jian", 1) < 1)
                return "你连凌云剑法都没练，还谈什么绝招可言？";


        if (me->query_skill("sword") < 140)
                return "你的剑法火候不足，学不了这一招！";

        if (me->query_skill("lingyun-jian", 1) < 160)
                return "你的凌云剑法修为还不够，自己下去练练再来吧。";

        tell_object(me, HIC "你学会了「剑气冲霄」。\n" NOR);

        if (me->can_improve_skill("sword"))
                me->improve_skill("sword", 1500000);
        if (me->can_improve_skill("lingyun-jian"))
                me->improve_skill("lingyun-jian", 1500000);
        me->improve_skill("martial-cognize", 1500000);
        me->set("can_perform/lingyun-jian/xiao", 1);


        return 1;
}
mixed ask_skill4()
{
        object me;

        me = this_player();

        if (me->query("can_perform/chuanyue/hao"))
                return "咦？这招你不是已经会了吗？";

        if (me->query("family/family_name") != query("family/family_name"))
                return RANK_D->query_respect(me) + "和本派素无瓜葛，何出此言？";

        if (me->query_skill("chuanyue-jian", 1) < 1)
                return "你连穿月剑法都没练，还谈什么绝招可言？";


        if (me->query_skill("sword") < 140)
                return "你的剑法火候不足，学不了这一招！";

        if (me->query_skill("chuanyue-jian", 1) < 160)
                return "你的穿月剑法修为还不够，自己下去练练再来吧。";

        tell_object(me, HIC "你学会了「皓月穿空」。\n" NOR);

        if (me->can_improve_skill("sword"))
                me->improve_skill("sword", 1500000);
        if (me->can_improve_skill("chuanyue-jian"))
                me->improve_skill("chuanyue-jian", 1500000);
        me->improve_skill("martial-cognize", 1500000);
        me->set("can_perform/chuanyue/hao", 1);


        return 1;
}

mixed ask_skill5()
{
        object me;

        me = this_player();

        if (me->query("can_perform/jiuqu-jian/wan"))
                return "咦？这招你不是已经会了吗？";

        if (me->query("family/family_name") != query("family/family_name"))
                return RANK_D->query_respect(me) + "和本派素无瓜葛，何出此言？";

        if (me->query_skill("jiuqu-jian", 1) < 1)
                return "你连九曲剑法都没练，还谈什么绝招可言？";


        if (me->query_skill("sword") < 140)
                return "你的剑法火候不足，学不了这一招！";

        if (me->query_skill("jiuqu-jian", 1) < 160)
                return "你的九曲剑法修为还不够，自己下去练练再来吧。";

        tell_object(me, HIC "你学会了「九曲十八弯」。\n" NOR);

        if (me->can_improve_skill("sword"))
                me->improve_skill("sword", 1500000);
        if (me->can_improve_skill("jiuqu-jian"))
                me->improve_skill("jiuqu-jian", 1500000);
        me->improve_skill("martial-cognize", 1500000);
        me->set("can_perform/jiuqu-jian/wan", 1);


        return 1;
}
mixed ask_skill6()
{
        object me;

        me = this_player();

        if (me->query("can_perform/poyang-jian/long"))
                return "咦？这招你不是已经会了吗？";

        if (me->query("family/family_name") != query("family/family_name"))
                return RANK_D->query_respect(me) + "和本派素无瓜葛，何出此言？";

        if (me->query_skill("poyang-jian", 1) < 1)
                return "你连破阳冷光剑都没练，还谈什么绝招可言？";


        if (me->query_skill("sword") < 140)
                return "你的剑法火候不足，学不了这一招！";

        if (me->query_skill("poyang-jian", 1) < 180)
                return "你的破阳冷光剑修为还不够，自己下去练练再来吧。";

        tell_object(me, HIC "你学会了「天外玉龙」。\n" NOR);

        if (me->can_improve_skill("sword"))
                me->improve_skill("sword", 1500000);
        if (me->can_improve_skill("poyang-jian"))
                me->improve_skill("poyang-jian", 1500000);
        me->improve_skill("martial-cognize", 1500000);
        me->set("can_perform/poyang-jian/long", 1);


        return 1;
}
mixed ask_skill7()
{
        object me;

        me = this_player();

        if (me->query("can_perform/poyang-jian/xian"))
                return "咦？这招你不是已经会了吗？";

        if (me->query("family/family_name") != query("family/family_name"))
                return RANK_D->query_respect(me) + "和本派素无瓜葛，何出此言？";

        if (me->query_skill("poyang-jian", 1) < 1)
                return "你连破阳冷光剑都没练，还谈什么绝招可言？";


        if (me->query_skill("sword") < 140)
                return "你的剑法火候不足，学不了这一招！";

        if (me->query_skill("poyang-jian", 1) < 160)
                return "你的破阳冷光剑修为还不够，自己下去练练再来吧。";

        tell_object(me, HIC "你学会了「神光再现」。\n" NOR);

        if (me->can_improve_skill("sword"))
                me->improve_skill("sword", 1500000);
        if (me->can_improve_skill("poyang-jian"))
                me->improve_skill("poyang-jian", 1500000);
        me->improve_skill("martial-cognize", 1500000);
        me->set("can_perform/poyang-jian/xian", 1);


        return 1;
}

mixed ask_skill8()
{
        object me;

        me = this_player();

        if (me->query("can_perform/hanbing-zhang/han"))
                return "咦？这招你不是已经会了吗？";

        if (me->query("family/family_name") != query("family/family_name"))
                return RANK_D->query_respect(me) + "和本派素无瓜葛，何出此言？";

        if (me->query_skill("hanbing-zhang", 1) < 1)
                return "你连寒冰掌都没练，还谈什么绝招可言？";


        if (me->query_skill("strike") < 140)
                return "你的掌法火候不足，学不了这一招！";

        if (me->query_skill("hanbing-zhang", 1) < 160)
                return "你的寒冰掌修为还不够，自己下去练练再来吧。";

        tell_object(me, HIC "你学会了「极天寒掌」。\n" NOR);

        if (me->can_improve_skill("strike"))
                me->improve_skill("strike", 1500000);
        if (me->can_improve_skill("hanbing-zhang"))
                me->improve_skill("hanbing-zhang", 1500000);
        me->improve_skill("martial-cognize", 1500000);
        me->set("can_perform/hanbing-zhang/han", 1);


        return 1;
}
mixed ask_skill9()
{
        object me;

        me = this_player();

        if (me->query("can_perform/hanbing-zhang/heng"))
                return "咦？这招你不是已经会了吗？";

        if (me->query("family/family_name") != query("family/family_name"))
                return RANK_D->query_respect(me) + "和本派素无瓜葛，何出此言？";

        if (me->query_skill("hanbing-zhang", 1) < 1)
                return "你连寒冰掌法都没练，还谈什么绝招可言？";


        if (me->query_skill("strike") < 140)
                return "你的掌法火候不足，学不了这一招！";

        if (me->query_skill("hanbing-zhang", 1) < 160)
                return "你的寒冰掌法修为还不够，自己下去练练再来吧。";

        tell_object(me, HIC "你学会了「踏雪无痕」。\n" NOR);

        if (me->can_improve_skill("strike"))
                me->improve_skill("strike", 1500000);
        if (me->can_improve_skill("hanbing-zhang"))
                me->improve_skill("hanbing-zhang", 1500000);
        me->improve_skill("martial-cognize", 1500000);
        me->set("can_perform/hanbing-zhang/heng", 1);


        return 1;
}
mixed ask_skill10()
{
        object me;

        me = this_player();

        if (me->query("can_perform/songshan-zhang/po"))
                return "咦？这招你不是已经会了吗？";

        if (me->query("family/family_name") != query("family/family_name"))
                return RANK_D->query_respect(me) + "和本派素无瓜葛，何出此言？";

        if (me->query_skill("songshan-zhang", 1) < 1)
                return "你连嵩山掌法都没练，还谈什么绝招可言？";


        if (me->query_skill("strike") < 140)
                return "你的掌法火候不足，学不了这一招！";

        if (me->query_skill("songshan-zhang", 1) < 160)
                return "你的嵩山掌法修为还不够，自己下去练练再来吧。";

        tell_object(me, HIC "你学会了「破山斧」。\n" NOR);

        if (me->can_improve_skill("strike"))
                me->improve_skill("strike", 1500000);
        if (me->can_improve_skill("songshan-zhang"))
                me->improve_skill("songshan-zhang", 1500000);
        me->improve_skill("martial-cognize", 1500000);
        me->set("can_perform/songshan-zhang/po", 1);


        return 1;
}
mixed ask_skill11()
{
        object me;

        me = this_player();

        if (me->query("can_perform/songshan-quan/chui"))
                return "咦？这招你不是已经会了吗？";

        if (me->query("family/family_name") != query("family/family_name"))
                return RANK_D->query_respect(me) + "和本派素无瓜葛，何出此言？";

        if (me->query_skill("songshan-quan", 1) < 1)
                return "你连嵩山拳法都没练，还谈什么绝招可言？";


        if (me->query_skill("cuff") < 140)
                return "你的拳法火候不足，学不了这一招！";

        if (me->query_skill("songshan-quan", 1) < 160)
                return "你的嵩山拳法修为还不够，自己下去练练再来吧。";

        tell_object(me, HIC "你学会了「千斤锤」。\n" NOR);

        if (me->can_improve_skill("cuff"))
                me->improve_skill("cuff", 1500000);
        if (me->can_improve_skill("songshan-quan"))
                me->improve_skill("songshan-quan", 1500000);
        me->improve_skill("martial-cognize", 1500000);
        me->set("can_perform/songshan-quan/chui", 1);


        return 1;
}