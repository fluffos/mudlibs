inherit ROOM;

void create()
{
        set("short", "幽深隧道");
        set("long",@LONG
这是一条幽深的隧道深不见底。
LONG);

	set("exits", ([
		"west"   : __DIR__"road",
		"up"   : __DIR__"huodong1",

	]));
    set("objects", ([
		"d/shenlong/npc/sai" : 1,

    ]));
        setup();
}
