// Room: /city/zahuopu.c
inherit ROOM;

void create()
{
	set("short", "师门办事处");
	set("long", @LONG
师门办事处是各大正邪中立门派领取任务的地方。
LONG );
	
	set("no_fight",1);
	set("exits", ([
	      "east"  : __DIR__"menpaizheng",
		"west"  : __DIR__"menpaixie",
		"north" : __DIR__"menpaizhong",
		"down" : __DIR__"guangchang",

	]));
	set("objects", ([
	]));
	setup();
	replace_program(ROOM);
}

