// Room: /city/zahuopu.c
inherit ROOM;

void create()
{
	set("short", "市集");
	set("long", @LONG
这是一广场大榕树下的市集，大箱小箱堆满了一地，都是一些交易用品。
LONG );
	set("item_desc", ([
		"纹身指南": "纹身需要弄到各类图纸才可以找青阳子纹哦，而图纸需要打火麒麟才会有机率爆出！\n",
	]));
	set("no_fight",1);
	set("exits", ([
		"up" :  __DIR__"level_up",
		"down" : __DIR__"guangchang",

	]));
	set("objects", ([
		"adm/npc/qingyangzi": 1,
	//	"d/city/npc/baibao": 1,
	]));
	setup();
	replace_program(ROOM);
}

