// Room: /city/nandajie1.c
inherit ROOM;

void create()
{
	set("short", "杨玉环居");
	set("long", @LONG
这是一件灯光昏暗的房间，一张大床占据了房间一半岛面积，边上各种器具看的人面红耳赤。
LONG );
	set("outdoors", "city");
	set("no_fight", 1);
	set("exits", ([

		"south" : __DIR__"kedian",
		
	]));
	set("objects", ([
		__DIR__"npc/yang" : 1,

	]));

	setup();
	replace_program(ROOM);
}

