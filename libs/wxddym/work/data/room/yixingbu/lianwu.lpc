// File(/data/room/yixingbu/lianwu.c) of yixingbu's room
// Create by LUBAN written by Doing Lu

     inherit PRIVATE_ROOM;

void create()
{
	set("short", "练武场");
	set("long", "此处便是水若寒苑的练武场，人在江湖，武功万万不可荒废。
");

	set("exits", ([
                "west" : __DIR__"houyuan",
	]));

        set("objects", ([
                "/clone/npc/mu-ren" : 4,
        ]));

        set("outdoors", "nanhai");
        set("no_sleep_room", 1);

        setup();
    
        set("room_owner", "水若寒");
        set("room_name", "水若寒苑");
        set("room_id", "srhy");
        set("room_owner_id", "yixingbu");
        set("room_position", "平原小路");
}
