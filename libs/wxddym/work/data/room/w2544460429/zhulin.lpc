// File(/data/room/w2544460429/zhulin.c) of w2544460429's room
// Create by LUBAN written by Doing Lu

     inherit PRIVATE_ROOM;

void create()
{
	set("short", "竹林");
	set("long", "这里种着好大一片竹林，非常幽静。风一起就听竹叶沙沙的响，
有时无语就去听涛阁默听竹浪涛声，参悟武功。
");

	set("exits", ([
                "west" : __DIR__"huayuan",
                "east" : __DIR__"tingtao",
	]));

        set("outdoors", "xx");
        set("sleep_room", 1);
        setup();

        set("room_owner", "无语");
        set("room_name", "静默阁");
        set("room_id", "jmg");
        set("room_owner_id", "w2544460429");
        set("room_position", "紫气台");
}
