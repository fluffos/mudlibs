// File(/data/room/w2544460429/jusuo.c) of w2544460429's room
// Create by LUBAN written by Doing Lu

#include <room.h>

     inherit PRIVATE_ROOM;

void create()
{
	set("short", "居所");
	set("long", "这就是静默阁主人无语休息、读书的地方。楼上就是主人的卧室，
西面则是书房。
");

	set("exits", ([
		"up"   : __DIR__"woshi",
                "west" : __DIR__"shufang",
	]));

        set("no_sleep_room", 1);
        setup();

        set("KEY_DOOR", ([
                "exit" : "east",
                "room" : __DIR__"huayuan",
        ]));
    
        set("room_owner", "无语");
        set("room_name", "静默阁");
        set("room_id", "jmg");
        set("room_owner_id", "w2544460429");
        set("room_position", "紫气台");
}
