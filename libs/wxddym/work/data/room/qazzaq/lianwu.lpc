// File(/data/room/qazzaq/lianwu.c) of qazzaq's room
// Create by LUBAN written by Doing Lu

     inherit PRIVATE_ROOM;

void create()
{
	set("short", "练武场");
	set("long", "此处便是苏安河的练武场，人在江湖，武功万万不可荒废。
");

	set("exits", ([
                "west" : __DIR__"houyuan",
	]));

        set("objects", ([
                "/clone/npc/mu-ren" : 4,
        ]));

        set("outdoors", "city");
        set("no_sleep_room", 1);

        setup();
    
        set("room_owner", "慕容秋枫");
        set("room_name", "苏安河");
        set("room_id", "sah");
        set("room_owner_id", "qazzaq");
        set("room_position", "草地");
}
