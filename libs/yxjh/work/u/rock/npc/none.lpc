#include <ansi.h>

inherit NPC;

void create() {
  set_name("未找到物件", ({ "path error" }));
  set("gender", "男性");
  set("age", 56);


  set("long", "这个家伙是没找到物件的代替品。\n");
  set("no_die", 1);
  set("no_suck", 1);
  setup();
}
