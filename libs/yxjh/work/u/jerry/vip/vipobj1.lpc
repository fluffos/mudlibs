//edit by rock
//2008.10.22

#include <localtime.h>
#include <ansi.h>
inherit ITEM;

//函数生命
int GiveVipItem(object me);

// 函数：所属类别
int query_xy() { return 1; }

//结构函数
void create() {
  set_name(HIY "一月贵宾卡" NOR, ({ "vip card1", "card" }));
  set("long",
    "拥有了这个道具你将成为浴血江湖的VIP贵宾成员。指令：upvip\n"
  );
  set("unit", "张");
}

void init() {
  add_action("do_vip", "upvip");
}

int do_vip(string arg) {
  object me, ob;
  me = this_player();
  ob = this_object();


  if (GiveVipItem(me)) {
    destruct(ob);
    return 1;
  } else {
    tell_object(me, HIR "升级贵宾权限失败，请联系Wiz。\n" NOR);
    return 1;
  }
  return 1;
}

//函数：赠送道具
int GiveVipItem(object me) {
  int time, i;
  mixed localtime;

  object shiyigift;


  if (!me) return 0;

  if (!me->query("jym_level/vip_time")) time = time() + 24 * 3600 * 31;
  else time = me->query("jym_level/vip_time") + 24 * 3600 * 31;
  localtime = localtime(time);

  message_vision(BLINK + HIR "$N成为了浴血江湖 VIP贵宾!恭喜恭喜\n" NOR, me);
  tell_object(
    me,
    sprintf(
      HIG "贵宾权限到期时间%d年%d月%d日%d时%d分%d秒。\n" NOR,
      localtime[LT_YEAR],
      1 + localtime[LT_MON],
      localtime[LT_MDAY],
      localtime[LT_HOUR],
      localtime[LT_MIN],
      localtime[LT_SEC]
    )
  );
  me->set("jym_level/vip", 1);
  me->set("jym_level/vip_time", time);

  if (!present("ji qimao", me) && shiyigift = new("/u/jerry/vip/ckobj")) {
    shiyigift->move(me);
    tell_object(me, HIY "你得到了机器猫。\n" NOR);
  } else tell_object(me, HIY "你已经拥有机器猫了。\n" NOR);

  if (!me->query("jerryjym") && !present(
    "room box",
    me
  ) && shiyigift = new("/u/jerry/vip/roombox")) {
    shiyigift->move(me);
    tell_object(me, HIY "你得到了浴血江湖 房间包裹。\n" NOR);
  } else tell_object(me, HIY "你已经拥有或使用过房间包裹了。\n" NOR);
  me->save();
  return 1;
}
