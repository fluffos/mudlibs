// Room: /city/beidajie1.c
// YZC 1995/12/04 
inherit ROOM;
void create()
{
	set("short", "府邸大门");
	set("long", @LONG
	这里是一扇气势如虹的大门，看来里面的人一定是
身价威望比较高的人。
LONG
	);
        set("outdoors", "city");

	set("exits", ([
		"enter" : __DIR__"keting",
	]));

	setup();
}
//是镜像
int is_mirror() { return 1; }
int kaishi() { return 1; }
