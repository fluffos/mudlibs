// edit by jerry@jhfy.nh 2002
#include <room.h>
inherit ROOM;

void create() {
  set("short", "长廊");
  set("long", @LONG
一条笔直的长廊，走廊两边的墙上刻着流传以旧的诗词歌赋，
还有各种名画，可见此间主人对其的喜爱。北方有一间休息室.
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "sleep_room2",
    "west": __DIR__ "lang4",
    "east": __DIR__ "lang1",
  ]));
  set("no_clean_up", 0);
  set("objects", ([
  ]));
  setup();
}
