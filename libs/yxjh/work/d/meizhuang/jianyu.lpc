inherit ROOM;

void create() {
  set("short", HIB "监狱" NOR);
  set("long", @LONG
这里就是囚禁任我行的地方，四周乱七八糟，臭气
冲天。 
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/d/heimuya/npc/renwoxing.lpc": 1,
  ]));

  set("no_fight", "1");
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
