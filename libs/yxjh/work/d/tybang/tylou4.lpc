inherit ROOM;
void create()
{
        set("short", HIR"三楼"NOR);
        set("long", @LONG
第一楼的特色也差不多，为了促进练习,每上一层身体都会
更重一些，这样使人的身体变得更轻,犹如到了无重空间一样，
忘记了重量的存在。增加了练习轻功的成果。
LONG
        );
        set("outdoors", "tybang");
        set("exits", ([
                "down" : __DIR__"tylou3",                            
                "up" : __DIR__"tylou5",                
        ]));
        set("objects", ([
        ]));
        setup();
}
int init()
{
        add_action("do_nocmds", "home");
        add_action("do_nocmds", "conjure");
        add_action("do_nocmds", "qigong");                
}
int do_nocmds(string arg)
{
        write("对不起，《浴血江湖》第一城禁止使用该指令！\n");
        return 1;
}
