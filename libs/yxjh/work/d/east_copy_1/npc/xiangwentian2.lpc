//edit by rock
//2008.10.20

#include <ansi.h>
inherit NPC;

int is_boss() { return 1; }
void create()
{
	set_name("向问天", ({ "xiang wentian"}));
	set("gender", "男性");
	set("age",56);
	set("long", "日月神教使者向问天，近些年隐姓埋名，四处打探教主下落。\n");

	set("no_kill",1);

	setup();
}