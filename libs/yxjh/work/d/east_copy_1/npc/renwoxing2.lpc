//edit by rock
//2008.10.20

#include <ansi.h>
inherit NPC;

int is_boss() { return 1; }
void create()
{
	set_name("任我行", ({ "ren woxing"}));
	set("gender", "男性");
	set("age",56);
	set("long", "传说中的日月神教教主，后来不明原因失踪。\n");

	set("no_kill",1);

	setup();
}