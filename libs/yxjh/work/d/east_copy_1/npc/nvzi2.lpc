//edit by rock
//2008.10.20

#include <ansi.h>
inherit NPC;

int is_jinying() { return 1; }
int ask_name();

void create()
{
	set_name("女子", ({ "nv zi"}));
	set("gender", "女性");
	set("age",26);
	set("long", "一位亭亭玉立，美若天仙的女子，举止优雅。\n");

	set("no_kill",1);

	setup();
}