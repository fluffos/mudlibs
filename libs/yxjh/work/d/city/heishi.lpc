//make by rock
#include <ansi.h>;
inherit ROOM;

void create()
{
        set("short", "黑市");
        set("long", @LONG
这里出售和收购一些草药，法器等物品，但这些物品的来历就无法
说清，而且这些也是严格保密的。
LONG
        );
        set("no_fight",1);
        set("no_steal",1);
        set("no_beg",1);
        set("objects", ([
//			__DIR__"obj/liandanlu" : 1,
			"/u/rock/npc/heishi" : 1,
			]));
        set("exits", ([
                "south" : __DIR__"happyjie",
			]));
        setup();
}