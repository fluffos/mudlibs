// xroad1.c
// cly@yxjh

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short",HBK"恶人谷小道"NOR);
        set("long", @LONG
一条弯曲的小道，看起来倒是干干净净，不过似乎有腐烂的气息弥留在空中
。小道两边成片的大树，让你立刻醒悟到，这条道是有人将树林从中切开铺成的
，你心中不禁有一些敬仰这个人。

LONG );
        set("objects", ([
        ]) );

		set("exits", ([
			"south" : __DIR__"pomiao",
			"east"  : __DIR__"xiaoting",
		]) );

        setup();

		replace_program(ROOM);
}

