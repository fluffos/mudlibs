//未开光的套装
inherit ITEM;

int is_kaiguang() { return 1; }

void create() {
  set_name("套装", ({ "tz" }));
  set_weight(350);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一件未开光的装备，无法使用。请到副本管理员处开光。\n");
    set("unit", "件");
  }
  setup();
}
