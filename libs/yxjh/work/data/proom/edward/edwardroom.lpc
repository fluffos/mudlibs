//edit by rock

#include "playerme.h"
#include "/d/rock/proom/zyao.lpc"
inherit ROOM;

int is_proom() { return 1; }

void create() {
  set("short", "温馨小家");
  set("long", @LONG
回家最舒服了，不是吗! 
LONG
  );
  set("exits", ([
    "out": "/d/taishan/fengchan",
  ]));

  set("no_new_clean_up", 0);
  set("no_magic", 1);
  set("objects", ([
    "/data/proom/" + PLAYNAME + "/" + PLAYNAME: 1,
    "/d/shaolin/npc/mu-ren": 4,

  ]));

  set("no_sleep_room", 1);
  set("pname", PLAYNAME);
  setup();

}
