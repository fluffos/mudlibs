// junquest_fail
#include <ansi.h>
#include <condition.h>
inherit F_CLEAN_UP;
int update_condition(object me, int duration) {
  me->apply_condition("can_dushu", duration - 1);
  tell_object(me, HIG "你已经请教了一段时间！\n" NOR);
  if (duration < 1) return 0;
  return CND_CONTINUE;
}
