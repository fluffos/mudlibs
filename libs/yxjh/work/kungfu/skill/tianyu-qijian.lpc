inherit SKILL;
string type() { return "zhongji"; }
int tydamage = (this_player()->query("tydamage/sword") ? this_player()->query("tydamage/sword") : this_player()->query_skill("tianyu-qijian")) * 15;

mapping *action = ({
  ([ "action": "$N使一式「海天一线」，手中$w嗡嗡微振，幻成一条疾光刺向$n的$l",
    "force": 560,
    "dodge": 280,
    "parry": 240,
    "damage": tydamage,
    "lvl": 0,
    "skill_name": BLK "海天一线" NOR,
    "damage_type": "割伤"
  ]),
  ([ "action": "$N错步上前，使出「闪电惊虹」，手中$w划出一道剑光劈向$n的$l",
    "force": 560,
    "dodge": 280,
    "parry": 240,
    "damage": tydamage,
    "lvl": 9,
    "skill_name": RED "闪电惊虹" NOR,
    "damage_type": "割伤"
  ]),
  ([ "action": "$N手中$w一抖，一招「日在九天」，斜斜一剑反腕撩出，攻向$n的$l",
    "force": 560,
    "dodge": 280,
    "parry": 240,
    "damage": tydamage,
    "lvl": 18,
    "skill_name": GRN "日在九天" NOR,
    "damage_type": "割伤"
  ]),

  ([ "action": "$N手中剑锵啷啷长吟一声，一式「" + BLU + "咫尺天涯" + NOR + "」，一道剑光飞向$n的$l",
    "force": 560,
    "dodge": 280,
    "parry": 240,
    "damage": tydamage,
    "lvl": 120,
    "skill_name": BLU "咫尺天涯" NOR,
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N一式「" + MAG + "怒剑狂花" + NOR + "」，手中$w舞出无数剑花，使$n难断虚实，无可躲避",
    "force": 560,
    "dodge": 280,
    "parry": 240,
    "damage": tydamage,
    "lvl": 140,
    "skill_name": MAG "怒剑狂花" NOR,
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N手中$w斜指苍天，剑芒吞吐，一式「" + BLU + "九弧震日" + NOR + "」，对准$n的$l斜斜击出",
    "force": 560,
    "dodge": 280,
    "parry": 240,
    "damage": tydamage,
    "lvl": 160,
    "skill_name": BLU "九弧震日" NOR,
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N一式「" + WHT + "漫天风雪" + NOR + "」，手腕急抖，挥洒出万点金光，刺向$n的$l" NOR,
    "force": 560,
    "dodge": 280,
    "parry": 240,
    "damage": tydamage,
    "lvl": 1050,
    "skill_name": WHT "漫天风雪" NOR,
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N一式「" + HIB + "天河倒泻" + NOR + "」，$w飞斩盘旋，如疾电般射向$n的胸口" NOR,
    "force": 560,
    "dodge": 280,
    "parry": 240,
    "damage": tydamage,
    "lvl": 1100,
    "skill_name": HIR "天河倒泻" NOR,
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N一式「" + HIR + "天外飞仙" + NOR + "」，$w突然从天而降，一片金光围掠$n全身" NOR,
    "force": 560,
    "dodge": 280,
    "parry": 240,
    "damage": tydamage,
    "lvl": 1200,
    "skill_name": HIY "天外飞仙" NOR,
    "damage_type": "挫伤"
  ])
});
int valid_enable(string usage) { return usage == "sword" || usage == "parry"; }
int practice_level() { return 60; }
mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("tianyu-qijian", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level > action[i - 1]["lvl"])
      return action[NewRandom(i, 20, level / 5)];
}

mixed hit_ob(object me, object victim, int damage_bonus) {
  if (damage_bonus < 110) return 0;
  if (me->query("neili") < 50000) return 0;
  if (random(10) < 2) return 0;
  //  第六等级的伤气
  if (me->query_skill("tianyu-qijian", 1) > 4000
    && me->query_skill("bahuang-gong", 1) > 1499
    && me->query("jym_level/level") >= 25) {
    victim->receive_damage("qi", (damage_bonus) * 2, me);
    return HIC "$N的八荒六合唯我独尊功与天宇奇剑完美结合，剑气如冰雨般刺向$n！！！\n" NOR;

  }

  //  第五等级的伤气
  if (me->query_skill("tianyu-qijian", 1) > 3000
    && me->query_skill("bahuang-gong", 1) > 1499
    && me->query("jym_level/level") >= 15) {
    victim->receive_damage("qi", damage_bonus, me);
    return WHT "$N运起八荒六合唯我独尊功，配合天宇奇剑发出强大剑气直扑$n！！！！！\n" NOR;
  }
  if (damage_bonus < 110) return 0;

  //  第四等级的伤气
  if (me->query_skill("tianyu-qijian", 1) > 2000
    && me->query_skill("bahuang-gong", 1) > 1499
    && me->query("jym_level/level") >= 10) {
    victim->receive_damage("qi", (damage_bonus - 100), me);
    return MAG "$N运起八荒六合唯我独尊功，配合天宇奇剑发出强大剑气直扑$n！！！！！\n" NOR;
  }

  //  第三等级的伤气
  if (me->query_skill("tianyu-qijian", 1) > 1000
    && me->query_skill("bahuang-gong", 1) > 500
    && me->query("jym_level/level") >= 10) {
    victim->receive_damage("qi", (damage_bonus - 100) / 3, me);
    return CYN "$N的八荒六合唯我独尊功豁然和天宇奇剑融会贯通，一股剑气激射而出！！！\n" NOR;
  }
  // 第二等级的伤气
  if (me->query_skill("tianyu-qijian", 1) > 400
    && me->query_skill("bahuang-gong", 1) > 300
    && me->query("jym_level/level") >= 2) {
    victim->receive_damage("qi", (damage_bonus - 100) / 8, me);
    return RED "$N的八荒六合唯我独尊功忽然与天宇奇剑融会贯通，一小股剑气随心而出！\n" NOR;
  }
}


int practice_skill(object me) {
  object weapon;
  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "sword")
    if (!objectp(weapon = me->query_temp("secondary_weapon"))
      || (string)weapon->query("skill_type") != "sword")
      return notify_fail("你使用的武器不对。\n");
  if ((int)me->query("qi") < 50)
    return notify_fail("你的体力不够练天羽奇剑。\n");
  me->receive_damage("qi", 25);
  return 1;
}
int valid_learn(object me) {
  object weapon;
  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "sword")
    if (!objectp(weapon = me->query_temp("secondary_weapon"))
      || (string)weapon->query("skill_type") != "sword")
      return notify_fail("你还是先找把剑来吧。\n");
  if (me->query("gender") != "女性")
    return notify_fail("灵鹫宫绝学只授女弟子\n");

  if (me->query_skill("tianyu-qijian") < 250 && me->query("family/master_id") != "tong lao")
    return notify_fail("天宇奇剑乃灵鹫宫绝技，你只能找童佬学习？\n");
  if (me->query_skill("sword", 1) <= me->query_skill("tianyu-qijian", 1))
    return notify_fail("你的基础不够，无法领会更高深的技巧。\n");
  if ((int)me->query_skill("bahuang-gong", 1) < 40)
    return notify_fail("你的八荒神功火候太浅。\n");
  if ((int)me->query("max_neili") < 20000)
    return notify_fail("你的内力不够。\n");

  return 1;
}


string perform_action_file(string action) {
  return __DIR__ "tianyu-qijian/" + action;
}
