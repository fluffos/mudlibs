//edit by rock
int main(object me, string arg) {
  object item;

  if (!arg) return 0;

  if (!objectp(item = present(arg, me))) return notify_fail("你身上没有这样东西。\n");
  return item->do_drink(item->query("id"));
}
