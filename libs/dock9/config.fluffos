###############################################################################
#              Customizable runtime config file for MudOS v22                 #
###############################################################################

# name of this mud
name : Dock 9

# port number to accept users on
port number : 40266

# absolute pathname of mudlib
mudlib directory : /home/sunyc/src/mudlib/libs/dock9/work

# debug.log and author/domain stats are stored here
log directory : /log

# the directories which are searched by #include <...>
include directories : /adm/include:/include

# The global include file is included automatically before every compile.
global include file : <global.h>

# Directory to save binaries in.  (if BINARIES is defined)
save binaries directory : /adm/temp

# the file which defines the master object
master file : /adm/obj/master

# the file where all global simulated efuns are defined.
simulated efun file : /adm/obj/simul_efun

# alternate debug.log file name (assumed to be in specified 'log directory')
debug log file : debug.log

# Same LPUniversity/Sapidlib-lineage requirement as lpuni (see
# libs/lpuni/config.fluffos): this codebase's ansi_parser.lpc pinkfish-tag
# colour system needs catch_tell() to actually fire for interactive
# players. The archive's own etc/local_options explicitly turns this on
# (`#define INTERACTIVE_CATCH_TELL`) -- without it, this driver's default
# (0) sends interactive player output straight to the socket via
# add_message(), bypassing catch_tell() entirely, so every %^TAG%^
# pinkfish marker renders as literal text instead of real ANSI escapes.
interactive catch tell : 1

time to clean up : 120000
time to swap : 0
time to reset : 1800
maximum bits in a bitfield : 1200
maximum local variables : 50
maximum evaluation cost : 10000000
maximum array size : 15000
maximum buffer size : 400000
maximum mapping size : 1500000
inherit chain size : 30
maximum string length : 500000
maximum read file size : 500000
maximum byte transfer : 10000
hash table size : 7001
object table size : 1501
default fail message : What?
default error message : You feel a disturbance in the force.

maximum users : 40
evaluator stack size : 1000
compiler stack size : 200
maximum call depth : 30
living hash table size : 100
