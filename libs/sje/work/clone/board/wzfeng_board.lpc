// the board by wzfeng

inherit BULLETIN_BOARD;

void create()
{
        set_name("浪翻云的留言板", ({ "board" }) );
     set("location", "/u/wzf/workroom");
   set("board_id", "wzfeng_board");
        set("long", "这是浪翻云的留言板。\n" );
        setup();
        set("capacity", 99);
        replace_program(BULLETIN_BOARD);
}

