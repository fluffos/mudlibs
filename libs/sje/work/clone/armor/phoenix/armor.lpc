// armor.c
// 朱雀之舞 [灵异]
// 成套装备
// by sunyf@sj2
#include <ansi.h>
#include <armor.h>

inherit CLOTH;


void create() {
  set_name(RED "朱雀之羽" NOR, ({ "phoenix armor", "armor", "jia" }));
  set_weight(1500);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "件");
    set(
      "long",
      RED "这是一件五彩斑斓的羽衣。" NOR "
防御指数：一百二十
成套装备：" HIR "[朱雀之舞]" NOR " " RED "朱雀之羽" NOR " " YEL "朱雀之眼" NOR " " GRN "朱雀之爪" NOR " " HIC "朱雀之翼" NOR "
特殊效果：" HIG "加快经脉运行(force)" NOR "\n"
    );
    set("value", 100000);
    set("material", "feather");
    set("wear_msg", RED "只见$N披上一件五彩斑斓的羽衣,顿时满堂生辉。\n" NOR);
    set("armor_prop/force", 10);
    set("armor_prop/armor", 120);
    set("warm_cloth", 1);
    set("power", 10);
  }
  setup();
}


/*
int ob_hit(object ob, object me, int damage)
{
        
        string msg;
	 
        if(damage<50) return 0;
        if(me->query_temp("lingyieff")&&(random(me->query_temp("lingyieff")+142)>70))
        {
if(random(6)<1)
msg = HIC"只见光芒大放，$n竟然好似腾空飞起，$N的攻击顿时全落空了！\n"NOR;   
else{
msg = HIM"只见光芒大放，$N攻击之处，$n所穿的羽衣竟然根根羽毛随力道流转，化解了$N的攻击！\n"NOR;  
     damage=damage*(random(5)+1)/10;
}
        
        message_vision(msg, ob,me);
	 
 	 
       damage=damage*(random(2)+1)/8;
	 
        return -damage;
        }
}
*/
