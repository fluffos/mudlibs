// u/keinxin/biwu.c
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIR "武斗场" NOR);
  set("long", @LONG
这是一个宏伟的武斗场。方圆极其开阔。旁边是高高的看台，上面红旗飘动。
两旁的兵器架上插满了各种兵器。
到了这里你不禁热血沸腾，想要好好的展示一下武功。
LONG
  );
  set("outdoors", "扬州");

  set("exits", ([

  ]));

  set("objects", ([

  ]));
  setup();
}
