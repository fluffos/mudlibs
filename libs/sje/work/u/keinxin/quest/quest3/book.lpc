// fojing1.c

inherit ITEM;

void create()
{
	set_name("房间指南", ({ "book" }));
	set_weight(200);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "本");
		set("long", "这是一本书。\n");
		set("value", 500);
		set("material", "paper");
	
	}
}
