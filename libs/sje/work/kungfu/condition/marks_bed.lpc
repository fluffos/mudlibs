// marks_bed.c 

#include <login.h>

int update_condition(object me, int duration)
{
        if (duration < 1) {
              me->clear_condition("marks_bed");
                  }
        if (!duration) return 0;
        me->apply_condition("marks_bed", duration - 1);
        return 1;
}
