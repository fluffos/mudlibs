// zm_job.c

#include <ansi.h>

int update_condition(object me, int duration) {
  if (duration <= 1) {
    me->delete_temp("renwu");
    return 0;
  }
  me->apply_condition("zm_job", duration - 1);
  return 1;
}
