int main(object me, string arg) {
  object *ob;
  int i;

  if (!arg) return 0;
  ob = filter_array(children(arg), (: clonep :));
  for (i = 0; i < sizeof(ob); i++) printf("%O %O\n", ob[i], environment(ob[i]));
  return 1;
}
