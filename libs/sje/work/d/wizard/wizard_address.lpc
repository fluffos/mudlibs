// Room: /d/wizard/wizard_address.c

inherit ROOM;

void create() {
  set("short", "巫师联络室");
  set("long", @LONG
这里是巫师的联络通讯室，所有巫师的联络地址，都写在这里，以便相互联络。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "wizard_room",
  ]));

  set("valid_startroom", 1);
  set("no_fight", "1");
  set("no_clean_up", 0);
  setup();

  call_other("/clone/board/wiz_l", "???");
  replace_program(ROOM);
}
