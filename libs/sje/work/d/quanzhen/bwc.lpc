// Room: bwc.c

#include <room.h>
inherit ROOM;

void create()
{
        set("short", "比武场");
        set("long", @LONG
这里是全真教的比武场，广场的两边放着十八般兵器，全真教自王重
阳传下门规，每年除夕前三日，门下第子大较武功，考查这一年来各人的
进境。
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"guangchang",
 ]));
        set("no_clean_up", 0);
        set("outdoors", "xxx");

        setup();
        replace_program(ROOM);
}
