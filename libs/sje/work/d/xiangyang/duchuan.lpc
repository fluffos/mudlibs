// Room: /d/city/duchuan.c

inherit ROOM;

void create() {
  set("short", "渡船");
  set("long", @LONG
一叶小舟，最多也就能载七、八个人。一名六十多岁的老艄公手持一根长
竹篙，正在船尾吃力地撑着船。
LONG
  );
  set("no_fight", 1);

  set("no_save", 1);

  setup();
  set("outdoors", "xiangyang");
  replace_program(ROOM);
}
