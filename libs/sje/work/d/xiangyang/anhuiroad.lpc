// /d/xiangyang/anhuiroad.c

inherit ROOM;

void create()
{
	set("short", "土路");
	set("long", @LONG
你走在一条土路上，偶尔有马匹经过，搞得尘土飞扬。这里是安徽境内，
可能会有山贼出没。往东进入江苏，西边是湖北。
LONG
	);
        set("outdoors", "安徽");

	set("exits", ([
		"west" : __DIR__"outeroad3",
                "east" : "/d/city/wroad3",
	]));

	setup();
	replace_program(ROOM);
}

