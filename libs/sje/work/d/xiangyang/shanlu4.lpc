inherit ROOM;

void create() {
  set("short", "山间小路");
  set("long", @LONG
你进入了一片小森林，走在林间小路上，密密麻麻的树林挡住了阳光，
这里显得十分阴暗，树木交错纵横，这里没有什么出路了.
LONG
  );
  set("outdoors", "襄阳");

  set("exits", ([
    "north": __DIR__ "shanlu3",
  ]));
  set("objects", ([
    "/d/dali/npc/mu_wanqing": 1,
  ]));

  setup();
}
