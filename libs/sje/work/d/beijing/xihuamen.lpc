// Room: /d/beijing/xuetang.c
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIC "西华门" NOR);
  set("long", @LONG
面前是两层楼高的城楼，门前空地足有麦场大，大门是开着的，不时
有些太监宫女进进出出，门外那几十个御林禁军正一丝不苟的检验着往来
人员的腰牌以核对身份。
LONG
  );
  set("outdoors", "京城");

  set("exits", ([
    "east": __DIR__ "xiananmen",
    "west": __DIR__ "ximendajie",
  ]));

  setup();
  replace_program(ROOM);
}
