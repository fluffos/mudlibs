// Room: /d/beijing/qianqiuting.c

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", HIC"千秋亭"NOR);
        set("long", @LONG
这亭子全用竹子搭建而成，内里一色儿都是朴而不拙的竹木器具，亭
外的杨柳暗绿，西北风飒飒吹来，遍地的落叶婆娑起舞，另是一番情致。
LONG
        );
        set("outdoors", "京城");

        set("exits", ([
                 "northeast" :__DIR__"yuhuayuan",
        ]));
        
        setup();
        replace_program(ROOM);
}

