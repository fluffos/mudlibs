// Room: /d/beijing/picai.c
#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", HIC"辟才胡同"NOR);
        set("long", @LONG
一条热闹繁华的胡同，离皇城只有几步之遥。住这里的多半是在外省京
官的家眷，其余多少也都扯得上关系，生活富足，无忧无虑。
LONG
        );
        set("outdoors", "京城");

        set("exits", ([
                "south" : __DIR__"tianshui",
                "east" : __DIR__"tiantan",
                "west" : __DIR__"qianmen2",
        ]));
        
        setup();
        replace_program(ROOM);
}

