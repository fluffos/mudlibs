// /d/mr/hssg.c
// by akuma 7:54 PM 3/6/2002

#include <ansi.h>
inherit ROOM;
inherit F_QUEST;

mapping *books = ({ 
([ "name": "肘後备急方", "id": "ji fang", "exp_reward": 50, "pot_reward": 10, ]),
([ "name": "寓意草", "id": "yuyi cao", "exp_reward": 50, "pot_reward": 10, ]),
([ "name": "三冈识略", 	"id":  "shi lue", "exp_reward": 80, "pot_reward": 40, ]),
([ "name": "药理经", "id": "yaoli jing", "exp_reward": 200, "pot_reward": 100, ]),
([ "name": "易经", "id": "yi jing", "exp_reward": 220, "pot_reward": 100, ]),
([ "name": "太极拳经", "id": "shu", "exp_reward": 200, "pot_reward": 100, ]),
([ "name": "道德经「下卷」", "id": "jing", "exp_reward": 100, "pot_reward": 25, ]),
([ "name": "道德经「上卷」", "id": "jing", "exp_reward": 100, "pot_reward": 25, ]),
([ "name": "道德经「第一章」", "id": "jing", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "道德经「第二章」", "id": "jing", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "道德经「第八章」", "id": "jing", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "道德经「第十三章」", "id": "jing", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "道德经「第十六章」", "id": "jing", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "道德经「第十八章」", "id": "jing", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "太极十三势", "id": "shu", "exp_reward": 100, "pot_reward": 40, ]),
([ "name": "拳谱", "id": "quan pu", "exp_reward": 50, "pot_reward": 25, ]),
([ "name": "洗髓经", "id": "xisui", "exp_reward": 300, "pot_reward": 100, ]),
([ "name": "风云手手法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "大金刚拳法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "般若掌法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "龙爪功法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "摩诃指法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "一指禅功", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "伏魔剑法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "如来千叶手手法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "罗汉拳法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "寂灭爪法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "修罗刀法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "弹腿腿法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "普渡杖法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "如影随形腿腿法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "散花掌法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
([ "name": "韦陀棍法", "id": "wuji", "exp_reward": 150, "pot_reward": 75, ]),
});

void create()
{
	set("short",MAG"还施水阁"NOR);
	set("long", @long
这就是举世闻名的姑苏慕容的还施水阁，这里据说有很多武林人事向
往以久的武功。只见墙上有一个书架子，上面摆满了各种武籍，但是有些
地方却留有空位，每个空位处都放有一张子条(paper)。
long);
	set("exits",([
		"east":__DIR__"hssg",
	]));
	set("item_desc", ([
		"paper" : "上面写着还施水阁中所尚缺武籍的名称。\n",
	]));
	setup();
}
void init()
{
        add_action ("do_kan", ({"kan", "看"}));
	add_action ("do_put", ({"fang", "放"}));
	add_action ("do_fangqi", "shibai");
}

mapping get_books()
{
	mapping quest = books[random(sizeof(books))];
        quest += (["last_id" : quest["id"],
                   "type" : "找" ]);
        return quest;
}

int do_kan(string arg)
{
	object me;
	mapping quest = books[random(sizeof(books))];
	me = this_player();
	if (me->query_condition("mrjob_busy")) return notify_fail ("你刚刚为还施水阁找到一本武籍，还是歇会儿吧。\n");
	if (me->query_temp("mr_job2")) return notify_fail ("一口吃不了一个胖子，你还是先找到手里那本再说吧。\n");
	if (me->is_busy()) return notify_fail("你现在正忙着呢！\n");
	if (me->is_fighting()) return notify_fail("你正在战斗中，没有闲心看这个！\n");
	if ( !living(this_player()) || arg != "paper" ) return 0;
	if ( me->query("combat_exp") < 100000 )
		return notify_fail ("你的武功尚未有练成，做这搜集武籍的事情恐怕还有危险。\n");
	if ( me->query("combat_exp") > 500000 )
		return notify_fail ("这等搜集武籍的小事，还是交给你的师弟、师妹去干吧。\n");

	quest = get_books();
	me->set_temp("mr_quest", quest);
	me->set_temp("mr_job2", 1);
	me->start_busy(1);
	write("还施水阁尚缺少「 "+me->query_temp("mr_quest/name")+"("+me->query_temp("mr_quest/id")+") 」。\n\n");
	return 1;
}

int do_put(string arg)
{
	object who, me, ob;
	mapping quest;
	who = this_player();
	me = this_object();

	if(who->is_busy() || who->is_fighting()) return notify_fail("你正忙着呢。\n");
	if(!arg) return notify_fail("你要放什么东西？\n");
	if(!objectp(ob = present(arg, who))) return notify_fail("你身上没有这样东西。\n");
	if(!who->query_temp("mr_quest")) return notify_fail("你没有任务呀，来凑什么热闹？\n"); 
	if(!mapp(quest = who->query_temp("mr_quest")) ) return notify_fail("还施水阁里不缺你这本书。\n"); 

	if(ob->query("name") != quest["name"]){
		if(ob->query("id") != quest["id"]) {
			return notify_fail("还施水阁里不缺你这本书。\n");
		}
		else{
			call_out("destroying", 0, ob, me);
			call_out("done", 3, who);       
		return 1;
		}
	}
	ob->move(me);
	who->start_busy(2);
	tell_object(me, HIY"\n你将书怀中的秘籍拿出，插入书架上的空位中。\n\n"NOR);
	destruct(ob);
	call_out("done", 2, who);
	who->add("mr_job", 1);
	return 1;
}

int do_fangqi(string arg)
{
	object me;
	mapping quest;
	me = this_player();
	if (me->is_busy()) return notify_fail("你现在正忙着呢！\n");
	if (me->is_fighting()) return notify_fail("你正在战斗中，没有闲心看这个！\n");
	if ( !living(this_player()) || arg != "paper" ) return 0;
	tell_object(me, GRN"无奈，你又将纸条放回了原处。\n"NOR);
	me->delete_temp("mr_quest");
	me->delete_temp("mr_job2");
	me->start_busy(1);
	me->apply_condtition("mrjob_busy", 1);
	return 1;
}

void done(object me)
{
	mapping quest;
	if(!me) return;
	quest = me->query_temp("mr_quest");
	tell_object(me, GRN"你获得出"+chinese_number(quest["pot_reward"])+
		"点潜能和"+chinese_number(quest["exp_reward"])+"点经验的奖励！\n"NOR);
	me->add("combat_exp", quest["exp_reward"]);
	me->add("potential", quest["pot_reward"]);
	if(me->query("potential") > me->query("max_pot")) 
        me->set("potential", me->query("max_pot"));
	me->delete_temp("mr_quest");
	me->delete_temp("mr_job2");
	return;
}
