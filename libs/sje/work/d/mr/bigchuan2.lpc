// d/mr/bigchuan2 
// by keinxin
// update 2003.2

#include <ansi.h>
#include <room.h>
inherit ROOM;
void create()
{
         set("short",HIY"饭厅"NOR);
         set("long",@long
这是一间饭厅, 虽然不大, 当时还是容得下许多慕容弟子进餐。
long);
         set("objects", ([
             __DIR__"obj/lingbai-xiaren" : 1,
             __DIR__"obj/dongsun-tang" : 1,
             __DIR__"obj/yingtao-huotui" : 1,
]));
         set("valid_startroom", 1);
         set("exits",([
             "east" : __DIR__"bigchuan3",
]));
         setup();
	 
}