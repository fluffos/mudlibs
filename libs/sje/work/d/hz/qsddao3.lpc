// /u/beyond/hz/qsddao3.c 青石大道
#include <ansi.h>
#include <room.h>
inherit ROOM;
void create() {
  set("short", GRN "青石大道" NOR);
  set("long", @LONG
过了小溪与灵隐寺相对的就是飞来峰。飞来峰上有五代、唐、宋各代的
石像几百尊，是佛家参拜的灵圣的宝地，东方就是宝淑塔。
LONG
  );
  set("objects", ([
    __DIR__ "npc/you": 2,
  ]));
  set("exits", ([
    "west": __DIR__ "lingyinsi",
    "eastup": __DIR__ "baoshuta",
  ]));
  setup();
}
