// /u/beyond/hz/lengqiao.c 西冷桥
#include <ansi.h>
#include <room.h>
inherit ROOM;
void create() {
  set("short", HIW "西泠桥" NOR);
  set("long", @LONG
这里就是西泠桥了，从这里看西湖，碧波万倾，山光水色，分外秀丽。
往东面走去就是孤山了。
LONG
  );
  set("exits", ([
    "east": __DIR__ "gushan",
  ]));
  setup();
}
