
#include <room.h>
#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", BLU"山洞"NOR);
	set("long",  @LONG
这里已经进入山体的内层，岩石大多为钟乳石，不停地有小水滴
从倒挂的石笋上面落下，发出噼啪的声音，引起了远处的一片回声。
LONG NOR);
	set("coor/x",4);
	set("coor/y",3);
	set("coor/z",1);
	set("exits", ([		
           		  
 		   "north" : __DIR__"dong"+(41+random(16)),
		   "east" : __DIR__"dong"+(41+random(16)),
		   "west" : __DIR__"dong"+(41+random(16)),
		   "south" : __DIR__"dong"+(41+random(16)),



		

	]));

 set("objects", ([
              
         ]));
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
