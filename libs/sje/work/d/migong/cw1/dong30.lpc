#include <room.h>
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", CYN "山洞" NOR);
  set("long", @LONG
这里是一个阴风阵阵的洞穴，石壁是灰质的，看起来很是坚硬，
不易遭到破坏，从吹出来的风的寒冷程度判断，这个洞应该很深。
LONG NOR);

  set("exits", ([
    "west": __DIR__ "dong31",
    "east": __DIR__ "dong29",


  ]));

  set("objects", ([

  ]));
  set("no_clean_up", 0);
  setup();
}
