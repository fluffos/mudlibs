// shishi2.c
// by shilling 97.2

#include <room.h>
inherit ROOM;

void create() {
  set("short", "石室");
  set("long", @LONG
这是一间石室，外面有光线隐隐约约的透进来。有一个须发全白
的老人做在一块巨石之上，他双目微闭，气若游丝。
LONG);
  set("exits", ([
    "west": __DIR__ "shiji2",
  ]));
  set("objects", ([
    __DIR__ "npc/wuyazi": 1,
  ]));

  set("no_clean_up", 0);
  setup();
}
