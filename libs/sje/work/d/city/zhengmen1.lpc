// Room: /city2/zhengmen.c

#include <room.h>

inherit ROOM;

void create() {
  set("short", "皇宫正门");
  set("long", @LONG
	这便是皇宫正门。巍峨雄伟的城墙上，'紫禁城'三个大字十分显眼，
气势辉煌。一张发黄的告示贴在墙上，许多官兵立在两旁。
LONG
  );
  set("outdoors", "扬州");


  set("exits", ([
    "south": __DIR__ "kedian",
  ]));
  set("objects", ([
    __DIR__ "npc/wujiang": 8,

  ]));

  setup();

}
