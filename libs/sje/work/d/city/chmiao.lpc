// Room: /d/city/chmiao.c

inherit ROOM;

void create() {
  set("short", "城隍庙");
  set("long", @LONG
这里是扬州城里的城隍庙。平日本来香客稀少，但最近突然拥挤起来。
LONG);
  set("exits", ([
    "east": __DIR__ "beidajie1",
    "northwest": "/d/wizard/guest_room",
    //        "enter" : __DIR__"xiangfang.lpc",
  ]));
  set("no_fight", 1);
  set("no_sleep_room", 1);
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/ymonk": 1,
    __DIR__ "obj/box": 1,
    //      __DIR__"npc/bao" :1,
  ]));
  setup();
  "/clone/board/wizto_b"->foo();
}

void init() {
  add_action("action", ({
    "dazuo",
    "du",
    "exercise",
    "lian",
    "practice",
    "respirate",
    "study",
    "tuna"
  }));
}

int action() {
  write("你无法静下心来修炼。\n");
  return 1;
}
