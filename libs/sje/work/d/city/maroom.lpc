// Room: /u/wzfeng/maroom.c
//wzfeng 98.10.21
#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "马房");
	set("long", @LONG
这是一个扬州城内的一个马房，有各种良马出售。马房的右边有一个方形
的饮水池，能同时让八匹骏马一起使用。马房的老板正热情的招待每一位来看
马，买马的客人。
LONG
	);
	set("outdoors", "yangzhou");
	set("objects", ([ /* sizeof() == 4 */
	
	__DIR__"npc/mafu" : 1,
	__DIR__"npc/qingma" : 1,
	__DIR__"npc/xiaohongma" : 1,
	__DIR__"npc/huangbiaoma" : 1,
]));
	set("exits", ([ /* sizeof() == 1 */
          "west" :__DIR__"guangchangdong",
]));
	set("no_clean_up", 0);
	setup();
}
int init()
{
}
