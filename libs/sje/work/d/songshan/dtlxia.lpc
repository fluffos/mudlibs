// Room: /u/swl/songshan/dtlxia.c

inherit ROOM;

void create()
{
	set("short", "大铁梁峡");
	set("long", @LONG
这里的道路十分限险，向道路之右望去，铁梁峡之右尽是怪石，道路
左侧却是万仞深壑，渺不见底。不禁让人胆颤心惊。
LONG);
	set("exits", ([
            "southdown" : __DIR__"qgping",
            "northup" : __DIR__"xtlxia",
        ]));
        set("jingli_cost/northup", 20);
	set("outdoors", "嵩山");
	setup();
	replace_program(ROOM);

}
