// atoi.c

int atoi(string str)
{
	int v;

	if( !stringp(str) || !sscanf(str, "%d", v) ) return 0;
	return v;
}

string strip(string str)
{
	string tmp;

	if (!stringp(str)) return str;
	while (sscanf(str, "%s[%*sm%s", str, tmp)) str += tmp;
	return str;
}
