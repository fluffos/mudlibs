#include <ansi.h>
inherit ITEM;
void create() {
  set_name(CYN "鹿鼎记" NOR, ({ "ldji" }));
  set("no_put", 1);
  set("value", 0);
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {

    set("unit", "本");
    set("treasure", 1);
    set("no_beg", 1);
    set("no_steal", 1);
    set("material", "paper");
    set("long", "神书之一。\n");
  }
  setup();
}


void init() {
  add_action("do_zhuang", "he");
}
int do_zhuang(string arg) {
  object me, newbook, ob;
  me = this_player();


  if (arg != "jinbooka")
    return notify_fail("你要合并什么？\n");

  newbook = new(__DIR__ "jbook1");

  if (!(ob = present("fhwz", me)) ||
    !(ob = present("xsfh", me)) ||
    !(ob = present("lcjue", me)) ||
    !(ob = present("tlbb", me)) ||
    !(ob = present("sdyxz", me)) ||
    !(ob = present("bmxxfeng", me))
  )
    tell_object(me, "你准备合并，但是突然发现书好象没齐。\n");
  else {
    if (ob->query("ownmake"))
      return notify_fail("这里面有假货吧。\n");
    if (userp(ob))
      return notify_fail("这里面有假货吧。\n");
    if (ob->is_character() || ob->is_corpse())
      return notify_fail("这里面有假货吧。\n");
    if (!ob->query("dynamic_quest"))
      return notify_fail("这里面有假货吧。\n");
    message_vision(HIW "$N把从怀中翻出的飞雪连天射白鹿七本书，并成了一本。\n" NOR, me);
    message(
      "channel:chat",
      HIR "【传闻】" HIG + me->query("name") + "得到" + HIR "金庸全集上册" + HIG "啦！\n" NOR,
      users()
    );
    newbook->move(me);
    destruct(present("fhwz", me));
    destruct(present("xsfh", me));
    destruct(present("lcjue", me));
    destruct(present("tlbb", me));
    destruct(present("sdyxz", me));
    destruct(present("bmxxfeng", me));

    destruct(this_object());
  }
  return 1;
}
