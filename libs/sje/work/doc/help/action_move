动作词 - 移动类

    这类的动作词通常用来将触动者移动到别的地方, 所以他有下列的参数, 
我们举例说明 :

    假如我们想要制造一个玩家跳过一个巨大的裂缝的效果, 我们先制造两
个相连的房间, 假设他们是 A-B, A 的东方出口衔接 B 的西方出口, 现在
我们站在 A, 利用 add_action 在 A 加入一个动词 jump, 作法是 :

      add_action move jump east

    这时, A 向 B 的出口会被打断, 造成一个由 B 向 A 的单向出口(这是
为了让你等一下能回来), 接下来, 你就可以用 jump east 来到达 B, 然后
在从 B go west 走回 A。

    动作词 jump 已经完成了, 接下来就要修饰它, 使它看起来更具有效果
, 我们可以利用 set_action 来修改下列参数 :

        check_ob : 是用来检查动作者身上有没有某件特定的物品的，如果有，
                   才让他可以使用那个动作词。
                   set_action jump check_ob <物品>

        check_time : 是用来检查动作者是不是在特定的时间做这件事情，如果是，
       才让他可以使用那个动作词。
                   set_action jump check_time <时间>

                   时间设定的值可以是 0 - 23，表示一天的 24 小时，在设定的
                   时间后的两个小时内，这个动作词才有效，也就是说，如果你把
                   时间定成 6，那这个动作词就只在一天的 6-8 点有效。如果你
                   希望整个白天都有效，你可以把时间设成 24，如果你希望晚上
                   有效，可以把时间设成 -1 (PS. 一天之中，5-19 点算白天)
                   如果你时间定超过 24，就表示不检查时间了。

        toward   : 这个参数就是动词后面必须加的词, 本来是 east, 但
                   是这样好像没有跳过裂缝的感觉, 你可以用
                        set_action jump toward gap
                   来把动作词改成 jump gap

        pre_msg  : 这个字串是当玩家跳过裂缝之时在 A 的人看到的讯息
                   你可以用 
                set_action jump pre_msg $N退后了几步, 用力往裂缝的那边跳去。
                   其中, $N 就是动作者的名字

        msg      :  这个字串是当玩家跳过裂缝之时玩家自己看见的讯息
                set_action jump msg 你退后了几步, 用力往裂缝的那边跳去。

       post_msg : 这个字串是当玩家跳过裂缝之时在 B 的人看到的讯息
                   用法跟上面的一样

    参数设定好了以后, 你可以从 A 跳到 B 了, 但是却可以从 B 走回 A,
有时候这是合理的, 但是如果你的打算不是这样, 你可以在 B 也设定动作词, 
再重覆上面的动作, 让你能从 B 跳回 A。


相关讯息 : action, action_use, add_action, set_action
