string chinese_number(int i) {
  return CHINESE_D->chinese_number(i);
}

string to_chinese(string str) {
  return CHINESE_D->chinese(str);
}

int is_chinese(string str) {
  // AGENTS.md §15h: this driver's strings are UTF-8 codepoints, not GBK
  // bytes -- str[0] is now a full Unicode codepoint, so the old GBK
  // lead-byte range (160-255) never matches real Chinese text. Use a
  // CJK Unified Ideographs codepoint range check instead, and the
  // length bound only needs one real character now (was >=2 bytes).
  if (strlen(str) >= 1 && str[0] >= 0x4e00 && str[0] <= 0x9fff) return 1;
  return 0;
}
