// Room: /u/xinyi/jifang.c
inherit ROOM;

void create()
{
  set ("short", "工作室主控机房");
  set ("long", @LONG
这里摆了一台超大型的“天涯二号”计算机(computer)，她的处理能力相当
之强。这个泥巴的一切事务全都是由她来处理的。
LONG);

  set("item_desc", ([ /* sizeof() == 1 */
  "computer" : "她的身体虽然很庞大，可以她的智慧却无人能及。
",
]));
  set("exits", ([ /* sizeof() == 1 */
  "down" : __DIR__"workroom",
]));
  setup();
  replace_program(ROOM);
}
