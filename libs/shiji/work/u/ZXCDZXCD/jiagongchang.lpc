#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "兽肉倒卖场");
  set("long", @LONG
这是一间堆满各种肉类的当铺,兽族的勇士们都把自己捕猎回来
的猎物,都拿到这里来卖,也是兽族的唯一经济来源.怪不得这里的
生意这么好!在墙上还有一块用虎皮精制而成的招牌(zhaopai),
招牌下坐着一个彪型大汉,他就是这里的老板,正大汗淋漓地加工着.
LONG
  ) set("no_fight", 1) set("no_steal", 1) set("no_beg", 1)  //      set("no_clean_up", 0)
  set("item_desc", (
    "zhaopai" : "公平交易\n
buy         买
sell        卖 
",
  ])
)
set("objects", (
  __DIR__ "npc/boss1" : 1,
])
)
set("exits", (
  "east" : __DIR__ "tianxinchi",
])
);

setup();
replace_program(ROOM);
}
