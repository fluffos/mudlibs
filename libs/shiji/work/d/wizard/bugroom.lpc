// Room: /d/wizard/wizard_room.c

inherit ROOM;

void create()
{
object me;
     set("short", "BUG报告房");
	set("long", @LONG
这里就是「世纪MUD」巫师的休息所在，刚粉刷过的四周墙壁上光
秃秃的什么都没有，屋子的正中央放着一张楠木桌，上面摆着一本
巫师留言簿(board)。
LONG );

	set("exits", ([ /* sizeof() == 1 */
                   "north" : "/d/wizard/wizard_room",
"south":"/d/wizard/newsroom",
	]));
	set("no_clean_up", 0);
 set("valid_startroom", 1);
	setup();

     call_other("/clone/board/bug_b", "???");
	replace_program(ROOM);
}
void init()
{
object me;
me=this_player();
if(!wizardp(me))
{
me->move("/d/tianlang/street");
}
}
