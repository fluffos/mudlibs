#include <room.h>

inherit ROOM;

void create() {
  set("short", "教室");
  set("long", @LONG
这是一间宽敞的教室，当前一个长宽红一米的高台，下面大约
放置了七八十条奇子，看到圣岛的弟子大都在这里接受武功教育，
教室的后端，有一个面积还比较大的空地，看来是临时指导场所。
LONG);
  set("exits", ([
    "west": __DIR__ "lianwu",
  ]));
  set("objects", ([
  ]));
  set("outdoors", "shendian");
  setup();
}
