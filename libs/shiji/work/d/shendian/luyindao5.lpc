#include <room.h>

inherit ROOM;

void create()
{
        set("short", "绿荫道");
        set("long", @LONG
走过一条宽宽的绿荫道，你现在的位置是圣殿的禁区,如此在向
前走会被守护武士给挡住的.
LONG );
        set("exits", ([
                "south" : __DIR__"luyindao3",
         "west" : __DIR__"jinqu",
        ]));
        set("objects", ([
        ]));
        set("outdoors", "shendian");
        setup();
}

