#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "卓卡内");
  set("long", @LONG
这里卓卡内舱，你绕得兴趣的东看看只摸摸，突然身后的门
吱的一声关闭了，紧接着卓卡一陈轻微的震动，好似离开了地面，
你不由得吓了一跳。原来这个卓卡只圣岛与XXXX城直接来往的便
捷工具，只要有人一上来，卓卡的电脑系统就会自动启动，飞向
XXXXXXX城。
LONG);
  set("outdoors", "shendian");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
