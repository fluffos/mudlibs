// Room: xiuxishi.c

inherit ROOM;

void init();

void create() {
  set("short", "寝宫");
  set("long", @LONG
这是间龙将的嫔妃的居所，这里一尘不染，干净异常。几个绝色美人
正在讨论着什么。
LONG);
  set("exits", ([
    "south": __DIR__ "dating",
  ]));

  set("sleep_room", 1);

  setup();
  replace_program(ROOM);
}
