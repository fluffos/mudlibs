#include <ansi.h>

inherit ROOM;

void create()
{
    set("short", "花园");
    set("long", @LONG
这是一个花园，各种奇花异草争奇斗艳，阵阵花香让你的 精神为
之一振。从这里往北是一个小楼，西面是走廊。
LONG );

    set("exits", ([
        "south" : __DIR__"zoulang4",
        "north" : __DIR__"moonroom1",

    ]));

    setup();
    replace_program(ROOM); 
}
