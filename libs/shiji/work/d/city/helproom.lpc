// Room: /city/helproom.c.c
// zurn 1995/12/23 

inherit ROOM;
void create()
{
        set("short", "急救室");
        set("long", @LONG 
这里是医疗中心的急救室，里面的大夫和护士神情都十分紧张，
正认真的给一个重伤员动手术，里面静静的只有仪器中传出的
心跳声。
LONG);
        set("objects", ([
         ]));
        set("exits", ([
                "down" : __DIR__"hospital",
        ]));
      setup();
}
