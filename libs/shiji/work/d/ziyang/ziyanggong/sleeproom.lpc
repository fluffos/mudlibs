

inherit ROOM;

void create()
{
	set("short", "休息室");
	set("long", @LONG                                    
这里是供弟子们修炼后休息的地方，里面的床铺已经铺好了，打扫的一
尘不染，你伸了个懒腰，躺在了一张床上。
LONG
	);
        set("sleep_room",1);
	set("exits", ([
	        "southeast" : __DIR__"dadian",
	]));

	setup();
	replace_program(ROOM);
}

