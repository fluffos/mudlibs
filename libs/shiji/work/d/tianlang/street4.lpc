// Room: /d/tianlang/street.c

inherit ROOM;
void create()
{
           set("short", "天狼大街");
        set("long", @LONG 
这里就是天狼星的大街,大街上人来人往真的是很热闹，
这里可能有些你不知到他是谁，可是他会来杀你。
这里还没开发好呢
LONG
);
    set("outdoors","tianlang");
        set("exits", ([
"south" : __DIR__"wangba",
                 "west" : __DIR__"street",  
       "east" : __DIR__"dcgate",
         "north" : __DIR__"klok",
     ]));
      setup();
}


int valid_leave(object me, string dir)
{
          if( (dir=="east") && me->query("age") < 25 && !wizardp(me) )
                return notify_fail("小孩子玩什么玩啊。\n");
        return ::valid_leave(me, dir);
}



