inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "办公室");
  set("long", @LONG
这里是特种部队的办公室，里面有关于这支部队成立
历来所有的历史，当然这里的人也是特种部队的指挥官
这里的布置很简单，只有一个人的桌椅。从这里往
东是特种部队的训练室。
LONG);
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "xunlianshi",
    "up": __DIR__ "bangongshi1",
  ]));
  set("objects", ([ "/kungfu/class/budui/su": 1, ]));
  setup();
}
