inherit ROOM;
void create() {
  set("short", "办公室三楼");
  set("long", @LONG
这里就是特种部队办公室三楼，这里的环境十分幽静，里面有
特种部队的一把手，也有许多特种部队的权威资料，不过这里
的布置实在是简单得不能在简单了，只有一张办公用的桌子和一
张椅子。
LONG);
  set("exits", ([
    "down": __DIR__ "bangongshi1",
  ]));
  set("objects", ([
    "/kungfu/class/budui/master": 1,
  ]));
  setup();
}
