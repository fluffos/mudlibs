// chuchangshi.c 武器库
inherit ROOM;
#include <ansi.h>

void create()
{
      set("short","武器库");
      set("long",@LONG
这里是特种部队的地下武器库，里面放满了各种高科技武器。
各种高科技武器。
LONG);
        set("exits",([ /* sizeof() == 1 */
          "up"     : __DIR__"didao3",
              ]));
        set("objects", ([
            "/clone/weapon/gun": 2,
       "/clone/weapon/gangdao": 2,
             ]) );    
      setup();
}

