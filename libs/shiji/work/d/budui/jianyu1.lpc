inherit ROOM;

void create() {
  set("short", "特种警署监狱");
  set("long", @LONG
这里四周冷冰冰的，没有一个人，什么声音都没有，只有那[吧嗒，吧嗒]的滴水声。
你绝望的蹲在监狱的角落里，无助的看着眼前的一切，心想：“要是谁能来救我，该
多好呀！
LONG
  );


  set("exits", ([
    "southeast": __DIR__ "jianyu",

  ]));

  set("objects", ([ __DIR__ "npc/fanren": 1, ]));
  setup();
  replace_program(ROOM);
}
