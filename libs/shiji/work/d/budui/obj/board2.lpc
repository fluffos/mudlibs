


#include <ansi.h>

inherit BULLETIN_BOARD;

void create()
{
	set_name(HIY"特种部队酒吧留言板"NOR, ({ "board" }) );
	
	set("board_id", "gaiboard");
	set("long", "这是特种部队酒吧的留言板。\n" );
	setup();
	set("capacity", 100);
	replace_program(BULLETIN_BOARD);
}


