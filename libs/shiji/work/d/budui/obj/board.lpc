inherit BULLETIN_BOARD;
#include <ansi.h>

void create()
{
        set_name(HIY"特种部队酒吧留言板"NOR, ({ "board" }) );
        set("location", "/d/budui/shougou");
        set("board_id", "dali_b");
        set("long", "这是特种部队的留言板。\n" );
        setup();
        set("capacity", 100);
        replace_program(BULLETIN_BOARD);
}


