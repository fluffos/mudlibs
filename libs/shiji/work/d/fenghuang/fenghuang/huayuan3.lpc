#include <ansi.h>
inherit ROOM;
void create() {
  set("short", "花园");
  set("long", @LONG
这里种着从血神星移植过来的血噬藤，血噬藤十分妖异，开的花极其艳美
但要是被她的藤茎触到，就必死无疑，不过其藤汁确是极补之物，听说宗
主正用它来练一种极为厉害的武功，所以宗主经常从囚牢里提取犯人来喂
血噬藤，所以虽然只有一株，但早已爬满整个院子了。
LONG);
  set("objects", ([
    //           __DIR__"obj/hua1": 2,
    //           __DIR__"obj/hua2": 2,
  ]));
  set("outdoors", "fenghuang");
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "mishi",
    "west": __DIR__ "qianyuan",
  ]));
  setup();
  replace_program(ROOM);
}
