#include <room.h>

inherit ROOM;

void create()
{
	set("short", "中央街");
	set("long", @LONG
这里靠近中央广场的,所以也十分繁荣,街面的西边是一家新开不久
的便利自助厅,东面便是邮政局了.
LONG );
	set("exits", ([
		"north" : __DIR__"zhouyanjie3",
		"south"  : __DIR__"guangchang",
		"west"  : __DIR__"zizu",
		"east"  : __DIR__"youzhen",
	]));
        set("objects", ([
//                "/d/shendian/npc/zhoubingung" : 1,
        ]));
	set("outdoors", "shendao");
	setup();
}
