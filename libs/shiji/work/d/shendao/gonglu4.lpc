#include <room.h>

inherit ROOM;

void create() {
  set("short", "公路");
  set("long", @LONG
这里已经接近圣岛中央广场的了,隐隐似乎听到广场那边使来
的吵闹声.街的两边行上突然多了起来,几乎个个脸上都扬溢着
微笑.
LONG);
  set("exits", ([
    "northeast": __DIR__ "zhouyanjie",
    "west": __DIR__ "gonglu3",
  ]));
  set("objects", ([
    //                "/d/shendian/npc/zhoubingung" : 1,
  ]));
  set("outdoors", "shendao");
  setup();
}
