#include <room.h>

inherit ROOM;

void create()
{
	set("short", "交叉路");
	set("long", @LONG
这里是圣岛的第二交叉路口,路口当中一位年轻的交通警正在指挥
的交通,所有的车辆在他有条不紊的指挥下,井然有序的移动着.这里已
经离中央广场有一段距离了,但似乎还能听到那边传来的喧闹声.
LONG );
	set("exits", ([
		"south"  : __DIR__"zhouyanjie3",
		"east"  : __DIR__"cheneast",
	]));
        set("objects", ([
//                "/d/shendian/npc/zhoubingung" : 1,
        ]));
	set("outdoors", "shendao");
	setup();
}
