#include <room.h>

inherit ROOM;

void create() {
  set("short", "投诉科");
  set("long", @LONG
这里是圣岛的投诉科,一位胖胖的警官坐在办公桌后面,依依呀
呀的哼着不知名的歌曲,你有什么事,就写在投诉薄上吧.
LONG);
  set("exits", ([
    "north": __DIR__ "cheneast2",

  ]));
  set("objects", ([
    //                "/d/shendian/npc/zhoubingung" : 1,
  ]));
  set("outdoors", "shendao");
  setup();
  "/clone/board/toushu_b"->foo();
}
