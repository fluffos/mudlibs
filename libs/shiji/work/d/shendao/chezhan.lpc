#include <room.h>

inherit ROOM;

void create()
{
	set("short", "汽车站");
	set("long", @LONG
这里是圣岛唯一一家汽车站,虽然现在的车站都不用汽油,而
改用始前能量,但是那种吱吱的燥音听起来,还是很不服务.
车站内宽荡荡的没有几个.
LONG );
	set("exits", ([
		"south"  : __DIR__"cheneast2",
		"northwest"  : __DIR__"chefang",
		"southeast"  : __DIR__"chefang1",
	]));
        set("objects", ([
//                "/d/shendian/npc/zhoubingung" : 1,
        ]));
	set("outdoors", "shendao");
	setup();
}
