#include <room.h>

inherit ROOM;

void create()
{
	set("short", "中央街");
	set("long", @LONG
这里已经十分接近中央广场的了,隐隐似乎听到广场那边使来
的吵闹声.街的两边行上突然多了起来,几乎个个脸上都扬溢着
微笑.
LONG );
	set("exits", ([
		"north" : __DIR__"zhouyanjie1",
		"southwest"  : __DIR__"gonglu4",
	]));
        set("objects", ([
//                "/d/shendian/npc/zhoubingung" : 1,
        ]));
	set("outdoors", "shendao");
	setup();
}
