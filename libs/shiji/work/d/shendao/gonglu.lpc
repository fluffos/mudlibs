#include <room.h>

inherit ROOM;

void create() {
  set("short", "公路");
  set("long", @LONG
这是已经不是高速行驶的道路，但相对来说这里的车流量也十分的
大，再向前走，就可以到一个交叉路。
LONG);
  set("exits", ([
    "north": __DIR__ "lukou",
    "south": __DIR__ "gaosulu6",
  ]));
  set("objects", ([
    //                "/d/shendian/npc/zhoubingung" : 1,
  ]));
  set("outdoors", "shendao");
  setup();
}
