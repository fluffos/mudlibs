
inherit W_BOARD;

void create()
{
        set_name("玩家交流簿", ({ "board" }) );
        set("location", "/d/ziyang/ziyangcheng/liefeng");
        set("board_id", "ziyangc_b");
        set("long", "这是一个专供玩家交流经验使用的留言板。\n" );
        setup();
        set("capacity", 30);
        replace_program(W_BOARD);
}

