

inherit ROOM;

void create()
{
	set("short", "休息室");
	set("long", @LONG                                    
这里是供游览累了的人休息的地方，里面的床铺已经铺好了，打扫的一
尘不染，你伸了个懒腰，躺在了一张床上。
LONG
	);
        set("sleep_room",1);
	set("exits", ([
	        "west" : __DIR__"jiazhai",
	]));
        set("objects",([
                __DIR__"npc/ndizi":1,
]));

	setup();
	replace_program(ROOM);
}

