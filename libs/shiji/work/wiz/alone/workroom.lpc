
inherit ROOM;


void create()
{
        set("short", "龙之工作室");
        set("long", @LONG
这里是巫师龙霁阳的工作室，角落里有一张床(bed),边上的桌子上面放
有一台个人电脑(computer),边上是一个大冰箱(icebox)，你不想拿点
什么东西吃吗?
LONG
        );

        set("exits", ([
          "up" : "/d/wizard/wizard_room",
          "down" : "/d/ziyang/ziyangcheng/liefeng",
          "east" : "/wiz/ftwc/workroom",
        ]) );
        set("item_desc", ([
        ]) );
        set("objects", ([
                        ]) );
        setup();
}

