// lengxue-dao.c 冷血刀法


inherit SKILL;

mapping *action = ({
([      "action" : HIM"$N高举手中$w,劈向$n，只见一道土柱朝$n直奔而去 "NOR,
        "force" : 100,
        "dodge" : -5,
        "parry" : 5,
        "lvl" : 0,
        "damage" : 50,
        "damage_type" : "割伤",
]),
([      "action" : HIG"$N就势一转，越转越快，只见满天刀气横飞，$n在刀气中竟然站立不住  "NOR,
         "force" : 140,
        "dodge" : 5,
        "parry" : 5,
        "lvl" : 8,
        "damage" : 65,
        "damage_type" : "割伤",
]),
([      "action" : HIW"$N默提气息，使出一招"HIR"「绝情绝义」"HIW"，手中$w化做一条长蛇飞向$n的$l  "NOR,
        "force" : 180,
        "dodge" : 10,
        "parry" : 5,
        "lvl" : 16,
        "damage" : 70,
        "damage_type" : "割伤",
]),
([      "action" : HIR"$N将手中$w急转起来，越转越快，化做一个光圈攻向$n  "NOR,
        "force" : 220,
        "dodge" : 5,
        "parry" : 5,
        "lvl" : 24,
        "damage" : 100,
        "damage_type" : "割伤",
]),
([      "action" : HIY"$N气息狂泻而出，手中$w直指天空，只见周围一片血光，沙石横飞，$n不只所措  "NOR,
        "force" : 250,
        "dodge" : -5,
        "parry" : 5,
        "lvl" : 48,
        "damage" : 150,
        "damage_type" : "割伤",
]),

});


int valid_enable(string usage) { return usage == "blade" || usage == "parry"; }

int valid_learn(object me)
{
        if ((int)me->query("max_neili") < 500)
                return notify_fail("你的内力不够。\n");
        if ((int)me->query_skill("duanqing-xinfa", 1) < 20)
                return notify_fail("你的断情心法火候太浅。\n");
        return 1;
                
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
        level   = (int) me->query_skill("xue-dao",1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
}

int practice_skill(object me)
{
        object weapon;

        if (!objectp(weapon = me->query_temp("weapon"))
        || (string)weapon->query("skill_type") != "blade")
                return notify_fail("你使用的武器不对。\n");
        if ((int)me->query("qi") < 100)
                return notify_fail("你的体力不够，练不了冷血刀法。\n");
        me->receive_damage("qi", 50);
        return 1;
}
