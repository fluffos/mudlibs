// /clone/board/wiz_b.c

inherit W_BOARD;

void create() {
  set_name("BUG留言簿", ({ "board" }));
  set("location", "/d/wizard/bugroom");
  set("board_id", "bug_b");
  set("long", "这是一个专供巫师使用的留言板。\n");
  setup();
  set("capacity", 30);
  replace_program(W_BOARD);
}
