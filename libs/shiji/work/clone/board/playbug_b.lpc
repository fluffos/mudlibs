// /clone/board/wiz_b.c

inherit W_BOARD;

void create() {
  set_name("玩家BUG留言簿", ({ "board" }));
  set("location", "/d/city/startroom");
  set("board_id", "playbug_b");
  set("long", "这是一个专供玩家使用的留言板。\n");
  setup();
  set("capacity", 30);
  replace_program(W_BOARD);
}
