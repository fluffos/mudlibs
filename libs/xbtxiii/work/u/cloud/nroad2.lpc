#pragma save_binary
// Room: /u/cloud/nroad2.c

inherit ROOM;

void create()
{
        set("coor",({2100,3850,0}));
	set("short", "南市场");
        set("long",
"这里是绮云镇的南市场，西面是杂货铺，向东可看到怡红院的二\n"
"楼。\n"
);
        set("exits", ([ /* sizeof() == 4 */
  "west" : "/u/cloud/zaihuoy",
  "north" : "/u/cloud/nroad1",
  "south" : "/u/cloud/cross",
]));
        set("outdoors", "cloud");

        setup();
        replace_program(ROOM);
}

