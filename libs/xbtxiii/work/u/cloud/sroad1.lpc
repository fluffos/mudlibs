#pragma save_binary
// Room: /u/cloud/sroad1.c
#include <roadbusy.h>

inherit ROOM;

void create()
{
        set("coor",({2100,3750,0}));
	set("short", "绮云镇街道");
        set("long",
"这里是绮云镇南，通往过江的渡口。\n"
);
        set("exits", ([ /* sizeof() == 2 */
  "north" : "/u/cloud/cross",
  "south" : "/u/cloud/dukou",
]));
        set("outdoors", "cloud");

        setup();
//        replace_program(ROOM);
}

