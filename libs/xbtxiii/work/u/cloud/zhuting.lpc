#pragma save_binary
// Room: /u/cloud/zhuting.c

inherit ROOM;

void create() {
  set("short", "竹亭");
  set("long",
    "这是一个竹亭,显然已经建了很久了,显得有些破旧。为了去上香\n"
    "的信徒和游客的方便，就有善人捐建了这个亭子，供路人歇歇脚。\n"
    "亭子里正有人在休息呢，还有几个小贩子。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "shijie3",
  ]));
  set("objects", ([
    "/d/shaolin/npc/xiao-fan": 1,
    "/u/cloud/npc/tea_vendor": 1,
  ]));
  set("outdoors", "choyin");

  setup();
  replace_program(ROOM);
}
