#pragma save_binary
// Room: /u/cloud/shijie5.c
#include <roadbusy.h>

inherit ROOM;

void create() {
  set("coor", ({ 4100, -900, 0 }));
  set("short", "石阶");
  set("long",
    "终于走到了青石板路的尽头了,前面就是少林寺了,你不由得加快\n"
    "了脚步。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": "/d/shaolin/shijie1",
    "northeast": "/d/shaolin/shijie4",
    "south": "/u/cloud/shijie4",
  ]));
  set("outdoors", "cloud");
  set("objects", ([ /* sizeof()==2 */
    "/d/shaolin/npc/xu-tong": 1,
    "/d/shaolin/npc/xu-ming": 1,
    "/d/icefire/npc/yuanzhen": 1,
  ]));
  setup();
  //        replace_program(ROOM);
}
