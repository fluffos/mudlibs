#pragma save_binary
// Room: /u/sunhill/road4.c
#include <roadbusy.h>

inherit ROOM;

void create() {
  set("coor", ({ 3450, 3380, 15 }));
  set("short", "盘山小径");
  set("long", @LONG
这是条崎岖的黄土小径。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "westdown": __DIR__ "road3",
    "southup": "/d/sanyen/sroad2",
    //"southup" : __DIR__"hillsmoke/entrance",
  ]));
  set("outdoors", "cloud");

  setup();
  //        replace_program(ROOM);
}
