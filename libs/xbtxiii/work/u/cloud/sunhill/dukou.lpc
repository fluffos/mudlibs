#pragma save_binary
// Room: /u/cloud/dukou.c

inherit ROOM;

void create()
{
        set("coor",({3450,3450,0}));
	set("short", "江南渡口");
        set("long", @LONG
这是个虽说不大却过客甚多的渡口。往南去是通往山烟寺里的黄土
径，北面就是无风亦起浪的泓水了。向北望去，隐约可见对岸绮云镇的
轮廓，袅袅炊烟升起。
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
  /*"north" : __DIR__"sroad1",*/
  "south" : __DIR__"road1",
]));

	set("objects", ([
                "/u/cloud/npc/boater2" : 1,
	]) );

        set("outdoors", "cloud");

        setup();
}
