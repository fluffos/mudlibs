#pragma save_binary
// Room: /u/cloud/eroad4.c

inherit ROOM;

void create() {
  set("coor", ({ 2300, 3800, 0 }));
  set("short", "茶场");
  set("long",
    "这里是绮云镇的茶场，绮云盛产茶叶，因此这里有不少茶工在忙\n"
    "着采茶。\n"
  );
  set("exits", ([ /* sizeof() == 1 */
    "northwest": "/u/cloud/eroad3",
    "south": "/u/cloud/eroad6.lpc",
    "east": "/d/icefire/haigang",

  ]));

  set("objects", ([
    "/u/cloud/npc/worker": 3,
  ]));
  set("clean_up", 1);
  set("outdoors", "cloud");

  setup();
}
