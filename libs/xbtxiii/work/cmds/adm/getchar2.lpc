#include <ansi.h>

int main(object me, string arg) {
  tell_object(me, "请按任意键！\n");
  get_char("getchar", 1, me);
  return 1;
}
void getchar(string str, object me) {
  string msg;
  msg = "你输入的键的编码是:";
  msg = msg + sprintf("%d", str);
  tell_object(me, msg + "\n");
}
