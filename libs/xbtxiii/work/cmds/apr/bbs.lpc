// bbstitle
#include <ansi.h>
inherit F_CLEAN_UP;

void create()
{
	seteuid(getuid());
}
int main(string arg)
{
	object bbs;
	if(!objectp(bbs=new("/obj/bbs")))
		return 0;
	write(bbs->long());
	destruct(bbs);
	return 1;
}