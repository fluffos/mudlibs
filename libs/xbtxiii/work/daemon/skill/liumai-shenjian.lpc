#pragma save_binary
// liumai-shenjian.c 六脉神剑

inherit SKILL;

mapping *action = ({
([  "action" : "$N使出[商阳剑],食指轻弹,指端射出一缕缕罡气,迅如闪电,刺向$n",
        "force" : 50,
        "finger" : 20,
        "parry" : 10,
        "dodge" : 60,
   "damage": 100,
//    "lvl" : 1,
    "skill_name" : "商阳剑",
 "damage_type" : "淤伤"
]),
([  "action" : "$N神情肃穆,无名指使出[关冲剑],招式拙滞古朴,却又暗藏杀着,令人无法招架,剑气沉沉,点向$n",
    "force" : 70,
        "finger" : 55,
        "parry" : 10,
   "dodge" : 40,
    "damage": 125,
//    "lvl" : 20,
    "skill_name" : "关冲剑",
 "damage_type" : "淤伤"
]),
([  "action" : "$N左手半推,中指迈出,使出了一招[中冲剑],气势雄迈,颇有石破天惊、风雨大至之势.一股剑气直逼$n的膻中大穴",
    "force" : 80,
  "dodge" : 75,
        "finger" : 60,
        "parry" : 50,
  "damage": 300,
//    "lvl" : 50,
    "skill_name" : "中冲剑",
 "damage_type" : "淤伤"
]),
([  "action" : "$N拇指虚按,一路[少商剑]使出.但见其剑路雄劲,虽寥寥数笔,然纵横倚斜,人莫可测,亦莫能挡.一束束锐利无俦的剑气,向$n的$l砍去",
    "force" : 90,
        "finger" : 65,
        "parry" : 20,
        "dodge" : 50,
 "damage": 350,
//    "lvl" : 100,
    "skill_name" : "少商剑",
 "damage_type" : "淤伤"
]),
([  "action" : "$N轻抬右臂,小指微弹,使出了最为精妙的[少冲剑].如清风拂柳般的丝丝剑气,遍袭$n周身大穴.",
	"force" : 110,
        "dodge" : 100,
        "finger" : 10,
        "parry" : 25,
    "damage": 150,
//    "lvl" : 10,
    "skill_name" : "少冲剑",
 "damage_type" : "淤伤"
]),
([  "action" : "$N左手轻握胸前,小指对着$n轻弹过去,正是精微难测的[少泽剑],剑气忽来忽去.令人难以捉摸.忽然就袭到了$n的$l",
    "force" : 100,
        "finger" : 25,
        "parry" : 15,
        "dodge" : 80,
    "damage": 150,
    "skill_name" : "少泽剑",
 "damage_type" : "淤伤"
])
});

  int valid_enable(string usage) { return usage=="parry"||usage=="finger"; }

int valid_learn(object me)
{
	if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
		return notify_fail("练六脉神剑必须空手。\n");
      if ((int)me->query_skill("force", 1) < 100)
        return notify_fail("你的内功心法火候不够,学六脉神剑于你有害无益。\n");
     if ((int)me->query("max_force") < 1000)
		return notify_fail("你的内力太弱，无法练六脉神剑。\n");
	return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
/*
        int i, level;
	level   = (int) me->query_skill("liumai-shenjian", 1);
        for(i = sizeof(action); i > 0; i--)
                if(level > action[i-1]["lvl"])
                        return action[NewRandom(i, 20, level/5)];
*/
return action[random(sizeof(action))];
}

int practice_skill(object me)
{
  if ((int)me->query("kee") < 30)
		return notify_fail("你的体力太低了。\n");
  if ((int)me->query("force") < 500)
		return notify_fail("你的内力不够练六脉神剑!\n");
  me->receive_damage("kee", 50);
 me->add("force", -100);
	return 1;
}
string perform_action_file(string action)
{
        return __DIR__"liumai-shenjian/" + action;
}
