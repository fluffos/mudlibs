#pragma save_binary
// killer.c

inherit F_CLEAN_UP;

int update_condition(object me, int duration)
{

    if(duration<1)
      {
       tell_object(me,"由于时间太久，官府不再通缉你了。\n");
       }  
    me->apply_condition("killer", duration - 1);
    if( !duration ) return 0;
    return 1;
}

