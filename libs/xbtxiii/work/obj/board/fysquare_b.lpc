// query_b.c

inherit BULLETIN_BOARD;

void create()
{
    set_name("凌云匾", ({ "board" }) );
    set("location","/d/fy/fysquare");
    set("board_id", "feitian_b");
    set("long", "这是一个供人留言记事的留言板(help board)。 \n" );
	setup();
	set("capacity", 200);
	replace_program(BULLETIN_BOARD);
}
