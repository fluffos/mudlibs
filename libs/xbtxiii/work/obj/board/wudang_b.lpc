// /clone/board/kedian_b.c

inherit BULLETIN_BOARD;

void create()
{
    set_name("武当派弟子留言板", ({ "board" }) );
    set("location", "/d/wudang/sanqingdian");
	set("board_id", "wudang_b");
	set("long", "有有用的话就往这里说，乱灌水杀无赦。\n" );
	setup();
	set("capacity", 30);
	replace_program(BULLETIN_BOARD);
}

