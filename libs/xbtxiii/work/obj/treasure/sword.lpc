#pragma save_binary
// sword:__DIR__"obj/broken_sword.lpc" 破残奇剑一把

#include <weapon.h>
#include <ansi.h>

inherit SWORD;
//inherit F_UNIQUE;

void create()
{
  set_name( RED"绝情剑" NOR,({ "sword" }));
	if( clonep() )
		set_default_object(__FILE__);
	else{
		set("unit", "把");
   set("long", @LONG

这是一把发出异样光芒的宝剑，相传是铸剑祖师当年用尽毕生精力
所铸.一看就知道此剑非同凡响，绝对是一把绝等宝剑.
LONG
);
    set("value", 100000);
		set("material", "异类金属");
  set("wield_msg", "$N「唰」地一声从剑鞘里抽出一把$n握在手中。顿时周围充满了杀气!\n");
        set("unwield_msg", "$N将手中的$n插入腰间的剑鞘。\n");
        set("replica_ob", "/obj/weapon/longsword");
    }
init_sword(120);
	setup();
}

