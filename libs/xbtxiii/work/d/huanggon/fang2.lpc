#pragma save_binary

inherit ROOM;

void create() {
  set("coor", ({ -10, 3000, 0 }));
  set("short", "房舍");
  set("long",
    "这里是一间小屋，一个太监正在这里休息。\n"
  );

  set("exits", ([
    "north": __DIR__ "zoulang1",
  ]));
  set("objects", ([
    "/d/huanggon/npc/taijian": 1,
  ]));

  setup();
  replace_program(ROOM);
}
