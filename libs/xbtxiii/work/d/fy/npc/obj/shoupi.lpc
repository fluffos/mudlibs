//shoupi.c 这个东西是用来缝制高级酒袋的。在风云城的裁缝处缝制
//将此物交给他就可以了(还要手工费）
inherit ITEM;
void create()
{
	set_name("兽皮", ({"hide"}));
	set_weight(800);
	if (clonep())
		set_default_object(__FILE__);
	else {
		set("long", "一张野兽的兽皮,可以用来缝制东西。\n");
		set("unit", "张");
		set("value", 2000);
	}
}
