#pragma save_binary

inherit ROOM;

void create() {
  add_light(this_object(), 1);
  set("coor", ({ 75, 5, 0 }));
  set("short", "张家铁铺");
  set("long",
    "风云老少都喜欢在这里打造称手的铁器，从火炉中冒出的火光将\n"
    "墙壁映得通红，屋子的角落里堆满了各式铁器的完成品或未完成\n"
    "品，像是锄头、铁锤、铲子、长剑等，叮叮当当的声音敲得满屋\n"
    "子响。门口有一个大木牌（ｓｉｇｎ）．\n"
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "ecloud3",
  ]));

  set("objects", ([
    __DIR__ "npc/smith": 1,
  ]));
  set("item_desc", ([
    "sign": @TEXT

目前我们可订作（ｄｉｎｇ）：

剑 (sword) , 刀  (blade) , 枪 (spear) ,
鞭 (whip) , 杖  (staff) , 杵 (hammer) ,

每件十两黄金和1000点潜能.

例子：
ｄｉｎｇ　ｓｗｏｒｄ　

张铁匠就会帮你作一把剑。

你可以用:  set sword_name 你的剑的名称   
来设定剑的名称
可以用以下颜色
$BLK$ - 黑色            $NOR$ - 恢复正常颜色
$RED$ - 红色            $HIR$ - 亮红色
$GRN$ - 绿色            $HIG$ - 亮绿色
$YEL$ - 土黄色          $HIY$ - 黄色
$BLU$ - 深蓝色          $HIB$ - 蓝色
$MAG$ - 浅紫色          $HIM$ - 粉红色
$CYN$ - 蓝绿色          $HIC$ - 天青色
$WHT$ - 浅灰色          $HIW$ - 白色
升级武器 (updateweapon) 

TEXT
  ]));

  setup();
  replace_program(ROOM);
}
