#pragma save_binary
// Room: /d/chuenyu/sunnyroad.c

inherit ROOM;

void create() {
  set("coor", ({ -700, 4000, 0 }));
  set("short", "山脚乡田");
  set("long",
    "你的两侧是茂盛的稻田，还有一条清澈见底的小溪。许多乡民在\n"
    "田里辛苦的忙碌着。再北面是黑松山。眺望山顶，你似乎可以看\n"
    "到一个颇具规模的城堡。城堡的最顶端插着一面大旗，大旗上绣\n"
    "着两个斗大的金字：淳于。\n"
  );
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/flower_seller": 1,
  ]));
  set("outdoors", "chuenyu");
  set("exits", ([ /* sizeof() == 3 */
    "north": __DIR__ "base_b_m",
    "southeast": "/d/village/valley1",
  ]));

  setup();
  replace_program(ROOM);
}
