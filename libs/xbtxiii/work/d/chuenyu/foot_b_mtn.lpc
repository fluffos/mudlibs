#pragma save_binary
// Room: /d/chuenyu/foot_b_mtn.c

inherit ROOM;

void create()
{
        set("coor",({-700,4050,10}));
	set("short", "黑松山腰");
        set("long",
"你正在艰难的走在唯一通往山顶的小路上。路边堆着很多面目狰\n"
"狞的巨石。几棵幼松顽强的从石缝中挤了出来。你在这里可以看\n"
"到山顶城堡的大致伦廓。\n"
);
        set("exits", ([ /* sizeof() == 2 */
        "west" : __DIR__"foot_b_mtn",
          "up" : __DIR__"high_b_mtn",
          ]));
          set("outdoors", "chuenyu");
          
          setup();
          replace_program(ROOM);
}
                  
