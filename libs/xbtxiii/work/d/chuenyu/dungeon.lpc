#pragma save_binary
// Room: /d/chuenyu/dungeon.c

inherit ROOM;

void create()
{
        set("coor",({-690,4080,-35}));
	set("short", "刑房");
        set("long",
"你处在一个阴湿的地牢里。地牢的角落里堆着一些干草，干草上\n"
"沾满了暗红色的血迹。许多骷髅被生锈的铁链绑在墙上。偶尔之\n"
"间，你还可以听到老鼠“吱吱”的尖叫声。\n"
);
        set("exits", ([ /* sizeof() == 2 */
        "west" : __DIR__"tunnel3",
          "north"   : __DIR__"tunnel4",
]));

        set("objects", ([
                __DIR__"npc/chuenyu" : 1,
                __DIR__"xiaojuan2" : 1,
        ]) );
                
                
        setup();
}
 
                                  
