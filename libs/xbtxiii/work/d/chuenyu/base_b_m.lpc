#pragma save_binary
// Room: /d/chuenyu/base_b_mtn.c
inherit ROOM;
#include <roadbusy.h>

void create()
{
        set("coor",({-700,4050,0}));
	set("short", "黑松山脚");
        set("long",
"一座高而险的山峰耸立在你面前。北面是一条上山的羊肠小路，\n"
"盘旋曲折。黑湮湮的松林给你不祥的感觉。\n"
);
        set("exits", ([ /* sizeof() == 2 */
        "south" : __DIR__"sunnyroad",
          "up" : __DIR__"foot_b_mtn",
]));
        set("outdoors", "chuenyu");
        
        setup();
//        replace_program(ROOM);
}
                          
