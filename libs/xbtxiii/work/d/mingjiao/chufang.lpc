// Room: /d/mingjiao/chufang.c

inherit ROOM;

void create()
{
	set("short", "素魔厨房");
	set("long",
"这是一间什麽也没有的空房间。\n"
);
	set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"zoulang4",
]));
	set("no_clean_up", 0);
    set("objects", ([
    "/d/mingjiao/obj/baozi" : 2,
    "/d/mingjiao/obj/oil" : 2,
]));

	setup();
	replace_program(ROOM);
}
