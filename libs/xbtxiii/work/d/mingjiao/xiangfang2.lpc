// Room: /d/mingjiao/xiangfang2.c

inherit ROOM;

void create()
{
	set("short", "东厢房");
	set("long",
"这是一间什麽也没有的空房间。\n"
);
	set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"zoulang2",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
