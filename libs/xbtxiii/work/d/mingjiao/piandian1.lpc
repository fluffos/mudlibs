// Room: /d/mingjiao/piandian1.c

inherit ROOM;

void create() {
  set("short", "右偏殿");
  set("long",
    "两恻叉着巨大的松明，下面是一排排雪亮的兵刃整齐地排放着，\n"
    "你不由从心底升起一阵傲气。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "qiandian",
    "north": __DIR__ "zoulang1",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
