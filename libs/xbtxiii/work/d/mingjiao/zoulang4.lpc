// Room: /d/mingjiao/zoulang4.c

inherit ROOM;

void create()
{
	set("short", "西走廊");
	set("long",
"这是一间什麽也没有的空房间。\n"
);
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"chufang",
  "south" : __DIR__"zoulang3",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
