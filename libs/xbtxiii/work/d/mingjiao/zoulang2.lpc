// Room: /d/mingjiao/zoulang2.c

inherit ROOM;

void create()
{
	set("short", "明教东走廊");
	set("long",
"这是一间什麽也没有的空房间。\n"
);
	set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"zoulang1",
  "east" : __DIR__"xiangfang2",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
