#pragma save_binary
// Room: /d/waterfog/midao2.c

inherit ROOM;

void create() {
  set("short", "天邪密室");
  set("long",
    "你正走在天邪派的密道当中,四周是黑漆漆的一团。你蹒跚而行，\n"
    "心中有一种莫名的恐惧。嘿，你突然发现这儿有一个向下的通道！\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "southup": __DIR__ "midao1",
    "down": __DIR__ "mimen",
    //"northup" : "/d/waterfog/stair2",
    //"southdown" : "/d/waterfog/sroad2",
  ]));
  // set("outdoors", "waterfog");

  setup();
}
