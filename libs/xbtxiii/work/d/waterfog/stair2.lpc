#pragma save_binary
// Room: /d/waterfog/stair2.c

inherit ROOM;

void create() {
  set("coor", ({ -100, 4680, 14 }));
  set("short", "白石阶梯");
  set("long",
    "你现在正走在一条长长的白石阶梯上，这些建造阶梯的白石出产\n"
    "于东海滨的裂瓶山，距离此地有近千里之遥，而且虽然年代久远，\n"
    "仍然可以看得出所用的全部都是品质最好的「拟玉」石，可见这\n"
    "里的主人必定大有来头。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "southdown": "/d/waterfog/stair1",
    "eastup": "/d/waterfog/stair3",
  ]));
  set("outdoors", "waterfog");

  setup();
  replace_program(ROOM);
}
