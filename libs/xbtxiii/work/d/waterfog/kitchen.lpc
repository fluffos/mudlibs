#pragma save_binary
// Room: /d/waterfog/kitchen.c

inherit ROOM;

void create() {
  set("coor", ({ -180, 4740, 25 }));
  set("short", "厨房");
  set("long",
    "这里是水烟阁的厨房，东首一座大灶，灶边堆著许多瓦瓮，北边\n"
    "是一张方桌，桌上锅碗瓢盆一应俱全，西边则是一个橱柜，往南\n"
    "可以回到厅中。\n"
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "eaststair",
  ]));

  setup();
}
