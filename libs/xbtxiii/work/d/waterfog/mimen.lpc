#pragma save_binary
// Room: /d/waterfog/mimen.c

inherit ROOM;

void create() {
  set("short", "天邪派库房");
  set("long",
    "你来到天邪派的库房。面前是一扇厚实的铁门,你推了几下,可是铁门纹丝不动。左边有扇小\n"
    "门，门上写着一排大字" EXIT "。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": "/d/waterfog/servant_room",
    "up": "/d/waterfog/midao2",
  ]));
  // set("outdoors", "waterfog");

  setup();
  // replace_program(ROOM);
}
