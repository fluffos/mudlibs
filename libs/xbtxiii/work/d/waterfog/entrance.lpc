#pragma save_binary
// Room: /d/waterfog/sroad3.c

inherit ROOM;

void create()
{
	set("coor",({-200,4720,25}));
	set("short", "水烟阁正门");
        set("long",
"这里是水烟阁的正门，一股沁人心脾的檀香使你精神为之一振，\n"
"正门共分三进，全部都是上等的檀木雕成，最里的门上悬著一块\n"
"匾额，上面龙飞凤舞地写著几个大字，可是你全然看不出到底是\n"
"什麽字。\n"
);
        set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"guildhall",
  "east"  : "/d/waterfog/easthall",  
  "south" : "/d/waterfog/frontyard",
  "west"  : "/d/waterfog/westhall",
]));
        set("objects",([
        __DIR__"npc/guard" : 2,
        ]));
        set("outdoors", "snow");

        setup();
        replace_program(ROOM);
}
