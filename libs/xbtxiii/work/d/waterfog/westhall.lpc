#pragma save_binary
// Room: /d/waterfog/westhall.c

inherit ROOM;

void create() {
  set("coor", ({ -220, 4720, 25 }));
  set("short", "水烟阁西侧花厅");
  set("long",
    "这里是水烟阁入口西侧的花厅，东边是水烟阁的正门，南边和西\n"
    "边围著一圈白玉栏杆，栏杆外种著许多茶花，往北是水烟阁的侧\n"
    "门，从这里可以看到通往水烟阁二楼的阶梯。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "weststair",
    "east": __DIR__ "entrance",
  ]));

  setup();
  replace_program(ROOM);
}
