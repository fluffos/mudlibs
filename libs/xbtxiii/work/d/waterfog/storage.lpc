#pragma save_binary
// Room: /d/waterfog/storage.c

inherit ROOM;

void create()
{
	set("coor",({-220,4740,25}));
	set("short", "柴房");
	set("long",
"这里是一间柴房，一捆一捆劈好的柴薪都整齐地堆放在柴房北侧，\n"
"留下一条通道通往东边仆役的卧室。往南可以回到水烟阁的西侧\n"
"厅。\n"
);
	set("exits", ([ /* sizeof() == 2 */
		"south" : __DIR__"weststair",
		"east" : __DIR__"servant_room",
	]));
	set("objects", ([
	]) );

	setup();
	replace_program(ROOM);
}
