#pragma save_binary
// Room: /d/waterfog/sroad2.c

inherit ROOM;

void create() {
  set("coor", ({ -100, 4660, 10 }));
  set("short", "白石阶梯");
  set("long",
    "你现在正走在一条长长的白石阶梯上，这些建造阶梯的白石出产\n"
    "於东海滨的裂瓶山，距离此地有近千里之遥，而且虽然年代久远，\n"
    "仍然可以看得出所用的全部都是品质最好的「拟玉」石，可见这\n"
    "里的主人必定大有来头。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "northup": "/d/waterfog/stair1",
    "south": "/d/ny/bforest/road5",
  ]));
  set("outdoors", "snow");

  setup();
}
