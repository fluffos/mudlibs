#pragma save_binary
// Room: /d/waterfog/weststair.c

inherit ROOM;

void create() {
  set("coor", ({ -220, 4730, 25 }));
  set("short", "西侧厅");
  set("long",
    "这里是水烟阁西侧的厅院，一道白石梯从这里通往二楼，往北是\n"
    "仆役的卧房跟柴房，南边是出水烟阁的侧门，往西则是一个可以\n"
    "眺望山谷的阳台。\n"
  );
  set("exits", ([ /* sizeof() == 3 */
    "up": __DIR__ "west_2f",
    "north": __DIR__ "storage",
    "south": __DIR__ "westhall",
  ]));

  setup();
  replace_program(ROOM);
}
