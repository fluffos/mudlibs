#pragma save_binary
// Room: /d/waterfog/easthall.c

inherit ROOM;

void create() {
  set("coor", ({ -180, 4720, 25 }));
  set("short", "水烟阁东侧花厅");
  set("long",
    "这里是水烟阁正门东边的花厅，东边和南边围著一圈白石栏杆，\n"
    "栏杆外种著许多茶花，往西是水烟阁的正门，往北则是东侧的侧\n"
    "厅，从这里可以看见通往二楼的阶梯。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "eaststair",
    "west": __DIR__ "entrance",
  ]));

  setup();
}
