#pragma save_binary
// Room: /d/waterfog/wpath1.c

inherit ROOM;

void create()
{
	set("short", "山坡小径");
	set("long",
"你现在正走在一条山坡上的小径，往南望去只见山下一条白色石\n"
"阶蜿蜒上山，通往东边不远处的一个平台，平台的北边就是赫赫\n"
"有名的武者圣地「水烟阁」，小径的北边整齐地种著一排枫树，\n"
"往西上了山坡通往位于山巅的「虹台」。\n"
);
	set("exits", ([ /* sizeof() == 2 */
  "west" : "/d/waterfog/wpath2",
  "east" : "/d/waterfog/frontyard",
]));
        set("objects", ([
          __DIR__"npc/little_tiger":1,
]));
	set("outdoors", "waterfog");

	setup();
	replace_program(ROOM);
}
