#pragma save_binary
// Room: /d/waterfog/sroad1.c

inherit ROOM;

void create()
{
        set("short", "青石官道");
        set("long",
"这是一条宽敞坚实的青石大道，大道沿著山腰往东向山下延伸，\n"
"一道长长的白石阶梯从这里往北边上山，从这里仰望阶梯的尽头\n"
"有一座雄伟的楼阁，那就是著名的「水烟阁」。\n"
);
        set("exits", ([ /* sizeof() == 2 */
  "north" : "/d/waterfog/sroad2",
  "south" : "/d/ny/sroad4",
]));
        set("outdoors", "snow");

        setup();
        replace_program(ROOM);
}
