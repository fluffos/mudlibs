#pragma save_binary
// Room: /d/snow/eroad2.c
#include <roadbusy.h>

inherit ROOM;

void create() {
  set("coor", ({ 125, -950, 0 }));
  set("short", "荒凉驿道");
  set("long",
    "这是一条宽阔的驿道，路面上深深的车辙印表明这里曾经多么繁\n"
    "忙,但现在路的两边早已长满了荒草.路继续向东面延伸,仿佛没\n"
    "有尽头,不知道通向何处.往西则是通往扬州。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": "/d/sea/eroad3",
    "west": "/d/sea/eroad1",
  ]));
  set("outdoors", "snow");

  setup();
  //        replace_program(ROOM);
}
