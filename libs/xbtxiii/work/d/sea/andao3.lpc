#pragma save_binary
inherit ROOM;

void create()
{
        set("short", "暗道");
        set("long",
"这里仍然很黑，但前面无尽的黑暗中似乎又隐隐透出一点儿亮光,\n"
"伴随着风声,你听到哗哗的声音,象是雷声,但更象是汹涌的波涛\n"
"拍打海岸的声音,你一阵高兴,莫非前面就是出海口?想到这,你不\n"
"禁加快了脚步.\n"
);
        set("exits", ([ /* sizeof() == 2 */
        "east": __DIR__"andao4",
        "west": __DIR__"andao2", 
 ]));
           
           setup();
           replace_program(ROOM);
}

                   
