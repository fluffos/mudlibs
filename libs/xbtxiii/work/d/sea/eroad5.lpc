#pragma save_binary
// Room: /d/sea/eroad5.c
#include <roadbusy.h>

inherit ROOM;

void create()
{
        set("coor",({275,-950,0}));
	set("short", "荒凉驿道");
        set("long",
"这是一条宽阔的驿道，这儿已经远离绮云镇了,路上的铺这一层\n"
"细沙,这种沙子与中原所见的沙子有很大的不同.从东面吹来的风\n"
"带有一点点咸味,前面是哪?你不禁暗暗问自己.\n"
);
        set("exits", ([ /* sizeof() == 2 */
  "east" : "/d/sea/yuchun",
  "west" : "/d/sea/eroad4",
]));
//        set("outdoors", "snow");

        setup();
//        replace_program(ROOM);
}
