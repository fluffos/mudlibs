#pragma save_binary
// Room: /d/sea/road5.c
#include <room.h>
inherit ROOM;

void create()
{
	set("coor",({325,-925,0}));
	set("short", "石板街");
	set("long",
"这是一条小街,路面是用海边常见的花岗岩石板铺就的.街道上冷\n"
"冷清清,两边的小店早早就关起门来,只在街的尽头有一家小杂货\n"
"店微微透出昏暗清冷的灯光,西面是一家渔舍.\n"
);
	set("exits", ([ /* sizeof() == 4 */
  "south" : __DIR__"yuchun",
  "north" : __DIR__"road2",
  "west" : __DIR__"farmhouse",
]));
	set("no_clean_up", 0);
	create_door("west","木柴门","east",DOOR_CLOSED);
	setup();
	replace_program(ROOM);
}
