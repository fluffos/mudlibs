#pragma save_binary
// luopan.c


inherit ITEM;

void create() {
  set_name("罗盘", ({ "luopan" }));
  set_weight(8000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "面");
    set("value", 0);
    set("long", "这是一个檀木刻制的罗盘,在海上它可以用来判别方向.\n");
  }
  setup();
}
