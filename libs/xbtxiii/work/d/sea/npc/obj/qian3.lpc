#pragma save_binary
inherit ITEM;

void create() {
  set_name("签牌", ({ "qian1" }));
  set_weight(100);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "支");
    set("long", "这是一支中等签，上面写着出海一般。\n");
    set("value", 0);
    set("material", "copper");
  }
}
