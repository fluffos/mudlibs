// Room: /d/quanzhen/guandao2.c

inherit ROOM;

void create() {
  set("short", "官道");
  set("long",
    "这是一条官府修建的大道，西面隐约可以看见武功镇的东门。远\n"
    "处好象可以看见巍峨的城墙。往东是通往中原的路。走在这里，\n"
    "你似乎有了一种‘劝君更进一杯酒，西出阳关无故人’的感觉。\n"
  );
  set("coor", ({ /* sizeof() == 3 */
    950,
    -260,
    0
  }));
  set("outdoors", "quanzhen");
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "jnroad1",
    "east": __DIR__ "guandao1",
  ]));

  setup();
}
