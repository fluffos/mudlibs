// Room: /d/quanzhen/jnroad5.c

inherit ROOM;

void create()
{
	set("short", "莽林边缘");
	set("long",
"东北边隐约有一条小路,透出的阳光令你不由的加快了脚步.四下\n"
"如死一般的寂静，只有风吹过树梢的沙沙之声。偶尔一两声虎啸\n"
"狼嚎随着腥风飘至耳际。\n"
);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "northeast" : __DIR__"jnroad4",
  "south" : __DIR__"jnroad6",
]));

	setup();
	replace_program(ROOM);
}
