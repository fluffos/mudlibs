// Room: /d/quanzhen/jnroad2.c

inherit ROOM;

void create()
{
	set("short", "黄土大道");
	set("long",
"这是一条黄土大道,再往北走莽林已经在你身后了.东北边是一望\n"
"无际的田地,你擦擦额上的汗水，心理一阵兴奋。脚步轻松了许\n"
"多。\n"
);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "northwest" : __DIR__"jnroad1",
  "east" : __DIR__"jnroad3",
]));

	setup();
	replace_program(ROOM);
}
