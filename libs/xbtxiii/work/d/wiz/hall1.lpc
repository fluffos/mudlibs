// Room: /d/wiz/hall1.c

inherit ROOM;

void create() {
  add_light(this_object(), 1);
  set("short", "巫师交谊厅");
  set("long",
    "这里是巫师聊天讨论的地方，如果巫师们要开会的话，也多半会\n"
    "选在这里举行，往东是巫师会客室。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": "/d/wiz/jobroom",
    "east": "/d/wiz/entrance",
    "west": __DIR__ "lockroom",
    "northwest": __DIR__ "jailroom",
  ]));

  setup();
  call_other("/obj/board/wizard_b", "???");
}
int clean_up() { return 0; }
