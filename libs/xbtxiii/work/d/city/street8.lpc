#pragma save_binary
// Room: /d/city/street8.c

inherit ROOM;

void create() {
  set("coor", ({ 50, 3300, 0 }));
  set("short", "京师街道");
  set("long",
    "你现在正走在京师的大街上，往东可以隐约看到城门铜扣上的反\n"
    "光，在你的北边是京城唯一的钱庄，往西有熙熙攘攘的人群，偶\n"
    "尔还能见到一俩个僧侣。\n"
  );
  set("exits", ([ /* sizeof() == 3 */
    "east": __DIR__ "street9",
    "north": "/d/city/bank",
    "west": "/d/city/street7", ]));
  set("objects", ([
    "d/snow/npc/scavenger": 1 ]));

  set("outdoors", "city");

  setup();
}
