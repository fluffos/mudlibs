#pragma save_binary
// nroad4.c

inherit ROOM;

void create() {
  set("coor", ({ -100, 3800, 0 }));
  set("short", "路口");
  set("long",
    "这里是个交叉路口，过往行人很多。路边有一个茶馆，是供行人\n"
    "歇息的地方。东南是通往京师的北驿道，西北是一条通往天驼关\n"
    "的的大道.\n"
  );
  set("exits", ([ /* sizeof() == 3 */
    "southeast": __DIR__ "nroad3",
    "northwest": __DIR__ "nroad5",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "city");

  setup();
}
