// trainee.c

inherit NPC;

void create()
{
    set_name("家丁", ({ "jiading" }) )
	set("gender", "男性" );
	set("age", 19);
    set("long", "你看到一位身材高大的家丁站在管家身后. /n")
    set("combat_exp", 1000)
	setup();
	carry_object(__DIR__"obj/linen")->wear();
}
