#pragma save_binary
// Room: /d/city/street17.c
inherit ROOM;
void create()
{
        set("coor",({0,2750,0}));
	set("short", "京师街道");
        set("long",
"你来到京师的大街上。南边是京师的南门，出了南门就可去南方\n"
"重镇……南海镇。北边是天桥，人声鼎沸。西边是刑场，一个一\n"
"般人极少去的地方。\n"
);
        set("exits", ([ /* sizeof() == 3 */
//             "west"   :    __DIR__"xingchang",
             "north"  :    __DIR__"bridge",
             "south"  :    __DIR__"southdoor",]));
        set("objects", ([
                "d/fy/pet/pettrainer1": 1 ]) );

        set("outdoors", "city");

        setup();
        replace_program(ROOM);
}

