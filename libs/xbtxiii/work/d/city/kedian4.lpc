#pragma save_binary

inherit ROOM;

void create() {
  set("coor", ({ 40, 2840, 0 }));
  set("short", "西客房");
  set("long",
    "这里是西客房，一个掌柜和两个伙计躲在角落里瑟瑟发抖。\n"
  );

  set("exits", ([
    "east": __DIR__ "kedian2",
  ]));
  set("objects", ([
    "/d/city2/npc/zhanggui": 1,
    "/d/city2/npc/huoji": 2,
  ]));

  setup();
}
