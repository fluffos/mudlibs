#pragma save_binary
// Room: /d/city/street14.c
inherit ROOM;
void create()
{
        set("coor",({-100,3300,0}));
	set("short", "京师街道");
        set("long",
"这里是京师的大街上。西边是京师的西一门，护国寺的僧人西去\n"
"取经必经此地。福缘好的话还可以看到几个有道高僧路过。\n"
);
        set("exits", ([ /* sizeof() == 3 */
                 "north"  :   "/d/city/bieyuan",
               "west"  :    __DIR__"newstreet1",
             "east"  :    __DIR__"street15",]));
/*        set("objects", ([
                "d/snow/npc/scavenger": 1 ]) );*/

        set("outdoors", "city");

        setup();
        replace_program(ROOM);
}

