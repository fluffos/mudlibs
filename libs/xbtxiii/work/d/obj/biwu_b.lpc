#pragma save_binary
// query_b.c

inherit BULLETIN_BOARD;

void create()
{
 set_name("武林匾", ({ "board" }) );
 set("location", "/d/player/square");
  set("board_id", "biwu_b");
    set("long", "这是一个供人留言记事的留言板(help board)。 \n" );
	setup();
	set("capacity", 30);
	replace_program(BULLETIN_BOARD);
}
