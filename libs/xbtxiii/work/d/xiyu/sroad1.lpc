#pragma save_binary
// Room: /d/xiyu/yroad0.c

inherit ROOM;

void create() {
  set("short", "黄土小径");
  set("long",
    "路越来越窄,看样子马上就要进入山区了.前面不远好象立了一个\n"
    "什么东西,走近一看,原来是一个路标(sign).\n"
  );
  set("item_desc", ([
    "sign": "星宿重地,非本教弟子,杀无赦!!!\n" ]));
  set("exits", ([ /* sizeof() == 2 */
    "southup": __DIR__ "nroad0",
    "northeast": __DIR__ "yroad3",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
