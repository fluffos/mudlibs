#pragma save_binary
// Room: /d/temple/ladder1.c

inherit ROOM;

void create()
{
	set("short", "石英岩石阶");
	set("long",
"长长的阶梯也终将到了尽头,阶梯的尽头矗立著一座山门,山门上\n"
"龙飞凤舞地写了三个字(words),笔力苍劲有力显得气势非凡。山\n"
"风吹来带著丝丝的寒意,令你有股高处不胜寒的感觉。\n"
);
        set("item_desc",([
           "words"  : "天山派\n",]) );
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/tguest" : 4,
]));
	set("outdoors", "xiyu");
	set("exits", ([ /* sizeof() == 2 */
  "westup" : __DIR__"entrance",
//  "northwest" : __DIR__"",
     "east" : "/d/npc/m_weapon/bian1",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
