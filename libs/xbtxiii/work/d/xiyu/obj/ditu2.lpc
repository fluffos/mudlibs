#pragma save_binary
//letter.c

inherit ITEM;

void create() {
  set_name("地图", ({ "map1" }));
  set("long", "这是一张假的沙漠地图,上面一个字也没有.\n");
  set("unit", "张");
  set("value", 30);
  set_weight(5);
  //    set("no_get",1);
  //    set("no_drop",1);
}
