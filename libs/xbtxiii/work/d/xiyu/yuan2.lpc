#pragma save_binary
// Room: /u/k/karrimor/temple/temple1.c

#include <room.h>

inherit ROOM;

void create() {
  set("short", "天山后院");
  set("long",
    "这里是天山派的后院,\n"
  );

  set("exits", ([ /* sizeof() == 3 */
    "south": __DIR__ "corr2",
    "northup": "/d/player/liuxian1",
  ]));

  set("valid_startroom", 1);
  set("objects", ([
    __DIR__ "npc/annihi": 1,
  ]));

  setup();

  // To cause the taoist's board to be loaded.
  //	call_other("/obj/board/taoist_b", "???");

  //    create_door("south", "红漆木门", "north");

  //	replace_program(ROOM);
}
