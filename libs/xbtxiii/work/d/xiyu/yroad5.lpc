#pragma save_binary
// Room: /d/xiyu/yroad0.c

inherit ROOM;

void create()
{
      set("short","黄土小径");
      set("long",
"走着，走着，一条黄土大道突入你眼前。这是通向嘉裕关的必由之路。你不时可以看到飞驰\n"
"而过的驿马,扬起阵阵尘土。道路两旁的小商贩也不时地吆喝着“买冰糖葫芦咯...",嘉裕关\n"
"就在西边不远处.从这里向东可以到达雪亭镇。\n"
);
      set("exits", ([ /* sizeof() == 2 */
   "east" : __DIR__"yroad4",
   "west" : __DIR__"",
   "northwest" : __DIR__"mroad",
]));      
      set("no_clean_up",0);

      setup();
      replace_program(ROOM);
}
