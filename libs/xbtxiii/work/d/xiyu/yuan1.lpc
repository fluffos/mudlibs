#pragma save_binary
// Room: /u/k/karrimor/temple/temple1.c

//#include <room.h>

inherit ROOM;

void create() {
  set("short", "晦明院");
  set("long", @LONG
你现在来到晦明院, 在你正面的墙上挂着一幅晦明禅师的画像。
东的圆形拱门(door)同后院相接。走过南边的红漆木门就是山门.
LONG
  );

  //	set("item_desc", ([
  //		"door": (: this_object(), ({"look_door", "east"}) :),
  //	]) );
  set("exits", ([ /* sizeof() == 3 */
    //		"west" : __DIR__"corridor2",
    "south": __DIR__ "entrance",
    "east": __DIR__ "corr1"
  ]));

  //	set("valid_startroom", 1);
  set("objects", ([
    CLASS_D("tiansan") + "/tangxin": 1,
    //		__DIR__"npc/trainer"  :1,
    //		__DIR__"npc/tfighter" :1,
  ]));
  //        create_door("east","木门","west",DOOR_CLOSED);

  setup();


  //	replace_program(ROOM);
}
