#pragma save_binary
// Room: /d/snow/eroad3.c

inherit ROOM;

void create()
{
        set("short", "星宿海");
        set("long",
"这里离黄河源地不远，有很大一片星星点点的水潭，当地人称它为"海子"，这些海子数量很\n"
"多,登高鸟瞰犹如落地的星星，故叫做"星宿海",这里牧草簇拥，奇葩争艳。难怪人们用"玉\n"
"镜横铺三千面,天上银河落人间"来形容它,向南不远就是"星宿教"\n"
);
        set("exits", ([ /* sizeof() == 2 */
	  "eastdown" : __DIR__"nroad1",
	  "southdown" : __DIR__"nroad1",
]));
	set("no_clean_up",0);
        set("outdoors", "xiyu");
        setup();
        replace_program(ROOM);
}
