#pragma save_binary
// Room: /d/snow/eroad3.c

inherit ROOM;

void create() {
  set("short", "山路");
  set("long",
    "这是一条说宽不宽，说窄倒也不窄的山路，路面用几块生满青苔\n"
    "的大石铺成，西面是一段坡地，从这里可以望见西边有几间房屋\n"
    "错落在林木间，东北面都是山壁。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "northdown": __DIR__ "nroad0",
    "westup": __DIR__ "sea",
  ]));
  set("outdoors", "snow");

  setup();
  replace_program(ROOM);
}
