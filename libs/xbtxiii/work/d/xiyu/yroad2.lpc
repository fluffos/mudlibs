#pragma save_binary
// Room: /d/xiyu/yroad0.c

inherit ROOM;

void create() {
  set("short", "黄土大道");
  set("long",
    "你现在正走在黄土大道上，这条笔直，平坦的大道一直通向嘉裕\n"
    "关从这里你依稀可以看到嘉裕关的一角，一想到马上可以进关了，\n"
    "你不由得加快了步伐，心里想着”又可以美美睡上一觉了。嘉裕\n"
    "关就眼前.\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "southeast": __DIR__ "yroad1",
    "northup": __DIR__ "caster",
  ]));
  set("no_clean_up", 0);

  setup();
}
