#pragma save_binary
// Room: /u/k/karrimor/temple/corridor2.c

inherit ROOM;

void create()
{
	set("short", "回廊");
	set("long",
"你现在正走在一条回廊上,东北方是一座花木扶苏的天井,穿过东\n"
"边的圆形拱门就可以到后院。\n"
);
	set("exits", ([ /* sizeof() == 2 */
     "west" : __DIR__"corr1",
     "north" : __DIR__"yuan2",
]));

	setup();
	replace_program(ROOM);
}
