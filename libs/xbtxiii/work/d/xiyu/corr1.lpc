#pragma save_binary
// Room: /u/k/karrimor/temple/corridor1.c
#include <room.h>
inherit ROOM;

void create()
{
	set("short", "回廊");
	set("long",
"你现在正走在一条回廊上,西北方是一座花木扶苏的天井,穿过西\n"
"边的圆形拱门就可以回到大殿。\n"
);
	set("exits", ([ /* sizeof() == 2 */
     "west" : __DIR__"yuan1",
     "east" : __DIR__"corr2",
]));
	setup();
//	replace_program(ROOM);
}
