#pragma save_binary
// Room: /d/xiyu/yroad0.c

inherit ROOM;

void create() {
  set("short", "大草原");
  set("long",
    "总算走出沙漠了.这里是多么空阔,多么辽远,多么....整个地面\n"
    "形成一片金色带绿的海洋.上面点缀着千万朵各种各样的花(flow\n"
    "er).草原,竟然有这么美丽.这里西邻天山,南边是一个小村庄.\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "shamea",
    "westup": __DIR__ "hpath1",
  ]));
  set("item_desc", ([
    "flower": "这朵小花颜色虽然是素净的白,花朵却开动人的
美,而且还飘逸着诱人的香.\n" ]));

  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
