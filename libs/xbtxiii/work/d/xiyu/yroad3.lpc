#pragma save_binary
// Room: /d/xiyu/yroad0.c

inherit ROOM;

void create() {
  set("short", "黄土大道");
  set("long",
    "你现在正站在嘉裕关外,道路两旁不时可以看到一堆堆白骨,显然\n"
    "不久前这里有过一场战斗.从这里向北望去是一望无际的大沙漠,\n"
    "不时一阵北风吹过,卷起一阵黄沙,弄得你满头是沙.南边群山连\n"
    "绵不断.\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "caster",
    "northwest": __DIR__ "shame",
    //   "southwest" : __DIR__"sroad1",
  ]));
  set("objects", ([
    "/d/xiyu/npc/waiter": 1
  ]));
  set("no_clean_up", 0);

  setup();
}
