// Room: gate.c

inherit ROOM;

void create() {
  set("coor", ({ -50, 2870, 0 }));
  set("short", "金风细雨楼入口");
  set("long",
    "你现在正站在金风细雨楼的大门口，两只巨大的石狮雄据在大门\n"
    "两侧，西侧一杆大旗迎风飘摆，上面写着几个龙飞凤舞的大字，\n"
    "赫然便是“金风细雨楼”。大门是用上等的檀木雕成的，门环在\n"
    "夕阳照射下闪闪发光。\n"
  );
  set("objects", ([ /* sizeof() ==2 */
    __DIR__ "npc/guard2": 1,
    __DIR__ "npc/guard": 1,
  ]));
  set("outdoors", "city");
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "jiedairoom",
    "north": "/d/city/newstreet3",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
