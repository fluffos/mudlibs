// This room is made by dingding.

#include <room.h>

inherit ROOM;

string look_sign(object me);

void create()
{
    set("short", "丁丁实验室");
    set("long",@LONG
这里是丁丁的实验室,很遗憾目前没有什么礼物可以送给你,我
非常抱歉,欢迎你的光临,并希望你在此逗留期间能给我一些指导性
建议,如果能够在这里建立一个人人喜欢的新场景,相信有些朋友会
很高兴,丁丁也不用每次都对天神说抱歉了。
    随时欢迎你的批评和指教，谢谢！
LONG
       );
     set("no_fight","1");
     set("no_magic","1");
     set("valid_startroom",1);
     set("objects",([ /* sizeof() == 1 */
                "u/j/jackii/npc/mei" : 1,
                    ]));
     set("exits",([ /* sizeof() == 1 */
         "east" : __DIR__"jiedairoom",
       ]));

	setup();
    call_other("/obj/board/ding_b", "???");
}
