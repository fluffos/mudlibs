// Room: /room3.c

inherit ROOM;

void create() {
  set("coor", ({ -50, 2850, 0 }));
  set("short", "厢房");
  set("long",
    "这里是厢房，只要是来拜访金风细雨楼的朋友，不论贵贱，都可\n"
    "在此享受到楼内提供的服务。由于京师最近发生了大地震，这里\n"
    "的房屋倒塌了许多，目前很多地方还在修复中，再往南是后花园，\n"
    "不过因为这里正在施工，花园现在暂不对外开放。\n"
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "jiedairoom",
  ]));

  setup();
  replace_program(ROOM);
}
