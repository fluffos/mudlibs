// Room: /zoulang1.c

inherit ROOM;

void create() {
  set("coor", ({ -30, 2860, 10 }));
  set("short", "走廊");
  set("long",
    "本处建造中，目前暂不开放。\n"
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "yuleroom",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
