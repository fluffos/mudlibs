#pragma save_binary
// Room: /d/choyin/bridge4.c

inherit ROOM;

void create()
{
	set("coor",({3540,3020,0}));
	set("short", "曲桥");
	set("long",
"这是一座弯弯曲曲的曲桥，横过翠柳湖连接东岸的雨桥庄跟西岸\n"
"的乔阴县城，从这里可以欣赏翠柳湖的美景，许多睡莲散布在桥\n"
"墩四周，湖面飘著数不尽的浮萍，浮萍底下有不少艳红锦鲤穿梭\n"
"来去。\n"
);
	set("exits", ([ /* sizeof() == 2 */
		"south" : __DIR__"bridge5",
		"west" : __DIR__"bridge3",
	]));
	set("outdoors", "choyin");
	set("objects", ([
		__DIR__"npc/girl" : 1,
		__DIR__"npc/maid" : 2,
	]) );

	setup();
	replace_program(ROOM);
}
