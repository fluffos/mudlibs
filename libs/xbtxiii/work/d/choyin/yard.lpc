#pragma save_binary
// Room: /d/choyin/yard.c

inherit ROOM;

void create() {
  set("short", "庭院");
  set("long",
    "这是一间什麽也没有的空房间。\n"
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "club",
  ]));
  set("no_clean_up", 0);

  setup();
}
