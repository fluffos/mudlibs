#pragma save_binary
// Room: /d/choyin/w_gate.c

inherit ROOM;

void create() {
  set("coor", ({ 3300, 3200, 0 }));
  set("short", "西城门");
  set("long",
    "这里乔阴县城的西门，一座高大的石牌楼上面刻著「西门」两个\n"
    "大字，西边一条土路青郁郁地通往北边的山上，石牌楼旁蹲著两\n"
    "只铁狮.与东城门相比,西门这一带还算热闹，常常还可以看到和\n"
    "尚呢!\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "w_street1",
    // "west" : __DIR__"spath1",
  ]));
  set("objects", ([
    __DIR__ "npc/cityguard": 2,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
