#pragma save_binary
// Room: /d/choyin/stonehole.c

inherit ROOM;

void create() {
  set("short", "石室");
  set("long",
    "石室黑漆漆的,双目不见物.你的双手好象触到了什么东西(objec\n"
    "t),惧意顿生.\n"
  );
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
