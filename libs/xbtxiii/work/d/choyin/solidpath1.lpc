#pragma save_binary
// Room: /d/choyin/solidpath1.c

inherit ROOM;

void create()
{
	set("coor",({3700,3200,0}));
	set("short", "土路");
	set("long",
"黄土铺就的小路.阴雨天,泥泞难行.往南就上山了.\n"
);
	set("exits", ([ /* sizeof() == 3 */
  "west" : __DIR__"e_gate",
  "south" : __DIR__"rockpath2",
  "north" : __DIR__"solidpath2",
]));
        set("outdoors", "choyin");

	setup();
	replace_program(ROOM);
}
