// Room: /d/choyin/e_street1.c

inherit ROOM;

void create()
{
	set("coor",({3600,3200,0}));
	set("short", "东大街");
	set("long",
"这里是乔阴县城的东大街，西边不远处的石台便是远近驰名的「\n"
"树王坟」，东大街往东一直通往东城门，街道两边有许多的民宅，\n"
"街上行人渐稀。\n"
);
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"tree_tomb",
  "east" : __DIR__"e_gate",
]));
        set("outdoors", "choyin");

	setup();
	replace_program(ROOM);
}
