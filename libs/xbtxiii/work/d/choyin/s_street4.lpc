#pragma save_binary
// Room: /d/choyin/s_street4.c

inherit ROOM;

void create() {
  set("coor", ({ 3450, 3050, 0 }));
  set("short", "承安街");
  set("long",
    "这里是乔阴县城城南的承安街，街道十分宽敞，南北两边都是一\n"
    "些贩卖南北货、日用品的商店，这里虽然正当乔阴县人车来往最\n"
    "频繁的要冲，可是却没有福林大街热闹，往东走便是南门广场，\n"
    "往西则是和昌平街的交叉路口。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "s_street5",
    "east": __DIR__ "s_street1",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "choyin");

  setup();
  replace_program(ROOM);
}
