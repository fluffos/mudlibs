#pragma save_binary
// Room: /d/xingxiu/xxroad4.c

inherit ROOM;

void create()
{
        set("coor",({-3650,1250,250}));
	set("short", "小路");
        set("long",
"你走在一条在山壁上人工开凿出来崎岖的小路上，这里，寒风凛\n"
"冽，西面是望不见底的深涧。\n"
);
        set("exits", ([
  "southdown" : __DIR__"tianroad3",
  "north" : __DIR__"xxroad5",
]));

        set("no_clean_up", 0);
        set("outdoors", "xingxiuhai");

        setup();
        replace_program(ROOM);
}
