//snot-arg.c

inherit ITEM;

void create()
{
	set_name(HIM"手绢",({"snot-arg"}));
	set("long",HIM"一个粉红色的手绢，是蓝手指亲手做的。\n"
	       "上面绣着几个字：珍重。\n"NOR);
	set("unit","片");
	set_weight(1);
	set("value",100);
	setup();	
}

