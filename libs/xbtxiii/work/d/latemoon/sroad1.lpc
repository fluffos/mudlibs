#pragma save_binary
inherit ROOM;

void create()
{
        set("short", "山路");
        set("long",
"你走在一条蜿蜒的小径,小径的两旁是半个人高的杂草,你可以感\n"
"受到清凉的微风从两侧吹来。\n"
);
        set("exits", ([ /* sizeof() == 2 */
         "southeast" :__DIR__"sroad2",
	//"north" : "/u/n/newone/gate",
]));
        set("outdoors", "latemoon");

        setup();
        replace_program(ROOM);
}
