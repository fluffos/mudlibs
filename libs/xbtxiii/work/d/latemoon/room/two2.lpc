#pragma save_binary

inherit ROOM;

void create() {
  add_light(this_object(), 1);
  set("coor", ({ 1400, 3700, 0 }));
  set("short", "夹道");
  set("long", @LONG
你走在后厅内院的夹道上，庄内像这样的走廊很多，你若没有地
理概念很容易迷路。长长的走道中, 纯金铸成的长灯架上挂了一排饰
丽的宫灯! 入夜之后灯火辉煌! 十分美丽!
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "guest2",
    "east": __DIR__ "two1",

  ]));

  setup();
  replace_program(ROOM);
}
