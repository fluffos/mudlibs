#pragma save_binary
inherit ROOM;

void create() {
  set("coor", ({ 1610, 3250, 0 }));
  set("short", "晚月庄后门");
  set("long",
    "这是晚月庄的后门，在美丽的湘园南侧，有一间小小的密室。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "paroad1",
    "south": __DIR__ "room1",

  ]));

  setup();
  replace_program(ROOM);
}
