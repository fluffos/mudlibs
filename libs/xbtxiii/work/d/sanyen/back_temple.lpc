#pragma save_binary
// Room: /d/sanyen/back_temple.c

inherit ROOM;

void create()
{
	set("coor",({3480,3480,20}));
	set("short", "后殿");
	set("long",
"一间宽大的后殿,正中有一尊药师如来的金身塑像。四周的墙上\n"
"唯妙唯肖地彩绘著十八罗汉,每个罗汉的旁边并写著他得道升天\n"
"的过程。\n"
);
	set("exits", ([ /* sizeof() == 2 */
  "south" : "/d/sanyen/corridor",
  "east" : "/d/sanyen/tower",
]));

     set("objects", ([
         __DIR__"npc/little_bonze" : 1 ]) );

	setup();
	replace_program(ROOM);
}
