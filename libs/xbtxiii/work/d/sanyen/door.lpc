#pragma save_binary
// Room: /d/sanyen/door.c

inherit ROOM;

void create() {
  set("coor", ({ 3480, 3400, 20 }));
  set("short", "山烟寺寺门");
  set("long",
    "出现在眼前的是一座宏伟壮丽的寺门,上方覆著镶金的瓦片,瓦片\n"
    "的下方挂著一方翡翠匾额,上面写著『山烟寺』三个烫金大字。\n"
    "正中的大门正紧紧关闭著,只有在迎接大人物时才会开启。现在\n"
    "只开著两旁的偏门。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": "/d/sanyen/road1",
    "south": "/d/sanyen/front_yard",
  ]));
  set("outdoors", "sanyen");

  setup();
}
