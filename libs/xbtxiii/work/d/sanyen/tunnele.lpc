#pragma save_binary
// Room: /d/sanyen/tunnele.c

inherit ROOM;

void create()
{
	set("short", "隧道口");
	set("long",
"你走进了一个凿穿山壁开成的隧道，东边是隧道的出口，往西不\n"
"远处也可以从这里瞧见西边的出口，一股硫磺的味道从隧道壁中\n"
"传来，想必左近便有温泉或是火山。\n"
);
	set("exits", ([ /* sizeof() == 2 */
  "west" : "/d/sanyen/tunnel",
  "east" : "/d/sanyen/gate",
]));

	setup();
	replace_program(ROOM);
}
