#pragma save_binary
// Room: /d/green/temple0.c

inherit ROOM;

void create() {
  set("coor", ({ 60, 4910, 10 }));
  set("short", "土地庙");
  set("long",
    "这是一间小小的土地庙,庙内相当的暗,一股湿气混上焚烧冥纸的\n"
    "味道使得屋内的的气味显得很奇特.供桌上零星地放个一些水果,\n"
    "一尊木刻的土地公像孤独地立在前面.庙外的小空地边有一个简\n"
    "陋的金纸亭.\n"
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": "/d/green/path2",
  ]));

  setup();
}
