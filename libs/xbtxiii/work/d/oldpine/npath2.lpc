#pragma save_binary
// Room: /d/oldpine/npath2.c

inherit ROOM;

void create() {
  set("coor", ({ 1400, 4700, 10 }));
  set("short", "林间小路");
  set("long",
    "你现在正走在一条林间的小路上，小路两旁长满了高耸的松树，\n"
    "从林间传来阵阵鸟叫声，小路往南北两个方向延伸出去。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "southeast": "/d/oldpine/npath3",
    "north": "/d/oldpine/npath1",
  ]));
  set("outdoors", "oldpine");

  setup();
}
