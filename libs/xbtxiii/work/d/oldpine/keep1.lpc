#pragma save_binary
// Room: /d/oldpine/keep1.c

inherit ROOM;

void create()
{
        set("coor",({3650,3850,10}));
	set("short", "老松寨秘密入口");
        set("long",
"这里只见一个木头搭成的大门，门框上挂著四、五的不知道是什\n"
"麽动物的头骨跟一些铁□，外面再铺上一层乾枯的松枝，在这样\n"
"一个森林里还真不容易发觉。\n"
);
        set("exits", ([ /* sizeof() == 2 */
  "west" : "/d/oldpine/pine2",
  "east" : "/d/oldpine/keep2",
]));
        set("objects", ([ /* sizeof() == 1 */
  "/d/oldpine/npc/bandit_guard" : 4,
]));

        setup();
        replace_program(ROOM);
}
