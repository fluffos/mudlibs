#pragma save_binary
// Room: /d/oldpine/passage.c

inherit ROOM;

void create() {
  set("coor", ({ 3600, 4000, -10 }));
  set("short", "秘密通道入口");
  set("long",
    "你现在正站在一个岩石的通道之中，在你南边有一片亮晶晶的帘\n"
    "幕(curtain)，从帘幕後面传来阵阵轰隆隆的水声，通道两边的\n"
    "石壁上安置著许多铜铸的烛台，照亮了这个□穴，往北似乎有亮\n"
    "光。\n"
  );
  set("item_desc", ([ /* sizeof() == 1 */
    "curtain": "这片帘幕似乎是用一种极薄的丝绸织成的，後面还传来阵阵水声。\n",
  ]));
  set("exits", ([ /* sizeof() == 2 */
    "north": "/d/oldpine/secrectpath1",
    "south": "/d/oldpine/waterfall",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/d/oldpine/npc/maniac": 1,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
