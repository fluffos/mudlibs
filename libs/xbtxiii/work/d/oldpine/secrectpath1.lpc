#pragma save_binary
// Room: /d/oldpine/secrectpath1.c

inherit ROOM;

void create()
{
        set("coor",({3600,4050,-10}));
	set("short", "秘密通道");
        set("long",
"你现在正站在一个岩石通道之中，从南边隐隐传来哗哗的水声，\n"
"岩壁上生著厚厚的青苔，一股恶臭从北边传来，像是什麽野兽，\n"
"也许你该回头出去了。\n"
);
        set("exits", ([ /* sizeof() == 1 */
  "south" : "/d/oldpine/passage",
]));
        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}
