#pragma save_binary
// Room: /d/oldpine/spath3.c
#include <roadbusy.h>

inherit ROOM;

void create()
{
        set("coor",({3500,3700,6}));
	set("short", "下坡道");
        set("long",
"这是一条笔直的坡道，北高南低，道路两旁长满了许多松树，从\n"
"松林间隐约可以听到东边传来流水的声音。\n"
);
        set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"spath4",
  "north" : __DIR__"spath2",
]));
        set("no_clean_up", 0);
        set("outdoors", "oldpine");

        setup();
//        replace_program(ROOM);
}
