#pragma save_binary
// Room: /d/oldpine/tree2.c

inherit ROOM;

void create() {
  set("short", "大松树上");
  set("long",
    "你现在正攀附在一株大松树的树干上，这里离地面已经有七、八\n"
    "丈高，如果不小心跌了下去，不断条腿也得去半条命，松树上结\n"
    "了许多松果，不过大部分都离树干很远，你摘不到。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "up": "/d/oldpine/tree3",
    "down": "/d/oldpine/tree1",
  ]));

  setup();
  replace_program(ROOM);
}
