#pragma save_binary
// Room: /d/village/lakebottom2.c

inherit ROOM;

void create() {
  set("coor", ({ 3600, 3850, -30 }));
  set("short", "湖底火焰洞");
  set("long",
    "从死水潭游上来,发现这里是湖底的火山口，炽热的岩浆正在翻\n"
    "滚着你感到一阵阵的热浪袭来,令人有一种窒息的感觉。北面似\n"
    "乎有一洞口，里面似乎有两盏灯忽亮忽灭，你的本能告诉你，那\n"
    "是一只庞然大物！！！\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "out": "/d/oldpine/riverbank1",
    "north": "/d/oldpine/riverhole1",
  ]));
  set("objects", ([
    __DIR__ "npc/firedragon": 1,
  ]));
  set("no_clean_up", 0);

  setup();
  //replace_program(ROOM);
}
