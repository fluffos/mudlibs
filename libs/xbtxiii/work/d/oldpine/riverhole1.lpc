#pragma save_binary
// Room: /d/village/lakebottom2.c

inherit ROOM;

void create() {
  set("coor", ({ 3600, 3900, -30 }));
  set("short", "湖底火焰洞");
  set("long",
    "这里十分光亮，地上到处都是骸骨，你一抬头...妈呀！！一只\n"
    "怎么样的怪物呀，你还没看清它，它已经向你扑来！！\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": "/d/oldpine/riverbuttom",
    "north": "/d/oldpine/riverhole",
  ]));
  set("no_clean_up", 0);

  setup();
  //replace_program(ROOM);
}
