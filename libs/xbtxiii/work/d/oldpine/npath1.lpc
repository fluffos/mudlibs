#pragma save_binary
// Room: /d/oldpine/npath1.c
#include <roadbusy.h>

inherit ROOM;

void create()
{
        set("coor",({1400,4800,10}));
	set("short", "林间小路");
        set("long",
"你现在正走在一条林间的小路，往北不远处走出这座林子就是雪\n"
"亭镇，小路两旁长满了许多高耸的松树，雪亭镇的居民都把这里\n"
"叫做「老松林」，据说林子里有株树龄上千的老松树，颇有灵验。\n"
);
        set("exits", ([ /* sizeof() == 2 */
  "south" : "/d/oldpine/npath2",
  "north" : "/d/ny/eroad3",
]));
        set("outdoors", "oldpine");

        setup();
//        replace_program(ROOM);
}
