// Room: /d/jiangnan/jinglin/qzroad2.c

inherit ROOM;

void create()
{
	set("short", "群山边缘");
	set("long",
"这里是群山的边缘,游客一般到这里都会折回去了,这里人迹稀疏,\n"
"只有隐约几个人影,大概是长住山边的农夫和猎人.\n"
);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "northwest" : __DIR__"qzroad3",
  "south" : __DIR__"qzroad1",
]));

	setup();
	replace_program(ROOM);
}
