// Room: /d/jiangnan/jinglin/qzroad3.c

inherit ROOM;

void create()
{
	set("short", "大驿道");
	set("long",
"这是一条大驿道.大概是山中的人开的,南边可以一直通向金陵。\n"
"东北是一望无际的莽林，只有风吹过树梢的沙沙之声。\n"
);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"qzroad4",
  "southeast" : __DIR__"qzroad2",
]));

	setup();
	replace_program(ROOM);
}
