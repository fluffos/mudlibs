// Room: /d/jiangnan/daguanyuan/trace1.c

#include <room.h>

inherit ROOM;


void create()
{
        add_light(this_object(),1);
	set("coor",({-220,-2400,0}));
	set("short", "厅房");
        set("long",
"转过屏风是小小三间厅房，厅后就是正房大院，是贾母的住处。\n"
);
 set("exits", ([ /* sizeof() = 2 */
        "north" : "/d/jiangnan/rongguo/zhenyard",
        "south" : "/d/jiangnan/rongguo/trace2",
	]));
      
        
       create_door("north","正房院门","south", DOOR_CLOSED);
  set("no_fight", 1);
        set("no_magic","1"); 
    set("objects", ([ 
//              __DIR__"npc/girl" : 1,
        ]));
        setup();
        replace_program(ROOM);
}
