// Room: /d/jiangnan/daguanyuan/lroom1.c

#include <room.h>

inherit ROOM;


void create() {
  set("short", "内房");
  set("long",
    "这儿是贾母住的内房.放着合着地步打的椅案。一个丫鬟正在一\n"
    "丝不苟地整理着房内的东西。着。\n"
  );
  set("exits", ([ /* sizeof() = 3 */
    "south": "/d/jiangnan/rongguo/zroom1",
  ]));
  //  set("objects",(["/daemon/class/reddream/jiamother":1])); 

  set("outdoors", "daguanyuan");
  set("clean_up", 1);
  create_door("south", "内房门", "north", DOOR_CLOSED);
  setup();
  replace_program(ROOM);
}
