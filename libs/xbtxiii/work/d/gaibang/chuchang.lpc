#pragma save_binary
// chuchang shi

inherit ROOM;

void create() {
  set("short", "储藏室");
  set("long",
    "这里是丐帮的储藏室，你现在只能看见一个个锁着的箱子。\n"
  );
  set("exits", ([
    "west": __DIR__ "mishi",
    "up": __DIR__ "undertre",
  ]));

  setup();
}
