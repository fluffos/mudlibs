inherit ITEM;
void create() {
  set_name("天山雪莲的叶子", ({ "leaf", }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "几片天山雪莲的叶子。不知道有什么用处。\n");
    set("value", 10000);
    set("unit", "片");
  }
  setup();
}
