inherit ITEM;
void create() {
  set_name("免死金牌", ({ "miansi jinpai", "sign", }));
  set_weight(10);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "如朕亲临 免死。\n");
    set("value", 100000);
    set("unit", "块");
    set("皇帝的笑容/免死金牌", 1);
  }
  setup();
}
