#pragma save_binary
#include <ansi.h>
inherit ROOM;
 
void create()
{
        set("short", "大海");
        set("long",
"你正处在茫茫大海之中。你极目远望，只见海天一线，周围连个\n"
"黑点都没有。耳边只有海浪的拍打声。\n"
);   
        setup();
        replace_program(ROOM) ;
}
