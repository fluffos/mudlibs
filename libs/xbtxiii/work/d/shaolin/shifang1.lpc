#pragma save_binary
// Room: /d/shoalin/shifang1

inherit ROOM;

void create()
{
        set("short", "石坊");
        set("long", @LONG
LONG
        );
        set("exits", ([ /* sizeof() ==2  */
  "west" : __DIR__"pineforest1",
  "east" : __DIR__"square1",
]));
       set("clean_up", 1);
        set("outdoors", "shaolin");
        set("objects", ([ /* sizeof() == 1 */
              __DIR__"npc/qing-wu" : 1,
]));
        setup();
        replace_program(ROOM);
}
