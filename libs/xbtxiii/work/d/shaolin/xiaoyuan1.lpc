#pragma save_binary
// Room: /d/shoalin/xiaoyuan1

inherit ROOM;

void create() {
  set("short", "鼓楼小院");
  set("long",
    "这是一间不大的小院，院子的中央有一棵菩提树，一个小沙弥正\n"
    "在树下清扫落叶。轻风吹过，传来阵阵沁人心脾的花香。\n"
  );
  set("exits", ([ /* sizeof() ==2  */
    "west": "/d/shaolin/gulou1",
    "east": "/d/shaolin/square2",
  ]));
  set("no_clean_up", 1);
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/xiao-ci": 1,
  ]));
  set("outdoors", "shaolin");

  setup();
  replace_program(ROOM);
}
