#pragma save_binary
// Room: /d/shaolin/shifang2.c

inherit ROOM;

void create()
{
        set("short", "石坊");
        set("long", @LONG
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
  "west" :__DIR__ "square1",
  "east" :__DIR__ "pineforest2",
]));
        set("clean_up", 1);
        set("outdoors", "snow");
        set("objects", ([ /* sizeof() == 1 */
              __DIR__"npc/qing-wei" : 1,
]));
        setup();
        replace_program(ROOM);
}
