#pragma save_binary
// Room: /d/shaolin/shanmen2.c

#include <room.h>
inherit ROOM;

void create() {
  set("short", "山门殿");
  set("long",
    "你正站在山门殿里,两面是八大金刚,正横眉竖目地盯着你,让你\n"
    "不由得想起幸好自己没做啥亏心事!从后殿门看进去,甬路旁是一\n"
    "片碑林。\n"
  );
  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "beilin2",
    "west": __DIR__ "cedian1",
    "south": __DIR__ "shanmen1",
    "east": __DIR__ "cedian2",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "shaolin");
  //       create_door("south", "黄铜大门","north", DOOR_CLOSED);

  setup();
  replace_program(ROOM);
}
