#pragma save_binary
// Room: /d/shoalin/bookroom
#include <room.h>
inherit ROOM;

void create() {
  set("short", "武器库");
  set("long", @LONG
LONG
  );
  set("exits", ([ /* sizeof() ==1  */
    "west": "/d/shaolin/bamboo2",
    "north": "/d/shaolin/fangjuku",
  ]));
  set("no_clean_up", 1);
  set("outdoors", "shaolin");
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/dao-cheng": 1,
  ]));
  create_door("west", "木门", "east", DOOR_CLOSED);
  setup();
  replace_program(ROOM);
}
