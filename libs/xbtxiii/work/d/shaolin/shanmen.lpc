#pragma save_binary
// Room: /d/shaolin/shanmen.c

#include <room.h>
inherit ROOM;

void create() {
  set("short", "山门");
  set("long",
    "你正站在山门前,头顶上悬挂着一个匾额(sign)。往里望去,是一\n"
    "个大殿。\n"
  );
  set("exits", ([ /* sizeof() == 4 */
    "north": "/d/shaolin/shanmen2",
    "south": "/d/shaolin/shijie1",
  ]));
  set("item-desc", ([
    "sign": "少林寺\n",
  ]));
  //        set("no_clean_up", 0);

  set("outdoors", "shaolin");
  create_door("south", "黄铜大门", "north", DOOR_CLOSED);

  setup();
}
