#pragma save_binary
// Room: /d/shaolin/cedian1.c

inherit ROOM;

void create() {
  set("short", "侧殿");
  set("long",
    "这里供奉的是伽蓝菩萨。\n"
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": "/d/shaolin/shanmen2",
  ]));
  set("outdoors", "shaolin");

  setup();
  replace_program(ROOM);
}
