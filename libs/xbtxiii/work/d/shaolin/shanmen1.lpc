#pragma save_binary
// Room: /d/shaolin/shanmen1.c

#include <room.h>
inherit ROOM;

void create()
{
        set("short", "山门");
        set("long",
"你正站在山门前,头顶上悬挂着一个匾额(sign)。往里望去,是一\n"
"个大殿。\n"
);
        set("exits", ([ /* sizeof() == 4 */
            "north" :__DIR__"shanmen2",
            "south" :__DIR__"shijie1",
           ]));
        set("item_desc", ([
            "sign": "少林寺!!!\n",
           ]) );

        set("outdoors", "shaolin");
       create_door("south", "黄铜大门","north", DOOR_CLOSED);
       set("clean_up",1);
       set("objects",([ /* sizeof() ==1*/
              __DIR__"npc/seng-bing3": 6,
]));
        setup();
        replace_program(ROOM);
}
