#pragma save_binary
// Room: /d/shaolin/cedian3.c

inherit ROOM;

void create()
{
        set("coor",({4090,-770,10}));
	set("short", "侧殿");
        set("long",
"这里供奉的是十八罗汉中的九个。\n"
);
        set("exits", ([ /* sizeof() == 1 */
            "east" : __DIR__"twdian",
            ]) );
        set("outdoors", "shaolin");

        setup();
        replace_program(ROOM);
}
