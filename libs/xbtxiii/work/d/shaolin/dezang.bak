// Room: /d/snow/dezang.c

inherit ROOM;

void create()
{
        set("short", "地藏殿");
        set("long", @LONG
地藏王菩萨神情庄重,凄苦,悲天悯人由此可见一斑.神像左手
中食两指斜指正上方,似乎有些奇怪.
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
  "southeast" : "/d/shaolin/square5",
]));
        set("clean_up", 1);
        set("objects", ([
           __DIR__"damo-ling":1,
]));
        set("outdoors", "shaolin");
        setup();
        replace_program(ROOM);
}
