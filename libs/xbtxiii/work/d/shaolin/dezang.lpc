#pragma save_binary
// Room: /d/snow/dezang.c

inherit ROOM;
int do_find ();
void create()
{
        set("short", "地藏殿");
        set("long",
"这里就是地臧殿，供奉着地臧王菩萨。你望着佛像，不禁有些心\n"
"醉。\n"
);
        set("exits", ([ /* sizeof() == 1 */
  "southeast" : __DIR__"square5",
]));
        set("clean_up", 1);
//       set("objects", ([
//           __DIR__"damo-ling":1,
//]));
//        set("outdoors", "shaolin");
        setup();
//        replace_program(ROOM);
}

void init ()
{
        add_action ("do_find", "find");
}

int valid_leave(object me,string dir)
{
        if ( dir=="southeast" )
                tell_object(me, "总觉得有些不甘心,还是回去找一找(find)\n");
        return ::valid_leave(me,dir);
}

int do_find ()
{
        object *list, me,token; 
        int i;

        me = this_player ();

        list = all_inventory(me);
//        i = sizeof(list);
//        while (i--) {
//              if (((string)list[i]->query("id")) == "damo-ling")
//                        return notify_fail("忙活了半天，没有什么新的发现。\n");
//                }

        if (random(me->query("kar") + 30) >= 35) {
                token = new(__DIR__"damo-ling");
                token->move(me);
                message_vision("$N竟找到了一个达摩令。\n", me);
                return 1;
                }

        return notify_fail ("白忙了半天，一无所获。\n");
}
