// Room: /d/shaolin/shanmen.c

#include <room.h>
inherit ROOM;

void create()
{
        set("short", "山门殿");
        set("long", @LONG
你正站在山门殿里,两面是八大金刚,正横眉竖目地盯着你,让你不由得想起幸好自己没做啥亏心事!从后殿门看进去,甬路旁是一片碑林。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "north" : "/d/shaolin/beilin2",
  "west" : "/d/shaolin/cedian1",
  "south" :"/d/shaolin/shijie1",
  "east" : "/d/shaolin/cedian2",
]));
        set("no_clean_up", 0);
        set("outdoors", "shaolin");
       create_door("south", "黄铜大门","north", DOOR_CLOSED);
        setup();
        replace_program(ROOM);
}
