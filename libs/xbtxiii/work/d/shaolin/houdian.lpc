#pragma save_binary
// Room: /d/shoalin/houdian
#include <room.h>
inherit ROOM;

void create() {
  set("coor", ({ 4100, -730, 20 }));
  set("short", "后殿");
  set("long",
    "后殿里供奉的是弥勒佛,他正笑嘻嘻地看着你呢。南面有通向走\n"
    "廊的红漆大门(door)。\n"
  );
  set("exits", ([ /* sizeof() ==4  */
    "west": "/d/shaolin/zoulang2",
    "east": "/d/shaolin/zoulang3",
    "north": "/d/shaolin/wuchang",
    "southdown": "/d/shaolin/guangchang4",
  ]));
  set("no_clean_up", 1);
  set("outdoors", "shaolin");
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/hui-ru": 1,
  ]));
  create_door("southdown", "红漆大门", "northup", DOOR_CLOSED);
  setup();
  replace_program(ROOM);
}
