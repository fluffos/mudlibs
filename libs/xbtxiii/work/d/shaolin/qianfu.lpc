#pragma save_binary
// Room: /d/snow/qianfu.c
#include <room.h>

inherit ROOM;

void create()
{
        set("short", "千佛殿");
        set("long",
"殿中气势磅礴的巨幅彩色壁画----五百罗汉朝毗卢,把你吸引住\n"
"了:画面长百丈,高两丈一尺五寸三分有余.以水,风,云,山林为背\n"
"景,清晰有致又浑然一体,色调朱,黑,十分和谐.五百个罗汉共分\n"
"为三十五组,每一组都有一个神奇的传说.\n"
);
        set("exits", ([ /* sizeof() == 1 */
  "southdown" : __DIR__"shijie11",
  "northup" : __DIR__"bamboo1",
]));
        set("no_clean_up", 0);
        set("outdoors", "shaolin");
        set("objects",([
                CLASS_D("shaolin") + "/qing-guan" : 1,
 ]) );
        create_door("northup","小木门","southdown",DOOR_CLOSED);
        setup();
        replace_program(ROOM);
}
