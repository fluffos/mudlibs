#pragma save_binary
// Room: /u/cloud/shijie4.c
#include <roadbusy.h>

inherit ROOM;

void create()
{
        set("coor",({4150,-850,0}));
	set("short", "石阶");
        set("long",
"走了大半天,终于看到了这条青石板路的尽头是一座雄伟的寺院-\n"
"---少林寺!还不快些走啊?!可还有一段路噢!\n"
);
        set("exits", ([ /* sizeof() == 3 */
   "southwest" : "/u/cloud/shijie5",
  "southdown" : "/d/shaolin/shijie3",
]));
        set("outdoors", "cloud");
        setup();
//        replace_program(ROOM);
}

