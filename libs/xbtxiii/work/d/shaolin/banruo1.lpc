#pragma save_binary
// Room: /d/shoalin/banruo1

inherit ROOM;

void create()
{
        set("coor",({4060,-725,20}));
	set("short", "般若堂");
        set("long", @LONG
LONG
        );
        set("exits", ([ /* sizeof() ==2  */
  "north" : "/d/shaolin/banruo2",
  "south" : "/d/shaolin/zoulang1",
]));
       set("no_clean_up", 1);
        set("outdoors", "shaolin");
        set("objects", ([ /* sizeof() == 2 */
              __DIR__"npc/cheng-xin1" : 1,
]));

        setup();
        replace_program(ROOM);
}
