#pragma save_binary
// Room: /d/shoalin/passage3

inherit ROOM;

void create()
{
        set("short", "走廊");
        set("long",
"这是后殿东边的走廊,从这儿可以看见后殿后面的练武场上有不\n"
"少僧人正在练武。\n"
);
        set("exits", ([ /* sizeof() ==2  */
  "east" : "/d/shaolin/passage4",
  "west" : "/d/shaolin/houdian",
]));
       set("no_clean_up", 1);
       set("outdoors", "shaolin");
        set("objects", ([ /* sizeof() == 2 */
              __DIR__"npc/dao-pin" : 1,
]));

        setup();
        replace_program(ROOM);
}
