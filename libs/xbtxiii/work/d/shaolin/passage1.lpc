#pragma save_binary
// Room: /d/shoalin/passage1

inherit ROOM;

void create()
{
        set("short", "走廊");
        set("long",
"这是后殿西边的走廊,北边是般若堂。\n"
);
        set("exits", ([ /* sizeof() ==2  */
  "north" : "/d/shaolin/banruo1",
  "east" : "/d/shaolin/passage2",
]));
       set("no_clean_up", 1);
        set("outdoors", "shaolin");

        setup();
        replace_program(ROOM);
}
