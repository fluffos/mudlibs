#pragma save_binary
// Room: /d/shaolin/jielu 

inherit ROOM;

void create()
{
        set("short", "戒律院");
        set("long",
"这间屋子特别的安静,只有一个老和尚坐在蒲团上闭目养神。\n"
);
        set("exits", ([ /* sizeof() == 4 */
  "southwest" : "/d/shaolin/guangchang3",
]));
        set("outdoors", "shaolin");
        set("objects", ([ /* sizeof() == 2 */
              __DIR__"npc/xuan-tong" : 1,
]));

        setup();
        replace_program(ROOM);
}

