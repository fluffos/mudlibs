// muwu1.c
// by shilling 97.2

inherit ROOM;

void create()
{
	set("short", "木屋");
	set("long",
"这里是一间不大的木头屋子，但是麻雀虽小，五藏俱全，屋子中\n"
"只是疏疏落落地摆着几件家具，所以也不显得怎么小。\n"
);
	set("exits", ([
		"north" : __DIR__"xiaodao5",
	]));
 	set("objects", ([
		__DIR__"npc/kanggl": 1,
	]));
//	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
