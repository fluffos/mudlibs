// Room: /d/xiaoyao/shulin3.c

inherit ROOM;

void create()
{
	set("short", "树林");
	set("long",
"这是一片茂密的树林。很多棵几十丈高的大树聚在一块，象一把\n"
"把琼天大伞，连日月都被遮蔽得暗然无光。树林中还不时可以听\n"
"到大自然发出的声响。\n"
);
	set("objects", ([ ]));
	set("outdoors", "xiaoyao");
	set("exits", ([ /* sizeof() == 4 */
		"south" : __DIR__"shulin3",
		"north" : __DIR__"shulin3",
		"east"  : __DIR__"shulin2",
                "west"  : "/d/xiaoyao/wdroad4",
	]));
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
