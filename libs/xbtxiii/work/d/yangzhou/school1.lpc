#pragma save_binary
// Room: /d/snow/school2.c

#include <room.h>

inherit ROOM;

void create() {
  set("coor", ({ -20, -830, 0 }));
  set("short", "福威镖局教练场");
  set("long",
    "你现在正站在一个宽敞的教练场中，地上铺著黄色的细砂，许多\n"
    "人正在这里努力地操练著，北边是一间高大的兵器厅，往南则是\n"
    "林震南镖头们休息的大厅。\n"
  );
  set("exits", ([ /* sizeof() == 3 */
    "west": "/d/yangzhou/biaoju",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/trainee": 2,
  ]));
  set("outdoors", "yangzhou");
  set("clean_up", 1);

  setup();
  replace_program(ROOM);
}
