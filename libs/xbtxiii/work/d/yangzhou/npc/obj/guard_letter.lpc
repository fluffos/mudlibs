// 扬州城官兵的信

inherit ITEM;

void create() {
  set_name("家信", ({ "letter" }));
  set_weight(600);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "封");
    set("long", "一封家信。是一个官兵写给远在宁远妻子的。\n");
    set("value", 100);
    set("扬州城官兵的信", 1);
  }
  setup();

}
