#pragma save_binary
// Room: /d/huashan/hroad2.c
#include <roadbusy.h>

inherit ROOM;

void create()
{
      set("coor",({-750,2500,0}));
	set("short","山路");
      set("long",
"你现在正走在一条崎岖的山路上，路险难行,从这里往西看去,隐\n"
"隐可以看见前面的高峰被白云所环绕，让你不自觉地要加快脚步，\n"
"恨不能马上就到达五岳岳之一的华山了，著名的华山派就位于华\n"
"山。\n"
);
      set("exits", ([ /* sizeof() == 2 */
   "east" : __DIR__"hroad1",
   "west" : __DIR__"hroad4",
]));      
      set("no_clean_up",0);

	      setup();
//        replace_program(ROOM);
}
