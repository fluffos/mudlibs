#pragma save_binary
// Room: /d/huashan/midong.c

#include <room.h>
inherit ROOM;

void create()
{
       set("coor",({-400,1600,1000}));
	set("short","秘洞");
       set("long",
"洞里漆黑一团，透过从洞口远远透过来的一丝亮光你发现四周的\n"
"洞壁上似乎刻着一些小人，但怎么也看不清楚。\n"
);

    set("exits", ([
        "south" :     __DIR__"houshan",
    ]));
    setup();
}

