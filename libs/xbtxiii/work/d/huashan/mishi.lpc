#pragma save_binary
// siguoya.c
// By Hop, 97.04.30
// Program by L.G.

inherit ROOM;

void create() {
  set("short", "华山密室");
  set("long",
    "这里是一间窄小的密室,好象很久没人来这呆过了。密室的墙壁\n"
    "上画满了围棋棋谱。好象很深奥，你一点也看不懂。另一面墙上\n"
    "刻了许多音律。都是些失传了的谱子。其中就有著名的广陵曲。\n"
  );

  setup();
}

void pipe_notify() {
  object ob;

  message("vision",
    "你听到一阵轧轧的轮盘绞动声，一块大石慢慢地被移开了。\n",
    this_object());
  set("exits/west", __DIR__ "yanfeng");
  if (ob = find_object(__DIR__ "yanfeng"))
    ob->set("exits/east", __FILE__);
  remove_call_out("close");
  call_out("close", 3, this_object());
}


void close(object room) {
  message("vision", "大石突然掉了下来，将洞口封了个严严实实。\n", room);
  room->delete("exits/west");
}
