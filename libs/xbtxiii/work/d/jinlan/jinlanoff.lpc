//   This is a room of jinlanoffice d/jinlan/jinlanoff.c

#include <ansi.h>
#include <room.h>
#include <login.h>
#define BOARD_CAPACITY query("capacity")
inherit ROOM;
inherit F_SAVE;
//int do_touch(string);
void create() {
  set("coor", ({ -70, 4830, 10 }));
  set("short", "会长办公室");
  set("long",
    "这里是金兰会会长的办公室,一般人是没法进来的,你为自己能站\n"
    "在这里感到很荣幸.办公室的中间是一张大写字桌,一张真皮沙发,\n"
    "桌上放满了各种各样的文件,最上面有一本入会申请表(Table),\n"
    "你可以在上面填写(filltable)入会申请.\n"
  );
  //set("objects",([__DIR__"npc/wushi":4]));
  set("exits", ([ /* sizeof() ==  2 */
    "north": __DIR__ "jinlanjie",
    //"south":__DIR__"jinlandian",
  ]));
  //     create_door("north","红漆大门","south",DOOR_CLOSED);
  set("valid_startroom", 1);
  set("no_fight", 1);
  setup();
}
