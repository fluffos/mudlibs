// jinlan_b.c

inherit  "/d/jinlan/bboard.lpc";

void create()
{
	set_name("金兰会入会申请表", ({"table" }) );
	set("location", "/d/jinlan/jinlanoff");
	set("board_id", "table");
	set("long",	"这是供愿加入本会的义士填写的申请表.\n" );
	setup();
	set("capacity", 30);
	replace_program("/d/jinlan/bboard.lpc");
}
