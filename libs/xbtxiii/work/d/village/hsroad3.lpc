#pragma save_binary
// wdroad3.c
// by Xiang

inherit ROOM;

void create()
{
	set("short", "黄土路");
	set("long",
"一条坑坑洼洼的土路，弯弯曲曲的通向山里。\n"
);
        set("outdoors", "wudang");

	set("exits", ([
		"southeast" : __DIR__"hsroad2",
		"north" : __DIR__"sexit",
	]));

	setup();
	replace_program(ROOM);
}

