// Room: /d/village/road5.c

inherit ROOM;

void create()
{
	set("coor",({-450,4000,0}));
	set("short", "青石小路");
	set("long",
"青石小路在这里分成两条，一条继续向西延伸，另一条伸向南面，\n"
"北面是一个鲜花盛开的小花园，闻到阵阵花香，你不仅心旷神怡。\n"
"西面遥远处是连绵的群山，东南是一片面积不小的湖泊。\n"
);
	set("exits", ([ /* sizeof() == 4 */
  "west" : __DIR__"road6",
  "north" : __DIR__"littlegarden",
  "southdown" : __DIR__"road7",
  "east" : __DIR__"road4",
]));

        set("outdoors","village");
	setup();
	replace_program(ROOM);
}
