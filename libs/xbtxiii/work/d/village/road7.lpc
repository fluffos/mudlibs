// Room: /d/village/road7.c

inherit ROOM;

void create()
{
	set("coor",({-450,3950,-10}));
	set("short", "青石小路");
	set("long",
"小路的两边种了一排垂柳，西边玉螺湖上的阵阵雾气透过柳枝飘\n"
"散过来。你的东边是一大片绿油油的瓜地，一条大沟横在小路和\n"
"瓜地之间，你可以走到东南向的瓜棚。\n"
);
	set("exits", ([ /* sizeof() == 4 */
  "southeast" : __DIR__"melonguard",
  "south" : __DIR__"road8",
  "west" : __DIR__"lakeside",
  "northup" : __DIR__"road5",
]));
        set("outdoors","village");

	setup();
	replace_program(ROOM);
}
