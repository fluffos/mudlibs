#pragma save_binary
// Room: /d/village/bighouse2.c

inherit ROOM;

void create()
{
        set("coor",({-780,2750,0}));
	set("short", "内室");
	set("long",
"这里是一间寝室，屋子中间有一张大床，床上的被褥居然是绸缎\n"
"的，看来这个土财主对自己并不是十分刻薄。屋里摆着几只木箱\n"
"子，墙角里有一只大柜子(cabinet)。\n"
);
        set("exits", ([ /* sizeof() == 1 */
  "west" :"/d/village/bighouse1",
]));
        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}
 
