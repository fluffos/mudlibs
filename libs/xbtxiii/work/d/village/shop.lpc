#pragma save_binary
// Room: /d/village/shop.c

inherit ROOM;

void create() {
  set("coor", ({ -750, 2810, 0 }));
  set("short", "杂货店");
  set("long",
    "这是一间小小的杂货店，经营些油、盐、酱、醋及日用百货，村\n"
    "子里的人多数是很穷，除了生活必需品，这里很少贩卖其它物品。\n"
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "eroad3",
  ]));
  //       set("no_clean_up", 0);

  set("objects", ([
    __DIR__ "npc/xie": 1
  ]));

  setup();
  replace_program(ROOM);
}
