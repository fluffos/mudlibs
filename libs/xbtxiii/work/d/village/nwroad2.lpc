#pragma save_binary
// Room: /d/village/nwroad2.c

inherit ROOM;

void create() {
  set("coor", ({ -810, 2810, 0 }));
  set("short", "碎石路");
  set("long",
    "这是一个宁静的小村子，稀稀落落的分布着几十间土坯泥房，看\n"
    "来村中人家不多，而且大都生活很艰辛。东南边就是村子中心的\n"
    "打谷场了，西南是间民房。\n"
  );
  set("exits", ([ /* sizeof() == 3 */
    "northwest": __DIR__ "nwroad1",
    "southeast": __DIR__ "square",
    "southwest": __DIR__ "nwhouse",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "xxx");

  setup();
  replace_program(ROOM);
}
