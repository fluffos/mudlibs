#pragma save_binary
// Room: /d/village/ehouse1.c

inherit ROOM;

void create() {
  set("coor", ({ -830, 2830, 0 }));
  set("short", "民宅");
  set("long",
    "这是一间十分简陋，四壁漏风的小房子，屋子里连一张象样的床\n"
    "都没有，只有一面门板上面铺着些干草。这屋子的主人一定是贫\n"
    "困潦倒，穷的叮当响。\n"
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "wexit",
  ]));
  set("no_clean_up", 0);
  set("objects", ([
    __DIR__ "npc/poorman": 1 ]));

  setup();
}
