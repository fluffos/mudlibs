#pragma save_binary
// Room: /d/village/shouse.c

inherit ROOM;

void create()
{
        set("coor",({-750,2650,0}));
	set("short", "小土房");
	set("long",
"这是一间用土坯搭成的房屋，屋里只有几件残破的家俱。在靠门\n"
"口的墙角有一只空空的笼子，大概曾经用来养过鸡、鸭之类的家\n"
"禽。\n"
);
        set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"sroad2",
]));
        set("no_clean_up", 0);
        set("objects", ([
                __DIR__"npc/oldwoman": 1 ]) );

        setup();
        replace_program(ROOM);
}
 
