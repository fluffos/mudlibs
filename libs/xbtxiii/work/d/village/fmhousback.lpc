#pragma save_binary
// Room: /d/village/fmhousback.c

inherit ROOM;

void create()
{
	set("short", "农舍后院");
	set("long",
"在后院的左侧有个猪圈，几只小白猪在里面胡吃海塞，右面是一\n"
"排鸡笼，两个红皮鸡蛋落在鸡笼后边的槽里。这村子的妇女经常\n"
"把自家产的鸡蛋拿到京城去卖，去扯些花布或作件衣服。\n"
);
	set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"farmhouse1",
]));
	//set("no_clean_up", 0);
	set("objects", ([
		"/d/village/npc/woman1":1,	
		]) );
	setup();
	//replace_program(ROOM);
}
