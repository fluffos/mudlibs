#pragma save_binary
// Room: /d/village/field.c

inherit ROOM;

void create()
{
	set("coor",({-500,3850,-10}));
	set("short", "稻田");
	set("long",
"这是一片绿油油的稻田，稻子的长势非常喜人，因为北面就是玉\n"
"螺湖，所以稻田的灌溉很方便。从这儿向西能看到忘情谷那郁郁\n"
"葱葱的山岭，东面能看到几间高大房屋高出柳树部分的屋檐。\n"
);
	set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"road9",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
