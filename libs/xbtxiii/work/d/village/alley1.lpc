#pragma save_binary
// Room: /d/village/alley1.c

inherit ROOM;

void create()
{
        set("coor",({-850,2700,0}));
	set("short", "小巷");
        set("long",
"这是一条黑暗、肮脏的小巷，空气中仿佛飘扬着一股罪恶的气息，\n"
"西北边通向小巷深处，正人君子是不会往那边走去的。往东走就\n"
"会回到村中大路了。\n"
);
        set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"sroad3",
  "northwest" : __DIR__"alley2",
]));
        set("no_clean_up", 0);
        set("outdoors", "xxx");

        setup();
        replace_program(ROOM);
}


