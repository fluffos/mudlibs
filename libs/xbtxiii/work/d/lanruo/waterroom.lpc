// Room: /d/lanruo/waterroom.c

inherit ROOM;

void create() {
  set("short", "水中居");
  set("long",
    "这是一间建设在湖中的小居，四周环境清幽，景色迷人。淡淡的\n"
    "花香从不远处瓢来。往北是小居通向外面的小木桥。\n"
  );
  set("exits", ([ /* sizeof() == 1 */
    //       "north" : "/d/lanruo/bridage",

    "north": __DIR__ "bridge",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/girl": 1,
    __DIR__ "npc/young": 1,
  ]));

  set("no_clean_up", 0);

  setup();
}
