// Room: /d/lanruo/herohouse.c

inherit ROOM;

void create() {
  set("short", "正气山庄");
  set("long",
    "眼前是一个写着“正气山庄”的牌匾，但由于日久失修，字迹已\n"
    "经模糊不清了。传说这里有一个老道士住在这里的，不过他性情\n"
    "怪僻，一般人都不会去若他，有些人给他取了个绰号“森山老道\n"
    "”，听说那个老道士对这个名还挺满意的。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "herohouse2",
    "southeast": __DIR__ "lroad3",
    "west": __DIR__ "bury_area",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
