// Room: /d/lanruo/herohouse.c
#include <room.h>

inherit ROOM;

void create() {
  set("short", "内堂");
  set("long",
    "这是一个内堂，四处灰尘乱飞。看来有好久没人住了。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "southeast": __DIR__ "herohouse2",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/daoshi": 1,
    "/d/gameroom/obj/table": 1,
  ]));
  set("no_clean_up", 0);
  setup();
  create_door("southeast", "木门", "northwest", DOOR_CLOSED);
}
