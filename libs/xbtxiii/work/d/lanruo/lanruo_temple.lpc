// Room: /d/snow/lanruo_temple.c

inherit ROOM;

void create() {
  set("short", "空房间");
  set("long",
    "这是一间什麽也没有的空房间。\n"
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "lanruo_gate",
  ]));
  set("no_clean_up", 0);

  setup();
}
