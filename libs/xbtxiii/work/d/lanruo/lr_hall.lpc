// Room: /d/lanruo/lr_hall.c

inherit ROOM;

void create() {
  set("short", "大雄宝殿");
  set("long",
    "这里是蓝若寺的大雄宝殿，里面只有一个老和尚在那里敲着木鱼，\n"
    "而且只有一尊佛像（foxiang），前面有一个小香炉，香炉中只\n"
    "有三株青香。\n"
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "lr_square",
  ]));
  set("item_desc", ([
    "foxiang": "一尊佛像,表面上涂了金漆,但已经脱落的七七八八了。\n",
  ]));
  set("no_clean_up", 0);
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/bonze": 1,
  ]));
  setup();
  replace_program(ROOM);
}
