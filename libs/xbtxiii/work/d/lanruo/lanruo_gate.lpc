// Room: /d/lanruo/lanruo_gate.c

inherit ROOM;

void create()
{
	set("short", "山门");
	set("long",
"山门由一块巨大的白石刻成，由于年代久远，山石的颜色暗淡，\n"
"石质剥落，绿色的苔藓密布其上，丛生的杂草随风摇摆。在草丛\n"
"中有一块倾倒的石碑(sign)。一个告示(warning)贴在路边的树\n"
"上。\n"
);
	set("item_desc", ([ /* sizeof() == 2 */
  "warning" : "[1;31m	“猛鬼出没，生人毋近！！
			雪亭县令印”
[2;37;0m",
  "sign" : "石碑上写着：      “兰若寺”
",
]));
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"lroad0",
  "east" : "/d/ny/bank",
]));
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/tcorpse" : 2,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
