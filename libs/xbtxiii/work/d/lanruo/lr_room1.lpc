// Room: /d/lanruo/lr_room1.c

inherit ROOM;

void create() {
  set("short", "北厢房");
  set("long",
    "这是一间破旧不堪的空房间，门窗不时发出吱呀。。吱呀。。。\n"
    "的声音，地上扑满了尘土、树叶，你渐渐觉得有点不安，好象有\n"
    "人在什么地方监视你似的。\n"
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "lr_square",
  ]));
  set("no_clean_up", 0);
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/nvgui": 1,
  ]));
  setup();
  replace_program(ROOM);
}
