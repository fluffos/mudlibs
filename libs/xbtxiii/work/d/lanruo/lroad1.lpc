// Room: /d/lanruo/lroad1.c

inherit ROOM;

void create() {
  set("short", "鬼打墙");
  set("long",
    "你正置身与一片浓密的树林之中，由于这里常年得不到日照，瘴\n"
    "气弥漫，使你什么也看不清。空气中偶尔传来一阵腐臭味。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "northeast": __DIR__ "lroad0",
    "south": __DIR__ "lroad4",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
