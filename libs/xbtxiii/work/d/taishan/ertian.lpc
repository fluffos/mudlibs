// Room: /d/taishan/.c
// Date: CSWORD 96/03/25
#include <roadbusy.h>

inherit ROOM;

void create()
{
	set("short", "二天门");
	set("long",
"这里又叫「中天门」。一般的登山者来到这里都已累极，但是\n"
"这里才是泰山的一半啊！面对前面险峻的十八盘，很多人都不禁\n"
"犹豫起来。\n"
);
	set("exits", ([
		"northup" : __DIR__"wudafu",
		"southdown" : __DIR__"huima",
	]));
//	set("no_clean_up", 0);
	set("outdoors", "taishan");
	setup();
//        replace_program(ROOM);
}

