// Room: /d/taishan/road1

inherit ROOM;

void create()
{
	set("short", "山路");
	set("long",
"一条清幽的山路。路两旁是高大的古木。从远处不时传来几声不\n"
"知是什么鸟类的鸣叫声。更加衬托出了宁静的气氛。\n"
);
	set("exits", ([
		"east" : __DIR__"road6",
		"west" : __DIR__"road4",
	]));
	set("outdoors", "taishan");
	setup();
	replace_program(ROOM);
}

