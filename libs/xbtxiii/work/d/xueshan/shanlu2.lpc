#pragma save_binary
//      shanlu2.c 山路
//      Designed by secret (秘密)
//      97/04/09
#include <roadbusy.h>

inherit ROOM;

void create() {
  set("coor", ({ -3600, -970, 100 }));
  set("short", "山路");
  set("long",
    "这是一条上山的小路。\n"
  );

  set("outdoors", "xueshan");

  set("exits", ([
    "northup": __DIR__ "shanmen",
    "eastdown": __DIR__ "shanlu1",
  ]));

  setup();
  //        replace_program(ROOM);
}
