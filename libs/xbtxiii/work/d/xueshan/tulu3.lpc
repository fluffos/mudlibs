#pragma save_binary
//      tulu3.c 土路
//      Designed by secret (秘密)
//      97/04/08
#include <roadbusy.h>

inherit ROOM;

void create() {
  set("coor", ({ -3700, -600, 0 }));
  set("short", "土路");
  set("long",
    "这是一条不起眼的土路。北边是著名的圣湖。\n"
  );

  set("outdoors", "xueshan");

  set("exits", ([
    "north": __DIR__ "tulu2",
    "south": "/d/secret/secret1/shenghu",
  ]));

  setup();
  //        replace_program(ROOM);
}
