#pragma save_binary
// gate.c

#include <ansi.h>

inherit ROOM;

void create() {
  set("short", HIW "第十八层地狱" NOR);
  set("long",
    "这里就是著名的第十八层地狱，凡是能够来到这里的人。除了巫\n"
    "师，就是那些已经活腻了的，永世不得超生的人们。\n"
  );
  set("objects", ([
    __DIR__ "npc/horse": 1
  ]));
  setup();

  replace_program(ROOM);
}
