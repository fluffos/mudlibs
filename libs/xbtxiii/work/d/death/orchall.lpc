#pragma save_binary
// gate.c

#include <ansi.h>

inherit ROOM;

void create()
{
       set("short", CYN "魔族大堂" NOR);
        set("long",
"这里充满着邪气，周围的环境发散出强烈的杀气，让人心惊胆战。\n"
"这里是魔族首脑的居住地。\n"
);
        set("exits", ([
              "north" : "/d/death/gate",
        ]) );
        set("objects", ([
//            "/daemon/class/orc/orc":1
        ]) );
        setup();
        replace_program(ROOM);
}
 
