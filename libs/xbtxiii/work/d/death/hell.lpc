#pragma save_binary
// Room: /d/death/hell.c

inherit ROOM;

void create() {
  add_light(this_object(), 1);
  set("short", "地狱入口");
  set("long",
    "你走到了十八层地狱的入口了，一阵阵哭号声从下面传上来。南\n"
    "面则是漆黑一片的冥海。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": "/d/death/temple",
    "down": __DIR__ "diaojin",
    //    "west" : __DIR__"suicide_room1",
  ]));
  set("no_magic", 1);

  setup();
}
