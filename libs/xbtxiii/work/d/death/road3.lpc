#pragma save_binary
// Room: /d/death/road3.c

inherit ROOM;

void create()
{
	set("short", "前方的路");
	set("long",
"在迷雾之中，你什么也看不清.......\n"
);
	set("exits", ([ /* sizeof() == 1 */
//  "south" : "/d/death/road2",
]));

	setup();
	replace_program(ROOM);
}
