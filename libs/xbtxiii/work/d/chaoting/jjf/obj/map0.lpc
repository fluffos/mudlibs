inherit ITEM;

int seteuid();
void create() {
  set_name("全国军事详分图", ({ "map" }));
  set_weight(100);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "份");

    set("long", "这是一张全国军事详分图.乃朝廷之至宝，价值连城！\n");

    set("value", 100000);
    set("material", "copper");
  }
}
