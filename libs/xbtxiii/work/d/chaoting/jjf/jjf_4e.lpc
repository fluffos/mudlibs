#pragma save_binary
// Room: /open/tony/jjf/jjf_2.c
inherit ROOM;

void create() {
  set("coor", ({ 110, 2940, 0 }));
  set("short", "东厢房");
  set("long", @LONG
此处是将军府的东厢，这儿是小承志住的地方，很有桌上有几卷书。
常常有佣人打扫，房间显得格外干净。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "jjf_4",
  ]));
  set("objects", ([ /* sizeof() == 0 */

    __DIR__ "jin_wei": 0,
  ]));
  set("no_clean_up", 0);

  set_temp("light", 1);
  setup();
}
