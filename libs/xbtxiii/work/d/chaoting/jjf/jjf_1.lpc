#pragma save_binary
// Room: /open/tony/jjf/jjf_1.c
inherit ROOM;

void create() {
  set("coor", ({ 100, 2910, 0 }));
  set("short", "将军府前厅");
  set("long", @LONG
这里将军府的前厅，正对厅门的是一块匾上书四个大字"世代公侯"，
厅的后部有一块极大的屏风，屏风后面直通，将军府的正厅。往南则可
以看到将军府那红色的大门。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "jjf_0",
    "north": __DIR__ "jjf_2",
  ]));
  set("objects", ([ /* sizeof() == 1 */

    __DIR__ "npc/hu_wei": 4,
  ]));
  set("no_clean_up", 0);

  set_temp("light", 1);
  setup();
}
