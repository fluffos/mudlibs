#pragma save_binary
// Room: /open/tony/jjf/jjf_2.c
inherit ROOM;

void create()
{
        set("coor",({100,2920,0}));
	set("short", "将军府正厅");
        set("long", @LONG
这里将军府的正厅，是辅国大将军袁崇焕处理日常军机，行政事务
之处，所以戒备非常森严，再往后就是将军府的花厅厢房内院。东边和
西边,各通向一个房间.往南则是前厅。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "south" : __DIR__"jjf_1",
"north" : __DIR__"jjf_3",
]));
     set("objects", ([ /* sizeof() == 2 */
                       
             __DIR__"npc/jiang_jun":  1,
       __DIR__"npc/jin_wei":  4,
]));

set_temp("light",1);
        setup();
        replace_program(ROOM);
}

