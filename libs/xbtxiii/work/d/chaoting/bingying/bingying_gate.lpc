#pragma save_binary
// Room: /u/cloud/dragonhill/nroad.c

inherit ROOM;

void create() {
  set("coor", ({ 0, 4500, 10 }));
  set("short", "辽东兵营寨门");
  set("long", @LONG
这里是辽东官兵的大营,朝廷在此驻扎了十万大军守卫边疆，镇
压土匪，以安定整个辽东，从此往南，你可以看到一队队官兵正在
抄练，除非你有兵符，否则无法进入兵营。往北是通向宁远的官道。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "north": "/u/cloud/dragonhill/nroad",
    "south": __DIR__ "bingying",
  ]));
  set("objects", ([
    __DIR__ "npc/zhiqin_bing": 1,
    __DIR__ "npc/bing2": 1,
  ]));
  set("outdoors", "dragonhill");

  setup();
}
