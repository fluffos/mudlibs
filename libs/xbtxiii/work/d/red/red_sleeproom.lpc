// inn.c

#include <room.h>

inherit ROOM;

void create() {
  set("short", "卧房");
  set("long",
    "你现在正站在舵主的卧房内,虽说是江湖上数一数二的会馆，这\n"
    "里的摆设还是很简单!\n"
  );

  set("exits", ([
    "south": "/d/red/red_hall" ]));

  setup();

}
