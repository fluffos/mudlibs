#pragma save_binary
// Room: /d/snow/eroad3.c

inherit ROOM;

void create()
{
        set("coor",({0,4600,10}));
	set("short", "山路");
        set("long",
"这是一条说宽不宽，说窄倒也不窄的山路，路面用几块生满青苔\n"
"的大石铺成，北面是一段黄土小径，从这里可以通向宁远城，南\n"
"面通向兵营，那里可不是一般人可以去的地方，这里你已经隐隐\n"
"约约的听到士兵们在操练的叫喊声了。\n"
);
        set("exits", ([ /* sizeof() == 2 */
      "north" : __DIR__"nroad3",
      "south" : "/u/cloud/dragonhill/nroad",
]));
	set("no_clean_up",0);
        set("outdoors", "snow");
//	set("objects",(["/d/snow/npc/txtrainee": 3]));
        setup();
        replace_program(ROOM);
}
