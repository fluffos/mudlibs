#pragma save_binary
// Room: /d/snow/nroad1.c

inherit ROOM;

void create() {
  set("coor", ({ 0, 4800, 10 }));
  set("short", "广场前的空地");
  set("long",
    "北面是一片树林，树林里面是恶霸镇关西的酒店，东面是一条石\n"
    "板路，向西可以通向嘉裕关，街上人来人往非常热闹。\n"
  );
  set("outdoors", "snow");
  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "nroad2",
    "south": __DIR__ "square",
    //  "west" : __DIR__"nroad3",
    //  "east" : __DIR__"nroad7",
  ]));
  set("no_clean_up", 0);

  set("objects", ([ __DIR__ "npc/playboy": 1, ]));
  setup();
  //    replace_program(ROOM)
  //    replace_program(ROOM);
}
//int valid_leave(object me, string dir)
//        if( dir=="south" && !wizardp(me) )
//                return notify_fail("那里施工中，请改道。\n");
//        return ::valid_leave(me, dir);
//}
