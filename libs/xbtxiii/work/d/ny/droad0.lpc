#pragma save_binary
// Room: /d/snow/droad0.c
#include <roadbusy.h>

inherit ROOM;

void create() {
  set("coor", ({ 0, 5000, 10 }));
  set("short", "山路");
  set("long",
    "这是一条杂草从生的小路，已多年没有人从这里走过，路旁树上\n"
    "栖息着几只乌鸦，不时传来凄惨又令人毛骨竦然的叫声，向前望\n"
    "去隐约可见一座高山。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "happyinn1",
    //   "north" : "/d/xiangyang/southgate2",
    "westup": __DIR__ "droad1",
  ]));
  set("outdoors", "snow");
  setup();
  //        replace_program(ROOM);
}
