#pragma save_binary

inherit ROOM;

void create()
{
        set("coor",({-70,4750,10}));
	set("short", "观景台");
	set("long",
"这是一间不大的亭院平台，台前有张小方桌,中央有一棵千年榕\n"
"树，轻风吹过，传来阵阵沁人心脾的花香。在烦嚣热闹的宁远城\n"
"中,竟然有这么个地方给游人们停歇的地方,一些老者们都爱到这\n"
"来,品茶谈天.\n"
);
	set("exits", ([ /* sizeof() == 2 */
//                "west" : "/d/snow/mstreet4",
  "north" : __DIR__"mstreet3",
               /* "east" : "/d/snow/bank",*/
	]));
    set("no_fight", 1);
        set("objects", ([
//               "/d/chaoting/npc/japanese" : 3,  
        ]));

	setup();
	replace_program(ROOM);
}
