#pragma save_binary
// Room: /d/green/fortgate.c
#include <room.h>
#include "/d/green/data.h"
inherit ROOM;
inherit F_SAVE;
inherit F_AUTOLOAD;
mapping path1;
nosave string aaa="file"; 
void create()
{  int ii;
	set("short", "寨门");
	set("long", @LONG
这是山寨的大门，门口有一个凶神恶煞的大汉。寨门紧闭，隐约听见寨子里人
喊马嘶，一片喧哗。
LONG
	);
//        write(aaa+ii);
	set("exits", ([ /* sizeof() == 1 */
  "west" : "/d/green/"+"fortgate",
]));
    set("objects", ([  
  "/d/green/npc/guard" : 1,
//  __DIR__"npc/lei" : 1,
]));
	set("outdoors","green");
	setup();
}

void init()
{
  string b;
  a=random(4);
  b=aaa+a+NATURE_D->game_time();  
//  write(b);
}

string query_save_file(){ return "try";}

void reset()
{
	object room;
        string filename="data";
        mapping path;
        string line;
	//seteuid(getuid());
        path1=(["001w":"003"]);
        path=(["001e":"002"]);
        if(path1==0) path1=path;
        else path1+=path;
//    save_object("/data/hello",0);
//        write("success?");
//        restore_object("/data/hello",0);
        path=path1;
        line=""+path["001e"];
//        write(line);
	::reset();
}
