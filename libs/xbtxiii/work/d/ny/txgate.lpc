#pragma save_binary
// Room: /d/snow/school1.c

#include <room.h>

inherit ROOM;

void create() {
  set("coor", ({ -200, 4770, 10 }));
  set("short", "水烟阁后门");
  set("long",
    "你正站在一间大宅院的入口，两只巨大的石狮镇守在大门的两侧，\n"
    "一阵阵吆喝与刀剑碰撞的声音从院子中传来，通过大门往东可以\n"
    "望见许多身穿灰衣的汉子正在操练。\n"
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": "/d/ny/txyard",
    "northeast": __DIR__ "nroad7",
  ]));
  set("outdoors", "snow");
  set("objects", ([
    "/d/snow/npc/txtrainee": 2,
    __DIR__ "npc/txtrainer": 1 ]));
  create_door("south", "红漆大门", "north", DOOR_CLOSED);

  setup();
}
