#pragma save_binary
// Room: /d/snow/school2.c

#include <room.h>

inherit ROOM;

void create() {
  set("coor", ({ 0, 4950, 10 }));
  set("short", "快活林");
  set("long",
    "你正站在一个空场中，空场西边上有一个小棚，里面摆着桌椅板\n"
    "凳，许多人正在吃饭，小棚外竖着一个招牌，上写：“快活林”\n"
    "路边树下有些打手和家丁休息，往南则是林间小路，往北就离开\n"
    "雪亭镇了。\n"
  );
  set("exits", ([ /* sizeof() = 3 */
    "north": __DIR__ "droad0",
    "west": __DIR__ "happyinn0",
    "south": __DIR__ "crossroad",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    "/u/cloud/npc/dashou": 1,
    __DIR__ "npc/room_waiter": 1,
  ]));
  set("outdoors", "snow");
  setup();
}
