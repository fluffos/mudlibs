// black_hole.c

#include <room.h>

inherit SPACE;

void create()
{
	if( clonep())
		set_default_object(__FILE__);
	else {
		set("short", "黑洞");
		set("long", @LONG
黑洞是質量中等以上的恆星最後的歸宿.
LONG
		);
		set("habitability", 0 );
	}

	setup();
}
