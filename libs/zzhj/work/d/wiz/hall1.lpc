// Room: /d/wiz/hall1.c

inherit ROOM;

void create()
{
	set("short", "FF 管理中心二樓----巫師交誼廳");
	set("long", @LONG
你看到一個簡樸裝潢的大廳, 四周柔和的色調讓人放鬆不少心情
. 在中央有一張大的會議桌, 桌上有著先進的立體投影裝置. 旁邊有
沙發和茶几, 也有供應飲料和茶水. 
    樓下是巫師會客室, 樓上是工作進度簡報室.
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"up" : "/d/wiz/jobroom",
		"down" : "/d/wiz/guestroom",
]));

	set("objects", ([
		"/obj/board/wizard_b" : 1,
	]) );

	set("no_clean_up", 0);

	setup();
}
