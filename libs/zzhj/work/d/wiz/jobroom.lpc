// Room: /d/wiz/jobroom.c

inherit ROOM;

void create()
{
	set("short", "FF 管理中心----工作進度簡報室");
	set("long", @LONG
這裡是巫師們報告自己工作進度的簡報室, 如果你手上有工作, 
最好常常到這裡報告工作進度, 以方便分派各位巫師負責的工作.
    樓下是巫師交誼廳.
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
		"down" : "/d/wiz/hall1",
	]) );

	set("objects", ([
		"/obj/board/wizard_j" : 1,
	]) );

	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
