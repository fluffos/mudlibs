// Room: /d/maniac/central_park_tower_3.c

inherit ROOM;

void create()
{
	set("short", "鐘塔頂端");
	set("long", @LONG
這裡是鐘塔的頂端, 只有少許的光線從採光用的小孔裡照進來
. 在這個狹小的空間裡, 擠滿了複雜的巨大齒輪、軸承和連桿, 都
已經鏽蝕得非常厲害, 難怪鐘塔上的大鐘連動一下的機會也沒有了.
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
		"down" : __DIR__"central_park_tower_2",
	]));
	set("no_clean_up", 0);
	set("mov_cost", 5);

	set("objects", ([
		__DIR__"npc/park_keeper" : 1
	]));

	setup();
	replace_program(ROOM);
}
