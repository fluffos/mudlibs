// Room: /d/maniac/central_park_tower_2.c

inherit ROOM;

void create()
{
	set("short", "鐘塔內部");
	set("long", @LONG
這裡是一段介於鐘塔頂端和鐘塔底部的迴旋石梯上. 你發現地上
的灰塵上有明顯的足跡 (foot_print), 似乎有人剛才來過這裡.
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"up" : __DIR__"central_park_tower_3",
		"down" : __DIR__"central_park_tower_1",
]));
	set("mov_cost", 20 );
	set("detail/foot_print",
"階梯上的足跡並不清楚, 在每一階石梯上都有幾個模糊不清的鞋印.
但是最清楚的鞋印每階只有一個, 表示最近來過的人只有單獨一位.\n");

	setup();
	replace_program(ROOM);
}
