// Room: /obj/f_road_3.c

inherit ROOM;

void create()
{
	set("short", "自由之路");
	set("long", @LONG
    道路往南北方向延伸, 北邊是橫跨梅尼亞克河的馬克菲爾大橋 
 (Mackphil Bridge). 在東南方是梅尼亞克市的中央廣場公園, 而
南方道路的西側可以看到許多商店的招牌.
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"south": __DIR__"f_road_1",
		"north": __DIR__"mackphil_bridge_1.lpc",
	]));
	set("no_clean_up", 0 );
	set("mov_cost", 20 );
	set("outdoors", 1 );

	setup();
}
