// Room: /d/maniac/central_park_5.c

inherit ROOM;

void create()
{
	set("short", "中央公園");
	set("long", @LONG
    這裡有一大堆灌木和步道, 看來以前可能是花園. 但是應該有花
的地方都已經雜草叢生. 看來公園的管理員並沒有細心整理此處.
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"north" : __DIR__"central_park_6",
		"west" : __DIR__"central_park_4",
]));

	set("mov_cost", 50 );
	set("outdoors", 1);
	setup();
	replace_program(ROOM);
}
