// Room: /d/maniac/central_park_6.c

inherit ROOM;

void create()
{
	set("short", "中央公園");
	set("long", @LONG
    走道旁濃密的樹蔭把天空遮敝了. 這兒常是情侶們幽會的場所.
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"west" : __DIR__"central_park_1",
		"south" : __DIR__"central_park_5",
		"north" : __DIR__"park_cabin_1",
	]));

	set("mov_cost", 30 );
	set("outdoors", 1);
	setup();
}
