// Room: /d/maniac/mackphil_bridge_1.c

inherit ROOM;

void create()
{
	set("short", "馬克菲爾大橋");
	set("long", @LONG
    這裡是馬克菲爾大橋的南端, 路面順著堤防的高度緩緩升起.
在你的身旁有一張紅色的告示牌 (sign).
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
		"south": __DIR__"f_road_3",
	]));
	set("no_clean_up", 0 );
	set("mov_cost", 30 );
	set("outdoors", 1 );

	set("detail/sign",
"警告 !! 由於馬克菲爾大橋已達使用壽命年限. 為免意外, 即日起封閉
馬克菲爾大橋. 請市民利用各處的渡船碼頭渡河.

                            梅尼亞克市政府工程事務局  2295.1.1.\n"
	);

	setup();
}
