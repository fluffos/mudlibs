// Room: /d/maniac/f_road_2.c

inherit ROOM;

void create()
{
	set("short", "自由之路");
	set("long", @LONG
    道路往南北方向延伸, 在東方的一片綠茵是中央廣場公園. 西方
是梅尼亞克市著名的 "黃玉酒館"  (Topaz Pub). 你可以聽到從酒館
中傳出的音樂和喧囂聲. 往北方不遠處有一座跨河大橋.
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"north"	: __DIR__"f_road_1",
		"west"	: __DIR__"topaz_pub",
]));
	set("no_clean_up", 0);
	set("mov_cost", 20 );
	set("outdoors", 1 );

	setup();
	replace_program(ROOM);
}
