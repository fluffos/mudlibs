// Room: /d/test/test_space.c

inherit ROOM;

void create()
{
	set("short", "測試用宇宙環境");
	set("long", @LONG
這裡是巫師用來測試用的宇宙空間.
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
		"earth" : "/d/maniac/park_1",
]));
	set("no_clean_up", 0);
	set("terrain", "space");
	setup();
}
