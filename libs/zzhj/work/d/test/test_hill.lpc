// Room: /d/test/test_hill.c

inherit ROOM;

void create()
{
	set("short", "測試用丘陵環境");
	set("long", @LONG
這裡是巫師用來測試用的丘陵空間.
丘陵是比較崎嶇的地形, 在此走動要耗費較多的精力..
LONG
	);
	set("exits", ([ /* sizeof() == 7 */
		"out"		: "/d/death/transporter",
		"space"		: __DIR__"test_space",
		"atmosphere"	: __DIR__"test_atmosphere",
		"ocean"		: __DIR__"test_ocean",
		"river"		: __DIR__"test_river",
		"mountain"	: __DIR__"test_mountain",
		"hill"		: __DIR__"test_hill",
]));
	set("no_clean_up", 0);
	set("terrain", "hill");
	setup();
}
