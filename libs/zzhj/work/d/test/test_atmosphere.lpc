// Room: /d/test/test_atmosphere.c

inherit ROOM;

void create()
{
	set("short", "測試用大氣圈環境");
	set("long", @LONG
這裡是巫師用來測試用的大氣圈空間.
大氣圈應該不多, 只要是會飛的東西大概都能進入.
LONG
	);
	set("exits", ([ /* sizeof() == 7 */
		"out"		: "/d/death/transporter",
		"space"		: __DIR__"test_space",
		"atmosphere"	: __DIR__"test_atmosphere",
		"ocean"		: __DIR__"test_ocean",
		"river"		: __DIR__"test_river",
		"mountain"	: __DIR__"test_mountain",
		"hill"		: __DIR__"test_hill",
]));
	set("no_clean_up", 0);
	set("terrain", "atmosphere");
	setup();
}
