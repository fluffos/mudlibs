#define HTML(x, y) "<" + (x) + ">" + (y) + "</" + (x) + ">"
#define HTML_PAGE(x, y) HTML("HEAD",HTML("TITLE",x)) + HTML("BODY",HTML("H1",y))

string process_input(string str)
{
	log_file("http_request", str);
	receive( HTML_PAGE("httpd echo", str) );
	destruct(this_object());
}
