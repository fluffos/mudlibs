// ss_s_cargo_1.c

#include <room.h>

inherit SS_ROOM;

void create()
{
	set("short", "轟天號艦橋");
	set("long", "這只是艘最簡陋的小型宇宙貨船.\n");

	set("exits", ([ /* sizeof() == 1 */
		"down"		: __DIR__"s_cargo_2",
        ]) );

	set("bridge", 1 );
	set("boarding_tube", 1 );

	setup();
}
