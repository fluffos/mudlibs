// enterprise_1.c

inherit SS_ROOM;

void create()
{
	set("short", "企業號艦橋");
	set("long", @LONG
    企業號的艦橋中央是艦長的座位, 正前方有一個巨大的螢幕, 顯示著攝影機
拍攝的外界影像. 右方是通訊官的座位, 左方是科學官的座位. 在後方是電梯.
LONG);

	set("exits", ([
		"down"	: __DIR__"enterprise_2",
        ]) );

	set("bridge", 1 );

	setup();
}
