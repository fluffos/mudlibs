// enterprise_3.c

inherit SS_ROOM;

void create()
{
	set("short", "企業號登艦通道");
	set("long", "這裡是企業號的出入通道, 上下企業號都需要經過此處.\n");
	set("boarding_tube", 1 );

	set("exits", ([
		"main"	:	__DIR__"enterprise_2",
	]) );

	setup();
}
