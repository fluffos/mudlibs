// void.c (re-created after Elon screwed it up on 07-24-95)

inherit ROOM;

void create()
{
	set("short","虛無");
	set("long",@LONG	
這裡就是傳說中的虛無 -- 大神和巫師們的終極邊疆. 你的身體飄在虛空之中, 
什麼也看不清楚. 猛然你面前冒出一隻紫色章魚, 遞給你一罐「寶路」(tm), 然後在
一旁靜靜地發呆. 呃....這種地方還是不要待太久比較好.
LONG
	);

	set("exits", ([
		"down"	: 	"/d/wiz/guestroom",
	]));

	setup();
	replace_program(ROOM);
}

