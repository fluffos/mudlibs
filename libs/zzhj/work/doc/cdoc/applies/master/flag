
	flag(4)			MudOS v21c2		(5 Sep 1994)

	名稱:
		flag - 當驅動程式啟動時, 處理 mudlib 所指定的特定旗標
		       (flag).
	語法:
		void flag( string );

		無傳回值 flag( 字串 );
	用法:
		驅動程式啟動時, 如果在作業系統的命令列下指定 -f 選項
		, 每一個其他的命令列選項都會當作 flag() 的參數呼叫它.
	翻譯:
		Spock @ FF	97.Aug.6.		(printed 3/16/95)
