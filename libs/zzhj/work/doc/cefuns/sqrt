
	sqrt(3)			MudOS v21c2		(5 Sep 1994)

	名稱:
		sqrt() - 傳回一個浮點數的平方根 (square root).
	語法:
		float sqrt( float f );

		浮點數 sqrt( 浮點數 f );
	用法:
		sqrt() 傳回參數 f  的正平方根. f 不可為負.
	參考:
		exp(3), log(3), pow(3)
	翻譯:
		Spock @ FF	97.jul.23.		(printed 3/16/95)
