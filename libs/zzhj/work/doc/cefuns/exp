
	exp(3)			MudOS v21c2		(5 Sep 1994)

	名稱:
		exp() - 以一個浮點數為指數, 自然對數 e  為底數
			計算其值.
	語法:
		float exp( float f );

		浮點數 exp( 浮點數 f );
	用法:
		exp() 傳回 e^f 的值.
	參考:
		log(3), pow(3), sqrt(3)
	翻譯:
		Spock @ FF	97.Feb.12.		(printed 3/16/95)
