
ES2 mudlib - FF extension v0.6c 函式說明書

程式: /adm/daemons/eventd.c

	此 daemon 由 Kenny of Broken History 所撰寫, 由 
	/include/event.h 所定義的 CONFIG_FILE (目前為 
	/adm/etc/event.list) 讀入各事件發生的時間及定義. 再利用以下的
	各函式呼叫及維護各事件.
	讀入的事件表儲存在 events 映射變數中.

	(Kenny of Broken History 補述)
	啊，是拿我以前在 BH 寫的哦，那個好像時間計算不精確，因為 BH
	已停止發展，所以有 bug 請自行處理，呵。但是，這個 daemon 的
	效率很差，可參考 Tmi2Lib 的 eventd，那個效率較好。

private mapping *read_table( string file )

	從 file 讀入事件表, 載入本物件的時候, 由本物件的 create() 呼叫.
	file 的格式請參考 /adm/etc/event.list

int remove_event( int event_id )

	本物件內部的 perform_event() 和 check_event() 函式呼叫此函式, 
	由 events 變數中刪除事件.

private void perform_event( mapping event )

	本物件內部的 check_event() 呼叫此函式以執行事件.

private void check_event()

	每隔一定的週期此函式呼叫自己檢查是否有事件需要執行. 此週期定義在
	/include/event.h 的 INTERVAL, 以秒為單位, 目前是 60 秒.

int add_event( mapping event )

	加入事件表以外的事件. event 映射變數的格式請參考本物件程式碼開頭,
	由 Kenny 所附上的註解.

mapping *query_events()

	傳回 events 變數的內容.

Spock of The Final Frontier	98.Sep.20.
