
ES2 mudlib - FF extension v0.6c 函式說明書

程式: /adm/daemons/eventd.c

string age_string( int time )

	time 是玩家的上線時間總秒數, 此函式把秒數轉換成 12m34d56h 格式.

string finger_all()

	由 /cmds/usr/finger.c 指令呼叫, 列出線上所有使用者的 finger
	資料.

string finger_user( string name )

	取得一位使用者的 finger 資料.

varargs string remote_finger_user( string name, int chinese_flag )

	使用 intermud 服務取得其他 mud 使用者的 finger 資料.

object acquire_login_ob( string id )

	複製名為 id 的玩家的 login 物件, 目前用於 finger 及信箱物件.

Spock of The Final Frontier	98.Sep.20.
