
ES2 mudlib - FF extension v0.6c 函式說明書

程式: /adm/simul_efun/overrides.c

nomask varargs void shutdown( int code )

	對 driver 原有的 shutdown() 外部函式加上檢查.

nomask varargs object snoop( object snooper, object snoopee )

	對 driver 原有的 snoop() 外部函式加上檢查.

Spock of The Final Frontier	98.Sep.16.
