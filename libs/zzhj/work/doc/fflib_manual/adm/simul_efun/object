
ES2 mudlib - FF extension v0.6c 函式說明書

程式: /adm/simul_efun/object.c

varargs int getoid( object ob )

	傳回物件的編號, 此編號就是 file_name() 傳回來的物件名稱後面的編號.

string file_owner( string file )

	只對巫師自己目錄下的檔案有效, 找出該檔案是哪一位巫師的.
	/adm/obj/master.c 的 log_error() 藉此決定如果物件發生錯誤,
	要把紀錄擺在哪裡. 巫師自己目錄底下的檔案出錯, 就把紀錄扔進巫師
	自己的目錄.

string domain_file( string file )

	只有對 /d/ 底下的區域檔案和 /u/ 底下的巫師檔案有效, 如果是區域
	檔案, 傳回區域目錄的名稱. 巫師檔案則傳回 Wizard.

string creator_file( string file )

	決定 file 檔案的創作者是誰, 依創作者不同, 而傳回不同的 id.
	此函式為 ES2 mudlib 安全系統的一部份, 決定此檔案的來源為何,
	該給予多大的權限.

string author_file( string file )

	只對 /u/ 底下的巫師檔案有效, 傳回該巫師的名字.

void destruct( object ob )

	把 driver 提供的 destruct() 函式, 加上一些檢查.

Spock of The Final Frontier	98.Sep.16.
