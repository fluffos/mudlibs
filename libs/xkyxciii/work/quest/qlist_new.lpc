#include <ansi.h>

inherit SKILL;

mapping *quest = ({
  ([ "quest": "/obj/money/thousand-gold",
    "SEN": 0,
    "time": 60,
    "exp_bonus": 1000,
  ]),
});

mapping query_quest() {
  return quest[random(sizeof(quest))];
}
