// workroom.c	(工作室)
// Key 1999-03-24

inherit ROOM;

void create()
{
        set("short", "工作室");
        set("long", @LONG

    这里是新盖的工作室，空荡荡的什么都没有，你最好还是自己尽快添加
一些摆设等东西吧。
LONG
        );

        set("exits", ([
		"out"	: "//d/center/guangchang",
        ]));

	setup();

        replace_program(ROOM);

}
