// inn_2f9.c
// 编程：中山大学  张三丰（Micro）----Micro_Liang@Hotmail.Com		1997.6

inherit ROOM;

void create()
{
	set("short", "神仙客栈客房");
        set("long", @LONG

    这里是客栈的客房，门牌上写着“单九”的字样。客房里面显得很宽
敞，靠近窗边是一张桌子，桌面上有两支红烛。另一边是一张单人床，床
上罗帐低垂，使人忍不住想躺下去(sleep)。
LONG
	);
	set("exits", ([
		"out" : __DIR__"inn_2f" 
	]) );
	set("valid_sleep",1);
	setup();
	replace_program(ROOM);
}
