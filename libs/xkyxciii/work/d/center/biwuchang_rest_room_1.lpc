// biwuchang_rest_room_1.c - 比武场休息室
// 编程：中山大学  张三丰（Micro）----Micro_Liang@Hotmail.Com           1997.7

#include <room.h>

inherit ROOM;

void create()
{
        set("short", "比武场休息室");
        set("long", @LONG

    这里是比武场的第一间休息室。
LONG
        );

        // set("outdoors", "center");
        set("no_fight", 1);
        set("no_magic", 1);
        set("no_spells", 1);
        set("no_steal", 1);

        set("exits", ([
                "east" : __DIR__"biwuchang_fight_room",
        ]) );                   

        setup();

        replace_program(ROOM);
}
