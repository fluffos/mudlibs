// main-gn.c - 泉州城－北城门

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "北城门");
        set("long", @LONG

    这里是泉州城的北城门。
LONG
        );

        set("city", 1);
        set("outdoors", "quanzhou");

        set("exits", ([
                "south" : __DIR__"main-n3",
        ]));

        setup();

        replace_program(ROOM);
}
