// Room: /d/quanzhou/main-e3n3.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create() {
  set("short", "镜仙书院");
  set("long", @LONG

    这里是泉州城的镜仙书院。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "main-e3n2",
  ]));
  set("no_clean_up", 0);
  set("city", 1);

  setup();
  replace_program(ROOM);
}
