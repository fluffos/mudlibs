// Room: /d/quanzhou/main.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create() {
  set("short", "百源川池");
  set("long", @LONG

    这里是泉州城的城中心百源川池。虽然这里是城中心，人们也只是脚步轻轻地
过往，并不喧哗。
LONG
  );
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/fighter-a": 1,
    __DIR__ "npc/fighter-b": 1,
  ]));
  set("outdoors", "quanzhou");
  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "main-n1",
    "west": __DIR__ "main-w1",
    "south": __DIR__ "main-s1",
    "east": __DIR__ "main-e1",
  ]));
  set("no_clean_up", 0);
  set("city", 1);

  setup();
  replace_program(ROOM);
}
