// mingguangcun.c -  明光村

inherit ROOM;

void create() {
  set("short", "明光村");
  set("long", @LONG
据路边社统计，全国具有高等学历的人口最密集的地方——375 路车，在这里就设了一
个站。 375 车站再走过一点就是北邮了。想当年大鱼还没有学会法术的时候，这里是
大鱼进城的必经之路。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "bupt_gate",
    "south": __DIR__ "xueyuannan_road",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "beijing");
  setup();
  replace_program(ROOM);
}
