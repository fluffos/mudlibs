// sw_road1.c- 大栅栏

inherit ROOM;

void create() {
  set("short", "大栅栏");
  set("long", @LONG
三教九流聚集的地方。在这里各种各样的人物都有，要分外小心自己的钱包。
LONG
  );
  set("exits", ([ /* sizeof() == 2*/
    "north": __DIR__ "w_road3",
    "south": __DIR__ "sw_road2",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "beijing");
  setup();
  replace_program(ROOM);
}
