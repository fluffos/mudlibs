// /d/beijing2/xingbu/qiushi3.c

inherit ROOM;

void create()
{
        set("short", "囚室");
        set("long", @LONG
这是刑部囚禁犯人的地方。但是守卫不见得特别森严。囚室上方有个小窗口(window)。
LONG
        );

        set("exits", ([ /* sizeof() == 1 */
      "west" :  __DIR__"qiushi2",
	]));
        set("item_desc", ([
                "window" : "这是让囚犯透气的小窗口。从这可以看见刑部后院的一间小屋。小屋布置得很精致\n"
"看得出她的主人一定是一个很有灵气的姑娘。小屋的窗台上放着一盘蓝色的玫瑰(Rose)。\n"

        ]) );


        set("no_clean_up", 0);
//        set("outdoors", "beijing");
/*        set("objects", ([ 
                __DIR__"npc/sharen_kuangmo" : 1,
                ]));
*/
        setup();
        replace_program(ROOM);

}

