// se_road1.c- 前门南大街

inherit ROOM;

void create() {
  set("short", "前门南大街");
  set("long", @LONG
守卫北京城和王宫的部队--御林军，就是驻扎在这条街上。另外，北京首富
张百万，华北地区最大的镖局也在这条街上。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "e_road3",
    "south": __DIR__ "se_road2",
    // 临时取消张百万，因为没有很多时间QC，以及其中的奖励好象太高。
    // "west" : __DIR__"zhang/rich",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "beijing");
  setup();
}
