// main-08.c - 紫禁城－太和殿

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "太和殿");
        set("long", @LONG

    这里是紫禁城的太和殿。
LONG
        );

        set("city", 1);
        // set("outdoors", "beijing");

        set("exits", ([
                "east"  : __DIR__"main-08-e1",
                "south" : __DIR__"main-07",
                "west"  : __DIR__"main-08-w1",
                "north" : __DIR__"main-09",
        ]));

        setup();

        replace_program(ROOM);
}
