// main-06.c - 紫禁城－太和门

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "太和门");
        set("long", @LONG

    这里是紫禁城的太和门。
LONG
        );

        set("city", 1);
        set("outdoors", "beijing");

        set("exits", ([
                "east"  : __DIR__"main-06-e1",
                "south" : __DIR__"main-05",
                "west"  : __DIR__"main-06-w1",
                "north" : __DIR__"main-07",
        ]));

        setup();

        replace_program(ROOM);
}
