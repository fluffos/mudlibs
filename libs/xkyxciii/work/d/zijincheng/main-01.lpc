// main-01.c - 紫禁城－天安门

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "天安门");
        set("long", @LONG

    这里是紫禁城的天安门。
LONG
        );

        set("city", 1);
        set("outdoors", "beijing");

        set("exits", ([
                // "east"  : __DIR__"main-01",
                // "south" : __DIR__"main-01",
                // "west"  : __DIR__"main-01",
                "north" : __DIR__"main-02",
                "out"   : "/d/beijing/main-n1",
        ]));

        setup();

        replace_program(ROOM);
}
