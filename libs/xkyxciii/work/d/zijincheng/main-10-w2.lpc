// main-10-w2.c - 紫禁城－慈宁宫

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "慈宁宫");
        set("long", @LONG

    这里是紫禁城的慈宁宫。
LONG
        );

        set("city", 1);
        // set("outdoors", "beijing");

        set("exits", ([
                "east"  : __DIR__"main-10-w1",
                "south" : __DIR__"main-09-w2",
                // "west"  : __DIR__"main-10-w3",
                "north" : __DIR__"main-11-w2",
        ]));

        setup();

        replace_program(ROOM);
}
