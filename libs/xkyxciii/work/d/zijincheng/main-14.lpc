// main-14.c - 紫禁城－神武门

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "神武门");
        set("long", @LONG

    这里是紫禁城的神武门。
LONG
        );

        set("city", 1);
        set("outdoors", "beijing");

        set("exits", ([
                "east"  : __DIR__"main-14-e1",
                "south" : __DIR__"main-13",
                "west"  : __DIR__"main-14-w1",
                // "north" : __DIR__"main-15",
        ]));

        setup();

        replace_program(ROOM);
}
