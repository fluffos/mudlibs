// main-09-w2.c - 紫禁城－咸若馆

#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "咸若馆");
  set("long", @LONG

    这里是紫禁城的咸若馆。
LONG
  );

  set("city", 1);
  // set("outdoors", "beijing");

  set("exits", ([
    "east": __DIR__ "main-09-w1",
    "south": __DIR__ "main-08-w2",
    // "west"  : __DIR__"main-09-w3",
    "north": __DIR__ "main-10-w2",
  ]));

  setup();

  replace_program(ROOM);
}
