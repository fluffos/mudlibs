// mz-07.c - 杭州的梅庄（黑白斋）
// 编程：中山大学  孔雀（Key）----ln96wgx@nct.zsu.edu.cn         1997.9

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "黑白斋");
        set("long", @LONG
                                                                
    这里是梅庄的黑白斋。
LONG
        );

        // set("outdoors", "meizhuang");

        set("exits", ([
                "east"  : __DIR__"mz-07e",
                "west"  : __DIR__"mz-07w",
                "south" : __DIR__"mz-06",
                "north" : __DIR__"mz-08",
        ]));

        setup();

        replace_program(ROOM);
}