// master-key.c 
// 编程：中山大学  张三丰（Micro）----exslyc@zsulink.zsu.edu.cn         1997.8

#include <ansi.h>

inherit NPC;
inherit F_MASTER;

void create() {
  set_name("孔雀", ({ "master key", "master" }));

  set("long", @LONG
这位是天师山谷的开山祖师之一。
LONG
  );

  set("title", "天师山谷开山祖师");
  create_family("天师山谷", 1, "祖师");
  set("rank_info/respect", "祖师");
  set("gender", "女性");
  set("nickname", HIY "孔雀" HIR "明" HIY "王" NOR);
  set("age", 20);

  set("str", 30);         // 膂力         
  set("cps", 30);         // 定力
  set("cor", 30);         // 胆识         
  set("per", 30);         // 容貌
  set("int", 30);         // 悟性         
  set("con", 30);         // 根骨
  set("spi", 30);         // 灵性         
  set("kar", 30);         // 福缘

  set("max_atman", 0); set("atman", 0);
  set("max_force", 1000); set("force", 2000); set("force_factor", 20);
  set("max_mana", 1000); set("mana", 2000);

  set("combat_exp", 8000000);
  set("SEN", 100);

  set_skill("force", 150); set_skill("huangting-force", 150);
  set_skill("sword", 70); set_skill("tianshi-sword", 70);
  set_skill("unarmed", 70); set_skill("tianshi-zhang", 70);
  set_skill("parry", 70);
  set_skill("move", 210); set_skill("qiankun-steps", 210);
  set_skill("dodge", 210);
  set_skill("literate", 200);
  set_skill("spells", 200); set_skill("necromancy", 200);
  set_skill("taoism", 200);

  map_skill("force", "huangting-force");
  map_skill("sword", "tianshi-sword");
  map_skill("unarmed", "tianshi-zhang");
  map_skill("parry", "tianshi-ling");
  map_skill("move", "qiankun-steps");
  map_skill("dodge", "qiankun-steps");
  map_skill("spells", "necromancy");
  /*
        set("chat_chance_combat", 60);
        set("chat_msg_combat", ({
                (: perform_action :),
                (: exert_function, "powerup" :),
                (: exert_function, "recover" :),
        }) );
*/
  setup();

  carry_object("/obj/misc/cloth")->wear();
  carry_object("/obj/weapon/longsword")->wield();
}
/*
void attempt_apprentice(object ob)
{

        if( ((string)ob->query("gender") != "女性")) {
                command("say 这位" + RANK_D->query_respect(ob) + "请回去吧，本座只收女徒。");
                return;
        } 
        if( ((int)ob->query("kar") < 20)) {
                command("say 依我看" + RANK_D->query_respect(ob) + "的资质还不够高？");
                return;
        } 
        command("smile");
        command("say 很好，" + RANK_D->query_respect(ob) + "多加努力，他日必定有成。\n");
        command("recruit " + ob->query("id") );
}

*/
