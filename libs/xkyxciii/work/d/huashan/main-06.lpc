// main-06.c - 华山主干线（通仙观）
// 编程：中山大学  张三丰（Micro）----exslyc@zsulink.zsu.edu.cn		1997.8

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "通仙观");
 	set("long", @LONG
								
    这里是华山的通仙观。
LONG
	);

        set("outdoors", "huashan");

 	set("exits", ([
		"northdown" : __DIR__"main-05",
		"eastup"    : __DIR__"main-07",
	]));

	setup();

	replace_program(ROOM);

}

