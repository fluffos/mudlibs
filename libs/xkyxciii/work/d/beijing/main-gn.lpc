// main-gw.c - 北京城－安定门

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "安定门");
        set("long", @LONG

    这里是北京城的安定门。
LONG
        );

        set("city", 1);
        set("outdoors", "beijing");

        set("exits", ([
                "south" : __DIR__"main-n4",
                "north" : __DIR__"ruyangwangfu/road3",
        ]));

        setup();

        replace_program(ROOM);
}
