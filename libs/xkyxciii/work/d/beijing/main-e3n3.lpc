// main-e3n3.c - 北京城－洋器铺

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "洋器铺");
        set("long", @LONG

    这里是北京城的洋器铺。
LONG
        );

        set("city", 1);
        // set("outdoors", "beijing");

        set("exits", ([
                "east"  : __DIR__"main-e4n3",
                "south" : __DIR__"main-e3n2",
                "west"  : __DIR__"main-e2n3",
                "north" : __DIR__"main-e3n4",
        ]));

        setup();

        replace_program(ROOM);
}
