// main-w2n4.c - 北京城－积水潭

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "积水潭");
        set("long", @LONG

    这里是北京城的积水潭。
LONG
        );

        set("city", 1);
        set("outdoors", "beijing");

        set("exits", ([
                "east"  : __DIR__"main-w1n4",
                "south" : __DIR__"main-w2n3",
                "west"  : __DIR__"main-w3n4",
                // "north" : __DIR__"main-w2n5",
        ]));

        setup();

        replace_program(ROOM);
}
