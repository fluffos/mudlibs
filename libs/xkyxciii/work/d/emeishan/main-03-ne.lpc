// main-03-ne.c - 峨眉山主干线（报国寺）

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "报国寺");
 	set("long", @LONG
								
    这里是报国寺。
LONG
	);

 	set("exits", ([
		"southwest" : __DIR__"main-03",
	]));

	setup();

	replace_program(ROOM);

}

