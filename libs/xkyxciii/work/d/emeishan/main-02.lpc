// main-02.c - 峨眉山主干线（伏虎寺）
// 编程：中山大学  张三丰（Micro）----exslyc@zsulink.zsu.edu.cn		1997.8

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "伏虎寺");
 	set("long", @LONG
								
    这里是峨眉山的伏虎寺。
LONG
	);

        set("outdoors", "emeishan");

 	set("exits", ([
		"northwest" : __DIR__"main-03",
		"northeast" : __DIR__"main-02-ne",
		"southdown" : __DIR__"main-01",
	]));

	setup();

	replace_program(ROOM);

}

