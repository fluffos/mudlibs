// main-10.c - 峨眉山主干线（观心坡）
// 编程：中山大学  张三丰（Micro）----exslyc@zsulink.zsu.edu.cn		1997.8

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "观心坡");
 	set("long", @LONG
								
    这里是峨眉山的观心坡。
LONG
	);

        set("outdoors", "emeishan");

 	set("exits", ([
		"westup"    : __DIR__"main-11",
		"eastdown"  : __DIR__"main-09",
	]));

	setup();

	replace_program(ROOM);

}

