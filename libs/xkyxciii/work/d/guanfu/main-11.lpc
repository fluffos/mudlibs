// main-11.c - 侠客城的官府（后堂楼）
// 编程：中山大学  孔  雀（Key）----ln96wgx@nct.zsu.edu.cn         1997.9

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "后堂楼");
        set("long", @LONG
                                                                
    这里是侠客城官府的后堂楼。
LONG
        );

        // set("outdoors", "xiakecheng");

        set("exits", ([
                "south" : __DIR__"main-10",
                "north" : __DIR__"main-12",
        ]));

        setup();

        replace_program(ROOM);
}