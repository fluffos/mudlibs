inherit ROOM;

void create()
{
        set("short", "巨木旗大厅");
        set("long", @LONG

这里是明教巨木旗的大厅。
LONG
        );

        set("exits", ([
                "east" : __DIR__"piazza_2",
        ]));

        set("objects", ([
                __DIR__"npc/wen-cangsong": 1,
                __DIR__"npc/wood-disciple": 2,
        ]) );


//      set("outdoors", "mingjiao");
        setup();
//      replace_program(ROOM);
}