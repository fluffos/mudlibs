// /d/gumu/sun-room.c
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "孙婆婆房");
  set("long", @TEXT 
    这里干燥宽畅，是孙婆婆的卧室，左面一张石榻，榻前一张石椅
右面一张石桌，摆设非常简单朴实。
TEXT);
  set("exits", ([
    "north": __DIR__ "mi-edge",

  ]));
  set("objects", ([
    "/d/gumu/npc/sun": 1 ]));
  setup();

}
