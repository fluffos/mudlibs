// main-08n.c - 古墓房间（书房）
// 编程：中山大学  张三丰（Micro）----exslyc@zsulink.zsu.edu.cn         1997.8

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "书房");
        set("long", @LONG
                                                                
    这里是进古墓甬道旁的书房。
LONG
        );

        // set("outdoors", "emeishan");

        set("exits", ([
                "south" : __DIR__"main-08",
        ]));

        setup();

        replace_program(ROOM);

}