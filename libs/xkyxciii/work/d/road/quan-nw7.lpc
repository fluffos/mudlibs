// Room: /d/road/quan-nw7.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create() {
  set("short", "回雁峰");
  set("long", @LONG
								
    这里是泉州西北的回雁峰。
LONG
  );
  set("objects", ([ /* sizeof() == 1 */
    "/d/quanzhou/npc/fighter-b": 1,
  ]));
  set("outdoors", "road");
  set("exits", ([ /* sizeof() == 3 */
    // Micro: 1999-02-25 取消衡山
    // "northeast" : __DIR__"temp-hengshan",
    "southeast": __DIR__ "quan-nw6",
    "northwest": __DIR__ "quan-nw8",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
