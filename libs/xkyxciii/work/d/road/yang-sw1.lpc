// yang-sw1.c - 扬州城向西南（江南水乡）
// 编程：中山大学  张三丰（Micro）----exslyc@zsulink.zsu.edu.cn		1997.8

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "江南水乡");
 	set("long", @LONG
								
    这里是扬州城西南的江南水乡。
LONG
	);

	set("outdoors", "road");

 	set("exits", ([
		"east"   : __DIR__"yang-sw2",
		"north"  : __DIR__"cje-harbour-s",
	]));

	setup();

	replace_program(ROOM);

}

