// Room: /d/road/xia-n4.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create()
{
	set("short", "冠云山");
	set("long", @LONG
								
    这里是侠客城以北的冠云山。
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
  "/d/quanzhou/npc/bee" : 1,
]));
	set("outdoors", "road");
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"xia-n5",
  "south" : __DIR__"xia-n3",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
