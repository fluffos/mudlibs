// Room: /d/road/quan-nw4.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create()
{
	set("short", "武夷山");
	set("long", @LONG
								
    这里是泉州西北的武夷山。
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
  "/d/quanzhou/npc/fighter-a" : 1,
]));
	set("outdoors", "road");
	set("exits", ([ /* sizeof() == 2 */
  "southeast" : __DIR__"quan-nw3",
  "northwest" : __DIR__"quan-nw5",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
