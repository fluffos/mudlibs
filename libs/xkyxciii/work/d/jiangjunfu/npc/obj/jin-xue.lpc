// jin-xue.c
// 编程：中山大学  张三丰（Micro）----Micro_Liang@Hotmail.Com		1997.9

#include <ansi.h>
#include <armor.h>

inherit BOOTS;

void create()
{
        set_name( HIY "黄金靴" NOR, ({ "jin xue", "xue" }) );
        set_weight( 5000 );
        if( clonep() )
                set_default_object(__FILE__);
        else {
		set("long", "一双普通的黄金靴。\n");
                set("unit", "双");
                set("value", 10000);
		set("material", "gold");
		set("armor_type", "boots");
		set("armor_prop/armor", 6 );
		set("armor_prop/defense", 0 );
                set("armor_prop/dodge", 0 );
        }
        setup();
}
