// main-08w.c - 侠客城的将军府（花  厅）
// 编程：中山大学  孔  雀（Key）----ln96wgx@nct.zsu.edu.cn         1997.9

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "花厅");
        set("long", @LONG
                                                                
    这里是侠客城将军府的花厅。
LONG
        );

        // set("outdoors", "xiakecheng");

        set("exits", ([
                "east"   : __DIR__"main-08",
        ]));

        set("objects", ([
                __DIR__"npc/pingxi-jiangjun": 1,
                __DIR__"npc/saobei-jiangjun": 1,
	]) );

        setup();

        replace_program(ROOM);
}