// yzg.c - 扬州城－扬州港

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "扬州港");
        set("long", @LONG

    这里是扬州城的扬州港。
LONG
        );

        set("city", 1);
        set("outdoors", "yangzhou");

        set("exits", ([
                // "east"   : __DIR__"yzg",
                "south"  : __DIR__"yzg-s1",
                // "west"   : __DIR__"yzg",
                "north"  : __DIR__"yzg-n1",
                "westup" : __DIR__"main-ge",
        ]));

        setup();

        replace_program(ROOM);
}
