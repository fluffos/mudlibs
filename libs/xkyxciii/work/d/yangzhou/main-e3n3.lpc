// main-e3n3.c - 扬州城－青竹林

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "青竹林");
        set("long", @LONG

    这里是扬州城的青竹林。
LONG
        );

        set("city", 1);
        set("outdoors", "yangzhou");

        set("exits", ([
                // "east"  : __DIR__"main-e4n3",
                "south" : __DIR__"main-e3n2",
                "west"  : __DIR__"main-e2n3",
                // "north" : __DIR__"main-e3n4",
        ]));

        set("objects", ([
                "/d/center/npc/liumang": 2,
        ]) );
        setup();

        replace_program(ROOM);
}
