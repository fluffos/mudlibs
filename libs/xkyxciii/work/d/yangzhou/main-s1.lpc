// main-s1.c - 扬州城－南大街

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "南大街");
        set("long", @LONG

    这里是扬州城的南大街。
LONG
        );

        set("city", 1);
        set("outdoors", "yangzhou");

        set("exits", ([
                "east"  : __DIR__"main-e1s1",
                "south" : __DIR__"main-s2",
                "west"  : __DIR__"main-w1s1",
                "north" : __DIR__"main",
        ]));

        setup();

        replace_program(ROOM);
}
