// main-gs.c - 扬州城－南城门

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "南城门");
        set("long", @LONG

    这里是扬州城的南城门。
LONG
        );

        set("city", 1);
        set("outdoors", "yangzhou");

        set("exits", ([
                "north" : __DIR__"main-s3",
        ]));

        set("objects", ([
                "/obj/npc/wokou": 4,
        ]) );

        setup();

        replace_program(ROOM);
}
