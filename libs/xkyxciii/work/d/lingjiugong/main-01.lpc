// main-01.c - 飘渺峰十八险（飘渺峰脚）
// 编程：中山大学  孔 雀（Key）----ln96wgx@nct.zsu.edu.cn               1997.9

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "飘渺峰脚");
        set("long", @LONG
                                                                
    这里是飘渺峰脚。
LONG
        );

        set("outdoors", "lingjiugong");

        set("exits", ([
                "east"     : "/d/road/bei-w20",
                // "northup"  : __DIR__"main-02",
                "northup"  : __DIR__"main-20",
        ]));

        setup();

        replace_program(ROOM);

}
