// apprentice.c
// 修改：中山大学  张三丰（Micro）----Micro_Liang@Hotmail.Com           1997.6

#include <ansi.h>

inherit F_CLEAN_UP;

int help(object me);
void decrease_skill(object ob);

void re_rank(object me) {
  string family, national;
  int exp;

  national = me->query("national");
  exp = me->query("combat_exp");
  family = me->query("family/family_name");

  if (exp >= 10000000) {
    switch (family) {
      // case "少林寺":   me->set("title", national+"族"+family + ""); return;
      case "武当派": me->set("title", national + family + "掌教长老"); return;
      case "鹿鼎门": me->set("title", national + family + "朝庭王爷"); return;
      case "华山派": me->set("title", national + family + "掌教"); return;
      case "逍遥派": me->set("title", national + family + "掌教"); return;
      case "灵鹫宫": me->set("title", national + family + "掌教"); return;
      case "逍遥北宗": me->set("title", national + family + "掌教"); return;
      // case "星宿海":   me->set("title", national+family + ""); return;
      case "神仙山庄": me->set("title", national + family + "掌教"); return;
      // case "天师山谷": me->set("title", national+family + ""); return;
      case "全真教": me->set("title", national + family + "掌教"); return;
      case "桃花岛": me->set("title", national + family + "掌教"); return;
      case "大理国": me->set("title", national + family + "国师"); return;
      // case "白驼山":   me->set("title", national+family + ""); return;
      case "丐帮": me->set("title", national + family + "九袋长老"); return;
      case "明教": me->set("title", national + family + "掌教"); return;
      case "日月神教": me->set("title", national + family + "掌教"); return;
      case "峨眉派": me->set("title", national + family + "掌教"); return;
      default:
    }
    return;
  }
  if (exp >= 5000000) {
    switch (family) {
      // case "少林寺":   me->set("title", national+family + ""); return;
      case "武当派": me->set("title", national + family + "掌教真人"); return;
      case "鹿鼎门": me->set("title", national + family + "朝庭侯爷"); return;
      case "华山派": me->set("title", national + family + "长老"); return;
      case "逍遥派": me->set("title", national + family + "长老"); return;
      case "灵鹫宫": me->set("title", national + family + "长老"); return;
      case "逍遥北宗": me->set("title", national + family + "长老"); return;
      // case "星宿海":   me->set("title", national+family + ""); return;
      case "神仙山庄": me->set("title", national + family + "长老"); return;
      // case "天师山谷": me->set("title", national+family + ""); return;
      case "全真教": me->set("title", national + family + "长老"); return;
      case "桃花岛": me->set("title", national + family + "长老"); return;
      case "大理国": me->set("title", national + family + "长老"); return;
      // case "白驼山":   me->set("title", national+family + ""); return;
      case "丐帮": me->set("title", national + family + "八袋弟子"); return;
      case "明教": me->set("title", national + family + "副掌教"); return;
      case "日月神教": me->set("title", national + family + "副掌教"); return;
      case "峨眉派": me->set("title", national + family + "长老"); return;
      default:
    }
    return;
  }
  if (exp >= 2000000) {
    switch (family) {
      // case "少林寺":   me->set("title", national+family + ""); return;
      case "武当派": me->set("title", national + family + "掌剑真人"); return;
      case "鹿鼎门": me->set("title", national + family + "朝庭爵爷"); return;
      case "华山派": me->set("title", national + family + "剑圣"); return;
      case "逍遥派": me->set("title", national + family + "剑圣"); return;
      case "灵鹫宫": me->set("title", national + family + "剑圣"); return;
      case "逍遥北宗": me->set("title", national + family + "剑圣"); return;
      // case "星宿海":   me->set("title", national+family + ""); return;
      case "神仙山庄": me->set("title", national + family + "剑圣"); return;
      // case "天师山谷": me->set("title", national+family + ""); return;
      case "全真教": me->set("title", national + family + "剑圣"); return;
      case "桃花岛": me->set("title", national + family + "剑圣"); return;
      case "大理国": me->set("title", national + family + "剑圣"); return;
      // case "白驼山":   me->set("title", national+family + ""); return;
      case "丐帮": me->set("title", national + family + "七袋弟子"); return;
      case "明教": me->set("title", national + family + "长老"); return;
      case "日月神教": me->set("title", national + family + "长老"); return;
      case "峨眉派": me->set("title", national + family + "剑圣"); return;
      default:
    }
    return;
  }
  if (exp >= 1000000) {
    switch (family) {
      // case "少林寺":   me->set("title", national+family + ""); return;
      case "武当派": me->set("title", national + family + "掌法道长"); return;
      case "鹿鼎门": me->set("title", national + family + "朝庭一品命官"); return;
      case "华山派": me->set("title", national + family + "剑神"); return;
      case "逍遥派": me->set("title", national + family + "剑神"); return;
      case "灵鹫宫": me->set("title", national + family + "剑神"); return;
      case "逍遥北宗": me->set("title", national + family + "剑神"); return;
      // case "星宿海":   me->set("title", national+family + ""); return;
      case "神仙山庄": me->set("title", national + family + "剑神"); return;
      // case "天师山谷": me->set("title", national+family + ""); return;
      case "全真教": me->set("title", national + family + "剑神"); return;
      case "桃花岛": me->set("title", national + family + "剑神"); return;
      case "大理国": me->set("title", national + family + "剑神"); return;
      // case "白驼山":   me->set("title", national+family + ""); return;
      case "丐帮": me->set("title", national + family + "六袋弟子"); return;
      case "明教": me->set("title", national + family + "光明使者"); return;
      case "日月神教": me->set("title", national + family + "日月法王"); return;
      case "峨眉派": me->set("title", national + family + "剑神"); return;
      default:
    }
    return;
  }
  if (exp >= 500000) {
    switch (family) {
      // case "少林寺":   me->set("title", national+family + ""); return;
      case "武当派": me->set("title", national + family + "执法道长"); return;
      case "鹿鼎门": me->set("title", national + family + "朝庭二品命官"); return;
      case "华山派": me->set("title", national + family + "剑王"); return;
      case "逍遥派": me->set("title", national + family + "剑王"); return;
      case "灵鹫宫": me->set("title", national + family + "剑王"); return;
      case "逍遥北宗": me->set("title", national + family + "剑王"); return;
      // case "星宿海":   me->set("title", national+family + ""); return;
      case "神仙山庄": me->set("title", national + family + "剑王"); return;
      // case "天师山谷": me->set("title", national+family + ""); return;
      case "全真教": me->set("title", national + family + "剑王"); return;
      case "桃花岛": me->set("title", national + family + "剑王"); return;
      case "大理国": me->set("title", national + family + "剑王"); return;
      // case "白驼山":   me->set("title", national+family + ""); return;
      case "丐帮": me->set("title", national + family + "五袋弟子"); return;
      case "明教": me->set("title", national + family + "光明法王"); return;
      case "日月神教": me->set("title", national + family + "日月护法"); return;
      case "峨眉派": me->set("title", national + family + "剑王"); return;
      default:
    }
    return;
  }
  if (exp >= 200000) {
    switch (family) {
      // case "少林寺":   me->set("title", national+family + ""); return;
      case "武当派": me->set("title", national + family + "执事道长"); return;
      case "鹿鼎门": me->set("title", national + family + "朝庭三品命官"); return;
      case "华山派": me->set("title", national + family + "剑师"); return;
      case "逍遥派": me->set("title", national + family + "剑师"); return;
      case "灵鹫宫": me->set("title", national + family + "剑师"); return;
      case "逍遥北宗": me->set("title", national + family + "剑师"); return;
      // case "星宿海":   me->set("title", national+family + ""); return;
      case "神仙山庄": me->set("title", national + family + "剑师"); return;
      // case "天师山谷": me->set("title", national+family + ""); return;
      case "全真教": me->set("title", national + family + "剑师"); return;
      case "桃花岛": me->set("title", national + family + "剑师"); return;
      case "大理国": me->set("title", national + family + "剑师"); return;
      // case "白驼山":   me->set("title", national+family + ""); return;
      case "丐帮": me->set("title", national + family + "四袋弟子"); return;
      case "明教": me->set("title", national + family + "光明护法"); return;
      case "日月神教": me->set("title", national + family + "日月使者"); return;
      case "峨眉派": me->set("title", national + family + "剑师"); return;
      default:
    }
    return;
  }
  if (exp >= 100000) {
    switch (family) {
      // case "少林寺":   me->set("title", national+family + ""); return;
      case "武当派": me->set("title", national + family + "掌经道人"); return;
      case "鹿鼎门": me->set("title", national + family + "朝庭四品命官"); return;
      case "华山派": me->set("title", national + family + "剑侠"); return;
      case "逍遥派": me->set("title", national + family + "剑侠"); return;
      case "灵鹫宫": me->set("title", national + family + "剑侠"); return;
      case "逍遥北宗": me->set("title", national + family + ""); return;
      // case "星宿海":   me->set("title", national+family + ""); return;
      case "神仙山庄": me->set("title", national + family + "剑侠"); return;
      // case "天师山谷": me->set("title", national+family + ""); return;
      case "全真教": me->set("title", national + family + "剑侠"); return;
      case "桃花岛": me->set("title", national + family + "剑侠"); return;
      case "大理国": me->set("title", national + family + "剑侠"); return;
      // case "白驼山":   me->set("title", national+family + ""); return;
      case "丐帮": me->set("title", national + family + "三袋弟子"); return;
      case "明教": me->set("title", national + family + "光明散人"); return;
      case "日月神教": me->set("title", national + family + "日月散人"); return;
      case "峨眉派": me->set("title", national + family + "剑侠"); return;
      default:
    }
    return;
  }
  if (exp >= 50000) {
    switch (family) {
      // case "少林寺":   me->set("title", national+family + ""); return;
      case "武当派": me->set("title", national + family + "执事道人"); return;
      case "鹿鼎门": me->set("title", national + family + "朝庭五品命官"); return;
      case "华山派": me->set("title", national + family + "剑客"); return;
      case "逍遥派": me->set("title", national + family + "剑客"); return;
      case "灵鹫宫": me->set("title", national + family + "剑客"); return;
      case "逍遥北宗": me->set("title", national + family + "剑客"); return;
      // case "星宿海":   me->set("title", national+family + ""); return;
      case "神仙山庄": me->set("title", national + family + "剑客"); return;
      // case "天师山谷": me->set("title", national+family + ""); return;
      case "全真教": me->set("title", national + family + "剑客"); return;
      case "桃花岛": me->set("title", national + family + "剑客"); return;
      case "大理国": me->set("title", national + family + "剑客"); return;
      // case "白驼山":   me->set("title", national+family + ""); return;
      case "丐帮": me->set("title", national + family + "二袋弟子"); return;
      case "明教": me->set("title", national + family + "堂主"); return;
      case "日月神教": me->set("title", national + family + "堂主"); return;
      case "峨眉派": me->set("title", national + family + "剑客"); return;
      default:
    }
    return;
  }
  if (exp >= 20000) {
    switch (family) {
      // case "少林寺":   me->set("title", national+family + ""); return;
      case "武当派": me->set("title", national + family + "迎客道人"); return;
      case "鹿鼎门": me->set("title", national + family + "朝庭六品命官"); return;
      case "华山派": me->set("title", national + family + "剑手"); return;
      case "逍遥派": me->set("title", national + family + "剑手"); return;
      case "灵鹫宫": me->set("title", national + family + "剑手"); return;
      case "逍遥北宗": me->set("title", national + family + "剑手"); return;
      // case "星宿海":   me->set("title", national+family + ""); return;
      case "神仙山庄": me->set("title", national + family + "剑手"); return;
      // case "天师山谷": me->set("title", national+family + ""); return;
      case "全真教": me->set("title", national + family + "剑手"); return;
      case "桃花岛": me->set("title", national + family + "剑手"); return;
      case "大理国": me->set("title", national + family + "剑手"); return;
      // case "白驼山":   me->set("title", national+family + ""); return;
      case "丐帮": me->set("title", national + family + "一袋弟子"); return;
      case "明教": me->set("title", national + family + "坛主"); return;
      case "日月神教": me->set("title", national + family + "坛主"); return;
      case "峨眉派": me->set("title", national + family + "剑手"); return;
      default:
    }
    return;
  }
  if (exp >= 10000) {
    switch (family) {
      // case "少林寺":   me->set("title", national+family + ""); return;
      case "武当派": me->set("title", national + family + "道人"); return;
      case "鹿鼎门": me->set("title", national + family + "朝庭七品命官"); return;
      case "华山派": me->set("title", national + family + "剑士"); return;
      case "逍遥派": me->set("title", national + family + "剑士"); return;
      case "灵鹫宫": me->set("title", national + family + "剑士"); return;
      case "逍遥北宗": me->set("title", national + family + "剑士"); return;
      // case "星宿海":   me->set("title", national+family + ""); return;
      case "神仙山庄": me->set("title", national + family + "剑士"); return;
      // case "天师山谷": me->set("title", national+family + ""); return;
      case "全真教": me->set("title", national + family + ""); return;
      case "桃花岛": me->set("title", national + family + "剑士"); return;
      case "大理国": me->set("title", national + family + "剑士"); return;
      // case "白驼山":   me->set("title", national+family + "剑士"); return;
      case "丐帮": me->set("title", national + family + "乞丐头"); return;
      case "明教": me->set("title", national + family + "教众"); return;
      case "日月神教": me->set("title", national + family + "教众"); return;
      case "峨眉派": me->set("title", national + family + "剑士"); return;
      default:
    }
    return;
  }
  return;
}

int main(object me, string arg) {
  object ob, old_app;
  mapping family, skills;
  int i;
  string *skname;

  if (me->query_temp("sleeping")) return notify_fail("你现在正躺着呢。\n");

  if (me->is_busy()) return notify_fail("你现在正忙着呢。\n");

  if (!arg) return help(me);

  if (arg == "cancel") {
    old_app = me->query_temp("pending/apprentice");
    if (!objectp(old_app)) return notify_fail("你现在并没有拜任何人为师的意思。\n");
    write("你改变主意不想拜" + old_app->name() + "为师了。\n");
    tell_object(old_app, me->name() + "改变主意不想拜你为师了。\n");
    me->delete_temp("pending/apprentice");
    return 1;
  }

  if (!(ob = present(arg, environment(me))) || !ob->is_character())
    return notify_fail("你想拜谁为师？\n");

  if (!living(ob)) return notify_fail("你必须先把" + ob->name() + "弄醒。\n");

  if (ob == me) return notify_fail("拜自己为师？好主意....不过没有用。\n");

  if (me->is_apprentice_of(ob)) {
    message_vision("$N恭恭敬敬地向$n磕头请安，叫道：“师父！”\n", me, ob);
    // ob->re_rank(me);        // 重新设定玩家的RANK
    re_rank(me);
    return 1;
  }

  if (!mapp(ob->query("family")))
    return notify_fail(ob->name() + "既不属於任何门派，也没有开山立派，不能拜师。\n");

  // If the target is willing to recruit us already, we do it.
  if ((object)ob->query_temp("pending/recruit") == me) {

    if (userp(ob) && !(int)ob->query("licensed_to_recruit"))
      return notify_fail("玩家不可收徒．\n");

    if (me->query("family") && ((string)me->query("family/family_name") != (string)ob->query("family/family_name"))) {
      message_vision("$N决定背叛师门，改投入$n门下！！！\n\n"
        "$N跪了下来向$n恭恭敬敬地磕了四个响头，叫道：「师父！」\n\n", me, ob);
      me->set("score", 0);
      // decrease_skill(me);
      me->add("betrayer", 1);  // 叛师标志
    } else
      message_vision("$N决定拜$n为师。\n\n"
        "$N跪了下来向$n恭恭敬敬地磕了四个响头，叫道：「师父！」\n\n", me, ob);

    ob->recruit_apprentice(me);
    ob->delete_temp("pending/recruit");

    tell_object(ob, "恭喜你新收了一名弟子！\n");
    family = me->query("family");
    printf("恭喜您成为%s的第%s代弟子。\n", me->query("family/family_name"),
      chinese_number(me->query("family/generation")));

    return 1;

  } else {

    old_app = me->query_temp("pending/apprentice");
    if (ob == old_app)
      return notify_fail("你想拜" + ob->name() + "为师，但是对方还没有答应。\n");
    else if (objectp(old_app)) {
      write("你改变主意不想拜" + old_app->name() + "为师了。\n");
      tell_object(old_app, me->name() + "改变主意不想拜你为师了。\n");
    }

    message_vision("$N想要拜$n为师。\n", me, ob);
    me->set_temp("pending/apprentice", ob);
    if (userp(ob)) {
      tell_object(ob, YEL "如果你愿意收" + me->name() + "为弟子，用 recruit 指令。\n" NOR);
    } else
      ob->attempt_apprentice(me);

    return 1;
  }
}

void decrease_skill(object ob) {
  mapping skl;
  string *sname;
  int i;

  if (!mapp(skl = ob->query_skills())) return;

  sname = keys(skl);

  for (i = 0; i < sizeof(skl); i++) {          // 叛师减武功1/20
    // if(skl[sname[i]] >= 100) continue;
    skl[sname[i]] = skl[sname[i]] * 19 / 20;
  }
}

int help(object me) {
  write(@HELP
指令格式：apprentice <对象>     拜某人为师
          apprentice cancel     取消拜师

这个指令能让你拜某人为师，如果对方也答应要收你为徒的话，就会立即
行拜师之礼，否则要等到对方用recruit 指令收你为弟子才能正式拜师。

请注意你已经有了师父，又背叛师门投入别人门下，你的武功将减掉1/20，
而且还会对以后的学习造成影响。

如果对你的师父使用这个指令，会变成向师父请安。
 
请参考相关指令：expell、recruit。

HELP
  );

  return 1;
}
