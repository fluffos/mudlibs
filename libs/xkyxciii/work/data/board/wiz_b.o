#/std/bboard.c
dbase (["long":"这是一块专供巫师使用的留言板。","id":"board","board_id":"wiz_b","capacity":30,"name":"巫师留言板","notes":({(["title":"晋升","msg":"今天设工作室人员 yuxin 和 ten 为 \"(arch)\",通过申请成为(apr)--Player_Master 的sunto和晋升完成!","author":"孔雀","time":920378465,]),(["title":"关于种族的改动!","msg":"1999-03-12：    １.为了加进种族修改了下面三个文件。	/adm/daemons/logind.c	/cmds/std/look.c	/cmds/usr/score.c    ２.从今天起，开始使用\"/doc/wiz/log_file.txt\"记录每次的改动。","author":"孔雀","time":921261424,]),(["title":"关于梦幻时空","msg":"'在梦幻时空中用compelete_dream有些问题，它去掉dream标志前先判断是不是在当前的房间，如果不是在当前的;房间则返回，而不去掉dream标志。如果之前玩家已经因为意;外情况例如运气或者给其他人使用移动位置的卡片的话就会;出错。如果你在用go命令的同时使用比如back card之类的移;位的卡片，必然会出错。要么将这个dream标志改成一定时间;的延时，要不在每个变动位置的运气或者卡片处增加一条去;掉dream标志的命令。我现在想简单一点只在compelete_dream;改一个命令的顺序，无论房间有无变动都先去掉dream标志。;现在尚未测试，不知效果如何。","author":"赵云","time":921377568,]),(["title":"program","msg":"private void compelete_dream(object me){        object env;        string str, tempstr;        int now_num, go_num, i;        if( !me ) return;'    ;''// 先清除dream标志，防止出现玩家给移位而不能清的情况。;;        me->delete_temp(\"dream\");;''if ( environment(me)!=this_object()) return'if( present(me) ) {'''// 如果玩家还在原地                env = environment(me);                // str = base_name(env);                // sscanf( str<, \"%d\", now_num );                now_num = query_posi_number();                i = random(6) + 1;                tempstr = CYN \"\\n时空骷子落了下来，上面的数字是：\"                         + chinese_number(i) + \"。\\n\" NOR;                message_vision(tempstr, me);                if( me->query_temp(\"wqa's room/luck\") > 0 ) {                        // 运气好时，每次走的点数＊２                        message_vision( HIY \"$N鸿运当头，前进的点数加倍！\\n\" NOR, me);                        me->add_temp(\"wqa's room/luck\", -1);                        i *= 2;                }                else if( me->query_temp(\"wqa's room/luck\")  0 ) {                        // 运气坏时，每次走的点数一点至二点                        message_vision( HIR $N霉运当头，每次最多只能走两格！\\n NOR, me);                        me-add_temp(\"wqa's room/luck\", 1);                        i = random(2) + 1;                }                message(\"vision\", CYN \"可是....\" + me->name()                         + \"已经不见了。\\n\" NOR, this_object(), ({me}));                go_num = now_num + i;                if( go_num > 80 ) go_num = go_num % 80;        // tell_object( me, CYN \"你现在的位置是：第\" + chinese_number(now_num) + \"格。\\n\" NOR );                // me->move( str0..<3 +sprintf(\"%c%c\", '0' + go_num / 10, '0' + go_num % 10) );                moveto_spec_position(me, go_num);                tempstr = CYN \"\\n$N穿越时空，来到第\" + chinese_number(go_num)                         + \"格。\\n\" NOR;''me->set_temp(\"wqa's room/change\",0);                message_vision(tempstr, me);        }        return;                }","author":"赵云","time":921377878,]),(["title":"ten 关于梦幻时空的改动","msg":"    ten 调试到梦幻时空的清理标志有问题并提出改正文件\"/std/wqa_room.c\"，通过!","author":"孔雀","time":921408529,]),(["title":"Micro 关于效率降低的改!","msg":"改动 /feature/command.c 以解决\"连续打 #10 ulook 的命令，只要那个U方向不存在，速度马上大降\"的问题","author":"孔雀","time":921618860,]),(["title":"re 关于效率降低的改！","msg":"效果好了一些，但仍然不快，而且打了许多命令后好长一段时间内不能行动。你的动作还没有完成，不能移动。你的动作还没有完成，不能移动。你的动作还没有完成，不能移动。","author":"赵云","time":921629107,]),(["title":"关于梦幻时空的小问题","msg":"又改了两个小地方，一个是使用卡片不会再出错，第二个是晕倒了后不能再玩。不过还有一个问题，醒后无法看到提出示信息，不知该从何处改.","author":"赵云","time":921704890,]),(["title":"Micro 关于效率降低的再改动!","msg":"更改了 /feature/action.c 和 command.c 会更合理一些吧。这两次是一种被动的改法，主要使玩家不要键入太多的错误命令。大家看看还有无好的想法。现在真想把 es2-mudlib 丢到天边去。","author":"孔雀","time":921781790,]),(["title":"sigh","msg":"改了以后，好象.....差不多，走错方向两次就会出一行“你的动作还没有完成，不能移动。”chat* faint得空我也看看那两个文件，也许...","author":"赵云","time":921890017,]),(["title":"re: 10","msg":"  the first error, delay 2 seconds  so, when the second error, user is in busy  then, can not move. no error.","author":"张三丰","time":921935195,]),(["title":"re 11","msg":"chat* shake micro这个两秒实在不见得，刚才试了几次，有时几分钟动弹不得，虽然最后还是能动..我想会不会有这种可能：叛断的时候，将delay的时间加大了...","author":"赵云","time":921958681,]),(["title":"关于种族的再改动","msg":"    就 Micro 提出的解决因增加种族而影响 title 的问题作了修正，改动了下面一些文件：        /adm/daemons/chard.c        /adm/daemons/combatd.c        /adm/daemons/logind.c        /cmds/std/look.c        /cmds/skill/apprentice.c        /std/room/wqa_room.c","author":"孔雀","time":922154501,]),(["title":"wqa_room & new command","msg":"    Ten 再次修改\"/std/room/wqa_room.c\"的bug,以更新。    找到了一个可以在 LP Mud 里用的\"vi\"命令代码，装上等待测试！改动的文件有：        \"/include/globals.h\"        \"/cmds/arch/vi.c\"        \"/feature/vi.c\"我的感觉一般，不知各位如何看？","author":"孔雀","time":922586419,]),}),"no_get":1,"location":"/d/wizard/wizard_room",])
