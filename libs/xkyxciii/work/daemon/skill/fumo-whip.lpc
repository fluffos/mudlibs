// fumo-whip.c - 伏魔鞭法

inherit SKILL;

mapping *action = ({
  ([ "action": "$N错步上前，一招「烈火腾云」，手中$w抖得笔直向$n的$l连刺数鞭",
    "force": 100,
    "dodge": 10,
    "parry": 10,
    "damage": 10,
    "lvl": 0,
    "skill_name": "烈火腾云",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N身形一转，一招「风满长空」，手中$w幻出满天鞭影，虚虚实实地袭向$n的$l",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 20,
    "lvl": 10,
    "skill_name": "风满长空",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N纵身轻轻跃起，一招「草深雾泽」，虚虚实实，手中$w连扫$n的三路要害",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 30,
    "lvl": 20,
    "skill_name": "草深雾泽",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「雷震四方」，手中$w幻出满天鞭影，连扫$n的几路要害",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 40,
    "lvl": 30,
    "skill_name": "雷震四方",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N手中一紧，一招「狂风骤雨」，手里$w一鞭接着一鞭不停地抽向$n的$l",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 50,
    "lvl": 40,
    "skill_name": "狂风骤雨",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N潜运真力，力贯$w，一招「雪花翩翩」，点点鞭影飘浮不定地点向$n的$l",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 60,
    "lvl": 50,
    "skill_name": "雪花翩翩",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N手中$w一抖，一招「天目昭辉」，以一个怪异的角度往$n的$l卷去",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 60,
    "skill_name": "天目昭辉",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N全身关节啪啪作响，一招「水到渠成」，手中$w狂风骤雨般地击向$n的$l",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 70,
    "skill_name": "水到渠成",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N将身一幌，手中$w一招「山高林密」，$n只觉得全身被笼罩在一团鞭影之中",
    "force": 260,
    "dodge": 80,
    "parry": 80,
    "damage": 120,
    "lvl": 80,
    "skill_name": "山高林密",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N腾空而起，手中$w一抖，使出一招「金刚伏魔」，如山鞭影，疾疾压向$n",
    "force": 280,
    "dodge": 90,
    "parry": 90,
    "damage": 140,
    "lvl": 100,
    "skill_name": "金刚伏魔",
    "damage_type": "瘀伤"
  ]),
});

int valid_enable(string usage) { return (usage == "whip") || (usage == "parry"); }

int valid_learn(object me) {
  object ob;
  int level;

  if ((int)me->query("max_force") < 200)
    return notify_fail("你的内力不够。\n");
  if (!(ob = me->query_temp("weapon"))
    || (string)ob->query("skill_type") != "whip")
    return notify_fail("你必须先找一条鞭才能练伏魔鞭法。\n");
  if ((int)me->query_skill("buddhism") < (int)me->query_skill("fumo-whip") / 2)
    return notify_fail("佛门每种绝技都要有慈悲佛法为之化解，你好象学得不够呀！\n");
  level = me->query_skill("fumo-whip");
  if (level * level * level / 10 > me->query("SEN"))
    return notify_fail("修练伏魔鞭法，要除暴安良，行侠仗义。你好象做得不够呀！\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("fumo-whip", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  object weapon;

  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "whip")
    return notify_fail("你使用的武器不对。\n");
  if ((int)me->query("kee") < 50)
    return notify_fail("你的体力不够练伏魔鞭法。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍伏魔鞭法。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "fumo-whip/" + action;
}

int enable_factor() { return 190; }

int learn_factor(int n) {
  if (n < 100) return 120;
  else return 100;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 1; }

int practice_factor() { return 100; }
int practice_bonus() { return 1; }
