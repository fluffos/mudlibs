// yizhi-chan.c - 一指禅

inherit SKILL;

mapping *action = ({
([	"action" : "$N侧身抬臂，右指划了个半圈，一式「瓶花落砚」击向$n的$l",
	"force"  	: 50,
        "dodge"  	: -10,
        "parry"  	: -10,
	"damage" 	: 5,
	"lvl"    	: 0,
	"skill_name"  	: "瓶花落砚",
    "damage_type"   : "刺伤"
]),
([	"action" : "$N左掌虚托，一式「寒梅吐蕊」，右指穿腋疾出，指向$n的胸前",
	"force"  	: 80,
        "dodge"  	: -5,
        "parry"  	: -5,
	"damage" 	: 10,
	"lvl"    	: 10,
	"skill_name"  	: "寒梅吐蕊",
    "damage_type"   : "刺伤"
]),
([	"action" : "$N俯身斜倚，左手半推，右手一式「初杏问酒」，向$n的$l划过",
	"force"  	: 100,
        "dodge"  	: 1,
        "parry"  	: 1,
	"damage" 	: 20,
	"lvl"    	: 20,
	"skill_name"  	: "初杏问酒",
    "damage_type"   : "刺伤"
]),
([	"action" : "$N双目微睁，一式「露草斜阳」，双手幻化出千百个指影，拂向$n的$l",
	"force"  	: 120,
        "dodge"  	: 10,
        "parry"  	: 10,
	"damage" 	: 30,
	"lvl"    	: 30,
	"skill_name"  	: "露草斜阳",
    "damage_type"   : "刺伤"
]),
([	"action" : "$N一式「仰月承霖」，左掌护住丹田，右手斜指苍天，蓄势点向$n的$l",
	"force"  	: 140,
        "dodge"  	: 20,
        "parry"  	: 20,
	"damage" 	: 40,
	"lvl"    	: 40,
	"skill_name"  	: "仰月承霖",
    "damage_type"   : "刺伤"
]),
([	"action" : "$N双掌平托胸前，十指叉开，一式「叶底留莲」，指向$n的周身大穴",
	"force"  	: 160,
        "dodge"  	: 30,
        "parry"  	: 30,
	"damage" 	: 50,
	"lvl"    	: 50,
	"skill_name"  	: "叶底留莲",
    "damage_type"   : "刺伤"
]),
([	"action" : "$N双掌翻飞，一式「清风拂桂」，指端迸出无数道劲气，射向$n的全身",
	"force"  	: 180,
        "dodge"  	: 40,
        "parry"  	: 40,
	"damage" 	: 60,
	"lvl"    	: 60,
	"skill_name"  	: "清风拂桂",
    "damage_type"   : "刺伤"
]),
([	"action" : "$N一式「菊圃秋霜」，并指如刃，一束束锐利无俦的刀气，凌虚向$n的$l砍去",
	"force"  	: 200,
        "dodge"  	: 50,
        "parry"  	: 50,
	"damage" 	: 70,
	"lvl"    	: 70,
	"skill_name"  	: "菊圃秋霜",
    "damage_type"   : "刺伤"
]),
([	"action" : "$N一式「伽叶微笑」，双手食指交叉，指端射出一缕罡气，穿过$n的$l",
	"force"  	: 220,
        "dodge"  	: 60,
        "parry"  	: 60,
	"damage" 	: 80,
	"lvl"    	: 80,
	"skill_name"  	: "伽叶微笑",
    "damage_type"   : "刺伤"
]),
([	"action" : "$N左掌竖立胸前，一式「佛祖拈花」，右手食指扣住拇指，轻轻对着$n一弹",
	"force"  	: 240,
        "dodge"  	: 70,
        "parry"  	: 70,
	"damage" 	: 100,
	"lvl"    	: 100,
	"skill_name"  	: "佛祖拈花",
    "damage_type"   : "刺伤"
]),
});

int valid_enable(string usage) { return (usage == "unarmed") || (usage == "parry"); }

int valid_learn(object me)
{
        int level;

	if ((int)me->query("max_force") < 100)
		return notify_fail("你的内力不够。\n");
	if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
        return notify_fail("练一指禅必须空手。\n");
	if( (int)me->query_skill("buddhism") < (int)me->query_skill("yizhi-chan") / 2 )
		return notify_fail("佛门每种绝技都要有慈悲佛法为之化解，你好象学得不够呀！\n");
        level= me->query_skill("yizhi-chan");
        if (level * level * level / 10 > me->query("SEN") )
                return notify_fail("修练一指禅，要除暴安良，行侠仗义。你好象做得不够呀！\n");
	return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
    level   = (int) me->query_skill("yizhi-chan",1);
        for(i = sizeof(action); i > 0; i--)
                if(level >= action[i-1]["lvl"]) return action[random(i)];
}

int practice_skill(object me)
{
	if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
        return notify_fail("练一指禅必须空手。\n");
	if ((int)me->query("kee") < 40)
        return notify_fail("你的体力不够练一指禅。\n");
	me->receive_damage("kee", 35);
        write("你按著所学练了一遍一指禅。\n");
	return 1;
}

string perform_action_file(string action)
{
    return __DIR__"yizhi-chan/" + action;
}

int enable_factor() { return 100; }

int learn_factor(int n) { 
   if (n<100) return 120;
   else return 100;
 }
int learn_bonus()  { return 1; }

int study_factor() { return 100; }
int study_bonus()  { return 3; }

int practice_factor() { return 110; }
int practice_bonus()  { return 1; }
