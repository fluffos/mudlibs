// dugu-jianqi.c - 独孤剑气

inherit SKILL;

mapping *action = ({
([      "action" : "$N一式「破剑式」，剑气如风，直刺$n的$l",
        "force"         : 180,
        "dodge"         : 20,
        "parry"         : 30,
        "damage"        : 30,
        "lvl"           : 0,
        "skill_name"    : "破剑式",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N一式「破刀式」，剑气划出一个大弧，平平地向$n的$l挥去",
        "force"         : 200,
        "dodge"         : 30,
        "parry"         : 40,
        "damage"        : 40,
        "lvl"           : 10,
        "skill_name"    : "破刀式",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N向前跃上一步，使出一式「破掌式」，左手剑诀，右手剑气直刺$n的$l",
        "force"         : 220,
        "dodge"         : 40,
        "parry"         : 50,
        "damage"        : 50,
        "lvl"           : 20,
        "skill_name"    : "破掌式",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N右手虚抱成圆，一式「破枪式」，中宫直进，剑气突地刺向$n的$l",
        "force"         : 240,
        "dodge"         : 50,
        "parry"         : 60,
        "damage"        : 60,
        "lvl"           : 30,
        "skill_name"    : "破枪式",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N一式「破索式」，剑气从上至下猛劈，挟爆裂之声骤然斩向$n的$l",
        "force"         : 260,
        "dodge"         : 60,
        "parry"         : 70,
        "damage"        : 80,
        "lvl"           : 40,
        "skill_name"    : "破索式",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N运气施出「破鞭式」，逼出一道混元剑气，闪电般破空射向$n的$l",
        "force"         : 280,
        "dodge"         : 70,
        "parry"         : 80,
        "damage"        : 100,
        "lvl"           : 50,
        "skill_name"    : "破鞭式",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N拔步挺腰，一式「破气式」，剑气沉滞无比，缓缓撞向$n的$l",
        "force"         : 300,
        "dodge"         : 80,
        "parry"         : 90,
        "damage"        : 120,
        "lvl"           : 60,
        "skill_name"    : "破气式",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N双腿半屈半蹲，一式「破箭式」，剑气划出无数圆圈，狂风骤起，笼罩天地",
        "force"         : 350,
        "dodge"         : 90,
        "parry"         : 100,
        "damage"        : 140,
        "lvl"           : 70,
        "skill_name"    : "破箭式",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N一式「总诀式」，虚虚一指，天地为之变色",
        "force"         : 400,
        "dodge"         : 100,
        "parry"         : 110,
        "damage"        : 160,
        "lvl"           : 80,
        "skill_name"    : "总诀式",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N一式「总诀式」，随手一指，无招无式，天地为之变色",
        "force"         : 500,
        "dodge"         : 120,
        "parry"         : 120,
        "damage"        : 180,
        "lvl"           : 100,
        "skill_name"    : "总诀式",
        "damage_type"   : "刺伤"
]),
});

int valid_enable(string usage) { return (usage == "unarmed") || (usage == "sword") || (usage == "parry"); }

int valid_learn(object me)
{
        if ((int)me->query("max_force") < 200)
                return notify_fail("你的内力不够。\n");
        return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
        level   = (int) me->query_skill("dugu-jianqi",1);
        for(i = sizeof(action); i > 0; i--)
                if(level >= action[i-1]["lvl"]) return action[random(i)];
}

int practice_skill(object me)
{
        object weapon;

        if ((int)me->query("kee") < 50)
                return notify_fail("你的体力不够练独孤剑气。\n");
        me->receive_damage("kee", 35);
        write("你按著所学练了一遍独孤剑气。\n");
        return 1;
}

string perform_action_file(string action)
{
        return __DIR__"dugu-jianqi/" + action;
}

int enable_factor() { return 230; }

int learn_factor(int n) { 
   if (n<100) return 105;
   else return 80;
 }
int learn_bonus()  { return 1; }

int study_factor() { return 100; }
int study_bonus()  { return 1; }

int practice_factor() { return 90; }
int practice_bonus()  { return 1; }
