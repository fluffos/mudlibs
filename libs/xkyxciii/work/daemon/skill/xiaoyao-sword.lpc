// xiaoyao-sword.c - 逍遥剑法

inherit SKILL;

mapping *action = ({
([      "action" : "只见$N足下轻点，$w圈转，一招「峰回路转」，$w轻灵飘逸，向$n的$l削落",
        "force"         : 80,
        "dodge"         : 5,
        "parry"         : 5,
        "damage"        : 5,
        "lvl"           : 0,
        "skill_name"    : "峰回路转",
        "damage_type"   : "刺伤"
]),
([      "action" : "只见$N一招「空山鸟语」，$w化作点点寒星，向$n的$l连刺几剑",
        "force"         : 100,
        "dodge"         : 5,
        "parry"         : 5,
        "damage"        : 10,
        "lvl"           : 10,
        "skill_name"    : "空山鸟语",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N身形陡然滑出数尺，一招「御风而行」，剑势如风，手中$w斩向$n的$l",
        "force"         : 120,
        "dodge"         : 10,
        "parry"         : 10,
        "damage"        : 20,
        "lvl"           : 20,
        "skill_name"    : "御风而行",
        "damage_type"   : "割伤"
]),
([      "action" : "$N手中$w中宫直进，一招「旭日东升」，剑锋直出，对准$n的$l刺出一剑",
        "force"         : 140,
        "dodge"         : 20,
        "parry"         : 20,
        "damage"        : 30,
        "lvl"           : 30,
        "skill_name"    : "旭日东升",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N纵身一跃，手中$w一招「金光泻地」轻飘飘地向$n的$l要害掠去",
        "force"         : 160,
        "dodge"         : 30,
        "parry"         : 30,
        "damage"        : 40,
        "lvl"           : 40,
        "skill_name"    : "金光泻地",
        "damage_type"   : "割伤"
]),
([      "action" : "$N手中$w向外一分，轻轻颤动，一招「柳暗花明」反手向$n$l刺去",
        "force"         : 180,
        "dodge"         : 40,
        "parry"         : 40,
        "damage"        : 50,
        "lvl"           : 50,
        "skill_name"    : "柳暗花明",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N横剑上前，身形一转，手中$w使一招「空谷秋虹」画出一道光弧斩向$n的$l",
        "force"         : 200,
        "dodge"         : 50,
        "parry"         : 50,
        "damage"        : 60,
        "lvl"           : 60,
        "skill_name"    : "空谷秋虹",
        "damage_type"   : "割伤"
]),
([      "action" : "$n只觉眼前一花，$N一招「紫气东来」，$w挟呼呼风声迅猛刺向$n的$l",
        "force"         : 220,
        "dodge"         : 60,
        "parry"         : 60,
        "damage"        : 80,
        "lvl"           : 70,
        "skill_name"    : "紫气东来",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N一招「春花秋月」，剑锋平指，剑势如风，手中$w直刺$n的$l",
        "force"         : 240,
        "dodge"         : 70,
        "parry"         : 70,
        "damage"        : 100,
        "lvl"           : 80,
        "skill_name"    : "春花秋月",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N身形一转，一招「逍遥自在」，$w剑光不定，招招刺向$n的$l要害",
        "force"         : 260,
        "dodge"         : 80,
        "parry"         : 80,
        "damage"        : 120,
        "lvl"           : 100,
        "skill_name"    : "逍遥自在",
        "damage_type"   : "刺伤"
]),
});

int valid_enable(string usage) { return (usage == "sword") || (usage == "parry"); }

int valid_learn(object me)
{
        object ob;
        int level;

        if ((int)me->query("max_force") < 150)
                return notify_fail("你的内力不够。\n");
        if( !(ob = me->query_temp("weapon"))
        ||      (string)ob->query("skill_type") != "sword" )
                return notify_fail("你必须先找一把剑才能练逍遥剑法。\n");
        return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
        level   = (int) me->query_skill("xiaoyao-sword",1);
        for(i = sizeof(action); i > 0; i--)
                if(level >= action[i-1]["lvl"]) return action[random(i)];
}

int practice_skill(object me)
{
        object weapon;

        if (!objectp(weapon = me->query_temp("weapon"))
        || (string)weapon->query("skill_type") != "sword")
                return notify_fail("你使用的武器不对。\n");
        if ((int)me->query("kee") < 40)
                return notify_fail("你的体力不够练逍遥剑法。\n");
        me->receive_damage("kee", 35);
        write("你按著所学练了一遍逍遥剑法。\n");
        return 1;
}

string perform_action_file(string action)
{
        return __DIR__"xiaoyao-sword/" + action;
}

int enable_factor() { return 100; }

int learn_factor(int n) { 
   if (n<100) return 115;
   else return 95;
 }
int learn_bonus()  { return 1; }

int study_factor() { return 100; }
int study_bonus()  { return 2; }

int practice_factor() { return 105; }
int practice_bonus()  { return 1; }
