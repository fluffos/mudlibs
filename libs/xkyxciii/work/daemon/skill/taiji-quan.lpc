// taiji-quan.c - 太极拳法

inherit SKILL;
/*
太极拳：揽雀尾、单鞭、提手上势、白鹤亮翅、搂膝拗步、手挥琵琶、野马分鬃、如封似闭
        高探马、双风贯耳、云手、斜飞势、乱环诀、白蛇吐言（有挤、粘、引、按等诀）
*/
mapping *action = ({
([      "action" : "$N拔步挺腰，左手虚按，右手使一招「白蛇吐信」, 向$n的$l击去",
        "force"         : 120,
        "dodge"         : 10,
        "parry"         : 10,
        "damage"        : 20,
        "lvl"           : 0,
        "skill_name"    : "白蛇吐信",
        "damage_type"   : "瘀伤"
]),
([      "action" : "$N向前跨上一步，右手虚划，左手一记「兜档棰」, 双掌击向$n的$l",
        "force"         : 140,
        "dodge"         : 20,
        "parry"         : 20,
        "damage"        : 20,
        "lvl"           : 10,
        "skill_name"    : "兜档棰",
        "damage_type"   : "瘀伤"
]),
([      "action" : "$N双手握拳，分别向前向后划弧，双手运足十成功力，一招「双雷贯耳」打向$n的$l",
        "force"         : 160,
        "dodge"         : 30,
        "parry"         : 30,
        "damage"        : 30,
        "lvl"           : 20,
        "skill_name"    : "双雷贯耳",
        "damage_type"   : "瘀伤"
]),
([      "action" : "$N错步向前，使出一招「揽雀尾」，双手划了个半圈，向$n的胸口平推过去",
        "force"         : 180,
        "dodge"         : 40,
        "parry"         : 40,
        "damage"        : 40,
        "lvl"           : 30,
        "skill_name"    : "揽雀尾",
        "damage_type"   : "瘀伤"
]),
([      "action" : "$N回身拧腰，施出「伏虎式」，左手击向$n的胸口，右手击$n的头部",
        "force"         : 200,
        "dodge"         : 50,
        "parry"         : 50,
        "damage"        : 60,
        "lvl"           : 40,
        "skill_name"    : "伏虎式",
        "damage_type"   : "瘀伤"
]),
([      "action" : "$N一招「左拦扫」，一招「右拦扫」，左一下，右一下，一气呵成，连绵不断",
        "force"         : 220,
        "dodge"         : 60,
        "parry"         : 60,
        "damage"        : 80,
        "lvl"           : 50,
        "skill_name"    : "右拦扫",
        "damage_type"   : "瘀伤"
]),
([      "action" : "只见$N右腿半屈半蹲，一招「如封似闭」，运拳如风，直撩向$n的$l",
        "force"         : 240,
        "dodge"         : 70,
        "parry"         : 70,
        "damage"        : 100,
        "lvl"           : 60,
        "skill_name"    : "如封似闭",
        "damage_type"   : "瘀伤"
]),
([      "action" : "$N回身拧腰，右手虚抱，一招「拨云瞻日」，左手中宫直进，击向$n的$l",
        "force"         : 260,
        "dodge"         : 80,
        "parry"         : 80,
        "damage"        : 120,
        "lvl"           : 70,
        "skill_name"    : "拨云瞻日",
        "damage_type"   : "瘀伤"
]),
([      "action" : "$N向前踏上一步，左手虚划，右手潜运真力，使出一式「指南针」直击$n的$l",
        "force"         : 280,
        "dodge"         : 90,
        "parry"         : 90,
        "damage"        : 140,
        "lvl"           : 80,
        "skill_name"    : "指南针",
        "damage_type"   : "瘀伤"
]),
([      "action" : "$N向前踏上一步，使出「三环套月」，双拳划出三个圆圈，运拳如风，绵绵不断圈向$n",
        "force"         : 300,
        "dodge"         : 100,
        "parry"         : 100,
        "damage"        : 160,
        "lvl"           : 100,
        "skill_name"    : "三环套月",
        "damage_type"   : "瘀伤"
]),
});


int valid_enable(string usage) { return (usage  == "unarmed") || (usage == "parry"); }

int valid_combine(string combo) { return combo=="taiji-sword"; }

int valid_learn(object me)
{
        object ob;
        int level;

        if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
                return notify_fail("练太极拳必须空手。\n");
        if ((int)me->query("max_force") < 200)
                return notify_fail("你的内力不够。\n");
        level= me->query_skill("taiji-quan");
        if (level * level * level/10 > me->query("SEN") )
                return notify_fail("修练太极拳，要除暴安良，行侠仗义。你好象做得不够呀！\n");
        return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
        level   = (int) me->query_skill("taiji-quan",1);
        for(i = sizeof(action); i > 0; i--)
                if(level >= action[i-1]["lvl"]) return action[random(i)];
}

int practice_skill(object me)
{
        if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
                return notify_fail("练太极拳必须空手。\n");
        if ((int)me->query("kee") < 50)
                return notify_fail("你的体力不够练太极拳。\n");
        me->receive_damage("kee", 35);
        write("你按著所学练了一遍太极拳。\n");
        return 1;
}

string perform_action_file(string action)
{
        return __DIR__"taiji-quan/" + action;
}

int enable_factor() { return 120; }

int learn_factor(int n) { 
   if (n<75) return 70;
   else return 115;
 }
int learn_bonus()  { return 1; }

int study_factor() { return 100; }
int study_bonus()  { return 1; }

int practice_factor() { return 95; }
int practice_bonus()  { return 1; }
