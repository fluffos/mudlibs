// dagou-stick.c - 打狗棒法

inherit SKILL;
/*
2     打狗棒法:
      a 如下招数:
        绊, 劈, 缠, 截, 挑, 印, 封, 转  (又称八字诀)
      b 描写可以这样, 例如
        使出打狗棒中的绊字诀, 在对手(名字)脚下一绊;
        使出打狗棒中的截(或劈,缠)字诀,在对手(名字)某部位截(或劈,缠)去.
3     棒法
      a  棒打双犬  天下无狗
      b  武功描述
         按狗低头    一招按狗低头,挥棒向对手某部位萘落
*/

mapping *action = ({
([      "action" : "$N双足轻点，灵如初燕，一招「棒打双犬」，棒声倏然，扫向$n$l",
        "force"         : 120,
        "dodge"         : 10,
        "parry"         : 10,
        "damage"        : 20,
        "lvl"           : 0,
        "skill_name"    : "棒打双犬",
        "damage_type"   : "挫伤"
]),
([      "action" : "$N倏地伸出$w，使出一招「压扁狗背」，棒头搭向$n的$l，轻轻向下按落",
        "force"         : 140,
        "dodge"         : 20,
        "parry"         : 20,
        "damage"        : 20,
        "lvl"           : 10,
        "skill_name"    : "压扁狗背",
        "damage_type"   : "挫伤"
]),
([      "action" : "只见$N手中$w伸出，一招「拨狗朝天」，指天打地，挑向$n的$l",
        "force"         : 160,
        "dodge"         : 30,
        "parry"         : 30,
        "damage"        : 30,
        "lvl"           : 20,
        "skill_name"    : "拨狗朝天",
        "damage_type"   : "挫伤"
]),
([      "action" : "$N手中$w从旁递出，一招「恶狗拦路」，$w侧抖旁缠，向外斜甩，攻向$n",
        "force"         : 180,
        "dodge"         : 40,
        "parry"         : 40,
        "damage"        : 40,
        "lvl"           : 30,
        "skill_name"    : "恶狗拦路",
        "damage_type"   : "挫伤"
]),
([      "action" : "只见$N举起$w，左一招「打草惊蛇」，右一招「拨草寻蛇」，分攻$n两侧",
        "force"         : 200,
        "dodge"         : 50,
        "parry"         : 50,
        "damage"        : 60,
        "lvl"           : 40,
        "skill_name"    : "拨草寻蛇",
        "damage_type"   : "挫伤"
]),
([      "action" : "$N提起$w，一招「棒打狗头」，击向$n的头顶，出手狠辣，正是「打狗棒法」中的高招",
        "force"         : 220,
        "dodge"         : 60,
        "parry"         : 60,
        "damage"        : 80,
        "lvl"           : 50,
        "skill_name"    : "棒打狗头",
        "damage_type"   : "挫伤"
]),
([      "action" : "$N倒提$w，扭身反背，顺势一招「反戳狗臀」，$w从胯底穿出，戳向$n$l",
        "force"         : 240,
        "dodge"         : 70,
        "parry"         : 70,
        "damage"        : 100,
        "lvl"           : 60,
        "skill_name"    : "反戳狗臀",
        "damage_type"   : "挫伤"
]),
([      "action" : "只见$N将身一幌，手中$w一招「关门打狗」对着$n的$l一连打出七棒",
        "force"         : 260,
        "dodge"         : 80,
        "parry"         : 80,
        "damage"        : 120,
        "lvl"           : 70,
        "skill_name"    : "关门打狗",
        "damage_type"   : "挫伤"
]),
([      "action" : "$N将身一幌，，手中$w一招「天下无狗」，$n只觉得全身被笼罩在一团棒影之中。",
        "force"         : 280,
        "dodge"         : 90,
        "parry"         : 90,
        "damage"        : 140,
        "lvl"           : 80,
        "skill_name"    : "天下无狗",
        "damage_type"   : "挫伤"
]),
([      "action" : "$N腾空而起，手中$w一抖，使出一招「天下无狗」，如山棍影，疾疾压向$n",
        "force"         : 300,
        "dodge"         : 100,
        "parry"         : 100,
        "damage"        : 160,
        "lvl"           : 100,
        "skill_name"    : "天下无狗",
        "damage_type"   : "挫伤"
]),
});

int valid_enable(string usage) { return (usage == "stick") || (usage == "parry"); }

int valid_learn(object me)
{
        object ob;

        if ((int)me->query("max_force") < 200)
                return notify_fail("你的内力不够。\n");
        if( !(ob = me->query_temp("weapon"))
        ||      (string)ob->query("skill_type") != "stick" )
                return notify_fail("你必须先找一根棒才能练打狗棒法。\n");
        return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
        level   = (int) me->query_skill("dagou-stick",1);
        for(i = sizeof(action); i > 0; i--)
                if(level >= action[i-1]["lvl"]) return action[random(i)];
}

int practice_skill(object me)
{
        object weapon;

        if (!objectp(weapon = me->query_temp("weapon"))
        || (string)weapon->query("skill_type") != "stick")
                return notify_fail("你使用的武器不对。\n");
        if ((int)me->query("kee") < 50)
                return notify_fail("你的体力不够练打狗棒法。\n");
        me->receive_damage("kee", 35);
        write("你按著所学练了一遍打狗棒法。\n");
        return 1;
}

string perform_action_file(string action)
{
        return __DIR__"dagou-stick/" + action;
}

int enable_factor() { return 200; }

int learn_factor(int n) { 
   if (n<100) return 105;
   else return 85;
 }
int learn_bonus()  { return 1; }

int study_factor() { return 100; }
int study_bonus()  { return 1; }

int practice_factor() { return 95; }
int practice_bonus()  { return 1; }
