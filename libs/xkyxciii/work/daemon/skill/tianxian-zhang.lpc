// tianxian-zhang - 天仙散花掌

inherit SKILL;

mapping *action = ({
([      "action" : "只见$N一声大喝，一招「鸟语花香」，双掌飘移不定，击向$n$l",
        "force"         : 100,
        "dodge"         : 10,
        "parry"         : 10,
        "damage"        : 10,
        "lvl"           : 0,
        "skill_name"    : "鸟语花香",
        "damage_type"   : "瘀伤"
]),
([      "action" : "$N倏忽欺至$n身前，一招「两小无猜」，左手虚抱，右手$w斜斜地划向$n的$l要害",
        "force"         : 120,
        "dodge"         : 10,
        "parry"         : 10,
        "damage"        : 20,
        "lvl"           : 10,
        "skill_name"    : "两小无猜",
        "damage_type"   : "瘀伤"
]),
([      "action" : "$N身形绕$n一转，只见$N一招「情意缠绵」，手掌轻轻地向$n的$l要害掠去",
        "force"         : 140,
        "dodge"         : 20,
        "parry"         : 20,
        "damage"        : 30,
        "lvl"           : 20,
        "skill_name"    : "情意缠绵",
        "damage_type"   : "瘀伤"
]),
([      "action" : "$N向前跃上一步，身形一转，一招「逍遥自在」，掌势如风，击向$n的$l要害",
        "force"         : 160,
        "dodge"         : 30,
        "parry"         : 30,
        "damage"        : 40,
        "lvl"           : 30,
        "skill_name"    : "逍遥自在",
        "damage_type"   : "瘀伤"
]),
([      "action" : "$N将手划了一圈，一招「共醉温柔」，$n只觉得全身被笼罩在掌风之中。",
        "force"         : 180,
        "dodge"         : 40,
        "parry"         : 40,
        "damage"        : 50,
        "lvl"           : 40,
        "skill_name"    : "共醉温柔",
        "damage_type"   : "瘀伤"
]),
([      "action" : "$N一招「春花多媚」，掌势如风，对着$n的$l一连击出三掌",
        "force"         : 200,
        "dodge"         : 50,
        "parry"         : 50,
        "damage"        : 60,
        "lvl"           : 50,
        "skill_name"    : "春花多媚",
        "damage_type"   : "瘀伤"
]),
([      "action" : "$N向前跃上一步，使出一式「春鸟多哀」直击$n的$l",
        "force"         : 220,
        "dodge"         : 60,
        "parry"         : 60,
        "damage"        : 80,
        "lvl"           : 60,
        "skill_name"    : "春鸟多哀",
        "damage_type"   : "瘀伤"
]),
([      "action" : "只见$N一招「春风多情」，双掌轻灵飘逸，如风一般卷向$n的$l",
        "force"         : 240,
        "dodge"         : 70,
        "parry"         : 70,
        "damage"        : 100,
        "lvl"           : 70,
        "skill_name"    : "春风多情",
        "damage_type"   : "瘀伤"
]),
([      "action" : "但见$N手中$w翩跹不定，一招「吹我罗裳」对着$n的$l一连击出七掌",
        "force"         : 260,
        "dodge"         : 80,
        "parry"         : 80,
        "damage"        : 120,
        "lvl"           : 80,
        "skill_name"    : "吹我罗裳",
        "damage_type"   : "瘀伤"
]),
([      "action" : "只见$N凭空一指，闲雅清隽，一招「衣带渐宽」缓缓击向$n的$l",
        "force"         : 280,
        "dodge"         : 90,
        "parry"         : 90,
        "damage"        : 140,
        "lvl"           : 100,
        "skill_name"    : "衣带渐宽",
        "damage_type"   : "瘀伤"
]),
});

int valid_enable(string usage) { return (usage == "unarmed") || (usage == "parry"); }

int valid_learn(object me)
{
        if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
                return notify_fail("练天仙散花掌必须空手。\n");
        if ((int)me->query("max_force") < 200)
                return notify_fail("你的内力不够。\n");
        return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
        level   = (int) me->query_skill("tianxian-zhang",1);
        for(i = sizeof(action); i > 0; i--)
                if(level >= action[i-1]["lvl"]) return action[random(i)];
}

int practice_skill(object me)
{
        object weapon;

        if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
                return notify_fail("练天仙散花掌必须空手。\n");
        if ((int)me->query("kee") < 50)
                return notify_fail("你的体力不够练天仙散花掌。\n");
        me->receive_damage("kee", 35);
        write("你按著所学练了一遍天仙散花掌。\n");
        return 1;
}

string perform_action_file(string action)
{
        return __DIR__"tianxian-zhang/" + action;
}

int enable_factor() { return 100; }

int learn_factor(int n) { 
   if (n<100) return 110;
   else return 90;
 }
int learn_bonus()  { return 1; }

int study_factor() { return 100; }
int study_bonus()  { return 1; }

int practice_factor() { return 100; }
int practice_bonus()  { return 1; }
