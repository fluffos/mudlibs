// yiyang-zhi - 一阳指

inherit SKILL;

mapping *action = ({
  ([ "action": "$N使一招「一阳指」，双指飘移不定指向$n$l",
    "force": 100,
    "dodge": 10,
    "parry": 10,
    "damage": 10,
    "lvl": 0,
    "skill_name": "一阳指",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N倏忽欺至$n身前，一招「一阳指」直指$n的$l",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 20,
    "lvl": 10,
    "skill_name": "一阳指",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N身形绕$n一转，一招「一阳指」向$n$l接连出指",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 30,
    "lvl": 20,
    "skill_name": "一阳指",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N身形拔起，在半空一招「一阳指」右指猛指$n的$l",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 40,
    "lvl": 30,
    "skill_name": "一阳指",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N使一招「一阳指」，纵起丈余直击$n的$l",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 50,
    "lvl": 40,
    "skill_name": "一阳指",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一声大喝，身形拔起，使出「一阳指」，指指不离$n的$l",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 60,
    "lvl": 50,
    "skill_name": "一阳指",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N双掌一错, 一招「一阳指」，幻出满天指影，指指不离$n的$l",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 60,
    "skill_name": "一阳指",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「一阳指」飘忽若神，双指一前一后，猛地指向$n的胸口",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 70,
    "skill_name": "一阳指",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N向前踏上一步，一招「一阳指」，迅捷无比地指向$n的$l要害！",
    "force": 260,
    "dodge": 80,
    "parry": 80,
    "damage": 120,
    "lvl": 80,
    "skill_name": "一阳指",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一声大喝使出「一阳指」，幻出满天指影，指指不离$n的$l",
    "force": 280,
    "dodge": 90,
    "parry": 90,
    "damage": 140,
    "lvl": 100,
    "skill_name": "一阳指",
    "damage_type": "瘀伤"
  ]),
});

int valid_enable(string usage) { return (usage == "unarmed") || (usage == "parry"); }

int valid_learn(object me) {
  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练一阳指必须空手。\n");
  if ((int)me->query("max_force") < 200)
    return notify_fail("你的内力不够。\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("yiyang-zhi", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  object weapon;

  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练一阳指必须空手。\n");
  if ((int)me->query("kee") < 50)
    return notify_fail("你的体力不够练一阳指。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍一阳指。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "yiyang-zhi/" + action;
}

int enable_factor() { return 120; }

int learn_factor(int n) {
  if (n < 100) return 110;
  else return 90;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 1; }

int practice_factor() { return 100; }
int practice_bonus() { return 1; }
