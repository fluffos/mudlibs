// liangyi-sword.c - 反两仪刀法

inherit SKILL;
/*
反两仪刀法（混沌一破、日月晦明、太乙生萌、两仪合德、万劫不复）
*/
mapping *action = ({
  ([ "action": "只见$N一招「天地定位」，将手中的$w虚虚一抱，刺向$n的$l要害",
    "force": 50,
    "dodge": -10,
    "parry": -10,
    "damage": 5,
    "lvl": 0,
    "skill_name": "天地定位",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N错步向前，一招「山泽通气」，手中的$w直挥向$n的$l",
    "force": 80,
    "dodge": -5,
    "parry": -5,
    "damage": 10,
    "lvl": 10,
    "skill_name": "山泽通气",
    "damage_type": "割伤"
  ]),
  ([ "action": "只见$N一招「雷风相薄」，身子骤然拔高数尺，手中$w自上而下向$n的$l砍去",
    "force": 100,
    "dodge": 1,
    "parry": 1,
    "damage": 20,
    "lvl": 20,
    "skill_name": "雷风相薄",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N大喝一声,手中$w中宫直进，力透刀锋，一招「水火不相射」，对准$n$l猛地砍出一刀",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 30,
    "lvl": 30,
    "skill_name": "水火不相射",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N向前踏上一步，身形一顿，闲雅清隽，一招「八卦相错」对准$n的$l斜斜劈出一刀",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 40,
    "lvl": 40,
    "skill_name": "八卦相错",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N说声：“好！”，双足轻点，灵如初燕，手中$w一招「阴阳相成」砍向$n的$l",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 50,
    "lvl": 50,
    "skill_name": "阴阳相成",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N滴溜溜的一转，手中$w一招「方圆动静」反手对准$n的$l一刀砍去",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 60,
    "lvl": 60,
    "skill_name": "方圆动静",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N错步上前，一招「方圆相成」，全身化作一道刀光，象一颗流星般直飞向$n",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 70,
    "lvl": 70,
    "skill_name": "方圆相成",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N拔步挺腰，一招「天地阴阳」，刀意若有若无，$w淡淡地向$n的$l挥去",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 80,
    "skill_name": "天地阴阳",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N潜运真力，一招「太极生两仪」，手中$w运发真气，闪电般破空射向$n的$l",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 100,
    "skill_name": "太极生两仪",
    "damage_type": "刺伤"
  ]),
});

int valid_enable(string usage) { return (usage == "blade") || (usage == "parry"); }

int valid_learn(object me) {
  object ob;
  int level;

  if ((int)me->query("max_force") < 100)
    return notify_fail("你的内力不够。\n");
  if (!(ob = me->query_temp("weapon"))
    || (string)ob->query("skill_type") != "blade")
    return notify_fail("你必须先找一把刀才能练反两仪刀法。\n");
  level = me->query_skill("fanliangyi-blade");
  if (level * level * level / 10 > me->query("SEN"))
    return notify_fail("修练反两仪刀法，要除暴安良，行侠仗义。你好象做得不够呀！\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("fanliangyi-blade", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  object weapon;

  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "blade")
    return notify_fail("你使用的武器不对。\n");
  if ((int)me->query("kee") < 40)
    return notify_fail("你的体力不够练反两仪刀法。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍反两仪刀法。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "fanliangyi-blade/" + action;
}

int enable_factor() { return 150; }

int learn_factor(int n) {
  if (n < 100) return 120;
  else return 100;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 3; }

int practice_factor() { return 110; }
int practice_bonus() { return 1; }
