// quanzhen-jianfa.c 全真剑法

inherit SKILL;
#include <ansi.h>
mapping *action = ({
  ([ "action": "$N踏奇门，走偏锋，剑尖颤了几颤，一招巧妙无比的" HIG " 分花拂柳 " NOR "，似左实右往$n刺去。",
    "force": 50,
    "dodge": 0,
    "damage": 30,
    "lvl": 0,
    "skill_name": "分花拂柳",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N使一招" HIG " 琴心三叠 " NOR "，身形向上飘起，手中$w虚虚实实幻出三点寒光射向$n的$l刺去!",
    "force": 50,
    "dodge": 5,
    "damage": 40,
    "lvl": 5,
    "skill_name": "琴心三叠",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N身形绕$n一转，猛然一式" HIB " 罡风扫叶 " NOR "，将$w舞出满天剑影，挟着劲风向$n扫去！",
    "force": 60,
    "dodge": 10,
    "damage": 50,
    "lvl": 15,
    "skill_name": "罡风扫叶",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N仰天长笑，身形飘然而起，在半空中身子一旋，一式" HIB " 狂歌痛饮 " NOR "手中$w狂风般地击向$n的$l!",
    "force": 70,
    "dodge": 10,
    "damage": 60,
    "lvl": 20,
    "skill_name": "狂歌痛饮",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N左手捏着剑诀，右足踏开一招" HIC " 定阳针 " NOR "向上斜刺，$w锵然跃出倏的化作几点星光射向$n的$l!",
    "force": 80,
    "dodge": 15,
    "damage": 70,
    "lvl": 30,
    "skill_name": "定阳针",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N向前迈去一大步，使出" HIC " 探海屠龙 " NOR "，手中$w使成一个光球，迅若奔雷击向$n的$l！",
    "force": 100,
    "dodge": 15,
    "damage": 80,
    "lvl": 40,
    "skill_name": "探海屠龙",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N运力一抖$w，一式" HIM " 马蹴落花 " NOR "，$w幻出点点剑花，飘然刺向$n的$l",
    "force": 150,
    "dodge": 20,
    "damage": 90,
    "lvl": 50,
    "skill_name": "马蹴落花",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N手中$w一指，纵起丈余，一招" HIM " 横行漠北 " NOR "，$w雷霆般射向$n的$l!",
    "force": 100,
    "dodge": 15,
    "damage": 60,
    "lvl": 60,
    "skill_name": "横行漠北",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N大喝一声，一招" HIW " 白虹经天 " NOR "，$w闪电般划出一道大圆弧劈向$n的$l!",
    "force": 150,
    "dodge": 0,
    "damage": 70,
    "lvl": 70,
    "skill_name": "白虹经天",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N身形向上飘起，突然一转身，头下脚上，手握剑，一招" HIW " 絮坠无声 " NOR "，无声无息地攻向$n。",
    "force": 200,
    "dodge": 25,
    "damage": 80,
    "lvl": 80,
    "skill_name": "絮坠无声",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N手中$w一抖，一招" HIR " 照破万缘 " NOR "，$w逼出耀眼光芒，奋力击向$n!",
    "force": 250,
    "dodge": 5,
    "damage": 130,
    "lvl": 90,
    "skill_name": "照破万缘",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N忽然身形高高跃起，使出" HIC " 浪" HIW "迹" HIB "天" HIM "涯 " NOR "，$w幻出漫天花瓣，迅如雷霆射向$n的$l！",
    "force": 300,
    "dodge": 15,
    "damage": 145,
    "lvl": 100,
    "skill_name": "浪迹天涯",
    "damage_type": "刺伤"
  ]),
});

int valid_enable(string usage) { return (usage == "sword") || (usage == "parry"); }
int valid_learn(object me) {
  object ob;
  int level;

  if ((int)me->query("max_force") < 100)
    return notify_fail("你的内力不够。\n");
  if (!(ob = me->query_temp("weapon"))
    || (string)ob->query("skill_type") != "sword")
    return notify_fail("你必须先找一把剑才能练全真剑法。\n");
  level = me->query_skill("quanzhen-sword");
  if (level * level * level / 10 > me->query("SEN"))
    return notify_fail("修练全真剑法，要除暴安良，行侠仗义。你好象做得不够呀！\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("quanzhen-sword", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {

  if ((int)me->query("kee") < 40
    || (int)me->query("force") < 3)
    return notify_fail("你的内力或气不够，没有办法练习全真剑法。\n");
  me->receive_damage("kee", 30);
  me->add("force", -3);
  write("你按著所学练了一遍太极剑法。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "quanzhen-sword/" + action;
}

int enable_factor() { return 200; }

int learn_factor(int n) {
  if (n < 75) return 65;
  else return 120;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 1; }

int practice_factor() { return 90; }
int practice_bonus() { return 1; }
