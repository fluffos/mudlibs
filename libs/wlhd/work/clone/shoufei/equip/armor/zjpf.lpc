//edit by rock 2008.9.22
// 自动生成：/make/equip/armor

#include <armor.h>
#include <ansi.h>
inherit ARMOR;

// 函数：所属类别
int query_xy() { return 1 ; }

// 函数：构造处理
void create()
{
        set_name("战将披风",({"zjpf"}));
        set_weight(5000);
        if( clonep() )
        set_default_object(__FILE__);
        else {
                 set("level", 60);            //装备等级
                 set("unit", "件");           //量词
                 set("material", "armor");
                 set("value",28500);             //价值
                 set("armor_prop/armor", 148); //物理防御
             }
        set("max_lasting",19500);   //耐久度

        setup();

        set("gender","男性");    //限制性别
}

#include "/clone/shoufei/long/armor.h"