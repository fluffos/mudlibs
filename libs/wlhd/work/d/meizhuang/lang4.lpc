// edit by jerry@jhfy.nh 2002
#include <room.h>
inherit ROOM;

void create() {
  set("short", "长廊");
  set("long", @LONG
延伸到西方的长廊到这里就走到尽头，西方传来阵阵花香，
那边似乎是一个花园。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "huayuan",
    "east": __DIR__ "lang2",
  ]));
  set("no_clean_up", 0);
  set("objects", ([
  ]));
  setup();
}
