// edit by jerry@jhfy.nh 2002
inherit ROOM;
void create()
{
        set("short", "休息室");
        set("long", @LONG
梅庄的生活一直比较简朴，因为主人的钱财都拿去购买诗词，
书籍了。这里除了一张床以外，什么也没有了。
LONG
        );
        set("no_clean_up", 0);
        set("no_fight", "1");
        set("sleep_room", "1");
        set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"lang2",
]));
        setup();
        replace_program(ROOM);
}

