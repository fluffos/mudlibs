
inherit ROOM;

void create()
{
set("short","入口");
        set("exits", ([
"west"  : __DIR__"taoyuanxiaojing",	
        ]));
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

