inherit ROOM;

void create()
{
	set("short", "奇幻海");
	set("long", @LONG
这是一个五光十色的世界，你在里面迷失了方向，只见一片海洋无边无际，远处一个帆船在远方向你驶来。

LONG
);
	set("exits", ([
"east" : "/d/party/golddrogon/dating.lpc",
		]));
	
	set("objects", ([
		"/clone/room/npc/peilian" : 1,
       ]) );
	
	set("owner", "bigtie7001");
	set("banghui", "golddrogon");
	
	setup();
}

