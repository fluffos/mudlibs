inherit ROOM;

void create()
{
	set("short", "水月阁");
	set("long", @LONG
这里是专供金钱帮帮中弟子练功习武的地方。
这就是他们的家，他们的驻地，一个让他们唯一可以放松一下心情、有安全依赖的处所。

LONG
);
	set("exits", ([
"east" : "/d/party/jinqian/dating.lpc",
		]));
	
	set("objects", ([
		"/clone/room/npc/peilian" : 1,
       ]) );
	
	set("owner", "bigtie7001");
	set("banghui", "jinqian");
	
	setup();
}

