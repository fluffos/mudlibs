inherit ROOM;

void create()
{
	set("short", "止欲");
	set("long", @LONG
每个人都有缺点，欲望主宰着这个世界，一个成功者，也就是欲求相对较少的人。割掉鸡鸡的人，即使没了性欲，也会将欲望转移到更复杂的方面，比如权力——所以，这世界上有一种叫“太监”的职业。

LONG
);
	set("exits", ([
"east" : "/d/party/yuhe/dating.lpc",
		]));
	
	set("objects", ([
		"/clone/room/npc/peilian" : 1,
       ]) );
	
	set("owner", "bigtie7001");
	set("banghui", "yuhe");
	
	setup();
}

