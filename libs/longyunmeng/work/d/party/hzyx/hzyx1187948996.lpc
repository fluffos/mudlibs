inherit ROOM;

void create()
{
	set("short", "天字甲房");
	set("long", @LONG
这里是清帮成员切磋武功的地方
有几个清帮成员正手持兵器在互相拆招切磋

LONG
);
	set("exits", ([
"south" : "/d/party/hzyx/xidajie5.lpc",
		]));
	
	set("objects", ([
		"/clone/room/npc/peilian" : 1,
       ]) );
	
	set("owner", "bigtie7001");
	set("banghui", "hzyx");
	
	setup();
}

