//*****************************************************************************
// Copyright (c) 2017-2026 - Allen Cummings, RealmsMUD, All rights reserved. See
//                      the accompanying LICENSE file for details.
//*****************************************************************************
inherit "/lib/modules/research/researchTree.lpc";

protected string WeaponType = "ERROR";
protected string WeaponSkill = "unarmed";
protected string* ValidWeaponTypes = ({ });

/////////////////////////////////////////////////////////////////////////////
protected void SetupResearch()
{
}

/////////////////////////////////////////////////////////////////////////////
private void FirstLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/blood-lash.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/blood-lash.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/root.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void ThirdLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/sanguine-surge.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/sanguine-surge.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/root.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void FifthLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/hemorrhage.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/hemorrhage.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/blood-lash.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void SeventhLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/vital-feed.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/vital-feed.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/sanguine-surge.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void NinthLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/soul-drain.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/soul-drain.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/blood-lash.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void EleventhLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/dark-potency.lpc", WeaponType));
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/vital-rupture.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/dark-potency.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/vital-feed.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/vital-rupture.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/hemorrhage.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void ThirteenthLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/blood-covenant.lpc", WeaponType));
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/argloths-reserve.lpc", WeaponType));
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/crimson-brand.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/blood-covenant.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/sanguine-surge.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-reserve.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/root.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/crimson-brand.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/vital-rupture.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void FifteenthLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/exsanguinate.lpc", WeaponType));
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/argloths-boon.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/exsanguinate.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/soul-drain.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-boon.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/root.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void SeventeenthLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/corrupting-tide.lpc", WeaponType));
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/blood-shriek.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/corrupting-tide.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/dark-potency.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/blood-shriek.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/crimson-brand.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void NineteenthLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/hemorrhagic-mastery.lpc", WeaponType));
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/argloths-call.lpc", WeaponType));
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/zhardegs-lash.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/hemorrhagic-mastery.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/blood-covenant.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-call.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-reserve.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/zhardegs-lash.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/blood-shriek.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void TwentyFirstLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/crimson-devastation.lpc", WeaponType));
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/argloths-endurance.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/crimson-devastation.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/exsanguinate.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-endurance.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-boon.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void TwentyThirdLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/soul-hunger.lpc", WeaponType));
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/blood-tide.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/soul-hunger.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/dark-potency.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/blood-tide.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/soul-drain.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void TwentyFifthLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/argloths-might.lpc", WeaponType));
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/life-fracture.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-might.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-call.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/life-fracture.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/blood-tide.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void TwentySeventhLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/scarlet-torrent.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/scarlet-torrent.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/blood-tide.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void TwentyNinthLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/argloths-finess.lpc", WeaponType));
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/crimson-precision.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-finess.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-endurance.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/crimson-precision.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/soul-hunger.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void ThirtyFirstLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/sanguine-eruption.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/sanguine-eruption.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/scarlet-torrent.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void ThirtyThirdLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/final-offering.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/final-offering.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/corrupting-tide.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void ThirtyFifthLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/mirosts-ruin.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/mirosts-ruin.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/life-fracture.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void ThirtySeventhLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/argloths-strength.lpc", WeaponType));
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/sanguine-resonance.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-strength.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-finess.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/sanguine-resonance.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/crimson-precision.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void ThirtyNinthLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/argloths-wrath.lpc", WeaponType));
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/argloths-fury.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-wrath.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/crimson-devastation.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-fury.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-might.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void FortyFirstLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/sanguine-mastery.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/sanguine-mastery.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/final-offering.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void FortyThirdLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/blood-weapon-mastery.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/blood-weapon-mastery.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/sanguine-resonance.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void FortyFifthLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/sylthasis-strike.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/sylthasis-strike.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/mirosts-ruin.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void FortySeventhLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/ruinous-potential.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/ruinous-potential.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/sanguine-mastery.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void FiftyFirstLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/covenant-of-corruption.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/covenant-of-corruption.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/blood-weapon-mastery.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
private void FiftySeventhLevel()
{
    addResearchElement(sprintf(
        "/guilds/disciple-of-argloth/paths/%s/blood/blood-obliteration.lpc", WeaponType));

    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/blood-obliteration.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/sylthasis-strike.lpc", WeaponType));
    addChild(
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/blood-obliteration.lpc", WeaponType),
        sprintf("/guilds/disciple-of-argloth/paths/%s/blood/argloths-wrath.lpc", WeaponType));
}

/////////////////////////////////////////////////////////////////////////////
protected void Setup()
{
    Name("Path of Blood");
    Source("Disciple of Argloth");
    Description("This skill provides the Disciple with the knowledge to "
        "channel Argloth's forbidden blood magic through their chosen "
        "weapon, combining martial and sanguine power.");
    SetupResearch();

    if (WeaponType && WeaponType != "ERROR")
    {
        addResearchElement(sprintf(
            "/guilds/disciple-of-argloth/paths/%s/blood/root.lpc", WeaponType));
        TreeRoot(sprintf(
            "/guilds/disciple-of-argloth/paths/%s/blood/root.lpc", WeaponType));

        FirstLevel();
        ThirdLevel();
        FifthLevel();
        SeventhLevel();
        NinthLevel();
        EleventhLevel();
        ThirteenthLevel();
        FifteenthLevel();
        SeventeenthLevel();
        NineteenthLevel();
        TwentyFirstLevel();
        TwentyThirdLevel();
        TwentyFifthLevel();
        TwentySeventhLevel();
        TwentyNinthLevel();
        ThirtyFirstLevel();
        ThirtyThirdLevel();
        ThirtyFifthLevel();
        ThirtySeventhLevel();
        ThirtyNinthLevel();
        FortyFirstLevel();
        FortyThirdLevel();
        FortyFifthLevel();
        FortySeventhLevel();
        FiftyFirstLevel();
        FiftySeventhLevel();
    }
}
