// FluffOS PORT: placeholder. Nothing in this port actually references a
// CONFIG_* constant from this header (checked: 0 uses in the one file that
// includes it) -- kept as an empty stub purely so the #include succeeds.
