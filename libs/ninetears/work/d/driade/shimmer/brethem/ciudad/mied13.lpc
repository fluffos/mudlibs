/* By Shimmer */

#define CREATOR "shimmer"

#include "../../path.h"

inherit BRETHEM+"cityroom.lpc";

void setup() {
   
   base_desc();
   set_exit_color("cyan");
   set_short("%^BOLD%^Ciudad de Brethem : %^BOLD%^%^BLACK%^Calle del Miedo%^RESET%^");
   long_desc();  

   crea_npcs();

add_exit("noroeste",     BRETHEM + "mied14.lpc","standard");
add_exit("este",         BRETHEM + "mied12.lpc","standard");
add_exit("sur",          BRETHEM + "castros8.lpc","standard");

}
