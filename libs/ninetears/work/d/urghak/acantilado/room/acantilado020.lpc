#include "../path.h"

inherit "/d/urghak/acantilado/room/acantilado_main.lpc";

void setup()
{
  do_desc(1,1);
  add_exit("norte", ACANTILADO+"acantilado019.lpc", "path");
  add_exit("sudoeste", ACANTILADO+"acantilado021.lpc", "path");
}
