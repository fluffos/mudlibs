#include "../path.h"

inherit "/d/urghak/acantilado/room/acantilado_main.lpc";

void setup()
{
  do_desc(1,1);
  add_exit("este", ACANTILADO+"acantilado008.lpc", "path");
  add_exit("oeste", ACANTILADO+"acantilado010.lpc", "path");
}
