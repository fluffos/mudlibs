#include "../path.h"

inherit "/d/urghak/acantilado/room/acantilado_main.lpc";

void setup()
{
  do_desc(1,1);
  add_exit("sur", ACANTILADO+"acantilado020.lpc", "path");
  add_exit("noreste", ACANTILADO+"acantilado018.lpc", "path");
}
