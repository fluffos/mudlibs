#include "../path.h"

inherit "/d/urghak/acantilado/room/acantilado_main.lpc";

void setup()
{
  do_desc(1,1);
  add_exit("arriba", ACANTILADO+"acantilado004.lpc", "path");
  add_exit("oeste", ACANTILADO+"acantilado006.lpc", "path");
}
