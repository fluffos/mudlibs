#include "../path.h"

inherit "/d/urghak/acantilado/room/acantilado_main.lpc";

void setup()
{
  do_desc(1,1);
  add_exit("este", ACANTILADO+"acantilado027.lpc", "path");
  add_exit("noroeste", ACANTILADO+"acantilado029.lpc", "path");
}
