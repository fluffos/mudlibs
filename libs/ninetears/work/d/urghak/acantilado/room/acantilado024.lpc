#include "../path.h"

inherit "/d/urghak/acantilado/room/acantilado_main.lpc";

void setup()
{
  do_desc(1,1);
  add_exit("sudeste", ACANTILADO+"acantilado023.lpc", "path");
  add_exit("oeste", ACANTILADO+"acantilado025.lpc", "path");
}
