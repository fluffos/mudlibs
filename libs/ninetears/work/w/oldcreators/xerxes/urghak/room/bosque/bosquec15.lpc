// Xerxes '02

#include "../../path.h"

inherit BOSQUE+"colina_main.lpc";

void setup()
{
      do_desc(1,1);
      
      add_exit("noroeste", BOSQUE + "bosquec13.lpc", "path");
      add_exit("este", BOSQUE + "bosquec16.lpc", "path");
      add_exit("sur", BOSQUE + "bosquec17.lpc", "path");
      add_exit("sudeste", BOSQUE + "bosquec18.lpc", "path");
	
} /* setup */