名稱：
	destruct() - 摧毀物件。
語法：
	varargs	int destruct(mixed ob, mixed amount);
用法：
	摧毀 ob，ob 可以是物件或是物件陣列，若未指定數量 amount，則會
	摧毀整個 ob，若 ob 為組合物件且有指定數量 amount，則只會扣除
	數量 amount，並不會徹底摧毀 ob 本身，ob 遭徹底摧毀時將會被呼
	叫 remove() 函式。
參考：

作者：
	Clode@RevivalWorld
