call_out(3)           LPC 系統函數           call_out(3)

名稱
     call_out() - 在同物件中, 延後某時間後呼叫其他函數

語法
     void call_out ( string | function fun, int delay,  mixed  arg );

描述
     設定在 <delay> 秒(大至上)後呼叫同物件中的函數 <fun>. 可以傳參數 <arg>.
     注意: 你不能在 call_out() 所呼叫的函數中使用 write(), say() 等,
	   因為 call_out() 所呼叫的函數中, this_player() 設成 NULL.
           要解決此問題, 可以在 driver 的 options.h 裡設定
		THIS_PLAYER_IN_CALL_OUT

請參考
     remove_call_out(3), call_out_info(3)

MudOS                Last change: 5 Sep 1994                    1

