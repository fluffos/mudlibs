/* This	program	is a part of RW	mudlib
 * See /doc/help/copyright for more info
 -----------------------------------------
 * File	  : worm.c
 * Author : Liam@RevivalWorld
 * Date	  : 2003-5-27
 * Note	  : 魚餌
 * Update :
 *  o 2000-00-00  

 -----------------------------------------
 */

#include <ansi.h>
#include <message.h>
#include <inherit.h>

inherit	STANDARD_OBJECT;

int is_bait()
{
	return 1;
}
void create()
{
	set_idname("tsou chip", "土司碎片");
	set_temp("status", HIY"魚餌"NOR);

	if( this_object()->set_shadow_database() ) return;

	set("unit", "片");
	set("value", 2);
	set("mass", 1);
	set("bait", 25);
	set("badsell", 1);

}