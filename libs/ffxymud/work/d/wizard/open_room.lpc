// topkan@xhc
inherit ROOM;

void create()
{
    set("short", "开线房");
    set("long", @LONG
这里就是新月传奇的开线房。
LONG );

    set("exits", ([ 
	"down" : "/d/city/wumiao",
      ]));
    set("no_fight", "1");
    set("no_clean_up", 0);
    setup();

    call_other("/clone/board/opennet_b", "???");
}

