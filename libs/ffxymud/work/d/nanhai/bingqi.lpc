inherit ROOM;

void create() {
  set("short", "兵器房");
  set("long", @LONG
这里是南海剑派的兵器房，四周放这几个兵器架，上面是南海剑派各
弟子平时练剑用的剑。
LONG
  );
  set("exits", ([
    "west": __DIR__ "painei",
  ]));
  set("objects", ([
    "/clone/weapon/gangjian.lpc": 20 ]));

  set("no_fight", 1);

  setup();
}
