// Room: /d/new/liangong13.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create() {
  set("short", "新手乐园");
  set("long", @LONG
这里是狐狸箱子专门为新手设立的学习区,凡是想成为高手的都可以
在这里学到自己想学的东西,不过要先拜师哦!面前的这个小姑娘就是江湖
第一高手,赶快拜吧!怕丢人?老大,现在是你求人家啊,等什么等?真是的,
吱吱歪歪,慢慢吞吞,象个女人,I服了YOU！
LONG
  );
  set("no_fight", 1);
  set("valid_startroom", 1);
  set("no_sleep_room", 1);
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "nandajie1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/qqing": 1,
  ]));
  set("no_new_clean_up", 0);

  setup();
}
