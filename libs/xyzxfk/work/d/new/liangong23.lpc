// Room: /d/new/liangong23.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create() {
  set("short", "听风阁");
  set("long", @LONG
远远的江湖上传来一阵歌声:"苍天笑,不再寂寥,豪情仍在痴痴笑笑."这里是听雨轩帮主小四同志的私人住所,据说他隐居与此已有多年.
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "xidajie1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/xiaosi": 1,
  ]));
  set("no_new_clean_up", 0);

  setup();
}
