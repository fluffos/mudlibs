#include <dbase.h>
#include <login.h>

int prevent_learn(object me, string skill)
{
	int betrayer;

	if( betrayer = me->query("betrayer") ) {
		if( me->query_skill(skill, 1) >= this_object()->query_skill(skill, 1) - betrayer * 20 ) {
			message_vision("$N︹丁癸$nぃ琌だ獺ヴ砛\琌稱癬$p眖玡璉玵畍ㄆ薄 ...\n",
				this_object(), me );
			command("say 垛 .... 畍毙常毙ㄤ︑絤");
			return 1;
		}
	}

	if( !me->is_apprentice_of(this_object())
	&&	(int)this_object()->query_skill(skill, 1) <= (int)me->query_skill(skill, 1) * 3 ) {
		command("hmm");
		command("pat " + me->query("id"));
		command("say 瘤礛琌и琌獶и罐肚 ....");
		command("say и毙硂ㄇ彩睱セ\ひㄤ临琌т畍厩");
		return 1;
	}

	return 0;
}
