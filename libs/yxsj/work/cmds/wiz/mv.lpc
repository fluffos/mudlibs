// mv.c
 
inherit F_CLEAN_UP;
 
int main(object me, string arg)
{
        string src, dst;
 
        seteuid(geteuid(me));
        if (!arg || sscanf(arg, "%s %s", src, dst)!=2 ) return
        notify_fail("Α: mv <郎> <ヘ夹郎> \n");
 
        src = resolve_path(me->query("cwd"), src);
        dst = resolve_path(me->query("cwd"), dst);
 
        if( !rename(src, dst) )
                write("Ok.\n");
        else
                write("⊿Τì镑弄糶舦\n");
        return 1;
}
 
int help(object me)
{
  write(@HELP
Α : mv <郎> <ヘ夹郎>
 
琵(﹑)э琘郎┪ヘ魁嘿
HELP
    );
    return 1;
}
 
