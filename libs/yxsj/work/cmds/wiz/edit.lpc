// edit.c

#include "/doc/help.h"

inherit F_CLEAN_UP;

int main(object me, string file)
{
	if( !file ) return notify_fail("Αedit <郎>\n");

	if( in_edit(me) ) return notify_fail("竒ㄏノ絪胯竟\n");

	seteuid(geteuid(me));
	ed(resolve_path(me->query("cwd"), file));
	return 1;
}

int help(object me)
{
  write(@HELP
Α : edit <郎>, edit here

ノ钡絬絪胯郎
HELP
    );
    return 1;
}
