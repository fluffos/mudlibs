// cleanup.c

inherit F_CLEAN_UP;

int main(object me, string str)
{
	object ob, where, *ob_list;
	int old_size, i;

	if (!str) return notify_fail("Αcleanup <郎>\n"); 

	str = resolve_path(me->query("cwd"), str);
	ob_list = children(str);
	old_size = sizeof(ob_list);
	ob_list->clean_up();
	for(i=0; i<sizeof(ob_list); i++)
		if( ob_list[i] && clonep(ob_list[i]) && !environment(ob_list[i]) )
			destruct(ob_list[i]);
	ob_list -= ({ 0 });
	printf("睲埃 %d ン\n", old_size - sizeof(ob_list));
	return 1;
}

int help(object me)
{
	write(@HELP
Αcleanup <郎>

癸┮Τ﹚郎ン㊣ clean_up睲埃┮Τ砆狡籹ㄓ琌⊿Τ
砆ンい床アン
HELP
	);
	return 1;
}

