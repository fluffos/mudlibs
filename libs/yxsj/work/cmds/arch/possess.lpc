// possess.c

inherit F_CLEAN_UP;

int main(object me, string arg)
{
	object ob;

	if( !arg || me != this_player(1)) return 0;
	ob = present(arg, environment(me));
	if( !ob || !living(ob))
		return notify_fail("硂柑⊿Τ硂ネ\n");
	if( geteuid(ob) != geteuid(me) )
		return notify_fail( ob->name() + " euid ㎝ぃ礚猭秈︽ō\n");
	if( ob->query_temp("body_ob") )
		return notify_fail( ob->name() + "竒砆ō筁\n");
	message_vision("$NてΘ笵▇苝秈$n砰ず\n", me, ob);
	LOGIN_D->enter_world(me, ob, 1);

	return 1;
}

int help()
{
	write(@TEXT
Αposssess <ヘ夹ネ>

ōネ硂ネゲ斗㎝Τ EUID
ōノ quit 穦ㄓō砰
TEXT
	);
	return 1;
}
