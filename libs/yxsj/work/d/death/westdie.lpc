// gate.c

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", HIW "褂" NOR);
	set("long", 
		"ネづ\n");
	set("exits", ([
	]) );
	set("objects", ([
	]) );
	setup();
	replace_program(ROOM);
}
