// Room: /d/pili/glass/glass.c

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", HIC"痆件挂"NOR);
	set("long", @LONG
硂柑獽琌痆件挂いみ琌キら临痷┮よ眖
ǎ┮Τ痆件挂凝礚框ぃ秤Μτ
笵癌ヘ睲玊肅フ緑禬蔼ミ浓Λ春
陪淮秇︑ぃ窽眖み柑ネ癬春ヵぇ薄
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/mastersu" : 1,
]));
	set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"road",
  "west" : __DIR__"bridge",
]));
	set("valid_startroom", 1);
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
