// bulletsb.c

inherit ITEM;

void create() {
  set_name("玄指神功\秘笈", ({ "bullets force book", "book", "bullets", "force", "bullets force" }));
  set_weight(600);
  set("unit", "本");
  set("long",
    "這是一本由素還真所寫，介紹他成名武功\－玄指神功\－的秘笈。\n"
    "你可以從上面學到一點有關玄指神功\的奧秘。\n");
  set("value", 100000);
  set("skill", ([
    "name": "bullets",  // name of the skill
    "exp_required": 50000,   // minimum combat experience required
    // to learn this skill.
    "sen_cost": 40,    // sen cost every time study this
    "difficulty": 30,    // the base int to learn this skill
    // modify is gin_cost's (difficulty - int)*5%
    "max_skill": 100    // the maximum level you can learn
    // from this object.
  ]));
}
