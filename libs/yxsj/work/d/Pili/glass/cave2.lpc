// Room: /d/pili/glass/cave2.c

inherit ROOM;

void create()
{
	set("short", "瑌");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"cave3",
  "south" : __DIR__"cave1",
]));
	set("light_up", -1);
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
