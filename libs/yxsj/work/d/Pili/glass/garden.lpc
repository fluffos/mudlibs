// Room: /d/pili/glass/garden.c

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", HIG"堕"NOR);
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("outdoors", "1");
	set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"pavilion",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
