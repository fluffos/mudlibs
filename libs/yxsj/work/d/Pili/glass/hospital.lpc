// Room: /d/pili/glass/hospital.c

inherit ROOM;

void create()
{
	set("short", "[1;33m洛繻[2;37;0m");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "enter" : __DIR__"clinic",
  "south" : __DIR__"roadn1",
]));
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/healersu" : 1,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
