// Room: /d/pili/glass/house.c

inherit ROOM;

void create() {
  set("short", "小屋");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "out": __DIR__ "roade4",
    "south": __DIR__ "bookroom",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
