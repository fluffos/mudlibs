// Room: /d/pili/glass/house.c

inherit ROOM;

void create()
{
	set("short", "");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "out" : __DIR__"roade4",
  "south" : __DIR__"bookroom",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
