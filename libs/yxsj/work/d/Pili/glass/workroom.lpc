// Room: /d/pili/glass/workroom.c

inherit ROOM;

void create()
{
	set("short", "盞");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"factory",
  "south" : __DIR__"workrm2",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
