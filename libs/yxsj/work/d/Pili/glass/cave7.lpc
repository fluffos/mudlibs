// Room: /d/pili/glass/cave7.c

inherit ROOM;

void create() {
  set("short", "山洞");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("light_up", -1);
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "cave6",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/stonebrain": 1,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
