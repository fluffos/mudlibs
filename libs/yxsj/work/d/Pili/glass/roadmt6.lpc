// Room: /d/pili/glass/roadmt6.c

inherit ROOM;

void create()
{
	set("short", "隔");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("outdoors", "1");
	set("exits", ([ /* sizeof() == 2 */
  "northup" : __DIR__"lotus",
  "south" : __DIR__"roadmt5",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
