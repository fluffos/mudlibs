// Room: /d/pili/glass/living2f.c

inherit ROOM;

void create()
{
	set("short", "﹡加");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "down" : __DIR__"living",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
