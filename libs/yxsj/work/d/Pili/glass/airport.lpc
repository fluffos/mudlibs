// Room: /d/pili/glass/airport.c

inherit ROOM;

void create() {
  set("short", "起降場");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "roade3",
  ]));
  set("no_clean_up", 0);

  setup();
}
