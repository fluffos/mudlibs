// Room: /d/pili/glass/airport.c

inherit ROOM;

void create()
{
	set("short", "癬初");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"roade3",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
