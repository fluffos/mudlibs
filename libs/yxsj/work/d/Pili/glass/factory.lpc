// Room: /d/pili/glass/factory.c

inherit ROOM;

void create() {
  set("short", "地下工廠");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "workroom",
    "up": __DIR__ "roade3",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
