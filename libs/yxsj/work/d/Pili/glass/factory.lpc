// Room: /d/pili/glass/factory.c

inherit ROOM;

void create()
{
	set("short", "紅");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"workroom",
  "up" : __DIR__"roade3",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
