// Room: /d/pili/glass/ward.c

inherit ROOM;

void create() {
  set("short", "病房");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "clinic",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/patient": 3,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
