// Room: /d/pili/glass/workrm2.c

inherit ROOM;

void create()
{
	set("short", "盞");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"workroom",
  "east" : __DIR__"workrm3",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
