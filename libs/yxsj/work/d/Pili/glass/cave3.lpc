// Room: /d/pili/glass/cave3.c

inherit ROOM;

void create()
{
	set("short", "瑌");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"cave4",
  "south" : __DIR__"cave2",
]));
	set("light_up", -1);
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
