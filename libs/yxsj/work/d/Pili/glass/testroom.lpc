// Room: /d/pili/glass/testroom.c

inherit ROOM;

void create()
{
	set("short", "代刚初");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("outdoors", "1");
	set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"roade3",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
