// Room: /d/pili/glass/cave1.c

inherit ROOM;

void create() {
  set("short", "山洞");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "cave2",
    "out": __DIR__ "roadmt4",
  ]));
  set("light_up", -1);
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
