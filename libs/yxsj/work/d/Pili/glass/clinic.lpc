// Room: /d/pili/glass/clinic.c

inherit ROOM;

void create() {
  set("short", "診療室");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "ward",
    "out": __DIR__ "hospital",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
