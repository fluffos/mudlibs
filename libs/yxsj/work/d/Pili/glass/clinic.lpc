// Room: /d/pili/glass/clinic.c

inherit ROOM;

void create()
{
	set("short", "禘励");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"ward",
  "out" : __DIR__"hospital",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
