// Room: /d/wiz/hall1.c

inherit ROOM;

void create() {
  set("short", "巫師交誼廳");
  set("long", @LONG
這裡是巫師聊天討論的地方﹐如果巫師們要開會的話﹐也多半會選
在這裡舉行﹐往東是巫師會客室，往西是各個巫師的工作室，你有時可
以看到一些蓬頭垢面的工作狂從裡面跑出來。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "east": __DIR__ "entrance",
    "north": __DIR__ "jobroom",
    "west": __DIR__ "road1",
  ]));
  set("no_clean_up", 0);

  setup();
  call_other("/obj/board/wizard_b", "???");
}
