// Room: /d/wiz/sysboard.c

inherit ROOM;

void create() {
  set("short", "系統公告室");
  set("long", @LONG
這裡是逸俠世界的系統公告室﹐四處擺放著各種奇奇怪怪的石雕、器具
與些物品﹐牆上還掛了些東西﹐看起來好像是刑具﹐聽說這邊離逸俠世界的
惡魔島相當近。在室內正中有吊著一塊佈告欄﹐用來公佈一些資訊及一些公
告事項。往南走去就是神仙會客室。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": "/d/wiz/entrance",
  ]));
  set("no_clean_up", 0);

  setup();
  call_other("/obj/board/sys_b", "???");
  replace_program(ROOM);
}
