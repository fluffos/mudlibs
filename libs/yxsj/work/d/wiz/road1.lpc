// Room: /d/wiz/road1.c

inherit ROOM;

void create() {
  set("short", "走廊");
  set("long", @LONG
這裡是各個巫師工作室間的走道，兩旁是各巫師的工作室。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "hall1",
    "north": "/u/d/daniel/workroom",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
