// Room: /d/wiz/road1.c

inherit ROOM;

void create()
{
	set("short", "ǐ碮");
	set("long", @LONG
硂柑琌畍丁ǐ笵ㄢ琌畍
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"hall1",
  "north" : "/u/d/daniel/workroom",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
