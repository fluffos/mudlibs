// Room: /d/school/teach4d.c

inherit ROOM;

void create()
{
	set("short", "粂いみ");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "up" : __DIR__"teach4",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
