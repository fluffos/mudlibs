// Room: /d/school/teach4d.c

inherit ROOM;

void create() {
  set("short", "外語中心");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "up": __DIR__ "teach4",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
