// Room: /d/school/testroom.c

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", HIR"σ初"NOR);
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/techchk" : 1,
]));
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 1 */
  "out" : __DIR__"teach33",
]));

	setup();
	replace_program(ROOM);
}
