// Room: /d/school/office5.c

inherit ROOM;

void create() {
  set("short", "辦公室");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "office6",
    "west": __DIR__ "office4",
  ]));

  setup();
  replace_program(ROOM);
}
