// school_b.c

#include <ansi.h>

inherit BULLETIN_BOARD;

void create()
{
	set_name(HIG"厩ネ掸癘茂"NOR, ({ "school board", "board", "school" }) );
	set("location", "/d/school/guard");
	set("board_id", "school_b");
	set("long", "硂琌厩ネ痙ē狾硂柑痙ē\n" );
	setup();
	set("capacity", 50);
	replace_program(BULLETIN_BOARD);
}
