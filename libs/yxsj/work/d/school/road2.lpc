// Room: /d/school/road2.c

inherit ROOM;

void create() {
  set("short", "小路");
  set("long", @LONG
你正走在一條教學大樓北邊的小路上，路邊種了一些樹木，這條小路是
由紅色人行磚舖成的，這裡平常較少學生經過，走在這裡感到自在悠閒。
南邊的鐵欄杆是為了防止物品掉下及為地下的教室採光用的。往東走去是東
大樓，往西走去是操場，從這裡可以進到廚房。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "west": __DIR__ "road1",
    "south": __DIR__ "ebuild1",
    "enter": __DIR__ "resturan",
  ]));
  set("outdoors", "1");
  set("no_clean_up", 0);

  setup();
}
