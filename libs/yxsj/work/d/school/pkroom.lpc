// Room: /d/school/pkroom.c

inherit ROOM;

void create() {
  set("short", "比賽場");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 1 */
    "up": __DIR__ "resturan",
  ]));

  setup();
  replace_program(ROOM);
}
