// Room: /d/school/pkroom.c

inherit ROOM;

void create()
{
	set("short", "ゑ辽初");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 1 */
  "up" : __DIR__"resturan",
]));

	setup();
	replace_program(ROOM);
}
