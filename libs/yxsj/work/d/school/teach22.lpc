// Room: /d/school/teach22.c

inherit ROOM;

void create()
{
	set("short", "毙厩加加");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 4 */
  "down" : __DIR__"teach2",
  "north" : __DIR__"teach12",
  "enter" : __DIR__"blkmakt",
  "up" : __DIR__"teach23",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
