// Room: /d/school/lab4fn.c

inherit ROOM;

void create()
{
	set("short", "龟喷加加");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"transout",
  "south" : __DIR__"lab4f",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
