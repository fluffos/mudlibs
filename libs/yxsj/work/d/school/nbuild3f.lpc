// Room: /d/school/nbuild3f.c

inherit ROOM;

void create()
{
	set("short", "加加");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 3 */
  "down" : __DIR__"nbuild2f",
  "enter" : __DIR__"classn2",
  "up" : __DIR__"nbuild4f",
]));

	setup();
	replace_program(ROOM);
}
