// Room: /d/school/office7.c

inherit ROOM;

void create()
{
	set("short", "快そ");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"office6",
  "out" : __DIR__"teach3",
]));

	setup();
	replace_program(ROOM);
}
