// Room: /d/school/teach33.c

inherit ROOM;

void create()
{
	set("short", "毙厩加加");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"teach23",
  "enter" : __DIR__"testroom",
]));

	setup();
	replace_program(ROOM);
}
