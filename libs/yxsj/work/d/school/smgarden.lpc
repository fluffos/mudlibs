// Room: /d/school/smgarden.c

#include <ansi.h>

inherit ROOM;

void create() {
  set("short", HIG "小花園" NOR);
  set("long", @LONG
這裡是逸俠中學一塊很少有人來的地方，大家每天都經過這裡，但是都
沒人去注意它。其實，這裡說是花園，還不如說是小樹林，長了一些濃密的
大樹。上體育課時，有時會讓球掉到這裡。
LONG
  );
  set("outdoors", "1");
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "obj/badminto": 3,
    __DIR__ "obj/baktball": 1,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "pground1",
  ]));
  set("no_clean_up", 0);

  setup();
}
