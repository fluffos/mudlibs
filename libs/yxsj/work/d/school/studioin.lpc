// Room: /d/school/studioin.c

#include <room.h>

inherit ROOM;

void create() {
  set("short", "視聽教室儲藏間");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "studio",
  ]));

  setup();
  create_door("out", "木門", "enter", DOOR_LOCKED + DOOR_CLOSED, 1);
}
