// Room: /d/school/studioin.c

#include <room.h>

inherit ROOM;

void create()
{
	set("short", "跌钮毙纗旅丁");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 1 */
  "out" : __DIR__"studio",
]));

	setup();
              create_door("out", "れ", "enter", DOOR_LOCKED+DOOR_CLOSED, 1);
	replace_program(ROOM);
}
