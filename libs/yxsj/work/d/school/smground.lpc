// Room: /d/school/smground.c

inherit ROOM;

void create()
{
	set("short", "獴巨初");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("outdoors", "1");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 4 */
  "east" : __DIR__"bikeplac",
  "north" : __DIR__"ebuild2",
  "west" : __DIR__"road4",
  "south" : __DIR__"trash",
]));

	setup();
	replace_program(ROOM);
}
