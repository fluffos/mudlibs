// Room: /d/school/classn2.c

inherit ROOM;

void create()
{
	set("short", "毙");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 1 */
  "out" : __DIR__"nbuild3f",
]));

	setup();
	replace_program(ROOM);
}
