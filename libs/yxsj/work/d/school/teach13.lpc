// Room: /d/school/teach13.c

inherit ROOM;

void create()
{
	set("short", "毙厩加加");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "down" : __DIR__"teach12",
  "south" : __DIR__"teach23",
]));

	setup();
	replace_program(ROOM);
}
