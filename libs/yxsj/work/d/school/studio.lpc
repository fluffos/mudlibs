// Room: /d/school/studio.c

#include <room.h>

inherit ROOM;

void create() {
  set("short", "視聽教室");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "enter": __DIR__ "studioin",
    "out": __DIR__ "nbuild4f",
  ]));

  setup();
  create_door("enter", "木門", "out", DOOR_LOCKED + DOOR_CLOSED, 1);
}
