// Room: /d/school/studio.c

#include <room.h>

inherit ROOM;

void create()
{
	set("short", "跌钮毙");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "enter" : __DIR__"studioin",
  "out" : __DIR__"nbuild4f",
]));

	setup();
              create_door("enter", "れ", "out", DOOR_LOCKED+DOOR_CLOSED, 1);
	replace_program(ROOM);
}
