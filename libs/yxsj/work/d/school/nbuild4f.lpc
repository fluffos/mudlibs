// Room: /d/school/nbuild4f.c

inherit ROOM;

void create() {
  set("short", "北大樓四樓");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 4 */
    "down": __DIR__ "nbuild3f",
    "west": __DIR__ "lab4f",
    "enter": __DIR__ "studio",
    "up": __DIR__ "nbuild5f",
  ]));

  setup();
  replace_program(ROOM);
}
