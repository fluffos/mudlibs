// Room: /d/school/nbuild4f.c

inherit ROOM;

void create()
{
	set("short", "加加");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 4 */
  "down" : __DIR__"nbuild3f",
  "west" : __DIR__"lab4f",
  "enter" : __DIR__"studio",
  "up" : __DIR__"nbuild5f",
]));

	setup();
	replace_program(ROOM);
}
