// Room: /d/school/gate.c

inherit ROOM;

void create() {
  set("short", "校門口");
  set("long", @LONG
這裡是逸俠中學的校門口，可以看到一扇電動鐵門及小門，門口有站崗
的教官及學生。現在是上學時間，有許多學生在走進校門。從此看入，可以
看到雄偉的教學大樓及操場。門口有一塊黑色的牌子(sign)，上面寫的有字。
從這裡走去便是逸俠中學，北方有一個留言板。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "up": "/d/wiz/entrance",
    "east": __DIR__ "pground1",
    "north": __DIR__ "guard",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/guardstu": 2,
    __DIR__ "npc/armyinst": 1,
  ]));
  set("item_desc", ([ /* sizeof() == 1 */
    "sign": "	牌子上寫著﹕

	    逸
	    俠
	    私
	    立
	    高
	    級
	    中
	    學
	    兼
	  電國補
	  子中校
	  科部

",
  ]));
  set("outdoors", 1);
  set("no_clean_up", 0);
  set("valid_startroom", 1);

  setup();
}
