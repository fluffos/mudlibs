// Room: /d/school/office2.c

inherit ROOM;

void create()
{
	set("short", "快そ");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"office1",
  "south" : __DIR__"office3",
]));

	setup();
	replace_program(ROOM);
}
