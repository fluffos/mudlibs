// Room: /d/school/pground1.c

inherit ROOM;

void create() {
  set("short", "操場");
  set("long", @LONG
這裡是逸俠中學的操場，地面完全用PU舖設，整齊順眼。操場上有一圈
200公尺的跑道，及一些籃球架和排球架。在圍牆邊有種樹，還有一些雙槓。
這裡的南邊有一個小小的花園，從這裡可以進到校友會辦公室。
LONG
  );
  set("outdoors", "1");
  set("exits", ([ /* sizeof() == 5 */
    "east": __DIR__ "pground2",
    "north": __DIR__ "pground6",
    "enter": __DIR__ "soffice",
    "south": __DIR__ "smgarden",
    "west": __DIR__ "gate",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/clearstu": 1,
  ]));
  set("no_clean_up", 0);

  setup();
}
