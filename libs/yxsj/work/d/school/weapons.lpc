// Room: /d/school/weapons.c

#include <room.h>

inherit ROOM;

void create() {
  set("short", "軍械室");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "obj/M16": 1,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "miliroom",
  ]));
  set("no_clean_up", 0);

  setup();
  create_door("out", "鐵門", "enter", DOOR_LOCKED + DOOR_CLOSED, 1);
  replace_program(ROOM);
}
