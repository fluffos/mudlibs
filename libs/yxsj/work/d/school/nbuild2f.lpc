// Room: /d/school/nbuild2f.c

inherit ROOM;

void create() {
  set("short", "北大樓二樓");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 3 */
    "down": __DIR__ "nbuild",
    "enter": __DIR__ "oldcompc",
    "up": __DIR__ "nbuild3f",
  ]));

  setup();
  replace_program(ROOM);
}
