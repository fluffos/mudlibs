// Room: /d/school/nbuild2f.c

inherit ROOM;

void create()
{
	set("short", "加加");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 3 */
  "down" : __DIR__"nbuild",
  "enter" : __DIR__"oldcompc",
  "up" : __DIR__"nbuild3f",
]));

	setup();
	replace_program(ROOM);
}
