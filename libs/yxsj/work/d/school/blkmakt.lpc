// Room: /d/school/blkmakt.c

#include <ansi.h>

inherit MARKET;

void create()
{
	set("short", WHT"堵カ"NOR);
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "out" : __DIR__"teach22",
]));
	set("no_clean_up", 0);

	setup();

}