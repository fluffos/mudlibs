// Room: /d/school/guardb.c

inherit ROOM;

void create()
{
	set("short", "矫");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/guard2" : 1,
]));
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 1 */
  "out" : __DIR__"sidedoor",
]));

	setup();
}

int valid_leave(object me, string dir)
{
	object mbox;

	if( mbox = me->query_temp("mbox_ob") ) {
		tell_object(me, "盢獺絚ユ倒矫\n");
		destruct(mbox);
	}
	return 1;
}