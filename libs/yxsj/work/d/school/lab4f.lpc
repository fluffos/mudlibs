// Room: /d/school/lab4f.c

inherit ROOM;

void create()
{
	set("short", "龟喷加加");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
  "east" : __DIR__"nbuild4f",
  "north" : __DIR__"lab4fn",
  "down" : __DIR__"lab3f",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
