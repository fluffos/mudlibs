// Room: /d/school/lab3f.c

inherit ROOM;

void create()
{
	set("short", "龟喷加加");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "down" : __DIR__"lab2f",
  "up" : __DIR__"lab4f",
]));

	setup();
	replace_program(ROOM);
}
