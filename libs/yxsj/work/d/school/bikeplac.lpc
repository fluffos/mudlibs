// Room: /d/school/bikeplac.c

inherit ROOM;

void create() {
  set("short", "車棚");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "obj/bicycle": 3,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "smground",
  ]));
  set("no_clean_up", 0);

  setup();
}
