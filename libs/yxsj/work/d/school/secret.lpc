// Room: /d/school/secret.c

inherit ROOM;

void create()
{
	set("short", "盞揣いみ");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"office3",
]));
	set("objects", ([ /* sizeof() == 1 */
	  __DIR__"obj/lightswo" : 1,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
