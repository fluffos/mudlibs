#include <ansi.h>
#include <armor.h>

inherit BOOTS;

void create()
{
        set_name(HIM"获"HIG"稻"HIC""HIY"ń"NOR, ({"phoenix shoes", "phoenix", "shoes"}));
        set_weight(600);
        if( clonep() )
        set_default_object(__FILE__);
        else {
set("long","尘获澳πを┮絪籹τΘ綾︹玱皗帝ぃ某艶\n"
"钮弧ウ镑莉眔らń硉\n");
        set("unit", "蛮");
        set("value", 100000);
        set("armor_type", "boots");
        set("armor_prop/dodge", 30);
        set("armor_prop/move", 30);
        set("armor_prop/armor", 30);
        set("armor_prop/attack", 30);
	  set("class", "taoist");
	  set("no_pawn", 1);
        }
        set("wear_msg", HIC"$N$n"HIC"箉眖竲肚ㄓ皚皚穢種钩礛ㄣΤ臘κ硉\n"NOR);
        set("unequip_msg", CYN"$Nр$n"CYN"叉ㄓ竲钩礚猭続莱ㄤㄓ溃τ禴\n"NOR);
        setup();
}
int query_autoload()
{
		return 1;
}
