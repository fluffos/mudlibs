// Room: /u/g/guo/room/3F-3.c

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "[1;35m档芠加[2;37;0m");
	set("long", @LONG

硂柑碞琌ト猌笵皘い程档芠加キ琌ぃ非ヴ︙盯馒单秈
诀盞今秈硂柑吏跌㏄キ礚玱留留谋眔㏄骸
皚猭礚猭贝加徽瞏矪旅帝或妓盞稱璶ǐㄤい玱
Τ碭ㄓ馋代蔼瞏臔矫咀︘隔ō娩临Τㄇ档︹废
铭ㄏ硂よキ睰ㄇ稰

LONG
	);
        set("no_transmit",1);
        set("no_drift",1);
	set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"3F-4",
  "west" : __DIR__"3F-2",
  "south" : __DIR__"3F-2",
  "east" : __DIR__"3F-2",
]));
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/guard3" : 2,
]));

	setup();
}
int valid_leave(object me,string dir)
{
   object guard;
   guard=present("moving",environment(me));

   if ((dir=="north") && guard)
   {
    message_vision(HIY"笵咀玡р咀ㄓ\n"NOR,me);
    return notify_fail("\n");
   }else return 1;
}
