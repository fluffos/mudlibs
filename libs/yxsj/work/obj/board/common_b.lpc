// common_b.c

#include <ansi.h>

inherit BULLETIN_BOARD;

void create()
{
	set_name(HIM"ノ硚痙ē狾"NOR, ({ "board" }) );
	set("location", "/d/school/guard");
	set("board_id", "common_b");
	set("long",	"硂琌ㄑ痙ē癘ㄆ痙ē狾\n" );
	setup();
	set("capacity", 30);
	replace_program(BULLETIN_BOARD);
}
