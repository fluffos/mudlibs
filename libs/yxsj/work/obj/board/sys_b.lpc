// sys_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("╰参そ茂", ({ "system board", "system", "sys" }) );
	set("location", "/d/wiz/sysboard");
	set("board_id", "sys_b");
	set("long",
		"硂琌そ╰参ㄆ兜痙ē狾\n" );
	setup();
	set("capacity", 50);
	replace_program(BULLETIN_BOARD);
}
