// wizard_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("畍痙ē狾", ({ "board" }) );
	set("location", "/d/wiz/hall1");
	set("board_id", "wizard_b");
	set("long",
		"硂琌ㄑ痙ē癘ㄆ痙ē狾\n" );
	setup();
	set("capacity", 50);
	replace_program(BULLETIN_BOARD);
}
