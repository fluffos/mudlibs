// swordsman_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("糃痙ē狾", ({ "board" }) );
	set("location", "/d/snow/schoolhall");
	set("board_id", "swordsman_b");
	set("long",	"硂琌ㄑ糃痙ē癘ㄆ痙ē狾\n" );
	setup();
	set("capacity", 30);
	replace_program(BULLETIN_BOARD);
}
