// wizard_j.c

inherit "/std/jboard";

void create()
{
	set_name("畍秈厨", ({ "job board", "board" }) );
	set("location", "/d/wiz/jobroom");
	set("board_id", "wizard_j");
	set("long",	"硂琌疭痙ē狾ㄑ畍厨︑秈\n"+
	"project 矗穝璸礶 report 矗秈厨 read 弄ず甧 terminate 埃\n"
	 );
	setup();
	replace_program("/std/jboard");
}
