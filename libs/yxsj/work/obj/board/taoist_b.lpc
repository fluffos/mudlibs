// taoist_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("璗痙ē狾", ({ "board" }) );
	set("location", "/d/temple/temple1");
	set("board_id", "taoist_b");
	set("long",
		"硂琌ㄑ璗痙ē癘ㄆ痙ē狾\n" );
	setup();
	set("capacity", 30);
	replace_program(BULLETIN_BOARD);
}
