int rmm(string arg, string file)
{
    if (!file) return notify_fail("你要删除那个档案?\n");
if ( rm(resolve_path(arg, file)) )
return 1;
else
return 0;
}
int mv(string arg, string file)
{
if ( rename(arg, file) )
return 1;
else
return 1;
}
