#include <ansi.h>
inherit F_CLEAN_UP;

int main(object me, string arg)
{
object *users, ob;
int i, user_num;
6c
object *users, ob, link_ob;

if( !me || !wizardp(me) ) return notify_fail("You do not have to priority to use this command.\n");
if( !(users = users()) ) return 0;
user_num = sizeof(users);
for(i = 0;i<user_num;i++){
link_ob = user[i]->query_temp("link_ob);
if(!link_ob) continue;
ip = link_ob->query(
link_ob = 
