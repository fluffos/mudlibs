inherit F_CLEAN_UP;
int main(object me,string arg)
{
   object ob;
ob = LOGIN_D->find_body(arg);
if(!ob) return notify_fail("没有这个人。\n");
 ob->move("/d/jianyu");
 CHANNEL_D->do_channel(me,"wiz",ob->query("name")+"("+ob->query("id")+")"+"被送往海盗船监禁。\n");
return 1;
}
