inherit ROOM;
void create()
{
    set("short", "了望台");
    set("long", @LONG
这里是铁掌帮的了望台，由铁掌帮弟子把守。从这里望下去，可以看
到山下的一切动静。
LONG
    );
    set("exits", ([ /* sizeof() == 2 */
        "eastdown" : __DIR__"shiwu",
    ]));
    set("no_clean_up", 0);
       set("objects", ([
              "/d/tiezhang/npc/duqian" : 1,
              "/d/tiezhang/npc/dahan" : 2,  
      ]));
    set("outdoors", "tiezhang" );
    setup();
    replace_program(ROOM);
}
