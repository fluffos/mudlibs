inherit ROOM;
void create()
{
    set("short", "石屋");
    set("long", @LONG
这里是一座五开间的石屋，东西两厢房隐隐透出灯光，据说这里
是铁掌帮，帮主铁掌水上飘裘千仞就隐居在此，闭门苦练铁掌神功，
有心要在二次华山论剑时夺取“武功天下第一”的荣号！
LONG
    );
    set("exits", ([ /* sizeof() == 4 */
        "west" : __DIR__"liangong",
        "east" : __DIR__"dating",
        "north" : __DIR__"xiaoyuan",
       "westup" : __DIR__"liaowang",
        "eastdown" : __DIR__"shilu2",
    ]));
      set("objects",([
        __DIR__"npc/dahan" : 2,
]));
    set("no_clean_up", 0);
    set("outdoors", "tiezhang" );
    setup();
      "/d/board/tiezhang_b"->foo();
    replace_program(ROOM);
}
