// Room: /u/hanbo/tiezhang
// HANBO 2000/28/08

inherit ROOM;
void create()
{
    set("short", "饭堂");
    set("long", @LONG
这里是一间饭厅，铁掌帮的弟子都在这里进餐，这里还有山里
出的特产又香又甜的弥猴桃。
LONG
    );
    set("exits", ([ /* sizeof() == 4 */
        "east" : __DIR__"xiaoyuan",
]));
    set("objects",([
        __DIR__"obj/mihuotao" : 3,
        __DIR__"obj/cha.lpc" : 2,
]));
    set("no_clean_up", 0);
    set("outdoors", "tiezhang" );
    setup();
    replace_program(ROOM);
}
