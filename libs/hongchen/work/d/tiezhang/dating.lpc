// Room: /u/hanbo/tiezhang
// HANBO 2000/28/08

inherit ROOM;
void create()
{
    set("short", "铁掌帮大厅");
    set("long", @LONG
铁掌帮原本是一个小小帮会, 自上届上官帮主传位给裘千仞之后,
裘千仞青出于蓝, 武功惊人, 而且极有才略, 数年之间把铁掌帮整顿
得好生兴旺, 自从“铁掌歼衡山”一役将衡山派打得一蹶不振之后, 
铁掌水上飘的名头威震江湖。
LONG
    );
    set("exits", ([ /* sizeof() == 2 */
         "west" : __DIR__"shiwu",
]));
    set("no_clean_up", 0);
    set("outdoors", "tiezhang" );
 set("objects", ([
              "/d/tiezhang/npc/shangguan" : 1,  
      ]));

    setup();
    replace_program(ROOM);
}
