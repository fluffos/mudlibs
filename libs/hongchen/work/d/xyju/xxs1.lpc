// /d/xyju/xxs1.c
// Swordman 2001/1/18
#include <ansi.h>
inherit ROOM;
void create()
{
	set("short", HIC"闲逸居"HIM"隐士休息室"NOR);
	set("long", @LONG
这是一间很大的客房，装饰不是十分豪华，
等待着住宿在这里的隐士自己来装修一番也许
效果就不一样了，大家可以自己选，并把自己
认为喜欢的房间描述发给wiz组。
LONG
	);
	set("sleep_room", "1");
	set("no_fight", "1");
        set("hotel",1);
	set("exits", ([
		"out" : __DIR__"lang1",
	]));
 set("outdoors","xyju");
	setup();
}
