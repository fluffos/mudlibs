// /u/zli/npc/beish.o
//created by pl2npc.c

inherit NPC;

void create()
{
    set_name("北双", ({ "bei sh" }) );
    set("gender", "男性");
    set("age", 14);
    set("mud_age", 69360);
    set("birthday", 1007951693);
    set("combat_exp", 129689);
    set("potential", 7776);

    set("qi", 250);
    set("eff_qi", 250);
    set("max_qi", 250);

    set("food", 1830);
    set("water", 1830);

    set("nickname", "[1;32m来也悠然 去也悠然---- 淡出红尘[2;37;0m");
    set("title", "白驼山派第三代弟子");

    set("jing", 150);
    set("eff_jing", 150);
    set("max_jing", 150);

    set("env", ([ /* sizeof() == 4 */
  "no_accept" : 0,
  "prompt" : 0,
  "title" : "none",
  "brief" : 0,
]));

    set("str", 28);
    set("int", 17);
    set("con", 14);
    set("dex", 21);
    set("kar", 34);
    set("per", 17);
    set("neili", 600);

    set("shen", -17);

    set("family", ([ /* sizeof() == 7 */
  "master_id" : "li",
  "title" : "弟子",
  "generation" : 3,
  "family_name" : "白驼山派",
  "enter_time" : 1011104633,
  "privs" : 0,
  "master_name" : "李教头",
]));
    set("betrayer", 2);
    set("weapon", ([ /* sizeof() == 8 */
  "id" : "my sword",
  "lv" : 1,
  "time" : "丁丑年一月三十一日巳时四刻",
  "name" : "[1;34m[2;37;0m",
  "value" : 0,
  "make" : 1,
  "or" : "千年神木",
  "type" : "剑",
]));

    set("MKS", 1);
    set("DIED", 8);

//set skills
    set_skill("force", 42 );
    set_skill("fadai-qigong", 109 );

    setup();
    if (query("gender") == "男性")
        carry_object("/clone/cloth/male"+(1+random(8))+"-cloth")->wear();
    else
        carry_object("/clone/cloth/female"+(1+random(8))+"-cloth")->wear();
}
