
#include <armor.h>

inherit BOOTS;

void create()
{
   set_name("靴子", ({ "boots"}) );
   set_weight(3000);
   if( clonep() )
     set_default_object(__FILE__);
   else {
     set("material", "铁");
     set("unit", "双");
     set("armor_prop/armor", 2);
   }
   setup();
}
