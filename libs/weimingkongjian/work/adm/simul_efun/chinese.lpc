
string chinese_number(int i)
{
	return CHINESE_D->chinese_number(i);
}
string to_chinese(string str)
{
	return CHINESE_D->chinese(str);
}
int is_chinese(string str)
{
	// AGENTS.md §15h: this was a GBK lead-byte range check
	// (str[0] > 160 && str[0] < 255, requiring 2 bytes per char). On this
	// driver str[i] is a Unicode codepoint and strlen() counts characters,
	// so check the CJK Unified Ideographs block directly against a single
	// character instead.
	if( strlen(str)>=1 && str[0] >= 0x4e00 && str[0] <= 0x9fff ) return 1;
	return 0;
}
