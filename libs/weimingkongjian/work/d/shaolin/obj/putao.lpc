// putao.c
inherit ITEM;
inherit F_FOOD;
void create() {
  set_name("葡萄", ({ "putao" }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一颗水灵灵的葡萄。\n");
    set("unit", "颗");
    set("food_remaining", 1);
    set("food_supplay", 5);
  }
}
