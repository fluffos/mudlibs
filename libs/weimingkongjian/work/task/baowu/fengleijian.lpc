// topkan@xhc
inherit ITEM;
#include <ansi.h>
#include "task.h"
void create()
{
      set_name(HIR "风雷锏",({"fenglei jian","jian"}));
        set_weight(80);
        if( clonep() )
                set_default_object(__FILE__);
        else {
          set("long","兵器。\n");
                set("unit", "件");
                set("task_object",1);
           set("owner_name",HIW "邀月");
           set("owner_id", "yao yue");
        }
}

