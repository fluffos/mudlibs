// topkan@xhc
inherit ITEM;
#include "task.h"
#include <ansi.h>
void create()
{
        set_name(HIG "铜锏", ({"tong jian", "jian"}));
        set_weight(80);
        if( clonep() )
                set_default_object(__FILE__);
        else {
    set("long","兵器。\n");
                set("unit", "件");
             set("owner_name",HIR "张三丰");
             set("owner_id", "zhang sanfeng");
	set("task_object",1);
        }
}

