// topkan@xhc
inherit ITEM;
#include "task.h"
#include <ansi.h>
void create()
{
      set_name(HIG "招牌", ({"zhao pai", "pai"}));
        set_weight(80);
        if( clonep() )
                set_default_object(__FILE__);
        else {
         set("long", "当铺的老字号招牌。\n");
                set("unit", "块");
                set("task_object",1);
       set("owner_name",HIW "唐楠");
             set("owner_id","tang nan");
        }
}
