// topkan@xhc
inherit ITEM;
#include "task.h"
#include <ansi.h>
void create()
{
        set_name(HIY "傻帽", ({"sha mao", "cap"}));
        set_weight(80);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("long", "帽子。\n");
                set("unit", "件");
               set("owner_name",HIG "张松溪");
               set("owner_id","zhang songxi");
	set("task_object",1);
        }
}
