// topkan@xhc
inherit ITEM;
#include "task.h"
#include <ansi.h>
void create()
{
     set_name(HIC "神枪", ({"X spear", "spear"}));
        set_weight(80);
        if( clonep() )
                set_default_object(__FILE__);
        else {
    set("long","兵器。\n");
                set("unit", "件");
     set("owner_name",HIY "范骅");
       set("owner_id", "fan ye");
	set("task_object",1);
        }
}

