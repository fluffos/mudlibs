// shanlu13.c
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "山路");
        set("long", @LONG
你走在回雁峰山路上，四望山峰错落，林壑幽深，溪泉飞瀑，云雾缭
绕，不觉心胸大快。高高矗立在西北方的就是衡山七十二峰之首的回
雁峰，东边远眺，云雾缭绕，隐隐听到水响。
LONG);
        set("exits", ([ /* sizeof() == 4 */
           "northwest"  : __DIR__"huiyanfeng",
           "east"  : __DIR__"shuiliandong",
        ]));
        set("outdoors", "henshan");
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

