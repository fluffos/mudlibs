// senlin.c
#include <ansi.h>
#include <room.h>
inherit ROOM;

string *room_name =(
{
	"练功房","密林","树林","中央广场","密室","北大街","南大街","东大街","西大街","长廊","草原","比武场","暗室","山路","小径",
});	

void create()
{
	int i;
	i = random(sizeof(room_name));
//	set("short", "练功房");
	set("short", room_name[i]);
	set("long", @LONG
这里是江湖朋友练功的地方。有几个江湖侠客正在专心致致地练武
，有几个手持兵器,在互相拆招,身法灵动，别的人则坐在地上的蒲团上
苦练内力。
LONG
);
        set("exits", ([          
        ]));
        
        set("outdoors", "flypproom");
        setup();
           
}
/*
int do_check(object me)
{
    object where=this_object();
    object *obj;
    int i;
    obj=deep_inventory(where);
    if (!sizeof(obj))   return 0;
    for (i=0;i<sizeof(obj);i++){
        if (obj[i]==me) continue;
        if (userp(obj[i]))
            return 1;
    }
    return 0;
    
}
*/
void init()
{
    object me=this_player();
	if(!wizardp(me) && me->query("id")!="snow" )
//	me->die();	
//	message_vision("$N一见房间里已经有人了，只好退了出去。\n",me);
	me->move("/d/new/lgting");

}
