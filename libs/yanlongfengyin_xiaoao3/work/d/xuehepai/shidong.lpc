// by mayue

inherit ROOM;
#include <ansi.h>

void create()
{
	set("short", "地下石洞");
	set("long", @LONG
这里是地下河的一处石洞，似乎是人为开凿出来的，四周没有任何出路。
急流水声不时传来。石壁似乎微微发光，镶嵌着点点荧光石，外面是急流，
看似平静，实则暗流汹涌。
LONG
	);

	set("exits", ([

	]));
	set("objects", ([
			"d/xuehepai/npc/gui": 1,
	]));
    

	setup();

}

