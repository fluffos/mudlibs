// wugong_poison.c
// by star  98.12.4
#include <ansi.h>
#include <condition.h>
inherit F_CLEAN_UP;

int update_condition(object me, int duration)
{
//	if(me->query_temp("nopoison")) return 0;
	if( duration < 1 ) return 0;
    if( !living(me) ) 
	{
        message("vision", me->name() + "痛苦地哼了一声。\n", environment(me), me);
    }
	else 
	{
        tell_object(me, "你中的蜈蚣毒发作了。\n");
        message("vision", me->name() + "身上爬着几只蜈蚣。\n", environment(me), me);
    }	
    me->receive_damage("qi",100 + random(me->query("max_qi")/100));
    me->receive_damage("jing", 100+random(me->query("max_jing")/100));
    me->receive_wound("qi",100 + random(me->query("max_qi")/100));
    me->receive_wound("jing", 100+random(me->query("max_jing")/100));
	me->apply_condition("wugong_poison", duration - 1);
    me->set_temp("die_for","中蜈蚣毒");
    me->delete_temp("last_damage_from");
    if( duration < 1 ) return 0;
	tell_object(me, HIG "你中的蜈蚣毒发作了！\n" NOR );
	return 1;
}

string query_type(object me)
{
       return "poison";
}
