//Cracked by Kafei
// by oyxb
// by acep

#include <ansi.h>
#include <condition.h>
inherit F_CLEAN_UP;
string* xuedao = ({
        "巨骨穴",
        "地仓穴",
        "肩井穴",
        "颊车穴",
        "承泣穴",
        "风池穴",
        "章门穴",
        "风府穴",
        "精促穴",
        "陶道穴",
        "强间穴",
        "少海穴",
        "犊鼻穴",
        "神门穴",
        "华盖穴",
        "大椎穴",
        "凤尾穴",
        "至阳穴",
        "劳宫穴",
        "百会穴",
        "灵台穴",
        "太阳穴",
        "膻中穴",
        "命门穴",
        "鸠尾穴",
        "三阴交",
        "天柱穴"
});


int update_condition(object me, int duration)
{
   if( duration < 1 ) return 0;
	if (!living(me)) 
	{
		message("vision", me->name() + "闷哼了一声。\n", environment(me), me);
	}
	else 
	{
        tell_object(me,HIR"你感到"+xuedao[random(sizeof(xuedao))]+"一阵酸麻，血气不畅，顿时动弹不得！\n"NOR);
        message("vision", me->name() + "的身子突然晃了两晃，四肢僵硬无比。\n",environment(me), me);
    }		
  if( !me->is_busy() ) me->start_busy(3);
    me->receive_damage("qi",100 + random(me->query("max_qi")/100));
    me->receive_damage("jing", 100+random(me->query("max_jing")/100));
    me->receive_wound("qi",100 + random(me->query("max_qi")/100));
    me->receive_wound("jing", 100+random(me->query("max_jing")/100));
    me->apply_condition("yyz_damage", duration - 1);
    me->set_temp("die_for","穴位淤塞");
    me->delete_temp("last_damage_from");	
    if( duration < 1 ) return 0;
	return 1;
}

string query_type(object me)
{
       return "hurt";
}
