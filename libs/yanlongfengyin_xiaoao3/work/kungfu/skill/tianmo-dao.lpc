//tianmo-dao 	天魔刀法	上乘武学400~1000
#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>

inherit SKILL;
string *order = ({
  "" HIY "",
  "" HIG "",
  "" RED "",
  "" MAG "",
  "" YEL "",
  "" HIC "",
  "" HIW "",
  "" HIR "",
  "" HIB "",
  "" CYN "",
  "" WHT "",
  "" HIM "",
  "" BLU ""
});

mapping *action = ({
  ([ "action": "$N手中$w一抖，一招" + (order[random(13)]) + "「唯我独尊」" NOR "，一刀向$n的$l砍去",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 0,
    "skill_name": "唯我独尊",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N一招" + (order[random(13)]) + "「万里独行」" NOR "，手中$w一晃，一道刀光射向$n的丹田",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "lvl": 8,
    "skill_name": "万里独行",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N身形急转，一招" + (order[random(13)]) + "「索命无常」" NOR "，手中$w从诡异的角度斩向$n的$l",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "lvl": 16,
    "skill_name": "索命无常",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N一招" + (order[random(13)]) + "「血流成河」" NOR "，$w自上而下，以不可思议速度劈向$n",
    "attack": 700,
    "parry": 700,
    "dodge": 700,
    "damage": 700,
    "force": 700,
    "lvl": 24,
    "skill_name": "血流成河",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N手中$w一挥，一招" + (order[random(13)]) + "「君临天下」" NOR "，拦腰砍向$n",
    "attack": 800,
    "parry": 800,
    "dodge": 800,
    "damage": 800,
    "force": 800,
    "lvl": 33,
    "skill_name": "君临天下",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N使出一招" + (order[random(13)]) + "「天火燎燃」" NOR "，$w化作数十道红光自上而下罩向$n",
    "attack": 900,
    "parry": 900,
    "dodge": 900,
    "damage": 900,
    "force": 900,
    "lvl": 42,
    "skill_name": "天火燎燃",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N一招" + (order[random(13)]) + "「穷途末路」" NOR "，错步向前，$w顺势挟风声劈向$n的$l",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "lvl": 51,
    "skill_name": "穷途末路",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N向上纵起丈余，一招" + (order[random(13)]) + "「群魔乱舞」" NOR "，$w在半空幻作千百刀光电射而下，把$n包围在一片刀影之中",
    "attack": 1000,
    "parry": 1000,
    "dodge": 1000,
    "damage": 1000,
    "force": 1000,
    "lvl": 60,
    "skill_name": "群魔乱舞",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N使出天魔八式中的起手式“万魔初醒”，手中的$w划出一溜光芒斩向$n的$l",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "damage_type": "割伤"
  ]),
  ([ "action": "$N手一抖，一招“魔焰万丈”手中的$w幻成一片刀花，散向$n的$l",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "damage_type": "割伤"
  ]),
  ([ "action": "$N刀锋忽转，手中的$w一招“魔光乍现”，闪电般劈向$n的$l",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "damage_type": "割伤"
  ]),
  ([ "action": "$N身影晃动，刀锋忽左忽右，一招“天魔独尊”，手中的$w，从中路突然砍至$n的$l",
    "attack": 700,
    "parry": 700,
    "dodge": 700,
    "damage": 700,
    "force": 700,
    "damage_type": "砍伤"
  ]),
  ([ "action": "$N的双脚点地，刀背贴身，一招“万刃天魔”全身转出一团刀光滚向$n",
    "attack": 800,
    "parry": 800,
    "dodge": 800,
    "damage": 800,
    "force": 800,
    "damage_type": "割伤"
  ]),
  ([ "action": "$N刀刃向上，一招“天魔回天”，从一个$n意想不到的角度撩向$n的$l",
    "attack": 900,
    "parry": 900,
    "dodge": 900,
    "damage": 900,
    "force": 900,
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N手中的$w左右横划，一招“天魔七现”，来来回回地狂扫向$n的$l",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "damage_type": "割伤"
  ]),
  ([ "action": "$N使出如意天魔连环八式中的收手式“万魔归宗”，手中的$w迅捷无比地砍向$n的$l",
    "attack": 1000,
    "parry": 1000,
    "dodge": 1000,
    "damage": 1000,
    "force": 1000,
    "damage_type": "砍伤"
  ]),
  ([ "action": "$N手中$w一闪，$n面前出现一轮充满魔意的新月......！",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "damage_type": "砍伤"
  ]),
  ([ "action": "$N身形缥缈，手中$w随意一挥，十万神魔顿时涌现在$n眼前！",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "damage_type": "砍伤"
  ]),
  ([ "action": "$N诡笑一声，$w刀光闪动，四面魔气竟然把$n笼罩其中！",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "damage_type": "割伤"
  ]),
  ([ "action": "$N手中$w突然变的血红,$w顿时魔意无限！$n心生惧意，竟然无法闪躲！",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "damage_type": "砍伤"
  ]),
  ([ "action": "$n眼睛一花，血红的$w诡异般出现在$n的喉前！",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "damage_type": "割伤"
  ]),
});

int valid_enable(string usage) { return usage == "blade" || usage == "parry"; }

int valid_combine(string combo) { return combo == "tianmo-jian"; }


int valid_learn(object me) {
  //	if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");
  if (me->query("family/family_name") != "日月神教")
    return notify_fail("你不是本门中人，无法学习本门技能。\n");
  if ((int)me->query("str", 1) < 25)
    return notify_fail("你的臂力不适合学习天魔刀。\n");
  if ((int)me->query("max_neili") < 10000)
    return notify_fail("你的内力不够。\n");
  if ((int)me->query_skill("tianmo-shengong", 1) < 1000)
    return notify_fail("你的天魔神功火候太浅。\n");
  if ((int)me->query_skill("blade", 1) < (int)me->query_skill("tianmo-dao", 1))
    return notify_fail("你的刀法基础太差。\n");
  if ((int)me->query("score", 1) < 10000)
    return notify_fail("你对我派毫无贡献而言，怎么可能传授你武功呢。\n");
  if (random(10) == 1) me->add("score", -10);
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  return action[random(sizeof(action))];  //2017.9.4阿飞改，将招式简单点
}

int practice_skill(object me) {
  object weapon;
  //	if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");		
  if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "blade")
    return notify_fail("你使用的武器不对。\n");
  if ((int)me->query("qi") < 50)
    return notify_fail("你的体力不够练天魔刀。\n");
  if (me->query("family/family_name") != "日月神教")
    return notify_fail("你不是日月神教弟子，不能练习本门武功。\n");
  if ((int)me->query_skill("blade", 1) < (int)me->query_skill("tianmo-dao", 1))
    return notify_fail("你的刀法基础太差。\n");
  me->receive_damage("qi", 25);
  return 1;
}
string perform_action_file(string action) {
  return __DIR__ "tianmo-dao/" + action;
}
/*	//原先天魔系的特效有问题，先和谐掉
mixed hit_ob(object me, object victim, int damage_bonus, int factor)
{
      
	int level, level2, i;
	object weapon;
	string name;
	weapon = me->query_temp("weapon");
	
        level = (int) me->query_skill("tianmo-shengong",1);
        level2= (int) me->query_skill("tianmo-shengong",1);
        if (me->query_temp("weapon"))
	        name = "手中" + weapon->name();
        else
	        name = "双掌";
if (!me->query_temp("action_flag") && random(4)==0 && level>=300 && me->query_skill_mapped("force") == "tianmo-shengong" )
{

	        message_vision(HIW "\n霎时间只见$N" HIW "身子猛摆，顿时化分为无数身影，" + name +
        	               HIW "\n宛若流星般分从不同的方位同时攻向$n" HIW "！\n"
                	       NOR, me, victim);
        me->set_temp("action_flag", 1);
        for (i = 0; i < 3; i++)
        {
                if (! me->is_fighting(victim))
                        break;

                COMBAT_D->do_attack(me, victim, weapon, 0);
        }
        me->delete_temp("action_flag");
}

}
*/
mixed hit_ob(object me, object victim, int damage_bonus) {
  int ap, dp, damage2, level;
  level = (int)me->query_skill("tianmo-shengong", 1);
  ap = (int)me->query("str", 1) * (int)me->query_skill("tianmo-dao", 1);
  dp = (int)victim->query("dex", 1) * (int)me->query_skill("parry", 1);
  damage2 = (int)me->query_skill("tianmo-dao", 1) + (int)me->query_skill("tianmo-shengong", 1);
  if (damage_bonus < 100) return 0;
  if (me->query_skill("tianmo-shengong", 1) < 100) return 0;
  if (me->query("jiali") < 1 || me->query("neili") < 100 || me->query_skill_mapped("force") != "tianmo-shengong") return 0;
  if (damage2 < 100) damage2 = 100;
  if (random(5) == 0 && (int)me->query_skill(
    "tianmo-dao",
    1
  ) > 300 && me->query("neili") > 500 && random(ap) > dp / 10 && me->query("jiali") > 0) {
    me->add("neili", -100);
    victim->receive_wound("qi", damage2 * 2 + damage_bonus, me);
    victim->receive_damage("qi", damage2 * 2 + damage_bonus, me);
    me->receive_damage("qi", level, victim);
    return HIY "$N默运天魔解体大法，一时刀光剑影，$n但见漫天刀光袭来，已是遍体鳞伤。\n" NOR;
  }
}
/*	//原先天魔系的特效有问题，先和谐掉
int ob_hit(object ob, object me, int damage)
{
        string msg;
        int j, ap, dp;
        int level,damage2;
        level = (int) me->query_skill("tianmo-shengong",1);
      
        ap = me->query("neili");
        dp = ob->query("neili");
	damage2= (int) me->query_skill("tianmo-shengong",1);
	if (random(6)==0 && level>=300 && me->query_skill_mapped("force") == "tianmo-shengong" && random(ap)>dp/10)
	{
		if (ob->query("neili") > damage2) ob->add("neili",-damage2);
		if (me->query("neili") < me->query("max_neili")) me->add("neili",damage2);
		msg = HIC "$N" HIC "一声冷笑! 隔空吸功!! " HIC "$n" HIC "全身功力如流水般源源不绝地吸入$N的身体！\n" NOR;
		message_vision(msg, me, ob);
		return j;
	}       
}*/
