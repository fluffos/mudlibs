// wuying-shenquan 	无影神拳	下乘武学100~300
#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
inherit SKILL;

mapping *action = ({
([	"action" : "$N一招「无对」，左掌叠于右掌之上，劈向$n",
		"attack":	100,
		"parry":	100,
		"dodge":	100,
		"damage":	100,
		"force":	100,
	"lvl" : 0,
	"skill_name" : "无对",
	"damage_type" : "瘀伤"
]),
([	"action" : "$N一招「无双」，面色诡异，双掌轻飘飘地拍向$n",
		"attack":	100,
		"parry":	100,
		"dodge":	100,
		"damage":	100,
		"force":	100,
	"lvl" : 20,
	"skill_name" : "无双",
	"damage_type" : "内伤"
]),
([	"action" : "$N一招「无拘」，双掌幻化一片幻影，将$n笼罩于内。",
		"attack":	200,
		"parry":	200,
		"dodge":	200,
		"damage":	200,
		"force":	200,
	"lvl" : 30,
	"skill_name" : "无拘",
	"damage_type" : "瘀伤"
]),

([	"action" : "$N一招「无束」，向$n的$l连击三掌",
		"attack":	200,
		"parry":	200,
		"dodge":	200,
		"damage":	200,
		"force":	200,
	"lvl" : 40,
	"skill_name" : "无束",
    "damage_type" : "瘀伤"
]),
([	"action" : "$N一招「无声」，只见一片掌影攻向$n",
		"attack":	300,
		"parry":	300,
		"dodge":	300,
		"damage":	300,
		"force":	300,
	"lvl" : 40,
	"skill_name" : "无声",
	"damage_type" : "瘀伤"
]),

([	"action" : "$N双掌平挥，不见掌影,不闻掌风,一招「无息」击向$n",
		"attack":	300,
		"parry":	300,
		"dodge":	300,
		"damage":	300,
		"force":	300,
	"lvl" : 60,
	"skill_name" : "无息",
            "damage_type" : "瘀伤"
]),
([	"action" : "$N一招「无影」，只见一片红影罩向$n",
		"attack":	300,
		"parry":	300,
		"dodge":	300,
		"damage":	300,
		"force":	300,
	"lvl" : 70,
	"skill_name" : "无影",
	"damage_type" : "内伤"
]),

([	"action" : "$N左掌虚晃，右掌一记「无踪」击向$n的头部",
		"attack":	300,
		"parry":	300,
		"dodge":	300,
		"damage":	300,
		"force":	300,
	"lvl" : 80,
	"skill_name" : "无踪",
           "damage_type" : "瘀伤"
]),

});

int valid_enable(string usage) { return usage=="unarmed" || usage=="parry"||usage=="cuff"; }


int valid_learn(object me)
{
//		if (!wizardp(me))
//		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
	if (me->query("family/family_name")!="梅庄")
		return notify_fail("你不加入本门，无法学习本门技能。\n");	
    if (me->query_skill("wuzheng-xinfa",1)<200)
        return notify_fail("你的本门内功基础不够。\n");		
	if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
		return notify_fail("练无影神拳必须空手。\n");
	if ((int)me->query("max_neili") < 10000)
		return notify_fail("你的内力太弱，无法练无影神拳。\n");

	return 1;
}

mapping query_action(object me, object weapon)
{
	return action[random(sizeof(action))];	//2017.9.4阿飞改，将招式简单点
}

int practice_skill(object me)
{
//		if (!wizardp(me))
//		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
	if (me->query("family/family_name")!="梅庄")
		return notify_fail("你不加入本门，无法学习本门技能。\n");	
    if (me->query_skill("wuzheng-xinfa",1)<100)
        return notify_fail("你的本门内功基础不够。\n");		
	if ((int)me->query("qi") < 30)
		return notify_fail("你的体力太低了。\n");
	if ((int)me->query("neili") < 20)
		return notify_fail("你的内力不够练无影神拳。\n");
	me->receive_damage("qi", 25);
	me->add("neili", -10);
	return 1;
}

string perform_action_file(string action)
{
	return __DIR__"wuying-shenquan/" + action;
}

mixed hit_ob(object me, object victim, int damage_bonus)
{
	int ap,dp,damage2;
	damage2 = (int)me->query_skill("wuying-shenquan",1);	
	ap = me->query_skill("wuying-shenquan",1);
	dp = victim->query_skill("parry",1);
    if( damage_bonus < 100 ) return 0;
	if ( me->query_skill("wuzheng-xinfa",1) < 100)	 return 0;
	if ( me->query("jiali") < 1 || me->query("neili") < 100 || me->query_skill_mapped("force") != "wuzheng-xinfa") return 0;	
	if (damage2<10) damage2=10;	

	if(	me->query_skill("force",1) >300
	    && random(ap) > dp/10
		&& me->query_skill_mapped("force")=="wuzheng-xinfa"
		&& random(5)==0 
		&& me->query("jiali") > 0)
	{
		victim->receive_damage("qi", damage2+damage_bonus/2 ,me);
		victim->receive_wound("qi", damage2+damage_bonus/2 ,me);
//		if (!victim->is_busy()) victim->start_busy(1);
		me->add("neili", -100);		
		return HIR "$N突然跳开数步远，摆马步，双拳聚于中心，猛的击出！$n被一股无声无息的拳劲击中，吐了一口血！\n" NOR;
	}
}