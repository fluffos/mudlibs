//npc武功，不开放!!		魔法剑	上乘武学400~1000
#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
#include "/kungfu/skill/eff_msg.h"
inherit SKILL;

//inherit F_SSERVER;

string *order = ({
  "" HIY "",
  "" HIG "",
  "" RED "",
  "" MAG "",
  "" YEL "",
  "" HIC "",
  "" HIW "",
  "" HIR "",
  "" HIB "",
  "" CYN "",
  "" WHT "",
  "" HIM "",
  "" BLU ""
});


mapping *action = ({
  ([ "action": "$N打出一道 " HIC "审判之圣雷" NOR " 手中$w向$n的$l直劈过来。",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N打出一道 " HIY "大阳之箭" NOR " 手中$w向$n的$l斜劈过来。",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "damage_type": "割伤"
  ]),
  ([ "action": "$N打出一道 " HIB "暗黑核融术" NOR " 手中的$w划了一个三角，向$n的$l刺了过去。",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N打出一道 " HIW "绝对零冰冻" NOR "手中的$w划了个大圆，向$n的$l刺了过去。",
    "attack": 700,
    "parry": 700,
    "dodge": 700,
    "damage": 700,
    "force": 700,
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N打出一道 " HIM "地狱力场" NOR "手中的$w对着$n的$l旋了过去。",
    "attack": 800,
    "parry": 800,
    "dodge": 800,
    "damage": 800,
    "force": 800,
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N打出一道 " HIR "琰魔焦热地狱" NOR "手中的$w对着$n的$l旋了过去。",
    "attack": 900,
    "parry": 900,
    "dodge": 900,
    "damage": 900,
    "force": 900,
    "damage_type": "刺伤"
  ]),

  ([ "action": "$N打出一道 " HIR "黄泉炼狱" NOR "手中的$w对着$n的$l刺了过去。",
    "attack": 1000,
    "parry": 1000,
    "dodge": 1000,
    "damage": 1000,
    "force": 1000,
    "damage_type": "刺伤"
  ]),
});

int valid_learn(object me) {
  return notify_fail("这种武功无法学习。\n");
}

int valid_enable(string usage) {
  return usage == "sword" || usage == "parry";
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  return action[random(sizeof(action))];  //2017.9.4阿飞改，将招式简单点
}

int practice_skill(object me) {
  if ((int)me->query("qi") < 300
    || (int)me->query("neili") < 300)
    return notify_fail("你的内力或气不够，没有办法练习西洋剑法。\n");
  me->receive_damage("qi", 300);
  me->add("neili", -300);
  //write("你按著所学练了一遍西洋剑法。\n");
  return 1;
}


mixed hit_ob(object me, object victim, int damage_bonus)

{
  int level;
  object weapon;
  level = (int)me->query_skill("magic-sword", 1);
  //        target = me->select_opponent();
  weapon = me->query_temp("weapon");

  if (random(2) == 1 && random(level) > 150) {
    if (userp(victim))
      victim->start_busy(3 + random(2));
    else victim->start_busy(2);
    return HIC "「冰晶术!!」！$N突然念咒,用了一个魔法，$n被冰冻住暂时不能动了!!\n" NOR;
  }
  if (random(2) == 1 && random(level) > 150 && victim->query("qi") > 50
    && victim->query("eff_qi") > 50) {
    if (userp(victim)) {
      if (victim->query("qi") / 12 > 20)
        victim->receive_damage("qi", victim->query("qi") / 12);
      if (victim->query("eff_qi") / 12 > 20)
        victim->receive_wound("qi", victim->query("eff_qi") / 12);
    } else {
      victim->receive_damage("qi", 1090);
      victim->receive_wound("qi", 1090);
    }
    return HIB "「暗黑核融术!!」！$N突然念咒,用了一个魔法，$n被炸得飞了起来!!\n" NOR;
  }
  if (random(2) == 1 && random(level) > 150 && victim->query("jing") > 50
    && victim->query("eff_jing") > 50) {
    if (userp(victim)) {
      if (victim->query("jing") / 12 > 20)
        victim->receive_damage("jing", victim->query("jing") / 12);
      if (victim->query("eff_jing") / 12 > 20)
        victim->receive_wound("jing", victim->query("eff_jing") / 12);
    } else {
      victim->receive_damage("jing", 500);
      victim->receive_wound("jing", 500);
    }
    return HIW "「流星光矢!!」！$N突然念咒,用了一个魔法，$n被打中后觉得头痛无比!!\n" NOR;
  }
  if (damage_bonus < 100) damage_bonus = 100;
  if (damage_bonus > 200) {
    victim->receive_wound("qi", damage_bonus);
    return HIR "突!!一阵剑影！！！一股鲜血从$n的身子喷出\n" NOR;
  }
  if (damage_bonus < 120) {
    victim->receive_wound("qi", damage_bonus);
    return YEL "刺!!一阵剑影！！！$n闷哼一声,身子晃了两晃！\n" NOR;
  } else {
    victim->receive_wound("qi", damage_bonus);
    return HIM "劈!!一阵剑影！！！你听到「嗤啦」一声轻响，脸上竟溅到一些血滴！\n" NOR;
  }

}
/*	//2017.3.25阿飞封掉ob_hit特效，怀疑与伤害为0的死亡有关
int ob_hit(object ob, object me, int damage)
{
	
        string msg, *limbs;
        int level;
        
        limbs = ob->query("limbs");
        level = me->query_skill("magic-sword", 1);

//        if( me->is_busy()) return 1;
        if( damage < 10 ) return 1;
        if (  random(level) > 50          ){
         msg = RED"$N面对$n的攻势。身形一闪，突然施放魔法!!!。\n"NOR;
            message_vision(msg, me, ob);
            if ( random(2) == 0 && random(level) > 160){
         msg = YEL"「 地精附身! 」。$N突然念咒,用了一个魔法，$n感觉被大地吸住不能动了!!。\n"NOR;
//             if (!ob->is_busy())
        if (userp(ob) && ob->query("qi") > 50
        && ob->query("eff_qi") > 50)
{
             ob->start_busy(3+random(2));
if (ob->query("qi")/12 > 20)
             ob->receive_damage("qi",ob->query("qi")/12);
if (ob->query("eff_qi")/12 > 20)
             ob->receive_wound("qi",ob->query("eff_qi")/12);
}
else
{
             ob->start_busy(3);
             ob->receive_damage("qi",100);
             ob->receive_wound("qi",100);
}
msg += damage_msg(damage, "瘀伤");
            }
else  if (random(2)==1&& random(level) > 160){
         msg = HIR"「火炎球!」。$N突然念咒,用了一个魔法，$n被一个火球打中了!!\n"NOR;
        if (userp(ob) && ob->query("qi") > 50
        && ob->query("eff_qi") > 50)
{
if (ob->query("qi")/12> 20)
             ob->receive_damage("qi",ob->query("qi")/12);
if (ob->query("eff_qi")/12 > 20)
             ob->receive_wound("qi",ob->query("eff_qi")/12);
}
else {
             ob->receive_damage("qi",200);
             ob->receive_wound("qi",200);
}
msg += damage_msg(damage, "瘀伤");
            }
else  if (random(2)==2&& random(level) > 150){
         msg = HIC"「霹雳闪电!」。$N突然念咒,用了一个魔法，$n被一个闪电打中了!!\n"NOR;
        if (userp(ob) && ob->query("qi") > 50 
        && ob->query("jing") > 50 && ob->query("eff_qi") > 50
        && ob->query("eff_jing") > 50)
{
if (ob->query("qi")/12 > 20)
             ob->receive_damage("qi",ob->query("qi")/12);
if (ob->query("eff_qi")/12 > 20)
             ob->receive_wound("qi",ob->query("eff_qi")/12);
if (ob->query("jing")/12 > 20)
             ob->receive_damage("jing",ob->query("jing")/12);
if (ob->query("eff_jing")/12 > 20)
             ob->receive_wound("jing",ob->query("eff_jing")/12);
}
else
{
             ob->receive_damage("qi",1000);
             ob->receive_wound("qi",1000);
             ob->receive_damage("jing",1000);
             ob->receive_wound("jing",1000);
}
msg += damage_msg(damage, "瘀伤");
            }
else
           msg = HIG"$n抢先出招，使$N的念咒失败了!。\n"NOR;
            message_vision(msg, me, ob);
            return damage;
        }
}
*/
