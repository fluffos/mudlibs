// fumo-jian.c -伏魔剑	上乘武学400~1000
#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
inherit SKILL;
mapping *action = ({
([  "action" : "$N一式「"+HIW+"穿云势"+NOR+"」，剑锋平指，剑势如风，一气呵成横扫$n的腰间",
		"attack":	400,
		"parry":	400,
		"dodge":	400,
		"damage":	400,
		"force":	400,
    "lvl" : 0,
    "skill_name" : HIW "穿云势" NOR,
    "damage_type" : "刺伤"
]),
([  "action" : "$N向前跃上一步，左手剑诀，右手$w使出一式「"+MAG+"破雾势"+NOR+"」直刺$n的$l",
		"attack":	400,
		"parry":	400,
		"dodge":	400,
		"damage":	400,
		"force":	400,
    "lvl" : 10,
    "skill_name" : MAG "破雾势" NOR,
    "damage_type" : "刺伤"
]),
([  "action" : "$N拔步挺腰，一式「"+YEL+"推山势"+NOR+"」，手中$w沉滞无比，缓缓挥剑撞向$n的$l",
		"attack":	500,
		"parry":	500,
		"dodge":	500,
		"damage":	500,
		"force":	500,
    "lvl" : 20,
    "skill_name" : YEL "推山势" NOR,
    "damage_type" : "刺伤"
]),
([  "action" : "$N一式「"+BLU+"搅海势"+NOR+"」，$w自左而右划出一个大弧，平平地向$n的$l挥去",
		"attack":	600,
		"parry":	600,
		"dodge":	600,
		"damage":	600,
		"force":	600,	
    "lvl" : 30,
    "skill_name" : BLU "搅海势" NOR,
    "damage_type" : "刺伤"
]),
([  "action" : "$N回身拧腰，右手虚抱成圆，一式「"+BLU+"开天势"+NOR+"」，$w中宫直进，刺向$n的胸口",
		"attack":	700,
		"parry":	700,
		"dodge":	700,
		"damage":	700,
		"force":	700,
    "lvl" : 40,
    "skill_name" : BLU "开天势" NOR,
    "damage_type" : "刺伤"
]),
([  "action" : "$N一式「"+YEL+"劈地势"+NOR+"」，$w从上至下猛劈，剑锋直出，挟爆裂之声骤然斩向$n的$l",
		"attack":	800,
		"parry":	800,
		"dodge":	800,
		"damage":	800,
		"force":	800,
    "lvl" : 49,
    "skill_name" : YEL "劈地势" NOR,
    "damage_type" : "刺伤"
]),
([  "action" : "$N运气施出「"+RED+"雷针势"+NOR+"」，$w剑尖逼出一道青芒，闪电般破空射向$n的$l",
		"attack":	900,
		"parry":	900,
		"dodge":	900,
		"damage":	900,
		"force":	900,
    "lvl" : 57,
    "skill_name" : RED "雷针势" NOR,
    "damage_type" : "刺伤"
]),
([  "action" : "$N双腿半屈半蹲，一式「"+BLK+"龙吞势"+NOR+"」，剑尖划出无数圆圈，狂风骤起，笼罩天地",
		"attack":	1000,
		"parry":	1000,
		"dodge":	1000,
		"damage":	1000,
		"force":	1000,
    "lvl" : 64,
    "skill_name" : BLK "龙吞势" NOR,
    "damage_type" : "刺伤"
]),
});
int practice_level(){   return 64;  }
int valid_enable(string usage) { return (usage == "sword") || (usage == "parry"); }
int valid_combine(string combo) { return combo=="cibei-dao"; }
int valid_learn(object me)
{
    object weapon;
//		if (!wizardp(me))
//		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
    if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "sword")
    if (!objectp(weapon = me->query_temp("secondary_weapon"))
    || (string)weapon->query("skill_type") != "sword")
        return notify_fail("你必须先找把剑来才能学伏魔剑。\n");
    if (me->query("family/family_name")!="少林派")
   return notify_fail("你不是佛门中人，无法学习本门技能。\n");
    if ((int)me->query("max_neili") < 10000)
        return notify_fail("你的内力不够。\n");
    if ((int)me->query("con",1) < 25)
        return notify_fail("佛门伏魔神通不是你这种资质之人可以学会的。\n");
    if ((int)me->query_skill("hunyuan-yiqi", 1) < 100 && (int)me->query_skill("yijing-force", 1) < 100 && (int)me->query_skill("yijinjing", 1) < 100 && (int)me->query_skill("damo-force", 1) < 100)
    return notify_fail("你的少林气功火候不够，无法学习少林武技。\n");
    if (me->query_skill("sword", 1) <=me->query_skill("fumo-jian", 1))
        return notify_fail("你的基础不够，无法领会更高深的技巧。\n");
	if ((int)me->query("score",1) < 10000)
		return notify_fail("你对我派毫无贡献而言，怎么可能传授你武功呢。\n");
	if (random(10)==1) me->add("score",-10);
    return 1;
}
string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}
mapping query_action(object me, object weapon)
{
	return action[random(sizeof(action))];	//2017.9.4阿飞改，将招式简单点
}
int practice_skill(object me)
{
    object weapon;
//		if (!wizardp(me))
//		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
    if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "sword")
    if (!objectp(weapon = me->query_temp("secondary_weapon"))
    || (string)weapon->query("skill_type") != "sword")
        return notify_fail("你使用的武器不对。\n");
    if (me->query("family/family_name")!="少林派")
   return notify_fail("你不是佛门中人，偷练少林绝技会走火入魔。\n");		
    if ((int)me->query("qi") < 50)
        return notify_fail("你的体力不够练伏魔剑。\n");
    me->receive_damage("qi", 35);
    return 1;
}

string perform_action_file(string action)
{
    return __DIR__"fumo-jian/" + action;
}

mixed hit_ob(object me, object victim, int damage_bonus, int factor, string limb)
{                                                                                                                
	int ap,dp,skill1,skill2,skill3,skill4,damage2;
	int flag;
	flag = 1;
    ap = (int) me->query_skill("fumo-jian",1);
    dp = (int) victim->query_skill("sword",1);
	skill1 = (int) me->query_skill("yijing-force",1);
	skill2 = (int) me->query_skill("hunyuan-yiqi",1);
	skill3 = (int) me->query_skill("yijinjing",1);	
	skill4 = (int) me->query_skill("damo-force",1);
	damage2 = (int) me->query_skill("fumo-jian",1);
if (damage_bonus < 100) return 0;
if (skill1 < 300 && skill2 < 200 && skill3 < 100 && skill4 < 200) return 0;
if (me->query_skill_mapped("force") != "yijinjing" && me->query_skill_mapped("force") != "hunyuan-yiqi" && me->query_skill_mapped("force") != "yijing-force" && me->query_skill_mapped("force") != "damo-force") return 0;
if ( me->query("jiali") < 1 || me->query("neili") < 100 ) return 0;	
if (damage2<10) damage2=10;
if (me->query_skill_mapped("force") == "yijinjing" ) flag = 4;
if (me->query_skill_mapped("force") == "hunyuan-yiqi" ) flag = 3;
if (me->query_skill_mapped("force") == "yijing-force" ) flag = 1;
if (me->query_skill_mapped("force") == "damo-force" ) flag = 3;
if ( random(6)==2 && random(ap) > dp/10 && me->query("jiali")>0 )
{
	victim->receive_wound("qi", damage2 * flag/2+damage_bonus/2,me);
	victim->receive_damage("qi", damage2 * flag/2+damage_bonus/2,me);
	me->add("neili", -100 );
	return HIR"只见“噗嗤”一声，剑气爆响，$n被割了一道伤口，血箭飚出！\n"NOR;
}
}