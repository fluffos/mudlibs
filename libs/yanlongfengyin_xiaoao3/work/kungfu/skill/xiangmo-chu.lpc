// xiangmo-chu.c 金刚降魔杵		下乘武学100~300
#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
inherit SKILL;
mapping *action = ({
  ([ "action": "$N高举手中$w，使出一招「" + BLK + "金刚再世" + NOR + "」，直劈$n的$l  ",
    "attack": 100,
    "parry": 100,
    "dodge": 100,
    "damage": 100,
    "force": 100,
    "skill_name": BLK "金刚再世" NOR,
    "lvl": 0,
    "damage_type": "挫伤",
  ]),
  ([ "action": "$N使出「" + YEL + "金刚伏魔" + NOR + "」，单手舞动$w，一伏身，$w横扫$n的下盘  ",
    "attack": 100,
    "parry": 100,
    "dodge": 100,
    "damage": 100,
    "force": 100,
    "skill_name": YEL "金刚伏魔" NOR,
    "lvl": 8,
    "damage_type": "挫伤",
  ]),
  ([ "action": "$N反身仰面，使出一式「" + YEL + "金刚宣法" + NOR + "」，双手握$W，一棍直刺$n的$l  ",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "skill_name": YEL "金刚宣法" NOR,
    "lvl": 16,
    "damage_type": "挫伤",
  ]),
  ([ "action": "$N使出一招「" + MAG + "引趣众生" + NOR + "」，双手举棍撩向$n的裆部  ",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "skill_name": MAG "引趣众生" NOR,
    "lvl": 24,
    "damage_type": "挫伤",
  ]),
  ([ "action": "$N舞动手中$w，使出「" + HIR + "歌舞阎罗" + NOR + "」，顿时罩住$n的全身  ",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "skill_name": HIR "歌舞阎罗" NOR,
    "lvl": 32,
    "damage_type": "挫伤",
  ]),
  ([ "action": "$N使出一招「" + RED + "浮游血海" + NOR + "」，全身贴地而飞，一棍直捣$n的$l  ",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "skill_name": RED "浮游血海" NOR,
    "lvl": 40,
    "damage_type": "挫伤",
  ]),
  ([ "action": "$N使出一式「" + HIY + "驱鬼御魔" + NOR + "」，以棍支地，双足飞揣$n的面门  ",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "skill_name": HIY "驱鬼御魔" NOR,
    "lvl": 48,
    "damage_type": "挫伤",
  ]),
  ([ "action": "$N运力于掌，使出「" + HIB + "荡魔除妖" + NOR + "」，飞身疾进，手中$w横扫$n的$l  ",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "skill_name": HIB "荡魔除妖" NOR,
    "lvl": 56,
    "damage_type": "挫伤",
  ]),
});
int valid_enable(string usage) { return usage == "staff" || usage == "parry"; }
int practice_level() { return 56; }
int valid_learn(object me) {
  object weapon;
  //		if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "staff")
    return notify_fail("你使用的武器不对。\n");
  if (me->query("family/family_name") != "雪山寺")
    return notify_fail("你不是雪山寺弟子，不能传授。\n");
  if ((int)me->query("max_neili") < 10000)
    return notify_fail("你的内力不够。\n");
  if (me->query_skill("staff", 1) <= me->query_skill("xiangmo-chu", 1))
    return notify_fail("你的基础不够，无法领会更高深的技巧。\n");
  if ((int)me->query_skill("longxiang", 1) < 100 && (int)me->query_skill("xiaowuxiang", 1) < 100)
    return notify_fail("你的雪山寺内功似乎火候太浅。\n");
  return 1;
}
string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}
mapping query_action(object me, object weapon) {
  return action[random(sizeof(action))];  //2017.9.4阿飞改，将招式简单点
}

int practice_skill(object me) {
  object weapon;
  //		if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "staff")
    return notify_fail("你使用的武器不对。\n");
  if (me->query("family/family_name") != "雪山寺")
    return notify_fail("你不是雪山寺弟子，不能练习。\n");
  if ((int)me->query("qi") < 150)
    return notify_fail("你的体力不够，练不了金刚降魔杵。\n");
  me->receive_damage("qi", 30);
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "xiangmo-chu/" + action;
}
mixed hit_ob(object me, object victim, int damage_bonus, int factor) {
  int ap, dp, damage2;
  damage2 = (int)me->query_skill("xiangmo-chu", 1);
  ap = me->query_skill("xiangmo-chu", 1);
  dp = victim->query_skill("parry", 1);
  if (damage_bonus < 100) return 0;
  if (me->query_skill("xiaowuxiang", 1) < 100 && me->query_skill("longxiang", 1) < 100) return 0;
  if (me->query("jiali") < 1 || me->query("neili") < 100 || (me->query_skill_mapped("force") != "xiaowuxiang" && me->query_skill_mapped("force") != "longxiang")) return 0;
  if (damage2 < 10) damage2 = 10;
  if (me->query_skill_mapped("force") == "longxiang") damage2 = damage2 * 2 + me->query("jiali");
  if (random(ap) > dp / 10 && random(6) == 2 && me->query("jiali") > 0) {
    victim->receive_damage("qi", damage2 + 100 + damage_bonus / 2, me);
    victim->receive_wound("qi", damage2 + 100 + damage_bonus / 2, me);
    me->add("neili", -100);
    return HIR "$N扛起锄头，反手就是一把锄头轮过来，如同锄地一般！$n被砸了个七晕八姠。\n" NOR;
  }
}
