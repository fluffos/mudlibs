//weituo-gun  	韦陀棍法	中乘武学200~600
#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
inherit SKILL;

mapping *action = ({
  ([
    "action": "$N跃在半空，一招「仙鹤展翅入灵山」，手中$w已化成无数棍影，令$n眼花缭乱，不知所措，连连倒退",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "lvl": 0,
    "skill_name": "仙鹤展翅入灵山",
    "damage_type": "挫伤"
  ]),
  ([
    "action": "$N挺$w将$n的招式架住，顺势一招「玉马衔环拜仙宫」，$w上下左右飞快搅动，身随棍走，向$n压了下来",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "lvl": 30,
    "skill_name": "玉马衔环拜仙宫",
    "damage_type": "挫伤"
  ]),
  ([
    "action": "$N一招「鸣鹿踏蹄觅仙草」，屈膝俯身，手中$w连点$n下盘，却未等招数用老，猛的一提，向$n的胸腹间戳去",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 60,
    "skill_name": "鸣鹿踏蹄觅仙草",
    "damage_type": "挫伤"
  ]),
  ([
    "action": "$N突然滚到在地，$n错愕间，一招「金鲤跃水潜天池」，竟从$n的裆下窜过，更不回头，$w反手扫向$n的$l",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "lvl": 80,
    "skill_name": "金鲤跃水潜天池",
    "damage_type": "挫伤"
  ]),
  ([
    "action": "$N一招「灵猿献果赴蓬莱」，身形如电，绕着$n飞快奔跑，手中$w一招快似一招，刹那间向$n连打出十六棍",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "lvl": 100,
    "skill_name": "灵猿献果赴蓬莱",
    "damage_type": "挫伤"
  ]),
  ([
    "action": "$N大踏步上前，劲贯双臂，手中$w大开大阖，呼呼风声中一招「飞鹰盘旋扫乾坤」扫向$n的腰间",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "lvl": 120,
    "skill_name": "飞鹰盘旋扫乾坤",
    "damage_type": "挫伤"
  ]),
  ([
    "action": "$N大喝一声，一招「天龙出水腾宇宙」，$w脱手飞出，夹着劲风射向$n的前心，随即抢到$n的身后，伸手又把$w抄在手中",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "lvl": 130,
    "skill_name": "天龙出水腾宇宙",
    "damage_type": "挫伤"
  ]),
  ([
    "action": "$N双目圆睁，口中默诵真言，一招「白象卷云憾天柱」，$w似有千斤，缓缓举起，又缓缓向$n的当头砸落",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "lvl": 150,
    "skill_name": "白象卷云憾天柱",
    "damage_type": "挫伤"
  ]),
});

int valid_enable(string usage) {
  return (usage == "club") || (usage == "parry");
}
int practice_level() {
  return 60;
}
int valid_learn(object me) {
  //		if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");		
  if ((int)me->query("max_neili") < 10000)
    return notify_fail("你的内力不够。\n");
  if (me->query_skill("club", 1) <= me->query_skill("weituo-gun", 1))
    return notify_fail("你的基础不够，无法领会更高深的技巧。\n");
  if (me->query("family/family_name") != "少林派")
    return notify_fail("你不是佛门中人，无法学习少林绝技。\n");
  if ((int)me->query_skill(
    "hunyuan-yiqi",
    1
  ) < 100 && (int)me->query_skill(
    "yijing-force",
    1
  ) < 100 && (int)me->query_skill(
    "yijinjing",
    1
  ) < 100 && (int)me->query_skill("damo-force", 1) < 100)
    return notify_fail("你的少林气功火候不够，无法学习少林武技。\n");
  if ((int)me->query("score", 1) < 5000)
    return notify_fail("你对我派毫无贡献而言，怎么可能传授你武功呢。\n");
  if (random(100) == 1) me->add("score", -5);
  return 1;
}
int practice_skill(object me) {
  object weapon;
  //		if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");		
  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "club")
    return notify_fail("你使用的武器不对。\n");
  if (me->query("family/family_name") != "少林派")
    return notify_fail("你不是佛门中人，无法练习少林绝技。\n");
  if ((int)me->query("qi") < 50)
    return notify_fail("你的体力不够练韦陀棍。\n");
  me->receive_damage("qi", 25);
  return 1;
}
string perform_action_file(string action) {
  return __DIR__ "weituo-gun/" + action;
}
string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  return action[random(sizeof(action))];  //2017.9.4阿飞改，将招式简单点
}


mixed hit_ob(object me, object victim, int damage_bonus, int factor, string limb) {
  int ap, dp, skill1, skill2, skill3, skill4, damage2;
  int flag;
  flag = 1;
  ap = (int)me->query_skill("weituo-gun", 1);
  dp = (int)victim->query_skill("parry", 1);
  skill1 = (int)me->query_skill("yijing-force", 1);
  skill2 = (int)me->query_skill("hunyuan-yiqi", 1);
  skill3 = (int)me->query_skill("yijinjing", 1);
  skill4 = (int)me->query_skill("damo-force", 1);
  damage2 = (int)me->query_skill("weituo-gun", 1);
  if (damage_bonus < 100) return 0;
  if (skill1 < 300 && skill2 < 200 && skill3 < 100 && skill4 < 200) return 0;
  if (me->query_skill_mapped("force") != "yijinjing" && me->query_skill_mapped("force") != "hunyuan-yiqi" && me->query_skill_mapped("force") != "yijing-force" && me->query_skill_mapped("force") != "damo-force") return 0;
  if (me->query("jiali") < 1 || me->query("neili") < 100) return 0;
  if (damage2 < 10) damage2 = 10;
  if (me->query_skill_mapped("force") == "yijinjing") flag = 4;
  if (me->query_skill_mapped("force") == "hunyuan-yiqi") flag = 3;
  if (me->query_skill_mapped("force") == "yijing-force") flag = 1;
  if (me->query_skill_mapped("force") == "damo-force") flag = 3;
  if (random(6) == 2 && random(ap) > dp / 10 && me->query("jiali") > 0) {
    victim->receive_wound("qi", damage2 * flag + damage_bonus, me);
    victim->receive_damage("qi", damage2 * flag + damage_bonus, me);
    me->add("neili", -100);
    return HIR "$n被棍风的巨力打中，发出一阵骨碎的声音！！\n" NOR;
  }
}
