// yingzhua-shou.c -鹰爪手（殷天正）	下乘武学100~300
#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
inherit SKILL;

mapping *action = ({
([	"action" : "$N腾空而起，半空一个筋斗，一式「金雕擒兔」，迅猛地抓向$n的$l",
		"attack":	100,
		"parry":	100,
		"dodge":	100,
		"damage":	100,
		"force":	100,
	  "lvl" : 0,
	"skills_name" : "金雕擒兔",
	"damage_type" : "内伤"
]),
([	"action" : "$N单腿弯曲站立，双臂向后舒展，一式「雄鹰展翅」，双爪一前一后拢向$n的$l",
		"attack":	100,
		"parry":	100,
		"dodge":	100,
		"damage":	100,
		"force":	100,
	  "lvl" : 10,
	"skills_name" : "雄鹰展翅",
      "damage_type" : "内伤"
]),
([	"action" : "$N一式「拔翅横飞」，全身向斜下里跃去，右腿一绷，双爪扣向$n的肩头",
		"attack":	100,
		"parry":	100,
		"dodge":	100,
		"damage":	100,
		"force":	100,
	  "lvl" : 20,
	"skills_name" : "拔翅横飞",
        "damage_type" : "内伤"
]),
([	"action" : "$N双爪交错上举，使一式「迎风振翅」，一拔身，分别袭向$n左右腋空门",
		"attack":	200,
		"parry":	200,
		"dodge":	200,
		"damage":	200,
		"force":	200,
	"lvl" : 30,
	"skills_name" : "迎风振翅",
        "damage_type" : "内伤"
]),
([	"action" : "$N全身滚动上前，一式「雄鹰现爪」，右手向羽翼般在$n眼前一拂，突然左手鬼魅般抓向$n的胸口",
		"attack":	200,
		"parry":	200,
		"dodge":	200,
		"damage":	200,
		"force":	200,
	"lvl" : 40,
	"skills_name" : "雄鹰现爪",
        "damage_type" : "内伤"
]),
([	"action" : "$N伏地滑行，一式「拨云瞻日」，上手袭向膻中大穴，下手反抓$n的裆部",
		"attack":	300,
		"parry":	300,
		"dodge":	300,
		"damage":	300,
		"force":	300,
	"lvl" : 50,
	"skills_name" : "拨云瞻日",
        "damage_type" : "内伤"
]),
([	"action" : "$N左右手掌爪互逆，一式「搏击长空」，无数道劲气破空而出，迅疾无比地击向$n",
		"attack":	300,
		"parry":	300,
		"dodge":	300,
		"damage":	300,
		"force":	300,
	"lvl" : 55,
	"skills_name" : "搏击长空",
        "damage_type" : "内伤"
]),
([	"action" : "$N腾空高飞三丈，一式「鹰翔万里」，天空中顿时显出一个巨灵爪影，缓缓罩向$n",
		"attack":	300,
		"parry":	300,
		"dodge":	300,
		"damage":	300,
		"force":	300,
	"lvl" : 60,
	"skills_name" : "金雕擒兔",
        "damage_type" : "内伤"
])
});

int valid_enable(string usage) { return usage=="hand" || usage=="unarmed"|| usage=="parry"; }

//int valid_combine(string combo) { return combo=="fengyun-shou"; }

int valid_learn(object me)
{
//		if (!wizardp(me))
//		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
	if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
		return notify_fail("练鹰手必须空手。\n");
    if (me->query("family/family_name")!="明教")
   return notify_fail("你不是明教弟子，学不了鹰王的独门绝技。\n");		
	if ((int)me->query_skill("unarmed",1) < 200)
		return notify_fail("你的基本功太差劲了，无法学鹰爪手。\n");
	if ((int)me->query_skill("shenghuo-shengong", 1) < 100)
		return notify_fail("你的圣火神功还不到家，无法学鹰爪手。\n");
	if ((int)me->query("max_neili") < 10000)
		return notify_fail("你的内力太弱，无法练鹰爪手。\n");
	return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"])
                        return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
	return action[random(sizeof(action))];	//2017.9.4阿飞改，将招式简单点
}

int practice_skill(object me)
{ 
//	if (!wizardp(me))
//		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
   if (me->query("family/family_name")!="明教")
   return notify_fail("你不是明教弟子，练习不了鹰抓手。\n");	
	if ((int)me->query("qi") < 30)
		return notify_fail("你的体力太低了。\n");
	if ((int)me->query("neili") < 20)
		return notify_fail("你的内力不够练鹰爪手。\n");
	me->receive_damage("qi", 30);
	me->add("neili", -10);
	return 1;
}

string perform_action_file(string action)
{
        return __DIR__"yingzhua-shou/" + action;
}

mixed hit_ob(object me, object victim, int damage_bonus)
{

		int ap,dp,level,damage2;
        level = (int) me->query_skill("yingzhua-shou",1);  
		ap = (int) me->query("dex",1) * (int) me->query_skill("yingzhua-shou",1);
		dp = (int) victim->query("str",1) * (int) me->query_skill("parry",1);
		damage2 = (int)me->query_skill("yingzhua-shou",1);	
	
    if( damage_bonus < 100 ) return 0;
	if ( me->query_skill("shenghuo-shengong",1) < 100)	 return 0;
	if ( me->query("jiali") < 1 || me->query("neili") < 100 || me->query_skill_mapped("force") != "shenghuo-shengong") return 0;	
	if (damage2<10) damage2=10;	
	
        if (random(5) == 0 && level > 100 && me->query("neili") > 500 && random(ap)>dp/10 && me->query("jiali")>0)
		{
           me->add("neili", -100 );
    //       if (!victim->is_busy()) victim->start_busy(random(3));
		   victim->receive_wound("qi", damage2+damage_bonus/2,me);
		   victim->receive_damage("qi", damage2+damage_bonus/2,me);
			return HIY"$N双爪连环抓向$n的背心，咔嚓一声，$n被抓了一个血洞。\n"NOR;
        }
}