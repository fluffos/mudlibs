#include <ansi.h>
inherit ITEM;
void create() {
  set_name("龙结草", ({ "longjie cao" }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "颗");
    set("long", "一颗龙结草\n");
  }

  setup();
}
