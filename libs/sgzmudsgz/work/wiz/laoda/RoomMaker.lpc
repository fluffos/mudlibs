/* RoomMaker.c
** Coded by 某某@三国志
** Code generated with RoomMaker v0.9.1 coded by Onyx@RedDragon
** Do not remove this header.
*/


inherit ROOM;

void setup(){
    set_light(1);
    set_brief("老大的会客厅");
    set_long(
"这里是老大的会客厅，简单但却朴素，实在也没什么能招待您了．"
);
    add_item("test" , "试管，老大用它做些摩法试验．");
    set_exits( ([
    ]) );
    set_objects( ([
    ]) );
}