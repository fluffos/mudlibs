// Updated by stefan on 10 Jan 1997
/* Do not remove the headers from this file! see /USAGE for more info. */

#define need(x) badness += x + " is required for LIMA libs.\n"

#define FOOTER "**********************************************************\n"
#define IMPOSSIBLE_TO_MISS_HEADER \
               FOOTER \
               "* You have incorrectly compiled the MudOS driver.  This  *\n" \
               "* driver is not compatible with the LIMA mudlib.  Please *\n" \
               "* make the following changes to 'options.h' (or          *\n" \
               "* 'local_options' if it exists) in the driver source,    *\n" \
               "* and recompile.                                         *\n" \
               FOOTER

// MudOS-era compile-time nagger. The shared default FluffOS build does
// not define ARRAY_RESERVED_WORD (and has the opposite NO_LIGHT /
// NO_ADD_ACTION / NO_WIZARDS / PACKAGE_UIDS polarity). Same as
// libs/sanguozhi: keep the file (other objects inherit it) but do not
// refuse the shared driver. The mudlib no longer uses the `array`
// reserved-word dialect.
void create() {
}
