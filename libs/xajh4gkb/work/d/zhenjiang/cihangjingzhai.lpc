inherit ROOM;
#include <ansi.h>
void create() {
  set("short", "慈航静斋");
  set("long", @LONG
这里就是武林圣地【慈航静斋】，位于南岭深处的「帝踏峰」，山路上有个两边
刻著「家在此山中，云深不知处」的石牌匾，跟著有著名为「七重门」的七道木
门，门上有莲花纹饰的门环，最後是一道枣红色的正门。一个大广场後面是主殿
「慈航殿」，丛林里有座藏著各样典籍的「藏典塔」，後山还有「赏雨亭」。
LONG
  );
  set("exits", ([
    "out": __DIR__ "shanlu6",
  ]));
  set("objects", ([
  ]));
  set("no_eat", 1);
  set("no_pretend", 1);
  set("outdoors", "zhenjiang");
  setup();
  replace_program(ROOM);
}
