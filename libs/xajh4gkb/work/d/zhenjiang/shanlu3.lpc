inherit ROOM;

void create() {
  set("short", "秦岭山脚");
  set("long", @LONG
这里已经到了秦岭山脚了。横看成岭侧成峰，远近高低各不同。据传武林
四大圣地之一的焚静山庄就位于秦岭之中。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "northup": __DIR__ "qinling",
    "westdown": __DIR__ "shanlu2",
    "east": "/d/longquan/kuangshan1",
  ]));
  set("outdoors", "zhenjiang");

  setup();
  replace_program(ROOM);
}
