inherit ROOM;
#include <ansi.h>
void create() {
  set("short", "焚静山庄");
  set("long", @LONG
这里就是武林圣地【焚静山庄】，山庄位于秦岭深处，隐于一片梨花林中，四周
静悄悄的，偶尔有几只猴子出没于梨树林。
LONG
  );
  set("exits", ([
    "out": __DIR__ "shanlu9",
  ]));
  set("objects", ([
  ]));
  set("no_eat", 1);
  set("no_pretend", 1);
  set("outdoors", "zhenjiang");
  setup();
}
