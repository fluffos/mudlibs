inherit ROOM;

void create() {
  set("short", "终南山");
  set("long", @LONG
这里已经到了南岭群山中了。家在此山中，云深不知处。据传武林
四大圣地之一的慈航静斋就位于南岭深处，每每於中原处於乱世时，
派出门人访寻真命天子，为天下拨乱反正。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "northup": __DIR__ "nanling",
    "southdown": __DIR__ "shanlu4",
  ]));
  set("outdoors", "zhenjiang");

  setup();
  replace_program(ROOM);
}
