inherit ROOM;

void create() {
  set("short", "山边小路");
  set("long", @LONG
这里已经到了南岭山脉，再往上走就是高耸入云的秦岭山脉了。其峰隐于云雾之中，
隐隐约约的能看见半山腰。风林阵阵，偶尔有虎啸声。传闻有绝世高手隐于山林，故
能降服秦岭猛兽，令其不得下山侵扰百姓。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "eastup": __DIR__ "shanlu3",
    "westdown": __DIR__ "shanlu1",
  ]));
  set("outdoors", "zhenjiang");

  setup();
}
