// rentou.c

#include <ansi.h>

inherit ITEM;

void create() {
  set_name(RED "段誉的人头" NOR, ({ "duanyu rentou", "rentou" }));
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "个");
    set("value", 0);
  }
  setup();
}


int query_autoload() { return 1; }
