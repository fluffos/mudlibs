// Room: /d/npc/m_weapon/lianbianshi.c
// This is a room made by roommaker. write by Fan. :)
inherit ROOM;
void create()
{
    set("short", "斧山");
    set("long", @LONG
这里是玄兵古洞的打造开山斧的地方。处在丛林密布的大山之中。居然能有这
样一座熔岩滚滚的火山口，真是让人不得不赞叹大自然的鬼斧神工。熔炉边站着
一位的壮年男子，墙上挂着一把极是锋利的巨斧，呆呆的看着火热的熔岩出神。
LONG
    );
    set("no_clean_up", 0);
    set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/shifu" : 1,
]));
    set("exits", ([ /* sizeof() == 1 */
  "northeast" : "/d/huashan/pubu",
]));
    setup();
    replace_program(ROOM);
}
