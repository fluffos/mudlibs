inherit ROOM;
void create()
{
	set("short", "崆峒山");
	set("long", @LONG
崆峒山自古就有“中华道教第一山”之美誉。西接六盘山，东望八百里秦川，
南依关山，北峙萧关，泾河与胭脂河南北环抱，交汇于望驾山前。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "southdown" : __DIR__"room52",
]));
	set("no_new_clean_up", 0);
	set("outdoors", "room50");
	set("objects", ([
                "/d/gaochang/npc/tang" : 1,
				"/d/gaochang/npc/zong" : 1,
        ]));
	setup();
	replace_program(ROOM);
}
