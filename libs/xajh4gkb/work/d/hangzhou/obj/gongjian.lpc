inherit ITEM;
void create()
{
        set_name(HIC"弓箭"NOR, ({ "gong jian"}));
        set_weight(100);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("long", "一张弓箭\n");
                set("unit", "张"); 
                set("no_get",1);
                set("no_drop",1);             
                set("no_put",1);
                set("no_give",1);   
                set("value",100000);
        }
    setup();
}

