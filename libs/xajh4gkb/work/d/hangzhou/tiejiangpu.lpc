// tiejiangpu.c
// Date: Nov.1997 by Venus
#include <room.h>
inherit ROOM;
void create() {
  set("short", "铁匠铺");
  set("long", @LONG
这是一家不小的打铁铺，专门打造普通百姓生活用品，店里摆了几座
火炉，一位铁匠正在火炉旁满头大汗地打造东西。一位老者坐在边上。
火炉边放着不少打好了的工具。
LONG);
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "road11",
  ]));

  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/smith": 1,
  ]));
  setup();
}
