//无情刀-名器
#include <weapon.h>
#include <ansi.h>

inherit BLADE;
inherit F_UNIQUE;

void create()
{
	set_name(HIW "无情刀" NOR, ({ "wuqing dao", "dao" }));
	set_weight(10000);
	if (clonep())
		set_default_object(__FILE__);
	else {
		set("unit", "把");
		set("long", "这是一把不平凡的刀，刀长三迟七寸，锋刃无瑕，一望就知是一把绝世宝刀。\n");
		set("value", 10000000);
//		set("unique", 1);
		set("weapon_prop/constitution", 10);
		set("weapon_prop/blade", 40);
		set("weapon_prop/dodge", 5);
		set("weapon_prop/strength", 10);
		set("weapon_prop/parry", 5);	
		set("material", "steel");
//		set("no_drop", "这样东西不能离开你。\n");
//		set("no_get", "这样东西不能离开那儿。\n");
		set("wield_msg", HIY "只听「铮」的一声，$N缓缓地抽出无情刀，只觉得一种肃杀之意充盈你的心间。\n" NOR);
		set("unwield_msg", HIY "无情刀自$N掌中飞起，在半空中一转，「唰」地跃入刀鞘。\n" NOR);
	}
	init_blade(550,2);
	setup();
}
int query_autoload() { return 1; }
/*int move(mixed dest)
{
	if(! userp(dest)) return ::move(dest);
	if( ::move(dest)==0) return 0;
	if (dest->query_temp("god_weapon")!=this_object()->query("id"))
	{
		message("rumor",HIM"【江湖传奇】"+ HIY"据说杀手楼主刀锋冷的成名兵器"+ this_object()->query("name")+HIY"被"+ dest->query("name")+"("+dest->query("id")+")拿到手了！\n"NOR,users());
		dest->set_temp("god_weapon",this_object()->query("id"));
	}
	return 1;
}*/