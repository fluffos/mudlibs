// honghua.c
inherit ITEM;

void create() {
  set_name("墨玉围棋", ({ "wei qi", "weiqi" }));
  set_weight(500);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "座");
    set("long", "由墨玉雕琢而成的棋盘，纵横十九路。\n");
    set("value", 10000);
  }
}

int query_autoload() { return 1; }
