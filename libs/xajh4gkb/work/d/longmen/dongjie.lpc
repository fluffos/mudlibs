inherit ROOM;
void create() {
  set("short", "小镇东街");
  set("long", @LONG
路上很干净，赶路的行人来来往往。一阵阵酒香从附近的一个铺子
里面传了出来，一些江湖豪客都进去买酒解馋。再往西走就到了镇中心了。
LONG);
  set("valid_startroom", 1);
  set("outdoors", "龙门");
  set("class", "血河派");
  set("exits", ([
    "west": __DIR__ "sqare",
    "north": __DIR__ "beijie",
  ]));
  //        set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
