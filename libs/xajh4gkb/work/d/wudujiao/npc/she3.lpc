// by victori 
inherit NPC;
#include "duchong.h"
#include <ansi.h>

void create() {
  int jibie = 2000;
  set_name("腾蛇", ({ "du she" }));
  set("race", "野兽");
  set("long", "一条十丈多长一人来粗的大蛇，据说腾蛇万年可以化龙飞去。\n");
  set("age", 10000);
  set("attitude", "peaceful");
  set("xyzx_sys/level", jibie);  //2016.5.22阿飞调整NPC属性
  set("max_jing", 1500);
  set("max_qi", 1500);
  set("max_neili", 4000);
  set("wuyongjiali", 200);
  set("neili", 1000);

  set_skill("force", 500 + jibie * 2 + random(jibie / 5));
  set_skill("dodge", 500 + jibie * 2 + random(jibie / 5));
  set_skill("unarmed", 500 + jibie * 2 + random(jibie / 5));
  set_skill("parry", 500 + jibie * 2 + random(jibie / 5));
  set_skill("shenlong-xinfa", 500 + jibie / 2 + random(jibie / 5));
  set_skill("yixingbu", 500 + jibie / 2 + random(jibie / 5));
  set_skill("shenlong-bashi", 500 + jibie / 2 + random(jibie / 5));
  map_skill("force", "shenlong-xinfa");
  map_skill("dodge", "yixingbu");
  map_skill("parry", "shenlong-bashi");
  map_skill("unarmed", "shenlong-bashi");

  set("limbs", ({ "头部", "身体", "尾巴" }));
  set("verbs", ({ "bite" }));

  set_temp("apply/attack", 300);
  set_temp("apply/defense", 200);
  set_temp("apply/armor", 300);
  add_temp("apply/damage", 200);

  set("combat_exp", 5000000 + random(100) * 100000);

  set_weight(5000);
  setup();
}
