//湛卢剑-名器

#include <ansi.h>
#include <weapon.h>
inherit SWORD;
inherit F_UNIQUE;

void create()
{
	set_name(HIC"湛卢剑"NOR, ({ "zhanlu jian", "sword", "jian" }));
	set_weight(20000);
	if (clonep())
		set_default_object(__FILE__);
	else {
		set("unit", "柄");
		set("long", "这是春秋名匠欧冶子所铸的五口宝剑之湛卢剑。\n");
		set("value", 20000);
		set("material", "steel");
		set("weapon_prop/personality", 8);
		set("wield_msg", "$N「唰」的一声抽出一口寒光闪闪的$n。剑面浮纹跃然闪动，实是一口难得的神兵。\n");
		set("unwield_msg", "$N将手中的$n插回剑鞘。\n");
		set("weapon_prop/force", 5);
		set("weapon_prop/sword", 30);
		set("weapon_prop/dodge", 5);
		set("weapon_prop/intelligence", 5);
		set("weapon_prop/parry", 5);		
	}
	init_sword(420,2);
	setup();
}

int query_autoload() { return 1; }
/*int move(mixed dest)
{
	if(! userp(dest)) return ::move(dest);
	if( ::move(dest)==0) return 0;
	if (dest->query_temp("god_weapon")!=this_object()->query("id"))
	{
		message("rumor",HIM"【江湖传奇】"+ HIY"据说欧冶子铸造的仁道之剑"+ this_object()->query("name")+HIY"被"+ dest->query("name")+"("+dest->query("id")+")拿到手了！\n"NOR,users());
		dest->set_temp("god_weapon",this_object()->query("id"));
	}
	return 1;
}*/