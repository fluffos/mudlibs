// zimu-zhen.c 子母针法	中乘武学200~600
#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
#include <condition.h>
inherit SKILL;
string *du = ({
  "ice_poison",
  "ill_dongshang",
  "ill_fashao",
  "ill_kesou",
  "ill_shanghan",
  "ill_zhongshu",
  "xx_poison",
  "cold_poison",
  "flower_poison",
  "rose_poison",
  "x2_poison",
  "sanpoison",
  "scorpion_poison",
  "qx_snake_poison",
  "anqi_poison",
  "yf_poison",
  "nx_poison",
  "chilian_poison",
  "yufeng_poison",
  "insect_poison",
  "tmzhuihun_poison",
  "tmpili_poison",
  "zm_poison",
});

string *order = ({
  "" HIY "",
  "" HIG "",
  "" RED "",
  "" MAG "",
  "" YEL "",
  "" HIC "",
  "" HIW "",
  "" HIR "",
  "" HIB "",
  "" CYN "",
  "" WHT "",
  "" HIM "",
  "" BLU ""
});
mapping *action = ({
  ([ "action": "$N双手连环挥舞，一招" + (order[random(13)]) + "「穿心针」" NOR "，$w直奔$n的胸腹要害",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "poison": 30,
    "lvl": 0,
    "skill_name": "穿心针",
    "post_action": (: call_other, WEAPON_D, "throw_weapon" :),
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N身形晃动，一招" + (order[random(13)]) + "「破气针」" NOR "，手中$w如一串银线，往$n咽喉打去",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "poison": 40,
    "lvl": 60,
    "skill_name": "破气针",
    "post_action": (: call_other, WEAPON_D, "throw_weapon" :),
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N五指微张，一招" + (order[random(13)]) + "「吸血针」" NOR "，手中$w飞蚊般往$n全身叮去",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "poison": 50,
    "lvl": 90,
    "skill_name": "吸血针",
    "post_action": (: call_other, WEAPON_D, "throw_weapon" :),
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N大喝一声，一招" + (order[random(13)]) + "「摄魂针」" NOR "，$w幻化出夺目光亮，打向$n",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "poison": 60,
    "lvl": 100,
    "skill_name": "摄魂针",
    "post_action": (: call_other, WEAPON_D, "throw_weapon" :),
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N错步一跃，一招" + (order[random(13)]) + "「夺魄针」" NOR "，$w带着阵阵阴风，打向$n全身",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "poison": 70,
    "lvl": 110,
    "skill_name": "夺魄针",
    "post_action": (: call_other, WEAPON_D, "throw_weapon" :),
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N双目精芒闪动，一招" + (order[random(13)]) + "「离别针」" NOR "，手中$w全数脱手飞出，排山倒海般打向$n全身",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "poison": 80,
    "lvl": 120,
    "skill_name": "离别针",
    "post_action": (: call_other, WEAPON_D, "throw_weapon" :),
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N纵声长啸，一招" + (order[random(13)]) + "「天魔针」" NOR "，$w竟然带着一股强烈劲风，扫向$n",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "poison": 90,
    "lvl": 130,
    "skill_name": "天魔针",
    "post_action": (: call_other, WEAPON_D, "throw_weapon" :),
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N眼中杀气突然大盛，一招" + (order[random(13)]) + "「地煞针」" NOR "，$w飞蝗般打向$n全身",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "poison": 100,
    "lvl": 200,
    "skill_name": "地煞针",
    "post_action": (: call_other, WEAPON_D, "throw_weapon" :),
    "damage_type": "刺伤"
  ]),
});

int valid_enable(string usage) {
  return (usage == "throwing") || (usage == "parry");
}
//2019-1-1阿飞新规划，技能代码完善
//string valid_combine() { return "juehu-shou"; }		//互备技能
string skill_pinji() { return "中乘"; }    //品级区分：上乘、中乘、下乘
string skill_xingbie() { return "通用"; }    //性别区分：男性、女性、无性、通用
string skill_zhengxie() { return "邪道"; }   //正邪区分：正道、邪道、中立
string skill_menpai() { return "唐门"; }    //门派区分：门派，江湖
string skill_yinyang() { return "中性"; }    //阴阳区分：阳、阴、中性
string skill_zhenying() { return "邪派"; }   //阵营区分：正派、邪派、中立、通用
int damage_damage() { return 60; }  //外伤系数
int force_damage() { return 60; }  //内伤系数
//int qi_heal() {return 50;}		//内功疗气系数
//int jing_heal() {return 50;}		//内功疗精系数
int practice_level() { return 100; }  //自练级别
int practice_add() { return 100; }  //自练效率
int perform_level() { return 300; }  //PFM最低使用级别

int valid_learn(object me) {
  object weapon;
  //	if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
  if ((int)me->query("max_neili") < 10000)
    return notify_fail("你的内力够练子母连环针, 再多练练吧。\n");
  if (me->query("family/family_name") != "唐门")
    return notify_fail("你不是唐门弟子，无法学习子母针。\n");
  if (!me->query("jm/huanhua"))
    return notify_fail(CYN "这神州结义萧秋水好大的胆子，竟敢强闯我唐门禁地！\n" CYN "这位" + RANK_D->query_respect(me) + CYN "欲入我唐门，需得去浣花派为唐门了结此段恩怨！\n" NOR);
  if ((int)me->query("family_score") < 6)
    return notify_fail(CYN "这位" + RANK_D->query_respect(me) + CYN "对江湖不够了解啊，我派功夫恐怕不能传授于你。\n" NOR);
  if ((int)me->query_skill("biyun-xinfa", 1) < 300)
    return notify_fail("你的碧云心法火候太浅。\n");
  if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "throwing")
    return notify_fail("你手上并没有暗器，怎么练？\n");
  if ((int)me->query("score", 1) < 5000)
    return notify_fail("你对我派毫无贡献而言，怎么可能传授你武功呢。\n");
  if (me->query("camp") != "邪派")
    return notify_fail("你需要加入邪派阵营，才能修练该武功。\n");
  if (random(100) == 1) me->add("score", -5);
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  return action[random(sizeof(action))];  //2017.9.4阿飞改，将招式简单点
}

int practice_skill(object me) {
  object weapon;
  //	if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
  if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "throwing")
    return notify_fail("你使用的武器不对。\n");
  if (me->query("family/family_name") != "唐门")
    return notify_fail("你不是唐门弟子，无法练习子母针。\n");
  if (!me->query("jm/huanhua"))
    return notify_fail(CYN "这神州结义萧秋水好大的胆子，竟敢强闯我唐门禁地！\n" CYN "这位" + RANK_D->query_respect(me) + CYN "欲入我唐门，需得去浣花派为唐门了结此段恩怨！\n" NOR);
  if ((int)me->query("family_score") < 6)
    return notify_fail(CYN "这位" + RANK_D->query_respect(me) + CYN "对江湖不够了解啊，我派功夫恐怕不能传授于你。\n" NOR);
  if ((int)me->query("qi") < 50)
    return notify_fail("你的体力不够练子母针法。\n");
  if (me->query("camp") != "邪派")
    return notify_fail("你需要加入邪派阵营，才能修练该武功。\n");
  me->receive_damage("qi", 5);
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "zimu-zhen/" + action;
}

mixed hit_ob(object me, object victim, int damage_bonus, int factor, string limb) {
  int ap, dp, damage1, damage2, damage, liannum;
  object weapon;
  weapon = me->query_temp("weapon");
  ap = me->query_skill(
    "zimu-zhen",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  dp = victim->query_skill(
    "parry",
    1
  ) * (victim->query("breakup") + victim->query("breakup") * victim->query("breakup") + 100) / 100;
  damage1 = (int)me->query_skill("zimu-zhen", 1);
  damage2 = (int)me->query_skill(
    "zimu-zhen",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  if (me->query_temp("lianzhao") >= 1) liannum = me->query_temp("lianzhao");
  else liannum = 1;
  if (me->query("neili") < 100 || me->query("jingli") < 100 || damage_bonus < 100) return 0;
  if (me->query_skill("biyun-xinfa", 1) < 100 || me->query_skill("zimu-zhen", 1) < 100) return 0;
  //	if ( me->query_skill_mapped("force") != "biyun-xinfa") return 0;
  if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "throwing") return 0;

  if (random(7) == 1 && random(ap) > dp / 3 && me->query("jiali") > 0) {
    damage = (damage1 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    victim->receive_damage("qi", damage, me);
    victim->receive_wound("qi", damage / 3, me);
    me->add("neili", -100);
    victim->apply_condition(du[random(sizeof(du))], 25);
    message_vision(
      HIR "$N中指一弹，赫然两根字母飞针无声无息飞出！$n只觉得浑身一痒，只见胸口一个红点，伤口流出了碧绿色的血！\n",
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤气" NOR
    );
    return;
  }
  if (random(ap) > dp / 3 && (int)me->query("jiajing") > 0 && random(7) == 2 && (int)me->query("breakup") > 1) {
    damage = (damage2 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    me->add("jingli", -100);
    victim->receive_damage("jing", damage, me);
    victim->receive_wound("jing", damage / 3, me);
    victim->apply_condition(du[random(sizeof(du))], 25);
    message_vision(
      HIG "$N顿悟" + me->query("cognize") + HIG "，衣袖一挥，两根牛毛飞针一前一后。$n斗了一会心闷气踹，口角溢出一丝黑血。\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤精" NOR
    );
    return;
  }
  if (random(ap) > dp / 3 && (int)me->query("jiali") > 0 && random(7) == 3 && (int)me->query("jm/tangmen")) {
    damage = ((damage1 + damage2) / 2 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    me->add("neili", -100);
    victim->receive_damage("qi", damage, me);
    victim->receive_wound("qi", damage / 3, me);
    victim->apply_condition("no_force", 25);
    message_vision(
      HIM "$N融汇唐门武学精髓，凝气成针，一式" + (order[random(13)]) + "「破气针」悄然钻入$n经脉！\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤气" NOR
    );
    return;
  }
}

//2017.3.25阿飞封掉ob_hit特效，怀疑与伤害为0的死亡有关
int ob_hit(object ob, object me, int damage) {
  string msg;
  int ap, dp, j, damage1, damage2;
  object weapon;
  weapon = me->query_temp("weapon");
  ap = me->query_skill(
    "zimu-zhen",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  dp = ob->query_skill(
    "sword",
    1
  ) * (ob->query("breakup") + ob->query("breakup") * ob->query("breakup") + 100) / 100;
  damage1 = me->query_skill("zimu-zhen", 1);
  damage2 = me->query_skill(
    "zimu-zhen",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  if (damage < 10) damage = 10;
  if (me->query("neili") < 100 || me->query("jingli") < 100) return damage;
  if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "throwing") return damage;
  if (me->query_skill(
    "biyun-xinfa",
    1
  ) < 300 || me->query_skill("zimu-zhen", 1) < 300) return damage;
  //	if (me->query_skill_mapped("force")!="biyun-xinfa" ) return damage;

  if (random(8) == 1 && random(ap) > dp / 4) {
    msg = YEL "$N见$n招式一招快过一招，发出一支" + (order[random(13)]) + "「定魂针」，$n觉得手腕一痛，真气戛然而止！\n" NOR;
    if (!ob->is_busy()) ob->start_busy(1);
    me->add("neili", -100);
    j = -(damage / 2 + random(damage / 2));
    message_vision(msg, me, ob);
    return j;
  }
  if (random(ap) > dp / 4 && random(8) == 2 && (int)me->query("breakup") > 1) {
    j = -(damage / 2 + random(damage / 2));
    ob->receive_damage("jing", damage2, me);
    ob->receive_wound("jing", damage2 / 2, me);
    me->add("jingli", -100);
    ob->add("neili", -(damage1 + damage2));
    message_vision(
      HIY "$N顿悟" + me->query("cognize") + HIY "，两道飞针射出，$n刚一出招时，突然所用的内力随着伤口流出。\n" NOR,
      me,
      ob,
      damage2,
      HBWHT + HIY "精神反伤" NOR
    );
    return j;
  }
  if (random(ap) > dp / 4 && random(8) == 3 && (int)me->query("jm/tangmen")) {
    j = -(damage / 3 + random(damage / 3));
    ob->receive_damage("qi", damage1, me);
    ob->receive_wound("qi", damage1 / 2, me);
    me->add("neili", -100);
    ob->apply_condition(du[random(sizeof(du))], 25);
    message_vision(
      HIB "$N融汇唐门武学精髓，$n刚出完招后，觉得身上的伤口一阵麻痒！难受的在地上打起了滚。\n" NOR,
      me,
      ob,
      damage1,
      HBWHT + HIY "气血反伤" NOR
    );
    return j;
  }
}
