// moon-blade.c 	圆月弯刀	上乘武学400~1000
// moon-blade.c

#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
inherit SKILL;

mapping *action = ({
  ([ "action": "$N挥舞$w，使出一招「圆月当空」，上劈下撩，左挡右开，齐齐罩向$n",
    "skill_name": "圆月当空",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 0,
    "damage_type": "割伤",
    "skill_name": "圆月当空",
  ]),
  ([ "action": "$N展身虚步，提腰跃落，一招「云遮雾扰」，刀锋一卷，拦腰斩向$n",
    "skill_name": "云遮雾扰",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "lvl": 30,
    "damage_type": "割伤",
    "skill_name": "云遮雾扰",
  ]),
  ([ "action": "$N手中$w一抖，一招「拨云见月」，手中$w化成一道银练，攻向$n的$l",
    "skill_name": "拨云见月",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "lvl": 50,
    "damage_type": "割伤",
    "skill_name": "拨云见月",
  ]),
  ([ "action": "$N神态悠闲，手中$w触地，一招「对影三人」从不可思议的角度划向$n的$l",
    "skill_name": "对影三人",
    "attack": 700,
    "parry": 700,
    "dodge": 700,
    "damage": 700,
    "force": 700,
    "lvl": 80,
    "damage_type": "割伤",
    "skill_name": "对影三人",
  ]),
  ([ "action": "$N手中$w手中$w不停地颤抖着，剑气不断的发出，这招「花前月下」齐齐罩向$n",
    "skill_name": "花前月下",
    "attack": 800,
    "parry": 800,
    "dodge": 800,
    "damage": 800,
    "force": 800,
    "lvl": 100,
    "damage_type": "割伤",
    "skill_name": "花前月下",
  ]),
  ([ "action": "$N手中$w如同流星暴雨，毫不停歇地朝$n砍去，正是一招「常娥奔月」",
    "skill_name": "常娥奔月",
    "attack": 900,
    "parry": 900,
    "dodge": 900,
    "damage": 900,
    "force": 900,
    "lvl": 120,
    "damage_type": "割伤",
    "skill_name": "常娥奔月",
  ]),
  ([ "action": "$N手中的$w带起一阵狂风，顿时无数朵刀花夹带着风雷之声一招「刀光月影」朝$n的$l处砍去",
    "skill_name": "刀光月影",
    "attack": 1000,
    "parry": 1000,
    "dodge": 1000,
    "damage": 1000,
    "force": 1000,
    "lvl": 140,
    "damage_type": "割伤",
    "skill_name": "刀光月影",
  ]),
});

int valid_enable(string usage) { return usage == "blade" || usage == "parry"; }
//2019-1-1阿飞新规划，技能代码完善
//string valid_combine() { return "juehu-shou"; }		//互备技能
string skill_pinji() { return "上乘"; }    //品级区分：上乘、中乘、下乘
string skill_xingbie() { return "通用"; }    //性别区分：男性、女性、无性、通用
string skill_zhengxie() { return "中立"; }   //正邪区分：正道、邪道、中立
string skill_menpai() { return "江湖"; }    //门派区分：门派，江湖
string skill_yinyang() { return "阳"; }    //阴阳区分：阳、阴、中性
string skill_zhenying() { return "通用"; }   //阵营区分：正派、邪派、中立、通用
int damage_damage() { return 92; }  //外伤系数
int force_damage() { return 53; }  //内伤系数
//int qi_heal() {return 50;}		//内功疗气系数
//int jing_heal() {return 50;}		//内功疗精系数
int practice_level() { return 100; }  //自练级别
int practice_add() { return 100; }  //自练效率
int perform_level() { return 300; }  //PFM最低使用级别

int valid_learn(object me) {
  return notify_fail("圆月弯刀没听说能由师傅传授的。\n");
}
/*
int valid_learn(object me)
{
	object weapon;
	weapon = me->query_temp("weapon");
//	if (!wizardp(me))
//		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
	if( me->query("con")<26 )
		return notify_fail("你研究了半天，只觉得根骨有些差，始终无法理解其中的真谛。\n");
	if( me->query("int")<30 )
		return notify_fail("你研究了半天，只觉得悟性有些差，始终无法理解其中的真谛。\n");
	if( me->query("str")<26 )
		return notify_fail("你研究了半天，感觉膂力有些低，始终无法随意施展。\n");
	if( me->query("dex")<39 )
		return notify_fail("你研究了半天，只觉依自己的身法灵动性，根本无法修习这门武功。\n");
	if( me->query("max_neili")<3000 )
		return notify_fail("你的内力不够。\n");
	if ((int)me->query_skill("force") < 500)
		return notify_fail("你的内功火候太浅。\n");
	if ((int)me->query_skill("moon-blade", 1) > 600)
		return notify_fail("上乘武学只能教到这里了，以后要靠自己勤加修习领悟。\n");	
	if (me->query_skill("moon-blade", 1) < 1000 &&(!objectp(weapon) || weapon->query("id") != "moon blade") )
		return notify_fail("你的圆月刀法大成之前必须有圆月弯刀配合才能学习。\n");
	if ((int)me->query("score",1) < 10000)
		return notify_fail("你对我派毫无贡献而言，怎么可能传授你武功呢。\n");
	if (random(10)==1) me->add("score",-10);	
	return 1;
}
*/
string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  return action[random(sizeof(action))];  //2017.9.4阿飞改，将招式简单点
}

int practice_skill(object me) {
  object weapon;

  //	if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
  if (!objectp(weapon = me->query_temp("weapon")) || weapon->query("skill_type") != "blade")
    return notify_fail("你使用的武器不对。\n");
  if (me->query("qi") < 70)
    return notify_fail("你的体力不够，练不了圆月弯刀。\n");
  if (me->query("neili") < 70)
    return notify_fail("你的内力不够，练不了圆月弯刀。\n");
  if (!me->present("moon blade") && me->query_skill("moon-blade") < 400)
    return notify_fail("你的圆月刀法大成之前必须有圆月弯刀配合才能练习。\n");
  me->receive_damage("qi", 55);
  me->add("neili", -61);
  return 1;
}

mixed hit_ob(object me, object victim, int damage_bonus, int factor, string limb) {
  int ap, dp, damage1, damage2, damage, liannum;
  object weapon;
  weapon = me->query_temp("weapon");
  ap = me->query_skill(
    "moon-blade",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  dp = victim->query_skill(
    "parry",
    1
  ) * (victim->query("breakup") + victim->query("breakup") * victim->query("breakup") + 100) / 100;
  damage1 = (int)me->query_skill("moon-blade", 1);
  damage2 = (int)me->query_skill(
    "moon-blade",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  if (me->query_temp("lianzhao") >= 1) liannum = me->query_temp("lianzhao");
  else liannum = 1;
  if (me->query("neili") < 100 || me->query("jingli") < 100 || damage_bonus < 100) return 0;
  if (me->query_skill("moon-blade", 1) < 100) return 0;
  //	if ( me->query_skill_mapped("force") != "yijing-force") return 0;
  if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "blade") return 0;

  if (random(7) == 1 && random(ap) > dp / 3 && me->query("jiali") > 0) {
    damage = (damage1 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    victim->receive_damage("qi", damage, me);
    victim->receive_wound("qi", damage / 3, me);
    me->add("neili", -100);
    if (!victim->is_busy()) victim->start_busy(1);
    message_vision(
      HIW "$N" HIW "仰天一啸，刀劲迸发，满天具是雪亮的刀光，犹如满月的月光般美丽！\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤气" NOR
    );
    return;
  }
  if (random(ap) > dp / 3 && (int)me->query("jiajing") > 0 && random(7) == 2 && (int)me->query("breakup") > 1) {
    damage = (damage2 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    me->add("jingli", -100);
    victim->receive_damage("jing", damage, me);
    victim->receive_wound("jing", damage / 3, me);
    if (!victim->is_busy()) victim->start_busy(1);
    message_vision(
      HIG "$N顿悟" + me->query("cognize") + HIG "，手中" + weapon->name() + HIG "爆发一道刺目的月华，艳丽但却让人感到落入森罗地狱般，全身止不住的战抖。\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤精" NOR
    );
    return;
  }
  if (random(ap) > dp / 3 && (int)me->query("jiali") > 0 && random(7) == 3 && (int)me->query("jm/modao")) {
    damage = ((damage1 + damage2) / 2 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    me->add("neili", -100);
    victim->receive_damage("qi", damage, me);
    victim->receive_wound("qi", damage / 3, me);
    message_vision(
      HIM "$N领悟魔刀精髓，霎时一股冰寒的刀气掠过$n全身，$n全身顿时鲜血淋漓，摇摇欲倒。\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤气" NOR
    );
    return;
  }
}


string perform_action_file(string action) {
  return __DIR__ "moon-blade/" + action;
}
