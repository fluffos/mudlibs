// leiting-axe.c	中乘武学200~600
#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
inherit SKILL;
mapping *action = ({
  ([ "action": "$N立定当地，时快时缓的将手中的$w在面前移动，突然一招「" + BLU + "樵夫问道" + NOR + "」向$n劈去",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "damage_type": "劈伤"
  ]),
  ([ "action": "$N渊停岳峙般的身形猛的升起五丈，一招「" + RED + "飞虹惊落" + NOR + "」寒光乍现般向$n劈去",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "damage_type": "劈伤"
  ]),
  ([ "action": "$N突然暴喝一声，手里的$w一挫，一招「" + HIW + "风雪故人来" + NOR + "」劈向$n",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "damage_type": "劈伤"
  ]),
  ([ "action": "$N斜移两步，手中$w发出阵阵寒光，一招「" + MAG + "似幻似真" + NOR + "」劈向$n",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "damage_type": "劈伤"
  ]),
  ([ "action": "$N双手一顿，一招「" + YEL + "流星雨" + NOR + "」,手中的$w以极快的速度劈向$n",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "damage_type": "劈伤"
  ]),
  ([ "action": "$N将$w舞得尤如万条金龙，使人不敢靠近，猛的一招「" + RED + "大屠魂" + NOR + "」向$n劈去",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "damage_type": "劈伤"
  ]),
  ([ "action": "$N手中的$w“铛”的一声，斧锋侧翻，光铧骤闪，一招「" + HIW + "绝一闪" + NOR + "」劈向$n",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "damage_type": "劈伤"
  ]),
  ([ "action": "$N斧尖指天，斧锋骤颤，一招「" + RED + "天泣血" + NOR + "」数十道冷光劈向$n",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "damage_type": "劈伤"
  ]),
  ([ "action": "$N突然一招「" + HIC + "刃无还" + NOR + "」,手中的$w像是一道穹苍中的一道闪电，劈向$n",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "damage_type": "劈伤"
  ]),
  ([ "action": "$N一越拔空，长啸仿如龙吟，一招「" + YEL + "天外归星" + NOR + "」$w犹如一个光球，劈向$n",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "damage_type": "劈伤"
  ]),
  ([ "action": "$N忽然步法一变，以不可言喻的快速围绕着$n，猛的一招「" + RED + "千魂入窍" + NOR + "」劈向$n",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "damage_type": "劈伤"
  ]),
});

int valid_enable(string usage) {
  return (usage == "axe" || usage == "parry");
}
//2019-1-1阿飞新规划，技能代码完善
//string valid_combine() { return "juehu-shou"; }		//互备技能
string skill_pinji() { return "中乘"; }    //品级区分：上乘、中乘、下乘
string skill_xingbie() { return "通用"; }    //性别区分：男性、女性、无性、通用
string skill_zhengxie() { return "中立"; }   //正邪区分：正道、邪道、中立
string skill_menpai() { return "江湖"; }    //门派区分：门派，江湖
string skill_yinyang() { return "阳"; }    //阴阳区分：阳、阴、中性
string skill_zhenying() { return "通用"; }   //阵营区分：正派、邪派、中立、通用
int damage_damage() { return 65; }  //外伤系数
int force_damage() { return 50; }  //内伤系数
//int qi_heal() {return 50;}		//内功疗气系数
//int jing_heal() {return 50;}		//内功疗精系数
int practice_level() { return 100; }  //自练级别
int practice_add() { return 100; }  //自练效率
int perform_level() { return 300; }  //PFM最低使用级别

int valid_learn(object me) {
  return notify_fail("雷霆斧法只能通过研读(study)而获得提高。\n");
}

mapping query_action(object me, object weapon) {
  int level = me->query_skill("leiting-axe", 1);
  if (me->query_temp("powerup") && random(level) > 300 && random(7) == 1 && me->query("jiali") > 100) {
    return ([ "action": "$N将斧缓缓提到胸前，神色沉重地施出「" + RED + "雷霆震天" + NOR + "」劈向$n的$l",
      "attack": 600,
      "parry": 600,
      "dodge": 600,
      "damage": 600,
      "force": 600,
      "damage_type": "劈伤",
    ]);
  }

  return action[random(sizeof(action))];  //2017.9.4阿飞改，将招式简单点
}
int practice_skill(object me) {
  object weapon;
  //	if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
  if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "axe")
    if (!objectp(weapon = me->query_temp("secondary_weapon")) || (string)weapon->query("skill_type") != "axe")
      return notify_fail("你使用的武器不对。\n");
  if ((int)me->query("qi") < 50)
    return notify_fail("你的体力不够练雷霆斧法。\n");
  me->receive_damage("qi", 25);
  return 1;
}
string perform_action_file(string action) {
  return __DIR__ "leiting-axe/" + action;
}

mixed hit_ob(object me, object victim, int damage_bonus) {
  int ap, dp, damage1, damage2, damage, liannum;
  object weapon;
  weapon = me->query_temp("weapon");
  ap = me->query_skill(
    "leiting-axe",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  dp = victim->query_skill(
    "parry",
    1
  ) * (victim->query("breakup") + victim->query("breakup") * victim->query("breakup") + 100) / 100;
  damage1 = (int)me->query_skill("leiting-axe", 1);
  damage2 = (int)me->query_skill(
    "leiting-axe",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  if (me->query_temp("lianzhao") >= 1) liannum = me->query_temp("lianzhao");
  else liannum = 1;
  if (me->query("neili") < 100 || me->query("jingli") < 100 || damage_bonus < 100) return 0;
  if (me->query_skill("leiting-axe", 1) < 100) return 0;
  if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "axe") return 0;
  //	if ( me->query_skill_mapped("force") != "yijing-force") return 0;

  if (random(7) == 1 && random(ap) > dp / 3 && me->query("jiali") > 0) {
    damage = (damage1 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    victim->receive_damage("qi", damage, me);
    victim->receive_wound("qi", damage / 3, me);
    me->add("neili", -100);
    message_vision(
      HIR "只见" + weapon->name() + HIR "划过$n" HIR "前胸，「刺啦」一声，血箭飚射。\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤气" NOR
    );
    return;
  }
  if (random(ap) > dp / 3 && (int)me->query("jiajing") > 0 && random(7) == 2 && (int)me->query("breakup") > 1) {
    damage = (damage2 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    me->add("jingli", -100);
    victim->receive_damage("jing", damage, me);
    victim->receive_wound("jing", damage / 3, me);
    message_vision(
      HIG "$N顿悟" + me->query("cognize") + HIG "，手中" + weapon->name() + HIG "金光大盛，一式「" + RED + "雷霆震天" + HIG + "」迅猛无比的劈向$n。\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤精" NOR
    );
    return;
  }
  if (random(ap) > dp / 3 && (int)me->query("jiali") > 0 && random(7) == 3 && (int)me->query("jm/leiting")) {
    damage = ((damage1 + damage2) / 2 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    me->add("neili", -100);
    victim->receive_damage("qi", damage, me);
    victim->receive_wound("qi", damage / 3, me);
    if (!victim->is_busy()) victim->start_busy(1);
    message_vision(
      HIM "$N洞悉黄金斧之谜，" + weapon->name() + HIM "化作幻影，连续劈向$n，将$n前后左右都封死。\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤气" NOR
    );
    return;
  }
}
