// riyue-bian.c 日月鞭法	上乘武学400~1000
// modified by Venus Oct.1997

#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
inherit SKILL;
string *order = ({
  "" HIY "",
  "" HIG "",
  "" RED "",
  "" MAG "",
  "" YEL "",
  "" HIC "",
  "" HIW "",
  "" HIR "",
  "" HIB "",
  "" CYN "",
  "" WHT "",
  "" HIM "",
  "" BLU ""
});

mapping *action = ({
  ([ "action": "$N手中$w挥出，$w势道奇急，却是绝无劲风，自$n$l横掠而过，相距已不逾半尺",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 33,
    "damage_type": "鞭伤"
  ]),
  ([ "action": "$N手腕一抖，$w直昂上来，撞向$n面门，灵动威猛，便如一条张牙舞爪的墨龙相似",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 63,
    "damage_type": "鞭伤"
  ]),
  ([ "action": "$N$w抖动，转成两个圆圈，从半空中往$n头顶盖下，不但招数精巧，内劲更是雄厚无比",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 93,
    "damage_type": "鞭伤"
  ]),
  ([ "action": "$N手运刚劲，$w上所发内力直如排山倒海一般，向着四面八方逼去，登时迫得$n连连倒退",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "lvl": 124,
    "damage_type": "内伤",
  ]),
  ([ "action": "$N手腕一抖，$w向$n胸口点到，化成一条笔直的兵刃，如长矛，如杆棒，疾刺而至，招数厉害之极",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "lvl": 185,
    "damage_type": "刺伤",
  ]),
  ([ "action": "$N鞭上内劲不绝催动，内功施展到了淋漓尽致，$w呼啸而出，向$n面门点去，鞭身未到，$w上所挟劲风已令$n一阵气窒",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "lvl": 205,
    "damage_type": "内伤",
  ]),
  ([ "action": "$N手中$w滚动飞舞，宛若灵蛇乱颤，鞭影纵横，似真似幻，骤出三招九式，每一式中都隐藏数十招变化，数十下杀手",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "lvl": 205,
    "damage_type": "鞭伤",
  ]),
  ([ "action": "$N端坐不动，一招" + (order[random(13)]) + "「裂石式」" NOR "，手中$w抖得笔直，对准$n的胸腹要害连刺数鞭",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 0,
    "skill_name": "裂石式",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N身形一转，一招" + (order[random(13)]) + "「断川式」" NOR "，手中$w如矫龙般腾空一卷，猛地向$n劈头打下",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 60,
    "skill_name": "断川式",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N力贯鞭梢，一招" + (order[random(13)]) + "「破云式」" NOR "，手中$w舞出满天鞭影，排山倒海般扫向$n全身",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 90,
    "skill_name": "破云式",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N力贯鞭梢，一招" + (order[random(13)]) + "「分海式」" NOR "，手中$w舞出满天鞭影，排山倒海般扫向$n全身",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 100,
    "skill_name": "分海式",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N力贯鞭梢，一招" + (order[random(13)]) + "「裂空式」" NOR "，手中$w舞出满天鞭影，排山倒海般扫向$n全身",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "lvl": 110,
    "skill_name": "裂空式",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N力贯鞭梢，一招" + (order[random(13)]) + "「佛光普照」" NOR "，手中$w舞出满天鞭影，排山倒海般扫向$n全身",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "lvl": 120,
    "skill_name": "佛光普照",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N力贯鞭梢，一招" + (order[random(13)]) + "「金刚伏魔」" NOR "，手中$w舞出满天鞭影，排山倒海般扫向$n全身",
    "attack": 700,
    "parry": 700,
    "dodge": 700,
    "damage": 700,
    "force": 700,
    "lvl": 130,
    "skill_name": "金刚伏魔",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N力贯鞭梢，一招" + (order[random(13)]) + "「佛法无边」" NOR "，手中$w舞出满天鞭影，排山倒海般扫向$n全身",
    "attack": 800,
    "parry": 800,
    "dodge": 800,
    "damage": 800,
    "force": 800,
    "lvl": 200,
    "skill_name": "佛法无边",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N端坐不动，一式" + (order[random(13)]) + "「阿那律刺」" NOR "，手腕力抬，$w滚动飞舞，宛如灵蛇乱颤，前缠后绕，其力沾粘不散",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "lvl": 250,
    "skill_name": "阿那律刺",
    "damage_type": "拉伤"
  ]),
  ([ "action": "$N一式" + (order[random(13)]) + "「目捷连刺」" NOR "，将手$w中抖得笔直，“呲呲”声中向$n疾刺而至，所挟劲风已令$n一阵气窒",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "lvl": 300,
    "skill_name": "目捷连刺",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N内劲到处，将$w抖动转成两个圆圈，一式" + (order[random(13)]) + "「舍利佛刺」" NOR "，跃起从半空中往$n$l缠下",
    "attack": 700,
    "parry": 700,
    "dodge": 700,
    "damage": 700,
    "force": 700,
    "lvl": 360,
    "skill_name": "舍利佛刺",
    "damage_type": "拉伤"
  ]),
  ([ "action": "$N劲走螺旋，一式" + (order[random(13)]) + "「富楼那刺」" NOR "，$w在$n眼前上圈下钩，左旋右转，连变了数种招式，忽然从$n$l处倒卷上来",
    "attack": 800,
    "parry": 800,
    "dodge": 800,
    "damage": 800,
    "force": 800,
    "lvl": 390,
    "skill_name": "富楼那刺",
    "damage_type": "拉伤"
  ]),
  ([ "action": "$N一声高喝，吸气催力，一式" + (order[random(13)]) + "「迦旋延刺」" NOR "，$w急速转动，鞭影纵横，直中有圆，圆中有直，似真似幻，绞向$n",
    "attack": 900,
    "parry": 900,
    "dodge": 900,
    "damage": 900,
    "force": 900,
    "lvl": 420,
    "skill_name": "迦旋延刺",
    "damage_type": "拉伤"
  ]),
  ([ "action": "$N含胸拔背、沉肩坠肘，一式" + (order[random(13)]) + "「弘忍能刺」" NOR "，力道灵动威猛，虚实分明，劲力从四面八方向$n挤压出去",
    "attack": 1000,
    "parry": 1000,
    "dodge": 1000,
    "damage": 1000,
    "force": 1000,
    "lvl": 450,
    "skill_name": "弘忍能刺",
    "damage_type": "拉伤"
  ]),
});

int valid_enable(string usage) {
  return (usage == "whip") || (usage == "parry");
}
//2019-1-1阿飞新规划，技能代码完善
//string valid_combine() { return "juehu-shou"; }		//互备技能
string skill_pinji() { return "上乘"; }    //品级区分：上乘、中乘、下乘
string skill_xingbie() { return "通用"; }    //性别区分：男性、女性、无性、通用
string skill_zhengxie() { return "中立"; }   //正邪区分：正道、邪道、中立
string skill_menpai() { return "少林派"; }    //门派区分：门派，江湖
string skill_yinyang() { return "中性"; }    //阴阳区分：阳、阴、中性
string skill_zhenying() { return "正派"; }   //阵营区分：正派、邪派、中立、通用
int damage_damage() { return 76; }  //外伤系数
int force_damage() { return 67; }  //内伤系数
//int qi_heal() {return 50;}		//内功疗气系数
//int jing_heal() {return 50;}		//内功疗精系数
int practice_level() { return 100; }  //自练级别
int practice_add() { return 100; }  //自练效率
int perform_level() { return 300; }  //PFM最低使用级别

int valid_learn(object me) {
  object weapon;
  //	if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
  if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "whip")
    return notify_fail("你必须先找一条鞭子才能练鞭法。\n");
  if (me->query("family/family_name") != "少林派")
    return notify_fail("你不是佛门中人，无法学习少林绝技。\n");
  if (!me->query("jm/songshan"))
    return notify_fail(CYN "这嵩山派与我寺同处一山，欲夺取武林盟主之位，我少林表示不服。\n" CYN "这位" + RANK_D->query_respect(me) + CYN "可否先为我少林潜入嵩山派夺取其秘密？我寺将来必有厚报。\n" NOR);
  if ((int)me->query("family_score") < 10)
    return notify_fail(CYN "这位" + RANK_D->query_respect(me) + CYN "对江湖不够了解啊，我寺功夫恐怕不能传授于你。\n" NOR);
  if ((int)me->query("max_neili") < 200000)
    return notify_fail("你的内力不足，没有办法练日月鞭法, 多练些内力再来吧。\n");
  if (me->query_skill("whip", 1) <= me->query_skill("riyue-bian", 1))
    return notify_fail("你的基础不够，无法领会更高深的技巧。\n");
  if ((int)me->query_skill("hunyuan-yiqi", 1) < 300 && (int)me->query_skill("yijinjing", 1) < 300)
    return notify_fail("你的少林内功火候不够，无法学习少林武技。\n");
  if ((int)me->query_skill("riyue-bian", 1) > 3000)
    return notify_fail("上乘武学只能教到这里了，以后要靠自己勤加修习领悟。\n");
  if ((int)me->query("score", 1) < 10000)
    return notify_fail("你对我派毫无贡献而言，怎么可能传授你武功呢。\n");
  if (me->query("camp") != "正派")
    return notify_fail("你需要加入正派阵营，才能修练该武功。\n");
  if (random(10) == 1) me->add("score", -10);
  return 1;
}

int practice_skill(object me) {
  object weapon;
  //	if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
  if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "whip")
    return notify_fail("你使用的武器不对。\n");
  if (me->query("family/family_name") != "少林派")
    return notify_fail("你不是佛门中人，偷练少林绝技会走火入魔。\n");
  if (!me->query("jm/songshan"))
    return notify_fail(CYN "这嵩山派与我寺同处一山，欲夺取武林盟主之位，我少林表示不服。\n" CYN "这位" + RANK_D->query_respect(me) + CYN "可否先为我少林潜入嵩山派夺取其秘密？我寺将来必有厚报。\n" NOR);
  if ((int)me->query("family_score") < 10)
    return notify_fail(CYN "这位" + RANK_D->query_respect(me) + CYN "对江湖不够了解啊，我寺功夫恐怕不能传授于你。\n" NOR);
  if ((int)me->query("qi") < 50)
    return notify_fail("你的体力不够练日月鞭法。\n");
  if (me->query("camp") != "正派")
    return notify_fail("你需要加入正派阵营，才能修练该武功。\n");
  me->receive_damage("qi", 45);
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int level;

  level = (int)me->query_skill("riyue-bian", 1);
  if (random(level) > 300 && me->query("neili") > 800 && random(20) > 15 && me->query("jiali") > 100) {
    me->add("neili", -50);
    return ([
      "action": HIW "$N心神宁定，$w" NOR + HIW "挥动时无声无息，内力返照空明，功力精纯，不露棱角，$n祗觉身周气流略有异状，这一下袭击事先竟无半点朕兆" NOR,
      "attack": 600,
      "parry": 600,
      "dodge": 600,
      "damage": 600,
      "force": 600,
      "damage_type": "内伤",
    ]);
  }
  if (random(me->query_str(1)) >= 100
    && random(level) > 400
    && me->query("neili") > 1000
    && random(20) > 15
    && me->query("jiali") > 100) {
    me->add("neili", -30);
    return ([
      "action": HIR "$N手腕一抖，但见飞舞的$w从天而下和$n缠在一起，一股排山倒海的内劲无声息的向$n胸口撞去！" NOR,
      "attack": 800,
      "parry": 800,
      "dodge": 800,
      "damage": 800,
      "force": 800,
      "damage_type": "内伤" ]);
  }
  if (random(me->query_str(1)) >= 200
    && random(level) > 500
    && me->query("neili") > 1000
    && random(20) > 15
    && me->query("jiali") > 100) {
    me->add("neili", -50);
    return ([
      "action": HIR "$N把手中$w抖动成圈，绕着$n越转越急，越转越快，$p登时身不由主，被套得连转了几身，眼看就要摔倒！" NOR,
      "attack": 1000,
      "parry": 1000,
      "dodge": 1000,
      "damage": 1000,
      "force": 1000,
      "damage_type": "摔伤" ]);
  }
  return action[random(sizeof(action))];  //2017.9.4阿飞改，将招式简单点
}

mixed hit_ob(object me, object victim, int damage_bonus, int factor, string limb) {
  object weapon;
  int ap, dp, skill1, skill2, skill3, skill4, damage1, damage2, damage, liannum;
  int flag = 1;
  weapon = me->query_temp("weapon");
  ap = (int)me->query_skill(
    "riyue-bian",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  dp = (int)victim->query_skill(
    "unarmed",
    1
  ) * (victim->query("breakup") + victim->query("breakup") * victim->query("breakup") + 100) / 100;
  skill1 = (int)me->query_skill("yijing-force", 1);
  skill2 = (int)me->query_skill("hunyuan-yiqi", 1);
  skill3 = (int)me->query_skill("yijinjing", 1);
  skill4 = (int)me->query_skill("damo-force", 1);
  damage1 = (int)me->query_skill("riyue-bian", 1);
  damage2 = (int)me->query_skill(
    "riyue-bian",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  if (me->query_temp("lianzhao") >= 1) liannum = me->query_temp("lianzhao");
  else liannum = 1;
  if (me->query("neili") < 100 || me->query("jingli") < 100 || damage_bonus < 100) return 0;
  if ((skill1 < 300 && skill2 < 200 && skill3 < 100 && skill4 < 200) || (int)me->query_skill(
    "riyue-bian",
    1
  ) < 100) return 0;
  //	if (me->query_skill_mapped("force") != "yijinjing" && me->query_skill_mapped("force") != "hunyuan-yiqi" && me->query_skill_mapped("force") != "yijing-force" && me->query_skill_mapped("force") != "damo-force") return 0;
  if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "whip") return 0;

  if (me->query_skill_mapped("force") == "yijinjing") flag = 4;
  if (me->query_skill_mapped("force") == "hunyuan-yiqi") flag = 2;
  if (me->query_skill_mapped("force") == "yijing-force") flag = 1;
  if (me->query_skill_mapped("force") == "damo-force") flag = 2;

  if (random(7) == 1 && random(ap) > dp / 3 && me->query("jiali") > 0) {
    damage = (damage1 * flag + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    victim->receive_damage("qi", damage, me);
    victim->receive_wound("qi", damage / 3, me);
    me->add("neili", -100);
    message_vision(
      HIR "$n被鞭风扫中，剧痛无比！被扫中的地方皮开肉裂。\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤气" NOR
    );
    return;
  }
  if (random(ap) > dp / 3 && (int)me->query("jiajing") > 0 && random(7) == 2 && (int)me->query("breakup") > 1) {
    damage = (damage2 * flag + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    me->add("jingli", -100);
    victim->receive_damage("jing", damage, me);
    victim->receive_wound("jing", damage / 3, me);
    if (!victim->is_busy()) victim->start_busy(2);
    message_vision(
      HIG "$N顿悟" + me->query("cognize") + HIG "，手中" + weapon->name() + HIG "幻化两道飞龙，一黑一白，一阴一阳，将$n缠住。\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤精" NOR
    );
    return;
  }
  if (random(ap) > dp / 3 && (int)me->query("jiali") > 0 && random(7) == 3 && (int)me->query("jm/shaolin")) {
    damage = ((damage1 * flag + damage2 * flag) / 2 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    me->add("neili", -100);
    victim->receive_damage("qi", damage, me);
    victim->receive_wound("qi", damage / 3, me);
    message_vision(
      HIM "$N融汇少林武功精髓，使出“日月诀”，手中" + weapon->name() + HIM "光芒大盛，银光一闪，于虚空中扫中$n，$n感觉身体断为两截！\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤气" NOR
    );
    return;
  }

}



string perform_action_file(string action) {
  return __DIR__ "riyue-bian/" + action;
}
