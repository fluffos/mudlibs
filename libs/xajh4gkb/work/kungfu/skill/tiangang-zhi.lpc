// tiangang-zhi.c 天罡指穴法	下乘武学100~300
// create by host dec,20
#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
inherit SKILL;

mapping *action = ({
  ([ "action": "$N左指划了个半圈，一式「浪起湖心」自下而上点向$n的丹田",
    "attack": 100,
    "parry": 100,
    "dodge": 100,
    "damage": 100,
    "force": 100,
    "lvl": 0,
    "skill_name": "浪起湖心",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N左右指双起，一招「瀑落清潭」，拂向$n阳关、风池、颈间三处穴门",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "lvl": 5,
    "skill_name": "瀑落清潭",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N晃身若蛇行，一式「龟蛇竞渡」，左指仿若无力般指向$n的$l",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "lvl": 10,
    "skill_name": "千夫所指",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N一式「倒索攀山」，右掌蓄式，右指幻出一缕指风，重重点向$n的$l",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "lvl": 20,
    "skill_name": "倒索攀山",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N一式「闪现雷鸣」，左指虚晃，右指风驰电掣般急急刺向$n的$l",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "lvl": 30,
    "skill_name": "闪现雷鸣",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N一式「地动山摇」，蓄集全身力气，双指排出一股气浪，击向$n",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "lvl": 40,
    "skill_name": "地动山摇",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N飞身而起，一式「水拍云崖」，右手划出一圈，拂向$n的$l",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "lvl": 60,
    "skill_name": "水拍云崖",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N凝神聚气，一式「泰山压顶」，双手幻出无数指风，从上而下罩$n",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "lvl": 80,
    "skill_name": "泰山压顶",
    "damage_type": "刺伤"
  ]),
});

int valid_enable(string usage) {
  return usage == "unarmed" || usage == "finger" || usage == "parry";
}
//2019-1-1阿飞新规划，技能代码完善
string valid_combine() { return "jinding-zhang"; }  //互备技能
string skill_pinji() { return "下乘"; }    //品级区分：上乘、中乘、下乘
string skill_xingbie() { return "通用"; }    //性别区分：男性、女性、无性、通用
string skill_zhengxie() { return "中立"; }   //正邪区分：正道、邪道、中立
string skill_menpai() { return "峨嵋派"; }    //门派区分：门派，江湖
string skill_yinyang() { return "中性"; }    //阴阳区分：阳、阴、中性
string skill_zhenying() { return "正派"; }   //阵营区分：正派、邪派、中立、通用
int damage_damage() { return 42; }  //外伤系数
int force_damage() { return 62; }  //内伤系数
//int qi_heal() {return 50;}		//内功疗气系数
//int jing_heal() {return 50;}		//内功疗精系数
int practice_level() { return 100; }  //自练级别
int practice_add() { return 100; }  //自练效率
int perform_level() { return 300; }  //PFM最低使用级别

int valid_learn(object me) {
  //	if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");		
  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("学习「天罡指穴法」必须空手。\n");
  if ((int)me->query_skill("linji-zhuang", 1) < 200)
    return notify_fail("你的「临济十二庄」火候不够，无法学习「天罡指穴法」。\n");
  if (me->query("family/family_name") != "峨嵋派")
    return notify_fail("你不是峨嵋弟子，无法学习。\n");
  if ((int)me->query("max_neili") < 10000)
    return notify_fail("你的内力太弱，无法学习「天罡指穴法」。\n");
  if (me->query("camp") != "正派")
    return notify_fail("你需要加入正派阵营，才能修练该武功。\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  return action[random(sizeof(action))];  //2017.9.4阿飞改，将招式简单点
}

int practice_skill(object me) {
  //	if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");		
  if (me->query("family/family_name") != "峨嵋派")
    return notify_fail("你不是峨嵋弟子，偷练技能小心啊。\n");
  if ((int)me->query("qi") < 30)
    return notify_fail("你的体力不够，休息一下再练吧。\n");
  if ((int)me->query("neili") < 20)
    return notify_fail("你的内力不够练「天罡指」。\n");
  if (me->query("camp") != "正派")
    return notify_fail("你需要加入正派阵营，才能修练该武功。\n");
  me->receive_damage("qi", 30);
  me->add("neili", -10);
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "tiangang-zhi/" + action;
}
mixed hit_ob(object me, object victim, int damage_bonus, int factor, string limb) {
  int ap, dp, damage1, damage2, damage, liannum;

  ap = me->query_skill(
    "tiangang-zhi",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  dp = victim->query_skill(
    "dodge",
    1
  ) * (victim->query("breakup") + victim->query("breakup") * victim->query("breakup") + 100) / 100;
  damage1 = (int)me->query_skill("tiangang-zhi", 1);
  damage2 = (int)me->query_skill(
    "tiangang-zhi",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  if (me->query_temp("lianzhao") >= 1) liannum = me->query_temp("lianzhao");
  else liannum = 1;
  if (me->query("neili") < 100 || me->query("jingli") < 100 || damage_bonus < 100) return 0;
  if (me->query_skill(
    "linji-zhuang",
    1
  ) < 100 || me->query_skill("tiangang-zhi", 1) < 100) return 0;
  //	if ( me->query_skill_mapped("force") != "linji-zhuang") return 0;
  if (objectp(me->query_temp("weapon"))) return 0;

  if (random(7) == 1 && random(ap) > dp / 3 && me->query("jiali") > 0) {
    damage = (damage1 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    victim->receive_damage("qi", damage, me);
    victim->receive_wound("qi", damage / 3, me);
    me->add("neili", -100);
    if (!victim->is_busy()) victim->start_busy(1);
    message_vision(
      HIR "$n听到“噗嗤”一声！被一道指风击中，穴道似乎淤塞了。\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤气" NOR
    );
    return;
  }
  if (random(ap) > dp / 3 && (int)me->query("jiajing") > 0 && random(7) == 2 && (int)me->query("breakup") > 1) {
    damage = (damage2 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    me->add("jingli", -100);
    victim->receive_damage("jing", damage, me);
    victim->receive_wound("jing", damage / 3, me);
    message_vision(
      HIG "$N顿悟" + me->query("cognize") + HIG "，腾空而起，双掌合一，十指如钩，疾向$n的击落，势道凶猛已极。\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤精" NOR
    );
    return;
  }
  if (random(ap) > dp / 3 && (int)me->query("jiali") > 0 && random(7) == 3 && (int)me->query("jm/emei")) {
    damage = ((damage1 + damage2) / 2 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    me->add("neili", -100);
    victim->receive_damage("qi", damage, me);
    victim->receive_wound("qi", damage / 3, me);
    message_vision(
      HIM "$N洞悉峨嵋派武学精髓，身形纵起，衣袂飘荡，双手轻轻拍动，居高临下，连绵不绝拍向$n。\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤气" NOR
    );
    return;
  }
}
