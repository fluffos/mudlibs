// yujiamu-quan.c 金刚瑜迦母拳	下乘武学100~300
#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
inherit SKILL;
mapping *action = ({
([      "action" : "$N并举双拳，使出一招「"+RED+"灌顶"+NOR+"」，当头砸向$n的$l  ",
		"attack":	100,
		"parry":	100,
		"dodge":	100,
		"damage":	100,
		"force":	100,
        "skill_name" : RED "灌顶" NOR,
        "lvl" : 0,
        "damage_type" : "瘀伤",
]),
([      "action" : "$N使出一招「"+BLU+"解苦"+NOR+"」，身形一低，左手护顶，右手一拳击向$n的裆部  ",
		"attack":	100,
		"parry":	100,
		"dodge":	100,
		"damage":	100,
		"force":	100,
        "skill_name" : BLU "解苦" NOR,
        "lvl" : 8,
        "damage_type" : "瘀伤",
]),
([      "action" : "$N使出一招「"+YEL+"颦眉"+NOR+"」，左拳虚击$n的前胸，一错身，右拳横扫$n的太阳穴  ",
		"attack":	200,
		"parry":	200,
		"dodge":	200,
		"damage":	200,
		"force":	200,
        "skill_name" : YEL "颦眉" NOR,
        "lvl" : 16,
        "damage_type" : "瘀伤",
]),
([      "action" : "$N神形怪异，使一招「"+HIR+"嗔恚"+NOR+"」，双拳上下击向$n的$l  ",
		"attack":	200,
		"parry":	200,
		"dodge":	200,
		"damage":	200,
		"force":	200,
        "skill_name" : HIR "嗔恚" NOR,
        "lvl" : 24,
        "damage_type" : "瘀伤",
]),
([      "action" : "$N使出一招「"+HIC+"静寂"+NOR+"」，双拳交错，缓缓击出，劲气直指$n的$l  ",
		"attack":	300,
		"parry":	300,
		"dodge":	300,
		"damage":	300,
		"force":	300,
        "skill_name" : HIC "静寂" NOR,
        "lvl" : 32,
        "damage_type" : "瘀伤",
]),
([      "action" : "$N微微一笑，使出一式「"+GRN+"妙音"+NOR+"」，双拳前后击出，直取$n的左胸  ",
		"attack":	300,
		"parry":	300,
		"dodge":	300,
		"damage":	300,
		"force":	300,
        "skill_name" : GRN "妙音" NOR,
        "lvl" : 40,
        "damage_type" : "瘀伤",
]),
([      "action" : "$N使出一招「"+HIW+"明心"+NOR+"」，全身疾转，双拳横扫$n的$l  ",
		"attack":	300,
		"parry":	300,
		"dodge":	300,
		"damage":	300,
		"force":	300,
        "skill_name" : HIW "明心" NOR,
        "lvl" : 48,
        "damage_type" : "瘀伤",
]),
([      "action" : "$N飞身一跃，使出一招「"+MAG+"制胜"+NOR+"」，一拳猛击$n咽喉  ",
		"attack":	300,
		"parry":	300,
		"dodge":	300,
		"damage":	300,
		"force":	300,
        "skill_name" : MAG "制胜" NOR,
        "lvl" : 56,
        "damage_type" : "瘀伤",
]),
});
int valid_enable(string usage) { return usage == "unarmed" || usage == "parry"; }
//2019-1-1阿飞新规划，技能代码完善
//string valid_combine() { return "juehu-shou"; }		//互备技能
string skill_pinji() {return "下乘";}				//品级区分：上乘、中乘、下乘
string skill_xingbie() {return "通用";}				//性别区分：男性、女性、无性、通用
string skill_zhengxie() {return "正道";}			//正邪区分：正道、邪道、中立
string skill_menpai() {return "雪山寺";}				//门派区分：门派，江湖
string skill_yinyang() {return "阳";}				//阴阳区分：阳、阴、中性
string skill_zhenying() {return "正派";}			//阵营区分：正派、邪派、中立、通用
int damage_damage() {return 63;}	//外伤系数
int force_damage() {return 45;}		//内伤系数
//int qi_heal() {return 50;}		//内功疗气系数
//int jing_heal() {return 50;}		//内功疗精系数
int practice_level() {return 100;}	//自练级别
int practice_add() {return 100;}	//自练效率
int perform_level() {return 300;}	//PFM最低使用级别

int valid_learn(object me)
{
//	if (!wizardp(me))
//		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
    if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
        return notify_fail("练金刚瑜迦母拳必须空手。\n");
    if ((int)me->query("max_neili") < 10000)
        return notify_fail("你的内力不够。\n");
	if (me->query("family/family_name")!="雪山寺")
		return notify_fail("你不加入雪山寺，学不了金刚瑜伽母拳。\n");
    if ((int)me->query_skill("longxiang", 1) < 100 && (int)me->query_skill("xiaowuxiang", 1) < 100)
        return notify_fail("你的雪山寺内功似乎火候太浅。\n");
    if (me->query_skill("unarmed", 1) <=me->query_skill("yujiamu-quan", 1))
        return notify_fail("你的基础不够，无法领会更高深的技巧。\n");
	if (me->query("camp")!="正派")	
		return notify_fail("你需要加入正派阵营，才能修练该武功。\n");
	return 1;	
}
string query_skill_name(int level)
{
	int i;
	for(i = sizeof(action)-1; i >= 0; i--)
	if(level >= action[i]["lvl"])
		return action[i]["skill_name"];
}
mapping query_action(object me, object weapon)
{
	return action[random(sizeof(action))];	//2017.9.4阿飞改，将招式简单点
}
int practice_skill(object me)
{
//	if (!wizardp(me))
//		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
    if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
        return notify_fail("练金刚瑜迦母拳必须空手。\n");
    if ((int)me->query_skill("longxiang", 1) < 100 && (int)me->query_skill("xiaowuxiang", 1) < 100)
        return notify_fail("你的雪山寺内功似乎火候太浅。\n");
	if (me->query("family/family_name")!="雪山寺")
		return notify_fail("你不加入雪山寺，练习不了金刚瑜伽母拳。\n");
    if ((int)me->query("qi") < 40)
        return notify_fail("你的体力不够，练不了金刚瑜迦母拳。\n");
	if (me->query("camp")!="正派")	
		return notify_fail("你需要加入正派阵营，才能修练该武功。\n");
    me->receive_damage("qi", 20);
    return 1;
}

string perform_action_file(string action)
{
	return __DIR__"yujiamu-quan/" + action;
}

mixed hit_ob(object me, object victim, int damage_bonus, int factor, string limb)
{
	int ap,dp, damage1,damage2,damage,liannum;

    ap = me->query_skill("yujiamu-quan",1)*(me->query("breakup")+me->query("breakup")*me->query("breakup")+100)/100;
	dp = victim->query_skill("parry",1)*(victim->query("breakup")+victim->query("breakup")*victim->query("breakup")+100)/100;
    damage1= (int) me->query_skill("yujiamu-quan",1);
	damage2 = (int)me->query_skill("yujiamu-quan",1)*(me->query("breakup")+me->query("breakup")*me->query("breakup")+100)/100;
	if( me->query_temp("lianzhao")>=1) liannum=me->query_temp("lianzhao");
	else liannum =1;
	if( me->query("neili") < 100 || me->query("jingli") < 100 || damage_bonus < 100 ) return 0;
	if ( (me->query_skill("xiaowuxiang",1) < 100 && me->query_skill("longxiang",1) < 100) || me->query_skill("yujiamu-quan",1)<100) return 0;
//	if ( me->query_skill_mapped("force") != "xiaowuxiang" && me->query_skill_mapped("force") != "longxiang" ) return 0;
	if (objectp(me->query_temp("weapon"))) return 0;

	if (random(7)==1 && random(ap) > dp/3 && me->query("jiali") > 0)
	{
		damage = (damage1+damage_bonus)*(liannum+20)/(20*liannum+1);
		victim->receive_damage("qi", damage ,me);
		victim->receive_wound("qi", damage/3 ,me);
		me->add("neili", -100);
		if (!victim->is_busy()) victim->start_busy(1);
		message_vision(HIR "$N左手成爪，右手竖立胸前！面目狰狞，右掌猛地推出，念叨：嗔！一股大力向$n袭来！\n" NOR, me,victim,damage,HBWHT+HIR"特效伤气"NOR);
        return ;
	}
	if( random(ap) > dp/3 && (int)me->query("jiajing")>0 && random(7)==2 && (int)me->query("breakup")>1 )
	{
		damage = (damage2+damage_bonus)*(liannum+20)/(20*liannum+1);
		me->add("jingli",-100);
		victim->receive_damage("jing", damage,me);
		victim->receive_wound("jing", damage/3,me);
		message_vision(HIG "$N顿悟"+me->query("cognize")+HIG"，如同醍醐灌顶，十指盘作莲花，轻轻一推。$n顿时喷出数道血箭。\n" NOR, me,victim,damage,HBWHT+HIR"特效伤精"NOR);
		return ;
	}
	if( random(ap) > dp/3 && (int)me->query("jiali")>0 && random(7)==3 && (int)me->query("jm/xueshan") )
	{
		damage = ((damage1+damage2)/2+damage_bonus)*(liannum+20)/(20*liannum+1);
		me->add("neili",-100);
		victim->receive_damage("qi", damage,me);
		victim->receive_wound("qi", damage/3,me);
		message_vision(HIM"$N融会雪山寺武学精髓，一道雄浑的“般若极”掌力击中$n，一声闷哼，$n瘫倒在地。\n" NOR, me,victim,damage,HBWHT+HIR"特效伤气"NOR);
		return ;
	}
}
