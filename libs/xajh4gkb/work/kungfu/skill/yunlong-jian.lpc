// yunlong-jian.c 云龙剑	中乘武学200~600
#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
inherit SKILL;
mapping *action = ({
  ([ "action": "\n$N使一式「" + YEL + "悠悠顺自然" + NOR + "」，手中$w嗡嗡微振，幻成一条白光刺向$n的$l",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "lvl": 0,
    "skill_name": YEL "悠悠顺自然" NOR,
    "damage_type": "刺伤"
  ]),
  ([ "action": "\n$N错步上前，使出「" + MAG + "春染一片绿" + NOR + "」，剑意若有若无，$w淡淡地向$n的$l挥去",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "lvl": 8,
    "skill_name": MAG "来去若梦行" NOR,
    "damage_type": "割伤"
  ]),
  ([ "action": "\n$N一式「" + WHT + "志当存高远" + NOR + "」，纵身飘开数尺，运发剑气，手中$w遥摇指向$n的$l",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "lvl": 15,
    "skill_name": WHT "志当存高远" NOR,
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N纵身轻轻跃起，一式「" + HIR + "表里俱澄澈" + NOR + "」，剑光如水，一泻千里，洒向$n全身",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "lvl": 20,
    "skill_name": HIR "表里俱澄澈" NOR,
    "damage_type": "割伤"
  ]),
  ([ "action": "$N手中$w中宫直进，一式「" + BLU + "随风潜入夜" + NOR + "」，无声无息地对准$n的$l刺出一剑",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "lvl": 30,
    "skill_name": BLU "随风潜入夜" NOR,
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N手中$w一沉，一式「" + HIB + "润物细无声" + NOR + "」，无声无息地滑向$n的$l",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 40,
    "skill_name": HIB "润物细无声" NOR,
    "damage_type": "割伤"
  ]),
  ([ "action": "\n$N手中$w斜指苍天，剑芒吞吐，一式「" + WHT + "河山万木春" + NOR + "」，对准$n的$l斜斜击出",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 50,
    "skill_name": WHT "云龙听梵音" NOR,
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N左指凌空虚点，右手$w逼出丈许雪亮剑芒，一式「" + RED + "万里一点红" + NOR + "」刺向$n的$l",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 60,
    "skill_name": RED "万里一点红" NOR,
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N合掌跌坐，一式「" + HIM + "我心化云龙" + NOR + "」，$w自怀中跃出，如疾电般射向$n的$l",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 70,
    "skill_name": HIM "我心化云龙" NOR,
    "damage_type": "刺伤"
  ]),
  ([ "action": "\n$N呼的一声拔地而起，一式「" + HIG + "日月与同辉" + NOR + "」，$w幻出万道光影，将$n团团围住",
    "attack": 400,
    "parry": 400,
    "dodge": 400,
    "damage": 400,
    "force": 400,
    "lvl": 80,
    "skill_name": HIG "日月与同辉" NOR,
    "damage_type": "内伤"
  ]),
  ([ "action": "$N随风轻轻飘落，一式「" + GRN + "清风知我意" + NOR + "」，手中$w平指，缓缓拍向$n脸颊",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "lvl": 90,
    "skill_name": GRN "清风知我意" NOR,
    "damage_type": "内伤"
  ]),
  ([ "action": "$N剑尖微颤作龙吟，一招「" + HIW + "高处不胜寒" + NOR + "」，切骨剑气如飓风般裹向$n全身",
    "attack": 500,
    "parry": 500,
    "dodge": 500,
    "damage": 500,
    "force": 500,
    "lvl": 100,
    "skill_name": HIW "高处不胜寒" NOR,
    "damage_type": "内伤"
  ]),
  ([ "action": "$N募的使一招「" + MAG + "红叶舞秋山" + NOR + "」，顿时剑光中几朵血花洒向$n全身",
    "attack": 600,
    "parry": 600,
    "dodge": 600,
    "damage": 600,
    "force": 600,
    "lvl": 110,
    "skill_name": MAG "红叶舞秋山" NOR,
    "damage_type": "内伤"
  ]),
});
int valid_enable(string usage) { return usage == "sword" || usage == "parry"; }
//2019-1-1阿飞新规划，技能代码完善
//string valid_combine() { return "yunlong-zhua"; }		//互备技能
string skill_pinji() { return "中乘"; }    //品级区分：上乘、中乘、下乘
string skill_xingbie() { return "通用"; }    //性别区分：男性、女性、无性、通用
string skill_zhengxie() { return "中立"; }   //正邪区分：正道、邪道、中立
string skill_menpai() { return "云龙门"; }    //门派区分：门派，江湖
string skill_yinyang() { return "中性"; }    //阴阳区分：阳、阴、中性
string skill_zhenying() { return "正派"; }   //阵营区分：正派、邪派、中立、通用
int damage_damage() { return 65; }  //外伤系数
int force_damage() { return 40; }  //内伤系数
//int qi_heal() {return 50;}		//内功疗气系数
//int jing_heal() {return 50;}		//内功疗精系数
int practice_level() { return 100; }  //自练级别
int practice_add() { return 100; }  //自练效率
int perform_level() { return 300; }  //PFM最低使用级别

int valid_learn(object me) {
  object weapon;
  //	if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
  if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "sword")
    return notify_fail("你必须先找把剑才能学剑法。\n");
  if ((int)me->query("max_neili") < 10000)
    return notify_fail("你的内力不够。\n");
  if ((int)me->query_skill("yunlong-xinfa", 1) < 300)
    return notify_fail("你的云龙心法火候太浅。\n");
  if (me->query("family/family_name") != "云龙门")
    return notify_fail("不加入云龙门，学不了本门技能。\n");
  if ((int)me->query_skill("yunlong-shengong", 1) < 300)
    return notify_fail("你的云龙神功火候不够，无法学习本门技能。\n");
  if ((int)me->query_skill("force", 1) < 300)
    return notify_fail("你的基本内功火候太浅。\n");
  if (me->query_skill("sword", 1) <= me->query_skill("yunlong-jian", 1))
    return notify_fail("你的基础不够，无法领会更高深的技巧。\n");
  if ((int)me->query("score", 1) < 5000)
    return notify_fail("你对我派毫无贡献而言，怎么可能传授你武功呢。\n");
  if (me->query("camp") != "正派")
    return notify_fail("你需要加入正派阵营，才能修练该武功。\n");
  if (random(100) == 1) me->add("score", -5);
  return 1;
}
string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  return action[random(sizeof(action))];  //2017.9.4阿飞改，将招式简单点
}

int practice_skill(object me) {
  object weapon;
  //	if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
  if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "sword")
    if (!objectp(weapon = me->query_temp("secondary_weapon")) || (string)weapon->query("skill_type") != "sword")
      return notify_fail("你使用的武器不对。\n");
  if (me->query("family/family_name") != "云龙门")
    return notify_fail("不加入云龙门，练习不了本门技能。\n");
  if ((int)me->query("qi") < 50)
    return notify_fail("你的体力不够练云龙剑法。\n");
  if (me->query("camp") != "正派")
    return notify_fail("你需要加入正派阵营，才能修练该武功。\n");
  me->receive_damage("qi", 25);
  return 1;
}
string perform_action_file(string action) {
  return __DIR__ "yunlong-jian/" + action;
}

mixed hit_ob(object me, object victim, int damage_bonus, int factor, string limb) {
  string msg;
  int ap, dp, damage1, damage2, damage, liannum;
  object weapon;
  weapon = me->query_temp("weapon");
  ap = me->query_skill(
    "yunlong-jian",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  dp = victim->query_skill(
    "parry",
    1
  ) * (victim->query("breakup") + victim->query("breakup") * victim->query("breakup") + 100) / 100;
  damage1 = (int)me->query_skill("yunlong-jian", 1);
  damage2 = (int)me->query_skill(
    "yunlong-jian",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  if (me->query_temp("lianzhao") >= 1) liannum = me->query_temp("lianzhao");
  else liannum = 1;
  if (me->query("neili") < 100 || me->query("jingli") < 100 || damage_bonus < 100) return 0;
  if (me->query_skill(
    "yunlong-shengong",
    1
  ) < 100 || me->query_skill("yunlong-jian", 1) < 100) return 0;
  //	if ( me->query_skill_mapped("force") != "yunlong-shengong") return 0;
  if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "sword") return 0;

  if (random(7) == 1 && random(ap) > dp / 3 && me->query("jiali") > 0) {
    damage = (damage1 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    victim->receive_damage("qi", damage, me);
    victim->receive_wound("qi", damage / 3, me);
    me->add("neili", -100);
    message_vision(
      MAG BLINK "你听到「嗤」一声轻响，好似凉风吹过，忽然间$n一道鲜血飙出！\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤气" NOR
    );
    return;
  }
  if (random(ap) > dp / 3 && (int)me->query("jiajing") > 0 && random(7) == 2 && (int)me->query("breakup") > 1) {
    damage = (damage2 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    msg = HIG "$N顿悟" + me->query("cognize") + HIG "，手中" + weapon->name() + HIG "如潜龙出渊刺向$n，带起一片片红叶，细看之下竟然鲜血点点。\n" NOR;
    victim->receive_damage("jing", damage, me);
    victim->receive_wound("jing", damage / 3, me);
    me->add("jingli", -100);
    message_vision(msg, me, victim, damage, HBWHT + HIR "特效伤精" NOR);
    return;
  }
  if (random(ap) > dp / 3 && (int)me->query("jiali") > 0 && random(7) == 3 && (int)me->query("jm/yunlong")) {
    damage = ((damage1 + damage2) / 2 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    me->add("neili", -100);
    victim->receive_damage("qi", damage, me);
    victim->receive_wound("qi", damage / 3, me);
    message_vision(
      HIM "$N融汇云龙门武学精髓，手中" + weapon->name() + HIM "顿时一弯，身形紧跟着一扭，一阵龙吟声响起，只见$N身化飞龙，快如闪电刺向$n，一道血光射出。\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤气" NOR
    );
    return;
  }
}
