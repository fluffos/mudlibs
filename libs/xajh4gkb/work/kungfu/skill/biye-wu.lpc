// biye-wu.c 碧叶随风舞	下乘武学100~300
#include <ansi.h>
#include <skill.h>
#include <weapon.h>
#include <combat.h>
#include <condition.h>
inherit SKILL;
string *du = ({
  "ice_poison",
  "ill_dongshang",
  "ill_fashao",
  "ill_kesou",
  "ill_shanghan",
  "ill_zhongshu",
  "xx_poison",
  "cold_poison",
  "flower_poison",
  "rose_poison",
  "x2_poison",
  "sanpoison",
  "scorpion_poison",
  "qx_snake_poison",
  "anqi_poison",
  "yf_poison",
  "nx_poison",
  "chilian_poison",
  "yufeng_poison",
  "insect_poison",
  "tmzhuihun_poison",
  "tmpili_poison",
  "zm_poison",
});



mapping *action = ({
  ([ "action": "$N双手扬起，轻飘飘一式「叶落」，掌若落叶，向$n的$l拍去",
    "attack": 100,
    "parry": 100,
    "dodge": 100,
    "damage": 100,
    "force": 100,
    "skill_name": "叶落",
    "lvl": 0,
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N左脚为轴心，右脚一旋，双手交替挥舞，如竹叶翻飞，正是「叶舞」，向$n的$l拍下",
    "attack": 100,
    "parry": 100,
    "dodge": 100,
    "damage": 100,
    "force": 100,
    "skill_name": "叶舞",
    "lvl": 20,
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N身体高高跃起，左手立在胸前，右臂微曲，使一招「叶飘零」，向$n的$l和面门打去",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "skill_name": "叶飘零",
    "lvl": 40,
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N微一运劲，双掌青筋爆露，一式「枯叶」，将$n浑身上下都笼罩在重重掌影之中",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "skill_name": "枯叶",
    "lvl": 60,
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N双掌平摊，左掌缓缓收至肩，右掌猛向前推出，\n掌风带着破空声，一招「弹枝」，如竹枝反弹，直奔$n心窝而去",
    "attack": 200,
    "parry": 200,
    "dodge": 200,
    "damage": 200,
    "force": 200,
    "skill_name": "弹枝",
    "lvl": 80,
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N突然收掌，由腹部向前向上推出，掌风如涛，一式「叶如潮」，一片劲风直逼$n",
    "attack": 300,
    "parry": 300,
    "dodge": 300,
    "damage": 300,
    "force": 300,
    "skill_name": "叶如潮",
    "lvl": 100,
    "damage_type": "瘀伤"
  ]),
});

int valid_enable(string usage) {
  return usage == "strike" || usage == "unarmed" || usage == "parry";
}
//2019-1-1阿飞新规划，技能代码完善
//string valid_combine() { return "juehu-shou"; }		//互备技能
string skill_pinji() { return "下乘"; }    //品级区分：上乘、中乘、下乘
string skill_xingbie() { return "通用"; }    //性别区分：男性、女性、无性、通用
string skill_zhengxie() { return "中立"; }   //正邪区分：正道、邪道、中立
string skill_menpai() { return "唐门"; }    //门派区分：门派，江湖
string skill_yinyang() { return "阴"; }    //阴阳区分：阳、阴、中性
string skill_zhenying() { return "邪派"; }   //阵营区分：正派、邪派、中立、通用
int damage_damage() { return 35; }  //外伤系数
int force_damage() { return 64; }  //内伤系数
//int qi_heal() {return 50;}		//内功疗气系数
//int jing_heal() {return 50;}		//内功疗精系数
int practice_level() { return 100; }  //自练级别
int practice_add() { return 100; }  //自练效率
int perform_level() { return 300; }  //PFM最低使用级别

int valid_learn(object me) {
  //	if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	
  if (me->query("family/family_name") != "唐门")
    return notify_fail("你不是本门中人，无法领悟本门技能的精髓。\n");
  if (!me->query("jm/huanhua"))
    return notify_fail(CYN "这神州结义萧秋水好大的胆子，竟敢强闯我唐门禁地！\n" CYN "这位" + RANK_D->query_respect(me) + CYN "欲入我唐门，需得去浣花派为唐门了结此段恩怨！\n" NOR);
  if ((int)me->query("family_score") < 6)
    return notify_fail(CYN "这位" + RANK_D->query_respect(me) + CYN "对江湖不够了解啊，我派功夫恐怕不能传授于你。\n" NOR);
  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练碧叶随风舞必须空手。\n");
  if ((int)me->query_skill("biyun-xinfa", 1) < 100)
    return notify_fail("你的碧云心法火候不够，无法学碧叶随风舞。\n");
  if ((int)me->query("max_neili") < 10000)
    return notify_fail("你的内力太弱，无法练碧叶随风舞。\n");
  if ((int)me->query("int", 1) < 30)
    return notify_fail("以你的悟性恐怕难以领悟碧叶随风舞的意境。\n");
  if (me->query("camp") != "邪派")
    return notify_fail("你需要加入邪派阵营，才能修练该武功。\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"])
      return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  return action[random(sizeof(action))];  //2017.9.4阿飞改，将招式简单点
}

int practice_skill(object me) {
  //	if (!wizardp(me))
  //		return notify_fail("巫师突然从黑暗中冲了出来，对你大吼一声：该门派没开放。\n");	 
  if (me->query("family/family_name") != "唐门")
    return notify_fail("你不是本门中人，如何学到本门高深技能，我要告诉掌门去。\n");
  if (!me->query("jm/huanhua"))
    return notify_fail(CYN "这神州结义萧秋水好大的胆子，竟敢强闯我唐门禁地！\n" CYN "这位" + RANK_D->query_respect(me) + CYN "欲入我唐门，需得去浣花派为唐门了结此段恩怨！\n" NOR);
  if ((int)me->query("family_score") < 6)
    return notify_fail(CYN "这位" + RANK_D->query_respect(me) + CYN "对江湖不够了解啊，我派功夫恐怕不能传授于你。\n" NOR);
  if ((int)me->query("qi") < 30)
    return notify_fail("你的体力太低了。\n");
  if ((int)me->query("neili") < 20)
    return notify_fail("你的内力不够练碧叶随风舞。\n");
  if (me->query("camp") != "邪派")
    return notify_fail("你需要加入邪派阵营，才能修练该武功。\n");
  me->receive_damage("qi", 25);
  me->add("neili", -10);
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "biye-wu/" + action;
}


mixed hit_ob(object me, object victim, int damage_bonus, int factor) {

  int ap, dp, damage1, damage2, damage, liannum;

  ap = (int)me->query_skill("biye-wu", 1);
  dp = (int)victim->query_skill("unarmed", 1);
  damage1 = (int)me->query_skill("biye-wu", 1);
  damage2 = (int)me->query_skill(
    "biye-wu",
    1
  ) * (me->query("breakup") + me->query("breakup") * me->query("breakup") + 100) / 100;
  if (me->query_temp("lianzhao") >= 1) liannum = me->query_temp("lianzhao");
  else liannum = 1;
  if (me->query("neili") < 100 || me->query("jingli") < 100 || damage_bonus < 100) return 0;
  if (me->query_skill("biyun-xinfa", 1) < 100 || me->query_skill("biye-wu", 1) < 100) return 0;
  //	if ( me->query_skill_mapped("force") != "biyun-xinfa") return 0;	
  if (objectp(me->query_temp("weapon"))) return 0;

  if (random(7) == 1 && me->query("jiali") > 0 && random(ap) > dp / 3) {
    damage = (damage1 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    me->add("neili", -100);
    victim->receive_damage("qi", damage, me);
    victim->receive_wound("qi", damage / 3, me);
    victim->apply_condition(du[random(sizeof(du))], 25);
    message_vision(
      HIR "$N突然收掌，然后迅速发掌! 掌中尽然有毒砂，这掌风尽然是五颜六色的！$n硬起头皮，以掌硬接！！结果身中巨毒！!\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤气" NOR
    );
    return;
  }
  if (random(ap) > dp / 3 && (int)me->query("jiajing") > 0 && random(7) == 2 && (int)me->query("breakup") > 1) {
    damage = (damage2 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    me->add("jingli", -100);
    victim->receive_damage("jing", damage, me);
    victim->receive_wound("jing", damage / 3, me);
    victim->apply_condition(du[random(sizeof(du))], 25);
    message_vision(
      HIR "$N内力灌注双掌，发挥出" + me->query("cognize") + HIR "的力量，掌风聚而不散。\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤精" NOR
    );
    return;
  }
  if (random(ap) > dp / 3 && (int)me->query("jiali") > 0 && random(7) == 3 && (int)me->query("jm/tangmen")) {
    damage = ((damage1 + damage2) / 2 + damage_bonus) * (liannum + 20) / (20 * liannum + 1);
    me->add("neili", -100);
    victim->receive_damage("qi", damage, me);
    victim->receive_wound("qi", damage / 3, me);
    victim->apply_condition(du[random(sizeof(du))], 25);
    message_vision(
      HIR "$N精研唐门毒功，洞悉唐门武功的奥秘，挥掌间毒气四散。\n" NOR,
      me,
      victim,
      damage,
      HBWHT + HIR "特效伤气" NOR
    );
    return;
  }
}
