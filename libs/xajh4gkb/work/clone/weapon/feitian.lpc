//飞天剑--神兵
#include <weapon.h>
#include <ansi.h>
inherit SWORD;

void create()
{
	set_name( HIR "飞天" NOR , ({ "feitian sword","sword","jian"}));      	
	set_weight(8000);
	if (clonep())
		set_default_object(__FILE__);
	else {
		set("unit", "柄");
		set("icon", "10030");
		set("long", HIC"传说中爱神的随身配剑,据说是用仙界炼器之法锻铸九九八十一年而成.据说此剑有飞天遁地之神通,故而名为"HIR"飞天!\n");
		set("no_give",1);
		set("no_get",1);
		set("no_put",1);
		set("no_drop",1);
		set("no_sell",1);
		set("value", 20000);
		set("material", "steel");
		set("weapon_prop/karma", 30);
		set("weapon_prop/constitution", 30);
		set("weapon_prop/intelligence", 30);
		set("weapon_prop/strength", 30);		
		set("weapon_prop/dodge", 50);
		set("weapon_prop/sword", 50);
		set("weapon_prop/parry", 50);		
		set("wield_msg",HIC"$N冷哼一声,双手平举向天.........\n"
			HIR"刹那间风云变色.仿佛与$N杀气呼应一般,天也为之震怒.随着一声惊天的\n"
			HIY"        ∠∈...霹.....○...★...●.....雳...々№...\n"
			HIC"一道巨大的闪电落在$N的手中.随着电光的慢慢敛去,一把电光吞吐的双手巨剑也慢慢的展露在$N的手中.\n" NOR);	
		set("unwield_msg", HIM"随着$N"HIR"杀气"HIC"的减退,"HIY"神剑"HIR"飞天"HIW"的光芒也越来越平和.终于$N的"HIR"杀气"HIC"完全消失,"HIR"飞天"HIY"也化成一道神光消失于九天之外！" NOR);
	}
	init_sword(1000,1);
	setup();
}
int query_autoload() { return 1; } 
//void owner_is_killed() {destruct(this_object());}

mixed hit_ob(object me,object victim,int damage_bonus)
{
	int damage1,damage2;
	if( damage_bonus < 100 || this_object()->query("weapon_prop/damage")<100) return 0;
	damage1 = damage_bonus+this_object()->query("weapon_prop/damage")*5;
	damage2 = damage_bonus/2+this_object()->query("weapon_prop/damage");
	if( random(5)==1 ) 
	{
		damage1 += random(damage1);
		damage2 += random(damage2);
		victim->receive_damage("jing",damage1,me);
		victim->receive_wound("jing",damage2,me);
		message_vision(HIM "随着$N"HIR"杀气"HIC"的增加，流动于"HIR"飞天"HIC"剑身的"HIY"电光"HIC"忽然绽放出"HIR"妖异"HIY"的光芒！\n" NOR, me,victim,damage1,HBWHT+HIR"特效伤精"NOR);
		return ;
	}
}

int move(mixed dest)
{
	if(! userp(dest)) return ::move(dest);
	if( ::move(dest)==0) return 0;
	if (dest->query_temp("god_weapon")!=this_object()->query("id"))
	{
		message("rumor",HIR"【江湖传奇】"+ HIC"据说飞天楼主的神兵"+ this_object()->query("name")+HIC"被"+ dest->query("name")+"("+dest->query("id")+")拿到手了！\n"NOR,users());
		dest->set_temp("god_weapon",this_object()->query("id"));
	}
	return 1;
}
//2019-3-9阿飞，暂时关闭这段代码，老是报错
/*int wield()
{
	if(!userp(this_player())) return ::wield();
//	if ( !wizardp(this_player()) && userp(this_player()))
//		return notify_fail(HIW"神兵认主，暂时不是你能使用的，等待巫师调整。\n"NOR);
	if ( stringp(this_player()->query_temp("wield_god_weapon")) && (string)this_player()->query_temp("wield_god_weapon")!= this_object()->query("id"))
		return notify_fail(HIW"神兵择主，岂能与其他神兵共事一主。\n"NOR);		
	if (::wield())
	{
		if (userp(this_player())) this_player()->set_temp("wield_god_weapon",this_object()->query("id"));
		return 1;
	}
	return 0;
}     	
*/