//天神之剑（巫师专用）--神兵
#include <weapon.h>
#include <ansi.h>
inherit SWORD;
void create()
{
    set_name(MAG "天神之剑" NOR, ({ "tianshen sword", "jian","sword" }));
    set_weight(9000);
    if (clonep())
        set_default_object(__FILE__);
    else {
        set("unit", "柄");
		set("icon", "10027");
        set("long", "这是一柄天神之剑。听说是天神用来自杀的东东。\n");
        set("no_sell",1);
		set("no_get",1);
		set("no_give",1);
		set("no_drop",1);	
		set("value", 20000000); 	
        set("material", "steel");
		set("weapon_prop/karma", 20);
		set("weapon_prop/personality", 20);
		set("weapon_prop/intelligence", 20);
		set("weapon_prop/strength", 20);
		set("weapon_prop/dexerity", 20);
		set("weapon_prop/constitution", 20);
		set("weapon_prop/dodge", 50);
		set("weapon_prop/force", 50);
		set("weapon_prop/sword", 50);
		set("weapon_prop/parry", 50);		
        set("wield_msg", "$N「唰」的一声抽出一柄$n握在手中。$N刹那间双目尽赤。一副想杀人的样子\n");
        set("unwield_msg", "$N将手中的$n插回剑鞘。\n");
    }
    init_sword(1000,1);
    setup();
}
int query_autoload() { return 1; }   
void owner_is_killed() {destruct(this_object());}

void init()
{
    add_action("do_kill","kill");
}

int do_kill(string arg)
{
    object me,obj;
    if (!this_object()->query("equipped")) return 0;
    me=this_player();
    if (!userp(me) )
        return 0;
    if (!arg)   return 0;
    if(!objectp(obj = present(arg, environment(me))))
        return notify_fail("这里没有这个人。\n");
    if (userp(obj) ){
        message_vision("\n$N对著$n喝道：「" 
        + RANK_D->query_rude(obj) + "！今日不是你死就是我活！」\n\n", me, obj);
        tell_object(obj, HIR "如果你要和" + me->name() 
            + "性命相搏，请你也对这个人下一次 kill 指令。\n" NOR);
        tell_object( me,
        HIR "\n\n你决定要自杀了，如果四十秒内不後悔，就真的永别了。\n\n\n" NOR);
        CHANNEL_D->do_channel(this_object(),"rumor",me->name() + "决定要自杀了。");
        me->start_busy(40);
        call_out("slow_suicide",5,me,40);
        return 1;
    }
}
protected int slow_suicide(object me,int sec)
{
    if (!me)    return 0;
    sec-=5;
    if (sec>1){
        tell_object(me, RED "\n\n\n你还有"+YEL+chinese_number(sec)+"秒"+RED+"的时间可以後悔。\t\t"+HIW+"如果你後悔了，请马上退出(quit)游戏。\n\n\n" NOR);
        call_out("slow_suicide",5,me,sec);
    } else   call_out("do_suicide",1,me);
    return 1;
}
int do_suicide(object me)
{
    object link_ob;
    if (!me)    return 0;
    link_ob = me->query_temp("link_ob");
    if( !link_ob ) return 0;
    seteuid(ROOT_UID);
    write("好吧，永别了:)。\n");
    tell_room(environment(me), me->name() +"自杀了，以後你再也看不到这个人了。\n", ({me}));
    me->save();
    destruct(me);
    return 0;
}

int move(mixed dest)
{
	if(! userp(dest)) return ::move(dest);
	if( ::move(dest)==0) return 0;
	if (dest->query_temp("god_weapon")!=this_object()->query("id"))
	{
		message("rumor",HIR"【江湖传奇】"+ HIC"据说神兵"+ this_object()->query("name")+HIC"被"+ dest->query("name")+"("+dest->query("id")+")拿到手了！\n"NOR,users());
		dest->set_temp("god_weapon",this_object()->query("id"));
	}
	return 1;
}
mixed hit_ob(object me, object victim, int damage_bonus)
{
	if( damage_bonus < 100 || this_object()->query("weapon_prop/damage")<100) return 0;
	if( random(5)==1 ) 
	{
		victim->receive_damage("qi",(damage_bonus+this_object()->query("weapon_prop/damage")*2),me);
		victim->receive_wound("qi",(damage_bonus/2+this_object()->query("weapon_prop/damage")),me);
		victim->receive_damage("jing",(damage_bonus+this_object()->query("weapon_prop/damage")*2),me);
		victim->receive_wound("jing",(damage_bonus/2+this_object()->query("weapon_prop/damage")),me);
		return HIC"天神之剑爆发出一道天神之光，万物战战兢兢！\n" NOR;
	}
}
//2019-3-9阿飞，暂时关闭这段代码，老是报错
/*int wield()
{
	if(!userp(this_player())) return ::wield();
	if ( !wizardp(this_player()) && !userp(this_player()))
		return notify_fail(HIW"神兵认主，暂时不是你能使用的，等待巫师调整。\n"NOR);
	if ( stringp(this_player()->query_temp("wield_god_weapon")) && (string)this_player()->query_temp("wield_god_weapon")!= this_object()->query("id"))
		return notify_fail(HIW"神兵择主，岂能与其他神兵共事一主。\n"NOR);		
	if (::wield())
	{
		if (userp(this_player())) this_player()->set_temp("wield_god_weapon",this_object()->query("id"));
		return 1;
	}
	return 0;
}*/