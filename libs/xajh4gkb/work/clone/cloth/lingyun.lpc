//凌云靴--名器
#include <ansi.h>
#include <armor.h>
inherit BOOTS;

void create() {
  set_name(YEL "凌云靴" NOR, ({ "lingyun xue", "lingyun", "xue" }));
  set_weight(800);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("material", "leather");
    set("unit", "双");
    set("long", "这是一双用凤羽做成的靴子，光滑柔韧，据说有轻身培元的效果。\n");
    set("value", 8000000);
    set("armor_type", "boots");
    set("armor_prop/dodge", 10);
    set("armor_prop/dexerity", 10);
    set("armor_prop/personality", 10);
    set("armor_prop/armor", 600);
    set(
      "wear_msg",
      CYN "$N穿上一双$n," CYN "脚底眩出一道" HIM "七彩光华，" HIB "$N顿觉" HIC "全身一轻，" HIY "似欲" GRN "乘风而去。\n" NOR
    );
    set("remove_msg", GRN "华光顿敛，$N把$n轻轻地脱了下来。\n" NOR);
  }
  setup();
}
int query_autoload() { return 1; }

/*int move(mixed dest)
{
	if(! userp(dest)) return ::move(dest);
	if( ::move(dest)==0) return 0;
	if (dest->query_temp("god_armor")!=this_object()->query("id"))
	{
		message("rumor",HIM"【江湖传奇】"+ HIB"据说稀世珍品"+ this_object()->query("name")+HIB"被"+ dest->query("name")+"("+dest->query("id")+")拿到手了！\n"NOR,users());
		dest->set_temp("god_armor",this_object()->query("id"));
	}
	return 1;
}
*/
