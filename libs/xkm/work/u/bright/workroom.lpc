//write by bright
// 明亮工作室

#include <room.h>
#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", HIW"听雨轩"NOR);
        set("long", @LONG
 这是明亮工作的地方,墙上有个挂相(photo)。
LONG
        );
        set("valid_startroom", 1);
        set("no_fight",1);
        set("exits", ([
             "jhy" : "/u/jhy/workroom",
             "xkxmud" : "/u/xkxmud/workroom",
             "wr" : "/d/wizard/wizard_room",
             "xx" : "/d/xingxiu/riyuedong",
             "cs" : "/d/city/guangchang",
             "lj" : "/d/lingjiu/biguan",
             "pro" : "/u/program/workroom",
             "snow" : "/u/snow/workroom",
        ]));

        set("item_desc", ([
                "photo" : "明亮的肖像。\n",
        ]));

        set("no_clean_up", 0);
        setup();

        call_other("/clone/board/bright_b", "???");
        //"/clone/board/bright_b"->foo();

}
