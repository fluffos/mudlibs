// Room: /data/group/ppker/4.c / ppker
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "空房间");
  set("long", @LONG
 你可以用(set_long)来写描述  (set_short) 来写名称，用(add_desc)设定
景点，用(set_gate)来设定大门(add_npc)来为这房屋加入npc!!。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": "/data/group/ppker/group",
  ]));
  set("group", "ppker");
  set("no_clean_up", 0);

  setup();
}
