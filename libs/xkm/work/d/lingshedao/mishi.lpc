//Cracked by Roath
//fear 2002 2 1
#include <ansi.h>
inherit ROOM;
void create() {
  set("short", HIB "密室" NOR);
  set("long", @LONG
这里是一间密室，里面灰尘扑鼻，墙角结满了蜘蛛网。密室里的空气
有点霉味，闻了令人难受。角落里有张木桌，桌上摆放着几个武器，还有
一些零星的物件。
LONG
  );
  set("exits", ([
    "out": __DIR__ "woshi",
  ]));
  //        set("outdoors", "lingshedao");
  set("objects", ([ /* sizeof() == 1 */
    "/d/lingshedao/obj/jinhua": 1,
  ]));
  set("cost", 1);
  setup();
  replace_program(ROOM);
}
