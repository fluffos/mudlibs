//Cracked by Roath
// fear 2002 1 30
#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "瀑布");
  set("long", @LONG
这是一座小瀑布，波浪拍打着岸边石块。波涛奔泻而流入东边的一条
小河。岸上摆着一个武器架，似乎常有人於此练武。
LONG
  );
  set("exits", ([
    "west": __DIR__ "river1",
    "eastdown": __DIR__ "river2",
  ]));

  set("outdoors", "lingshedao");

  set("cost", 1);
  setup();
  replace_program(ROOM);
}
