//Cracked by Roath

//wzfeng 2002 1 21

inherit ROOM;

void create()
{
        set("short", "山路");
        set("long", @LONG
这里是岛屿近海处，四周树木葱翠，花开遍地。前面是一片山崖，
西边似有一条小路通向海岸，东面能隐隐看见几座山峰。
LONG
        );
        set("exits", ([
                "west" : __DIR__"xiaolu",
				 "east" : __DIR__"shanlu1",
				 "up" : __DIR__"shanya",
        ]));

	set("outdoors", "lingshedao");

	set("cost", 2);
        setup();
	replace_program(ROOM);
}

