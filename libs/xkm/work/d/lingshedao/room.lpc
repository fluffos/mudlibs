//Cracked by Roath
#include <ansi.h>
//wzfeng 2002 1 21

inherit ROOM;

void create()
{
        set("short", GRN"前院"NOR);
        set("long", @LONG
这里是孤岛群山中的一座民房，石砌而成，上盖砖瓦，四周花草盛开，
娇艳动人，似乎有人精心照料。房前有一院子，四周用篱笆围住，院中有
些饲养的鸡鸭，顿时给整个孤岛带来勃勃生机。
LONG
        );
        set("exits", ([
               "east" : __DIR__"shuichi",
				 "enter" : __DIR__"room1",
        ]));

		set("outdoors", "lingshedao");
		set("cost", 1);
		setup();
        "/clone/board/lingshe_b"->foo();
		replace_program(ROOM);
}

