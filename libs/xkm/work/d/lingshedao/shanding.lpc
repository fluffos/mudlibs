//Cracked by Roath
//fear 2002 2 1
#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", "山顶");
        set("long", @LONG
灵蛇岛的山顶的周围有很多光秃秃的山石，山顶上孤零零的盖着一所
茅屋。山顶上隐隐可以听见海中波涛之声。茅屋前面有些树木野草，一阵
朔北风吹来，刮得草木猎猎作响，更增一番凄怅悲凉之意。
LONG
        );
        set("exits", ([
                 "south" : __DIR__"shandian",
                 "enter" : __DIR__"maowu",
        ]));

	set("outdoors", "lingshedao");

	set("cost", 1);
        setup();
	replace_program(ROOM);
}

