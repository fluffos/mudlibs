//Cracked by Roath
// fear 2002 2 1
#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", GRN"竹林"NOR);
        set("long", @LONG
这里是一片茂密的竹林，枝叶密密重重，阳光都被遮蔽了起来，竹林
里的景色显得非常幽静。东南边有条小径，往前走去可以听见潺潺溪水声。
LONG
        );
        set("exits", ([
               "southeast" : __DIR__"river1",
        ]));

		set("outdoors", "lingshedao");
        set("objects", ([ /* sizeof() == 1 */
        "/d/xiakedao/obj/pole" : 1,
         ]));

		set("cost", 1);
		setup();
		replace_program(ROOM);
}

