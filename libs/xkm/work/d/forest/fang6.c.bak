inherit ROOM;
#include <ansi.h>;
void create()
{
        set("short", HIR"凤翼堂练功房"NOR);
        set("long", "这是凤翼堂香主专用练功的地方。
"NOR);
        set("exits", ([ 
"dating":_DIR_"dating2",
"yushi": _DIR_"room1",
       ]));
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}
