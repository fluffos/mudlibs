// mrdamen.c
// By hotjc 2001.11.3

inherit ROOM;

void create() {
  set("short", "慕容大门");
  set("long", @LONG
这里是慕容的大门只看这门就知道里面一定很气派,抬头看去只见
门楣上书「琴韵小筑」四字只见这字写的是龙飞凤舞并不拘泥与成法似
乎使用将某种上乘武功融会其中.
LONG
  );
  set("exits", ([
    "enter": __DIR__ "qing1",
    "south": __DIR__ "xiaodao",
  ]));


  setup();
  replace_program(ROOM);
}
