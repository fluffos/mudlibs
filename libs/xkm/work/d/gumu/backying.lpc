//Cracked by Roath
// 蒙古后营
// Modified by pro

inherit ROOM;
#include <ansi.h>;
void create()
{
        set("short", "大营后");
	set("long", @LONG
这里是主帅营的背后，防守比较薄弱。刚刚主帅匆匆逃走，一封火
漆封包的密信丢在了地上。
LONG
	);
        set("outdoors", "gumu");

	set("exits", ([
                 "xiaolu" : "/d/zhongnan/guolin",
	]));

	set("cost", 1);
	setup();
//	replace_program(ROOM);
}

