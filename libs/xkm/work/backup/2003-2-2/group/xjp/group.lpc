// Room: /data/group/xjp/group.c / xjp
#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "仙剑派 [1;32m总舵[37;0m");
	set("long", @LONG
总舵是帮派最重要的地方，也是帮派的精神象徵，平时帮主与长老们谈论
帮派重要政策的最佳场所，总舵的存在与否关系着帮派能不能存在，所谓舵在
帮在，舵亡帮亡，希望属下能好好保护这里。 
LONG
	);
	set("room_max", 80);
	set("tax", 5);
	set("no_clean_up", 0);
	set("npc_max", 40);
	set("item_max", 40);
	set("no_change", 1);
	set("group", "xjp");
	set("valid_startroom", 1);
	set("people", 11);
	set("gold", 1501);
	set("group_name", "仙剑派");
	set("story", 1);
	set("no_delete", 1);
	set("master", "londe");

	setup();
	replace_program(ROOM);
}
