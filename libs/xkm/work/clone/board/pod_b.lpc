//pod's board

#include <ansi.h>
inherit BULLETIN_BOARD;

void create() {
  set_name(HIG "天使画册" NOR, ({ "board" }));
  set("location", "/u/pod/workroom");
  set("board_id", "pod_b");
  set("long", HIG "让这片空白留下你五彩的足迹。\n" NOR);
  setup();
  set("capacity", 100);
}
