// /u/dubei/xingyan.c

inherit ROOM;

void create()
{
	set("short", "星岩");
	set("long", @LONG
顺山道爬到这里，只见四周壁立，山上密布石沟、石笋、石块。从山顶眺
望这里宛如银河星辰，故名星岩。一条小路通向山顶，崎岖难行。
LONG
	);
        set("outdoors", "黑木崖");

	set("exits", ([
		 "southup" : __DIR__"zzquan",
		 "north" :__DIR__"xxtan",
	]));

        set("objects", ([
//                __DIR__"npc/ " : 1,
        ]));
	setup();
	replace_program(ROOM);
}

