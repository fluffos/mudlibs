// /u/dubei/xxtan.c

inherit ROOM;

void create()
{
	set("short", "猩猩滩");
	set("long", @LONG
只见这里山石殷红如血，一片长滩，水流湍急，两旁峭壁拔地而起，如
刀砍斧劈。让人心理发出一丝丝震撼。原来到了有名的猩猩滩了。
LONG
	);
        set("outdoors", "黑木崖");

	set("exits", ([
		 "south" : __DIR__"xingyan",
		 
	]));

        set("objects", ([
//                __DIR__"npc/ " : 1,
        ]));
	setup();
	replace_program(ROOM);
}

