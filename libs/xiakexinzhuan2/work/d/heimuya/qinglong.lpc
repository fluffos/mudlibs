// /u/dubei/qinglong.c

inherit ROOM;

void create()
{
	set("short", "青龙堂");
	set("long", @LONG
这里是青龙堂。堂内柱子上雕着盘龙，四面黑沉沉的，只有前方一束火
把烧得噼啪作响，一人正借着光亮看什么东西，此人正是青龙堂堂主“黄面
尊者”贾布。
LONG
	);
        set("exits", ([
		  "north" :__DIR__"zongtan",
                   "southup" :__DIR__"yixian",                 

	]));

        set("objects", ([
//                __DIR__"npc/ " : 1,
        ]));
	setup();
	replace_program(ROOM);
}

