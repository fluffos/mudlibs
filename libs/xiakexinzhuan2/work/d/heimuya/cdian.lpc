// /u/dubei/cdian.c

inherit ROOM;

void create()
{
	set("short", "成德殿门");
	set("long", @LONG
这是座汉白玉大门，门朝南，三门并排，其间连以红色短垣，柱头的云
板和异兽，构成门上的装饰，结构奇特。门上挂着厚厚的帷幕。
LONG
	);
     
          set("exits", ([
		 "south" : __DIR__"cdian1",
		 "west" :__DIR__"zoulang",
	]));

        set("objects", ([
//                __DIR__"npc/ " : 1,
        ]));
	setup();
	replace_program(ROOM);
}

