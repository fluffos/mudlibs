// /u/dubei/dadao.c

inherit ROOM;

void create()
{
	set("short", "大道");
	set("long", @LONG
这是一条笔直的石板大路，每块石板都有桌面大小，平平整整。若在
别处毫不希奇，可此处高临绝顶，运这么多这么大的石板不知要花费多少
力气。
LONG
	);
        set("outdoors", "黑木崖");

	set("exits", ([
		 "northeast" : __DIR__"caoan",
		 "southwest" :__DIR__"yunmen",
	]));

        set("objects", ([
//                __DIR__"npc/ " : 1,
        ]));
	setup();
	replace_program(ROOM);
}

