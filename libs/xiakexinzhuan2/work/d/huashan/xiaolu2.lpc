// yunu.c

inherit ROOM;

void create()
{
        set("short", "玉女峰小径");
	set("long", @LONG
走完这条小径，便能到达峰顶的思过崖。
LONG
        );
        set("exits", ([ /* sizeof() == 3 */
  "southdown" : __DIR__"xiaolu1",
  "northup" : __DIR__"siguoya",
]));
        set("outdoors", "华山" );

        setup();
        replace_program(ROOM);
}
 
