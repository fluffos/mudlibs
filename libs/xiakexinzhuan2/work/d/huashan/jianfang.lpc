//Room: jianfang.c
//YZC 1995/12/04

#include <room.h>
inherit ROOM;

void create()
{
        set("short", "兵器房");
	set("long", @LONG
“剑”并不重要，而是持剑人的修为，一个剑客的剑不应该在手上，
这是华山派的“武器库”剑房，华山弟子的佩剑都是从这里取用。
LONG
        );
set("objects",([
__DIR__"obj/zhujian" : 2,
__DIR__"obj/changjian" : 4,
]));

        set("exits",([
            "west" : __DIR__"liangong",
]));
setup();
}
 
