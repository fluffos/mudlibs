// letters.c
inherit ITEM;

void create() {
  set_name("大轮寺法王手谕", ({ "shou yu" }));
  set_weight(50);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "封");
    set("long",
      "这是金轮法王的亲笔手谕，可以号令密宗所有弟子。\n");
    set("material", "paper");
  }
}
