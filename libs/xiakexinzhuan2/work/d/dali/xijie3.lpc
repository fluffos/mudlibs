inherit ROOM;

void create()
{
    set("short", "大理城西大街");
	set("long", @LONG
这是大理西大街，大理百姓把这里叫三月街，又叫观音街。是白族三月
贸市古街。北面是一个小裁缝店，进进出出不少附近的居民，南面是一家
风味店，有个种名小吃卖，很是热闹。
LONG
	);
        set("outdoors", "dali");
	set("exits", ([ /* sizeof() == 2 */
	   "east" : __DIR__"xijie1",
	   "west" : __DIR__"ximen",
	  "south" : __DIR__"fengwei",
	  "north" : __DIR__"caifeng",
]));
      set("objects", ([
                    __DIR__"npc/haoke" : 1,
	]));

	setup();
//	replace_program(ROOM);
}
