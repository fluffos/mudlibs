inherit ROOM;

void create()
{
set("short", "玉虚观");
	set("long", @LONG
这里就是玉虚观内了，迎面大亭供着元始天尊的金身塑像，左
手边还有一尊略小点的太乙真人金身塑像，大厅中央站着一位中年
道姑，手里拿着杆拂尘。
LONG
	);
        set("outdoors", "dali");
	set("exits", ([ /* sizeof() == 1 */
	    "out" : __DIR__"yuxuguanmen",
	    "east" : __DIR__"shuifang",
           "west" : __DIR__"shufang1",

]));
       set("objects", ([
	__DIR__"npc/yuxu" : 1 ]) );

	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
