inherit ROOM;

void create()

{
       set("short", "御道");
	set("long", @LONG
这是一片大广场，你走在广场中间的御道上，这道是用青砖
铺就的，道两旁都是守护皇宫的卫兵。
LONG
	);
        set("outdoors", "dali");
	set("exits", ([ 
	    "south" : __DIR__"yudao1",
	    "north" : __DIR__"yudao3",
	]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
