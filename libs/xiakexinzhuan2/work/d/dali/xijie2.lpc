// Room: /dali/xijie2.c
// bbb 1997/06/11

inherit ROOM;

void create()
{
    set("short", "大理城西大街");
	set("long", @LONG
这里相对于西边的三月贸市街和东边的城中心，显得安静了好多，南边
一座衙门，极少有人进出。北边茶馆的旗帜迎风轻轻的在飘，一些茶客在里
面边喝茶，边聊着天。
LONG
	);
        set("outdoors", "dali");
	set("exits", ([ /* sizeof() == 2 */
   "east" : __DIR__"dalics",
   "west" : __DIR__"xijie1",
  "south" : __DIR__"yamen",
  "north" : __DIR__"chaguan",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
