// by cool 98.2.20
inherit ROOM;

void create()
{
	set("short", "善阐侯府");
	set("long", @LONG
这座府邸规模略小。门口上悬一大牌，几个金字“善阐侯府”，善
阐侯高升泰是镇南王的结义兄弟，多年来为皇上立下战功，特赐此府
邸。府内堂的的门都紧闭着，没有什么出口。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
     "south" : __DIR__"dongjie4",
]));

 set("objects", ([
            __DIR__"npc/bing" : 3,
            __DIR__"npc/gaoshengtai" : 1,
]) );


	setup();

	replace_program(ROOM);
}
