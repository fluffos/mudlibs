inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIC "绸缎庄" NOR);
  set("long", @LONG
一进门。老板就热情的迎上来了，这家铺面很大，这儿所卖的绸缎大多都是
从中原购来的，另外也卖一些当地产布匹。
LONG
  );
  set("exits", ([
    "west": __DIR__ "nanjie4",
  ]));

  //        set("objects", ([
  //                       "/d/city/npc/jiading" : 2 
  //]) );
  setup();
  replace_program(ROOM);

}
