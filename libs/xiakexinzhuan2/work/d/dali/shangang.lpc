inherit ROOM;

void create()
{
set("short", "山岗");
	set("long", @LONG
你转过了一道山岗，迎面的是一条笔直大道，道边种有青青杨柳
树，随风飘扬。
LONG
	);
        set("outdoors", "dali");
	set("exits", ([ /* sizeof() == 1 */
	    "north" : __DIR__"dadao5",
	    "east" : __DIR__"xiaodao2",
]));
set("objects", ([
		__DIR__"npc/dadao" : 3,
		]));

	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
