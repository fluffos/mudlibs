inherit ROOM;
#include <ansi.h>
void create()
{
        set("short",HIC"茶花园"NOR);
      	set("long", @LONG
你走在一个小山坡上，这里到处都是盛开的鲜花，你有点眼花缭乱了，
你漫步在这花园中，心情舒畅许多。 
LONG
);
        set("exits", ([ 
        "northup" : __DIR__"chahuayuan3",
        "southdown" : __DIR__"chahuayuan",
	]));

         set("objects", ([
                        "/d/dali/obj/chahua2" : 1,
]) );
      setup();
	replace_program(ROOM);

}

