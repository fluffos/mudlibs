// Room: /d/city/yizhan.c

inherit ROOM;

void create()
{
	set("short", "驿站");
	set("long", @LONG
这是扬州驿站，负责传送和京城里的往来公文。因为暂时还不开放给百姓
使用，所以没什么可参观的。
LONG
	);

	set("exits", ([
		"west" : __DIR__"beidajie3",
	]));

/*	set("objects", ([
		__DIR__"npc/xiaoer2" : 1,
	]));*/

	setup();
	replace_program(ROOM);
}

