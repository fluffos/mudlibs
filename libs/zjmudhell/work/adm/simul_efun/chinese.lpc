string chinese_number(int i) {
  return CHINESE_D->chinese_number(i);
}

string to_chinese(string str) {
  return CHINESE_D->chinese(str);
}

int is_chinese(string str) {
  int i;

  if (strlen(str) < 1) return 0;

  // On this driver strlen() counts characters (not GBK byte pairs) and
  // str[i] is a Unicode codepoint (not a raw GBK byte) -- check the CJK
  // Unified Ideographs codepoint range instead.
  for (i = 0; i < strlen(str); i++)
    if (str[i] < 0x4e00 || str[i] > 0x9fff) return 0;

  return 1;
}
