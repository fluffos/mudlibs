// event_room.c by 活 ghoster@ms14.url.com.tw

inherit ROOM;
#include <ansi.h>

void create()
{
	::setup();
}

void init()
{
	add_action("do_go","go");
}

int do_go(string direction)
{
  string event,exit,exit1;
  int e_happen;
  object me;

  if(!direction){return 0;}

  me=this_player();
  event=me->query("event");
  e_happen=environment(me)->query("e_happen")*2; 

  if(!e_happen)
    {write("祇瞷Τㄇ拜肈.....,叫盢癟禗wiz!!\n");
     return 1;}

  exit=environment(me)->query("event_exit");
  if(direction==exit)
  {
     exit1=event[e_happen..e_happen];  //exit1=材e_happenㄆンㄆン絏
     if(environment(me)->query("event_exit"+exit1))
        me->move(environment(me)->query("event_exit"+exit1));
     else me->move(environment(me)->query("event_exit0"));
        //讽exit1!=0event_exit1,event_exit2....ㄤいㄆン┬丁
        //exit1=0┪exit1ぃボぃ祇ネヴ︙ㄆン┬丁
     return 1;
  } 
}