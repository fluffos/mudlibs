// fighter_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("猌痙ē狾", ({ "board" }) );
	set("location", CLASS_D("fighter") + "/guildhall");
	set("board_id", "fighter_b");
	set("long",	"硂琌ㄑ猌狶い痙ē癘ㄆ痙ē狾\n" );
	setup();
	set("capacity", 30);
	replace_program(BULLETIN_BOARD);
}
