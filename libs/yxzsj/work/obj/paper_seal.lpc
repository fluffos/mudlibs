// paper_seal.c

inherit COMBINED_ITEM;

void create()
{
	set_name("才", ({"paper seal", "seal", "paper"}));
	set("long",
		"硂琌笵ノㄓ礶才才ㄣΤ猭\ノ\n");
	set("unit", "舼");
	set("base_unit", "眎");
	set("base_weight", 5);
	set_amount(1);
}

