#include <armor.h>
#include <command.h>
int chest_found=0;
inherit NPC;
void create()
{
    object ob;
  set_name("笵担",({"boy"}) );
  set("long","琌︗担睲苯\n");
  set("gender","╧┦");
  set("race", "摸");
  set("age",14);
  set("combat_exp",1200);
  set("max_kee",750);
  set("max_gin",600);
  set("max_sen",600);
  set("max_force",300);
  set("force",600);
  set("force_factor", 30);
  set("warexp",10);
  set_skill("dodge",5);
  set_skill("parry",10);
  set("class","taoist");
  set_temp("apply/dodge", 30);
  set_temp("apply/sword", 30);
  set_temp("apply/damage", 30);
  set_temp("apply/armor", 30);
  set("chat_chance",4);
          set("chat_msg",  ({
                          (: command, "east" :),
                          (: command, "west" :),
                  }) );
set("inquiry", ([
    "獺":"硂璶拜ρ穦笵\n",
    "ρ":"ΩΤ籄λ狶笵苯ê娩тт\n",
    "籄λ狶笵":"и⊿筁瑿╆簆ぃ筁и钮弧笵芠﹁よΤλ狶\n",

]));
 
 
setup();
      carry_object("/d/reason/obj/broom.lpc")->wield();
      carry_object("/obj/cloth.lpc")->wear();
}
int accept_object(object who,object ob)
{

if( ob->id("blue stone") ) {
command("consider");
command("giggle");
command("say 垛ρ氮す瓳ê秈舘\n");
command("addoil");
            who ->set_temp("marks/秈",1);
             return 1;
           }
         }
