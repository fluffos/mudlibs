#include <armor.h>
#include <ansi.h>
inherit HEAD;

void create()
{
        set_name("彩ガ︾", ({ "suit" }) );
        set_weight(3000);
        if( clonep() )
                set_default_object(__FILE__);
        else {
        set("unit", "眎");
                set("value", 2000);
                set("material", "cloth");
                set("armor_prop/armor", 1);
                set("armor_prop/id", ({"big byg"}) );
                set("armor_prop/name", ({"ψ"}) );
                set("armor_prop/short", ({" 冻  眏┮ ⊿titleψ(Big Byg)
"}) );
                set("armor_prop/title", ({"硚"}) );
                set("armor_prop/long", ({
                        " 冻  眏┮ ⊿titleψ(Big Byg)
\n"
                }) );
        }
        setup();
}