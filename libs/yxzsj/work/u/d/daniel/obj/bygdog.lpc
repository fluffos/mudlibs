// bygdog.c
#include <ansi.h>
inherit NPC;
inherit MOUNT;
 
void create()
{
        set_name(HIG""HIY"ψ"NOR, ({ "byg dog","dog","byg" }) );
        set("race", "偿脋");
        set("age", 100);
        set("long", "硂琌唉伐Τ艶┦猌\獶盽蔼眏刚帝肕(mount)\n");
        set("value", 100000);
        set("str", 200);
        set("cor", 260);
 
        set("limbs", ({ "繷场", "ō砰", "玡竲", "繴竲", "浮场", "竲", "Юぺ" }) );
        set("verbs", ({ "bite", "claw" }) );
        
        set("chat_chance", 10);
        set("chat_msg", ({
                (: command, "dog" :),
		    (: command, "roll" :),
		    (: command, "worm" :),
		    (: command, "say иψ..." :),
                })
        );

        set("food",150);
        set("combat_exp",10000);//肕硂で皑┮惠蔼exp(Τ皑ㄓ絤\so..
                               //钡riding lv ㄓ﹚惠璶单....)
        set("move",50);//肕硂で皑┮糤move lv
        set("dodge",50);//肕硂で皑┮糤dodge lv
        set("power",100);//肕硂で皑┮糤ю阑
        set("armor",100);//肕硂で皑┮糤ň縨
        set_skill("riding",100);//肕硂で皑┮惠riding lv
        set("addmove",5000);//肕硂で皑┮糤璽
        set("type","horse");
        setup();
}
