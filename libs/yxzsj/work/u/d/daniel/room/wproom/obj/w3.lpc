#include <weapon.h>
#include <ansi.h>
inherit F_UNIQUE;
inherit BLADE;  
 
void create()
{
        set_name(HIY"礡"HIC"à"HIW""NOR, ({"rhinoceros@ blade","blade"}) );
        set_weight(2500);
        if(clonep() )
                set_default_object(__FILE__);
        else {
                set("unit", "р");
                set("long", "硂琌р礡獶盽腳窾炳┮Τ琌ぇㄣΤ玠耞ホ\n");
                set("value",15000);
                set("material", "steel");
                set("wield_msg", "$N眖璉繲い┺$n搐もいō祇待待羘\n");
                set("unwield_msg", "$N盢もい$nΜ秈璉繲い\n");
        }
        init_blade(30, SECONDARY);  

        setup();
}
