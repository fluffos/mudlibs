#include <weapon.h>
#include <ansi.h>
inherit F_UNIQUE;
inherit SWORD;
 
void create()
{
        set_name(HIG"痚"HIM""HIC""HIM""HIR"辟"NOR, ({"torando sword","sword"}) );
        set_weight(1500);
        if(clonep() )
                set_default_object(__FILE__);
        else {
                set("unit", "р");
                set("long", "硂琌糃窾ネ皌糃琌讽绢苯ぱ礚寄も竟ョ妮ぇ\n");
                set("value",3500);
                set("material", "steel");
set("wield_msg", "$N耻羘$nǎ$n癹\伴搐もい\n");
                set("unwield_msg", "$N盢もい$nΜ秈璉糃繲\n");
        }
        init_sword(40, SECONDARY);
        setup();
}
