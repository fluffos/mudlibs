// Room: /d/school/secret.c

inherit ROOM;

void create()
{
	set("short", "盞揣いみ");
	set("long", @LONG

硂柑琌Guo盞膀柑Τ贺程秈蔼м猌竟

LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"workroom",
]));
	set("objects", ([ /* sizeof() == 1 */
	  __DIR__"obj/nuke" : 1,
	  __DIR__"obj/SM-2" : 1,
	  __DIR__"obj/TLAM" : 1,
	  __DIR__"obj/lasermak" : 1,

]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
