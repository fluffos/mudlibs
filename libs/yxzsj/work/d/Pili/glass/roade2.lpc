// Room: /d/pili/glass/roade2.c

inherit ROOM;

void create()
{
	set("short", "硄笵");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("outdoors", "1");
	set("exits", ([ /* sizeof() == 3 */
  "east" : __DIR__"roade3",
  "northup" : __DIR__"roadmt5",
  "west" : __DIR__"pavilion",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
