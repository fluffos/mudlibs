// Room: /d/pili/glass/entrance.c

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "痆件挂");
	set("long", @LONG
碞琌猌狶芠秤临痷︘┮⌒痆件挂泊辨
ǎ春睲竧纔懂︑礛ぱΘぃ穃砆嘿挂瞷
碞硂丁挂矪みいぃ稱璶ず把芠Τ
遏ホ窸(stone)眖硂临柑Τ畒爵瑈痷琌ぃ
眔よ
LONG
	);
	set("outdoors", "1");
	set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"bridge",
]));

	set("item_desc", ([ /* sizeof() == 1 */
  "stone" :
"
            ホ窸糶帝

		"HIC"痆

		"HIG"件

		"HIY"

		"HIM"挂"NOR"

",
]));

	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
