// Room: /d/pili/glass/workrm3.c

inherit ROOM;

void create() {
  set("short", "秘密工作室");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "workrm2",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
