// Room: /d/pili/glass/living.c

inherit ROOM;

void create()
{
	set("short", "﹡");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
  "out" : __DIR__"roads1",
  "south" : __DIR__"livingin",
  "up" : __DIR__"living2f",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
