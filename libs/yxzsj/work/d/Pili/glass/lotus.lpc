// Room: /d/pili/glass/lotus.c

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", HIW"撤浆"NOR);
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("outdoors", "1");
	set("exits", ([ /* sizeof() == 1 */
  "southdown" : __DIR__"roadmt6",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
