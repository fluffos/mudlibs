// Room: /d/pili/glass/roads1.c

inherit ROOM;

void create()
{
	set("short", "硄笵");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("outdoors", "1");
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"road",
  "enter" : __DIR__"living",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
