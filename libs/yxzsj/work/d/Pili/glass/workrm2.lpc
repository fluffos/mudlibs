// Room: /d/pili/glass/workrm2.c

inherit ROOM;

void create() {
  set("short", "秘密工作室");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "workroom",
    "east": __DIR__ "workrm3",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
