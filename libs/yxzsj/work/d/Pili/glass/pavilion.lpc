// Room: /d/pili/glass/pavilion.c

inherit ROOM;

void create()
{
	set("short", "[1;33m睤獸[2;37;0m");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/oneline" : 1,
]));
	set("exits", ([ /* sizeof() == 3 */
  "south" : __DIR__"garden",
  "east" : __DIR__"roade2",
  "west" : __DIR__"roade1",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
