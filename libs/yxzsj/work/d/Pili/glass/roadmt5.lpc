// Room: /d/pili/glass/roadmt5.c

inherit ROOM;

void create()
{
	set("short", "隔");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("outdoors", "1");
	set("exits", ([ /* sizeof() == 2 */
  "southdown" : __DIR__"roade2",
  "north" : __DIR__"roadmt6",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
