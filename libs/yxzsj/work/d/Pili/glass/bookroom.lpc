// Room: /d/pili/glass/bookroom.c

inherit ROOM;

void create()
{
	set("short", "┬");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"house",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
