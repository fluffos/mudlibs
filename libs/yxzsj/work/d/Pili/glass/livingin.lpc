// Room: /d/pili/glass/livingin.c

inherit ROOM;

void create() {
  set("short", "內室");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "living",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
