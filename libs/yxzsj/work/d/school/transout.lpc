// Room: /d/school/transout.c

inherit ROOM;

void create() {
  set("short", "物質傳送站");
  set("long", @LONG
這裡是逸俠中學的物質傳送站，你一走到平臺上就看到了一個奇異的建
築物。長的像是一個反應器加上一個衛星天線，事實上也差不多。物體可以
從這裡進去經過掃描後分解成原子，以高速粒子束射向特殊衛星後，可以立
刻傳送到任何有接收站的地方，再由傳送的分子結構資料重組。你一到這就
可以感覺到空氣中有一股不平凡的靜電氣息。
LONG
  );
  set("outdoors", "1");
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "lab4fn",
    "enter": __DIR__ "trans",
  ]));
  set("no_clean_up", 0);

  setup();
}
