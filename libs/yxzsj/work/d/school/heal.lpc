// Room: /d/school/heal.c

inherit ROOM;

void create() {
  set("short", "保健室");
  set("long", @LONG
這裡是學校的保健室，上體育課或平常要是有人受傷，就會到這裡來找
保健老師醫治。有時要量身高、體重，或是體檢，也會到這裡來檢查。和一
般的保健室一樣，這裡也有許多藥瓶、醫療用品，還有幾張床。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "pground3",
  ]));
  set("no_clean_up", 0);
  set("no_fight", 1);
  set("no_magic", 1);
  set("objects", ([
    "/d/school/npc/healtech": 1
  ]));

  setup();
}
