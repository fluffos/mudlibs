// Room: /d/school/guardin.c

inherit SAVEQ_ROOM;

int do_save(object me, object obj);
int do_get(int item_num);
private int pay_money(object me);

void create() {
  set("short", "守衛室");
  set("long", @LONG
這裡是守衛室的裡面，可以看到有桌椅等日常用品。送來的東西都會
在這裡存放，你也可以把東西暫時寄放在這裡。想要知道如何存放，可以
問(ask)守衛伯伯有關"寄放"的事。
LONG
  );
  set("no_clean_up", 0);
  set("no_fight", 1);
  set("no_spells", 1);
  set("no_magic", 1);
  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "guard",
  ]));
  set("objects", ([
    "/d/school/npc/guard": 1,
  ]));

  setup();
}
