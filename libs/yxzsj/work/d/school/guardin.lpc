// Room: /d/school/guardin.c

inherit SAVEQ_ROOM;

int do_save(object me, object obj);
int do_get(int item_num);
private int pay_money(object me);

void create()
{
	set("short", "矫");
	set("long", @LONG
硂柑琌矫柑Τ慈单ら盽ノ珇癳ㄓ狥﹁常穦
硂柑р狥﹁既盚硂柑稱璶笵︙
拜(ask)矫Τ闽"盚"ㄆ
LONG
	);
	set("no_clean_up", 0);
	set("no_fight", 1);
	set("no_spells", 1);
	set("no_magic", 1);
	set("exits", ([ /* sizeof() == 1 */
  "out" : __DIR__"guard",
]));
	set("objects", ([
		"/d/school/npc/guard": 1,
	]) );

	setup();
}