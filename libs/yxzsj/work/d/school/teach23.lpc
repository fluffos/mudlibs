// Room: /d/school/teach23.c

inherit ROOM;

void create()
{
	set("short", "毙厩加加");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 3 */
  "east" : __DIR__"teach33",
  "north" : __DIR__"teach13",
  "down" : __DIR__"teach22",
]));

	setup();
	replace_program(ROOM);
}
