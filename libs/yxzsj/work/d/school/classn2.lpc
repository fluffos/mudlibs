// Room: /d/school/classn2.c

inherit ROOM;

void create() {
  set("short", "教室");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "nbuild3f",
  ]));

  setup();
  replace_program(ROOM);
}
