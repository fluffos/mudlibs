// Room: /d/school/testroom.c

#include <ansi.h>

inherit ROOM;

void create() {
  set("short", HIR "考場" NOR);
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/techchk": 1,
  ]));
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "teach33",
  ]));

  setup();
}
