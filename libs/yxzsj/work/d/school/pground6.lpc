// Room: /d/school/pground6.c

inherit ROOM;

void create() {
  set("short", "操場");
  set("long", @LONG
這裡是逸俠中學的操場，地面完全用PU舖設，整齊順眼。操場上有一圈
200公尺的跑道，及一些籃球架和排球架。在圍牆邊有種樹，還有一些雙槓。
這裡的北邊是實驗大樓，再過去便是福利社。
LONG
  );
  set("outdoors", "1");
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 3 */
    "east": __DIR__ "pground5",
    "north": __DIR__ "lab",
    "south": __DIR__ "pground1",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/playstu": 4,
    __DIR__ "obj/baktball": 1,
  ]));

  setup();
  replace_program(ROOM);
}
