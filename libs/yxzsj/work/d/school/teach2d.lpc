// Room: /d/school/teach2d.c

inherit ROOM;

void create()
{
	set("short", "瓜繻");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 3 */
  "enter" : __DIR__"office8",
  "up" : __DIR__"teach2",
  "east" : __DIR__"teach3d",
]));

	setup();
	replace_program(ROOM);
}
