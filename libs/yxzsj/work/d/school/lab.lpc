// Room: /d/school/lab.c

inherit ROOM;

void create() {
  set("short", "實驗大樓");
  set("long", @LONG
這棟大樓是逸俠中學的實驗大樓，有各種的實驗室，像物理、化學、生
物等，還有電子科的實習室。學校各科的教室和實驗室都在這裡，這裡再往
北是福利社，可以看到許多的學生。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "nbuild",
    "north": __DIR__ "store",
    "up": __DIR__ "lab2f",
    "south": __DIR__ "pground6",
  ]));
  set("outdoors", "1");
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "obj/trashcan": 1,
  ]));
  set("no_clean_up", 0);

  setup();
}
