// Room: /d/school/office5.c

inherit ROOM;

void create()
{
	set("short", "快そ");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"office6",
  "west" : __DIR__"office4",
]));

	setup();
	replace_program(ROOM);
}
