// Room: /d/school/resttech.c

inherit ROOM;

void create() {
  set("short", "廁所");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "teach1",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
