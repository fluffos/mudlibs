// Room: /d/school/nbuild5f.c

inherit ROOM;

void create()
{
	set("short", "加き加");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "down" : __DIR__"nbuild4f",
  "enter" : __DIR__"music",
]));

	setup();
	replace_program(ROOM);
}
