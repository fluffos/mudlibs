// Room: /d/school/teach3.c

inherit ROOM;

void create() {
  set("short", "教學大樓");
  set("long", @LONG
這裡是逸俠中學的教學大樓，絕大部分的教室都在這裡。你所在的位置
是教學大樓一樓南邊走廊的東邊，可以看到許多的學生和老師在走動。此處
交通四通八達，東邊是學校的東大樓，往西邊是辦公室外的走廊，往南是一
條小路。從這裡可以進到辦公室，下樓梯是圖書館。這一排走廊就在老師辦
公室旁，可以從窗戶看到老師們，常可以看到老師進進出出。附近有幾台飲
水機，你可以在這裡將容器裝滿水(fill 容器)。
LONG
  );
  set("resource", ([ /* sizeof() == 1 */
    "water": 1,
  ]));
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 5 */
    "east": __DIR__ "ebuild2",
    "enter": __DIR__ "office7",
    "down": __DIR__ "teach3d",
    "south": __DIR__ "road4",
    "west": __DIR__ "teach2",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/clearstu": 1,
  ]));

  setup();
  replace_program(ROOM);
}
