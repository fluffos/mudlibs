// Room: /d/school/miliroom.c

#include <room.h>

inherit ROOM;

void create()
{
	set("short", "瓁癡毙");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "enter" : __DIR__"weapons",
  "up" : __DIR__"ebuild2",
]));
	set("no_clean_up", 0);

	setup();
              create_door("enter", "臟", "out", DOOR_LOCKED+DOOR_CLOSED, 1);
	replace_program(ROOM);
}
