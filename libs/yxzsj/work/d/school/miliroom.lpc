// Room: /d/school/miliroom.c

#include <room.h>

inherit ROOM;

void create() {
  set("short", "軍訓教室");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "enter": __DIR__ "weapons",
    "up": __DIR__ "ebuild2",
  ]));
  set("no_clean_up", 0);

  setup();
  create_door("enter", "鐵門", "out", DOOR_LOCKED + DOOR_CLOSED, 1);
  replace_program(ROOM);
}
