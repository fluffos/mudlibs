// Room: /d/school/pground5.c

inherit ROOM;

void create() {
  set("short", "操場");
  set("long", @LONG
這裡是逸俠中學的操場，地面完全用PU舖設，整齊順眼。操場上有一圈
200公尺的跑道，及一些籃球架和排球架。在圍牆邊有種樹，還有一些雙槓。
這裡的北邊是北大樓，往東邊是教學大樓。
LONG
  );
  set("outdoors", "1");
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "pground6",
    "east": __DIR__ "pground4",
    "north": __DIR__ "nbuild",
    "south": __DIR__ "pground2",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "obj/trashcan": 1,
  ]));
  set("no_clean_up", 0);

  setup();
}
