// Room: /d/school/office1.c

inherit ROOM;

void create() {
  set("short", "辦公室");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "office2",
    "out": __DIR__ "teach1",
  ]));

  setup();
  replace_program(ROOM);
}
