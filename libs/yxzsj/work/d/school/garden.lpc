// Room: /d/school/garden.c

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", HIG "い畑堕" NOR);
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("outdoors", "1");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "southeast" : __DIR__"ebuild2",
  "northeast" : __DIR__"ebuild1",
]));

	setup();
	replace_program(ROOM);
}
