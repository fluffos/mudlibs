// Room: /d/school/garden.c

#include <ansi.h>

inherit ROOM;

void create() {
  set("short", HIG "中庭花園" NOR);
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("outdoors", "1");
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "southeast": __DIR__ "ebuild2",
    "northeast": __DIR__ "ebuild1",
  ]));

  setup();
}
