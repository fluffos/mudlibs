// Room: /d/school/sbuild.c

inherit ROOM;

void create() {
  set("short", "電子科大樓");
  set("long", @LONG
這裡是逸俠中學的電子科大樓，獨自立在校園的南方。電子科的學生和
斑級都在這裡，一般的學生很少來這裡。從這裡進去是電子科的辦公室，上
體育課時也可看到學生來這裡借球。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "up": __DIR__ "sbuild2f",
    "enter": __DIR__ "ecoffice",
    "north": __DIR__ "pground2",
  ]));
  set("outdoors", "1");
  set("no_clean_up", 0);
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/estudent": 1,
    __DIR__ "npc/gestuden": 1,
  ]));

  setup();
  replace_program(ROOM);
}
