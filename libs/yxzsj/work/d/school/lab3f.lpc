// Room: /d/school/lab3f.c

inherit ROOM;

void create() {
  set("short", "實驗大樓三樓");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "down": __DIR__ "lab2f",
    "up": __DIR__ "lab4f",
  ]));

  setup();
}
