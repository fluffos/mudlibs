// Room: /d/school/guard.c

inherit ROOM;

void create()
{
	set("short", "矫");
	set("long", @LONG
硂柑琌秇獿い厩矫碞タ娩キ盽矫穦硂柑
狦厩Τ狥﹁а癘盿τ璶產癳ㄓ穦盚硂柑Τ痙ē
狾厩盽硂柑ㄓ痙ē临Τщ布ノ狥﹁
LONG
	);
	set("outdoors", "1");
	set("objects", ([ /* sizeof() == 1 */
  "/obj/ballot" : 1,
]));
	set("exits", ([ /* sizeof() == 2 */
  "enter" : __DIR__"guardin",
  "south" : __DIR__"gate",
]));
	set("no_clean_up", 0);

	setup();
	call_other( "/obj/board/common_b", "???" );
}
