// Room: /d/school/guard.c

inherit ROOM;

void create() {
  set("short", "守衛室");
  set("long", @LONG
這裡是逸俠中學的守衛室，就在正門旁邊，平常守衛伯伯會在這裡。如
果同學有東西忘記帶，而要家人送來，也會先寄放在這裡。門口有一個留言
板，同學們常到這裡來留言，還有一個投票用的東西。
LONG
  );
  set("outdoors", "1");
  set("objects", ([ /* sizeof() == 1 */
    "/obj/ballot": 1,
  ]));
  set("exits", ([ /* sizeof() == 2 */
    "enter": __DIR__ "guardin",
    "south": __DIR__ "gate",
  ]));
  set("no_clean_up", 0);

  setup();
  call_other("/obj/board/common_b", "???");
}
