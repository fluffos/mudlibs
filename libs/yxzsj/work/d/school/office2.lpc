// Room: /d/school/office2.c

inherit ROOM;

void create() {
  set("short", "辦公室");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "office1",
    "south": __DIR__ "office3",
  ]));

  setup();
  replace_program(ROOM);
}
