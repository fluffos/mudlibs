// Room: /d/school/ebuild2.c

inherit ROOM;

void create() {
  set("short", "東大樓");
  set("long", @LONG
這裡是逸俠中學的東大樓，在學校裡算是較古老的大樓，但是仍很堅固
，大樓的每一層都有通道和教學大樓連在一起。這裡是東大樓的南邊，往南
是風雨操場，往西是教學大樓，往北也是東大樓。從這裡下去是軍訓教室，
進去是電腦室，西北邊還可以看到中庭花園。附近有幾台飲水機，你可以在
這裡將容器裝滿水(fill 容器)。
LONG
  );
  set("exits", ([ /* sizeof() == 6 */
    "enter": __DIR__ "comproom",
    "northwest": __DIR__ "garden",
    "west": __DIR__ "teach3",
    "south": __DIR__ "smground",
    "north": __DIR__ "ebuild1",
    "down": __DIR__ "miliroom",
  ]));
  set("resource", ([ /* sizeof() == 1 */
    "water": 1,
  ]));
  set("outdoors", "1");
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
