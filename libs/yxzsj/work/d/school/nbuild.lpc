// Room: /d/school/nbuild.c

inherit ROOM;

void create() {
  set("short", "北大樓");
  set("long", @LONG
這裡是逸俠中學的北大樓，是學校中較古老的大樓。北大樓和實驗大樓
在學校的北方，雖離教學大樓有一段距離，但卻不會人煙稀少。因為有福利
社及各科教室，使這一區不但不會冷清，每天還可以看見操場上人來人往，
好不熱鬧。從這裡進去是工藝教室，往北是補校教室。
LONG
  );
  set("exits", ([ /* sizeof() == 5 */
    "up": __DIR__ "nbuild2f",
    "enter": __DIR__ "workroom",
    "west": __DIR__ "lab",
    "south": __DIR__ "pground5",
    "north": __DIR__ "classni",
  ]));
  set("outdoors", "1");
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
