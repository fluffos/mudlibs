// Room: /d/school/ecoffice.c

inherit ROOM;

void create() {
  set("short", "電子科辦公室");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "sbuild",
  ]));

  setup();
}
