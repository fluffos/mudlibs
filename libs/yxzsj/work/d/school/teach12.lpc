// Room: /d/school/teach12.c

inherit ROOM;

void create()
{
	set("short", "毙厩加加");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/clearstu" : 1,
]));
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 3 */
  "up" : __DIR__"teach13",
  "down" : __DIR__"teach1",
  "south" : __DIR__"teach22",
]));

	setup();
	replace_program(ROOM);
}
