// Room: /d/school/lab2f.c

inherit ROOM;

void create() {
  set("short", "實驗大樓二樓");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "down": __DIR__ "lab",
    "up": __DIR__ "lab3f",
  ]));

  setup();
}
