// Room: /d/school/teach1d.c

inherit ROOM;

void create()
{
	set("short", "穦某");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "up" : __DIR__"teach1",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
