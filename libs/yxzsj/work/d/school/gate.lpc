// Room: /d/school/gate.c

inherit ROOM;

void create()
{
	set("short", "");
	set("long", @LONG
硂柑琌秇獿い厩筿笆臟のΤ盺
毙﹛の厩ネ瞷琌厩丁Τ砛厩ネǐ秈眖
动岸毙厩加の巨初Τ遏堵︹礟(sign)糶Τ
眖硂柑ǐ獽琌秇獿い厩よΤ痙ē狾
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
  "up" : "/d/wiz/entrance",
  "east" : __DIR__"pground1",
  "north" : __DIR__"guard",
]));
	set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/guardstu" : 2,
  __DIR__"npc/armyinst" : 1,
]));
	set("item_desc", ([ /* sizeof() == 1 */
  "sign" : "	礟糶帝

	    秇
	    獿
	    ╬
	    ミ
	    蔼
	    
	    い
	    厩
	    
	  筿瓣干
	  い
	  场

",
]));
	set("outdoors", 1);
	set("no_clean_up", 0);
	set("valid_startroom", 1);

	setup();
	replace_program(ROOM);
}
