// Room: /d/school/bikeplac.c

inherit ROOM;

void create()
{
	set("short", "ó醋");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"obj/bicycle" : 3,
]));
	set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"smground",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
