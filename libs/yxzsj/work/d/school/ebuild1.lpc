// Room: /d/school/ebuild1.c

inherit ROOM;

void create() {
  set("short", "東大樓");
  set("long", @LONG
這裡是逸俠中學的東大樓，在學校裡算是較古老的大樓，但是仍很堅固
，大樓的每一層都有通道和教學大樓連在一起。這裡是東大樓的北邊，往北
是一條小路，往西是教學大樓，往南也是東大樓，西南邊還可以看到中庭花
園。
LONG
  );
  set("exits", ([ /* sizeof() == 5 */
    "north": __DIR__ "road2",
    "southwest": __DIR__ "garden",
    "south": __DIR__ "ebuild2",
    "west": __DIR__ "teach4",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "1");

  setup();
  replace_program(ROOM);
}
