// Room: /d/school/teach1.c

inherit ROOM;

void create() {
  set("short", "教學大樓");
  set("long", @LONG
這裡是逸俠中學的教學大樓，絕大部分的教室都在這裡。你所在的位置
是教學大樓一樓北邊走廊的西邊，可以看到許多的學生和老師在走動。此處
交通四通八達，往東是走廊，西邊是學校的操場，往南也是教學大樓。從這
裡可以進到辦公室，下樓梯是會議室，還有一個廁所。附近有幾台飲水機，
你可以在這裡將容器裝滿水(fill 容器)。
LONG
  );
  set("exits", ([ /* sizeof() == 7 */
    "east": __DIR__ "teach4",
    "enter": __DIR__ "office1",
    "down": __DIR__ "teach1d",
    "west": __DIR__ "pground4",
    "south": __DIR__ "teach2",
    "restroom": __DIR__ "resttech",
    "up": __DIR__ "teach12",
  ]));
  set("no_clean_up", 0);
  set("resource", ([ /* sizeof() == 1 */
    "water": 1,
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/jstudent": 1,
    __DIR__ "npc/gjstuden": 1,
  ]));

  setup();
  replace_program(ROOM);
}
