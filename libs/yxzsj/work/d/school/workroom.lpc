// Room: /d/school/workroom.c

inherit ROOM;

void create() {
  set("short", "工藝教室");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "nbuild",
  ]));

  setup();
  replace_program(ROOM);
}
