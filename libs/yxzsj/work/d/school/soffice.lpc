// Room: /d/school/soffice.c

inherit ROOM;

void create()
{
	set("short", "ね穦快そ");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "out" : __DIR__"pground1",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
