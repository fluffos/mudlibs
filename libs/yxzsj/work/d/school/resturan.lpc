// Room: /d/school/resturan.c

inherit ROOM;

void create()
{
	set("short", "紁┬");
	set("long", @LONG
硂琌丁ぐ或⊿Τ┬丁
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 3 */
  "down" : __DIR__"pkroom",
  "out" : __DIR__"road2",
  "north" : __DIR__"cook",
]));

	setup();
	replace_program(ROOM);
}
