// Room: /d/school/resturan.c

inherit ROOM;

void create() {
  set("short", "廚房");
  set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 3 */
    "down": __DIR__ "pkroom",
    "out": __DIR__ "road2",
    "north": __DIR__ "cook",
  ]));

  setup();
  replace_program(ROOM);
}
