// Room: /d/school/sidedoor.c

inherit ROOM;

void create()
{
	set("short", "凹");
	set("long", @LONG
硂柑琌秇獿い厩凹臟Τ盺毙﹛の厩ネ
瞷琌厩丁Τ砛厩ネ眖眖矪秈獽琌獴巨初の毙厩
加┕狥︰В初のó醋
LONG
	);
	set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/navyinst" : 1,
  __DIR__"npc/guardstu" : 2,
]));
	set("outdoors", "1");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 3 */
  "east" : __DIR__"trash",
  "north" : __DIR__"road4",
  "enter" : __DIR__"guardb",
]));

	setup();
	replace_program(ROOM);
}
