// Room: /d/school/sidedoor.c

inherit ROOM;

void create() {
  set("short", "側門");
  set("long", @LONG
這裡是逸俠中學的側門，可以看到一扇鐵門，門口有站崗的教官及學生。
現在是上學時間，有許多學生從此門出入。從此處進入便是風雨操場及教學
大樓，往東可以看到垃圾場及車棚。
LONG
  );
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/navyinst": 1,
    __DIR__ "npc/guardstu": 2,
  ]));
  set("outdoors", "1");
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 3 */
    "east": __DIR__ "trash",
    "north": __DIR__ "road4",
    "enter": __DIR__ "guardb",
  ]));

  setup();
}
