// Room: /d/school/pground3.c

inherit ROOM;

void create() {
  set("short", "操場");
  set("long", @LONG
這裡是逸俠中學的操場，地面完全用PU舖設，整齊順眼。操場上有一圈
200公尺的跑道，及一些籃球架和排球架。在圍牆邊有種樹，還有一些雙槓。
這裡的南邊是一條小路，東邊是教學大樓，從這裡可以進到學校的保健室。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "teach2",
    "enter": __DIR__ "heal",
    "west": __DIR__ "pground2",
    "north": __DIR__ "pground4",
    "south": __DIR__ "road3",
  ]));
  set("outdoors", "1");
  set("no_clean_up", 0);
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/sstudent": 1,
    __DIR__ "npc/gsstuden": 1,
  ]));

  setup();
}
