// Room: /d/school/teach4.c

inherit ROOM;

void create() {
  set("short", "教學大樓");
  set("long", @LONG
這裡是逸俠中學的教學大樓，絕大部分的教室都在這裡。你所在的位置
是教學大樓一樓北邊走廊的西邊，可以看到許多的學生和老師在走動。這裡
的的東邊是學校的東大樓，往西是北邊的走廊，往北是一條小路，還可以看
到在南方有一個中庭花園，下樓梯是外語中心。這一排的教室都是要外考的
國三生的，他們正面臨著聯考的壓力，努力的在為即將來臨的挑戰做準備。
你可以看到有一個白色的牌子(sign)，立在走廊的旁邊。
LONG
  );
  set("item_desc", ([
    "sign": @long 
	牌子上寫著:

	上課時間
	請勿通行
	
long
  ]));
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "ebuild1",
    "west": __DIR__ "teach1",
    "down": __DIR__ "teach4d",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
