#include <obj.h>

inherit ROOM;

void create()
{
	set("short", "丢紂");
	set("long", @LONG
硂琌丁だρ侣丢紂玡ㄑ帝碙羪
丢紂瘤ρ侣琌㏄砆氯Θ疩堵肅︹陪ボ硂柑ゲ﹚
讽獺畕穛ヵ
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"south" : "/d/snow/eroad1",
		"west" : "/d/snow/square",
	]));
	set("no_fight", "1");
	set("objects", ([ /* sizeof() == 2 */
		OBJ_PAPER_SEAL : 2,
		__DIR__"npc/keeper" : 1,
	]));

	setup();
	replace_program(ROOM);
}
