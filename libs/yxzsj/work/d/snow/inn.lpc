// inn.c
#include <room.h>
inherit ROOM;

void create()
{
set("short","竡刺");
	set("long", @LONG
瞷タ撤獸马玭娩產刺柑硂產刺瘤玱琌よ蛾
きκńず程璽脖產刺沮弧琌︗冻笴狦
褐絫瞏玴杠砛硂柑笿埃ぇㄓ︑
常尺舧籈栋硂柑ユ传硚ǎ籇硂柑ゴ钮砛Τ届
綼よΤ遏疩れ繨Θ┷礟(sign)
LONG
	);
	set("valid_startroom", 1);

	set("item_desc", ([
		"sign":"┷礟糶帝都刺娩逼缠白肈\n",
]));
	set("exits", ([
		"east"    	: "/d/snow/square",
		"up"		: "/d/snow/inn_2f",
		"west"	: "/d/wiz/entrance",
        ]) );

	set("objects", ([
        ]) );

	setup();
	call_other( "/obj/board/common_b", "???" );
}
