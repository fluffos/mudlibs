// gate.c

#include <ansi.h>

inherit ROOM;

void create() {
  set("short", HIW "墓地" NOR);
  set("long",
    "生人勿近。\n");
  set("exits", ([
  ]));
  set("objects", ([
  ]));
  setup();
  replace_program(ROOM);
}
