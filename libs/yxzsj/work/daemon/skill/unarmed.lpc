// unarmed.c

#include <ansi.h>

inherit SKILL;

void skill_improved(object me)
{
	int s;

	s = me->query_skill("force", 1);
	if( s%10==9 && (int)me->query("str") < s/4) {
		tell_object(me, HIW "パ对絤猌美籑矗蔼\n" NOR);
		me->add("str", 2);
	}
}
