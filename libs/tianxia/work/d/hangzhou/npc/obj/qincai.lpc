// qincai.c

inherit ITEM;

void create() {
  set_name("芹菜", ({ "celery" }));
  set_weight(100);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "这是一捆鲜亮的芹菜。\n");
    set("unit", "捆");
    set("value", 50);
  }
}
