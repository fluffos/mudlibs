inherit ITEM;
#include <ansi.h>
void create() {
  set_name(HIR "暗杀令" NOR, ({ "kill plate", "plate" }));
  set_weight(100);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "块");
    set("long", "这是一块鲜红的令牌。\n");
  }
}
