inherit ITEM;
inherit F_FOOD;

void create() {
  set_name("鸡腿", ({ "ji tui" }));
  set_weight(1200);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一块香喷喷鸡大腿。\n");
    set("unit", "块");
    set("food_remaining", 4);
    set("food_supply", 25);
  }
}
