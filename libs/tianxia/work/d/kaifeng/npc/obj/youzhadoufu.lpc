
inherit F_FOOD;
inherit ITEM;

void create()
{
        set_name("油炸豆腐", ({ "youzha doufu" , "doufu"}) );
        set_weight(10);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("long", "一盘刚出锅的油炸豆腐。\n");
                set("unit", "盘");
                set("value", 100);
                set("food_remaining", 4);
                set("food_supply", 10);
                set("material", "food");
        }
        setup();
}

