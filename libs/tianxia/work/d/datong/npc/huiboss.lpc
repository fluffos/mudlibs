// huiboss.c

inherit DRUGGER;

void create() {
  set_name("依不死", ({ "yi busi" }));

  set("title", "回春堂掌柜");

  set("long", @TEXT
他就是回春堂掌柜。
TEXT
  );
  set("gender", "男性");
  set("age", 65);
  set("con", 20);
  set("str", 20);
  set("combat_exp", 200);

  // 药铺昼夜营业。
  set("sell_all_day", 1);

  set("vendor_goods", ([
    DRUG_DIR "medicine_ganmao": -1,
    DRUG_DIR "huandan": 30,
    DRUG_DIR "s_hd": 20,
  ]));

  setup();
  carry_object(ARMOR_DIR "cloth")->wear();
}
