// picture_1.c

#include <ansi.h>
inherit ITEM;

void create()
{
	set_name(YEL"寒梅映雪图"NOR, ({ "picture", "tu"  }) );
	set_weight(50);
	if( clonep() )
		set_default_object(__FILE__);
	else
	{
		set("unit", "幅");
		set("value", 50000);
		set("long","一幅寒梅图（长描述）\n");
	}
	setup();
}
