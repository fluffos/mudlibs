// neck.c

#include <armor.h>

inherit NECK;

void create()
{
	set_name("牛皮颈环", ({ "neck armor", "neck" }) );
	set_weight(300);
	if( clonep() )
		set_default_object(__FILE__);
	else
	{
		set("unit","副");
		set("material", "iron");
		set("value", 2500);
		set("armor_prop/armor", 30);
	}
	setup();
}
