//lz_guard.c 林芝村东口岗哨
inherit ROOM;

void create() {
  set("short", "村东岗哨");
  set("long", @LONG
这里是林芝村东口的岗哨。屋子里面收拾的很干净，旁边的
兵器架上放满了各种各样的武器，正在休息的卫士有的在擦拭兵
器，有的在睡觉。原来原始森林中有一只猛兽经常出来伤人，为
了保卫村民们的安全才设置了这个岗哨。
LONG
  );

  set("exits", ([
    "north": __DIR__ "lz_eroad4",
  ]));
  set("objects", ([
    __DIR__ "npc/guardian": 6,
  ]));
  setup();
}
