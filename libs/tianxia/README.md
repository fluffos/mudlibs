# 天下 Beta

## 游戏名称

本游戏自称 **《天下》Beta**，作者署名"发现号（Find@tx）"，与本项目中的
"泥潭"（nitan）系列师出同门（同一作者），但代码库本身是完全独立的一套。

## 简介

一款正处于早期开发阶段的中文武侠 MUD（自我介绍即写着"正处于发展时期，
会有经常的更改和增添"）。新角色会先进行天赋点数自由分配（六项属性各有
上下限，可手动加减或交由系统随机分配），然后被"师傅"安排到"松竹小院"这
座清幽的院落中开始故事，由 NPC 书童递上入住手册，带有一点角色扮演的仪式
感。

## 如何运行

```
cd libs/tianxia && ~/src/fluffos/build-debug/src/driver config.fluffos
```

游戏端口：**40044**。请务必在 `libs/tianxia` 目录下启动。**注意**：驱动
自带一个 30 秒的启动保护期——启动日志出现 `Accepting telnet connections
on 0.0.0.0:40044.` 后，远程（非本机）连接仍需再等待约 30 秒，过早连接
会看到"《天下》Beta正在启动过程中，请稍候再来。"并被断开，属于正常设
计，稍等片刻重连即可。**本机（127.0.0.1，含 WASM/浏览器内连线）已豁免
这个保护期**，启动完成后可立即连线（见下方「WASM 版本兼容性」）。

## 如何连接游玩

```
python3 scripts/mudclient.py 127.0.0.1 40044
```

或直接用 MUD 客户端 telnet 连接 `127.0.0.1 40044`。注册流程为：英文代号
→ 确认新建角色 → 中文姓名（**注意：不可以"唐"或"踏雪"开头**——游戏内有
一个"唐门"势力，这两个开头的姓名会被系统保留拒绝，换个姓氏即可）→ 密码
（两次）→ 性别 → 天赋点数分配（输入 0 交由系统随机产生）→ 确认天赋，全
部完成后即进入"松竹小院"这一起始场景。

## 已知问题

- lpcc 静态编译检查显示约 99.1% 的文件通过，未通过部分主要是巫师个人建
  设/测试目录下的孤立内容、旧式的邮件/网络中继守护进程（本环境未启用），
  以及两件武器/防具类型（"叉"和"面具"）缺少对应的基础类定义，涉及的物品
  和 NPC 极少，均不在注册与主线游玩路径上。
- 核心的注册、登录、探索（look）、查看状态（score）以及退出存档流程均已
  反复验证工作正常，且已确认游戏不会再把普通的程序编译"警告"误当作严重
  错误显示给玩家。技能查询指令（skills）也已确认工作正常。
- **WASM 版本兼容性**：在新的 WASM 构建下能正常启动（无严重的预加载错
  误）。本次 WASM 落地专项已将 30 秒开机保护期、IP 封禁表 (`ban_d`)、
  巫师登录地址白名单 (`ip_d`) 三处网关都改为对本机/回环地址 (127.0.0.1)
  无条件放行（详见 NOTES.md），此前"测试工具连线与保护期冲突"的限制已
  不复存在。

## 管理员账号 / Admin account

- **ID**: `fluffos`
- **初始密码**: `Mud@2026`（**注意特殊情况**：本 mudlib 对巫师账号有
  单独的密码强度规则——巫师身份的账号，密码必须至少 10 位且同时包含大
  写字母、小写字母和特殊符号 (`WIZ_PASSWD_CHK`，见 `logind.lpc`
  `check_wiz_legal_password()`)。以普通玩家密码规则注册的 `Mud@2026`
  （8 位）在被提升为 `(admin)` 之后的第一次登录会被系统强制要求改密，
  **实际登录密码已改为 `Mud@2026Admin`**（13 位，含大小写字母和符号，
  满足巫师密码规则）。
- **中文名**: 浮浮
- **权限**: `(admin)`（本 mudlib 巫师等级表的最高级别，登录时显示
  「目前权限：(admin)」，已验证 `update /cmds/usr/score.lpc` 可正常重
  编译文件）
- 权限授予方式：`/adm/etc/wizlist` 数据文件追加 `fluffos (admin)`
  （`securityd.lpc` 启动时读取该表，只在 `create()` 时载入一次，改表后
  需要重启驱动才会生效）。
- 小提示：`update` 命令不能用来更新它自己（`update
  /cmds/wiz/update.lpc` 会在执行中把自己 destruct 掉、无任何输出——这是
  原始代码的固有行为，与本项目另几个 lib 观察到的现象一致），更新其他
  文件一切正常。
- **警告**：这是本项目所有 mudlib 统一预置的本地游玩默认账号。若要对
  公网开放主机，请先修改此密码。
