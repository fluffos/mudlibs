// Room: /u/diabio/feitian/feitian/niurouguan.c
inherit ROOM;

void create()
{
  set ("short", "牛肉馆");
  set ("long", @LONG
你带着满肚子的疑问来到了曾经闻名一时的京都牛肉馆.但这里是一片
废墟,旁边的人告诉你,这里是因为老板娘牵扯到了绯村剑心的战斗
而被摧毁了.在废墟旁立着一个牌子(paizi).
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"beijie",
]));
  set("item_desc", ([ /* sizeof() == 1 */
  "paizi" : "正在施工建设中
",
]));
  setup();
  replace_program(ROOM);
}
