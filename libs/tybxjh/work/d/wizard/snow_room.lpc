// Room: /d/wizard/wizard_room.c

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIY "深海" NOR);
  set("long", HIW @LONG
寂寞的不只有我，你也是一个吗？如果你也是，记得来找我，
一个人是寂寞的，两个人，或许就是快乐的。让我们一起远
离寂寞。

           搞定我其实不难，只是你太懒…………

LONG
    NOR);


  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "wizard_room",
    "south": __DIR__ "newsroom",
    "down": "/d/city/chatroom2",
  ]));
  set("no_fight", "1");
  set("no_clean_up", 0);
  setup();
  call_other("/d/board/snow_b", "???");

  replace_program(ROOM);
}
