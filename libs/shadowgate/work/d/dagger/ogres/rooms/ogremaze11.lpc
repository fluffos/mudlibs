
#include <std.h>
#include "../ogres.h"

inherit STORAGE"3rd.lpc";

void create(){
   ::create();
   set_exits(([
       "southwest" : ROOMS"ogremaze8",
   ]));
}
void reset(){
   ::reset();
   if(!present("corpse"))
      new(OBJ"corpse.lpc")->move(TO);
}

