// lian gengyao

#include <ansi.h>
inherit NPC;
inherit F_MASTER;
string ask_me();

void greeting(object);
void init();
void create()
{
	set_name("Ң", ({"lian gengyao", "lian" }));
	set("nickname", HIC"޵д󽫾"NOR );
	set("gender", "" );
	set("age", 40);
	set("long", "λ߳ƵĴ󽫾ǰ,ÿ˶âڱ.\n");
	set("attitude", "friendly");
	set("shen_type", 1);

	set("str", 30);
	set("int", 25);
	set("con", 22);
	set("dex", 29);

	set("max_qi", 3000);
	set("max_jing", 1500);
	set("neili", 3000);
	set("max_neili", 3000);
	set("combat_exp", 500000);
	set("score", 30000);
	set("jiali", 50);
	set_skill("force", 140);
	set_skill("dodge", 120);
	set_skill("unarmed", 120);
	set_skill("parry", 120);
	set_skill("claw", 120);
	set_skill("blade",100);
	set_skill("xixing-dafa", 100);
	set_skill("sougu", 120);
	set_skill("tiyunzong", 120);
	set_skill("taiji-dao", 100);

	map_skill("force", "xixing-dafa");
	map_skill("dodge", "tiyunzong");

	map_skill("parry", "sougu");
	map_skill("blade", "taiji-dao");


	create_family(HIR""NOR,2,"Ա"+HIB"ƽ"NOR );

     set("inquiry", ([
		"ͥ" : "\n֮ͥ,ев,볯Ϊ(j
oin chaoting)",
	 ]) );

	set("chat_chance", 2);
	set("chat_msg", ({
	  }) );
	setup();
	carry_object(__DIR__"obj/aaa")->wield();
	carry_object(__DIR__"obj/jinduan")->wear();

}


void attempt_apprentice(object ob)
{
	if ((int)ob->query_skill("force", 1) > 4) {
	command("say ð, Ҿˡ\n");
	command("recruit "+ ob->query("id"));
		ob->set("class", "guanyuan");
		return;
	}
		command("say 㻹Ƕѧ㶫,ΪЧ.\n");
}

void init()
{
::init();
add_action("do_join", "join");
}


int do_join(string arg)
{
	object ob;
	ob = this_player();
	if( !arg || arg!="chaoting")
	return notify_fail("Ҫʲô֯\n");
	else
	{
		ob->create_party(HIR""NOR"Ʒ");
		ob->set("class", "officer");
		return notify_fail("ѾǳͥԱ\n");
		command("ҢЦ:,ֶͥһ˲\n");

	}
	return 0;

}

