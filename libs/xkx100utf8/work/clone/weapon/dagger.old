// gangjian.c

#include <weapon.h>
inherit DAGGER;

void create()
{
	set_name("ͨذ", ({ "dagger", "bishou" }));
	set_weight(6000);
	if (clonep())
		set_default_object(__FILE__);
	else {
		set("unit", "");
		set("long", "һͨСذס˱ȽϺʡ\n");
		set("value", 2000);
		set("material", "steel");
		set("wield_msg", "$NӶͳһ$nС\n");
		set("unwield_msg", "$Nе$nŻض\n");
	}
	init_dagger(25,0);
	setup();
}
