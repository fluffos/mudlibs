#include <ansi.h>
inherit ITEM;
void create() {
  set_name(HIC "朝廷密令" NOR, ({ "ctmling" }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "封");
    set("material", "paper");
    set("long", "一封朝廷密令\n");
  }

  setup();
}
