#include <ansi.h>
inherit ITEM;
void create() {
  set_name(BLU "赏罚令" NOR, ({ "sfl" }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "块");
    set("material", "stone");
    set("long", "上面写着:令。\n");
  }
  setup();
}
