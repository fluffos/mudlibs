inherit	ROOM;
void create()
{
	 set("short", "清风细雨楼的青龙堂");
	set("long", @LONG
这里是楼中要地，他管辖着楼的北六省生意。屋中华丽无比，古玩甚
多。好多人忙来忙去，但井然有序。堂主却是武林小辈的夜孤魂。但拳法独
步天下，罕有对手。
LONG
	);
	set("exits", ([	/* sizeof() == 4 */
    "north" : __DIR__"pinxiang",
    "east" : __DIR__"dating"
]));	    
	set("objects", ([
	__DIR__"npc/master1" : 1,
			]) );

	set("outdoors",	"fengyun");
	set("coor/x",45);

	set("coor/y",-20);
	set("coor/z",0);
	setup();
	replace_program(ROOM);
}