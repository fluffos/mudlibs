//sku@00-11-19 
inherit ROOM;
void create ()
{
  set ("short", "[45m快乐的休息室[0m");
  set ("long", @LONG
这是快乐的工作间,书桌上一台[42m奔腾760[0m在飞快的
运转着。靠窗放着一张小床，隐隐约约你听到风云城
里的欢声笑语。这都是快乐辛勤劳动的结果呀。 
LONG);

  set("coor", ([ /* sizeof() == 3 */
  "z" : 1000000,
  "y" : 0,
  "x" : -10,
]));
  set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"midao.lpc",
]));
  set("valid_startroom", 1);
  set("objects", ([ /* sizeof() == 1 */
  __DIR__"obj/book" : 1,
]));
  set("no_magic", 1);

  setup();
  replace_program(ROOM);
}
