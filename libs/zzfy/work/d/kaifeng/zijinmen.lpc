#include <ansi.h>
inherit ROOM;
void create() {
  set("short", YEL "紫禁城后门" NOR);
  set("long", @LONG
这里是皇宫大内的紫禁城后门，这里警备森严，禁止任何闲杂人
等进入，月圆之夜,紫禁之颠！的江湖对决，看来是无人可会了。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "out": __DIR__ "zijinmen",
    "south": __DIR__ "gmroad",
  ]));
  set("objects", ([
  ]));

  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
}
