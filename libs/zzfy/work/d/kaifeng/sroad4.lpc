#include <ansi.h>
inherit	ROOM;
void create()
{
	 set("short", "汴梁南大街");
	set("long", @LONG
这里是开封汴梁的南大街,大街两边都是买各种物品的小摊贩,人
来人往,热闹非凡,因为是京城,各式各样的人都有.从这里向东是
悦来客栈,向西是侠隐巷。
LONG
	);
	set("exits", ([	/* sizeof() == 4 */
     "north" : __DIR__"sroad3",
 "west" : __DIR__"xyroad",
    "south" : __DIR__"sgate"
]));	    
	set("objects", ([
			]) );

	set("outdoors",	"fengyun");
	set("coor/x",55);

	set("coor/y",-20);
	set("coor/z",0);
	setup();
	replace_program(ROOM);
}
