#include <ansi.h>
inherit ROOM;
void create() {
  set("short", "汴梁西大街");
  set("long", @LONG
这里是开封汴梁的西大街,大街两边都是买各种物品的小摊贩,人
来人往,热闹非凡,因为是京城,各式各样的人都有.从这里向北是
开封府大堂,向南走就是贡院。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "wroad3",
    "east": __DIR__ "wroad1"
  ]));
  set("objects", ([
  ]));

  set("outdoors", "fengyun");
  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
}
