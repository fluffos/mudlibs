#include <ansi.h>
inherit ROOM;
void create() {
  set("short", YEL "京都棋苑" NOR);
  set("long", @LONG
京都棋院乃是大宋的国手交流的场所，大宋虽然国风并不剽悍，但
是全国却风行象棋，而且多能征善战的国手，而且围棋水平也非同
寻常。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "yujie3",
    "east": __DIR__ "qishi1",
    "south": __DIR__ "qishi2",
    "north": __DIR__ "qishi3",
    //"up":__DIR__"qiyuan1",
  ]));
  set("objects", ([
  ]));

  set("outdoors", "fengyun");
  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
  replace_program(ROOM);
}
