#include <ansi.h>
inherit ROOM;
void create() {
  set("short", YEL "紫禁御道" NOR);
  set("long", @LONG
这里是紫禁城里官员上朝觐见的大道,这里南接午朝门,向北直
达龙亭脚下,御道的西面是是杨家湖,清澈透底,东面是潘家湖
湖水却是浑浊无比,虽经朝廷数次治理,也未曾改观。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "wuchao",
    "northup": __DIR__ "lttaijie",
  ]));
  set("objects", ([
  ]));

  set("outdoors", "fengyun");
  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
}
