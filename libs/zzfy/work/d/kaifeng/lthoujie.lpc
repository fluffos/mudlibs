#include <ansi.h>
inherit ROOM;
void create() {
  set("short", YEL "龙亭后街" NOR);
  set("long", @LONG
这里是皇帝上朝的必经之路，向南就是龙亭，向西是御花园，向
东是御书房，向北就是宫门内大街。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "southup": __DIR__ "longting",
    "north": __DIR__ "gmroad",
    "east": __DIR__ "yushufang",
    "west": __DIR__ "yuhuayuan",
  ]));
  set("objects", ([
  ]));

  set("outdoors", "fengyun");
  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
  replace_program(ROOM);
}
