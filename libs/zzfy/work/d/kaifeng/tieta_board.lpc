// tieta_board.c
// wdl by 2000/12/25

inherit BULLETIN_BOARD;

void create() {
  set_name("开封的留言板", ({ "board" }));
  set("location", __DIR__ "tieta");
  set("board_id", "common_fy");
  set("long", "这是一个供游客留言记事的留言板。\n");
  setup();
  set("capacity", 30);
  replace_program(BULLETIN_BOARD);
}
