#include <ansi.h>
inherit	ROOM;
void create()
{
set("short",HBBLU"铁塔广场"NOR);
 set("long",@LONG        
这里是开封汴梁的铁塔广场，雄伟的铁塔矗立在广场的正中，这里
正北是各个衙门的所在地御街，东南西却是繁华的闹市区，铁塔之
上贴的是京城的通告。
LONG
	);
	set("exits", ([	/* sizeof() == 4 */
"south":__DIR__"sroad1",
"east":__DIR__"eroad1",
"north":__DIR__"yujie3",
"west":__DIR__"wroad1",
]));	    
	set("objects", ([
			]) );

	set("outdoors",	"fengyun");
	set("coor/x",55);

	set("coor/y",-20);
	set("coor/z",0);
	setup();
// call_other( "/obj/board/tieta", "???" );
	replace_program(ROOM);
}
