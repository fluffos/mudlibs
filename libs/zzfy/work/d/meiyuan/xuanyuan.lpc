inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIC "轩辕厅" NOR);
  set("long", @LONG
这里是主人平时练功的地方。地方很宽敞，大理石
铺成的地面，上面又铺了一张地毯。房间两侧有两个兵
器架，十八般兵器一应俱全。前面桌子的刀架上放着两
柄刀，据说是梅园的男女主人当年共闯江湖时用过的。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "xifeng",
    "up": __DIR__ "tingyu"

  ]));

  set("coor/x", -640);
  set("coor/y", -490);

  set("coor/z", 30);
  setup();
  replace_program(ROOM);
}
