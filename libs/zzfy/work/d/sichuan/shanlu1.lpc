inherit ROOM;
void create()
{
        set("short", "山路");
        set("long", @LONG
出了沉香镇，先是一段平缓的上山路，地势是越来越高,入川的高山
就在眼前了。
LONG
        );

        set("exits", ([ /* sizeof() == 4 */
  	"east" : "/d/chenxiang/cxw",
  	"westup" : __DIR__"shudao1",
	]) );


   	set("outdoors","taoyuan");
        set("coor/x",-560);
	set("coor/y",-500);
	set("coor/z",0);
	set("coor/x",-560);
	set("coor/y",-500);
	set("coor/z",0);
	setup();
        replace_program(ROOM);

}
