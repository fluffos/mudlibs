// path1.c

inherit ROOM;

void create() {
  set("short", "蜀中小镇");
  set("long", @LONG
这里是四川盆地里的一个小镇，刚从蜀道过来的人们总是先到这里休息，从这里向南
就是以毒功威镇天下的四川唐门，向西就是美丽锦绣的峨眉山了。
LONG
  );

  set("exits", ([ /* sizeof() == 3 */
    "south": __DIR__ "lianou",
    "eastup": __DIR__ "shanlu2",
    "north": __DIR__ "fanguan",
  ]));

  set("no_clean_up", 0);
  set("outdoors", "sichuan");
  set("coor/x", -1500);
  set("coor/y", -1000);
  set("coor/z", -100);
  set("coor/x", -1500);
  set("coor/y", -1000);
  set("coor/z", -100);
  setup();
}
