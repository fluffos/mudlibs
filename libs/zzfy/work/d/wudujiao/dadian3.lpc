// TIE@FY3 ALL RIGHTS RESERVED
inherit ROOM;
void create() {
  set("short", "褐蜈分舵");
  set("long", @LONG
这里是褐蜈分舵，房间里布置的古朴而不是尊严，但你不难发现有一个特色，那就
是屋内都布置成了褐色：褐色的墙，褐色的门，褐色的窗...
正对面一张挂毯，上面绣着一只张牙舞爪的千足蜈蚣。

LONG
  );
  set("exits", ([
    "north": __DIR__ "hyt",
    "east": __DIR__ "dadian4",
  ]));
  set("objects", ([
    __DIR__ "npc/hewu": 1,
    __DIR__ "obj/guatan2": 1,
  ]));

  set("coor/x", -60);
  set("coor/y", -450);
  set("coor/z", 0);
  set("coor/x", -60);
  set("coor/y", -450);
  set("coor/z", 0);
  set("coor/x", -60);
  set("coor/y", -450);
  set("coor/z", 0);
  setup();
}
