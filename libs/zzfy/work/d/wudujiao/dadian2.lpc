// TIE@FY3 ALL RIGHTS RESERVED
inherit ROOM;
void create() {
  set("short", "赤蝎分舵");
  set("long", @LONG
这里是赤蝎分舵，房间里布置的古朴而不是尊严，但你不难发现有一个特色，那就
是屋内都布置成了红色：红色的墙，红色的门，红色的窗...
正对面一张挂毯，上面绣着一只张牙舞爪的硬甲蝎子。

LONG
  );
  set("exits", ([
    "south": __DIR__ "hyt",
    "east": __DIR__ "dadian1",
  ]));
  set("objects", ([
    __DIR__ "npc/chixie": 1,
    __DIR__ "obj/guatan1": 1,
  ]));

  set("coor/x", -60);
  set("coor/y", -410);
  set("coor/z", 0);
  set("coor/x", -60);
  set("coor/y", -410);
  set("coor/z", 0);
  set("coor/x", -60);
  set("coor/y", -410);
  set("coor/z", 0);
  setup();
}
