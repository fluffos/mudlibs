// Room: zhengtang.c
// atu 2000/01/10 
#include <ansi.h>
// void kf_same(object who,object me);

inherit ROOM;
void create() {
  set("short", "签押房");
  set("long", @LONG
这里是开封府的签押房，平常捕头衙役们都在这里等候包大人传招。北
面就是大堂了，许多人从这里进进出出，包大人正在升堂问案。
LONG
  );
  set("exits", ([
    "south": "/d/fy/hall",
    "north": __DIR__ "zhengtang",
  ]));
  set("coor/x", 50);
  set("coor/y", 10);
  set("coor/z", 0);

  set("objects", ([
    __DIR__ "npc/zhanzhao": 1,
  ]));
  set("coor/x", 50);
  set("coor/y", 10);
  set("coor/z", 0);
  setup();
  replace_program(ROOM);
}
