#include <ansi.h>
inherit ITEM;

void create()
{
	set_name(HIR"请柬"NOR,({"qingjian","jian"}));
	set_weight(3);
	set("unit","张");
    if( clonep() )
		set_default_object(__FILE__);
       else {set("long","");}
}
