// letter.c

inherit ITEM;

void create() {
  set_name("推荐信", ({ "letter" }));
  set_weight(10);
  if (!clonep()) {
    set("unit", "封");
    set("value", 0);
    set("long", "\n");
    set("pl_name", "");
  }
  setup();
}
