// Room: xiuxishi.c

inherit ROOM;

void create() {
  set("short", "小店");
  set("long", @LONG
这间小店的老板是唐门唐顺，所以游客们通常都不敢来惠顾。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "road",
  ]));
  set("objects", ([
    __DIR__ "npc/waiter": 1,
  ]));

  set("no_clean_up", 0);
  set("outdoors", "tangmen");
  set("coor/x", -1210);
  set("coor/y", -20);
  set("coor/z", -40);

  setup();
  replace_program(ROOM);
}
