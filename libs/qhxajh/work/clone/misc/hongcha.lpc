// hongcha.c

inherit ITEM;

void create()
{
	set_name("红茶", ({ "hong cha", "cha", "tea" }) );
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("long",	"这是普普通通的红茶。\n");
		set("unit", "包");
	set("value",200);
    	}
	setup();
}
