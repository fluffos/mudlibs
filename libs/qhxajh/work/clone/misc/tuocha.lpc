// tuocha.c

inherit ITEM;

void create() {
  set_name("沱茶", ({ "tuo cha", "cha", "tea" }));
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "这便是闻名西南的沱茶。\n");
    set("unit", "包");
    set("value", 100);
  }
  setup();
}
