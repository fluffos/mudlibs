// Filename : wendang.c
//
// ChangeLog: 
//	1997-02-22	Falcon	create

#include <dbase.h>

inherit F_CLEAN_UP;
inherit F_DBASE;
inherit F_SAVE;

void create() {
  seteuid(getuid());
}

void setup() {
  //    restore();
}

string query_save_file() {
  string fn;
  fn = sprintf("wendang_%d_%d", query("year"), query("month"));

  return DATA_DIR + "wendang/" + fn;
}
