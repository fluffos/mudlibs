// /d/city/nanyang/shanzhai.c
#include <ansi.h>
inherit ROOM;
void create() {
  set("short", "山寨入口");
  set("long", @LONG
这里既是伏牛山上强人聚居的巢穴了。巨木搭成的山寨门立在一个狭窄的山
口处，两边皆是山石，几名土匪正嘻嘻哈哈地聊天，不时骂骂咧咧的。由南面一
路下山就是南阳城。东面入了山寨，可就......
LONG);
  set("exits", ([
    "southwest": __DIR__ "qingsong",
    "east": __DIR__ "weihu",
  ]));
  set("outdoors", "nanyang");
  setup();
}
