xlqy_early: §10.7 round three batch 4 re-verification of combat/apprenticeship/economy — genuinely clean, no bug found

Re-tested the three angles flagged as "未覆盖" by the original 2026-08-06/07
pass, not realizing a 2026-08-24 follow-up had already closed them (fixed a
real familymaster.lpc apprenticeship bug then). This session independently
confirmed combat (real damage, real NPC death), player death+revival (forced
via admin smash, clean revival with full inventory/health), a second,
previously-untested apprenticeship system (武馆/Dongfang family job-hall,
separate code path from the FAMILYMASTER fix), and shop economy (value/pawn/
retrieve with correct multi-denomination currency change-making) all work
correctly. Also independently verified this archive's std/money.lpc is NOT
in the AGENTS.md §7.199 commented-out-query_autoload lineage.

Investigated but judged NOT a bug (documented as an observation): ordinary
equipment does not survive a normal quit, traced to std/equip.lpc's
query_autoload() being unconditionally 0 with no matching autoload()
receiver anywhere in the class hierarchy (structurally different from the
real §7.199 regression, which had an intact receiver). Corroborated as
deliberate design by a dedicated "always re-grant basic starting clothes on
login" safety net elsewhere in the codebase, and by the identical pattern
in two confirmed sibling libs.

Also documents a testing-methodology finding: this environment's `telnet`
client is not 8-bit clean and silently corrupts typed Chinese text (local
echo still displays it correctly, masking the corruption) — `nc` in the
same tmux harness works correctly. Cost significant time before the
raw-socket cross-check caught it; noted for future testers.

No code changes — NOTES.md documentation only.
