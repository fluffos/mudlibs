#include <dbase.h>
#include <move.h>
#include <name.h>

inherit ITEM;

void create()
{
    set("value", 0);
    set("no_sell", 1);
    set("no_get", 1);
    set("no_put", 1);
    set("no_give", 1);
    set("no_steal", 1);
    set("no_drop", 1);
}

