#include <ansi.h>

inherit F_CLEAN_UP;

void create() { seteuid(getuid()); }

mixed main(object me, string arg) {
  GIFT_D->show_gifts();
  return 1;
}

int help(object me) {
  write(@HELP
指令格式 : showgifts

显示所有任务系统的奖励。
HELP
  );
  return 1;
}
