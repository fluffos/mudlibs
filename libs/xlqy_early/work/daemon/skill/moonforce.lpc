inherit FORCE;

string exert_function_file(string func) {
  return __DIR__ "moonforce/" + func;
}

mapping query_heal_cfg() {
  return ([
    "desc": "$N坐下来运功疗伤，脸上泛出一丝红晕，不久，吐出一口瘀血，脸色看起来好多了。",
  ]);
}
