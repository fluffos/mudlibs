#include <condition.h>
#include <ansi.h>

int update_condition(object me, int duration) {
  object attacker;

  if (!me->is_fighting()
    || !living(me)
    || duration <= 1) {
    message_vision(HIG "\n$N感觉内力运行通畅无阻了！\n" NOR, me);
    me->delete_temp("biqi");
    return 0;
  }

  me->apply_hb_condition("biqi", duration - 1);

  return CND_CONTINUE;
}

void clear_condition(object me, int duration) {
  me->delete_temp("biqi");
}
