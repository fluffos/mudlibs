#include <condition.h>
#include <ansi.h>

int update_condition(object me, int duration) {
  object attacker;

  if (!me->is_fighting()
    || !living(me)
    || duration <= 1) {
    message_vision(HIG "\n$N感觉自己又能够调动天地元气了！\n" NOR, me);
    me->delete_temp("jinzhou");
    return 0;
  }

  me->apply_hb_condition("jinzhou", duration - 1);

  return CND_CONTINUE;
}

void clear_condition(object me, int duration) {
  me->delete_temp("jinzhou");
}
