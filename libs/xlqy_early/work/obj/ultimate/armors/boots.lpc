#include <armor.h>

inherit ULTIMATE_ARMOR;

void create()
{
    set_name("ѥ", ({"boots"}));
    set_weight(1000);

    if ( clonep() )
    {
        set_default_object(__FILE__);
    }
    else
    {
        set("material", "leather");
        set("unit", "˫");
        set("armor_prop/armor", 1);
        set("armor_type", TYPE_BOOTS);
    }

    ::create();

    setup();
}

