#include <room.h>
inherit ROOM;

void create()
{
        set("short", "客厅");
        set("long", @LONG

客厅正面的墙上挂的是关公关武帝的画像。下面摆着一个香案，香案上香
炉青烟袅绕。旁边是张八仙桌，桌子左右各放了张太师椅。平常萧总镖头
就在这里接待客人，商谈生意。客厅左边通向一条长廊，右边应该是通向后院。
LONG
        );

        set("exits", ([
        "south" : __DIR__"dayuan",
        ]));

        set("objects", ([
        ]));

        setup();
}
