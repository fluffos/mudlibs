// by bugbug & alading @ 缥缈水云间, 2003-4-5
inherit __DIR__ "guai.lpc";


void set_skills(int max_value, int level) {
  int guai_value;
  ::set_skills(max_value, level);

  if (level == 1) guai_value = max_value + max_value / 12;
  else if (level == 2) guai_value = max_value + max_value / 10;
  else guai_value = max_value + max_value / 8;
  create_family("大雪山", 3, "弟子");
  set_skill("cuixin-zhang", guai_value + random(16));
  set_skill("xiaoyaoyou", guai_value + random(16));
  set_skill("sword", guai_value + random(16));
  set_skill("throwing", guai_value + random(16));
  set_skill("bainiao-jian", guai_value + random(16));
  set_skill("ningxie-force", guai_value + random(16));
  set_skill("literate", 180);
  set_skill("dengxian-dafa", guai_value + 20 + random(16));
  map_skill("spells", "dengxian-dafa");
  map_skill("force", "ningxie-force");
  map_skill("unarmed", "cuixin-zhang");
  map_skill("parry", "bainiao-jian");
  map_skill("sword", "bainiao-jian");
  map_skill("dodge", "xiaoyaoyou");
  map_skill("throwing", "bainiao-jian");
  set("force_factor", query_skill("force") / 2);
  set("mana_factor", query_skill("spells") / 2);
  if (guai_value > 200) {
    set("skill_level/sword", "expert");
    set("skill_level/throwing", "expert");
  }
  if (guai_value > 300) {
    set("skill_level/sword", "master");
    set("skill_level/throwing", "master");
  }
  if (guai_value > 600) {
    set("skill_level/sword", "grandmaster");
    set("skill_level/throwing", "grandmaster");
  }
  carry_object("/d/obj/armor/jinjia.lpc")->wear();
  carry_object("/d/npc/obj/qin.lpc")->wield();
  set("chat_chance_combat", 75);
  set("chat_msg_combat", ({
    (: perform_action, "sword", "fengwu" :),
    (: perform_action, "sword", "chaofeng" :),
    (: exert_function, "ningxie" :),
  }));


}
