// 作者：tonii@飘渺水云间，2003-5-3于浙大，修改请保留此行。
// ROOM: /d/hsl/jiluo_path9

inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "幽暗小路");
  set("long", @LONG

这是条幽暗的小路，树影婆娑好像许多人影在晃动，透过树叶
照射到路面的阳光也显得虚弱无力，周围充满压抑的气氛。远
远望去可以看到北面如林的墓碑，那儿就是幽暗坟场。
LONG);

  set("exits",
    ([
      "south": __DIR__ "jiluo_path8",
      "north": __DIR__ "jiluo_fenchang",
    ]));


  set("outdoors", "hsl");
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);

}
