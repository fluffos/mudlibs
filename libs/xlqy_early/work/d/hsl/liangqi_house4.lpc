// 作者：tonii@飘渺水云间，2003-5-2于浙大，修改请保留此行。
// ROOM: /d/hsl/liangqi_house4

inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "两栖族首领营帐");
  set("long", @LONG

这是两栖族首领的营帐，和普通居民的营帐没有什么两样，
唯一不同的是里面没有床等生活用品，取而代之的是供开会
用的一张大大的桌子以及一些凳子。两栖族的首领白天就在
这儿处理一些部落的日常事务。
LONG);

  set("exits",
    ([
      "out": __DIR__ "liangqi_street2",
    ]));

  /*  set("objects", 
  ([ 
   "/d/hsl/npc/liangqi_leader" : 1,
  ]));
*/

  set("no_clean_up", 0);

  setup();

}
