//作者：tonii@飘渺水云间，2003-5-6于浙大，修改请保留此行。
//milanduobqp_boss.c

inherit F_VENDOR_SALE;

void create() {
  reload("milanduobqp_boss");
  set_name("默罕默得", ({ "boss mohanmode", "boss", "mohanmode" }));
  set("title", "武器店老板");
  set("gender", "男性");
  set("age", 40);
  set("long", "米兰多武器店的老板，他是一名精明的波斯商人。");
  set("combat_exp", 10000);
  set_skill("unarmed", 50);
  set_skill("dodge", 50);
  set_skill("parry", 50);

  set("vendor_goods", ([
    "dagger": "/d/obj/weapon/dagger/dagger.lpc",
    "axe": "/d/hsl/obj/tujueaxe.lpc",
    "hammer": "/d/obj/weapon/hammer/hammer",
    "fork": "/d/obj/weapon/fork/fork",
    "tiegun": "/d/obj/weapon/stick/bintiegun",
    "tielian": "/d/obj/weapon/whip/tielian",
    "loulanblade": "/d/hsl/obj/loulanblade.lpc",
    "spear": "/d/obj/weapon/spear/tieqiang",
    "staff": "/d/obj/weapon/staff/chanzhang",
    "westsword": "/d/hsl/obj/westsword.lpc",
  ]));

  setup();
  carry_object("/d/obj/cloth/choupao")->wear();

}

void init() {
  object ob;

  ::init();
  if (interactive(ob = this_player()) && !is_fighting()) {
    remove_call_out("greeting");
    call_out("greeting", 1, ob);
  }
  add_action("do_vendor_list", "list");
}

void greeting(object ob) {
  if (!ob || !visible(ob) || environment(ob) != environment()) return;
  switch (random(3)) {
    case 0:
      say(name() + "笑眯眯地说：“这位客官要点什么？”\n");
      break;
  }
}
