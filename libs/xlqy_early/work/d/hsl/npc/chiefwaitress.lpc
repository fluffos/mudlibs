//作者：tonii@飘渺水云间，2003-5-6于浙大，修改请保留此行。
// chiefwaitress.c

inherit F_VENDOR_SALE;

void create() {
  reload("chiefwaitress");
  set_name("服务生领班", ({ "chief waitress", "waitress" }));
  set("long", "她是店中的服务生领班，非常热情能干。\n");
  set("gender", "女性");
  set("combat_exp", 1000);
  set("age", 20);
  set("per", 23);
  set("attitude", "friendly");
  set_skill("unarmed", 40);
  set("vendor_goods", ([
    "jiudai": "/d/ourhome/obj/hdjiudai",
    "gourou": "/d/ourhome/obj/gourou",
    "jitui": "/d/ourhome/obj/jitui",
    "huasheng": "/d/ourhome/obj/huasheng",
  ]));

  setup();
  add_money("silver", 2);
  carry_object("/d/obj/cloth/skirt")->wear();

}
void init() {
  object ob;

  ::init();
  if (interactive(ob = this_player()) && !is_fighting()) {
    remove_call_out("greeting");
    call_out("greeting", 1, ob);
  }
  add_action("do_vendor_list", "list");
}

void greeting(object ob) {
  if (!ob || environment(ob) != environment()) return;
  switch (random(1)) {
    case 0:
      say(name() + "鞠了个躬，说道：这位" +
        RANK_D->query_respect(ob)
        + "，欢迎光临本店！\n");
      break;
  }
}
