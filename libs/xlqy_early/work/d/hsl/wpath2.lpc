// 作者：tonii@飘渺水云间，2003-4-29于浙大，修改请保留此行。
// ROOM: /d/hsl/wpath2.c

inherit ROOM;

#include <ansi.h>

void create() {
  set("short", "山路");
  set("long", @LONG

一条弯弯曲曲的山路，抬头就能望见不远处的范特希雪山，道路已经
变得狭窄和崎岖，遍布岩石和荆棘，马车已经无法通行，只能依靠
两只脚了。
LONG);

  set("exits",
    ([
      "west": __DIR__ "wpath1",
      "east": __DIR__ "wshanlu",
    ]));
  set("outdoors", "hsl");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
