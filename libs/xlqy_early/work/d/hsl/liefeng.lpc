// 作者：tonii@飘渺水云间，2003-5-3于浙大，修改请保留此行。
// ROOM: /d/hsl/liefeng

inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "大地的裂缝");
  set("long", @LONG

这是个两侧有着极其陡峭的悬崖、中间狭长而幽深的大峡
谷，从天空中往下望去，就像是大地开了一条裂缝。
LONG);

  set("exits",
    ([
      "south": __DIR__ "republic_path5",
    ]));

  set("objects",
    ([
      "/d/hsl/npc/master/bulaite": 1,
    ]));

  set("outdoors", "hsl");
  set("no_clean_up", 0);

  setup();

}
