#include <mudlib.h>
inherit ARMOR;
void create ()
{
set ("id", ({"helmet", "helmet"}) );
set ("short", "green helmet");
set ("long", "A small green helmet. It has a visor with small slats in it. This helmet looks like something the Romans might have worn.\n");
set ("mass", 50);
set_armor_type("helmet");
set ("armor",2);
set("value",52);
}
