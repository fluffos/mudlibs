#include <mudlib.h>
inherit ARMOR;
void create ()
{
set ("id", ({"robes", "robes"}) );
set ("short", "purple robes");
set ("long", "The long and flowing robe of a Sinner. It is a deep purple which indicates the true power of its user.\n");
set ("mass", 10);
set_armor_type("cloak");
set ("armor", 1);
set ("value",25);
}
