#include <mudlib.h>
inherit OBJECT ;
void create() {
set("id", ({ "controler"}));
set("short", "N64 Controler");
set("long", @ppp
This is a genuine original N64 controler, however it does not do
much good here since this is a mud which utilizes computers, not
N64's.  But it still is a neat item.......
ppp
);
set("material_type", "plastic");
set("value", "100");
set("name", "Nintendo 64 Controller");
}
