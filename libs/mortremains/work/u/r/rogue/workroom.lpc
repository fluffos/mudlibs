/*// This file contains the code for a generic workroom.// It is used by _home.c if your own workroom won't load.// It is used by _sponsor.c to provide new wizards//   with a basic home that they can edit to their own taste.*/// config.h only included here for the defines of START and VOID// it doesn't need to be included in most rooms.#include <config.h>#include <mudlib.h>inherit ROOM;void create(){  ::create();  seteuid( getuid() );  set( "light", 1 );            set( "short", "Control Room - Zentari Station" );  // NB: "@EndText" should not have spaces after it.  // "EndText" should be on a line of its own (no indentation, tabs or spaces)  set( "long", @EndTextThis is the Control Room for the Zentari Station. All mainframes and more obscure server systems are located here.  The heat is very apparent because of the large number of machines running constantly. Other then the impressive array of machinary, the little wall that is exposed, are whitewashed and thick.  In addition to the computer hardware present, there is a large ammount of security equipment. This is the heart of the station and if anyone that was not allowed in here was able to gain access, the station would fall into his or her hands. EndText  );  set( "exits", ([    "start" : START,    "void"  : VOID,  ]) );}                       
