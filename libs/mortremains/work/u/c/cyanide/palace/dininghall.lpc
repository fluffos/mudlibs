#include "main.h"

inherit MAIN+"/palacestd.lpc";

void create()
{
    ::create();
    set ("short", "Dining Hall");
    set( "long", @EndText
EndText
    );
    set( "exits", ([
      "west" : MAIN+"/phalls1.lpc",
      "east" : MAIN+"/kitchen.lpc",
    ]) );

    set ("item_desc", ([
    ]) );
}

/* EOF */
