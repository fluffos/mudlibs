inherit OBJECT;

void create() {
    set("long", "An empty styrofoam cup.\n");
    set("short", "an empty styrofoam cup");
    set("id", ({ "cup", "empty cup" }) );
    set("smell", "It smells like old coffee.");
}

// EOF
