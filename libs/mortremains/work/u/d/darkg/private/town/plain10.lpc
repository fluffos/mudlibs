#include <mudlib.h>
#include "fuckstick.h"

inherit ROOM ;

void create() {
        	::create();
	seteuid(getuid()) ;
     set ("light", 1) ;
    set("long", @ENDTEXT
ENDTEXT);
set ("short","Grassy plains");
     set ("exits", ([
   "south" : AROOM+"plain18.lpc",
   "west" : AROOM+"plain9.lpc",
   "east" : AROOM+"plain11.lpc",
   "north" : AROOM+"plain5.lpc",
]));
reset();
	}
