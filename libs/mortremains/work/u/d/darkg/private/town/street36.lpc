#include <mudlib.h>
#include "fuckstick.h"

inherit ROOM;

void create()

{
 ::create();
 seteuid(getuid());
  set("light", 1);
  set("short", "Wade");
  set("exits", ([
       "east" : AROOM+"tavern2.lpc",
       "north" : AROOM+"street30.lpc",
       "south" : AROOM+"street34.lpc",
 ]));
}
