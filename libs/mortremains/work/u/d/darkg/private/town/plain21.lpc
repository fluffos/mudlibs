#include <mudlib.h>
#include "fuckstick.h"

inherit ROOM ;

void create() {
        	::create();
	seteuid(getuid()) ;
     set ("light", 1) ;
    set("long", @ENDTEXT
ENDTEXT);
set ("short","Rolling hills");
     set ("exits", ([
   "east" : AROOM+"plain22.lpc",
   "north" : AROOM+"plain15.lpc",
   "west" : AROOM+"plain20.lpc",
]));
reset();
	}
