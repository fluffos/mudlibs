#include <mudlib.h>
#include "fuckstick.h"

inherit ROOM ;

void create() {
        	::create();
	seteuid(getuid()) ;
     set ("light", 1) ;
    set("long", @ENDTEXT
ENDTEXT);
set ("short","Grassy plains");
     set ("exits", ([
   "south" : AROOM+"plain16.lpc",
   "west" : AROOM+"plain7.lpc",
   "east" : AROOM+"plain9.lpc",
   "north" : AROOM+"plain3.lpc",
]));
reset();
	}
