#include <mudlib.h>
#include "fuckstick.h"

inherit ROOM ;

void create() {
        	::create();
	seteuid(getuid()) ;
     set ("light", 1) ;
    set("long", @ENDTEXT
ENDTEXT);
set ("short","Rolling hills");
     set ("exits", ([
   "north" : AROOM+"plain18.lpc",
   "west" : AROOM+"plain23.lpc",
   "south" : AROOM+"swamp1.lpc",
]));
reset();
	}
