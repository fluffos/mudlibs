
#include <mudlib.h>

inherit OBJECT ;

void create() {
	set( "id", ({ "tool", "a tool", "tool" }) );
	set ( "short", "a tool");
	"some tool. that takeda is working on."
EndText
 );
   set ("mass", 0);
   set ("bulk", 0);
   set ("value", ({ 0 }) );
}

  void init () {

 }

 int kill(string str) {
	return;
}
