#include <mudlib.h>

inherit OBJECT;
   void create(){
   seteuid(getuid());
   set("author", "cattt");
   set("id", ({ "invis_room_keeper" }) );
   set("short", "foo");
   set("long", @foo
You should not be seeing this, as it is invisible.
foo
  );
   set("value", 0);
 }
