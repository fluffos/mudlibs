inherit OBJECT;

void create() {
  set("short", "a marker");
  set ("invisible", 1);
  set ("prevent_drop", 1);
  set ("id", ({"#MARKER#", "marker"}) );
  set ("long", "A marker so that the Darkmir guards will attack you.\n");
}
