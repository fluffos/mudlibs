#include <mudlib.h>
#include "baator.h"

inherit BAATOR+"avernus/fortstd.lpc";

void create() {
    ::create();
    set ("exits", ([
      "west" : BAATOR+"avernus/fortbel2.lpc",
      "south" : BAATOR+"avernus/fortbel6.lpc"
    ]));
    set ("objects" , ([ 
      "amduscias" : BAATOR+"monsters/amduscias"
    ]));
    reset();
}

/* EOF */
