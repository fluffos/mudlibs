// Cyanide coded this.

#include "school.h"

inherit S_STD_ROOM;

void create() {
    ::create();
    set("short", "");
    set("long", @EndText
EndText
    );
    set("exits", ([
    "west"  : S_ROOM("h15.lpc"),
    "east" : S_ROOM("h16.lpc")
    ]) );
    set("item_desc", ([
    ]) );
}

/* EOF */         