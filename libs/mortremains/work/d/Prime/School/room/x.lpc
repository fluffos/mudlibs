// Cyanide coded this.

#include "school.h"

inherit S_STD_ROOM;

void create() {
    ::create();
    set("short", "");
    set("long", wrap(
      ) );
    set("exits", ([ 
    ]) );
    set("item_desc", ([
    ]) );
}

/* EOF */
