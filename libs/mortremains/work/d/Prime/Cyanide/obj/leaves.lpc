// A pile of leaves.
// Utterly useless.
// Cyanide - Nov 9 98

inherit OBJECT;

void create() {
    set("short", "a pile of leaves");
    set("long", "Just a basic, boring pile of leaves.\n");
    set("mass", 30);  // Very bulky!
    set("id", ({ "pile", "leaves" }) );
}
