// Cyanide coded this.

#include "macrosoft.h"

inherit MS_STD_ROOM;

void create() {
    ::create();
    set("short", "");
    set("long", wrap(
      ) );
    set("exits", ([ 
      "east" : FLOOR_2("breakroom.lpc"),
      "north" : FLOOR_2("floor11.lpc"),
    ]) );
    set("item_desc", ([
    ]) );
}

/* EOF */
