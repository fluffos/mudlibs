#include "macrosoft.h"

inherit FLOOR_5("std5.lpc");

void create() {
    ::create();
    set("exits", ([
      "south" : FLOOR_5("c2.lpc"),
    ]) );
    set("objects", ([
      "boss" : MS_MON("5boss.lpc")
    ]) );
reset();
}

/* EOF */
