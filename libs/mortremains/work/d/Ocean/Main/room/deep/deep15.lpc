#include <mudlib.h>
#include "ocean.h"

inherit DEEP+"deepstd.lpc";

#define NAME    (string)this_player()->query("name")

void create()
{
    ::create();
    set( "exits", ([
      "east" : DEEP+"deep14.lpc",
      "west" : DEEP+"deep16.lpc",
    ]) );
    set("objects" , ([
      "shark" : O_MON+"shark.lpc",
      "catfish" : O_MON+"cat.lpc",
    ]) );
    reset();
}
