#adm/daemons/mailmesg.c
remain ({"ragevortex",})
mesg "Add this function to the minicow:void die() {    object milk;    milk = clone_object(\"/u/r/ragevortex/items/milk.c\"); // Is that right path?        if (milk)           // This ensures we cloned it right.        milk->move( this_object() );    ::die();    // This calls the original die() dunction defined in                 // monster.c}-Sleep: A completely inadequate substitute for caffeine."
