#adm/daemons/mailmesg.c
remain ({"zur",})
mesg "I fixed drop.armor class is now calculated correctly.  If you want a base armor    class, set (\"base_ac\").  It doesn't account for dex - that's     done in user  (at the moment, at least)."
