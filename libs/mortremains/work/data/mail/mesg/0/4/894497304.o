#adm/daemons/mailmesg.c
remain ({"kyako",})
mesg "BEGIN FORWARDED TEXT:Kyako writes:> how would i have one object tell another object one command line?> without having to make a whole new routine> just one command> would it be target -> command(\"fjshadkfsad\");> if target is the object?> -kyako> the command() function calls a command in an object, and it hasa few catches.command(\"say blah\") will be as though the object commands said \"blah\", of course.The only problem with command is that the command funccan only be called by:  a) The object to be commanded (i.e., this_object() )  b) Root-C"
