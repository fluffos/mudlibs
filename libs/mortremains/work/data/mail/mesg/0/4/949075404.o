#adm/daemons/mailmesg.c
remain ({"dalceon",})
mesg "Your problem lies in the fact that often, when find_object() iscalled, the orb isn't loaded. You may want to use the functionfind_object_or_load(). It works the exact same way, but if theobject isn't loaded, it loads it for you. It's one of the lesserknown efuns, and one I use very often...Let me know if it works (or doesn't...)-Cy"
