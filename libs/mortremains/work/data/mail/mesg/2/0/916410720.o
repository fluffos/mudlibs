#adm/daemons/mailmesg.c
remain ({"driver","cyanide",})
mesg "BEGIN FORWARDED TEXT:Nightmask writes:> > segmentation fault, was crashing the mud> :)> _NM_> A segmentation fault in an object just means that that's the memory location where the seqmentation fault occured. It's not what caused the crash.  ;)-C"
