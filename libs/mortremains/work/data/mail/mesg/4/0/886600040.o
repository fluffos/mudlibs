#adm/daemons/mailmesg.c
remain ({"morrigu",})
mesg "ok, the problem was in create()You MUST inherit the original monster create() functionyou do this by adding this line in your create function:::create();(see orccook for example)also make sure you do the same for the init() functionie add this line in that function:::init();(also see the orc cook or any of mymonsters for examples)-keep codin-Ilz"
