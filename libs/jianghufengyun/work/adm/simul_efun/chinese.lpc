
string chinese_number(int i)
{
	return CHINESE_D->chinese_number(i);
}
string to_chinese(string str)
{
	return CHINESE_D->chinese(str);
}
int is_chinese(string str)
{
	// AGENTS.md §15h: was a GBK lead-byte range check (str[0]>160 &&
	// str[0]<255), always false against this driver's UTF-8 codepoints.
	// Replaced with a CJK Unicode codepoint range check.
	if( strlen(str)>=1 && str[0] >= 0x4e00 && str[0] <= 0x9fff ) return 1;
	return 0;
}
