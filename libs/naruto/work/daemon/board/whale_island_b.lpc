inherit BULLETIN_BOARD;

void create()
{
        set_name("米特留言店", ({ "mite board", "board" }) );
        set("location", "/world/area/whale_island/mite_hotel");
        set("board_id", "_MITE_");
        setup();
        set("capacity", 200);
        replace_program(BULLETIN_BOARD);
}

