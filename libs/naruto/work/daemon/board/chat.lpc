
inherit BULLETIN_BOARD;

void create()
{
	set_name("閒聊留言板", ({ "chat board", "board" }) );
	set("location", "/world/area/wizard/power");
	set("board_id", "chat");
	setup();
	set("capacity", 200);
	replace_program(BULLETIN_BOARD);
}
