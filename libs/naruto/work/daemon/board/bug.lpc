
inherit BULLETIN_BOARD;

void create()
{
	set_name("臭蟲回報板", ({ "bug board", "board" }) );
	set("location", "/world/area/wizard/bug");
	set("board_id", "bug");
	setup();
	set("capacity", 200);
	replace_program(BULLETIN_BOARD);
}
