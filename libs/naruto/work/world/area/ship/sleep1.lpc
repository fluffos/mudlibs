#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "臥房");
        set("long", @LONG
臥房一樓，旁邊有個樓梯可以到樓下。臥房一樓可以說簡陋的不
能在簡陋了，只有幾張吊床，其他的東西什麼也沒有。
LONG);
    set("exits", ([
        "down" : __DIR__"sleep2.lpc",
        "south" : ([ "filename" : __DIR__"lufi.lpc",
                       "x_axis" : 9,
                       "y_axis" : 15,
                  ]),
    ]));
    setup();
}
