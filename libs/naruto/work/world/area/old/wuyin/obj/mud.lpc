#include <ansi.h>

inherit COMBINED_ITEM;

void create()
{
    set_name(YEL"爛泥巴"NOR, ({ "mud" }));
    if( !clonep() ) {
        set("base_unit", "沱");
        set("base_value", 0);
        set("base_weight", 100);
        set("long", "一沱爛泥巴。\n");
    }
    setup();
}
