#include <ansi.h>
inherit COMBINED_ITEM;

void create()
{
    set_name(HIW"鯊魚皮"NOR, ({ "sharkskin" }));

    if( !clonep() ) {
        set("base_unit", "張");
        set("base_value", 200);
        set("base_weight", 1000);
        set("long", "鯊魚的外皮，似乎蠻堅硬的。\n");
    }
    setup();
}
