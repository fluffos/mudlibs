#include <ansi.h>

inherit COMBINED_ITEM;

void create()
{
    set_name(HIK"熊毛"NOR, ({ "bear's hair", "hair" }));

    if( !clonep() ) {
        set("base_unit", "根");
        set("base_value", 50);
        set("base_weight", 600);
        set("long", "一根粗粗灰灰的熊毛。\n");
    }
    setup();
}
