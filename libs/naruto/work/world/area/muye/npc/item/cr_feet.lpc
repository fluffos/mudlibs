#include <ansi.h>

inherit COMBINED_ITEM;

void create()
{
    set_name(HIK"蟑螂腳"NOR, ({ "cockroach's foot", "foot" }));
    if( !clonep() ) {
        set("base_unit", "支");
        set("base_value", 4);
        set("base_weight", 100);
        set("long", "一支惡臭的蟑螂腳。\n");
    }
    setup();
}