//Cracked by Roath
//anshi.c
#include <room.h>
inherit ROOM;

void create() {
  set("short", "亭底暗室");
  set("long", @LONG
这是牡丹亭底下的一间暗室。黑咕窿咚的什么也看不清。你试着
到处摸了摸(mo)。 
LONG);

  set("exits",
    ([  //sizeof() == 4
    "up": __DIR__ "mudanting",
    ]));

  create_door("up", "桌底暗门", "down", DOOR_CLOSED);
  set("no_clean_up", 1);

  setup();
}
