#include <ansi.h>
inherit "/d/maze/lingzhu-feng/room.lpc";

void create() {
  set("short", "内室地下");
  set("channel_id", "风灵珠副本");
  set("long", @LONG
四周黑漆漆一片，你只有运足眼力，方能看到一尺见方的景象。
你摸索的前行，冷不丁就被自己的喘气声吓了一跳。

LONG);
  set("alternative_die", 1);
  set("virtual_room", 1);
  set("exits", ([
    "south": __DIR__ "maze/exit",
    "up": "/d/gao/neishi",
  ]));
  set("objects", ([
    "/d/maze/lingzhu-feng/npc/boss": 1,
  ]));
  setup();
}
