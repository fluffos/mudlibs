#include <ansi.h>
inherit "/d/maze/lingzhu-tu/room.lpc";

nosave string color = ({
  HIC,
  HIW,
  HIB,
  HIM,
  HIR,
  HIG,
  HIY,
  CYN,
  WHT,
  MAG,
  BLU,
  RED,
  GRN,
  YEL
})[random(14)];

void create() {
  set("short", "坑洞");
  set("channel_id", "土灵珠副本");
  set("long", @LONG
正上方就是飞凤峰谷底，四面全是土，你不由地屏住呼吸。
LONG);
  set("alternative_die", 1);
  set("virtual_room", 1);
  set("exits", ([
    "west": __DIR__ "maze/entry",
  ]));
  set("objects", ([
    //        	"/d/maze/lingzhu-tu/npc/yao" : 1,
  ]));
  setup();
}
