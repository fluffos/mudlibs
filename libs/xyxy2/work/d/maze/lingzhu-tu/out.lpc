#include <ansi.h>
inherit "/d/maze/lingzhu-tu/room.lpc";

void create() {
  set("short", "坑洞");
  set("channel_id", "土灵珠副本");
  set("long", @LONG
正上方就是飞凤峰谷底，四面全是土，你不由地屏住呼吸。
LONG);
  set("alternative_die", 1);
  set("virtual_room", 1);
  set("no_magic", 1);
  set("no_fight", 1);
  set("exits", ([
    "east": __DIR__ "maze/exit",
    "up": "/d/meishan/fengdi",
  ]));
  setup();
}
