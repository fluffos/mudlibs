inherit ITEM;

void create() {
  set_name("粗磁酒瓶(空)", ({ "jiuping", "skin" }));
  set_weight(500);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一个用来装酒的粗磁酒瓶，能装两，三斤白酒。\n");
    set("unit", "个");
    set("value", 80);
  }
  setup();
}
