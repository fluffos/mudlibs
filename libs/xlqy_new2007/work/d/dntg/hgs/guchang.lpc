// by bugbug & alading @ 缥缈水云间, 2003-4-5
// Room: /d/4world/guchang.c
inherit ROOM;

void greeting(object who);
void create ()
{
  set ("short", "谷场");
  set ("long", @LONG

这是靠近城边的一个打谷场，场上很平并压的很硬。边上高高低低
有几个草堆，几棵大柳树下有几条野狗在打瞌睡。场中有几只老母
鸡在啄谷粒吃。
LONG);

  set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/muji" : 3,
]));
  set("outdoors", "/d/4world");
  set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"east2",
]));

  setup();
}
void init()
{
	mapping job;
	object who = this_player();
	if(!userp(who)) return;
	if(!mapp(job=who->query("wuguan/job")))
		return;
	if(job["job"] == "抓鸡")
	   call_out("greeting",1,who);

}
void greeting(object who)
{
	object hen;
	if(environment(who)->query("short") != "谷场")
		return;
	if(present("old hen",environment(who)))
		return;
	hen = new(__DIR__"npc/muji");
	hen->move(environment(who));
	message_vision("旁边晃晃悠悠走来一只母鸡，全心全意啄着地上的谷粒\n",who);
		
	
	
}
