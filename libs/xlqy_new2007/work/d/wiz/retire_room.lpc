// by bugbug & alading @ 缥缈水云间, 2003-4-5
inherit ROOM;
#include <ansi.h>

void create ()
{
        set ("short",HIR "仙侣情缘疗养院" NOR);
        set ("long", @LONG
这里是仙侣情缘疗养院,退休的老玩家都在这里颐养天年。

LONG);

        set("exits", 
        ([ 
		"south" :"/d/wiz/entrance",
        ]));

        set("objects", 
        ([ 
        ]));
	set("no_fight",1);
	set("no_magic",1);
    setup();
}
