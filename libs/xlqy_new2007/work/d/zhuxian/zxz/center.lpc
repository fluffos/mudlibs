// by bugbug & alading @ 缥缈水云间, 2003-4-5
inherit ROOM;                                                        
#include "bancast.h"

void create ()
{
  set ("short", "阵中心" );   
  set ("long", @LONG   
原来这里就是阵中心了。
再往上看去，云雾缭绕，正是诛仙阵神坛所在！
LONG);
    set("exits", ([ 
               "up" : __DIR__"shentan",
//               "north" : __DIR__"yaofang",
                  ]));
    set("objects", ([ 
//               __DIR__"npc/guard" : 2,
                  ]));
                   set("no_burn",1);
    setup();
}

void init() 
{
  add_action("do_quit", "quit");  add_action("do_sleep", "sleep");
  add_action("do_ji", banned_action);
  add_action("do_cast",({"cast","burn"}));
}

