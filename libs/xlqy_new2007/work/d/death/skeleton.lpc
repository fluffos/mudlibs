// by bugbug & alading @ 缥缈水云间, 2003-4-5
// zhting.c

inherit ROOM;

void create() {
  set("short", "招魂塔");
  set("long", @LONG
这里是地府的招魂塔所在，死去的活物可以在此得到不死的永生。(reng corpse)
LONG);

  set("exits", ([ /* sizeof() == 4 */
    "down": __DIR__ "new-walk2",
  ]));
  set("hell", 1);
  set("throw_corpse_able", 1);
  // set("valid_startroom", 1);

  setup();
}
