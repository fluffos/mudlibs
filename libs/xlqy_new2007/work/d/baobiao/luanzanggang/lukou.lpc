inherit ROOM;
void create() {
  set("short", "路口");
  set("long", @LONG
这里就是乱葬岗的入口了，听说里边有怪物出没，没有一定工夫还是别去的好。
LONG);
  set("exits", ([
    "east": __FILE__,
    "south": __DIR__ "mi2",
    "north": __FILE__,
    "west": "/d/changan/fendui",

  ]));
  set("no_clean_up", 0);
  setup();
}
