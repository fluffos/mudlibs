#include <ansi.h>

inherit ITEM;
void create()
{
        set_name(HIG"镖车"NOR, ({"biao che", "che",}));
        set_weight(88888);
        set("long","一辆镖局运镖的镖车，上面插着振远镖局的五花翎旗。\n");
  set("unit", "辆");
  set("no_put",1);
        set("no_sell",1);
        set("no_get",1);
        set("value",1);
        set("level",1);
        set("arrive",0);
        setup();
}
