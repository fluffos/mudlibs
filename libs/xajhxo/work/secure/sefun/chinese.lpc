//
// Filename : /adm/simul_efun/chinese.c
//

string chinese_place_name(string str) {
  return CHINESE_D->chinese_place_name(str);
}

string chinese_number(int i) {
  return CHINESE_D->chinese_number(i);
}

string to_chinese(string str) {
  return CHINESE_D->chinese(str);
}

// AGENTS.md §15h fix (same as secure/simul_efun/chinese.lpc). This copy
// (secure/obj/sefun.lpc's include) appears unreachable in this lib
// (nothing loads secure/obj/sefun.lpc), fixed anyway as cheap insurance.
int is_chinese(string str) {
  if (strlen(str) >= 1 && str[0] >= 0x4e00 && str[0] <= 0x9fff)
    return 1;
  return 0;
}
