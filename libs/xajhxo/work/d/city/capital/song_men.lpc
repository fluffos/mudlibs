// 汴京  song_men.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
        set("short",HIW "宋门" NOR);
        set("long", @LONG
宋门在旧城东墙，因为出城去的道路通向南京应天府，也就是宋这个地方，
因此俗称宋门，它的正名叫做丽京门。南边不远处，汴河自东水门流出。用来运
送江南物资到京师的汴河每年都进行整修，设专官主持。水深六尺，宽六丈，水
堤以石板为标记，疏浚工程认真之极。
LONG
        );
        set("objects", ([ 
         ]));
        set("exits", ([    
            "west" : __DIR__"bianhe_jie3",
            "east" : __DIR__"xsng_men",
         ]));
        set("detail",([
        ]));
        set("outdoors", "capital");
        setup();
}

