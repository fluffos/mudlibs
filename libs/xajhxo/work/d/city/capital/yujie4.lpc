// 汴京   yujie4.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
        set("short",HIW "御  街" NOR);
        set("long", @LONG
御路两旁是两带深数尺宽一丈的御沟，灌流金水河水，沟中种植了荷花无数，
沟边更有桃梨海棠等各种花树，春香满街，姹紫不断。大道两旁尽开些店铺茶肆
，道上人来人往，车马喧沸，络绎不绝；南面是汴河，一座气派的石桥跨河而过。
西南一个院落是都亭驿；东面一条沿河大街直通宋门。
LONG
        );
        set("objects", ([        
         ]));
        set("exits", ([  
            "north" : __DIR__"yujie3",
            "south" : __DIR__"tianhan_qiao", 
            "southwest" : __DIR__"duting_yi", 
            "east" : __DIR__"bianhe_jie1",
    ]));
    set("detail",([
    ]));
        setup();
}
void init()
{
}

