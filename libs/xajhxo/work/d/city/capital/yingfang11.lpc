// 汴京 yingfang7.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit YINGFANG;
void create()
{
        set("short","营房");
        set("long", @LONG     
一幢宽敞的营房，外间靠墙放着武器，甲胄。是营中士兵的讲习所。里间则
是一通大炕。平时这里白天只有几个军士看屋子。其余的人有的操练，有的
巡逻，还有的就去结伙四处游玩去了。       
LONG
        );
        set("objects", ([ 
    ]));
        set("exits", ([    
            "east" : __DIR__"caolian_chang7",
    ]));
    set("detail",([
    ]));     
        setup();
}

