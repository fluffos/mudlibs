//汴京 LIQUIDs: nverhong_jiu.c 

inherit ITEM;
inherit F_LIQUID;

#include <ansi.h>

void create() {
  set_name(RED "女儿红" NOR, ({ "nver hong", "hong", "jiu" }));
  set_weight(300);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", @LONG
        一个竹子制成的大酒杯，里面恰好能装一角酒。
LONG);
    set("unit", "杯");
    set("value", 120);
    set("max_liquid", 3);
    set("drink_msg", "$N端起酒杯喝了一大口女儿红。\n");
  }
  set("liquid", ([
    "type": "alcohol",
    "name": "女儿红",
    "remaining": 3,
    "drink_apply": 20,
    "drunk_apply": 10,
  ]));
}
void init() {
  add_action("do_drink", "drink");
}

int drink_effect(object who, object this) {}
