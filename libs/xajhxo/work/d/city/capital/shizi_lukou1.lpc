// 汴京  shizi_lukou1.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
        set("short","十字路口");
        set("long", @LONG
东面是一条打扫得很整洁的街道，一直通向太庙大门。西边就是景灵宫的东
墙了。那里是供奉历代先皇遗像的地方。南面一条小巷，直通大相国寺。北面是
一片繁华的商业区，人来人往。
LONG
        );
        set("objects", ([ 
         ]));
        set("exits", ([   
            "south" : __DIR__"tianshui_xiang1",
            "north" : __DIR__"jieshen_xiang1",
           "east" : __DIR__"jingling_jie1",
         ]));
        set("detail",([
        ]));
        set("outdoors", "capital");
        setup();
}
void init()
{
}

