// 汴京   jingling_jie2.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
        set("short","景灵宫东街");
        set("long", @LONG 
街西直通景灵宫东门，一眼望去，可见门前一排全副武装的士兵。东面一直
走就是太庙。因为皇帝往太庙祭祀经常要走这条路，因此打扫得相当干净。街北
是大理寺和国子监。
LONG
        );
        set("objects", ([ 
         ]));
        set("exits", ([  
            "west" : __DIR__"jingling_jie1",
            "east" : __DIR__"taimiao_jie",
         ]));
        set("detail",([
        ]));
        set("outdoors", "capital");
        setup();
}
void init()
{
}

