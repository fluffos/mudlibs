// room:/d/dali/neitang.c  writen by king Jan-21-97

#include <room.h>
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "内堂");
  set("long", @LONG
这里是王府内堂，四壁悬满了字画，居中一块横匾，上写“逍遥堂”三个大
字，下面是一行小字“丁卯自题”，四个角落里摆了好些珍奇古玩，当中是一张
圆桌，几个圆凳，平时王爷有红颜知己时长在此饮酒。下人们轻易不的入内。
LONG
  );
  set("exits", ([
    "south": __DIR__ "beilang",
  ]));

  setup();
  replace_program(ROOM);
}
