// Room: /d/city/yangzhou/bei_xiaojie.c

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", YEL "草河北小街" NOR);
  set("long", @LONG
这是草河小街的尽头了，北边就是扬州城墙了。西边是扬州的府学「资政书
院」，西北边是魁星阁。这里游人稀少，只有几个书生匆匆而过，府学的门口有
几个顽劣学童在嬉笑打闹，给这寂静的街道添了几分生气。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "xiaojie",
    "west": __DIR__ "zizheng_shuyuan",
    "northwest": __DIR__ "kuixing_ge",
    "east": __DIR__ "kaiming_qiao",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/qiu_xingxing": 1,
    __DIR__ "npc/yang_leishi": 1,
  ]));
  set("outdoors", "yangzhou");
  set("no_clean_up", 0);
  set("no_kill", "yangzhou");

  setup();
}
