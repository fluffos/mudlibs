// Room: /d/city/yangzhou/hongqiao_jie.c

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", YEL "虹桥街" NOR);
  set("long", @LONG
虹桥街虽是青石铺地，地面却多有裂缝，也不平整，大概虹桥街是条老街的
缘故。两边的屋檐密密重重，傍河而建，数条小巷穿插其中。行人大多匆匆而过，
偶然有马蹄磕击青石的“哒哒”声。北面就是崇雅书院，增先生在此传授学问。
LONG
  );
  set("no_clean_up", 0);
  set("outdoors", "yangzhou");
  set("no_kill", "yangzhou");
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "hongqiao_chasi",
    "north": __DIR__ "chongya_shuyuan",
    "west": __DIR__ "beimen_dajie",
    "east": __DIR__ "hongqiao",
  ]));

  setup();
}
