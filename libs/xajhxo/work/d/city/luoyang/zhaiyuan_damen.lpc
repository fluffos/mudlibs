// rooms:/d/luoyang/zhaiyuan_damen.c

#include <room.h>

inherit ROOM;

void create() {
  set("short", "宅院大门");
  set("long", @LONG
你来到一座大大的宅院门前，只见院子的围墙很高，院门紧闭，门前的行人
很少，和外面热闹的洛阳城形成鲜明的对比，你很难在洛阳城中找到这么安静的
一块地方来，不由的想停下脚步休息一会儿。
LONG
  );
  set("exits", ([
    "west": __DIR__ "tongtuo_jie2",
    // 此处尚待完善
  ]));
  set("outdoors", "luoyang");
  setup();
  replace_program(ROOM);
}
