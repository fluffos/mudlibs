// Room: /xuanwu/xiuxi_shi2.c

inherit ROOM;

void create() {
  set("short", "休息室");
  set("long", @LONG
掀开软帘，就到了休息室里。墙上挂着一只别致的野羊头骨。头
骨上两只黑黑的大洞正对着你，流露出的是它临死的恐惧，还是对这
弱肉强食的世界的无奈？几只宽大的扶手靠背椅上铺着斑斓的虎皮，
几根长长的虎毛在气流中微微的摆动，犹如溪流中的水草。屋子中间
，一盆火正欢快地跳跃着，把活泼泼的光和温暖散发到屋子的每一个
角落。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "neiyuan",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
