// Room: /u/ysg/room/qingbao.c
// (C) ysg 5/9/1997

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "情报室");
  set("long", @LONG
这里是玄才坛的情报室。这是一间小屋子，屋子里井井有条的放着七、八个
架子，架子上按格一层一层整整齐齐的堆放着江湖上各大门派和顶尖高手的卷宗
。只要你是本坛弟子，又得到了坛主的同意，你什么时候都可以来研习武林中各
门各派的武功和高手的脾气、性情。屋子里陈设很简单，只在屋子角落里放了几
张椅子让人坐着看书。
LONG
  );

  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "midao13",
    "north": __DIR__ "yanxishi",
  ]));
  set("objects", ([
    "/clone/book/yirong_book": 1,
  ]));
  setup();
}
