// Room: /u/ysg/room/xiuxi2.c
// (C) ysg 5/9/1997

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "休息室");
  set("long", @LONG
这里是玄阳坛里的休息室。一走近这里，你就听到了一阵“呼～～～呼～～
～”的打鼾声，让你感觉自己也不由得困了起来。走进门里后，你就看见一群没
有任务的杀手正横七竖八的躺在榻上休息。
LONG
  );

  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "xiuxi1",
  ]));

  setup();
}
