// Room: /u/ysg/room/shenf2.c

inherit ROOM;

void create() {
  set("short", "身份甄别处");
  set("long", @LONG
你终于走出了密道，来到了总舵的玄才坛，这里是总舵的身份甄别处，所有
的一品堂弟子，不论你是什么职务，到了这里都得出示你的腰牌，接收检查。看
到你走过来，一名玄才坛弟子马上朝你走了过来...。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "northeast": __DIR__ "luang",
    "north": __DIR__ "xiuxi3",
    "east": __DIR__ "groom2",
    "southwest": __DIR__ "xuancai_tang_rukou",
  ]));
  set("no_clean_up", 0);

  setup();
}
