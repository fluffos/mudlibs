// Room: zhongting.c

inherit ROOM;

void create() {
  set("short", "钟亭");
  set("long", @LONG
一座古老的钟亭. 年代仿佛和整座大宅同样久远. 红木雕栏用精细的工艺刻
着花鸟鱼虫. 只是由于年代久远而稍有褪色. 钟亭成八角, 每角的飞檐都高高挑
起, 并各挂着一串风铃, 微风拂过, 叮咚之声飘渺入云. 
    南边是个一个小山坡, 山坡绿草如茵, 仿佛这里不是风沙满天的西北. 
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "xiaoshanpo",
    "north": __DIR__ "yongdao2",
  ]));
  set("objects", ([
    __DIR__ "npc/saodi_puren": 1,
  ]));
  set("outdoors", "lnwm");
  setup();
}
