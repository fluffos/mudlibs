// fozhu.c
//by leonhart

inherit ITEM;

void create() {
  set_name("佛珠", ({ "fozhu" }));
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一串檀香木佛珠\n");
    set("unit", "串");
    set("value", 100);
  }
  setup();
}
