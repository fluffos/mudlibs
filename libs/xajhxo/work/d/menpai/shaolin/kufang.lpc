// Room: /d/shaolin/kufang.c
// Date: 1997 1 10
// by fencer

#include <room.h>
inherit ROOM;

void create() {
  set("short", "武器库");
  set("long", @LONG
这里整整齐齐的摆放着各种兵器，四下一望，你顿觉眼花缭乱。十八般兵器
之外，许多江湖上少见的奇门兵器，也收集得非常齐全。
LONG
  );

  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "jiadao.lpc",
    "west": __DIR__ "kufang1.lpc",
    //      "north": __DIR__"kufang2.lpc",
  ]));
  set("objects", ([
    __DIR__ "npc/xuying": 1,
    BLADE_DIR "mudao": 1,
    SWORD_DIR "changjian": 1,
  ]));
  setup();
}
