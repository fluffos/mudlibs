// Room: /d/shaolin/banruo_tang2.c

inherit ROOM;

void create() {
  set("short", "首座居室");
  set("long", @LONG
空落落的屋子里，连张床都没有，只在靠墙处有一个蒲团。你实在很难相信
，这居然就是位高权重的般若堂首座    大师住的地方。
LONG
  );
  set("exits", ([
    "west": __DIR__ "banruo_tang1.lpc",
  ]));

  setup();
}
