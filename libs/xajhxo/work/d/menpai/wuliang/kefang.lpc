inherit ROOM;
void create() {
  set("short", "客房");
  set("long", @LONG
屋内陈设极为简单，一床，一几，几上搁了一盆君子兰，迎面墙壁上是一幅
写意山水，侧面一扇小门引向后面的怡心阁，尽管平时这里都没有人住，但每天
都有人来打扫，所以极是洁净。
LONG
  );
  set("exits",
    ([
      "east": __DIR__ "huayuan",
      "west": __DIR__ "yixin_ge",
    ]));
  set("objects", ([
    __DIR__ "npc/puren": 2,
  ]));
  setup();
  replace_program(ROOM);
}
