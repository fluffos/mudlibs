// room:/d/menpai/duanshi/qianyuan.c  writen by lala July-25-97

#include <room.h>
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "王府前院");
  set("long", @LONG
这里是镇南王府的前院，一色的青砖铺地，四周种了几棵大树。这里是王府
下人们活动的中心，到各处都要经过这里。向西是饭厅，向东是帐房。
LONG
  );
  set("exits", ([
    "south": __DIR__ "qianyuan2",
    "north": __DIR__ "wangfu_dayuan",
    "west": __DIR__ "fanting",
    "east": __DIR__ "zhangfang",
  ]));
  set("item_desc", ([
    "north": "只见一座雕刻着一条穿云飞龙的绿琉璃瓦朱红影壁墙严严实实地挡住了你的视线。\n",
  ]));
  set("outdoors", "dali");
  setup();
  call_other(CLONE_DIR "board/duan_b", "???");
}
