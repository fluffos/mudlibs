// kill_lanzhou.c

inherit F_CLEAN_UP;

int update_condition(object me, int duration) {
  me->apply_condition("kill_lanzhou", duration - 1);
  if (!duration) {
    return 0;
  }
  return 1;
}
