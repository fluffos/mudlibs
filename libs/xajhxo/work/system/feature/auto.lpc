// auto.c
// Amber Edit on 1997-12-15 9:06
// To Set a Way Which Program Itself Can Let User Do Command.
// 原来的autod.c的功能。

void AutoAct(string cmd) {
  if (wizardp(this_object()))
    return;  // 防止有人作坏事，呵呵。
    command(cmd);
}
