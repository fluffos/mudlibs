// west_b.c

inherit BULLETIN_BOARD;

void create() {
  set_name("无量剑派西宗留言板", ({ "board" }));
  set("location", "/d/menpai/wuliang/west/keting");
  set("board_id", "west_b");
  set("long", "这是一个无量剑派西宗弟子留言记事的留言板。\n");
  setup();
  set("capacity", 150);
  replace_program(BULLETIN_BOARD);
}
