// moguo.c

inherit ITEM;

void create() {
  set_name("磨锅茶", ({ "moguo cha", "cha", "tea" }));
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "这是普普通通的磨锅茶。\n");
    set("unit", "包");
    set("value", 600);
  }
  setup();
}
