// huacha.c

inherit ITEM;

void create() {
  set_name("花茶", ({ "hua cha", "cha", "tea" }));
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "这是普普通通的花茶。\n");
    set("unit", "包");
    set("value", 300);
  }
  setup();
}
