string absolute_path(string curr, string newp)
返回基于一个相对路径的绝对路径

int absolute_value(int x)
绝对值

string base_name(mixed val)
返回一个 object 没有扩展名的文件名

string convert_name(string str)
返回 cap name 对应的真名

mixed copy(mixed val)
返回 array 或者 mapping 的一个拷贝

mixed *distinct_array(mixed *arr)
a faster, better namedfunction based on Huthar's uniq_array()


