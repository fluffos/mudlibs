Command: a
Effect : append characters
Modes  : @x, <string>

Examples:
a       - prompt for and append new text after current char. pointer position
a @15   - prompt for and insert new text after character position 15
a <str> - append the string <str> after the current char. pointer position

Also see the help on strings.
 