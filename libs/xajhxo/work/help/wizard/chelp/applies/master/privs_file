
	privs_file(4)		MudOS v21c2		(5 Sep 1994)

	名称:
		privs_file - 为新创造的物件指定一个私有字串 (privs
			     string).
	语法:
		string privs_file( string filename );

		字串 privs_file( 字串 filename );
	用法:
		当新档案创造出来时, 会呼叫主宰物件 (master object) 中
		的 privs_file() 函式. 物件的档案名称为其参数, 而
		privs_file()  传回的字串就用作此新物件的私有字串.

		只有在驱动程式编译时定义 PRIVS  选项, privs_file() 才
		有作用.
	参考:
		query_privs(3), set_privs(3)
	翻译:
		Spock @ FF	97.Aug.6.		(printed 3/16/95)
