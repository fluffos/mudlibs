
	get_save_file_name(4)	MudOS v21c2		(5 Sep 1994)

	名称:
		get_save_file_name - 於使用者不正常断线时, 备份其修改
				     过的内容.
	语法:
		string get_save_file_name( string );

		字串 get_save_file_name( 字串 );
	用法:
		当一个玩家於编辑程式中编辑档案时断线的话, ed()  函式会
		呼叫 get_save_file_name().  此函式应传回与原来档案不同
		的名称, 以免覆写原来的档案.
	注意:
		此函式以前名为 get_ed_buffer_save_file_name().
	参考:
		ed(3)
	翻译:
		Spock @ FF	97.Aug.6.		(printed 3/16/95)
