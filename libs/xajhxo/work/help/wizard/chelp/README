	MudOS v21c2 原文文件中文翻译 v1			97.Aug.12.

	［此份文件不包括 doc/efuns, 请另外索取 cefuns 中文翻译文件］


	此份文件加上译者日前所公开的 efuns  文件翻译, 大致上已将 MudOS
	v21c2 之原文文件翻译完成. 不过, 因为译者所学不多, 难免有遗珠之憾
	. 未能翻译之文件清单列於附注一.

	这份文件中的分类结构如下:

	doc
	├———applies			驱动程式对各个物件呼叫的函式
	│      ├———interactive	驱动程式对玩家物件呼叫的函式
	│      └———master		驱动程式对主宰物件呼叫的函式
	├———concepts		撰写 LPC  所必备的一些概念
	├———lpc
	│      ├———constructs	LPC 程式的基本指令字
	│      ├———preprocessor	LPC 程式的前置处理区命令
	│      └———types		LPC 的变数、函式的型态定义
	└———ref			化石....:)

	文件中如有错误或是更好的翻译, 请来函指正. 如果您愿意翻译附注一中
	所列之文件, 也请来函告知, 并允许译者收录於往後的版本中.

	此文件可以随意流通, 但是您如果要用於公开架设之 mud  上, 请来函告
	知. 另外本文件不得用於商业营利用途.
						Spock @ FF	97.Aug.12.

附注一:
	以下二十九份档案为译者所力有未逮之处....:Q

	doc/concepts/oop
	doc/concepts/socket_efuns
	doc/defines 共十份文件
	doc/defines 共十一份文件
	doc/driver  共五份文件
	doc/lpc/types/substructures

附注二:
	在 ES 2 公开之 mudlib 程式中所附之 doc/ref  文件中, 已有六份文件
	是由 Wade 兄及 anslasax 兄在远古以前所翻译的古迹. 这些文件都与译
	者所翻译者一起保留在同一个目录中. 如果您看不惯译者所译之版本, 可
	有另外的选择....

	以下由 wade 兄所翻译:

		doc/ref/lpc.2darray
		doc/ref/lpc.driver
		doc/ref/lpc.function
		doc/ref/lpc.lpc
		doc/ref/lpc.object

	原文相同的文件:

		doc/concepts/lpc 与 lpc.lpc
		doc/concepts/MudOSdriver 与 lpc.driver
		doc/concepts/objects 与 lpc.object
		doc/lpc/types/array.2d 与 lpc.2darray
		doc/lpc/types/function 与 lpc.function

	以下文件由 anslasax 兄所翻译: 

		ref/lpc.mud

	附注三:
		以下所采用之中文译名, 文件中大多不再补注其英文原名.

		admin			mud 系统管理者
		administrator		admin 的原文, 但在此通常指
					  主机管理员
		Annihilator		安老大, 安尼席洛特
		argument		参数
		array			阵列
		buffer			暂存区
		character		字元
		driver			驱动程式
		efun			超越函式, emulated function
		element			元素
		euid			effective user id 之缩写.
					  译为有效使用者识别名称.
		flag			旗标
		force, the		原力
		function		函式或函式指标
		global			全域
		heartbeat		心跳
		id			识别名称
		information		资讯或资料
		interger		整数
		local			本域、区域
		mapping			映射
		message			讯息
		mixed			译为「混合」, 此为 LPC  所
					  特有的资料型态, 允许此资
					  料为任何种类的资料型态.
		null			空值
		object			物件
		player			玩家
		pointer			指标
		query			询问、查询
		return			传回 (动词)
		runtime			编译时段
		string			字串
		source code		原始程式码
		system			系统
		target			目标
		uid			user id 之缩写. 使用者识别
					  名称
		value			值
		void			原意为「无」, 视情况翻译为
					  无传回值或无参数.
		wizard			巫师
		zero			零或零值
