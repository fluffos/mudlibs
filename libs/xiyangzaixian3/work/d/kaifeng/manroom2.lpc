inherit ROOM;

void create()
{
	set("short", "小屋");
	set("long", @LONG
这是专供仆人休息的小屋。屋里放着五六张床，挤挤地占掉了屋
子的一大半地方。屋角很散乱地放了一些日常生活用具。中央放着一
张桌子，上面摆一茶壶和几个杯子。
LONG
	);
	set("exits", ([
		"west" : __DIR__"hualang8",
	]));
	set("objects", ([
		"/d/kaifeng/npc/puren" : 2,
	]) );    

//	set_light(0) ;
	set("sleep_room","1") ;
	setup();
	replace_program(ROOM);
}
