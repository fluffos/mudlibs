// Room: /d/huanggon/jianlao2.c

inherit ROOM;

void create() {
  set("short", "皇宫监牢");
  set("long", @LONG
这里黑漆漆的什么也没有, 四周墙上粘满了血迹, 一个狱卒看你进来
马上拔出手中的长刀来。
LONG
  );
  set("objects", ([
    //      "/d/huanggon/npc/baobiao" : 1,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "up": __DIR__ "houyuan",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
