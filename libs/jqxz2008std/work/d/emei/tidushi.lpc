inherit ROOM;

void create() {
  set("short", "剃度室");
  set("long", @LONG
这里是峨眉派剃度室。
LONG
  );

  set("exits", ([
    "northeast": __DIR__ "dadian",
  ]));

  set("objects", ([
    "/kungfu/class/emei/kong": 1,
  ]));

  setup();
  replace_program(ROOM);
}
