// SN:Rb>2@_Jd2mWDH9^c
// ITEM Made by player(残旅:canlv) /data/item/c/canlv-hand.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Sat Oct  2 20:05:19 2004
#include <ansi.h>
#include <armor.h>

inherit HANDS;
inherit F_ITEMMAKE;

void create() {
  set_name("佛手[2;37;0m", ({ "hand" }));
  set_weight(2700);
  set("item_make", 1);
  set("unit", "双");
  set("long", "这是由[1;35m神铁[2;37;0m炼制而成的一双铁掌。
如来佛的手，你说厉害不？[2;37;0m
铁掌缘上刻着一行小字：残旅(canlv)
");
  set("value", 2100000);
  set("point", 160);
  set("material", "magic stone");
  set("wear_msg", "如来佛一挥手，大喊一声，我的手借你用一下吧！[2;37;0m\n");
  set("remove_msg", "如来佛喃喃道，我要吃饭了，把手还给我吧！[2;37;0m\n");
  if (!check_clone()) return;
  restore();
  set("armor_prop/unarmed_damage", apply_damage());
  set("armor_prop/strike", 10);
  set("armor_prop/hand", 10);
  set("armor_prop/armor", 10);

  setup();
}

string long() { return query("long") + item_long(); }
