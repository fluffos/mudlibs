// SN:IK6_VR3e^<][n8]i
// ITEM Made by player(拾荒者:walker) /data/item/w/walker-walkerf.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Sat Sep 11 03:39:26 2004
#include <ansi.h>
#include <armor.h>

inherit HEAD;
inherit F_ITEMMAKE;

void create() {
  set_name("[2;37;0m[33m男巫师[37m头骨[2;37;0m", ({ "walkerf" }));
  set_weight(4500);
  set("item_make", 1);
  set("unit", "顶");
  set("long", "这是由[1;35m神铁[2;37;0m炼制而成的一顶头盔。
头盔缘上刻着一行小字：拾荒者(walker)
");
  set("value", 2100000);
  set("point", 226);
  set("material", "magic stone");
  set("wear_msg", "[33m$N[33m装备[2;37;0m[33m男巫师[37m头骨[2;37;0m[33m。[2;37;0m\n");
  set("remove_msg", "[33m$N[33m脱下了[2;37;0m[33m男巫师[37m头骨[2;37;0m[33m。[2;37;0m\n");
  if (!check_clone()) return;
  restore();
  set("armor_prop/armor", apply_armor());

  setup();
}

string long() { return query("long") + item_long(); }
