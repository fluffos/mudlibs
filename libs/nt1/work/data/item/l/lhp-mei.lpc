// SN:T]?O^g14h]ZL1?Q;
// ITEM Made by player(少龙:lhp) /data/item/l/lhp-mei.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Wed Jun  9 15:27:28 2004
#include <ansi.h>
#include <weapon.h>

inherit SWORD;
inherit F_ITEMMAKE;

void create() {
  set_name("[1;37m踏雪寻梅[2;37;0m", ({ "mei" }));
  set_weight(9000);
  set("item_make", 1);
  set("unit", "柄");
  set("long", "这是由[1;35m神铁[2;37;0m炼制而成的一柄剑。
[1;37m驿外断桥边，寂寞开无主。已是黄昏独自愁，更著风和雨。无意苦争春，一任群芳妒。零落成泥碾作尘，只有香如故。[2;37;0m
剑柄上刻着一行小字：少龙(lhp)
");
  set("value", 2100000);
  set("point", 682);
  set("material", "magic stone");
  set("wield_msg", "[37m十年生死两茫茫。不思量，自难忘。千里孤坟，无处话凄凉。纵使相逢应不识，尘满面，鬓如霜。[2;37;0m\n");
  set("unwield_msg", "[35m夜来幽梦忽还乡。小轩窗，正梳妆。相顾无言，惟有泪千行。料得年年肠断处，明月夜，短松冈。[2;37;0m\n");
  if (!check_clone()) return;
  restore();
  init_sword(apply_damage());

  setup();
}

string long() { return query("long") + item_long(); }
