// SN:IAe;Nb>b^lRR?lM9
// ITEM Made by player(唐三藏:gangbj) /data/item/g/gangbj-jia.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Thu Nov 13 13:07:05 2003
#include <ansi.h>
#include <armor.h>

inherit ARMOR;
inherit F_ITEMMAKE;

void create() {
  set_name("[1;36m宝甲[2;37;0m", ({ "jia" }));
  set_weight(9000);
  set("item_make", 1);
  set("unit", "张");
  set("long", "这是由[1;35m神铁[2;37;0m炼制而成的一张护甲。
这件战甲刀剑之痕密布，看上去陪伴主人经历了不少的岁月。[2;37;0m
护甲缘上刻着一行小字：唐三藏(gangbj)
");
  set("value", 2100000);
  set("point", 201);
  set("material", "magic stone");
  set("wear_msg", "[1;37m$N穿上一件$n[1;37m。[2;37;0m\n");
  set("remove_msg", "[1;37m$N将$n[1;37m脱下收起。[2;37;0m\n");
  if (!check_clone()) return;
  restore();
  set("armor_prop/armor", apply_armor());
  set("armor_prop/parry", apply_armor() / 15 + 1);

  setup();
}

string long() { return query("long") + item_long(); }
