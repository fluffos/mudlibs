// SN:EDXco8H:d06?]PJ;
// ITEM Made by player(唐五藏:tangwu) /data/item/t/tangwu-jia.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Sat Dec 13 21:19:31 2003
#include <ansi.h>
#include <armor.h>

inherit ARMOR;
inherit F_ITEMMAKE;

void create() {
  set_name("[1;31m宝[1;36m甲[2;37;0m", ({ "jia" }));
  set_weight(9000);
  set("item_make", 1);
  set("unit", "张");
  set("long", "这是由[1;35m神铁[2;37;0m炼制而成的一张护甲。
看上去似乎在制造过程中出了一些意外，不怎么样。[2;37;0m
护甲缘上刻着一行小字：唐五藏(tangwu)
");
  set("value", 2100000);
  set("point", 80);
  set("material", "magic stone");
  set("wear_msg", "[1;33m$N穿上一套$n[1;33m。[2;37;0m\n");
  set("remove_msg", "[1;33m$N将$n[1;33m脱了下来。[2;37;0m\n");
  if (!check_clone()) return;
  restore();
  set("armor_prop/armor", apply_armor());
  set("armor_prop/parry", apply_armor() / 15 + 1);

  setup();
}

string long() { return query("long") + item_long(); }
