// SN:iNEKSmIE9NF\[GP9
// ITEM Made by player(小黑:hei) /data/item/h/hei-heia.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Fri Jan 23 14:23:50 2004
#include <ansi.h>
#include <armor.h>

inherit HANDS;
inherit F_ITEMMAKE;

void create() {
  set_name("[1;32m落叶[2;37;0m", ({ "heia" }));
  set_weight(2700);
  set("item_make", 1);
  set("unit", "双");
  set("long", "这是由[1;35m神铁[2;37;0m炼制而成的一双铁掌。
[1;37m小黑的铁掌[2;37;0m
铁掌缘上刻着一行小字：小黑(hei)
");
  set("value", 2100000);
  set("point", 220);
  set("material", "magic stone");
  set("wear_msg", "[1;37m一道[1;33m光[1;37m扫过天空，只见$n[1;37m飞进$N的掌中！[2;37;0m\n");
  set("remove_msg", "[33m$N[33m脱下了[1;32m落叶[2;37;0m[33m。[2;37;0m\n");
  if (!check_clone()) return;
  restore();
  set("armor_prop/hand", 10);
  set("armor_prop/armor", 10);
  set("armor_prop/unarmed_damage", apply_damage());
  set("armor_prop/strike", 10);

  setup();
}

string long() { return query("long") + item_long(); }
