// SN:TNabGXI:>QF59cOg
// ITEM Made by player(胡逸:meng) /data/item/m/meng-clu.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Tue Nov  9 23:52:28 2004
#include <ansi.h>
#include <armor.h>

inherit HANDS;
inherit F_ITEMMAKE;

void create() {
  set_name("[1;37m晨露[2;37;0m", ({ "clu" }));
  set_weight(2700);
  set("item_make", 1);
  set("unit", "双");
  set("long", "这是由[1;35m神铁[2;37;0m炼制而成的一双铁掌。
铁掌缘上刻着一行小字：胡逸(meng)
");
  set("value", 2100000);
  set("point", 145);
  set("material", "magic stone");
  set("wear_msg", "[1;31m$N使出慈悲枪法「舍身喂鹰」，将浑身的功力都运到点钢枪上！\n[2;37;0m\n");
  set("remove_msg", "[1;37m$N一声轻吟，双掌挥舞出一个大圈，横推而出，顿时一股汹涌澎湃的劲气如波浪一般向你涌来。\n[2;37;0m\n");
  if (!check_clone()) return;
  restore();
  set("armor_prop/armor", 10);
  set("armor_prop/strike", 10);
  set("armor_prop/hand", 10);
  set("armor_prop/unarmed_damage", apply_damage());

  setup();
}

string long() { return query("long") + item_long(); }
