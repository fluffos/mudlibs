// SN:]HMPX56?Y?ATA4i=
// File(/data/room/xiaodongxi/chashi.c) of xiaodongxi's room
// Create by LUBAN written by Doing Lu

#include <room.h>

inherit PRIVATE_ROOM;

void create() {
  set("short", "茶室");
  set("long", @LONG
一个玲珑小巧的茶室，很是显得精致。正中有一紫檀木茶几，边
上站着两个眉目清秀的小丫环，笑盈盈的看着你，等着主人的传唤。[2;37;0m
LONG);

  set("exits", ([
    "west": __DIR__ "yingke",
  ]));

  set("objects", ([
    "/d/room/roomnpc/teagirl": 2,
  ]));

  create_door("west", "木门", "east", DOOR_CLOSED);

  set("no_sleep_room", 1);
  setup();

  set("room_owner", "眉眉");
  set("room_name", "爱的小屋");
  set("room_id", "loveer");
  set("room_owner_id", "xiaodongxi");
  set("room_position", "碎石小道");
}
