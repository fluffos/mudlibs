// SN:8<K2ZLdDMXeBSWPa
// File(/data/room/god/dayuan.c) of god's room
// Create by LUBAN written by Doing Lu

     inherit PRIVATE_ROOM;

void create()
{
        set("short", "云海大院");
	set ("long", @LONG
[1;31m这是上帝居住的地方[1;32m四周看起来云雾弥蒙，看不清哪里是边缘，传说上

帝经常休息的地方，不过没有人看见过他的到来和离去。如风一样来，如风一

样去。[2;37;0m
LONG );

	set("exits", ([
		"north"  : __DIR__"qianting",
                "out"    : "/d/city/mingren",
	]));

        set("outdoors", "unknow");
        set("no_sleep_room", 1);
        set("valid_startroom", 1);
        setup();

        set("room_owner", "上帝");
        set("room_name", "云海");
        set("room_id", "god");
        set("room_owner_id", "god");
        set("room_position", "名人堂");
}
