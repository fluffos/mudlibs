// SN:PGT`AoJAkjZRL4CH
// File(/data/room/lingling/xiaoyuan.c) of lingling's room
// Create by LUBAN written by Doing Lu

     inherit PRIVATE_ROOM;

void create()
{
        set("short", "翎卿居小院");
	set ("long", @LONG
[1;36m雾渐渐浓了起来，崎岖的路面让人不易通过，再加上浓雾妨
    碍了视线，走到著里只好一步一步小心翼翼的慢慢前进，不
    然一个不小心就会跌个狗吃屎罗。心里正在咒骂著路难行的
    时候，透过雾气眼前似乎出现了间小小木屋。[2;37;0m
LONG );

        set("room_key", "1 of lingling");
	setup();

	set("exits", ([
                "out"    : "/d/hangzhou/shanlu7",
	]));

        set("outdoors", "hangzhou");
        set("no_sleep_room", 1);
        set("valid_startroom", 1);

        set("KEY_DOOR", ([
                "exit" : "north",
                "room" : __DIR__"dating",
        ]));
    
        set("room_owner", "慕容翎");
        set("room_name", "翎卿居");
        set("room_id", "ling");
        set("room_owner_id", "lingling");
        set("room_position", "山路");
}
