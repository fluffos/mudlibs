// SN:<;DBDG3\oP]mf;G5
// File(/data/room/satyrdm/jusuo.c) of satyrdm's room
// Create by LUBAN written by Doing Lu

#include <room.h>

inherit PRIVATE_ROOM;

void create() {
  set("short", "居所");
  set("long", @LONG
这就是蚂蚁二号主人训练专家休息、读书的地方。楼上就是主人
的卧室，西面则是书房。 
LONG);

  set("exits", ([
    "up": __DIR__ "woshi",
    "west": __DIR__ "shufang",
  ]));

  set("no_sleep_room", 1);
  setup();

  set("KEY_DOOR", ([
    "exit": "east",
    "room": __DIR__ "huayuan",
  ]));

  set("room_owner", "训练专家");
  set("room_name", "蚂蚁二号");
  set("room_id", "antstwo");
  set("room_owner_id", "satyrdm");
  set("room_position", "碎石小道");
}
