// SN:6hdcTm=7h>b];H]2
// File(/data/room/master/lianwu.c) of master's room
// Create by LUBAN written by Doing Lu

     inherit PRIVATE_ROOM;

void create()
{
	set("short", "练武场");
	set ("long", @LONG
此处便是蛟龙出海的练武场，人在江湖，武功万万不可荒废。 
LONG );

	set("exits", ([
                "west" : __DIR__"houyuan",
	]));

        set("objects", ([
                "/clone/npc/mu-ren" : 4,
        ]));

        set("outdoors", "playertown");
        set("no_sleep_room", 1);

        setup();
    
        set("room_owner", "殷正廉");
        set("room_name", "蛟龙出海");
        set("room_id", "master");
        set("room_owner_id", "master");
        set("room_position", "碎石小道");
}
