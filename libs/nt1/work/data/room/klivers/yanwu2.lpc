// SN:O]\S>K9Didh@^`E5
// File(/data/room/klivers/yanwu2.c) of klivers's room
// Create by LUBAN written by Doing Lu

     inherit PRIVATE_ROOM;

void create()
{
        set("short", "看看之家演武厅");
	set ("long", @LONG
这是看看之家的演武厅，有时有些江湖朋友来会，想一试身手，
就在这里比武较量。 
LONG );

	set("exits", ([
                "east" : __DIR__"yishiting",
	]));

        set("objects", ([
                "/clone/weapon/zhubang" : 1,
                "/clone/weapon/zhujian" : 1,
                "/clone/weapon/mudao"   : 1,
        ]));

        set("no_sleep_room", 1);
        setup();

        set("room_owner", "逍遥一剑");
        set("room_name", "看看之家");
        set("room_id", "kkroom");
        set("room_owner_id", "klivers");
        set("room_position", "南大街");
}
