// SN:g^ef6?Ib>W51X:YW
// File(/data/room/changhong/dating.c) of changhong's room
// Create by LUBAN written by Doing Lu

#include <room.h>

inherit PRIVATE_ROOM;

void create() {
  set("short", "白云深处大厅");
  set("long", @LONG
这就是白云深处主人长虹迎送客人的地方。正中有些桌椅，侧面
是两扇屏风。东首是主人长虹的房间，有一扇木门虚掩着。南边是大
门，出去就是院子。 
LONG);

  setup();

  set("exits", ([
    "east": __DIR__ "woshi",
    "north": __DIR__ "houyuan",
  ]));

  create_door("east", "木门", "west", DOOR_CLOSED);

  set("no_sleep_room", 1);

  set("KEY_DOOR", ([
    "exit": "south",
    "room": __DIR__ "xiaoyuan",
  ]));

  set("room_owner", "长虹");
  set("room_name", "白云深处");
  set("room_id", "baiyun");
  set("room_owner_id", "changhong");
  set("room_position", "青砖路");
}
