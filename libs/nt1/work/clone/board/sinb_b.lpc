inherit BULLETIN_BOARD;

void create() {
  set_name("小黑板", ({ "board" }));
  set("location", "/u/sinb/workroom");
  set("board_id", "sinb_b");
  set("long", "安全中心的小黑板，上面密密麻麻地写了一些字。\n");
  setup();
  set("capacity", 3000);
}
