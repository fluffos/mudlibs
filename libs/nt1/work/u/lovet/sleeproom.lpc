// lovet's sleeproom 
// by lovet

#include <ansi.h>
#include <room.h>
inherit CREATE_CHAT_ROOM;

int is_chat_room() { return 1; }

void create() {
  set("short", "卧室");
  set("long", @LONG
这里是执着的卧房。陈设简单而又别致。一张绣榻放
在墙角，两只金钩轻搭纱帐。中间一张木制圆桌，摆有一水晶花瓶，插着 
每天采集来的鲜花。左边一个衣柜（yi gui），放有主人喜爱的衣服。 
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "west": "/u/lovet/workroom",
  ]));

  set("valid_startroom", 1);
  set("no_fight", "1");
  set("sleep_room", 1);
  set("no_clean_up", 0);
  setup();
}

void init() {
  add_all_action();
}
