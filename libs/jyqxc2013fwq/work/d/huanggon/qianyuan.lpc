inherit ROOM;

void create()
{
	set("short", "清和殿前院");
	set("long", @LONG
这里是清和殿前院。院里种着些青菜。
。西边是一条走廊，通往清和殿。
LONG
	);
        set("outdoors", "huanggon");
	set("resource/water", 1);
	set("exits", ([
		"west" : "/d/huanggon/zoulang2",
	]));

	setup();
	replace_program(ROOM);
}

