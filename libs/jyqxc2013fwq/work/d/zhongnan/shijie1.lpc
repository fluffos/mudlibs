inherit ROOM;

void create() {
  set("short", "青石阶");
  set("long", @LONG
这是青石铺成的台阶。
LONG
  );

  set("exits", ([
    "south": __DIR__ "lianwuchang",
    "northup": __DIR__ "shijie2",
  ]));

  set("objects", ([
    "/kungfu/class/quanzhen/liu": 1,
  ]));

  setup();
  replace_program(ROOM);
}
