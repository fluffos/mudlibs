inherit BULLETIN_BOARD;

void create()
{
        set_name("仙剑派密言版",({"swordsecret board", "board"}));
        set("location","/open/gsword/room/eqroom");
        set("board_id","swordsecret");
        set("long",     "仙剑派留言版。\n");
        setup();
        set("capacity", 30);
    set("master",({"roger"}));
}
