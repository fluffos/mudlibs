inherit ROOM;

void create ()
{
  set ("short", "[1;35m过去时空[0m 水烟阁前");
  set ("long", @LONG
    这里是水烟阁的正厅，简单的陈设中透出一股尊贵不凡的气派，一
张黝黑的太师椅端坐在大厅中央，这里同时也是「武者”们议事的所在
，每年的正月十七都要在水烟阁外的「虹台”推选武林盟主，然后在这
里举行登位大典。

LONG);

  set("outdoors", "cele");
  set("valid_startroom", 1);
  set("exits", ([ /* sizeof() == 1 */
  "south": "/d/cele/enter",
]));

        set("objects", ([
                "/d/cele/npc/champion":1,
                "/d/cele/npc/master":1
        ]) );


  setup();
}
