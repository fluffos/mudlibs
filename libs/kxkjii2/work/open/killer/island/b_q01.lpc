#include <ansi.h>
inherit ROOM;
void create ()
{
  set ("short", "[1;37m房间[0m");
  set ("long", @LONG
在一阵的错愕之下，你不小心踏破了底板，跌出了这艘船之外，紧接着又有许
多的残骸跟着掉了下来，‘碰’的一声，你的头上被某种金属重物打到，看来这艘
船真的差不多快塌掉了。
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"is15",
]));
  set("objects", ([ /* sizeof() == 1 */
  __DIR__"obj/box" : 1,
]));
  set("outdoors", 1);

  setup();
}
