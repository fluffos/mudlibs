// Room: /u/f/fire/room/room1-9.c
inherit ROOM;
#include <ansi.h>
#include "marksman.h"
void create() {
  set("short", "[32m长沙道[2;37;0m");
  set("long", @LONG
这是一条相当宽敞的道路，在道路两旁伫立着用黄铜所铸成的李
广大人的铜像，栩栩如生且威风凛凛不禁让人感到它的神圣威严，你
可以看到长沙城此地热闹无比。
LONG);

  set("objects", ([ /* sizeof() == 1 */
    "/open/gsword/mob/sells2.lpc": 1,
  ]));
  set("outdoors", "/open/marksman");
  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "room1-13.lpc",
    "west": __DIR__ "room1-11.lpc",
    "south": __DIR__ "room1-8",
    "east": __DIR__ "room1-15.lpc",
  ]));

  setup();
}
