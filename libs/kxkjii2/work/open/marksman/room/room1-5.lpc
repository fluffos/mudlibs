// Room: /u/f/fire/room/room1-5.c
inherit ROOM;
#include <ansi.h>
#include "marksman.h"
void create ()
{
  set ("short", "[1;34m张家铁铺[2;37;0m");
  set ("long", @LONG
你走进一间充满热气的房子中，这里是长沙城最大的打铁铺，在
房间的中央有一口高有四尺的大火炉，红红的火焰从炉中不断的冒出
。在火炉中则有许多已经烧红的铁块。在火炉的旁边则是一缸好大的
清水。房内的铁匠正在赶制一流的武器，你可以在这选出一流的武器
来使用。
想知道这里有那些武器可以选用的话请用(view)来查看!!!
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"room1-4",
]));

  set("objects", ([ /* sizeof() == 1 */
C_NPC"/chang-sen" : 1,
]));
  setup();
}
