// Room: /u/f/fire/room/room1-29.c
inherit ROOM;
#include <ansi.h>
#include "marksman.h"
void create ()
{
  set ("short", "[1;33m强者之道[2;37;0m");
  set ("long", @LONG
从这里可以更加的清楚的音乐，不只高亢的琴声，还有钗h旋律
简单而柔和的钢琴声。更令人注意的是在这似乎是跟着这个音乐的节
拍在走动，各各脸上露出一副快乐的表情。
LONG);

  set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"room1-31",
  "south" : __DIR__"room1-27.lpc",
]));
  set("objects", ([ /* sizeof() == 1 */
C_NPC"/wu-mon.lpc" : 1,
]));

set("light_up",1);
  setup();
}
