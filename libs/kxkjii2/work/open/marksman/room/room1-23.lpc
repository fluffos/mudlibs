#include <room.h>
inherit ROOM;
#include "marksman.h"
#include <ansi.h>
#include "marksman.h"
void create ()
{
  set ("short", "[1;33m强者之道[2;37;0m");
  set ("long", @LONG
你现在正站在将军府，此地气态万千，远近高低望之，都有不同
的气象，其设计可谓巧夺天工。所有雕刻的鸟兽草木，都是远从远处
请来的大师呕心沥血之作，站在此地，不免要赞叹人类工迹之伟大。
LONG);

  set("exits", ([ /* sizeof() == 4 */
  "west" : __DIR__"room1-26.lpc",
  "north" : __DIR__"room1-24.lpc",
  "out" : __DIR__"room1-22.lpc",
  "east" : __DIR__"room1-25.lpc",
]));
  set("item_desc", ([ /* sizeof() == 1 */
  "out" : (: look_door,     "out" :),
]));
  set("objects", ([ /* sizeof() == 1 */
C_NPC"/marker" : 1,
]));
  set("light_up", 1);

  setup();
}
