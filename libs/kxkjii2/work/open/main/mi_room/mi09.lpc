inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "[5m[1;37m狂风七星阵阵心[2;37;0m");
  set("long", @LONG

    你走进这里, 突然一阵狂风吹起, 括起漫天风沙, 你置
身在这疾风当中, 顿感一阵晕炫, 眼前一片模糊, 看不清出
四周的事物。

LONG);

  set("exits", ([ /* sizeof() == 1 */
    "leave": "/open/main/room/maiu-1.lpc",
  ]));
  set("light_up", 1);
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/m-ya": 1,
  ]));
  set("no_transmit", 1);

  setup();
}
