// Room: /u/s/sueplan/newplan/area/room10
inherit ROOM;

void create() {
  set("short", "[1;37m天相[1;33m星[0m");
  set("long", @LONG
    纣王手下有许多忠臣，其中有一位闻太师，却是不能被遗忘的
存在。‘太师’即为首相，如我国的行政院长。闻太师不但掌握全国行
政，而且兼任大元帅。
    可足闻太师在殷、周最后一场大决战中，被周军功击攻击而战死。
太白金星感动他的忠勇精神，就引魂到天界，请他永住天相星。

LONG);

  set("objects", ([ /* sizeof() == 1 */
    "/open/scholar/room/newplan/npc/scholar_houng.lpc": 1,
  ]));
  set("no_transmit", 1);
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "room9.lpc",
    "阵心": __DIR__ "jroom.lpc",
  ]));

  setup();
}
int valid_leave(object me, string dir) {
  if (dir == "阵心" && present("scholar", environment(me))) {
    return notify_fail("你发觉有人挡在你面, 不让你过!!!\n");
  }
  return 1;
}
