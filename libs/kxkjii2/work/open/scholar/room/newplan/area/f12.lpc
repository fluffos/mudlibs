// Room: /u/s/sueplan/newplan/area/f12.c
inherit ROOM;

void create() {
  set("short", "[1;33m吟风阁[0m");
  set("long", @LONG
风徐徐吹来，将笼罩着的薄雾吹散，定眼一看，已来到了吟风阁门前
；看这宅第苍松环抱难辨大小，一色的白墙灰瓦相当素雅，门口两只石狮
子张嘴微笑着，似乎也安于这隐世独立般的情调；一走到了门口，一位丫
鬟正笑吟吟地挡住了你的去路，看来你可得依着规矩送上名帖才行。
LONG);

  set("exits", ([ /* sizeof() == 2 */
    "enter": __DIR__ "f13.lpc",
    "west": __DIR__ "f11",
  ]));
  set("outdoors", "/u/s");

  setup();
}
int valid_leave(object me, string dir) {
  if (dir == "enter") {
    if (me->query_mark("sixgod-plan") && me->query_temp("six_ask") < 4)
      return notify_fail("这里你是进不去的!!\n");
  }
  return 1;
}
