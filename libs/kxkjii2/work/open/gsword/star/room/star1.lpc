// Room: /u/b/blue/room/star1.c
#include <ansi.h>
inherit ROOM;

void create ()
{
  set ("short", "[1;36m瑶光楼[2;37;0m");
  set ("long", @LONG
这是七星塔中的第一层楼, 塔中充斥无数炫丽的光线, 
令你走到此楼, 瞬间眼睛睁不开来, 过了不久, 眼睛渐渐
的适应了四周的亮度, 只见眼前站着一位威严的剑士, 令
你心生畏惧, 让你有点不敢向前迈去．
LONG);

  set("exits", ([ /* sizeof() == 2 */
  "up" : __DIR__"room3.lpc",
  "out" : __DIR__"room2.lpc",
]));
  set("objects", ([ /* sizeof() == 1 */
  "/open/gsword/star/npc/star7.lpc" : 1,
]));
  set("light_up", 1);

  setup();
}
int valid_leave(object me, string dir)
{
if( dir=="up" && present("starman",environment(me)))
    if(!me->query_temp("allow_read",1))
   return notify_fail("你以为你能活得过去吗??"
+RANK_D->query_respect(me)+"你去死吧!\n");
return ::valid_leave(me,dir);
}
