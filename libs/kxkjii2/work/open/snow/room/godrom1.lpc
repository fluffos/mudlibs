// Room: /open/snow/room/godrom1.c
// 天灵谷
#include <room.h>
#include <ansi.h>
inherit ROOM;
void create ()
{
  set ("short", "[1;35m天灵谷[2;37;0m");
  set ("long", @LONG
这里就是传说中的天灵谷，谷中长满了许多不知名的奇珍异草散发出
的香味让人心旷神怡，消除了你身上的疲倦，在花草之中有一条小路
向前方而去。
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"godrom2.lpc",
]));
  set("no_auc", 1);
  set("no_transmit", 1);

  setup();
}
