// Room: /u/d/dhk/questsfan/rm27
inherit ROOM;
#include <ansi.h>
void create ()
{
  set ("short", "百[1;32m草谷[2;37;0m");
  set ("long", @LONG
顾名思义，就是充满各式各样的奇花异草的山谷。这里可以说是
整座嵩山数有药用、疗效的奇花异草最聚集，也是最齐全的地方。而
且传闻这里住着一位有着＂毒神药圣″之称的前辈，也因为他的存在
令整座百草谷更增添不少神秘的色彩。

LONG);

  set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/oldman.lpc" : 1,
]));
  set("exits", ([ /* sizeof() == 1 */
  "eastup" : __DIR__"rm21",
]));
  set("outdoors", "/u/d");
  set("no_fight",1);

  setup();
}
