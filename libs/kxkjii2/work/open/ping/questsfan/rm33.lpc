// Room: /u/d/dhk/questsfan/rm28
inherit ROOM;
#include <ansi.h>
void create() {
  set("short", "藏[1;35m仙谷[2;37;0m");
  set("long", @LONG
这里是嵩山最有名的＂嵩山双谷″之一，有着‘仙人故乡’的雅
称的藏仙谷。据一些江湖谣传，若是修道之人在将近得道成仙之前能
前往此谷修行、居住一段时日便能立即得道成仙。不过你仔细向四周
打量一番，发觉此地只不过是一座普通的山谷，顶多是环境略为优雅
、清新一点罢了，实在看不出它的特别在何处，实在令人纳闷呀！

LONG);

  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "obj/flower.lpc": 1,
  ]));
  set("outdoors", "/u/d");
  set("no_transmit", 1);
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "rm34",
    "west": __DIR__ "rm32",
  ]));

  setup();
}
