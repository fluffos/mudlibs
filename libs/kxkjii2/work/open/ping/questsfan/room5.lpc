// Room: /u/d/dhk/questsfan/room5
inherit ROOM;
#include <ansi.h>
#include <room.h>
void create ()
{
  set ("short", "[0;36m密道[2;37;0m");
  set ("long", @LONG
暗暗的走道中，忽然飘来一阵凉风。这时你心想是出
口到了吗？你卯足全劲仔细搜索，果然走道的尽头上方有
一处若隐若现的出口，而且风就好像是从那里来的。

LONG);
set("no_transmit", 1);
  set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"room4",
]));

  setup();
}
void init()
{
        add_action("do_search", "search");
}

int do_search()
{
        object who;
        who = this_player();

        tell_object(who,"\n你仔细搜索，终于找到一个隐密的出口。\n\n");
        who->move(__DIR__"room6");
        return 1;
}
