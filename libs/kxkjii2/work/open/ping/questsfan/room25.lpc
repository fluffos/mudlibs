// Room: /u/d/dhk/questsfan/room25
inherit ROOM;
#include <ansi.h>
void create ()
{
  set ("short", "圆[1;33m晶[0m层");
  set ("long", @LONG
这一区的结晶依然没有颜色，但是却非常特别的满
地均是一颗颗浑圆剔透，光滑有致的圆晶，若此时有另
一堆世界上最棒，评价最高的的珍珠与之相比。仍会逊
色许多。这正是善集天下奇珍异宝的天堂呀！如果天天
在此与这些圆晶对望，都不会感到厌烦呢！若硬要形容
它，那我只能用＂丽″这一个字来形容。

LONG);

  set("outdoors", "/u/d");
  set("no_transmit", 1);
  set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"room24",
  "westup" : __DIR__"room26",
]));

  setup();
}
