#include <ansi.h>

inherit BOOK;
inherit F_NOCLONE;

void create() {
  set_name("「疯魔拳」", ({ "fengmo-unarmed book" }));
  set_weight(500);
  /*
        if( clonep() )
                set_default_object(__FILE__);
        else*/ {
    set("unit", "本");
    set("long", "这是一本用薄绢写成的书。上书：“疯魔拳”。\n书上署名周不通(butong)。\n");
    set("value", 1);
    set("auto_load", 1);
    set("material", "silk");
    set("skill", ([
      "name": "fengmo-unarmed",
      "exp_required": 100000,
      "jing_cost": 40,
      "difficulty": 100,
      "max_skill": 199,
      "min_skill": 0
    ]));
  }
  setup();
  check_clone();
}
