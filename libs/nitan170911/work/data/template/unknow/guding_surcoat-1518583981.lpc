// guding_surcoat.c
// Create by TEMPLATE_D. Tue Aug 18 11:12:31 2015.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit SURCOAT;

#include <template/armor.h>
void create() {
  set_name("古锭披风[2;37;0m", ({ "guding surcoat" }));
  set_name("古锭披风[2;37;0m", ({ "guding surcoat" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一件古锭披风[2;37;0m(guding surcoat)。
");
    set("unit", "件");
    set("value", 200);
    set("material", "cloth");
    set("armor_prop", ([ /* sizeof() == 3 */
      "neili_recover": 80,
      "armor": 550,
      "derive_effect": 6,
    ]));
  }

  set_weight(500);
  set("can_sign", 1);
  set("insert", ({ /* sizeof() == 1 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 1 */
        "max_neili": 10000,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("armor_type", "surcoat");
  set("quality_level", 5);
  set("no_sell", 1);
  set("colorname", "[1;33m古锭披风[2;37;0m[2;37;0m");
  set("stable", 0);
  set("consistence", 100);
  set("color", "[1;33m");
  set("auto_load", 1);
  set("enchase/apply_prop/max_neili", 10000);
  set("enchase/used", 1);
  set("enchase/flute", 1);
  set("set_data", 1);
  set("no_store", 1);
  set("item_owner", "snowman");
  setup();
}

int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
