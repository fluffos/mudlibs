// languang_feihun3.c
// Create by TEMPLATE_D. Sun Dec 14 21:10:59 2014.
#include <ansi.h>
#include <armor.h>

inherit HANDS;

void create()
{
	set_name("[1;36m蓝光飞魂[2;37;0m", ({ "languang feihun3" }));
        set_weight(4000);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("unit", "双");
	set("value", 2000000);
	set("armor_prop", ([ /* sizeof() == 3 */
  "str" : 14,
  "armor" : 100,
  "damage" : 300,
]));


	set("limit", ([ /* sizeof() == 2 */
  "scborn" : 1,
  "exp" : 5000000,
]));



	set("material", "gold");
        }
	set("armor_type", "hands");
	set("no_store", 1);
	set("item_owner", "fds");
	set("no_sell", 1);
	set("stable", 25);
	set("consistence", 100);
        setup();
}

int query_autoload()
{
        return 1;
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

