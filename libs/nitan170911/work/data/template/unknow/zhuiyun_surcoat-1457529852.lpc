// zhuiyun_surcoat.c
// Create by TEMPLATE_D. Thu Aug 20 22:07:47 2015.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit SURCOAT;

#include <template/armor.h> 
void create()
{
	set_name("追云披风[2;37;0m", ({ "zhuiyun surcoat" }));
	set_name("追云披风[2;37;0m", ({ "zhuiyun surcoat" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件追云披风[2;37;0m(zhuiyun surcoat)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 4 */
  "avoid_die" : 2.500000,
  "research_effect" : 3,
  "avoid_poison" : 3.000000,
  "armor" : 1380,
]));
        }

        set_weight(500);
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 1 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "practice_times" : 15,
        "research_effect" : 3,
        "derive_times" : 15,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("armor_type", "surcoat");
	set("quality_level", 6);
	set("no_sell", 1);
	set("colorname", "[41;1m[1;37m追云披风[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[41;1m[1;37m");
	set("auto_load", 1);
	set("enchase/apply_prop/derive_times", 15);
	set("enchase/apply_prop/research_effect", 3);
	set("enchase/apply_prop/practice_times", 15);
	set("enchase/used", 1);
	set("enchase/flute", 1);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "koilian");
        setup();
}

int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

