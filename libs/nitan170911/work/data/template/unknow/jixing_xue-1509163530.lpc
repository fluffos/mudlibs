// jixing_xue.c
// Create by TEMPLATE_D. Tue Dec  9 13:37:31 2014.
#include <ansi.h>
#include <armor.h>

inherit BOOTS;

void create() {
  set_name("[1;31m火麒麟·疾行[2;37;0m", ({ "jixing xue" }));
  set_weight(10);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    set("unit", "双");
    set("long", "[1;31m这是一双通体火红的靴子，散发着令人窒息的光芒。
");
    set("value", 2000000);

    set("armor_prop", ([ /* sizeof() == 1 */
      "dex": 60,
    ]));
    set("material", "boots");
    set("limit", ([ /* sizeof() == 2 */
      "exp": 50000000,
      "dex": 80,
    ]));



  }
  set("armor_type", "boots");
  set("no_store", 1);
  set("item_owner", "sywu");
  set("no_sell", 1);
  set("stable", 50);
  set("consistence", 100);
  setup();
}

int query_autoload() {
  return 1;
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
