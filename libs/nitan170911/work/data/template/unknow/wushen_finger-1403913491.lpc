// wushen_finger.c
// Create by TEMPLATE_D. Wed Jan 29 14:19:28 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit FINGER;

#include <template/armor.h>
void create() {
  set_name("无神指套[2;37;0m", ({ "wushen finger" }));
  set_name("无神指套[2;37;0m", ({ "wushen finger" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一件无神指套[2;37;0m(wushen finger)。
");
    set("unit", "件");
    set("value", 200);
    set("material", "cloth");
    set("armor_prop", ([ /* sizeof() == 4 */
      "kar": 3,
      "armor": 10,
      "unarmed_damage": 2200,
      "derive_effect": 3,
    ]));

  }

  set_weight(500);
  set("can_sign", 1);
  set("insert", ({ /* sizeof() == 1 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "avoid_defense": 1.500000,
        "da_power": 2.000000,
        "damage": 13600,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("armor_type", "finger");
  set("quality_level", 4);
  set("no_sell", 1);
  set("colorname", "[1;35m无神指套[2;37;0m[2;37;0m");
  set("stable", 0);
  set("consistence", 100);
  set("color", "[1;35m");
  set("auto_load", 1);
  set("enchase/apply_prop/damage", 13600);
  set("enchase/used", 1);
  set("enchase/flute", 1);
  set("set_data", 1);
  set("no_store", 1);
  set("item_owner", "kiss");
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
