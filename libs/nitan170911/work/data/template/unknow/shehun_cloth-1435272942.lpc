// shehun_cloth.c
// Create by TEMPLATE_D. Tue Nov 11 08:17:43 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit CLOTH;

#include <template/armor.h>
void create() {
  set_name("摄魂战衣[2;37;0m", ({ "shehun cloth" }));
  set_name("摄魂战衣[2;37;0m", ({ "shehun cloth" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一件摄魂战衣[2;37;0m(shehun cloth)。
");
    set("unit", "件");
    set("value", 200);
    set("material", "cloth");
    set("armor_prop", ([ /* sizeof() == 4 */
      "study_effect": 3,
      "armor": 550,
      "jing_recover": 40,
      "max_qi": 4500,
    ]));
  }

  set_weight(500);
  set("can_sign", 1);
  set("insert", ({ /* sizeof() == 1 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "max_potential": 50000,
        "avoid_poison": 1.500000,
        "practice_effect": 3,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("armor_type", "cloth");
  set("quality_level", 5);
  set("no_sell", 1);
  set("colorname", "[1;33m摄魂战衣[2;37;0m[2;37;0m");
  set("stable", 0);
  set("consistence", 100);
  set("color", "[1;33m");
  set("auto_load", 1);
  set("enchase/apply_prop/practice_effect", 3);
  set("enchase/apply_prop/max_potential", 50000);
  set("enchase/used", 1);
  set("enchase/flute", 1);
  set("set_data", 1);
  set("no_store", 1);
  set("item_owner", "lide");
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
