// taixuan_shigu_cloth.c
// Create by TEMPLATE_D. Sat Jan 25 13:04:56 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit CLOTH;

#include <template/armor.h>
void create() {
  set_name("太玄噬谷战衣[2;37;0m", ({ "taixuan shigu cloth" }));
  set_name("太玄噬谷战衣[2;37;0m", ({ "taixuan shigu cloth" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一件太玄噬谷战衣[2;37;0m(taixuan shigu cloth)。
");
    set("unit", "件");
    set("value", 200);
    set("material", "cloth");
    set("armor_prop", ([ /* sizeof() == 4 */
      "reduce_busy": 2.500000,
      "dp_power": 3.000000,
      "armor": 900,
      "avoid_forget": 2.500000,
    ]));
  }

  set_weight(500);
  set("can_sign", 1);
  set("insert", ({ /* sizeof() == 2 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 2 */
        "con": 3,
        "max_experience": 50000,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "str": 3,
        "int": 3,
        "dp_power": 1.500000,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("armor_type", "cloth");
  set("quality_level", 6);
  set("no_sell", 1);
  set("colorname", "[41;1m[1;37m太玄噬谷战衣[2;37;0m[2;37;0m");
  set("stable", 0);
  set("consistence", 100);
  set("color", "[41;1m[1;37m");
  set("auto_load", 1);
  set("enchase/apply_prop/max_experience", 50000);
  set("enchase/apply_prop/con", 3);
  set("enchase/apply_prop/int", 3);
  set("enchase/apply_prop/str", 3);
  set("enchase/used", 2);
  set("enchase/flute", 2);
  set("set_data", 1);
  set("no_store", 1);
  set("item_owner", "btmudc");
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
