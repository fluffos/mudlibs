// huiyue_blade.c
// Create by TEMPLATE_D. Sat Jan  4 10:57:17 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// axe.c
// 自动创建斧头的基准文件。                             /* COMMENT_EXIT  */

#include <weapon.h>

inherit BLADE;

#include <template/weapon.h>

void create() {
  set_name("彗月刀[2;37;0m", ({ "huiyue blade" }));
  set_name("彗月刀[2;37;0m", ({ "huiyue blade" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一柄彗月刀[2;37;0m(huiyue blade)。
");
    set("unit", "柄");
    set("value", 200);
    set("material", "steel");
    set("wield_msg", "$N「唰」的一声抽出一柄$n握在手中。
");
    set("unwield_msg", "$N将手中的$n收回刀鞘。
");
  }

  //**    $function_name($argument);                      /* FUNCTION_ENTRY */
  set_weight(5000);
  init_blade(5, 2); /* FUNCTION_EXIT  */ set("can_sign", 1);
  set("insert", ({ /* sizeof() == 4 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 1 */
        "study_effect": 3,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "str": 3,
        "through_armor": 2.000000,
        "kar": 3,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 2 */
        "con": 3,
        "avoid_dodge": 1.500000,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "study_effect": 3,
        "int": 3,
        "max_qi": 4500,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("rigidity", 1);
  set("quality_level", 6);
  set("no_sell", 1);
  set("colorname", "[41;1m[1;37m彗月刀[2;37;0m[2;37;0m");
  set("stable", 37);
  set("weapon_prop/damage", 4500);
  set("consistence", 100);
  set("color", "[41;1m[1;37m");
  set("auto_load", 1);
  set("enchase/apply_prop/max_qi", 4500);
  set("enchase/apply_prop/int", 3);
  set("enchase/apply_prop/kar", 3);
  set("enchase/apply_prop/con", 3);
  set("enchase/apply_prop/str", 3);
  set("enchase/apply_prop/study_effect", 6);
  set("enchase/used", 4);
  set("enchase/flute", 4);
  set("set_data", 1);
  set("flag", 6);
  set("verbs", ({ /* sizeof() == 3 */
    "slash",
    "slice",
    "hack"
  }));
  set("no_store", 1);
  set("item_owner", "btmudc");
  set("skill_type", "blade");
  setup();
}

int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
