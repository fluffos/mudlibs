// fengxing_head.c
// Create by TEMPLATE_D. Tue Oct 21 14:06:51 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit HEAD;

#include <template/armor.h>
void create() {
  set_name("风行头盔[2;37;0m", ({ "fengxing head" }));
  set_name("风行头盔[2;37;0m", ({ "fengxing head" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一件风行头盔[2;37;0m(fengxing head)。
");
    set("unit", "件");
    set("value", 200);
    set("material", "cloth");
    set("armor_prop", ([ /* sizeof() == 4 */
      "practice_effect": 3,
      "armor": 850,
      "max_qi": 4500,
      "derive_effect": 3,
    ]));
  }

  set_weight(500);
  set("can_sign", 1);
  set("insert", ({ /* sizeof() == 1 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "kar": 3,
        "neili_recover": 80,
        "max_qi": 4500,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("armor_type", "head");
  set("quality_level", 5);
  set("who_get/time", 1412660194);
  set("who_get/id", "money");
  set("no_sell", 1);
  set("colorname", "[1;33m风行头盔[2;37;0m[2;37;0m");
  set("stable", 0);
  set("consistence", 100);
  set("color", "[1;33m");
  set("auto_load", 1);
  set("enchase/apply_prop/max_qi", 4500);
  set("enchase/apply_prop/kar", 3);
  set("enchase/apply_prop/neili_recover", 80);
  set("enchase/used", 1);
  set("enchase/flute", 1);
  set("set_data", 1);
  set("no_store", 1);
  set("item_owner", "tangtang");
  setup();
}

int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
