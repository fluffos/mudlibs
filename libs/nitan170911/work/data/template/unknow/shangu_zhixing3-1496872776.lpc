// shangu_zhixing3.c
// Create by TEMPLATE_D. Sat Apr 12 23:32:02 2014.
#include <armor.h>
#include <ansi.h>

inherit NECK;

void create()
{
	set_name("[1;36m山谷之星[2;37;0m", ({ "shangu zhixing3" }));
        set_weight(500);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("unit", "串");
	set("value", 200000);
	set("material", "gold");
	set("armor_prop", ([ /* sizeof() == 3 */
  "str" : 4,
  "armor" : 200,
  "dex" : 4,
]));


	set("limit", ([ /* sizeof() == 3 */
  "int" : 30,
  "exp" : 1500000,
  "dex" : 30,
]));




        }
	set("armor_type", "neck");
	set("no_store", 1);
	set("item_owner", "zhu");
	set("no_sell", 1);
	set("stable", 25);
	set("consistence", 100);
        setup();
}        

int query_autoload()
{
        return 1;
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

