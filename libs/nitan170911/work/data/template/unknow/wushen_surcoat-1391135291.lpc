// wushen_surcoat.c
// Create by TEMPLATE_D. Sat Dec  7 03:38:59 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit SURCOAT;

#include <template/armor.h> 
void create()
{
	set_name("无神披风[2;37;0m", ({ "wushen surcoat" }));
	set_name("无神披风[2;37;0m", ({ "wushen surcoat" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件无神披风[2;37;0m(wushen surcoat)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 3 */
  "dodge" : 30,
  "armor" : 520,
  "defense" : 30,
]));
        }

        set_weight(500);
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 1 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 2 */
        "neili_recover" : 80,
        "dex" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("armor_type", "surcoat");
	set("quality_level", 4);
	set("who_get/time", 1386358657);
	set("who_get/id", "tnta");
	set("no_sell", 1);
	set("colorname", "[1;35m无神披风[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[1;35m");
	set("auto_load", 1);
	set("enchase/apply_prop/dex", 3);
	set("enchase/apply_prop/neili_recover", 80);
	set("enchase/used", 1);
	set("enchase/flute", 1);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "btmudb");
        setup();
}

int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

