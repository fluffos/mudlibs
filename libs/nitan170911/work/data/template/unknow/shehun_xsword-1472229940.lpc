// shehun_xsword.c
// Create by TEMPLATE_D. Tue Feb 23 23:12:10 2016.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// sword.c
// 自动创建剑的基准文件。                               /* COMMENT_EXIT  */

#include <weapon.h>

inherit XSWORD;

#include <template/weapon.h>

void create()
{
	set_name("摄魂萧[2;37;0m", ({ "shehun xsword" }));
	set_name("摄魂萧[2;37;0m", ({ "shehun xsword" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一柄摄魂萧[2;37;0m(shehun xsword)。
");
	set("unit", "柄");
	set("value", 200);
	set("material", "steel");
	set("wield_msg", "$N「唰」的一声抽出一柄$n握在手中。
");
	set("unwield_msg", "$N将手中的$n收回。
");
        }

//**    $function_name($argument);                      /* FUNCTION_ENTRY */
        set_weight(5000);
        init_xsword(5, 2);                              /* FUNCTION_EXIT  */
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 4 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "avoid_force" : 1.500000,
        "study_effect" : 3,
        "kar" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 1 */
        "str" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "attack" : 30,
        "avoid_force" : 1.500000,
        "learn_effect" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "attack" : 30,
        "add_poison" : 3,
        "add_blind" : 2.000000,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("quality_level", 6);
	set("who_get/time", 1456163252);
	set("who_get/id", "lovenun");
	set("no_sell", 1);
	set("colorname", "[41;1m[1;37m摄魂萧[2;37;0m[2;37;0m");
	set("stable", 37);
	set("consistence", 100);
	set("weapon_prop/damage", 6900);
	set("weapon_prop/research_effect", 3);
	set("actions", 0);
	set("color", "[41;1m[1;37m");
	set("auto_load", 1);
	set("enchase/apply_prop/add_poison", 3);
	set("enchase/apply_prop/learn_effect", 3);
	set("enchase/apply_prop/kar", 3);
	set("enchase/apply_prop/str", 3);
	set("enchase/apply_prop/study_effect", 3);
	set("enchase/apply_prop/attack", 60);
	set("enchase/used", 4);
	set("enchase/flute", 4);
	set("set_data", 1);
	set("flag", 6);
	set("verbs", ({ /* sizeof() == 3 */
  "slash",
  "slice",
  "thrust"
}));
	set("no_store", 1);
	set("item_owner", "dmsl");
	set("skill_type", "sword");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

