// mingshen_throwing.c
// Create by TEMPLATE_D. Wed Apr 30 23:43:18 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// axe.c
// 自动创建斧头的基准文件。                             /* COMMENT_EXIT  */

#include <weapon.h>

inherit THROWING;

#include <template/weapon.h>

void create()
{
	set_name("溟神暗器[2;37;0m", ({ "mingshen throwing" }));
	set_name("溟神暗器[2;37;0m", ({ "mingshen throwing" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一把五十个溟神暗器[2;37;0m(Mingshen throwing)。
");
	set("unit", "把");
	set("base_unit", "个");
	set("value", 2000);
	set("base_value", 40);
	set("base_weight", 100);
	set("material", "steel");
	set("wield_msg", "$N「唰」的一声抽出一把$n握在手中。
");
	set("unwield_msg", "$N将手中的$n藏于袖间。
");
        }

//**    $function_name($argument);                      /* FUNCTION_ENTRY */
        set_weight(5000);
        set_amount(50);
        init_throwing(5, 2);                            /* FUNCTION_EXIT  */
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 1 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 2 */
        "kar" : 3,
        "damage" : 39600,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("rigidity", 1);
	set("quality_level", 5);
	set("no_sell", 1);
	set("colorname", "[1;33m溟神暗器[2;37;0m[2;37;0m");
	set("weapon_prop/damage", 4750);
	set("weapon_prop/derive_effect", 3);
	set("weapon_prop/study_effect", 3);
	set("color", "[1;33m");
	set("auto_load", 1);
	set("enchase/apply_prop/damage", 39600);
	set("enchase/apply_prop/kar", 3);
	set("enchase/used", 1);
	set("enchase/flute", 1);
	set("set_data", 1);
	set("flag", 2);
	set("verbs", ({ /* sizeof() == 1 */
  "throw"
}));
	set("no_store", 1);
	set("item_owner", "xoxo");
	set("skill_type", "throwing");
        setup();
}

int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

