// fengren_whip.c
// Create by TEMPLATE_D. Thu Jan 30 18:34:30 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// axe.c
// 自动创建斧头的基准文件。                             /* COMMENT_EXIT  */

#include <weapon.h>

inherit WHIP;

#include <template/weapon.h>

void create() {
  set_name("风刃鞭[2;37;0m", ({ "fengren whip" }));
  set_name("风刃鞭[2;37;0m", ({ "fengren whip" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一条风刃鞭[2;37;0m(fengren whip)。
");
    set("unit", "条");
    set("value", 200);
    set("material", "steel");
    set("wield_msg", "$N「唰」的一声抽出一条$n握在手中。
");
    set("unwield_msg", "$N将手中的$n缠回腰间。
");
  }

  //**    $function_name($argument);                      /* FUNCTION_ENTRY */
  set_weight(5000);
  init_whip(5, 2); /* FUNCTION_EXIT  */ set("can_sign", 1);
  set("insert", ({ /* sizeof() == 3 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "learn_times": 15,
        "derive_times": 15,
        "damage": 39600,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "avoid_parry": 1.500000,
        "con": 3,
        "damage": 39600,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "max_jing": 2200,
        "through_armor": 2.000000,
        "learn_times": 15,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("rigidity", 1);
  set("quality_level", 5);
  set("no_sell", 1);
  set("colorname", "[1;33m风刃鞭[2;37;0m[2;37;0m");
  set("stable", 37);
  set("weapon_prop/damage", 4750);
  set("weapon_prop/max_neili", 10000);
  set("consistence", 100);
  set("color", "[1;33m");
  set("auto_load", 1);
  set("enchase/apply_prop/damage", 79200);
  set("enchase/apply_prop/con", 3);
  set("enchase/apply_prop/derive_times", 15);
  set("enchase/apply_prop/learn_times", 30);
  set("enchase/apply_prop/max_jing", 2200);
  set("enchase/used", 3);
  set("enchase/flute", 3);
  set("set_data", 1);
  set("flag", 2);
  set("verbs", ({ /* sizeof() == 1 */
    "whip"
  }));
  set("no_store", 1);
  set("item_owner", "kiss");
  set("skill_type", "whip");
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
