// jinsi_shoutao2.c
// Create by TEMPLATE_D. Sun Mar 24 01:12:52 2013.
// shoutao.c  手套

#include <armor.h>
#include <ansi.h>

inherit HANDS;

void create()
{
	set_name("[1;37m金丝手套[2;37;0m", ({ "jinsi shoutao2" }));
        set_weight(500);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("unit", "双");
	set("long", "[1;37m这是一双金丝铸造而成的手套，闪闪发亮。
[2;37;0m");
	set("value", 750000);
	set("material", "gold");
	set("armor_prop", ([ /* sizeof() == 2 */
  "armor" : 500,
  "unarmed_damage" : 700,
]));

	set("limit", ([ /* sizeof() == 2 */
  "int" : 55,
  "exp" : 2000000,
]));



        }
	set("armor_type", "hands");
	set("no_store", 1);
	set("item_owner", "spirit");
	set("no_sell", 1);
	set("stable", 25);
	set("consistence", 100);
        setup();
}

int query_autoload()
{
        return 1;
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
