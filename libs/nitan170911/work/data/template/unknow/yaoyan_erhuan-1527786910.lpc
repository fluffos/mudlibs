// yaoyan_erhuan.c
// Create by TEMPLATE_D. Sun Aug 23 23:15:45 2015.
#include <armor.h>
#include <ansi.h>

inherit EARRING;

void create() {
  set_name("[1;31m火麒麟·妖焰[2;37;0m", ({ "yaoyan erhuan" }));
  set_weight(100);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    set("unit", "副");
    set("long", "[1;31m你定睛一看，却无法辩识清楚，妖焰的火光闪耀刺眼，令人敬畏。
[2;37;0m");
    set("value", 200000);
    set("material", "gold");
    set("armor_prop", ([ /* sizeof() == 2 */
      "con": 40,
      "magic_find": 10,
    ]));

    set("limit", ([ /* sizeof() == 2 */
      "sex": 1,
      "exp": 10000000,
    ]));



  }
  set("armor_type", "earring");
  set("no_store", 1);
  set("item_owner", "rgthyj");
  set("no_sell", 1);
  set("stable", 25);
  set("consistence", 100);
  setup();
}

int query_autoload() {
  return 1;
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
