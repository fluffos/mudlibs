// zhuixing_club.c
// Create by TEMPLATE_D. Sat Oct 26 16:12:17 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// axe.c
// 自动创建斧头的基准文件。                             /* COMMENT_EXIT  */

#include <weapon.h>

inherit CLUB;

#include <template/weapon.h>

void create() {
  set_name("追星棍[2;37;0m", ({ "zhuixing club" }));
  set_name("追星棍[2;37;0m", ({ "zhuixing club" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一根追星棍[2;37;0m(zhuixing club)。
");
    set("unit", "根");
    set("value", 200);
    set("material", "steel");
    set("wield_msg", "$N「唰」的一声抽出一根$n握在手中。
");
    set("unwield_msg", "$N将手中的$n背到背上。
");
  }

  //**    $function_name($argument);                      /* FUNCTION_ENTRY */
  set_weight(5000);
  init_club(5, 2); /* FUNCTION_EXIT  */ set("can_sign", 1);
  set("insert", ({ /* sizeof() == 2 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 1 */
        "con": 3,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "avoid_parry": 1.500000,
        "add_poison": 3,
        "magic_find": 3,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("rigidity", 1);
  set("quality_level", 5);
  set("no_sell", 1);
  set("colorname", "[1;33m追星棍[2;37;0m[2;37;0m");
  set("stable", 37);
  set("weapon_prop/damage", 2750);
  set("weapon_prop/research_effect", 6);
  set("consistence", 100);
  set("color", "[1;33m");
  set("auto_load", 1);
  set("enchase/apply_prop/magic_find", 3);
  set("enchase/apply_prop/add_poison", 3);
  set("enchase/apply_prop/con", 3);
  set("enchase/used", 2);
  set("enchase/flute", 2);
  set("set_data", 1);
  set("flag", 18);
  set("verbs", ({ /* sizeof() == 3 */
    "bash",
    "crush",
    "slam"
  }));
  set("no_store", 1);
  set("item_owner", "sol");
  set("skill_type", "club");
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
