// lanxing_hands.c
// Create by TEMPLATE_D. Mon Apr 28 02:45:31 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit HANDS;

#include <template/armor.h>
void create()
{
	set_name("谰星掌套[2;37;0m", ({ "lanxing hands" }));
	set_name("谰星掌套[2;37;0m", ({ "lanxing hands" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件谰星掌套[2;37;0m(lanxing hands)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 5 */
  "attack" : 30,
  "add_damage" : 2.500000,
  "armor" : 10,
  "unarmed_damage" : 4750,
  "research_times" : 15,
]));

        }

        set_weight(500);
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 3 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 1 */
        "practice_effect" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "avoid_force" : 1.500000,
        "con" : 3,
        "study_times" : 15,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 1 */
        "avoid_force" : 1.500000,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("armor_type", "hands");
	set("quality_level", 5);
	set("no_sell", 1);
	set("colorname", "[1;33m谰星掌套[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[1;33m");
	set("auto_load", 1);
	set("enchase/apply_prop/study_times", 15);
	set("enchase/apply_prop/con", 3);
	set("enchase/apply_prop/practice_effect", 3);
	set("enchase/used", 3);
	set("enchase/flute", 3);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "laulj");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

