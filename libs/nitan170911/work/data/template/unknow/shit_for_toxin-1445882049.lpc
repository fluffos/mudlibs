// shit_for_toxin.c
// Create by TEMPLATE_D. Fri Jun 21 15:25:17 2013.
// gigiflower.c  ^_^

#include <armor.h>
#include <ansi.h>
inherit HEAD;
void create()
{
	set_name("[33m一大坨便便[2;37;0m", ({ "shit for toxin" }));
        set_weight(100);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/ {
	set("unit", "坨");
	set("long", "这是胡子长的[33m一大坨便便[2;37;0m。
");
	set("wear_msg", "[2;37;0m$N悄悄从身后掏出[33m一大坨便便[2;37;0m顶在头上。
[2;37;0m");
	set("remove_msg", "[2;37;0m$N轻轻地把头上[33m一大坨便便[2;37;0m又塞进裤裆里。
[2;37;0m");
	set("value", 0);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 1 */
  "armor" : 1,
]));
        }
	set("enchase", ([ /* sizeof() == 1 */
  "apply_prop" : ([ /* sizeof() == 2 */
      "per" : -25,
      "int" : -25,
    ]),
]));






	set("armor_type", "head");
	set("no_store", 1);
	set("item_owner", "toxin");
	set("no_sell", 1);
	set("stable", 0);
	set("consistence", 100);
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

