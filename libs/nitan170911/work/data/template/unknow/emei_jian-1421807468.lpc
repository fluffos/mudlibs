// emei_jian.c
// Create by TEMPLATE_D. Sun Jun  1 23:24:28 2014.
#include <weapon.h>
#include <ansi.h>
#include <armor.h> 
inherit SWORD;

void create()
{
	set_name("[2;37;0m[36m娥眉剑[2;37;0m", ({ "emei jian" }));
        set_weight(1000);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/ {
	set("unit", "柄");
	set("long", "这是一柄精金铁铸造的细剑，蜀山女弟子所配，剑刃锋利冷寒。
");
	set("value", 3000);
	set("material", "steel");
	set("wield_msg", "$N「唰」的一声抽出一柄$n握在手中。
");
	set("unwield_msg", "$N将手中的$n插回剑鞘。
");
        }
	set("weapon_prop", ([ /* sizeof() == 7 */
  "avoid_force" : 8,
  "avoid_attack" : 8,
  "avoid_parry" : 8,
  "avoid_dodge" : 8,
  "fatal_blow" : 24,
  "add_forget" : 3,
  "damage" : 1200,
]));







        init_sword(1200);
	set("rigidity", 300);
	set("flag", 6);
	set("verbs", ({ /* sizeof() == 3 */
  "slash",
  "slice",
  "thrust"
}));
	set("no_store", 1);
	set("item_owner", "juan");
	set("no_sell", 1);
	set("stable", 37);
	set("consistence", 100);
	set("skill_type", "sword");
        setup();
}


int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

