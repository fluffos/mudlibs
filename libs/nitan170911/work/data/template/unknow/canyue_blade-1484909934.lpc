// canyue_blade.c
// Create by TEMPLATE_D. Wed Dec 11 01:08:27 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// axe.c
// 自动创建斧头的基准文件。                             /* COMMENT_EXIT  */

#include <weapon.h>

inherit BLADE;

#include <template/weapon.h>

void create()
{
	set_name("残月刀[2;37;0m", ({ "canyue blade" }));
	set_name("残月刀[2;37;0m", ({ "canyue blade" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一柄残月刀[2;37;0m(canyue blade)。
");
	set("unit", "柄");
	set("value", 200);
	set("material", "steel");
	set("wield_msg", "$N「唰」的一声抽出一柄$n握在手中。
");
	set("unwield_msg", "$N将手中的$n收回刀鞘。
");
        }

//**    $function_name($argument);                      /* FUNCTION_ENTRY */
        set_weight(5000);
        init_blade(5, 2);                               /* FUNCTION_EXIT  */
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 2 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "max_potential" : 50000,
        "da_power" : 2.000000,
        "damage" : 24600,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "attack" : 30,
        "str" : 3,
        "con" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("rigidity", 1);
	set("quality_level", 4);
	set("who_get/time", 1386649505);
	set("who_get/id", "hsa");
	set("no_sell", 1);
	set("colorname", "[1;35m残月刀[2;37;0m[2;37;0m");
	set("stable", 37);
	set("consistence", 100);
	set("weapon_prop/damage", 3000);
	set("weapon_prop/dex", 3);
	set("actions", 0);
	set("color", "[1;35m");
	set("auto_load", 1);
	set("enchase/apply_prop/damage", 24600);
	set("enchase/apply_prop/con", 3);
	set("enchase/apply_prop/str", 3);
	set("enchase/apply_prop/max_potential", 50000);
	set("enchase/apply_prop/attack", 30);
	set("enchase/used", 2);
	set("enchase/flute", 2);
	set("set_data", 1);
	set("flag", 6);
	set("verbs", ({ /* sizeof() == 3 */
  "slash",
  "slice",
  "hack"
}));
	set("no_store", 1);
	set("item_owner", "allun");
	set("skill_type", "blade");
        setup();
}

int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

