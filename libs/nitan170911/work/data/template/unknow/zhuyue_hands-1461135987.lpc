// zhuyue_hands.c
// Create by TEMPLATE_D. Mon Nov  2 12:18:55 2015.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit HANDS;

#include <template/armor.h>
void create()
{
	set_name("逐月掌套[2;37;0m", ({ "zhuyue hands" }));
	set_name("逐月掌套[2;37;0m", ({ "zhuyue hands" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件逐月掌套[2;37;0m(zhuyue hands)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 3 */
  "armor" : 10,
  "avoid_dodge" : 1.500000,
  "unarmed_damage" : 1650,
]));

        }

        set_weight(500);
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 1 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "research_effect" : 3,
        "research_times" : 15,
        "study_times" : 15,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("armor_type", "hands");
	set("quality_level", 3);
	set("no_sell", 1);
	set("colorname", "[1;31m逐月掌套[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[1;31m");
	set("auto_load", 1);
	set("enchase/apply_prop/research_times", 15);
	set("enchase/apply_prop/study_times", 15);
	set("enchase/apply_prop/research_effect", 3);
	set("enchase/used", 1);
	set("enchase/flute", 1);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "myjobs");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

