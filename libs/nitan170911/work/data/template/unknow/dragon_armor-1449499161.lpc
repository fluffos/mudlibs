// dragon_armor.c
// Create by TEMPLATE_D. Mon Jul  1 00:03:46 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit ARMOR;

#include <template/armor.h>
void create() {
  set_name("[1;36;42m龙神鳞甲[2;37;0m[2;37;0m", ({ "dragon armor" }));
  set_name("[1;36;42m龙神鳞甲[2;37;0m[2;37;0m", ({ "dragon armor" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一件普通的护甲。
");
    set("unit", "件");
    set("value", 10000);
    set("material", "cloth");
    set("armor_prop", ([ /* sizeof() == 1 */
      "armor": 200,
    ]));
  }

  set_weight(500);
  set("insert", ({ /* sizeof() == 2 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 2 */
        "str": 2,
        "armor1": 60,
      ]),
      "name": "玄石[2;37;0m[2;37;0m[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 2 */
        "con": 2,
        "force": 2,
      ]),
      "name": "玄石[2;37;0m[2;37;0m[2;37;0m",
    ])
  }));
  set("armor_type", "armor");
  set("quality_level", 3);
  set("no_sell", 1);
  set("stable", 0);
  set("consistence", 100);
  set("require/con", 30);
  set("require/level", 20);
  set("enchase/apply_prop/force", 2);
  set("enchase/apply_prop/con", 2);
  set("enchase/apply_prop/armor1", 60);
  set("enchase/apply_prop/str", 2);
  set("enchase/used", 2);
  set("enchase/flute", 2);
  set("mod_mark", "A3001");
  set("set_data", 1);
  set("mod_name", "[1;36;42m龙之神力[2;37;0m[2;37;0m");
  set("mod_level", "M3000");
  set("no_store", 1);
  set("item_owner", "longfy");
  setup();
}

int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
