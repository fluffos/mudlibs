// vip_xunzhang.c
// Create by TEMPLATE_D. Fri Aug 15 23:09:14 2014.
#include <armor.h>
#include <ansi.h>

int is_xunzhang() { return 1; }
inherit MEDAL;

void create()
{
	set_name("[1;33mVIP勋章[2;37;0m", ({ "vip xunzhang" }));
	set("long", "[1;37m为泥潭做出重大贡献而获得的勋章，非常珍贵，乃睿者的象征。
[2;37;0m");
        set_weight(500);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("unit", "枚");
	set("value", 100000);
	set("material", "gold");
	set("armor_prop", ([ /* sizeof() == 4 */
  "practice_times" : 300,
  "learn_times" : 300,
  "derive_times" : 300,
  "research_times" : 300,
]));



        }
	set("enchase", ([ /* sizeof() == 1 */
  "SN" : "hy1",
]));
	set("bindable", 1);
	set("auto_load", 1);
	set("armor_type", "medal");
	set("no_store", 1);
	set("item_owner", "uniax");
	set("no_sell", 1);
	set("stable", 25);
	set("consistence", 100);
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

