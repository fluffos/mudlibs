// xingtian_ring.c
// Create by TEMPLATE_D. Wed Sep 11 16:26:54 2013.
// This program is a part of NT MudLIB 

#include <ansi.h>
#include <armor.h>
inherit RINGS;

void create() {
  set_name("[42;1m刑天戒指[2;37;0m", ({ "xingtian ring" }));
  set_weight(500);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    set("unit", "个");
    set("long", "[1;33m这便是刑天戴过的戒指，通体血光滔天。
[2;37;0m");
    set("value", 3000);
    set("no_sell", 1);
    set("rigidity", 8000);
    set("material", "tian jing");
    set("no_pawn", 1);
    set("armor_prop", ([ /* sizeof() == 1 */
      "armor": 10,
    ]));
  }

  set("mod_mark", "A2031");
  set("mod_level", "M2030");
  set("mod_name", "[41;1m刑天套装[2;37;0m");
  set("quality_level", 3);

  set("armor_type", "rings");
  set("no_store", 1);
  set("item_owner", "xueer");
  set("stable", 50);
  set("consistence", 100);
  setup();
}

int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
