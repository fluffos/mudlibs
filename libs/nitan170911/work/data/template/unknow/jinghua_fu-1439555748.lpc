// jinghua_fu.c
// Create by TEMPLATE_D. Sun Feb 15 04:26:54 2015.
#include <armor.h>
#include <ansi.h>

inherit MYHEART2;

void create()
{
	set_name("[1;31m火麒麟·净化[2;37;0m", ({ "jinghua fu" }));
        set_weight(1);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("material", "paper");
	set("unit", "张");
	set("long", "[1;31m这是一张火红色的灵符，火焰中隐约浮现出「净化」二字。
[2;37;0m");
	set("value", 10000000);
	set("limit", ([ /* sizeof() == 1 */
  "exp" : 50000000,
]));


	set("armor_prop", ([ /* sizeof() == 3 */
  "int" : 40,
  "qy" : 3,
  "fy" : 3,
]));



        }
	set("no_repair", "这东西我可没法修理。
");
	set("armor_type", "myheart2");
	set("no_store", 1);
	set("item_owner", "lucy");
	set("no_sell", 1);
	set("stable", 1);
	set("consistence", 100);
        setup();
}

int query_autoload()
{
        return 1;
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

