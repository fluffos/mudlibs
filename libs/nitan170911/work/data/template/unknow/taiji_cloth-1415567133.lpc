// taiji_cloth.c
// Create by TEMPLATE_D. Mon Dec 23 21:52:09 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit CLOTH;

#include <template/armor.h>
void create()
{
	set_name("太极战衣[2;37;0m", ({ "taiji cloth" }));
	set_name("太极战衣[2;37;0m", ({ "taiji cloth" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件太极战衣[2;37;0m(taiji cloth)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 3 */
  "research_effect" : 9,
  "armor" : 900,
  "derive_effect" : 3,
]));
        }

        set_weight(500);
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 2 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 2 */
        "research_effect" : 3,
        "dp_power" : 1.500000,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 1 */
        "qi_recover" : 40,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("armor_type", "cloth");
	set("quality_level", 6);
	set("no_sell", 1);
	set("colorname", "[41;1m[1;37m太极战衣[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[41;1m[1;37m");
	set("auto_load", 1);
	set("enchase/apply_prop/qi_recover", 40);
	set("enchase/apply_prop/research_effect", 3);
	set("enchase/used", 2);
	set("enchase/flute", 2);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "btmudc");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

