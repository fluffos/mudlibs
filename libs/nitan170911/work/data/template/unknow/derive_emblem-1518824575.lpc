// derive_emblem.c
// Create by TEMPLATE_D. Sun Oct 11 20:14:44 2015.
#include <armor.h>
#include <ansi.h>

int is_xunzhang() { return 1; }
inherit MEDAL;

void create()
{
	set_name("[35m紫金汲取勋章[2;37;0m", ({ "derive emblem" }));
        set_weight(3000);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("long", "[35m带在身上可以提升300点汲取速度的效果。
[2;37;0m");
	set("unit", "块");
	set("can_summon", 1);
	set("value", 500000);
	set("material", "gold");
	set("armor_prop", ([ /* sizeof() == 1 */
  "derive_times" : 300,
]));
        }
	set("enchase", ([ /* sizeof() == 1 */
  "SN" : "jq1",
]));
	set("auto_load", 1);
	set("armor_type", "medal");
	set("no_sell", 1);
	set("stable", 25);
	set("consistence", 100);
	set("no_store", 1);
	set("item_owner", "yinxi");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

