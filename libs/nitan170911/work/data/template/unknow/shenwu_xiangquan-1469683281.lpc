// shenwu_xiangquan.c
// Create by TEMPLATE_D. Sat Nov 15 15:39:29 2014.
// necklace.c 金项链

#include <armor.h>
#include <ansi.h>

inherit NECK;

void create() {
  set_name("[1;33m神武项圈[2;37;0m", ({ "shenwu xiangquan" }));
  set_weight(500);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    set("unit", "串");
    set("value", 200000);
    set("material", "gold");
    set("armor_prop", ([ /* sizeof() == 1 */
      "armor": 300,
    ]));
  }
  set("armor_type", "neck");
  set("no_store", 1);
  set("item_owner", "changanwd");
  set("no_sell", 1);
  set("stable", 25);
  set("consistence", 100);
  setup();
}

int query_autoload() {
  return 1;
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
