// wisdom_boots.c
// Create by TEMPLATE_D. Thu Apr 11 15:00:55 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit BOOTS;

#include <template/armor.h>
void create() {
  set_name("[42;1m文曲星靴[2;37;0m[2;37;0m", ({ "wisdom boots" }));
  set_name("[42;1m文曲星靴[2;37;0m[2;37;0m", ({ "wisdom boots" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "蕴含了文曲星的精元和无上智慧(神)。[2;37;0m
");
    set("unit", "件");
    set("value", 2000000);
    set("material", "cloth");
    set("armor_prop", ([ /* sizeof() == 1 */
      "armor": 10,
    ]));
  }

  set_weight(500);
  set("insert", ({ /* sizeof() == 2 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 2 */
        "armor1": 30,
        "int": 8,
      ]),
      "name": "[1;35m玄石[2;37;0m[2;37;0m[2;37;0m[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 2 */
        "learn_effect": 10,
        "practice_effect": 10,
      ]),
      "name": "[1;35m玄石[2;37;0m[2;37;0m[2;37;0m[2;37;0m",
    ])
  }));
  set("armor_type", "boots");
  set("quality_level", 2);
  set("no_sell", 1);
  set("stable", 0);
  set("consistence", 100);
  set("require/con", 20);
  set("require/level", 10);
  set("enchase/apply_prop/practice_effect", 10);
  set("enchase/apply_prop/int", 8);
  set("enchase/apply_prop/armor1", 30);
  set("enchase/apply_prop/learn_effect", 10);
  set("enchase/used", 2);
  set("enchase/flute", 2);
  set("mod_mark", "A2004");
  set("set_data", 1);
  set("mod_name", "[42;1m文曲星学习套装(神)[2;37;0m[2;37;0m");
  set("mod_level", "M2000");
  set("no_store", 1);
  set("item_owner", "xtf");
  setup();
}

int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
