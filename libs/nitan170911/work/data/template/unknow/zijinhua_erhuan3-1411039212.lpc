// zijinhua_erhuan3.c
// Create by TEMPLATE_D. Fri Oct 25 00:08:08 2013.
#include <armor.h>
#include <ansi.h>

inherit EARRING;

void create()
{
	set_name("[1;31m紫金花耳环[2;37;0m", ({ "zijinhua erhuan3" }));
        set_weight(100);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("unit", "副");
	set("value", 200000);
	set("material", "silver");
	set("armor_prop", ([ /* sizeof() == 2 */
  "armor" : 1,
  "dex" : 10,
]));

	set("limit", ([ /* sizeof() == 1 */
  "sex" : 1,
]));


        }
	set("armor_type", "earring");
	set("no_store", 1);
	set("item_owner", "sol");
	set("no_sell", 1);
	set("stable", 20);
	set("consistence", 100);
        setup();
}        

int query_autoload()
{
        return 1;
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

