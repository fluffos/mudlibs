// yecha_fu.c
// Create by TEMPLATE_D. Thu Jun  6 23:36:28 2013.
#include <armor.h>
#include <ansi.h>

inherit MYHEART;

void create() {
  set_name("[1;31m夜叉[2;37;0m", ({ "yecha fu" }));
  set_weight(1);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    set("material", "paper");
    set("unit", "张");
    set("long", "[1;31m这是一张道符，上面画着一个奇特的鬼怪，名曰：夜叉。
[2;37;0m");
    set("value", 95000);
    set("limit", ([ /* sizeof() == 3 */
      "str": 60,
      "con": 50,
      "exp": 2000000,
    ]));




    set("apply_prop", ([ /* sizeof() == 1 */
      "damage": 100,
    ]));
    set("armor_prop", ([ /* sizeof() == 3 */
      "str": 2,
      "int": 2,
      "unarmed_damage": 100,
    ]));


  }
  set("no_repair", "这东西我可没法修理。
");
  set("armor_type", "myheart");
  set("no_store", 1);
  set("item_owner", "dlinwy");
  set("who_get/time", 1370532884);
  set("who_get/id", "dlinwy");
  set("no_sell", 1);
  set("stable", 1);
  set("consistence", 100);
  setup();
}

int query_autoload() {
  return 1;
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
