// huanwu_surcoat.c
// Create by TEMPLATE_D. Sun Aug 10 16:38:59 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit SURCOAT;

#include <template/armor.h> 
void create()
{
	set_name("幻舞披风[2;37;0m", ({ "huanwu surcoat" }));
	set_name("幻舞披风[2;37;0m", ({ "huanwu surcoat" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件幻舞披风[2;37;0m(huanwu surcoat)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 4 */
  "parry" : 30,
  "avoid_blind" : 1.500000,
  "dp_power" : 1.500000,
  "armor" : 750,
]));
        }

        set_weight(500);
	set("can_sign", 1);
	set("armor_type", "surcoat");
	set("quality_level", 5);
	set("no_sell", 1);
	set("colorname", "[1;33m幻舞披风[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[1;33m");
	set("auto_load", 1);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "clrak");
        setup();
}

int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

