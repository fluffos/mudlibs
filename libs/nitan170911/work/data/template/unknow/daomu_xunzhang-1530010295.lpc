// daomu_xunzhang.c
// Create by TEMPLATE_D. Fri Jul  8 16:04:29 2016.
#include <armor.h>
#include <ansi.h>

int is_xunzhang() { return 1; }
inherit MEDAL;

void create() {
  set_name("[1;31m盗墓[1;33m勋章[2;37;0m", ({ "daomu xunzhang" }));
  set("long", "[1;32m盗墓勋章，非常珍贵，可免疫秦皇陵墓里怪物的busy特技。
[2;37;0m");
  set_weight(500);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    set("unit", "枚");
    set("value", 200000);
    set("material", "bronze");
    set("armor_prop", ([ /* sizeof() == 6 */
      "str": 6,
      "con": 6,
      "dex": 6,
      "int": 6,
      "daomu": 1,
      "damage": 200,
    ]));

    set("special", ([ /* sizeof() == 1 */
      "desc": "[1;31m佩带后可免疫秦皇陵墓里怪物的busy特技。[2;37;0m",
    ]));





  }
  set("enchase", ([ /* sizeof() == 1 */
    "SN": "dmxz",
  ]));
  set("armor_type", "medal");
  set("no_store", 1);
  set("item_owner", "baishou");
  set("no_sell", 1);
  set("stable", 50);
  set("consistence", 100);
  setup();
}

int query_autoload() {
  return 1;
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
