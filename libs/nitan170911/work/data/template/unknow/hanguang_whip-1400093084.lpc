// hanguang_whip.c
// Create by TEMPLATE_D. Fri Dec 13 14:33:39 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// axe.c
// 自动创建斧头的基准文件。                             /* COMMENT_EXIT  */

#include <weapon.h>

inherit WHIP;

#include <template/weapon.h>

void create() {
  set_name("寒光鞭[2;37;0m", ({ "hanguang whip" }));
  set_name("寒光鞭[2;37;0m", ({ "hanguang whip" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一条寒光鞭[2;37;0m(hanguang whip)。
");
    set("unit", "条");
    set("value", 200);
    set("material", "steel");
    set("wield_msg", "$N「唰」的一声抽出一条$n握在手中。
");
    set("unwield_msg", "$N将手中的$n缠回腰间。
");
  }

  //**    $function_name($argument);                      /* FUNCTION_ENTRY */
  set_weight(5000);
  init_whip(5, 2); /* FUNCTION_EXIT  */ set("can_sign", 1);
  set("insert", ({ /* sizeof() == 3 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "double_damage": 2.000000,
        "research_effect": 3,
        "derive_times": 15,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "attack": 30,
        "int": 3,
        "add_poison": 3,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 2 */
        "magic_find": 3,
        "max_experience": 50000,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("rigidity", 1);
  set("quality_level", 6);
  set("no_sell", 1);
  set("colorname", "[41;1m[1;37m寒光鞭[2;37;0m[2;37;0m");
  set("stable", 37);
  set("weapon_prop/damage", 4500);
  set("consistence", 100);
  set("color", "[41;1m[1;37m");
  set("auto_load", 1);
  set("enchase/apply_prop/magic_find", 3);
  set("enchase/apply_prop/max_experience", 50000);
  set("enchase/apply_prop/add_poison", 3);
  set("enchase/apply_prop/derive_times", 15);
  set("enchase/apply_prop/int", 3);
  set("enchase/apply_prop/research_effect", 3);
  set("enchase/apply_prop/attack", 30);
  set("enchase/used", 3);
  set("enchase/flute", 3);
  set("set_data", 1);
  set("flag", 2);
  set("verbs", ({ /* sizeof() == 1 */
    "whip"
  }));
  set("no_store", 1);
  set("item_owner", "reader");
  set("skill_type", "whip");
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
