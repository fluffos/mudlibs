// shehun_wrists.c
// Create by TEMPLATE_D. Tue Dec  3 00:43:03 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit WRISTS;

#include <template/armor.h>
void create() {
  set_name("摄魂护腕[2;37;0m", ({ "shehun wrists" }));
  set_name("摄魂护腕[2;37;0m", ({ "shehun wrists" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一件摄魂护腕[2;37;0m(shehun wrists)。
");
    set("unit", "件");
    set("value", 200);
    set("material", "cloth");
    set("armor_prop", ([ /* sizeof() == 4 */
      "research_effect": 3,
      "avoid_blind": 3.000000,
      "dp_power": 1.500000,
      "armor": 900,
    ]));
  }

  set_weight(500);
  set("can_sign", 1);
  set("armor_type", "wrists");
  set("quality_level", 6);
  set("no_sell", 1);
  set("colorname", "[41;1m[1;37m摄魂护腕[2;37;0m[2;37;0m");
  set("stable", 0);
  set("consistence", 100);
  set("color", "[41;1m[1;37m");
  set("auto_load", 1);
  set("set_data", 1);
  set("no_store", 1);
  set("item_owner", "reader");
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
