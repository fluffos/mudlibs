// lengyue_guan.c
// Create by TEMPLATE_D. Mon Feb  9 17:25:42 2015.
#include <armor.h>
#include <ansi.h>

inherit HEAD;

void create() {
  set_name("[1;33m天降·冷月冠[2;37;0m", ({ "lengyue guan" }));
  set_weight(100);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    set("unit", "顶");
    set("long", "[1;33m传说乃月宫宝物，蕴涵着非凡之神力。
[2;37;0m");
    set("value", 1500000);
    set("armor_prop", ([ /* sizeof() == 5 */
      "blade": 100,
      "str": 30,
      "con": 30,
      "dex": 30,
      "damage": 3000,
    ]));




    set("special", ([ /* sizeof() == 1 */
      "desc": "有效刀法等级提高100级。",
    ]));
    set("limit", ([ /* sizeof() == 2 */
      "scborn": 1,
      "exp": 100000000,
    ]));





  }
  set("armor_type", "head");
  set("no_store", 1);
  set("item_owner", "hua");
  set("no_sell", 1);
  set("stable", 50);
  set("consistence", 100);
  setup();
}

int query_autoload() {
  return 1;
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
