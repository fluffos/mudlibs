// longpao.c
// Create by TEMPLATE_D. Thu Apr  4 00:45:00 2013.
// longpao.c

#include <ansi.h>
#include <armor.h>
inherit CLOTH;

void create()
{
	set_name("[1;31m大红[2;37;0m[1;33m金边[2;37;0m[1;31m滚龙袍[2;37;0m", ({ "longpao" }));
	set("long", "这是一件崭新大红金边滚龙袍，是新郎的专用礼服。
");
        set_weight(2000);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/ {
	set("material", "cloth");
	set("unit", "件");
	set("armor_prop", ([ /* sizeof() == 1 */
  "armor" : 100,
]));
	set("male_only", 1);
        }
	set("armor_type", "cloth");
	set("no_store", 1);
	set("item_owner", "dcgz");
	set("no_sell", 1);
	set("stable", 0);
	set("consistence", 100);
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

