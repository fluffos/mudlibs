// fenghuang_chai3.c
// Create by TEMPLATE_D. Tue May 21 16:28:09 2013.
#include <armor.h>
#include <ansi.h>

inherit HAIRPIN;

void create() {
  set_name("[1;31m凤凰金钗[2;37;0m", ({ "fenghuang chai3" }));
  set_weight(500);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    set("unit", "只");
    set("value", 200000);
    set("material", "gold");
    set("armor_prop", ([ /* sizeof() == 3 */
      "int": 3,
      "con": 5,
      "armor": 1,
    ]));


    set("limit", ([ /* sizeof() == 1 */
      "sex": 1,
    ]));


  }
  set("armor_type", "hairpin");
  set("no_store", 1);
  set("item_owner", "lurong");
  set("no_sell", 1);
  set("stable", 25);
  set("consistence", 100);
  setup();
}

int query_autoload() {
  return 1;
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
