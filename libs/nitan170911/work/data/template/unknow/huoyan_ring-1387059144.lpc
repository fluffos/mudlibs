// huoyan_ring.c
// Create by TEMPLATE_D. Mon Dec  2 13:13:24 2013.
#include <armor.h>
#include <ansi.h>

inherit RINGS;

void create() {
  set_name("[1;31m火眼之戒[2;37;0m", ({ "huoyan ring" }));
  set_weight(500);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    set("unit", "枚");
    set("long", "[1;31m由火麒麟的精魄幻化而成的戒指，闪动着火焰的光芒，令人神往。
[2;37;0m");
    set("value", 20000000);
    set("material", "gold");
    set("armor_prop", ([ /* sizeof() == 5 */
      "str": 20,
      "int": 20,
      "con": 20,
      "dex": 20,
      "damage": 2000,
    ]));





    set("limit", ([ /* sizeof() == 2 */
      "scborn": 1,
      "exp": 100000000,
    ]));




  }
  set("armor_type", "rings");
  set("no_store", 1);
  set("item_owner", "toxin");
  set("no_sell", 1);
  set("stable", 25);
  set("consistence", 100);
  setup();
}

int query_autoload() {
  return 1;
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
