// kuangfeng_xue3.c
// Create by TEMPLATE_D. Sat Mar 26 13:45:28 2016.
#include <ansi.h>
#include <armor.h>

inherit BOOTS;

void create()
{
	set_name("[1;37m天降·狂风靴[2;37;0m", ({ "kuangfeng xue3" }));
        set_weight(10);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("unit", "双");
	set("long", "[1;37m这是一双西域银狐之皮制成的靴子，据说穿上后行走如狂风。
");
	set("value", 2000000);

	set("armor_prop", ([ /* sizeof() == 2 */
  "dodge" : 300,
  "dex" : 60,
]));

	set("special", ([ /* sizeof() == 1 */
  "desc" : "有效轻功等级提高300级。",
]));
	set("material", "boots");
	set("limit", ([ /* sizeof() == 2 */
  "scborn" : 1,
  "exp" : 100000000,
]));



        }
	set("armor_type", "boots");
	set("no_store", 1);
	set("item_owner", "kokoy");
	set("no_sell", 1);
	set("stable", 50);
	set("consistence", 100);
        setup();
}

int query_autoload()
{
        return 1;
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

