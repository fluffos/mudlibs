// hanyue_qingyi_whip.c
// Create by TEMPLATE_D. Sat Dec  7 03:13:04 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// axe.c
// 自动创建斧头的基准文件。                             /* COMMENT_EXIT  */

#include <weapon.h>

inherit WHIP;

#include <template/weapon.h>

void create() {
  set_name("寒月情义鞭[2;37;0m", ({ "hanyue qingyi whip" }));
  set_name("寒月情义鞭[2;37;0m", ({ "hanyue qingyi whip" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一条寒月情义鞭[2;37;0m(hanyue qingyi whip)。
");
    set("unit", "条");
    set("value", 200);
    set("material", "steel");
    set("wield_msg", "$N「唰」的一声抽出一条$n握在手中。
");
    set("unwield_msg", "$N将手中的$n缠回腰间。
");
  }

  //**    $function_name($argument);                      /* FUNCTION_ENTRY */
  set_weight(5000);
  init_whip(5, 2); /* FUNCTION_EXIT  */ set("can_sign", 1);
  set("rigidity", 1);
  set("quality_level", 4);
  set("no_sell", 1);
  set("colorname", "[1;35m寒月情义鞭[2;37;0m[2;37;0m");
  set("stable", 37);
  set("weapon_prop/damage", 2200);
  set("consistence", 100);
  set("actions", 0);
  set("color", "[1;35m");
  set("auto_load", 1);
  set("set_data", 1);
  set("flag", 2);
  set("verbs", ({ /* sizeof() == 1 */
    "whip"
  }));
  set("no_store", 1);
  set("item_owner", "btmudb");
  set("skill_type", "whip");
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
