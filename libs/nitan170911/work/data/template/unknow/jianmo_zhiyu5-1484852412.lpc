// jianmo_zhiyu5.c
// Create by TEMPLATE_D. Sat Mar 26 13:45:00 2016.
#include <armor.h>
#include <ansi.h>

inherit HEAD;

void create() {
  set_name("[1;33m剑魔之羽[2;37;0m", ({ "jianmo zhiyu5" }));
  set_weight(1);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    set("unit", "件");
    set("long", "[1;33m这是由独孤求败取神雕羽毛精心制作而成的头饰，其中似乎蕴涵了高深的武学真谛。
[2;37;0m");
    set("value", 1500000);
    set("armor_prop", ([ /* sizeof() == 5 */
      "str": 40,
      "sword": 240,
      "con": 40,
      "dex": 40,
      "damage": 6000,
    ]));




    set("special", ([ /* sizeof() == 1 */
      "desc": "有效剑法等级提高240级。",
    ]));
    set("limit", ([ /* sizeof() == 2 */
      "scborn": 1,
      "exp": 100000000,
    ]));




  }
  set("armor_type", "head");
  set("no_store", 1);
  set("item_owner", "kokoy");
  set("no_sell", 1);
  set("stable", 50);
  set("consistence", 100);
  setup();
}

int query_autoload() {
  return 1;
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
