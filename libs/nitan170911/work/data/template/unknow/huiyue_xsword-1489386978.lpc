// huiyue_xsword.c
// Create by TEMPLATE_D. Sat Feb  1 19:36:47 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// sword.c
// 自动创建剑的基准文件。                               /* COMMENT_EXIT  */

#include <weapon.h>

inherit XSWORD;

#include <template/weapon.h>

void create()
{
	set_name("彗月萧[2;37;0m", ({ "huiyue xsword" }));
	set_name("彗月萧[2;37;0m", ({ "huiyue xsword" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一柄彗月萧[2;37;0m(huiyue xsword)。
");
	set("unit", "柄");
	set("value", 200);
	set("material", "steel");
	set("wield_msg", "$N「唰」的一声抽出一柄$n握在手中。
");
	set("unwield_msg", "$N将手中的$n收回。
");
        }

//**    $function_name($argument);                      /* FUNCTION_ENTRY */
        set_weight(5000);
        init_xsword(5, 2);                              /* FUNCTION_EXIT  */
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 3 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 1 */
        "int" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 2 */
        "max_neili" : 10000,
        "study_times" : 15,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 2 */
        "ap_power" : 2.000000,
        "damage" : 39600,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("quality_level", 5);
	set("no_sell", 1);
	set("colorname", "[1;33m彗月萧[2;37;0m[2;37;0m");
	set("stable", 37);
	set("weapon_prop/damage", 4750);
	set("weapon_prop/max_experience", 50000);
	set("weapon_prop/learn_effect", 3);
	set("consistence", 100);
	set("color", "[1;33m");
	set("auto_load", 1);
	set("enchase/apply_prop/damage", 39600);
	set("enchase/apply_prop/study_times", 15);
	set("enchase/apply_prop/int", 3);
	set("enchase/apply_prop/max_neili", 10000);
	set("enchase/used", 3);
	set("enchase/flute", 3);
	set("set_data", 1);
	set("flag", 6);
	set("verbs", ({ /* sizeof() == 3 */
  "slash",
  "slice",
  "thrust"
}));
	set("no_store", 1);
	set("item_owner", "seco");
	set("skill_type", "sword");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

