// youming_xsword.c
// Create by TEMPLATE_D. Sat Oct 26 16:03:29 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// sword.c
// 自动创建剑的基准文件。                               /* COMMENT_EXIT  */

#include <weapon.h>

inherit XSWORD;

#include <template/weapon.h>

void create()
{
	set_name("幽明萧[2;37;0m", ({ "youming xsword" }));
	set_name("幽明萧[2;37;0m", ({ "youming xsword" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一柄幽明萧[2;37;0m(youming xsword)。
");
	set("unit", "柄");
	set("value", 200);
	set("material", "steel");
	set("wield_msg", "$N「唰」的一声抽出一柄$n握在手中。
");
	set("unwield_msg", "$N将手中的$n收回。
");
        }

//**    $function_name($argument);                      /* FUNCTION_ENTRY */
        set_weight(5000);
        init_xsword(5, 2);                              /* FUNCTION_EXIT  */
	set("can_sign", 1);
	set("quality_level", 3);
	set("no_sell", 1);
	set("colorname", "[1;31m幽明萧[2;37;0m[2;37;0m");
	set("stable", 37);
	set("weapon_prop/damage", 1950);
	set("weapon_prop/research_effect", 3);
	set("consistence", 100);
	set("color", "[1;31m");
	set("auto_load", 1);
	set("set_data", 1);
	set("flag", 6);
	set("verbs", ({ /* sizeof() == 3 */
  "slash",
  "slice",
  "thrust"
}));
	set("no_store", 1);
	set("item_owner", "sol");
	set("skill_type", "sword");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

