// fu_dai.c
// Create by TEMPLATE_D. Mon Mar 25 14:07:43 2013.
#include <armor.h>
#include <ansi.h>

inherit MYHEART2;

void create() {
  set_name("[1;31m福袋[2;37;0m", ({ "fu dai" }));
  set_weight(1);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    set("material", "paper");
    set("unit", "个");
    set("long", "[1;31m扶桑国有绣制福袋的习俗，据说带在身边可以逢凶化吉。
[2;37;0m");
    set("value", 1000000);
    set("limit", ([ /* sizeof() == 1 */
      "exp": 2000000,
    ]));


    set("armor_prop", ([ /* sizeof() == 1 */
      "fy": 3,
    ]));

  }
  set("no_repair", "这东西我可没法修理。
");
  set("armor_type", "myheart2");
  set("no_store", 1);
  set("item_owner", "samesame");
  set("no_sell", 1);
  set("stable", 1);
  set("consistence", 100);
  setup();
}

int query_autoload() {
  return 1;
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
