// tiangang_finger.c
// Create by TEMPLATE_D. Tue Apr 30 21:35:47 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit FINGER;

#include <template/armor.h>
void create()
{
	set_name("天罡指套[2;37;0m", ({ "tiangang finger" }));
	set_name("天罡指套[2;37;0m", ({ "tiangang finger" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件天罡指套[2;37;0m(tiangang finger)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 3 */
  "max_neili" : 10000,
  "armor" : 10,
  "unarmed_damage" : 1650,
]));

        }

        set_weight(500);
	set("can_sign", 1);
	set("armor_type", "finger");
	set("quality_level", 3);
	set("who_get/time", 1367304960);
	set("who_get/id", "app");
	set("no_sell", 1);
	set("colorname", "[1;31m天罡指套[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[1;31m");
	set("auto_load", 1);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "fourfour");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

