// dianguang_finger.c
// Create by TEMPLATE_D. Fri Jul 10 20:59:05 2015.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit FINGER;

#include <template/armor.h>
void create()
{
	set_name("电光指套[2;37;0m", ({ "dianguang finger" }));
	set_name("电光指套[2;37;0m", ({ "dianguang finger" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件电光指套[2;37;0m(dianguang finger)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 5 */
  "add_freeze" : 2.500000,
  "add_damage" : 5.000000,
  "armor" : 10,
  "ap_power" : 2.000000,
  "unarmed_damage" : 6900,
]));

        }

        set_weight(500);
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 4 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 2 */
        "max_neili" : 10000,
        "da_power" : 2.000000,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 1 */
        "str" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 2 */
        "max_neili" : 10000,
        "derive_effect" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 2 */
        "learn_effect" : 3,
        "learn_times" : 15,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("armor_type", "finger");
	set("quality_level", 6);
	set("no_sell", 1);
	set("colorname", "[41;1m[1;37m电光指套[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[41;1m[1;37m");
	set("auto_load", 1);
	set("enchase/apply_prop/derive_effect", 3);
	set("enchase/apply_prop/learn_times", 15);
	set("enchase/apply_prop/learn_effect", 3);
	set("enchase/apply_prop/max_neili", 20000);
	set("enchase/apply_prop/str", 3);
	set("enchase/used", 4);
	set("enchase/flute", 4);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "tat");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

