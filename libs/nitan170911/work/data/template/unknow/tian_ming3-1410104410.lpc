// tian_ming3.c
// Create by TEMPLATE_D. Tue May 21 16:27:40 2013.
#include <armor.h>
#include <ansi.h>

inherit NECK;

void create()
{
	set_name("[1;31m天命[2;37;0m", ({ "tian ming3" }));
        set_weight(500);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("unit", "串");
	set("value", 200000);
	set("material", "gold");
	set("armor_prop", ([ /* sizeof() == 5 */
  "str" : 8,
  "int" : 4,
  "con" : 6,
  "dex" : 6,
  "damage" : 200,
]));




	set("limit", ([ /* sizeof() == 2 */
  "scborn" : 1,
  "exp" : 35000000,
]));



        }
	set("armor_type", "neck");
	set("no_store", 1);
	set("item_owner", "lurong");
	set("no_sell", 1);
	set("stable", 25);
	set("consistence", 100);
        setup();
}        

int query_autoload()
{
        return 1;
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

