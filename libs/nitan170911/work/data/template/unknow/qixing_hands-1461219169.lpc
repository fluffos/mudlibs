// qixing_hands.c
// Create by TEMPLATE_D. Sun Dec  8 14:23:49 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit HANDS;

#include <template/armor.h>
void create()
{
	set_name("七星掌套[2;37;0m", ({ "qixing hands" }));
	set_name("七星掌套[2;37;0m", ({ "qixing hands" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件七星掌套[2;37;0m(qixing hands)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 4 */
  "add_damage" : 7.500000,
  "armor" : 10,
  "ap_power" : 2.000000,
  "unarmed_damage" : 4500,
]));

        }

        set_weight(500);
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 1 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "practice_times" : 15,
        "ap_power" : 2.000000,
        "derive_effect" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("armor_type", "hands");
	set("quality_level", 6);
	set("no_sell", 1);
	set("colorname", "[41;1m[1;37m七星掌套[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[41;1m[1;37m");
	set("auto_load", 1);
	set("enchase/apply_prop/derive_effect", 3);
	set("enchase/apply_prop/practice_times", 15);
	set("enchase/used", 1);
	set("enchase/flute", 1);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "loose");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

