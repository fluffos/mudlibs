// guanri_youming_finger.c
// Create by TEMPLATE_D. Sun Feb 16 09:12:36 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit FINGER;

#include <template/armor.h>
void create()
{
	set_name("贯日幽溟指套[2;37;0m", ({ "guanri youming finger" }));
	set_name("贯日幽溟指套[2;37;0m", ({ "guanri youming finger" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件贯日幽溟指套[2;37;0m(guanri youming finger)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 5 */
  "double_damage" : 2.000000,
  "practice_effect" : 3,
  "add_poison" : 3,
  "armor" : 10,
  "unarmed_damage" : 4750,
]));

        }

        set_weight(500);
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 2 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "max_potential" : 50000,
        "through_armor" : 2.000000,
        "int" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "double_damage" : 2.000000,
        "research_times" : 15,
        "damage" : 39600,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("armor_type", "finger");
	set("quality_level", 5);
	set("no_sell", 1);
	set("colorname", "[1;33m贯日幽溟指套[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[1;33m");
	set("auto_load", 1);
	set("enchase/apply_prop/damage", 39600);
	set("enchase/apply_prop/research_times", 15);
	set("enchase/apply_prop/int", 3);
	set("enchase/apply_prop/max_potential", 50000);
	set("enchase/used", 2);
	set("enchase/flute", 2);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "bzm");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

