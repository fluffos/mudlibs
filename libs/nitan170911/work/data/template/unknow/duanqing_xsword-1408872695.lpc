// duanqing_xsword.c
// Create by TEMPLATE_D. Sat Dec  7 03:40:14 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// sword.c
// 自动创建剑的基准文件。                               /* COMMENT_EXIT  */

#include <weapon.h>

inherit XSWORD;

#include <template/weapon.h>

void create() {
  set_name("断情萧[2;37;0m", ({ "duanqing xsword" }));
  set_name("断情萧[2;37;0m", ({ "duanqing xsword" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一柄断情萧[2;37;0m(duanqing xsword)。
");
    set("unit", "柄");
    set("value", 200);
    set("material", "steel");
    set("wield_msg", "$N「唰」的一声抽出一柄$n握在手中。
");
    set("unwield_msg", "$N将手中的$n收回。
");
  }

  //**    $function_name($argument);                      /* FUNCTION_ENTRY */
  set_weight(5000);
  init_xsword(5, 2); /* FUNCTION_EXIT  */ set("can_sign", 1);
  set("insert", ({ /* sizeof() == 3 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 1 */
        "add_blind": 2.000000,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 1 */
        "avoid_force": 1.500000,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 1 */
        "max_jing": 2200,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("quality_level", 6);
  set("no_sell", 1);
  set("colorname", "[41;1m[1;37m断情萧[2;37;0m[2;37;0m");
  set("stable", 37);
  set("weapon_prop/damage", 4500);
  set("consistence", 100);
  set("actions", 0);
  set("color", "[41;1m[1;37m");
  set("auto_load", 1);
  set("enchase/apply_prop/max_jing", 2200);
  set("enchase/used", 3);
  set("enchase/flute", 3);
  set("set_data", 1);
  set("flag", 6);
  set("verbs", ({ /* sizeof() == 3 */
    "slash",
    "slice",
    "thrust"
  }));
  set("no_store", 1);
  set("item_owner", "btmudb");
  set("skill_type", "sword");
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
