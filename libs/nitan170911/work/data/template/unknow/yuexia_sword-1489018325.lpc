// yuexia_sword.c
// Create by TEMPLATE_D. Sat Sep 27 04:09:47 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// sword.c
// 自动创建剑的基准文件。                               /* COMMENT_EXIT  */

#include <weapon.h>

inherit SWORD;

#include <template/weapon.h>

void create() {
  set_name("月霞剑[2;37;0m", ({ "yuexia sword" }));
  set_name("月霞剑[2;37;0m", ({ "yuexia sword" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一柄月霞剑[2;37;0m(yuexia sword)。
");
    set("unit", "柄");
    set("value", 200);
    set("material", "steel");
    set("wield_msg", "$N「唰」的一声抽出一柄$n握在手中。
");
    set("unwield_msg", "$N将手中的$n插回剑鞘。
");
  }

  //**    $function_name($argument);                      /* FUNCTION_ENTRY */
  set_weight(5000);
  init_sword(5, 2); /* FUNCTION_EXIT  */ set("can_sign", 1);
  set("insert", ({ /* sizeof() == 2 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "research_effect": 3,
        "research_times": 15,
        "damage": 50000,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "research_effect": 3,
        "max_potential": 50000,
        "int": 3,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("rigidity", 1);
  set("quality_level", 6);
  set("no_sell", 1);
  set("colorname", "[41;1m[1;37m月霞剑[2;37;0m[2;37;0m");
  set("stable", 37);
  set("weapon_prop/damage", 6900);
  set("weapon_prop/derive_effect", 3);
  set("weapon_prop/practice_effect", 3);
  set("consistence", 100);
  set("color", "[41;1m[1;37m");
  set("auto_load", 1);
  set("enchase/apply_prop/damage", 50000);
  set("enchase/apply_prop/research_times", 15);
  set("enchase/apply_prop/int", 3);
  set("enchase/apply_prop/max_potential", 50000);
  set("enchase/apply_prop/research_effect", 6);
  set("enchase/used", 2);
  set("enchase/flute", 2);
  set("set_data", 1);
  set("flag", 6);
  set("verbs", ({ /* sizeof() == 3 */
    "slash",
    "slice",
    "thrust"
  }));
  set("no_store", 1);
  set("item_owner", "mud");
  set("skill_type", "sword");
  setup();
}

int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
