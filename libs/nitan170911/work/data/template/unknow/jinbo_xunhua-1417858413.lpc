// jinbo_xunhua.c
// Create by TEMPLATE_D. Mon Apr  8 20:59:44 2013.
//Edited by fandog, 02/15/2000
#include <ansi.h>;
#include <armor.h>;
inherit HEAD;

void create() {
  set_name("[1;33m金波旬花[2;37;0m", ({ "jinbo xunhua" }));
  /*if( clonep() )
                set_default_object(__FILE__);
        else*/ {
    set("unit", "朵");
    set("long", "这金色的花朵，便是奇毒无比的金波旬花。“波旬”两字是
梵语，是“恶魔”的意思。这毒花是从天竺传来的，原来天
竺人叫它为“恶魔花”，
");



    set("value", 90);
    set("material", "plant");
    set_weight(10);
    set("armor_prop", ([ /* sizeof() == 2 */
      "per": 3,
      "armor": 0,
    ]));

    set("wear_msg", "$N轻轻地把一朵$n戴在头上。
");
    set("unequip_msg", "$N轻轻地把$n从头上摘了下来。
");
  }
  set("armor_type", "head");
  set("startroom", "/d/jingzhou/huayuan");
  set("no_store", 1);
  set("item_owner", "touxin");
  set("no_sell", 1);
  set("stable", 50);
  set("consistence", 100);
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
