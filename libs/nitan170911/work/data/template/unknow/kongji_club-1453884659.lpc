// kongji_club.c
// Create by TEMPLATE_D. Sat Oct 26 16:04:34 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// axe.c
// 自动创建斧头的基准文件。                             /* COMMENT_EXIT  */

#include <weapon.h>

inherit CLUB;

#include <template/weapon.h>

void create() {
  set_name("空寂棍[2;37;0m", ({ "kongji club" }));
  set_name("空寂棍[2;37;0m", ({ "kongji club" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一根空寂棍[2;37;0m(kongji club)。
");
    set("unit", "根");
    set("value", 200);
    set("material", "steel");
    set("wield_msg", "$N「唰」的一声抽出一根$n握在手中。
");
    set("unwield_msg", "$N将手中的$n背到背上。
");
  }

  //**    $function_name($argument);                      /* FUNCTION_ENTRY */
  set_weight(5000);
  init_club(5, 2); /* FUNCTION_EXIT  */ set("can_sign", 1);
  set("insert", ({ /* sizeof() == 1 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "attack": 30,
        "research_effect": 3,
        "practice_effect": 3,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("rigidity", 1);
  set("quality_level", 3);
  set("no_sell", 1);
  set("colorname", "[1;31m空寂棍[2;37;0m[2;37;0m");
  set("stable", 37);
  set("weapon_prop/damage", 1650);
  set("weapon_prop/research_effect", 3);
  set("consistence", 100);
  set("color", "[1;31m");
  set("auto_load", 1);
  set("enchase/apply_prop/practice_effect", 3);
  set("enchase/apply_prop/research_effect", 3);
  set("enchase/apply_prop/attack", 30);
  set("enchase/used", 1);
  set("enchase/flute", 1);
  set("set_data", 1);
  set("flag", 18);
  set("verbs", ({ /* sizeof() == 3 */
    "bash",
    "crush",
    "slam"
  }));
  set("no_store", 1);
  set("item_owner", "sol");
  set("skill_type", "club");
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
