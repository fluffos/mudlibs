// zhufu_hand.c
// Create by TEMPLATE_D. Thu Jul 31 17:52:29 2014.
// This program is a part of NT MudLIB 
 
#include <ansi.h>
#include <armor.h>
inherit HANDS;
 
void create()
{
	set_name("[1;33m神[1;37m之[1;35m祝福[2;37;0m[1;36m铁掌[2;37;0m", ({ "zhufu hand" }));
        set_weight(500);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("unit", "柄");
	set("long", "[1;33m这便是神之祝福铁掌。
[2;37;0m");
	set("value", 3000);
	set("no_sell", 1);
	set("rigidity", 8000);
	set("material", "tian jing");
	set("no_pawn", 1);
	set("armor_prop", ([ /* sizeof() == 2 */
  "armor" : 10,
  "unarmed_damage" : 500,
]));

        }
        
	set("quality_level", 2);
	set("armor_type", "hands");
	set("no_store", 1);
	set("item_owner", "xyfb");
	set("stable", 50);
	set("consistence", 100);
        setup();
}



int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

