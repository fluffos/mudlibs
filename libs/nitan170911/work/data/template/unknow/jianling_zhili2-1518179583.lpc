// jianling_zhili2.c
// Create by TEMPLATE_D. Mon Aug  3 14:59:10 2015.
#include <armor.h>
#include <ansi.h>

inherit MYHEART2;

void create()
{
	set_name("[1;32m剑灵之力[2;37;0m", ({ "jianling zhili2" }));
        set_weight(1);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("material", "paper");
	set("unit", "张");
	set("long", "[1;32m这是一张淡蓝色的护符，其外形有如蝴蝶翅膀一般。
[2;37;0m");
	set("value", 10000000);
	set("limit", ([ /* sizeof() == 3 */
  "jianling" : 1,
  "exp" : 100000000,
  "thborn" : 1,
]));


	set("armor_prop", ([ /* sizeof() == 4 */
  "int" : 60,
  "T_jianling" : 1,
  "qy" : 4,
  "fy" : 4,
]));




	set("special", ([ /* sizeof() == 1 */
  "desc" : "【剑灵套装】部件之一（剑灵之怒、剑灵之痕、剑灵之力、剑灵之魂）。
                  关于剑灵套装的传说参见 help boss
",
]));







        }
	set("no_repair", "这东西我可没法修理。
");
	set("armor_type", "myheart2");
	set("no_store", 1);
	set("item_owner", "xlqx");
	set("no_sell", 1);
	set("stable", 1);
	set("consistence", 100);
        setup();
}

int query_autoload()
{
        return 1;
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

