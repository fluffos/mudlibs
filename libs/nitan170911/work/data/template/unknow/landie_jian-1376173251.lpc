// landie_jian.c
// Create by TEMPLATE_D. Mon May 27 11:06:54 2013.
#include <ansi.h>
#include <weapon.h>
inherit SWORD;

void create()
{
	set_name("[1;36m蓝蝶宝剑[2;37;0m", ({ "landie jian" }));
        set_weight(100);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/ {
	set("unit", "柄");
	set("long", "[1;36m这是一柄充满传说的宝剑，乃由崆峒仙境的蓝蝶仙子的眼泪所化，由爱生悲，蓝蝶宝剑蕴藏着巨大的威力。
[2;37;0m");
	set("value", 2000);
	set("limit", ([ /* sizeof() == 5 */
  "str" : 700,
  "int" : 700,
  "con" : 700,
  "exp" : 2088888888,
  "dex" : 700,
]));







	set("material", "steel");
	set("wield_msg", "一道蓝色的剑芒闪过，只见剑芒化作一直蓝色的蝴蝶，光芒退去$N[1;36m手中却多了一把宝剑。
");
	set("unwield_msg", "[1;36m$N[1;36m一声长叹，$n化作一缕蓝色青烟消失了。
[2;37;0m");
        }
        init_sword(5000);
	set("rigidity", 1250);
	set("flag", 6);
	set("verbs", ({ /* sizeof() == 3 */
  "slash",
  "slice",
  "thrust"
}));
	set("no_store", 1);
	set("item_owner", "juan");
	set("no_sell", 1);
	set("stable", 37);
	set("weapon_prop/damage", 5000);
	set("consistence", 100);
	set("skill_type", "sword");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

