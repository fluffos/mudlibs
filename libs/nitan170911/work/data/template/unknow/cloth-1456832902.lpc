// cloth.c
// Create by TEMPLATE_D. Sat Jun 22 18:48:55 2013.
#include <ansi.h>
#include <armor.h>

inherit CLOTH;

void create() {
  set_name("胸部半开纱衣", ({ "cloth" }));
  set_weight(3000);
  /*if( clonep() )
                set_default_object(__FILE__);
        else*/ {
    set("unit", "件");
    set("material", "cloth");
    set("armor_prop", ([ /* sizeof() == 1 */
      "armor": 1,
    ]));
  }
  set("armor_type", "cloth");
  set("no_store", 1);
  set("item_owner", "dcgz");
  set("no_sell", 1);
  set("stable", 0);
  set("consistence", 100);
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
