// xingshi_huan4.c
// Create by TEMPLATE_D. Mon Dec  2 13:13:16 2013.
#include <armor.h>
#include <ansi.h>

inherit RINGS;

void create()
{
	set_name("[1;33m天降·醒世环[2;37;0m", ({ "xingshi huan4" }));
        set_weight(30);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("unit", "枚");
	set("long", "[1;33m传说中狐仙所佩带的指环。
[2;37;0m");
	set("value", 200000);
	set("material", "gold");
	set("armor_prop", ([ /* sizeof() == 4 */
  "unarmed" : 200,
  "con" : 30,
  "magic_find" : 12,
  "damage" : 3000,
]));



	set("special", ([ /* sizeof() == 1 */
  "desc" : "有效拳法等级提高200级。
有效拳脚等级提高200级。
有效掌法等级提高200级。
有效指法等级提高200级。
",
]));




	set("limit", ([ /* sizeof() == 2 */
  "scborn" : 1,
  "exp" : 100000000,
]));




        }
	set("armor_type", "rings");
	set("no_store", 1);
	set("item_owner", "toxin");
	set("no_sell", 1);
	set("stable", 25);
	set("consistence", 100);
        setup();
}

int query_autoload()
{
        return 1;
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

