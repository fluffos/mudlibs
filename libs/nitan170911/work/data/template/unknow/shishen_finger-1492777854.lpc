// shishen_finger.c
// Create by TEMPLATE_D. Sun Dec 13 10:50:08 2015.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit FINGER;

#include <template/armor.h>
void create() {
  set_name("弑神指套[2;37;0m", ({ "shishen finger" }));
  set_name("弑神指套[2;37;0m", ({ "shishen finger" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一件弑神指套[2;37;0m(shishen finger)。
");
    set("unit", "件");
    set("value", 200);
    set("material", "cloth");
    set("armor_prop", ([ /* sizeof() == 6 */
      "add_freeze": 2.500000,
      "research_effect": 3,
      "practice_effect": 3,
      "armor": 10,
      "unarmed_damage": 6900,
      "add_forget": 2.500000,
    ]));

  }

  set_weight(500);
  set("can_sign", 1);
  set("insert", ({ /* sizeof() == 4 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "research_effect": 3,
        "learn_effect": 3,
        "derive_effect": 3,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "through_armor": 2.000000,
        "int": 3,
        "add_blind": 2.000000,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 2 */
        "avoid_parry": 1.500000,
        "max_experience": 50000,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "learn_effect": 3,
        "learn_times": 15,
        "derive_times": 15,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("armor_type", "finger");
  set("quality_level", 6);
  set("no_sell", 1);
  set("colorname", "[41;1m[1;37m弑神指套[2;37;0m[2;37;0m");
  set("stable", 0);
  set("consistence", 100);
  set("color", "[41;1m[1;37m");
  set("auto_load", 1);
  set("enchase/apply_prop/derive_effect", 3);
  set("enchase/apply_prop/int", 3);
  set("enchase/apply_prop/learn_effect", 6);
  set("enchase/apply_prop/max_experience", 50000);
  set("enchase/apply_prop/derive_times", 15);
  set("enchase/apply_prop/learn_times", 15);
  set("enchase/apply_prop/research_effect", 3);
  set("enchase/used", 4);
  set("enchase/flute", 4);
  set("set_data", 1);
  set("no_store", 1);
  set("item_owner", "tank");
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
