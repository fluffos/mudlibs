// lieyan_zhiri_cloth.c
// Create by TEMPLATE_D. Mon Nov 10 10:35:04 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit CLOTH;

#include <template/armor.h>
void create()
{
	set_name("烈焰炽日战衣[2;37;0m", ({ "lieyan zhiri cloth" }));
	set_name("烈焰炽日战衣[2;37;0m", ({ "lieyan zhiri cloth" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件烈焰炽日战衣[2;37;0m(lieyan zhiri cloth)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 4 */
  "max_jing" : 2200,
  "reduce_damage" : 2.500000,
  "armor" : 750,
  "max_experience" : 50000,
]));
        }

        set_weight(500);
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 2 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "learn_effect" : 3,
        "qi_recover" : 40,
        "max_experience" : 50000,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "research_effect" : 3,
        "practice_effect" : 3,
        "derive_effect" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("armor_type", "cloth");
	set("quality_level", 5);
	set("no_sell", 1);
	set("colorname", "[1;33m烈焰炽日战衣[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[1;33m");
	set("auto_load", 1);
	set("enchase/apply_prop/derive_effect", 3);
	set("enchase/apply_prop/max_experience", 50000);
	set("enchase/apply_prop/practice_effect", 3);
	set("enchase/apply_prop/qi_recover", 40);
	set("enchase/apply_prop/learn_effect", 3);
	set("enchase/apply_prop/research_effect", 3);
	set("enchase/used", 2);
	set("enchase/flute", 2);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "fuyuan");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

