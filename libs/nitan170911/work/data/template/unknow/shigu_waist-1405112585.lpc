// shigu_waist.c
// Create by TEMPLATE_D. Sat Dec  7 03:12:17 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit WAIST;

#include <template/armor.h>
void create()
{
	set_name("噬谷腰带[2;37;0m", ({ "shigu waist" }));
	set_name("噬谷腰带[2;37;0m", ({ "shigu waist" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件噬谷腰带[2;37;0m(shigu waist)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 3 */
  "avoid_poison" : 1.500000,
  "jing_recover" : 40,
  "armor" : 520,
]));
        }

        set_weight(500);
	set("can_sign", 1);
	set("armor_type", "waist");
	set("quality_level", 4);
	set("no_sell", 1);
	set("colorname", "[1;35m噬谷腰带[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[1;35m");
	set("auto_load", 1);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "btmudb");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

