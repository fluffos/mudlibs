// zixue_huan.c
// Create by TEMPLATE_D. Mon Jan 12 13:53:45 2015.
#include <armor.h>
#include <ansi.h>

inherit RINGS;

void create()
{
	set_name("[35m紫血环[2;37;0m", ({ "zixue huan" }));
        set_weight(500);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("unit", "枚");
	set("long", "[35m罕见的紫色血玉锻造而成，入手冰冷刺骨，佩戴后有特殊功效。
[2;37;0m");
	set("value", 20000000);
	set("material", "gold");
	set("armor_prop", ([ /* sizeof() == 3 */
  "add_tihui_gift" : 5,
  "int" : 10,
  "damage" : 1000,
]));


	set("special", ([ /* sizeof() == 1 */
  "desc" : "[1;31m杀怪、杀BOSS所得体会奖励额外增加5%。[2;37;0m",
]));
	set("limit", ([ /* sizeof() == 2 */
  "scborn" : 1,
  "exp" : 100000000,
]));



        }
	set("armor_type", "rings");
	set("no_store", 1);
	set("item_owner", "jpgz");
	set("who_get/time", 1421041493);
	set("who_get/id", "NONE");
	set("no_sell", 1);
	set("stable", 25);
	set("consistence", 100);
        setup();
}

int query_autoload()
{
        return 1;
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

