// lingfeng_boots.c
// Create by TEMPLATE_D. Sun Dec  7 22:55:59 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit BOOTS;

#include <template/armor.h>
void create()
{
	set_name("灵风靴子[2;37;0m", ({ "lingfeng boots" }));
	set_name("灵风靴子[2;37;0m", ({ "lingfeng boots" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件灵风靴子[2;37;0m(lingfeng boots)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 4 */
  "practice_times" : 15,
  "avoid_poison" : 1.500000,
  "qi_recover" : 40,
  "armor" : 850,
]));
        }

        set_weight(500);
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 1 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "research_effect" : 3,
        "dp_power" : 1.500000,
        "dex" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("armor_type", "boots");
	set("quality_level", 5);
	set("no_sell", 1);
	set("colorname", "[1;33m灵风靴子[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[1;33m");
	set("auto_load", 1);
	set("enchase/apply_prop/dex", 3);
	set("enchase/apply_prop/research_effect", 3);
	set("enchase/used", 1);
	set("enchase/flute", 1);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "mud");
        setup();
}

int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

