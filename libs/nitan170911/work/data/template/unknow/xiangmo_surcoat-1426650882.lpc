// xiangmo_surcoat.c
// Create by TEMPLATE_D. Sat Jan  4 11:03:26 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit SURCOAT;

#include <template/armor.h> 
void create()
{
	set_name("降魔披风[2;37;0m", ({ "xiangmo surcoat" }));
	set_name("降魔披风[2;37;0m", ({ "xiangmo surcoat" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件降魔披风[2;37;0m(xiangmo surcoat)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 5 */
  "avoid_busy" : 2.500000,
  "reduce_busy" : 2.500000,
  "armor" : 900,
  "counter_damage" : 2.500000,
  "avoid_freeze" : 0,
]));
        }

        set_weight(500);
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 2 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "avoid_attack" : 1.500000,
        "dp_power" : 1.500000,
        "armor" : 7700,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 2 */
        "max_neili" : 10000,
        "con" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("armor_type", "surcoat");
	set("quality_level", 6);
	set("no_sell", 1);
	set("colorname", "[41;1m[1;37m降魔披风[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[41;1m[1;37m");
	set("auto_load", 1);
	set("enchase/apply_prop/armor", 7700);
	set("enchase/apply_prop/con", 3);
	set("enchase/apply_prop/max_neili", 10000);
	set("enchase/used", 2);
	set("enchase/flute", 2);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "btmudc");
        setup();
}

int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

