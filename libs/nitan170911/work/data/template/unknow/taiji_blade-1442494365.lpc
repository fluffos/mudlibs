// taiji_blade.c
// Create by TEMPLATE_D. Tue Oct 21 08:11:31 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// axe.c
// 自动创建斧头的基准文件。                             /* COMMENT_EXIT  */

#include <weapon.h>

inherit BLADE;

#include <template/weapon.h>

void create()
{
	set_name("太极刀[2;37;0m", ({ "taiji blade" }));
	set_name("太极刀[2;37;0m", ({ "taiji blade" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一柄太极刀[2;37;0m(taiji blade)。
");
	set("unit", "柄");
	set("value", 200);
	set("material", "steel");
	set("wield_msg", "$N「唰」的一声抽出一柄$n握在手中。
");
	set("unwield_msg", "$N将手中的$n收回刀鞘。
");
        }

//**    $function_name($argument);                      /* FUNCTION_ENTRY */
        set_weight(5000);
        init_blade(5, 2);                               /* FUNCTION_EXIT  */
	set("can_sign", 1);
	set("rigidity", 1);
	set("quality_level", 5);
	set("no_sell", 1);
	set("colorname", "[1;33m太极刀[2;37;0m[2;37;0m");
	set("stable", 37);
	set("weapon_prop/damage", 3250);
	set("weapon_prop/derive_effect", 3);
	set("weapon_prop/research_effect", 3);
	set("weapon_prop/study_effect", 3);
	set("consistence", 100);
	set("color", "[1;33m");
	set("auto_load", 1);
	set("set_data", 1);
	set("flag", 6);
	set("verbs", ({ /* sizeof() == 3 */
  "slash",
  "slice",
  "hack"
}));
	set("no_store", 1);
	set("item_owner", "lide");
	set("skill_type", "blade");
        setup();
}

int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

