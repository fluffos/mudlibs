// wu_wei3.c
// Create by TEMPLATE_D. Sat Apr 12 08:16:18 2014.
#include <armor.h>
#include <ansi.h>

inherit NECK;

void create()
{
	set_name("[1;33m无为[2;37;0m", ({ "wu wei3" }));
        set_weight(500);
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("unit", "串");
	set("value", 200000);
	set("material", "gold");
	set("armor_prop", ([ /* sizeof() == 3 */
  "joblv" : 1,
  "str" : 5,
  "damage" : 100,
]));


	set("special", ([ /* sizeof() == 1 */
  "desc" : "[1;31m可破格提升转世职业技能1级。[2;37;0m",
]));
	set("limit", ([ /* sizeof() == 2 */
  "scborn" : 1,
  "exp" : 40000000,
]));



        }
	set("armor_type", "neck");
	set("no_store", 1);
	set("item_owner", "zun");
	set("no_sell", 1);
	set("stable", 25);
	set("consistence", 100);
        setup();
}        

int query_autoload()
{
        return 1;
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

