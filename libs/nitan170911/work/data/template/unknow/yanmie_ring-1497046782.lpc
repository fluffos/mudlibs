// yanmie_ring.c
// Create by TEMPLATE_D. Sun Sep  7 11:59:25 2014.
#include <armor.h>
#include <ansi.h>

inherit RINGS;

void create() {
  set_name("[1;31m火麒麟·湮灭[2;37;0m", ({ "yanmie ring" }));
  set_weight(500);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    set("unit", "枚");
    set("long", "[1;31m火麒麟乃上古神兽，拥有无穷神力，直到有一天，被更强大的力量消灭后，
临死之前的怒怨，在一瞬间凝聚成的湮灭之戒，蕴涵了巨大的威力。
[2;37;0m");

    set("value", 20000000);
    set("material", "gold");
    set("armor_prop", ([ /* sizeof() == 2 */
      "magic_find": 20,
      "damage": 4000,
    ]));


    set("limit", ([ /* sizeof() == 2 */
      "scborn": 1,
      "exp": 100000000,
    ]));




  }
  set("armor_type", "rings");
  set("no_store", 1);
  set("item_owner", "lxer");
  set("no_sell", 1);
  set("stable", 25);
  set("consistence", 100);
  setup();
}

int query_autoload() {
  return 1;
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
