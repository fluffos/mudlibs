// jianmo_zhiyu2.c
// Create by TEMPLATE_D. Mon Aug 18 11:14:43 2014.
#include <armor.h>
#include <ansi.h>

inherit HEAD;

void create() {
  set_name("[1;36m剑魔之羽[2;37;0m", ({ "jianmo zhiyu2" }));
  set_weight(1);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    set("unit", "件");
    set("long", "[1;36m这是由独孤求败取神雕羽毛精心制作而成的头饰，其中似乎蕴涵了高深的武学真谛。
[2;37;0m");
    set("value", 1500000);
    set("armor_prop", ([ /* sizeof() == 5 */
      "str": 35,
      "sword": 80,
      "con": 35,
      "dex": 35,
      "damage": 4000,
    ]));




    set("special", ([ /* sizeof() == 1 */
      "desc": "有效剑法等级提高80级。",
    ]));
    set("limit", ([ /* sizeof() == 2 */
      "scborn": 1,
      "exp": 100000000,
    ]));




  }
  set("armor_type", "head");
  set("no_store", 1);
  set("item_owner", "clrak");
  set("no_sell", 1);
  set("stable", 50);
  set("consistence", 100);
  setup();
}

int query_autoload() {
  return 1;
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
