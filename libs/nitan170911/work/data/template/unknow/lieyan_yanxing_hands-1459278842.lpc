// lieyan_yanxing_hands.c
// Create by TEMPLATE_D. Sat May 11 13:29:16 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit HANDS;

#include <template/armor.h>
void create()
{
	set_name("烈焰雁行掌套[2;37;0m", ({ "lieyan yanxing hands" }));
	set_name("烈焰雁行掌套[2;37;0m", ({ "lieyan yanxing hands" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件烈焰雁行掌套[2;37;0m(lieyan yanxing hands)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 5 */
  "skill" : 0,
  "armor" : 10,
  "unarmed_damage" : 2750,
  "derive_effect" : 3,
  "max_qi" : 4500,
]));

        }

        set_weight(500);
	set("can_sign", 1);
	set("armor_type", "hands");
	set("quality_level", 5);
	set("no_sell", 1);
	set("colorname", "[1;33m烈焰雁行掌套[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[1;33m");
	set("auto_load", 1);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "kuzhi");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

