// pangu_leiming_armor.c
// Create by TEMPLATE_D. Sat Nov 29 11:19:43 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit ARMOR;

#include <template/armor.h>
void create()
{
	set_name("盘古雷鸣护甲[2;37;0m", ({ "pangu leiming armor" }));
	set_name("盘古雷鸣护甲[2;37;0m", ({ "pangu leiming armor" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件盘古雷鸣护甲[2;37;0m(pangu leiming armor)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 5 */
  "research_effect" : 3,
  "avoid_busy" : 2.500000,
  "practice_effect" : 3,
  "armor" : 1380,
  "derive_effect" : 3,
]));
        }

        set_weight(500);
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 2 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 1 */
        "avoid_attack" : 1.500000,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ]),
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "research_effect" : 3,
        "kar" : 3,
        "avoid_poison" : 1.500000,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("armor_type", "armor");
	set("quality_level", 6);
	set("no_sell", 1);
	set("colorname", "[41;1m[1;37m盘古雷鸣护甲[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[41;1m[1;37m");
	set("auto_load", 1);
	set("enchase/apply_prop/kar", 3);
	set("enchase/apply_prop/research_effect", 3);
	set("enchase/used", 2);
	set("enchase/flute", 2);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "zhaoyun");
        setup();
}

int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

