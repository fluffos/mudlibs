// pangu_mengshen_wrists.c
// Create by TEMPLATE_D. Sun Feb  1 10:18:54 2015.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit WRISTS;

#include <template/armor.h>
void create() {
  set_name("盘古梦神护腕[2;37;0m", ({ "pangu mengshen wrists" }));
  set_name("盘古梦神护腕[2;37;0m", ({ "pangu mengshen wrists" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一件盘古梦神护腕[2;37;0m(pangu mengshen wrists)。
");
    set("unit", "件");
    set("value", 200);
    set("material", "cloth");
    set("armor_prop", ([ /* sizeof() == 4 */
      "study_effect": 3,
      "research_effect": 3,
      "reduce_damage": 2.500000,
      "armor": 850,
    ]));
  }

  set_weight(500);
  set("can_sign", 1);
  set("insert", ({ /* sizeof() == 1 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "research_effect": 3,
        "parry": 30,
        "research_times": 15,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("armor_type", "wrists");
  set("quality_level", 5);
  set("no_sell", 1);
  set("colorname", "[1;33m盘古梦神护腕[2;37;0m[2;37;0m");
  set("stable", 0);
  set("consistence", 100);
  set("color", "[1;33m");
  set("auto_load", 1);
  set("enchase/apply_prop/research_times", 15);
  set("enchase/apply_prop/parry", 30);
  set("enchase/apply_prop/research_effect", 3);
  set("enchase/used", 1);
  set("enchase/flute", 1);
  set("set_data", 1);
  set("no_store", 1);
  set("item_owner", "snake");
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
