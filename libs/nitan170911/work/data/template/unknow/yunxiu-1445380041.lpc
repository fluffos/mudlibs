// yunxiu.c
// Create by TEMPLATE_D. Thu Apr  4 01:12:14 2013.
// yunxiu.c

#include <ansi.h>
#include <armor.h>
inherit CLOTH;

void create() {
  set_name("[36m青边[2;37;0m[1;31m戴花[2;37;0m[1;35m水云袖[2;37;0m", ({ "yunxiu" }));
  set("long", "这是一件崭新的新娘出阁时穿的礼服。
");
  set_weight(2000);
  /*if (clonep())
                set_default_object(__FILE__);
        else*/ {
    set("material", "cloth");
    set("unit", "件");
    set("armor_prop", ([ /* sizeof() == 1 */
      "armor": 100,
    ]));
    set("female_only", 1);
  }
  set("armor_type", "cloth");
  set("no_store", 1);
  set("item_owner", "juice");
  set("no_sell", 1);
  set("stable", 0);
  set("consistence", 100);
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
