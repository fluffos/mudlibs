// shishen_finger.c
// Create by TEMPLATE_D. Tue Sep 23 20:37:36 2014.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit FINGER;

#include <template/armor.h>
void create()
{
	set_name("失神指套[2;37;0m", ({ "shishen finger" }));
	set_name("失神指套[2;37;0m", ({ "shishen finger" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一件失神指套[2;37;0m(shishen finger)。
");
	set("unit", "件");
	set("value", 200);
	set("material", "cloth");
	set("armor_prop", ([ /* sizeof() == 5 */
  "con" : 3,
  "armor" : 10,
  "ap_power" : 2.000000,
  "add_blind" : 2.000000,
  "unarmed_damage" : 3250,
]));

        }

        set_weight(500);
	set("can_sign", 1);
	set("armor_type", "finger");
	set("quality_level", 5);
	set("who_get/time", 1411459928);
	set("who_get/id", "mini");
	set("no_sell", 1);
	set("colorname", "[1;33m失神指套[2;37;0m[2;37;0m");
	set("stable", 0);
	set("consistence", 100);
	set("color", "[1;33m");
	set("auto_load", 1);
	set("set_data", 1);
	set("no_store", 1);
	set("item_owner", "buton");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

