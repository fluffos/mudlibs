// chanyi_pifeng.c
// Create by TEMPLATE_D. Sun Oct 18 14:41:55 2015.
#include <armor.h>
#include <ansi.h>

inherit SURCOAT;

void create()
{
	set_name("[1;32m蝉翼披风[2;37;0m", ({ "chanyi pifeng" }));
        set_weight(1);
	set("long", "[1;32m东方不败所穿戴的披风，薄如蝉翼，飘逸无比。
[2;37;0m");
        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
	set("unit", "件");
	set("material", "paper");
	set("value", 3000000);
	set("armor_prop", ([ /* sizeof() == 6 */
  "str" : 70,
  "int" : 50,
  "dodge" : 300,
  "armor" : 100,
  "dex" : 200,
  "damage" : 8000,
]));





	set("special", ([ /* sizeof() == 1 */
  "desc" : "[1;31m提升有效轻功300级。[2;37;0m",
]));
	set("limit", ([ /* sizeof() == 2 */
  "scborn" : 1,
  "exp" : 100000000,
]));



        }
	set("no_repair", "这东西我可没法修理。
");
	set("armor_type", "surcoat");
	set("no_store", 1);
	set("item_owner", "skoi");
	set("who_get/time", 1445150487);
	set("who_get/id", "NONE");
	set("no_sell", 1);
	set("stable", 1);
	set("consistence", 100);
        setup();
}

int query_autoload()
{
        return 1;
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

