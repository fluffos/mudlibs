// moluo_finger.c
// Create by TEMPLATE_D. Mon Apr 27 01:28:54 2015.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// armor.c
// 自动创建防具的基准文件。                             /* COMMENT_EXIT  */

#include <armor.h>
inherit FINGER;

#include <template/armor.h>
void create() {
  set_name("摩罗指套[2;37;0m", ({ "moluo finger" }));
  set_name("摩罗指套[2;37;0m", ({ "moluo finger" }));

  /*if (clonep())
                set_default_object(__FILE__);
        else*/
  {
    //**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
    set("long", "这是一件摩罗指套[2;37;0m(moluo finger)。
");
    set("unit", "件");
    set("value", 200);
    set("material", "cloth");
    set("armor_prop", ([ /* sizeof() == 5 */
      "double_damage": 4.000000,
      "armor": 10,
      "da_power": 2.000000,
      "ap_power": 2.000000,
      "unarmed_damage": 5100,
    ]));

  }

  set_weight(500);
  set("can_sign", 1);
  set("insert", ({ /* sizeof() == 3 */
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 3 */
        "study_effect": 3,
        "max_neili": 10000,
        "damage": 31600,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 1 */
        "practice_times": 15,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ]),
    ([ /* sizeof() == 3 */
      "id": "magic stone",
      "apply_prop": ([ /* sizeof() == 2 */
        "int": 3,
        "avoid_parry": 1.500000,
      ]),
      "name": "[1;35m玄石[2;37;0m",
    ])
  }));
  set("bind_owner", "yanlei");
  set("armor_type", "finger");
  set("quality_level", 6);
  set("no_sell", 1);
  set("colorname", "[41;1m[1;37m摩罗指套[2;37;0m[2;37;0m");
  set("stable", 0);
  set("consistence", 100);
  set("color", "[41;1m[1;37m");
  set("auto_load", 1);
  set("enchase/apply_prop/damage", 31600);
  set("enchase/apply_prop/int", 3);
  set("enchase/apply_prop/max_neili", 10000);
  set("enchase/apply_prop/study_effect", 3);
  set("enchase/apply_prop/practice_times", 15);
  set("enchase/used", 3);
  set("enchase/flute", 3);
  set("set_data", 1);
  set("no_store", 1);
  set("item_owner", "yanlei");
  set("bindable", 3);
  setup();
}
int receive_summon(object me) {
  return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me) {
  return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }
