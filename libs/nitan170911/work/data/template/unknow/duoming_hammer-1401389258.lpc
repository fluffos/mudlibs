// duoming_hammer.c
// Create by TEMPLATE_D. Sat Dec  7 04:56:43 2013.
// $file_name.c                                         /* COMMENT_ENTRY */
// Create by TEMPLATE_D. $time.                         /* COMMENT_ENTRY */
// axe.c
// 自动创建斧头的基准文件。                             /* COMMENT_EXIT  */

#include <weapon.h>

inherit HAMMER;

#include <template/weapon.h>

void create()
{
	set_name("夺命锤[2;37;0m", ({ "duoming hammer" }));
	set_name("夺命锤[2;37;0m", ({ "duoming hammer" }));

        /*if (clonep())
                set_default_object(__FILE__);
        else*/
        {
//**            set("$dbase_key", "$dbase_value\n");    /* DBASE_ENTRY */
	set("long", "这是一柄夺命锤[2;37;0m(duoming hammer)。
");
	set("unit", "柄");
	set("value", 200);
	set("material", "steel");
	set("wield_msg", "$N「唰」的一声抽出一柄$n握在手中。
");
	set("unwield_msg", "$N将手中的$n背到背上。
");
        }

//**    $function_name($argument);                      /* FUNCTION_ENTRY */
        set_weight(5000);
        init_hammer(5, 2);                              /* FUNCTION_EXIT  */
	set("can_sign", 1);
	set("insert", ({ /* sizeof() == 1 */
  ([ /* sizeof() == 3 */
    "id" : "magic stone",
    "apply_prop" : ([ /* sizeof() == 3 */
        "max_jing" : 2200,
        "add_poison" : 3,
        "derive_effect" : 3,
      ]),
    "name" : "[1;35m玄石[2;37;0m",
  ])
}));
	set("rigidity", 2);
	set("quality_level", 4);
	set("who_get/time", 1386362585);
	set("who_get/id", "tntc");
	set("no_sell", 1);
	set("colorname", "[1;35m夺命锤[2;37;0m[2;37;0m");
	set("stable", 37);
	set("weapon_prop/damage", 2200);
	set("weapon_prop/max_experience", 50000);
	set("consistence", 100);
	set("color", "[1;35m");
	set("auto_load", 1);
	set("enchase/apply_prop/derive_effect", 3);
	set("enchase/apply_prop/add_poison", 3);
	set("enchase/apply_prop/max_jing", 2200);
	set("enchase/used", 1);
	set("enchase/flute", 1);
	set("set_data", 1);
	set("flag", 2);
	set("verbs", ({ /* sizeof() == 3 */
  "bash",
  "crush",
  "slam"
}));
	set("no_store", 1);
	set("item_owner", "btmudb");
	set("skill_type", "hammer");
        setup();
}
int receive_summon(object me)
{
        return ITEM_D->receive_summon(me, this_object());
}

// 隐藏物品
int hide_anywhere(object me)
{
        return ITEM_D->hide_anywhere(me, this_object());
}


int is_no_clone() { return 1; }

