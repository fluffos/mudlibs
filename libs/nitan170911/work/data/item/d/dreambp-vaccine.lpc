// ITEM Made by player(梦旅人:dreambp) /data/item/d/dreambp-vaccine.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Fri Apr  5 21:46:25 2013
#include <ansi.h>
#include <weapon.h>

inherit THROWING;
inherit F_ITEMMAKE;

void create() {
  set_name("[1;33m禽流感疫苗[2;37;0m", ({ "vaccine" }));
  set_weight(1350);
  set("item_make", 1);
  set("unit", "把");
  set("long", "这是由[37m精铁[2;37;0m炼制而成的一把暗器。
");
  set("makeinfo", "[1;31m
梦旅人(dreambp)[1;31m 出品
[2;37;0m");
  set("value", 700000);
  set("base_weight", 13);
  set("base_unit", "枚");
  set("base_value", 1);
  set("point", 30);
  set("max_consistence", 100);
  set("material", "iron");
  set("material_file", "/d/item/obj/jingtie");
  set("quality_level", 2);
  set("hand_msg", "[1;32m$N$N精心配制了一支禽流感疫苗。[2;37;0m\n");
  set("unhand_msg", "[1;34m$N为在场的每一位注射了禽流感疫苗。[2;37;0m\n");
  if (!check_clone()) return;
  restore();
  set_amount(100);
  init_throwing(apply_damage());

  setup();
}

string long() { return short() + query("makeinfo") + query("long") + item_long(); }
