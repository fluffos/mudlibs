// ITEM Made by player(五毒教主:angelbbs) /data/item/a/angelbbs-nla.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Sat May 25 21:42:25 2013
#include <ansi.h>
#include <armor.h>

inherit HEAD;
inherit F_ITEMMAKE;

void create()
{
	set_name("[1;37m飞[1;31m笑[2;37;0m", ({ "nla" }));
	set_weight(4500);
	set("item_make", 1);
	set("unit", "顶");
	set("long", "这是由[1;35m神铁[2;37;0m炼制而成的一顶头盔。
");
	set("makeinfo", "[1;31m
五毒教主(angelbbs)[1;31m 出品
[2;37;0m");
	set("value", 2100000);
	set("point", 210);
	set("max_consistence", 100);
	set("material", "magic stone");
	set("material_file", "/d/item/obj/butian");
	set("quality_level", 2);
	set("wear_msg", "[33m$N[33m装备[33m$n[33m。[2;37;0m\n");
	set("remove_msg", "[33m$N[33m脱下了[33m$n[33m。[2;37;0m\n");
	if( !check_clone() ) return;
	restore();
	set("armor_prop/armor", apply_armor());

	setup();
}

string long() { return short() + query("makeinfo") + query("long") + item_long(); }
