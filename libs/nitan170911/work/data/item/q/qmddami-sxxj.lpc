// ITEM Made by player(寡言沉默:qmddami) /data/item/q/qmddami-sxxj.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Wed Jul 10 20:58:13 2013
#include <ansi.h>
#include <weapon.h>

inherit THROWING;
inherit F_ITEMMAKE;

void create()
{
	set_name("[5m[1;31m伤心小箭[2;37;0m", ({ "sxxj" }));
	set_weight(1800);
	set("item_make", 1);
	set("unit", "把");
	set("long", "这是由[1;35m神铁[2;37;0m炼制而成的一把暗器。
");
	set("makeinfo", "[1;31m
寡言沉默(qmddami)[1;31m 出品
[2;37;0m");
	set("value", 2100000);
	set("base_weight", 18);
	set("base_unit", "枚");
	set("base_value", 1);
	set("point", 400);
	set("max_consistence", 100);
	set("material", "magic stone");
	set("material_file", "/d/item/obj/butian");
	set("quality_level", 2);
	set("hand_msg", "[1;33m未见手动，只看伤心小箭突地握在手中。[2;37;0m\n");
	set("unhand_msg", "[1;35m伤心小箭哪里去了？[2;37;0m\n");
	if( !check_clone() ) return;
	restore();
	set_amount(100);
	init_throwing(apply_damage());

	setup();
}

string long() { return short() + query("makeinfo") + query("long") + item_long(); }
