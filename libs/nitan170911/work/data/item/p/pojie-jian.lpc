// ITEM Made by player(破姐:pojie) /data/item/p/pojie-jian.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Thu Jul 10 16:26:13 2014
#include <ansi.h>
#include <weapon.h>

inherit SWORD;
inherit F_ITEMMAKE;

void create()
{
	set_name("论剑[2;37;0m", ({ "jian" }));
	set_weight(9000);
	set("item_make", 1);
	set("unit", "柄");
	set("long", "这是由[1;35m神铁[2;37;0m炼制而成的一柄剑。
好贱[2;37;0m
");
	set("makeinfo", "[1;31m
破姐(pojie)[1;31m 出品
[2;37;0m");
	set("value", 2100000);
	set("point", 237);
	set("max_consistence", 100);
	set("material", "magic stone");
	set("material_file", "/d/item/obj/butian");
	set("quality_level", 2);
	set("wield_msg", "啊！我的眼睛[2;37;0m\n");
	set("unwield_msg", "眼睛又好了[2;37;0m\n");
	if( !check_clone() ) return;
	restore();
	init_sword(apply_damage());

	setup();
}

string long() { return short() + query("makeinfo") + query("long") + item_long(); }
