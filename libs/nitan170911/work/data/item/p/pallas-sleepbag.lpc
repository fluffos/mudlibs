// ITEM Made by player(雪飘飘:pallas) /data/item/p/pallas-sleepbag.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Tue Mar 26 00:58:53 2013
#include <ansi.h>
#include <armor.h>

inherit WAIST;
inherit F_ITEMMAKE;

void create()
{
	set_name("睡袋[2;37;0m", ({ "sleepbag" }));
	set_weight(1440);
	set("item_make", 1);
	set("unit", "条");
	set("long", "这是由[33m乌金丝[2;37;0m炼制而成的一条腰带。
");
	set("makeinfo", "[1;31m
雪飘飘(pallas)[1;31m 出品
[2;37;0m");
	set("value", 1500000);
	set("point", 114);
	set("max_consistence", 100);
	set("material", "silk");
	set("material_file", "/d/item/obj/wujins");
	set("quality_level", 2);
	set("wear_msg", "[33m$N[33m装备[33m$n[33m。[2;37;0m\n");
	set("remove_msg", "[33m$N[33m脱下了[33m$n[33m。[2;37;0m\n");
	if( !check_clone() ) return;
	restore();
	set("armor_prop/armor", apply_armor());

	setup();
}

string long() { return short() + query("makeinfo") + query("long") + item_long(); }
