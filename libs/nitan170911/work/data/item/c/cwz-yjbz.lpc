// ITEM Made by player(欧阳星云:cwz) /data/item/c/cwz-yjbz.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Sat Jul 27 21:37:38 2013
#include <ansi.h>
#include <weapon.h>

inherit WHIP;
inherit F_ITEMMAKE;

void create() {
  set_name("[1;37m研究无名鞭子[2;37;0m", ({ "yjbz" }));
  set_weight(15000);
  set("item_make", 1);
  set("unit", "根");
  set("long", "这是由[1;35m无名之物[2;37;0m炼制而成的一根鞭。
");
  set("makeinfo", "[1;31m
欧阳星云(cwz)[1;31m 出品
[2;37;0m");
  set("value", 34560000);
  set("point", 1723);
  set("max_consistence", 100);
  set("material", "no name");
  set("material_file", "/clone/goods/noname2");
  set("quality_level", 2);
  set("wield_msg", "[1;36m$N[1;36m一声清啸，抖出一根[1;36m$n[1;36m。[2;37;0m\n");
  set("unwield_msg", "$N随手一抹，收起了$n。\n");
  if (!check_clone()) return;
  restore();
  init_whip(apply_damage());

  setup();
}

string long() { return short() + query("makeinfo") + query("long") + item_long(); }
