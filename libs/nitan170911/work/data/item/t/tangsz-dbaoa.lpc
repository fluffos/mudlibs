// ITEM Made by player(唐三藏:tangsz) /data/item/t/tangsz-dbaoa.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Sun Oct 12 17:08:39 2014
#include <ansi.h>
#include <armor.h>

inherit HANDS;
inherit F_ITEMMAKE;

void create() {
  set_name("[1;36m五雷[1;35m合天幛[2;37;0m[2;37;0m", ({ "dbaoa" }));
  set_weight(4500);
  set("item_make", 1);
  set("unit", "双");
  set("long", "这是由[1;35m无名之物[2;37;0m炼制而成的一双铁掌。
[33m相传，盘古在开天辟地后，全身幻化为自然中的万物，那把辟开天地的巨斧，也分裂成几块。
后来此斧一块碎片在人间几经流转，后被修真界前辈高人炼化成法宝五雷合天幛，此法宝呈
褐金光芒。[2;37;0m
");
  set("makeinfo", "[1;31m
唐三藏(tangsz)[1;31m 出品
[2;37;0m");
  set("value", 35740000);
  set("point", 1782);
  set("max_consistence", 100);
  set("material", "no name");
  set("material_file", "/clone/goods/noname2");
  set("quality_level", 6);
  set("wear_msg", "[33m$N[33m装备[33m$n[33m。[2;37;0m\n");
  set("remove_msg", "[33m$N[33m脱下了[33m$n[33m。[2;37;0m\n");
  if (!check_clone()) return;
  restore();
  set("armor_prop/unarmed_damage", apply_damage());
  set("armor_prop/armor", 10);
  set("armor_prop/ap_power", 2.000000);
  set("armor_prop/research_effect", 6);
  set("armor_prop/double_damage", 2.000000);

  setup();
}

string long() { return short() + query("makeinfo") + query("long") + item_long(); }
