// ITEM Made by player(白石茉奈:bai) /data/item/b/bai-sleepbag.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Tue Sep 16 22:52:09 2014
#include <ansi.h>
#include <weapon.h>

inherit THROWING;
inherit F_ITEMMAKE;

void create() {
  set_name("睡袋[2;37;0m", ({ "sleepbag" }));
  set_weight(1350);
  set("item_make", 1);
  set("unit", "把");
  set("long", "这是由[37m精铁[2;37;0m炼制而成的一把暗器。
");
  set("makeinfo", "[1;31m
白石茉奈(bai)[1;31m 出品
[2;37;0m");
  set("value", 700000);
  set("base_weight", 13);
  set("base_unit", "枚");
  set("base_value", 1);
  set("point", 30);
  set("max_consistence", 100);
  set("material", "iron");
  set("material_file", "/d/item/obj/jingtie");
  set("quality_level", 2);
  set("hand_msg", "[1;36m$N[1;36m一声清啸，握着一把[1;36m$n[1;36m。[2;37;0m\n");
  set("unhand_msg", "$N随手一抹，收起了$n。\n");
  if (!check_clone()) return;
  restore();
  set_amount(100);
  init_throwing(apply_damage());

  setup();
}

string long() { return short() + query("makeinfo") + query("long") + item_long(); }
