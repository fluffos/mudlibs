// ITEM Made by player(流川枫:balll) /data/item/b/balll-taj.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Sun Oct 11 08:07:40 2015
#include <ansi.h>
#include <weapon.h>

inherit SWORD;
inherit F_ITEMMAKE;

void create() {
  set_name("[5m[1;31m龙渊[2;37;0m", ({ "taj" }));
  set_weight(9000);
  set("item_make", 1);
  set("unit", "柄");
  set("long", "这是由[1;35m神铁[2;37;0m炼制而成的一柄剑。
[1;36m这是远古传下来的名剑龙渊，鹿角手柄黄金吞口，三尺剑锋殷虹如雪、剑气逼人。[2;37;0m
");
  set("makeinfo", "[1;31m
流川枫(balll)[1;31m 出品
[2;37;0m");
  set("value", 2100000);
  set("point", 100);
  set("max_consistence", 100);
  set("material", "magic stone");
  set("material_file", "/d/item/obj/butian");
  set("quality_level", 2);
  set("wield_msg", "[1;36m$N[1;36m一声清啸，抖出一柄[1;36m$n[1;36m。[2;37;0m\n");
  set("unwield_msg", "$N随手一抹，收起了$n。\n");
  if (!check_clone()) return;
  restore();
  init_sword(apply_damage());

  setup();
}

string long() { return short() + query_makeinfo() + query("long") + item_long(); }
