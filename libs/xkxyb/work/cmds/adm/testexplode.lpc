int main()
{
        write("这个命令用来测试OS的explode设定。");
        write("请输入带分隔符“,”的字符串：");
        input_to("test");
        return 1;
}
int test(string z)
{

        string *x;
        int i,j;
        x= explode(z,",");
        i= sizeof (x);
        printf("total=%d:\n",i);
        for(j=0;j<i;j++){
                printf("j=%d,x=%s\n",j,x[j]);
        }
        return 1;
}
