#include <std.h>

inherit OBJECT;

void create() {
    ::create();
    set_name("key of demons");
    set_long("The key is shapes of that of a snake.");
    set_id(({ "key", "key of demons", "demon_event_key" }));
    set_short("Key of Demons");
}

