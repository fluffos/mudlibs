#include <std.h>
#include <dragonevent.h>

inherit BEACH;

void create() {
  ::create();
  set_properties((["light" : 2, "night light" : 1, "no teleport" : 1, "beach" : 1, "coords" : ({ 135, 15, 0 }) ]));
  set_short("a beach");
  set_long("This is a sandy beach. Huge cliffs rise to the east and south, blocking off all access to the land.");
  set_exits( ([
             ]) );
  set_items( ([
    "cliffs" : "Gigantic rocky ridges that loom far above everything else.",
             ]) );
  set_letter("m");
}
