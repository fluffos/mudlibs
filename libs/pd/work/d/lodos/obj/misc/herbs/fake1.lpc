#include <std.h>
inherit OBJECT;

void create() {
 ::create();
   set_name("herbs");
   set_short("some rare looking herbs");
   set_long("The herbs have long, dark green stems and leaves with "
     "round black bulbs at the bases of each. There are five black "
     "bulbs total. Several grey vines hold the 
}
