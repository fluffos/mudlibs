#include <std.h>
#include <aziraphale.h>

inherit VIRTUALROOM;

void create() {
  set_server(KEEPROOM "zforest/pathvserver.lpc");
  ::create();
  set_exits(([
    "west": KEEPROOM "zforest/x9y12.lpc",
    "southeast": KEEPROOM "zforest/x10y14.lpc",
  ]));
}
