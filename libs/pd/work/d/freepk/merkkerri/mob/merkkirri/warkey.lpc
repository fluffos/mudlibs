#include <mjungle.h>
#include <std.h>
inherit OBJECT;

void create()  {
::create();
set_name("warlord's key");
set_short("warlord's key");
set_long("This key unlocks the door to Samul Ku'Thull's chamber.");
set_id(({"key", "warlord's key"}));
set_weight(2);
set_value(0);
}
