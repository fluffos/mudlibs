#include <std.h>
#include <lost.h>

inherit VIRTUALROOM;

void create() {
     set_server(SERVER);
   ::create();
     set_exits(([
        "east"      : ROOMS "lost_field001.lpc",
        "west"      : ROOMS "lost_field018.lpc",
        "south"     : ROOMS "lost_field003.lpc",        
      ]));
}
