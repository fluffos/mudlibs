#include <std.h>
#include <gcity.h>
#include <mjungle.h>

inherit ROOM;

nosave void create()
{
    ::create();
    set_short("%^YELLOW%^Magic Shop%^");
    set_long("%^YELLOW%^This shop is where the Merkkirri come to buy magic goods and %^YELLOW%^in particular, magic stones.  A sign lists what is available.%^RESET%^");

    set_exits ( ([ "south" : GCITY "gcity5.lpc" ]) );
    set_properties( ([ "no attack":1,
	"no magic": 1,
	"no bump": 1,
	"light":2,
	"night light":2 ]) );
}

void reset()
{
    ::reset();
    if(!present("gracken")) {
	new(GMOB "gracken.lpc")->move(this_object());
    }
}
