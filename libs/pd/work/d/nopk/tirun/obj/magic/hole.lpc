// To be finished when i have time

#include <std.h>
#include <sindarii.h>
inherit OBJECT;

void create() {
 ::create();
   set_name("hole");
   set_short("a portable hole");
   set_long("At first glance, a black circular sheet of cloth, the "
      "portable hole is a strange magical item, that once set down, "
      "will create a hole in the ground. That hole may be concealed, "
      "revealed, opened, and closed.");
   set_weight(5);
   set_value(200);
}
