#include <std.h>
#include <arlon2.h>
inherit VIRTUALROOM;
void create() {
  set_server(SERVER);
  ::create();

  set_exits(([
    "west" : ROOMS + "roads/60.lpc",
    "east" : ROOMS + "roads/1.lpc",
    "north" : ROOMS + "roads/37.lpc",
    "south" : ROOMS + "roads/36.lpc",
  ]));
}
