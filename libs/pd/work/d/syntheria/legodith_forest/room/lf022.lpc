#include <std.h>
#include <legodith_forest.h>

inherit VIRTUALROOM;

void create() {
    set_server(SERVER);
    ::create();
    set_exits( ([ 
        "south"       : ROOMS "lf023.lpc",
        "northwest"   : ROOMS "lf027.lpc",
        "northeast"   : ROOMS "lf018.lpc"
    ]) );
}
