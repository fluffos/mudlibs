#include <std.h>
#include <legodith_forest.h>

inherit VIRTUALROOM;

void create() {
    set_server(SERVER);
    ::create();
    set_exits( ([ 
        "north"        : ROOMS "lf010.lpc",
        "south"        : ROOMS "lf008.lpc",       
        "west"         : ROOMS "lf012.lpc",
    ]) );
}
