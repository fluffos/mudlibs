#include <std.h>
inherit MONSTER;

void create() {
 ::create();
   set_name("Penis");
   set_short("The Masked Penis from Venus");
   set_long("This is a masked penis from Venus.  It is looking for something to feed upon... Maybe it will choose you!");
   set_id(({ "penis, masked, monster, masked penis from venus" }));
set_id(({ "penis", "masked", "monster" }));
   set_level(7);
   set_race("penis");
   set_body_type("human");
   set_gender("male");
}
