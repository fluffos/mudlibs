eval string *files, data; files = this_player()->wild_card("*"); for(int i=0; i < sizeof(files); i++) { write(files[i]); data = read_file(files[i]); if(data) { replace_string(data, "^M", ""); write_file(files[i], data, 1); } }
