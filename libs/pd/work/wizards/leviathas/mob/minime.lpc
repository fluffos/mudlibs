#include <std.h>
inherit MONSTER;

void create() {
::create();
    set_name("minime");
    set_short("Mini Me");
    set_long("Mini Me is short and cute.");
    set_id(({"minime", }));
    set_level(5);
    set_race("midgit");
    set_body_type("human");
    set_gender("neuter");

    add_money("gold", 10000);     
    add_money("platinum", 5000);

    set_skills(({"attack", 2, "melee", 2,);    
    set_stats("constitution", 50, "strength", 2,);  

     
  }
