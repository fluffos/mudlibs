_marry.c

Fixes a small error with the help file where two newline characters were printed inbetween each line.
It also seems like this should be in the abilities directory and not the high mortal directory, seeing as how only clergy can do it.
Original version retrieved from  /cmds/hm/_marry.c  on  7-16-08
