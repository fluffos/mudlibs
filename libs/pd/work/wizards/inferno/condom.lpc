#include <std.h>
inherit ARMOUR;

void create() {
 ::create();
   set_name("condom");
   set_short("a condom");
   set_long("For those travelers who are not quite so battle scared and "
     "still have a certain limb. Use before September 1832.");
   set_weight(1);
   set_limbs(({ "penis" }));
   set_ac(1500);
}
