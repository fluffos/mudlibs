#include <std.h>
inherit ROOM;

void create() {
 ::create();
   set_properties(([ "forest" : 1, "outdoors" : 1, "mountains" : 1,
                     "light" : 2 ]));
   set_short("In a strange forest");
   set_day_long("The trees rest firmly in the ground, silent and watching. "
     "
   set_night_long(
   set_items(([
   ]));
}
