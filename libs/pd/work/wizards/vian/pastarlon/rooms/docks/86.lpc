#include <std.h>
#include <pastarlon.h>
inherit VIRTUALROOM;
void create() {
  set_server(DOCKSERVER);
  ::create();
  set_exits(([
    "northwest": ROOMS + "docks/95.lpc",
    "east": ROOMS + "docks/87.lpc",
    "southwest": ROOMS + "docks/85.lpc",
  ]));
}
