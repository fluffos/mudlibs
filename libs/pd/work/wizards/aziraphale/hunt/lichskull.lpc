#include <std.h>
inherit OBJECT;
void create()
{
::create();
set_id(({"skull", "lich skull"}));
set_name("a lich's skull");
set_long("\n");
set_short("skull of a lich");
set_weight(0);
set_value(0);
}
