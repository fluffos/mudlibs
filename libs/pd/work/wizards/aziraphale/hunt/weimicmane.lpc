#include <std.h>
inherit OBJECT;
void create()
{
::create();
set_id(({"mane", "weimic mane",}));
set_name("weimic's mane");
set_long("\n");
set_short("the mane of a weimic");
set_weight(0);
set_value(0);
}
