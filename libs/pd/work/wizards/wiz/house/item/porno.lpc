#include <std.h>
inherit OBJECT;

void create() {
:: create();

   set_name("A porno DVD");
   set_short("A porno movie on DVD");
   set_long("A pornographic film entitled Horny College Girls.\n"
            "This surely must belong to wiz!");
   set_id(({"porno", "dvd"}));
   set_mass(1);
   set_value(0);
}
