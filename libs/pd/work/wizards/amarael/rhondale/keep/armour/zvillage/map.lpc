#include <std.h>
#include <aziraphale.h>
inherit OBJECT;

void create() {
   ::create();
     set_id(({"map", "mapxzor"}));
     set_name("map");
     set_long("This is a map that has been rolled up and bound with a leather cord, which for some reason you are unable to untie.");
     set_short("a map");
     set_weight(0);
     set_value(0);
}
