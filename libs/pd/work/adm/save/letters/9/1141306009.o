#/daemon/letter.c
__Letter "Yes, u are right.  Imagine this.  Array's are mappings that their \"key\" is set to the allocated space that they exist at.  So, array[0] will return whatever information is in the 0 spot.For mappings, it uses \"keys\".  So, instead of calling var[0], you would call var[\"name\"].And yes, mappings can be nested into eachother, as well as arrays can be nested into mappings and vice-versa.Whit"
__Undeleted ({"ironman",})
