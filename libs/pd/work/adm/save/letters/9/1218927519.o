#/daemon/letter.c
__Letter "can you change /std/living/combat.cline 156so that the line      message(\"my_combat\", sprintf(\"Supernatural forces prevent you \"        \"from attacking %s.\", (string)victim->query_cap_name()),is only printed if(victim)I made a monster that kicks off an event when you try to attack it, which also ends up destructing the instance, which means that the monster no longer exists at that point."
__Undeleted ({"nulvect",})
