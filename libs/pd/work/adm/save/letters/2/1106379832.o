#/daemon/letter.c
__Letter "namespace CommissionSales{    class CommissionSales    {        static void Main(string[] args)        {            // Declare array of type integer            // Initialize elements of array            int[] grossSales = { 636, 931, 399, 164, 944, 799, 141, 898, 246, 135 };            int total = 0;  // Accumulator            // Set the weekly sales for employees            for (int count = 0; count < grossSales[count]; count++)            {                total += (int)(.09 * grossSales[count]) + 200;            }        }    }}"
__Undeleted ({"nulvect",})
