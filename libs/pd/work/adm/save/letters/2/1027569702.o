#/daemon/letter.c
__Letter "#include <std.h>inherit DAEMON;mapping players;players = ([]);int cmd_tstats(string str){   string *players;   players = keys(mapping);   i = sizeof(players);      if(!str)    players = ([\"windwillow\" : 2, \"hermes\" : 1]);   while(i--) {   write( players[i] + \": \" + mapping[players[i]]); }}"
__Undeleted ({"nightshade",})
