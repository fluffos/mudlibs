#/daemon/letter.c
__Letter " if (sscanf(str, \"*.%s\", str) == 1) {     str = get_dir(this_player()->query_cwd()+\"*.\"+str);     i = sizeof(str);     while (i--)     {       message(\"system\",deep[i]+\": \"+((res=catch(call_other(deep[i],\"???\"))) ?          res : \"updated and loaded.\"), this_player());        }    }I don't have a files or ext variable so used str."
__Undeleted ({"nulvect",})
