#/daemon/letter.c
__Letter " static void Main(string[] args)        {            int num,                digit = 0,                encryptNum = 0;            // Read number and convert to integer            Console.Write(\"Enter a four-digit integer: \");            num = Int32.Parse(Console.ReadLine());            for (int power = 0; power < 4; power++)            {                switch (power)                {                    case 0:                        digit = (num / 10) % 10;                        break;                    case 1:                        digit = num;                        break;                    case 2:                        digit = (num / 1000) % 10;                        break;                    case 3:                        digit = (num / 100) % 10;                        break;                }                digit = (digit + 7) % 10;                encryptNum += digit * (int)Math.Pow(10, power);            }"
__Undeleted ({"nulvect",})
