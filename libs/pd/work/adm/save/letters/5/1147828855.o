#/daemon/letter.c
__Letter "Take a look at this, see if you can figure it outIt should sort in descending order.int do_sort(mixed, mixed);mixed main(){  mapping x = ([ \"nesoo\" : ([ \"quiz\" : 0, \"score\" : 2 ]), \"iron\" : ([ \"quiz\" : 0, \"score\" : 0 ]), \"daboura\" : ([\"quiz\" : 1, \"score\" : 5]) ]);  mixed y;  mixed z = ({});  int i = sizeof(x);  string* key = keys(x);  while(i--)  {    z += ({ ({key[i], x[key[i]][\"quiz\"], x[key[i]][\"score\"]}) });  }  y = sort_array(z, \"do_sort\");  return y;}int do_sort(mixed a, mixed b){  if( a[2] > b[2] )    return -1;  else if( a[2] == b[2] )    return 0;  else    return 1;}You can test it out on builders, just put it up somewhere and \"call <filename>;main\" to run it."
__Undeleted ({"ironman",})
