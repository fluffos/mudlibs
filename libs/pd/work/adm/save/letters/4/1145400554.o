#/daemon/letter.c
__Letter "You can use PLAYER_D->query_monster(#); to get what appears to be the name of the monster. You'll have to #include <daemohs.h> to use PLAYER_D.Example:int* x = this_player()->query_kills();string mname = PLAYER_D->query_monster(x[0]);write(\"The first monster in your kills list is \" + mname + \".\");"
__Undeleted ({"nesoo","ironman",})
