#/daemon/letter.c
__Letter "You may have the code, as long as it allows admins to also come into the room.  For example if(!archp(this_player()) && this_player()->query_name != \"pyro\" && this_player()->query_name() != \"azuriel\") {Whit"
__Undeleted ({"pyro",})
