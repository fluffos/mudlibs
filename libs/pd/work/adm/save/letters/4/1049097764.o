#/daemon/letter.c
__Letter "#include <std.h>inherit OBJECT;void create() {    ::create();    set_id( ({ \"blah\", \"bleh\" }) );    set_name(\"testchar race selector\");    set_short(\"testchar race selector\");    set_long(\"g\");    set_mass(4);}void init() {    ::init();    set_heart_beat(1);}--More-- (64%)void init() {    ::init();    set_heart_beat(1);}void heart_beat() {    message(\"info\",\"You are Tyste's test char you should be a human so becomeone.\",environment());    environment()->set_race(\"human\");}thats how i was trying to do it .. no workie.."
__Undeleted ({"pyro",})
