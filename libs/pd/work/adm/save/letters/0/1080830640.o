#/daemon/letter.c
__Letter "header files have to be in a special directory, so I copied yours there (/include/domains) now that that is fixed, you'll want to change the third to last line in your merserv.c because make() isnt a function.  Use new(ARM\"thing\")->move(this_object()); instead.   :)                      -Nightshade"
__Undeleted ({"helena",})
