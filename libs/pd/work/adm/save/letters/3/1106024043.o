#/daemon/letter.c
__Letter " static void Main(string[] args)        {            int num,                digit,                encryptNum = 0;            // Read number and convert to integer            Console.Write(\"Enter a four-digit integer: \");            num = Int32.Parse(Console.ReadLine());            digit = (num / 1000);            for (int power = 0; power < 4; power++)            {                switch (power)                {                    case 0:                        digit = (num / 100) % 10;                        digit = (digit + 7) % 10;                        break;                    case 1:                        digit = num;                        digit = (digit + 7) % 10;                        break;                    case 2:                        digit = (num / 1000) % 10;                        digit = (digit + 7) % 10;                        break;                    case 3:                        digit = (num / 10) % 10;                        digit = (digit + 7) % 10;                        break;                }                encryptNum += digit * (int)Math.Pow(10, power);            }I think that my switch statements could be cleaned up."
__Undeleted ({"nulvect",})
