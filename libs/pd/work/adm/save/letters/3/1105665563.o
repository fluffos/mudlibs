#/daemon/letter.c
__Letter "#include <iostream>#include <stdlib.h>using namespace std;int main(int argc, char *argv[]){    int height; // height of triangles    int width; // width of single triangle/space        for(height = 1; height <= 10; height++)    {            // triangle A            for( width = 0; width < height; width++ )            {                cout << \"*\";            }                        // Space A            for( width = 0; width < (11 - height); width++ )            {                cout << \">\";            }                        // triangle B -- note: same as Space A            for( width = 0; width < (11 - height); width++ )            {                cout << \"*\";            }                        // Space B -- note: same as triangle A, but doubled            for( width = 0; width < ((height * 2) - 1) ; width++ )                cout << \">\";                        // triangle C -- Exactly the same as B            for( width = 0; width < (11 - height); width++ )            {                cout << \"*\";            }                        // Space C -- Exactly the same as C            for( width = 0; width < (11 - height); width++ )            {                cout << \">\";            }                        // triangle D -- Same as A            for( width = 0; width < height; width++ )            {                cout << \"*\";            }                        cout << endl;    }        system(\"PAUSE\");	    return 0;}"
__Undeleted ({"daos",})
