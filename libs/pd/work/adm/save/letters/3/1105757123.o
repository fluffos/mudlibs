#/daemon/letter.c
__Letter "static void Main(string[] args){int num,// Number to encryptdigit = 0,// Digit of numberpower,// CounterencryptNum = 0; // Accumulator// Read four digit number and convert to integerConsole.Write(\"Enter a four digit number: \");num = Int32.Parse(Console.ReadLine());for (power = 0; power < 4; power++){digit = (digit + 7) % 10;}encryptNum += digit;Console.WriteLine(\"The number encrypted is \" + encryptNum);}}"
__Undeleted ({"nulvect",})
