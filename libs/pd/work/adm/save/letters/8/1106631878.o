#/daemon/letter.c
__Letter "using System;namespace Duplicate{	/// summary	/// Summary description for Duplicate.	/// Author	Jimmie Tryon	/// Date	24 JAN 2005		/// Displays if a number is a duplicate of a number.	/// /summary	class Duplicate	{		STAThread		static void Main(string[] args)		{			int num = 0;			Console.Write(\"Enter 3 numbers: \");			for (int count = 0; count < 3; count++)			{				while (num  10 && num  100)				{					Console.WriteLine(\"Number must be between 10 and 100.\");					Console.Write(\"Enter another number: \");				}			} 		}	}}"
__Undeleted ({"nulvect",})
