#/daemon/letter.c
__Letter "//basically it needs to work like this. if player types 'take stock', then theytake poisoned food. but, if they type 'take ingrediant', then they takeantidote food. daos said to do it as if take blah1 then do this, else do this.and i'm not that comfy with else atm :-\\void init() {  ::init();    add_action(\"take\", \"take\");}int take(string str) {  if(str)  if(str == \"stock\")  {    stock();  }  if(str == \"ingredients\")  {    ingredients();  }  return 0;}int stock() {    new(ITEMS\"bfood\")->move(this_player());    write(\"You take a piece of stock.\");    return 1;}int ingredients() {    new(ITEMS\"<the ingredient file>\")->move(this_player());    write(\"You some of the ingredients.\");    return 1;}"
__Undeleted ({"helena",})
