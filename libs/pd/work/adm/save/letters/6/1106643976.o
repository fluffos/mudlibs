#/daemon/letter.c
__Letter "namespace Duplicate{    class Duplicate    {        static void Main(string[] args)        {            int [] num;            num = new int[3];                        Console.Write(\"Enter 3 numbers: \");            for (int count = 0; count < 3; count++)            {                num[count] = Int32.Parse(Console.ReadLine());                while (num[count] < 10 || num[count] > 100)                {                    Console.WriteLine(\"Number must be between 10 and 100.\");                    Console.Write(\"Enter another number: \");                    num[count] = Int32.Parse(Console.ReadLine());                }                for (int count = 0; count < 3; count++)                {            }            for (int count = 0, flag = 0; count < 3; count++)            {                if (num[count] == num)                {                    flag = 1;                }                else                {                    flag = 0;                }            }                            }    }"
__Undeleted ({"nulvect",})
