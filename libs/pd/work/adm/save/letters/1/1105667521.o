#/daemon/letter.c
__Letter "	static void Main(string[] args)		{			int lineCount,		// Line counter				count,			// Counter for asterisks				spacesCount;	// Counter for spaces			for (lineCount = 1; lineCount <= 10; lineCount++)			{				// Pattern A				for(count = 0; count < lineCount; count++)				{					Console.Write(\"*\");						}				for (spacesCount = 0; spacesCount < (11 - lineCount); spacesCount++)				{					Console.Write(\">\");					// Pattern B				}				for (count = 0; count < (11 - lineCount); count++)				{					Console.Write(\"*\");				}				for (spacesCount = 0; spacesCount < ((lineCount * 2) - 1); spacesCount++)				{					Console.Write(\">\");				}											// Pattern C				for (count = 0; count < (11 - lineCount); count++)				{					Console.Write(\"*\");				}				for (count = 0; count < (11 - lineCount); count++)				{					Console.Write(\">\");				}				// Pattern D				for (count = 0; count < lineCount; count++)				{					Console.Write(\"*\");				}				Console.WriteLine();			}		}	}"
__Undeleted ({"nulvect",})
