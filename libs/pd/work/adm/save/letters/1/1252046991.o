#/daemon/letter.c
__Letter "You would do something like this:  for (int i = sizeof(children(YOUR_AREA_MOBS+\"someguy\")); i > 0; i--)    new(YOUR_AREA_MOBS+\"someguy\")->move(this_object());Hope that helps."
__Undeleted ({"randalar",})
