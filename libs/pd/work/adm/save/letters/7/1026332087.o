#/daemon/letter.c
__Letter "could this be it?  if(Uses > 1)    str += \" uses left)\";  else    str += \" use left)\";  return str;}as the blank uses and this:  if(Uses > 1)    str += \" uses left)\";  else    str += \"1 use left)\";  return str;}being set for one use left? since on the If(Uses >1) is searching for everything above one and its saying if its at one to say blank uses."
__Undeleted ({"nightshade",})
