#/daemon/letter.c
__Letter "ed robes.ca//death robes#include <std.h>inherit ARMOUR;int wear_it();int unwear_it();void create() {     ::create();     set_name(\"robe\");     set(\"id\", ({ \"robe\", \"deathrobe\", \"robes\" }) );     set(\"short\", \"%^BOLD%^%^BLACK%^A long black robe\");     set(\"long\", \"This is a very long black robe made completly out of black materials. The strength of death itself seems to rap itself around the cloak denying death to the wearer\");     set_ac(999999999);     set_limbs( ({ \"torso\", \"head\", \"right arm\", \"left arm\", \"right leg\", \"leftleg\", \"right hand\", \"left hand\"}) );     set_weight(6);     set_curr_value(\"gold\", 0);     set_type(\"robes\");     set_wear((: wear_it() :));     set_remove((: unwear_it() :));}int wear_it() {	if(this_living()->query_name() == \"death\" && (string)query_wielded()->query_level() == 400) return 1;	write(\"As you put on the robe, death seems to seep out of the robe and into your hand.\");	this_player()->add_hp(-80);	write(\"deaths grip loosens on you for a second, and you quickly throw the robe to the ground.\");	this_player()->force_me(\"drop robe\");	return 0;}int unwear_it() {	if((string)query_wielded()->query_name() == \"death\" && (string)query_wielded()->query_level() == 400) return 0;	write(\"As you touch the robe, death seems to seep out of the robe and into your hand.\");	this_player()->add_hp(-80);	write(\"deaths grip loosens on you for a second, and you quickly throw the robe to the ground.\");	this_player()->force_me(\"drop robe\");	return 1;}"
__Undeleted ({"wolfspirit",})
