=======================================================================

SYNTAX :: check_ids( string targ );

This sfun uses the string passed as the variable targ to locate
the object that has the ID that matches. If there is a match, the
object's filename and path are returned.

As of this time, the function is only present here on builders.
