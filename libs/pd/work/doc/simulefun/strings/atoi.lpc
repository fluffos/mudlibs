===========================================================================

SYNTAX :: atoi( target );

This sfun converts a string into an int. It will not work if the string
that was passed into it contains non-intergers.
