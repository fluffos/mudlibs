SYNTAX ansi_item( <object> );

This sfun will check the the maximum HP and current hp of a living
object. It will then return a string containing a color code 
depending on the percentage of remaining HP.
