====================================================================

SYNTAX :: copy( mixed targets );

This sfun will make a duplicate version of the mapping or array
that you passed to it as an arguement. The returned value will
have no pointers to the mapping/target stored in "targets".
