
==========================================================================

%^RED%^%^BOLD%^Syntax ::%^RESET%^ query_touches()

This function is contained in all objects. It will return all the
touches contained in the object. The information will be returned in
mapping format ([]).

see also ::

set_touch , set_touches , remove_touches , remove_touch

