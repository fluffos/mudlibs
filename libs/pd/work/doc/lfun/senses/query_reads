
==========================================================================

%^RED%^%^BOLD%^Syntax ::%^RESET%^ query_reads()

This function is contained in all objects. It will return all the
reads contained in the object. The information will be returned in
mapping format ([]).

see also ::

set_read , set_reads , remove_reads , remove_read

