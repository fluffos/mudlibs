Nightmare Mudlib 3.2 SimulEfun: admp()

int admp(object);

DESCRIPTION
Checks to see if an object in fact is an administrator (member  
of the superuser group) as defined in security.h  Some admin 
commands make checks to see if the person issuing the command 
is an admin with superuser priviledges.

LOCATION
/adm/simul_efun/admp.c

SEE ALSO
ambassadorp(), high_mortalp(), wizardp(), archp()
