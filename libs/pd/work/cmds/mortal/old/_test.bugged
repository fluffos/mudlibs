#include <std.h>


int cmd_test()
{
 object *all;
 int a, b;
 string outp;
  a = sizeof(all = all_inventory( environment( this_player() ) ));
  if (!all) return 1;
  for(b=0; b<a; b++)
    {
     outp = arrange_string(
    }
}

