//密洞内 
// by steel 
// change by wind
#include "ansi.h"
inherit ROOM;
//#include "mimi.h"
void create()
{
        object gold;
        set("short", "内洞");
        set("long", @LONG
        这里就是堆放天下密宝的密洞了!!快拿吧!    
洞顶刻的有字.(zi)
LONG
        );
set("valid_startroom", 1);
set("no_clean_up",1);
set("no_quit",1);
        set("exits", ([
                "north" : __DIR__"midong",
                ]));
    set("item_desc",([
                "zi" : HIR "
                   人     鸟
                   为     为 
                   财     食
                   死     亡 
                   \n\n"NOR,
        ]));   

   
        set("objects",([
            __DIR__"obj/cw_armor":1,
            __DIR__"obj/cw_boots":1, 
            __DIR__"obj/cw_cloth":1,
            __DIR__"obj/cw_hat" :1,
            __DIR__"obj/book-blade":1,
            __DIR__"obj/book-parry":1,
                     __DIR__"npc/corpse":5,
//            "/clone/money/diamond" :1,
//          "/clone/money/feicui" :1,
//            "/clone/money/gold":1,       
        ]) );
 setup();
//  gold=present("gold",this_object());
//  gold->set_amount(50);
// gold=present("diamond",this_object());
//   gold->set_amount(500);
//gold=present("feicui",this_object());
//   gold->set_amount(500);
} 

int valid_leave(object me, string dir)
{
        int num_dia,num_fei,total_steps;
        object gold,feicui;
       //  total_steps = 20;

 gold=present("diamond",this_object());
//if(gold) num_dia=gold->query_amount();
//feicui=present("feicui",this_object());
//if(feicui) num_fei=feicui->query_amount();
// if(!gold || !feicui || num_dia!=500 || num_fei!=500)
//if (!gold || !feicui)
//{
// tell_object(this_player(),"山洞突然坍塌,你未能幸免.\n");
// this_player()->die();     
//            return notify_fail(HIR"你死了。\n"NOR);
//             }
//if (num_dia<499 || num_fei< 499)
//{
// tell_object(this_player(),"山洞突然坍塌,你未能幸免.\n");
// this_player()->die();
//            return notify_fail(HIR"你死了。\n"NOR);
//             }
//  addmimi(this_player(),"ne",1024,"宝藏");
        return ::valid_leave(me,dir);
}
