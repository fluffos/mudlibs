// Placeholder global-include file. This archive shipped no dedicated
// mudlib-wide header (no include/config.h or similar) -- this driver
// requires SOME real, readable file for the "global include file"
// config key (omitting it entirely produces a phantom
// "Cannot read #include file" on every single compile; see this
// project's AGENTS.md, basis lib's onboarding notes). Added purely as
// conversion-tooling infrastructure, not original archive content.
