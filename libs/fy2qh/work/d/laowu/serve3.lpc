inherit ROOM;
void create() {
  set("short", "小客栈");
  set("long", @LONG
简陋的客厅当中供着一个手捧金元宝的财神爷，后面的一扇门上
，挂着已洗的发白的蓝布棉门帘，上面还贴着张斗大的红“喜”字，
无论谁走进这里，都可以看得出这地方的主人，一定是整天在做着发
财梦的穷小子。
LONG
  );
  set("exits", ([
    "east": __DIR__ "serve32",
    "south": __DIR__ "binghe4",
  ]));
  set("valid_startroom", 1);
  set("objects", ([
    __DIR__ "npc/waiter.lpc": 1,
  ]));
  setup();
  replace_program(ROOM);
}
