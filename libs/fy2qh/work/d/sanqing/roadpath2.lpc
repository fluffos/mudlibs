inherit ROOM;

void create() {
  set("short", "青石小径");
  set("long", @LONG
这是一条幽静的小道，西面直通三清宫的藏经阁，地上长满了青
苔，稍不留意就会滑倒，不过轻功高的人或许能安然度过。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "southeast": __DIR__ "roadpath",
    "north": __DIR__ "roadpath3",
  ]));
  setup();
  replace_program(ROOM);
}
