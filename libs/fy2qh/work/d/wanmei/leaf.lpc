// token.c


inherit ITEM;

void create() {
  set_name("枯黄的叶子", ({ "leaf" }));
  set_weight(10);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "片");
    set("long", "一片枯黄的叶子。\n");
    set("material", "paper");
    set("value", 0);
  }
  setup();
}
