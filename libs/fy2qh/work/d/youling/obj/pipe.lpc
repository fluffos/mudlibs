// token.c


inherit ITEM;

void create() {
  set_name("铜哨", ({ "pipe" }));
  set_weight(10);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "个");
    set("long", "一根约两寸多长的铜哨。\n");
    set("material", "wood");
    set("value", 0);
  }
  setup();
}
