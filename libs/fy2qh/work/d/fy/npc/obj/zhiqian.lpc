// token.c


inherit ITEM;

void create() {
  set_name("纸钱", ({ "zhiqian", "money" }));
  set_weight(10);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "捆");
    set("long", "一捆祭祀用的纸钱。\n");
    set("material", "paper");
    set("value", 1000);
  }
  setup();
}
