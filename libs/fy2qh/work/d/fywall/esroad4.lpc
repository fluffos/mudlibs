inherit ROOM;
void create() {
  set("short", "角楼");
  set("long", @LONG
这里是风云城的角楼，从这里可以通向风云城的南门和东门，这
里是战略功防的要地，一般人等是不许登临的。角楼有一丈来高，几
个值勤兵四散巡逻着，防止闲杂人等蓄意进行破坏。墙角堆着一些火
炮丸，看来是军用物资，你在这里可要小心了，万一被值勤兵发现，
后果不堪设想。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "esroad5",
    "north": __DIR__ "esroad3",
  ]));
  set("objects", ([
    __DIR__ "npc/duty_solider": 3,
    __DIR__ "obj/bomb": 3,
  ]));
  set("outdoors", "fengyun");
  setup();
}
