inherit ROOM;

void create() {
  object con, item;
  set("short", "书房");
  set("long", @LONG
这里是一间摆满了书的书房，书虽多却并不嫌乱，看来侍女经常
整理这里，这里左右各放着几个红木书架，书架上放的都是些武功秘
籍。
LONG
  );
  set("exits", ([
    "east": __DIR__ "shuimu4",
  ]));
  set("objects", ([
    __DIR__ "npc/gongnu2": 1,
  ]));
  setup();
  replace_program(ROOM);
}
