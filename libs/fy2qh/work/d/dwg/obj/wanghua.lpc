// bag.c

inherit ITEM;

void create() {
  set_name("忘魂花", ({ "wanghua" }));
  set_weight(500);
  set_max_encumbrance(100);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "朵");
    set("long", "一棵碧绿的草\n");
    set("value", 1);
  }
}

int is_container() { return 1; }
