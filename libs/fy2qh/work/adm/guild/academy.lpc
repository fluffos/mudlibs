// Room: /adm/guild/academy.c

inherit ROOM;

void create() {
  set("short", "巫师学院");
  set("long", @LONG
这里是巫师们研究 LPC 的地方，如果你是新手巫师，欢迎来到
这里和其他人互相讨论，如果你是老手，也请不吝回答新手的问题。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "guildhall",
  ]));

  setup();
  load_object("/daemon/board/lpc");
  replace_program(ROOM);
}
