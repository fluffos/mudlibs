// moon_b.c

inherit BULLETIN_BOARD;

void create()
{
        set_name("三界留言板", ({ "board" }) );
      set("location", "/d/sanjie/langerdian1");
        set("board_id", "sjsboard");
        set("long",     "三界留言板。\n" );
set("capacity", 80);
        setup();
        replace_program(BULLETIN_BOARD);
}

