// honglou_b.c by none on 96/10/25

inherit BULLETIN_BOARD;

void create()
{
        set_name("小天的留言板", ({ "board" }) );
        set("location", "/u/ltzn/workroom.c");
        set("board_id", "ltzn_b");
        set("long",     
"如果你对我这个wiz有什么意见的话可以在这里留言，和我交流）\n" );
        setup();
        set("capacity", 30);
        replace_program(BULLETIN_BOARD);
}
