// wizard_b.c

inherit BULLETIN_BOARD;

void create()
{
        set_name("密码取回板", ({ "board" }) );
        set("location", "/d/wiz/mima");
        set("board_id", "mima_b");
	set("long",
                "这是一个的板子， post。\n" );
	setup();
	set("capacity", 150);
	replace_program(BULLETIN_BOARD);
}
