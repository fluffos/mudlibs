// Obj: /obj/wiz/cyl_sword#70747.c 
 
inherit ITEM; 
 
void create() 
{ 
	set_name("霹雳旋风剑", ({"obj"}));
	set("long", @LONG
这是一个不值钱的废物。

LONG
	);
	set("id", "obj");
	set_weight(1);

	setup();
}
