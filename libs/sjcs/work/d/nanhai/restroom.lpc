// Room: /d/nanhai/restroom.c

inherit ROOM;

void create()
{
	set("short", "休息室");
	set("long", @LONG

这里是普陀山的休息室，屋子里空荡荡的什么也没有，不知普陀
山门人弟子们是怎么样睡觉的。
LONG
	);
	set("light_up", 1);
	set("sleep_room", 1);
	set("no_fight", 1);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"zoulang2",
]));
	set("no_magic", 1);

	setup();
}
