inherit ROOM;

int block_cmd(string);
void create ()
{
 set ("short", "三界传说美容院");
set ("long", @LONG

坐落在长安城内的一家超时空的现代化美容院，里面装备齐全，是你
美容变性花钱的好去处！
LONG);

  set("exits", ([ /* sizeof() == 1 */
"north" : "/d/city/kezhan",
]));
 set("objects",([
//"/d/city/npc/meirong":1,
//   "//d/city/npc/tonggang":1,
//     /d/city/npc/sanpei":1,
 ]));
      set("no_time", 1);
    set("no_fight", 1);
  set("no_clean_up", 1);
      set("no_exert", 1);
      set("no_magic", 1);

  setup();
}

void init()
{
    add_action("block_cmd", "", 1);
}

int block_cmd(string args)
{
string verb;
verb = query_verb();
    if (verb == "bian") return 1;
    return 0;
}
