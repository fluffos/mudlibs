// created by snowcat.c 10/21/1997
// room: /d/cith/lichun.c

inherit ROOM;

void create ()
{
    set ("short", "丽春院");
  set ("long", @LONG

长安城里最大的娱乐场所，客栈老板在后院建的，院门漆成大彩红色，
方便了江湖上的单身侠士们，老板也赚了大钱，装饰豪华，非一般人
能进。近来，传闻这里有邪气，显得越发神秘。
LONG);

  set("exits", ([
    "west"         : __DIR__"kezhan",
      ]));
  set("objects", ([
          "/d/city/npc/sanpei"   : 0,
      ]));

  setup();
}

