// Room: workroom.c

inherit ROOM;

void create()
{
        set("short", "异次元空间");
        set("long", @LONG
你飞到了异次元空间,你现在要往哪里去呢???请选择一下吧!!
请注意一点里面的怪都很厉害，不要轻易的去尝试！
后悔了吗？后悔了你现在还可以回去！
南城客栈 -kz  空间 -n.s.e.w
LONG
        );
        set("no_kill",1);
        set("no_dazuo",1);
        set("no_fight",1);
         set("no_steal",1);
         set("pingan",1);
         set("no_beg",1);       
set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"kongjian1",
  "west" : __DIR__"kongjian1",
   "up":"d/xingji/shiluodao/md",
  "east" : __DIR__"kongjian1",
  "south" : __DIR__"kongjian1",

                "kz" : "/d/city/kezhan",
                ]));
        set("valid_startroom","1");
        setup();
}

