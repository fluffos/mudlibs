inherit S_SKILL;

string *defend_msg = ({
  "但是1种defend$l。\n",
  "但是2种defend$l。\n",
  "但是3种defend$l。\n",
  "但是4种defend$l。\n",
  "但是5种defend$l。\n",
});

string query_defend_msg() {
  return defend_msg[random(sizeof(defend_msg))];
}

int valid_learn(object me) { return 1; }
