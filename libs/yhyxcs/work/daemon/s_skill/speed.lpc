inherit S_SKILL;

string *speed_msg = ({
  "但是1种speed$l。\n",
  "但是2种speed$l。\n",
  "但是3种speed$l。\n",
  "但是4种speed$l。\n",
  "但是5种speed$l。\n",
});

string query_speed_msg() {
  return speed_msg[random(sizeof(speed_msg))];
}

int valid_learn(object me) { return 1; }
