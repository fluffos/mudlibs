inherit ITEM;

void create() {
  set_name("磁碟", ({ "disk", "diskette" }));
  set_weight(100);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "张");
    set("long", "一张标准的磁碟。\n");
    set("value", 0);
  }
}
