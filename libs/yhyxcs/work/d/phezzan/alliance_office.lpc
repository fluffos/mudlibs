//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "驻在武官办公室");
  set("long", @LONG
简洁明快的布局，对面的暗红色墙面上镶嵌着同盟的军徽----金
黄五角星。一台相当大的存储机立在角落里，维欧拉上尉褐色的
办公桌却占去了房间的大部分空间，上面有通讯用的TV电话，连
接着存储机的电脑，还有不多的文件整齐的摆放在桌子的一角。
LONG
  );

  set("globe", "phezzan");

  set("item_desc", ([
    "存储机": "一台专门用于存储海量数据的机器，大多用来记录星系的信息。\n",
    "cunchuji": "一台专门用于存储海量数据的机器，大多用来记录星系的信息。\n",
    "tv phone": "一台普通的TV电话，没什么特别的。\n",
    "TV电话": "一台普通的TV电话，没什么特别的。\n",
    "phone": "一台普通的TV电话，没什么特别的。\n",
    "telephone": "一台普通的TV电话，没什么特别的。\n",
  ]));

  set("exits", ([
    "west": __DIR__ "alliance_dorm",
    "south": __DIR__ "alliance_embassy",
  ]));

  set("objects", ([
    __DIR__ "npc/weioula": 1,
    __DIR__ "obj/computer": 1,
  ]));

  set("coor/x", -70);
  set("coor/y", 20);
  set("coor/z", 0);

  setup();
}
