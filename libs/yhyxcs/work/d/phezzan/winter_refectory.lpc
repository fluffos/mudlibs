//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "冬馆餐厅");
        set("long", @LONG
房间里最显眼的莫过于对面墙上硬木雕刻的冬馆的徽章----冬青
树。淡紫色花纹的壁纸与餐桌上的衬布显得格外和谐。简单而朴实的
餐具整齐的摆放在桌面上。
LONG
        );

	set("exits", ([
		"east" : __DIR__"w_gallery1",
	]));

	set("globe","phezzan");

	set("coor/x",-65);
	set("coor/y",85);
	set("coor/z",20);

	create_door("east", "木门", "west", 1);

	setup();
}
