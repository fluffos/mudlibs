//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "中央银行侧门");
  set("long", @LONG
暗红色大理石地板倒映着天花板上星星点点的装饰小灯，发出柔
和的暗黄色的光芒。几株绿色植物列立两厢，大街上的喧闹嘈杂代之
以这里的安静与清新。从这里直通银行的咖啡厅。
LONG
  );

  set("globe", "phezzan");

  set("exits", ([
    "east": __DIR__ "coffee_bar",
    "west": __DIR__ "nstreet2",
  ]));

  //	set("objects", ([
  //	__DIR__"npc/bookseller" : 1,
  //	]) );

  set("coor/x", 20);
  set("coor/y", 60);
  set("coor/z", 0);

  setup();
}
