//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create()
{
	set("short","中央银行营业厅");
	set("long", @LONG
大厅内外真是两个世界。暗红色大理石地板倒映着天花板上星星
点点的装饰小灯，发出柔和的暗黄色的光芒。大街上的喧闹嘈杂代之
以这里的安静与清新。西边设有为解除客户星际旅行的疲劳而设有的
咖啡厅。营业口有十个之多，其中第一个是办理接洽服务的。
LONG
	);

	set("globe","phezzan");

	set("exits",([
//		"east" : __DIR__"restroom", 
		"south" : __DIR__"center_bank_s",
		"west" : __DIR__"coffee_bar",
		"slot1" : __DIR__"slot1",
	]));

//	set("objects", ([
//	__DIR__"npc/bookseller" : 1,
//	]) );

	set("coor/x",60);
	set("coor/y",60);
	set("coor/z",0);

	setup();
}
