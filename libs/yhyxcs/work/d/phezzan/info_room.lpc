//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "领主府资料室");
	set("long", @LONG
费沙的商船可以在同盟或帝国的任何星域内航行，因此这里的资
料无论是航路还是资源储有量等等，无疑是最可信的。
LONG
	);

	set("globe","phezzan");

	set("exits", ([
		"east" : __DIR__"maingov_hall",
	]));

	set("objects", ([
		__DIR__"npc/manager" : 1,
	]) );

	set("coor/x",-10);
	set("coor/y",130);
	set("coor/z",0);

	setup();
}
