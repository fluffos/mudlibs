//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "官邸寝室");
  set("long", @LONG
一共的四张床位显示着驻在人员的最大编制。墨绿色的墙围和雪
白的四壁，陈设也只有一个床头柜和上面的台灯而已。不大的一扇窗
子，可以看到外面的一片乱草丛。
LONG
  );

  set("globe", "phezzan");

  set("exits", ([
    "east": __DIR__ "alliance_office",
  ]));

  set("coor/x", -70);
  set("coor/y", 30);
  set("coor/z", 0);

  setup();
}
