//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "自治领主府");
	set("long", @LONG
这里是费沙自治领的最高权利机关所在地，所有与费沙政治，经济，
军事有关的法令和条文都由这里制定颁布。玄关由高大厚实而透明的合
金玻璃制成，内部大厅耀眼的吊灯依稀可见。
LONG
	);

	set("globe","phezzan");

	set("exits", ([
		"north" : __DIR__"maingov_hall",
		"south" : __DIR__"nstreet3",
	]));

	set("coor/x",0);
	set("coor/y",120);
	set("coor/z",0);

	setup();
}
