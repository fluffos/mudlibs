//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "别墅大厅");
        set("long", @LONG
大厅内因应靠海的特质，除了吊灯、壁画以外，四处均以一些海
洋的动植物装饰着。海风不时吹进来，使得浮动着的装饰品充满了动
感。通往二楼的楼梯设计在右手边。
LONG
	);

	set("exits", ([
		"south" : __DIR__"shore",
		"northup" : __DIR__"shore_parlor",
	]));

	set("globe","phezzan");
	setup();
}
