//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "香格里拉饭店");
  set("long", @LONG
香格里拉饭店可以说是费沙平民百姓满足口福的地方，所以布置
也就相应简单，但仍然能称得上富丽堂皇。所有装饰、大吊灯也是尽
可能的大幅面。高朋满坐的大厅内无论何时都是充满了喧嚣。
LONG
  );

  set("globe", "phezzan");

  set("exits", ([
    "west": __DIR__ "hotelstreet",
  ]));

  set("objects", ([
    __DIR__ "npc/waiter": 1,
  ]));

  set("coor/x", -20);
  set("coor/y", -30);
  set("coor/z", 0);

  setup();
}
