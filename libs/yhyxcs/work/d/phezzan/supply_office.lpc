inherit ROOM;

void messaging();

void create() {
  set("short", "宇宙港给养中心");
  set("long", @LONG
这里是宇宙港给养中心。费沙有着银河系最为完善的补给系统，
其惊人的高效率来自行星上充沛的物资储备。透过办公室的后窗，能
够看到下面搬运机械群繁忙的操作场面。
LONG
  );
  set("globe", "phezzan");

  set("exits", ([
    "west": __DIR__ "starport",
  ]));

  set("objects", ([
    __DIR__ "npc/supplier": 1,
  ]));

  set("coor/x", 10);
  set("coor/y", -300);
  set("coor/z", 0);

  setup();
}
