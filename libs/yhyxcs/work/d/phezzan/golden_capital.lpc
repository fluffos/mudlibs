//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create()
{
	set("short","黄金之都");
	set("long", @LONG
高硬度合金玻璃的高大建筑映出淡蓝的光彩，整个广场无时无刻
不是停满了车。从大门入口进进出出的各种发色的男男女女无一不是
兴高采烈。几个巨大的电梯上下穿梭，显示着费沙的繁华。
LONG
	);

	set("globe","phezzan");

	set("exits", ([
		"north" : __DIR__"estreet2",
//		"south" : __DIR__"elevator",
	]));

	set("objects", ([
		__DIR__"npc/seller" : 1,
	]) );

	set("coor/x",60);
	set("coor/y",-20);
	set("coor/z",0);

	setup();
}
