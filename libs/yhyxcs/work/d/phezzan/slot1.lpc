//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "第一营业口");
  set("long", @LONG
可视电话就在左手边，想来这种覆盖整个银河系的民用通讯公司
也只能是费沙人的专利了。一个高脚的皮凳，使你可以舒适与对面的
费沙营业小姐进行简单的谈话，并享受银行为你提供的服务。
LONG
  );

  set("globe", "phezzan");

  set("exits", ([
    "back": __DIR__ "center_bank_hall",
  ]));

  set("objects", ([
    __DIR__ "npc/bank_lady": 1,
  ]));

  set("coor/x", 60);
  set("coor/y", 60);
  set("coor/z", 0);

  setup();
}
