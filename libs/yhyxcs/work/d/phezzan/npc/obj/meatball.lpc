inherit ITEM;
inherit F_FOOD;

void create()
{
	set_name("肉丸子", ({ "meatball" }) );
	set_weight(300);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("long", "一份香喷喷的肉丸子。\n");
		set("unit", "份");
		set("value", 20);
		set("food_remaining", 5);
		set("food_supply", 20);
	}
}
