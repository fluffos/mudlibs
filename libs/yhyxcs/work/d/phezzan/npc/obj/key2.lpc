inherit ITEM;

void create()
{
	set_name("电子钥匙", ({ "key2" }));
	set_weight(500);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "只");
		set("long",
			"一只电子钥匙，看上去样子怪怪的，好象半片钥匙一样。\n");
		set("value", 0);
	}
}
