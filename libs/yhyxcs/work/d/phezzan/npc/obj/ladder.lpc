inherit ITEM;

void create() {
  set_name("合金梯", ({ "ladder" }));
  set_weight(50000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "部");
    set("long",
      "一部野战用合金梯，士兵主要用它来翻墙越壕。\n");
    set("value", 2000);
  }
}
