//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "费尔莱丁溪谷");
        set("long", @LONG
费尔莱丁溪谷是一个巨大喀斯特地貌群的统称。在这个风景区里
建有许多别墅、山庄，是那些很有经济头脑的费沙商人于半世纪前修
建的。青山绿树之间，藏有无数溪流、瀑布和人造湖，风景美不胜收。
******************************************************
秘密：
可以考虑设计一个溶洞迷宫，里面捡点东西。
******************************************************
LONG
	);

	set("exits", ([
//		"east" : __DIR__"farm",
	]));

	set("taxi",([
		__DIR__"travel_station" : 4000,
	]) );

	set("globe","phezzan");
	setup();
}
