//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "冬馆内庭");
  set("long", @LONG
这里应该是这栋别馆主人散步的地方，一个小巧的凉亭建在绿草
如茵的小丘之上。别馆的四周种植者一大圈整齐高大的绿色乔木，足
以构成了别馆的第二道围墙，显然这里的主人非常爱好自然。
LONG
  );
  set("exits", ([
    "south": __DIR__ "winter_tree",
  ]));

  set("coor/x", -30);
  set("coor/y", 100);
  set("coor/z", 0);

  set("globe", "phezzan");
  setup();
}
