//Created by RoomEditer on 00-10-8.
inherit ROOM;

void create()
{
	set("short","上层北1全向通道");
	set("long",@LONG
这里是连接上层和中层以及上层各区之间的重要枢纽，与其他层不
同的是这里并没有，太多的兵力来把守，也许因为上层本身的机能就是
作战的原因吧。
LONG
	);
	set("globe","geiersburg");
	set("exits",([
		"east" : "/d/geiersburg/ubpass1",
		"west" : "/d/geiersburg/uapass2",
		"south" : "/d/geiersburg/upcenterroad",
		"north" : "/d/geiersburg/unroad2",
		"down" : "/d/geiersburg/otisn0",
	]));
	set("coor/x",0);
	set("coor/y",30);
	set("coor/z",60);
	setup();
}
