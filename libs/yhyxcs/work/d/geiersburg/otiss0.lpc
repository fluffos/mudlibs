//Created by RoomEditer on 00-9-30.
inherit ROOM;

void create()
{
	set("short","中央大路南1全向通道");
	set("long",@LONG
这是要塞内部最为重要的通道之一，这里连接着要塞的上中下三
层。作为要塞内部的枢纽，自然配备了重兵把守。所有的人要想通过
这里都必须经过严格的盘查。
LONG
	);
	set("globe","geiersburg");
	set("exits",([
		"east" : "/d/geiersburg/ccwepass",
		"west" : "/d/geiersburg/cfpass",
		"south" : "/d/geiersburg/ccsroad2",
		"north" : "/d/geiersburg/ccroad",
                "up":"/d/geiersburg/otissup1",
                "down":"/d/geiersburg/otissdown1",
	]));
	set("coor/x",0);
	set("coor/y",-30);
	set("coor/z",0);
	setup();
}
