//Created by RoomEditer on 00-10-14.
inherit ROOM;

void create()
{
	set("short","司令官警卫室");
	set("long",@LONG
这里有一个班的警卫在守卫着，但他们却只是充充“门面”的。
对于坎普来说个人不需要警卫来保护。“与其用最精锐的陆战部队来
保护自己，还不如把他们用在前线更合适一些”，因为有这种论调所
以警卫班的人员是由那些年迈的老兵组成的。这一点到是和“某人”
有着相似之处。

LONG
	);
	set("globe","geiersburg");
	set("exits",([
		"east" : "/d/geiersburg/commander",
		"west" : "/d/geiersburg/upcenterroad",
	]));
	set("coor/x",30);
	set("coor/y",0);
	set("coor/z",60);
	setup();
}
