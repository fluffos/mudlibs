//Created by RoomEditer on 00-10-14.
inherit ROOM;

void create()
{
	set("short","D区1号通道");
	set("long",@LONG
这是上层Ｄ区的主要通道之一，来来往往的车辆和士兵们构成了
主要的内容。在这里高速和高节奏成为这里的主要特点。由于这里是
以战争为主要机能的一层，所以看不到任何非战人员的存在。
LONG
	);
	set("globe","geiersburg");
	set("exits",([
		"east" : "/d/geiersburg/udpass2",
		"south" : "/d/geiersburg/udm1",
		"northeast" : "/d/geiersburg/umcr",
	]));
	set("coor/x",-120);
	set("coor/y",-30);
	set("coor/z",60);
	setup();
}
