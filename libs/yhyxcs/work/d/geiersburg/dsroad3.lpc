//Created by RoomEditer on 00-10-15.
inherit ROOM;

void create() {
  set("short", "S3通道");
  set("long", @LONG
这是要塞内部的主要通道之一，路上到处是军用陆行车在行驶。
在路边的路牌上写着“S3通道”的字样。
LONG
  );
  set("globe", "geiersburg");
  set("exits", ([
    "east": "/d/geiersburg/wf",
    "north": "/d/geiersburg/dsroad2",
  ]));
  set("coor/x", 0);
  set("coor/y", -120);
  set("coor/z", -60);
  setup();
}
