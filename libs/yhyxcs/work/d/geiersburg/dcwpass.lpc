//Created by RoomEditer on 00-10-15.
inherit ROOM;

void create() {
  set("short", "C区瓦普通道");
  set("long", @LONG
这是一条狭长的甬道，窄的只能允许两个人同时并肩走这样的设
计当然是为了安全的考虑，另外在头顶上有无数的自动防御光线枪和
普通的步枪以及无力化瓦斯喷射口，而控制这些防卫装置的都在前面
的瓦普控制室里。
LONG
  );
  set("globe", "geiersburg");
  set("exits", ([
    "east": "/d/geiersburg/dcwpc",
    "west": "/d/geiersburg/dsroad2",
  ]));
  set("coor/x", 60);
  set("coor/y", -60);
  set("coor/z", -60);
  setup();
}
