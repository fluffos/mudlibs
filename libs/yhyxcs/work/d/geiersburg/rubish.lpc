//Created by RoomEditer on 00-10-14.
inherit ROOM;

void create() {
  set("short", "垃圾处理中心");
  set("long", @LONG
这里是要塞的垃圾处理中心。只要是有人生存的地方就会产生。
所以垃圾处理中心对要塞来说是必不可缺的。由于技术相当发达，
几乎所有的垃圾在这里都可以回收再利用。
LONG
  );
  set("globe", "geiersburg");
  set("exits", ([

    "west": "/d/geiersburg/dnroad3",
  ]));
  set("coor/x", 60);
  set("coor/y", 120);
  set("coor/z", -60);
  setup();
}
