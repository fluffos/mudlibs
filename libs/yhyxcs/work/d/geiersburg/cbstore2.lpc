//Created by RoomEditer on 00-10-8.
inherit ROOM;

void create() {
  set("short", "B区１号弹药库");
  set("long", @LONG
这里装满了各种各样的武器和弹药，与核燃料库不同的是这里到
处有卫兵在巡逻。而且在进入时必须解除武装，而在弹药库里的武器
也只有出了弹药库由专人解除密码后才能启用。防卫可说是相当的完
善。
LONG
  );
  set("globe", "geiersburg");
  set("exits", ([
    "east": "/d/geiersburg/cbstore3",
    "west": "/d/geiersburg/ccnroad2",
  ]));
  set("coor/x", 30);
  set("coor/y", 60);
  set("coor/z", 0);
  setup();
}
