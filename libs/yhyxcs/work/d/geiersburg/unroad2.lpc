//Created by RoomEditer on 00-10-8.
inherit ROOM;

void create()
{
	set("short","N2通道");
	set("long",@LONG
这是要塞内部的主要通道之一，路上到处是军用陆行车在行驶。
在路边的路牌上写着“N2通道”的字样。
LONG
	);
	set("globe","geiersburg");
	set("exits",([

		"south" : "/d/geiersburg/otisnup1",
		"north" : "/d/geiersburg/unroad3",
	]));
	set("coor/x",0);
	set("coor/y",60);
	set("coor/z",60);
	setup();
}
