//Created by RoomEditer on 00-10-8.
inherit ROOM;

void create()
{
	set("short","3号战舰空港");
	set("long",@LONG
这里是宇宙舰队出击的空港之一，在这里可以容纳４００００艘
的战舰同时停泊与补给。所以无论从数字上还是外观上用壮观一词来
形容可以说一点也不为过。
LONG
	);
	set("globe","geiersburg");
	set("exits",([

		"south" : "/d/geiersburg/ccnroad6",
	]));
	set("coor/x",0);
	set("coor/y",210);
	set("coor/z",0);
	setup();
}
