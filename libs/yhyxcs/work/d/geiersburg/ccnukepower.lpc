//Created by RoomEditer on 00-9-30.
inherit ROOM;

void create()
{
	set("short","Ｃ区核动力室");
	set("long",@LONG
这里是要塞的动力来源之一，由于有放射性污染，这里平时除了
工作人员以外，是不允许任何人靠近的，和原始的火力发电厂不一样
的是核熔炉发出的不是红彤彤而是惨蓝的光芒，这使得这里被笼罩上
了一层诡异的气氛，是一个任谁也不想多待的地方。
LONG
	);
	set("globe","geiersburg");
	set("exits",([

		"west" : "/d/geiersburg/ccenjoyroom",
	]));
	set("coor/x",120);
	set("coor/y",-120);
	set("coor/z",0);
	setup();
}
