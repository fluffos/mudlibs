//Created by RoomEditer on 00-10-8.
inherit ROOM;

void create() {
  set("short", "B区２号弹药库");
  set("long", @LONG
这里装满了各种各样的武器和弹药，这里到处有卫兵在巡逻。而
且在进入时必须解除武装，而在弹药库里的武器也只有出了弹药库由
专人解除密码后才能启用。防卫可说是相当的完善。
LONG
  );
  set("globe", "geiersburg");
  set("exits", ([

    "west": "/d/geiersburg/cbstore2",
  ]));
  set("coor/x", 90);
  set("coor/y", 60);
  set("coor/z", 0);
  setup();
}
