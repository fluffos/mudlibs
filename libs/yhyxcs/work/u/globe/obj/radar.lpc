inherit ITEM;

void create() {
  set_name("索敌雷达", ({ "radar" }));
  set("no_get", 1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "颗");
    set("long", "一颗无人驾驶的自多索敌雷达卫星。\n");
    set("value", 0);
  }
}
