inherit ITEM;

void create() {
  set_name("处女神之项链", ({ "necklace" }));
  set("no_get", 1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "颗");
    set("long", "被杨威利击毁的十二颗处女神项链残骸。\n");
    set("value", 0);
  }
}
