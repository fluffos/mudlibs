inherit PLANET;

void create()
{
	set_name("行星奥丁", ({ "odin" }) );
	set_weight(50000);
	set_max_encumbrance(1000000000);
	set("no_get", 1);
	set("no_shown", 1);
	set("prep","orbit");
	set("landto","/d/odin/starport");
	set("x-dir",100);
	set("y-dir",100);

	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "颗");
		set("long", "奥丁的描述。\n");
	}
}
