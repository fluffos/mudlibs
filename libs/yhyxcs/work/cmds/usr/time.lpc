// time.c

#include <ansi.h>
inherit F_CLEAN_UP;

int main() {
  write(CYN "现在是" + CHINESE_D->chinese_date(time()) + "。\n" NOR);
  return 1;
}

int help(object me) {
  write(@HELP
指令格式: time

这个指令让你知道现在的时间。

HELP
  );
  return 1;
}
