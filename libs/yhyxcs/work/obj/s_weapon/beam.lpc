#include <s_weapon.h>
#include <ansi.h>

inherit BEAM;

void create() {
  set_name("磁力炮", ({ "beam" }));
  set("long", "磁力炮。\n");
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "座");
  }
  //beam factor, defend factor, speed factor
  init_beam(10, 2, 0);
  setup();
}
