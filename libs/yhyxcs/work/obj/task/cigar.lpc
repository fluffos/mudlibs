inherit ITEM;

void create()
{
	set_name("雪茄", ({ "cigar" }) );
	set("long","一支粗粗的棕黑色雪茄，带着浓重的烟草味道。\n");
	set_weight(200);
	set("owner_name","安德鲁安·鲁宾斯基");
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "支");
	}
	setup();
}
