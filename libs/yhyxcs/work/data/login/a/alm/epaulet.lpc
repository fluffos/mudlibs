#include <armor.h>

inherit EPAULET;

void create()
{
	set_name("费沙上尉肩章", ({ "epaulet" }) );
	set("long","费沙上尉肩章的长描述。\n");
	set_weight(200);
	set("no_drop",1);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "对");
		set("armor_prop/armor", 1);
	}
	setup();
}
