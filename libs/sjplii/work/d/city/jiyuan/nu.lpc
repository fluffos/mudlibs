inherit ROOM;
void create()
{
        set("short", "红酥手");
        set("long", @LONG
红酥手，黄藤酒，满城春色宫墙柳。
东风恶，欢情薄。一怀愁绪，几年离索。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "northeast" : __DIR__"xue",
  "south" : __DIR__"lastbridge",
]));
        set("objects", ([
        __DIR__"npc/nu" : 1,
                        ]) );

        setup();
        replace_program(ROOM);
}
