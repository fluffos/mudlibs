inherit ROOM;
void create()
{
        set("short", "芳草依");
        set("long", @LONG
送春去，春去人间无路。
秋千外，芳草连天，谁遣风沙暗南浦。
依依甚意绪。漫忆海门飞絮。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"lastbridge",
  "southeast" : __DIR__"hua",
]));
        set("objects", ([
        __DIR__"npc/cao" : 1,
                        ]) );

        setup();
        replace_program(ROOM);
}
