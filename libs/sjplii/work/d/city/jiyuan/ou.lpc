inherit ROOM;
void create()
{
        set("short", "红藕香");
        set("long", @LONG
红藕香残玉萤秋，轻解罗裳，独上兰舟。
云中谁寄锦书来？雁字回时，月满西楼。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "south" : __DIR__"bridge2",
]));
        set("objects", ([
        __DIR__"npc/ou" : 1,
                        ]) );

        setup();
        replace_program(ROOM);
}
