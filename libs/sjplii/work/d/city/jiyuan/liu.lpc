inherit ROOM;
void create()
{
        set("short", "杨柳青");
        set("long", @LONG
雪云散尽，放晓晴池院。杨柳于人便青眼。
更风流多处，一点梅心相映远。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "southeast" : __DIR__"yue",
  "west" : __DIR__"xue",
]));
        set("objects", ([
        __DIR__"npc/liu" : 1,
                        ]) );

        setup();
        replace_program(ROOM);
}
