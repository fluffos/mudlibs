inherit ROOM;
void create()
{
        set("short", "月华收");
        set("long", @LONG
月华收，云淡霜天曙。西征客，此时情苦。
翠娥执手送临歧，轧轧开朱户。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"bridge2",
]));
        set("objects", ([
        __DIR__"npc/yuehua" : 1,
                        ]) );

        setup();
        replace_program(ROOM);
}
