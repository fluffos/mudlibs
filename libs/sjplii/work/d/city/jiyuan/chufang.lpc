//jiyuan chufang.c
inherit ROOM;

void create ()
{
        set ("short", "厨房");
        set ("long", @LONG
这里是春风楼的厨房，不时有阵阵香气从这里传出，另人食指大动。
据说这里的厨师都是各地的名厨，而这里的小吃也可谓长安一绝。
LONG);

       
        set("exits", 
        ([ //sizeof() == 4
              "north" : __DIR__"dating",
	   ]));

        set("objects", 
        ([ //sizeof() == 1
//		__DIR__"npc/dahan" : 2,
        ]));


        set("outdoors", "biaoju");
	set("no_clean_up", 0);
	        setup();
}


