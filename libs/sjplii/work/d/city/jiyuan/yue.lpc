inherit ROOM;
void create()
{
        set("short", "东厢月");
        set("long", @LONG
楼阴缺，栏杆影卧东厢月。
东厢月，一天风露，杏花如雪。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "south" : __DIR__"grassroom",
  "northwest" : __DIR__"liu",
]));
        set("objects", ([
        __DIR__"npc/yue" : 1,
                        ]) );

        setup();
        replace_program(ROOM);
}
