//changan city

inherit ROOM;

void create() {
  set("short", "杂货行");
  set("long", @LONG
这是京城最负胜名的"百货全"杂货铺。这里虽说地方不大，但是人却
不少，熙熙攘攘颇为热闹。据说这里的老板喜好收藏，藏品丰厚，其中有
不少稀奇古怪的奇珍异宝，所以引来大批的客人。但是这的老板神龙见首
不见尾，要与他交易，谈何容易。正面墙上挂着一块小匾(sign)。
LONG);

  set("exits",
    ([  //sizeof() == 4
    "west": __DIR__ "zhuque-s2",
    ]));
  set("item_desc", ([ /* sizeof() == 1 */
    "sign": "
客官可以在这里：

	投标竞价：bid 
	预付定金：retrieve 
	设定售价比例：set_price 
	店铺当前状态：status 

" ]));

  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/zahuo_boss": 1,
    ]));

  set("no_clean_up", 0);
  setup();
}
