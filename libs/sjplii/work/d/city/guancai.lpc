inherit ROOM;
void create() {
  set("short", "棺材店");
  set("long", @LONG
店门口的招牌是几个已被雨淋日晒得变了颜色的花圈。店口的地
上放着几口打造好，但还没有上漆的薄皮棺材。 四面的墙上零乱的挂
着几串纸钱和纸花。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "xuanwu-w2",
  ]));
  set("objects", ([
    __DIR__ "npc/guancai_boss": 1,
  ]));

  setup();
  replace_program(ROOM);
}
