// Room: /u/shandong/ta-road5.c

inherit ROOM;

void create()
{
        set("short", "泰安城里");
        set("long", @LONG
这里是泰安城里的一个丁字路口。西面店铺的招牌上什么字也没写，
而是画了一个象征铜钱的标志，显然是一家钱庄。东面则是当地赫赫有
名的药铺----“济世堂”，远远的就能闻到一丝草药的香气，门口有一
个妇人正在药铺门口煎药。
    往西走是一家钱庄；
    往东走是药铺--济世堂；
    往南走则一直通向南门。
LONG
        );
        set("exits", ([ /* sizeof() == 3 */
  "west" : __DIR__"ta-bank",
  "east" : __DIR__"ta-yaodian",
  "south" : __DIR__"ta-road4",
  "north" : __DIR__"minzhai1",
]));
        set("outdoors", "shandong");
        set("objects", 
        ([ //sizeof() == 1
                "/npc/newnpc/jinyanpeng" : 1,
        ]));
	set("hell", 1);
	set("valid_startroom", 1);

        setup();
}
