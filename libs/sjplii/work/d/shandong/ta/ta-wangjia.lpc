// Room: /u/shandong/ta-wangjia.c

#include <room.h>

inherit ROOM;

void create()
{
        set("short", "王员外家");
        set("long", @LONG
这里是王员外的私宅。由于去年王员外外出经商时被土匪所杀，现
在一家老小只能坐吃山空了。幸得王员外为人不错，家中有事时街坊邻
居都来帮忙。王员外的小儿子宝宝还不懂事，正在院子里和一只小狗玩
着。
    往南走是一条东西走向的小街。
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"ta-road6",
  "west" : __DIR__"ta-woshi", 
]));
        set("objects", 
        ([ //sizeof() == 1
                __DIR__"npc/boy" : 1,
                __DIR__"npc/dog" : 1,
        ]));
        create_door("west","木门","east",DOOR_CLOSED);
	set("hell", 1);

        setup();
        replace_program(ROOM);
}
