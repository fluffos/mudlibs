// Room: /u/shandong/ta-inn3.c

inherit ROOM;

void create() {
  set("short", "小酒店");
  set("long", @LONG
这里是一家简陋的小酒店。几张陈旧的木桌、板凳零散的摆在四周，
柜台上摆着几个大大小小的酒坛子，几只令人讨厌的苍蝇正绕着顾客的
盘子打转。店小二正站在柜台前懒洋洋的打着哈欠。
    往东走是一条南北走向的小街。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "ta-road8",
  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/waiter": 1,
    ]));
  set("hell", 1);
  setup();
}
