// Room: /u/shandong/songlin1.c

inherit ROOM;

void create()
{
	set("short", "黑龙潭的松树林");
	set("long", @LONG
你走进一片松树林。抬头望去，满树都是松果。如此大的松果你却
是从没见过。想必那松子一定好吃的很（你不禁流口水了）。你想试试
能不能爬上去。可是树太高了爬是不大可能。还是踢两脚吧，定不定掉
下几个来也是好的。
    往西走是一条小径。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"songlin1",
  "east" : __DIR__"songlin13",
  "north" : __DIR__"songlin8",
  "south" : __DIR__"songlin9",
]));

       set("outdoors","shandong");
        set("objects", 
        ([ //sizeof() == 1
		__DIR__"npc/songshu" : 3,
        ]));

	set("hell", 1);

	setup();
       replace_program(ROOM);
}
