// Room: /u/shandong/hl-xiaojing1.c

inherit ROOM;

void create() {
  set("short", "黑龙潭的小径");
  set("long", @LONG
你在小径中小心在走着。两旁茅草齐腰。你在双手已护在胸前了，
生怕忽然跳出一只猛兽把你给吃了。现在你发现了一个丁字路口。
    往南走似乎有一片树林；
    往东走则还是一条若隐若现的小径；
    往北走你就回到入口处了。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */

    "north": __DIR__ "hl-rukou",
    "east": __DIR__ "hl-xiaojing2",
    "south": __DIR__ "hl-shulin1",
  ]));

  set("outdoors", "shandong");
  set("objects",
    ([  //sizeof() == 1
    "/obj/npc/shaonian": 2,
    ]));

  set("hell", 1);

  setup();
}
