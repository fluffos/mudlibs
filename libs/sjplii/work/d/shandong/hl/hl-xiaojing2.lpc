// Room: /u/shandong/hl-xiaojing2.c

inherit ROOM;

void create() {
  set("short", "黑龙潭的小径");
  set("long", @LONG
这里是一条小径。景色镁砂变化也没有遇到什么黑蛇、黑龙。怕是
当地人编出来的故事吓人的吧。不由得你的胆子也壮了几分。你向北望
去，好象前方有一片草地，一条弯弯曲曲的小溪从身边一直流向那里。
    往北走是一片草地；
    往西走是个丁字路口。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */

    "north": __DIR__ "hl-caodi",
    "west": __DIR__ "hl-xiaojing1",
  ]));

  set("outdoors", "shandong");

  set("hell", 1);

  setup();
  replace_program(ROOM);
}
