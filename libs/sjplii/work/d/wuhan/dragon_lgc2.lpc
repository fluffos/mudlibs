// Room: /u/wuhan/dragon_lgc2.c

inherit ROOM;

void create() {
  set("short", "练功场");
  set("long", @LONG
这里是海鲸帮弟子们练功的地方。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "dragon_lgc",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/peilian": 4,
  ]));
  set("no_clean_up", 0);
  //	set("valid_startroom", 1);

  setup();
  replace_program(ROOM);
}
