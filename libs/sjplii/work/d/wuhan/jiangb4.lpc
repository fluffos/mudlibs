// Room: /new/jinling/jiangbian4.c

inherit ROOM;

void create()
{
	set("short", "黄石");
	set("long", @LONG
这里是已经是湖北境内，距离武昌已经不远了。武昌历来都是湖北
重镇，兵家必争之地。
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/hanzi" : 2,
]));
	set("outdoors", "jinling");
	set("exits", ([ /* sizeof() == 2 */
  "southeast" : __DIR__"jiangb3",
  "northwest" : __DIR__"jiangb5",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
