// dragon_wc.c

inherit ROOM;

void create() {
  set("short", "茅房");
  set("long", @LONG
这是海鲸帮普通弟子如厕的地方, 非常宽敞明亮, 收拾得
也还干净. 地上整整齐齐放了一排马桶. 
    据说这里准备修得更好些.
LONG);
  set("exits", ([
    "west": __DIR__ "dragon_residence1",
  ]));
  set("objects", ([
    __DIR__ "obj/matong": 1,
  ]));
  set("outdoors", "wuhan");
  set("no_clean_up", 0);
  setup();
}
