// Room: /u/wuhan/ljguiyuan1.c

inherit ROOM;

void create()
{
	set("short", "桂园");
	set("long", @LONG
你眼前出现一个大庄园, 用一圈高大的围墙围住。大门上方
悬着块金匾, 刻着两个篆字：桂园
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"ljguiyuan2",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
