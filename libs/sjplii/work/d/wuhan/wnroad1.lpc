// Room: /u/wuhan/wnroad1.c

inherit ROOM;

void create()
{
	set("short", "小巷");
	set("long", @LONG
这是一条小巷。两边却都是高墙大院的大户人家。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	"north" : __DIR__"wnroad2",
  "south" : __DIR__"wroad3",
  "west" : __DIR__"ghall",
	    "east" : __DIR__"bank",
]));
        set("objects", 
        ([
              "/npc/nknpc" : 1,
        ]));
	setup();
	replace_program(ROOM);
}
