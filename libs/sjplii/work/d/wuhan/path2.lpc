// Room: /u/wuhan/shanzai/path2.c

inherit ROOM;

void create()
{
	set("short", "上山路");
	set("long", @LONG
你来到李乐山脚下。一条蜿蜒的石阶路延山而上，夹路是一棵
棵狰狞丑陋的大树，树干间缠满了了碗口粗的青藤。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "southup" : __DIR__"path3",
  "northwest" : __DIR__"path1",
]));

	set("outdoors", "wuhan");
	setup();
}

int valid_leave(object who, string exit)
{
	if( exit == "southup" )who->start_busy(2);
	return ::valid_leave(who, exit);
}
