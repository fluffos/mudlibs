// Room: h_zheng.c

inherit ROOM;

void create() {
  set("short", "郑寡妇家");
  set("long", @LONG
这里是郑寡妇家。房里的家具都是一尘不染，放得整整齐齐。靠墙放着一张
雕花双人大床，上面齐整地铺放着双龙戏凤的床单，绣花枕头和缝着大红团
丝被面的大被子。咋一看，到象是新婚洞房。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "ljin2",
  ]));
  set("objects",
    ([
      __DIR__ "npc/zheng": 1
    ]));

  setup();
  replace_program(ROOM);
}
