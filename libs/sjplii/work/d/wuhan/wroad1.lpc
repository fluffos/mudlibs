// Room: /u/wuhan/wroad1.c

inherit ROOM;

void create() {
  set("short", "青石马路");
  set("long", @LONG
这是一条青石大马路, 可以并排跑五匹高头大马。街道两侧的梧桐十步一株, 茂密的
阔叶遮起一路阴凉。由于武昌海鲸帮在长江水道上的兴起, 这一带到处都是江湖人物。
    从这里往北就是海鲸帮的总舵, 一对盘龙柱立在门前, 大红朱门八字大开, 里面人声
鼎沸。南面是海鲸帮开的一家招呼江湖朋友的酒楼。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "dragon_entrance",
    "south": __DIR__ "wjiulou",
    "west": __DIR__ "wsroad1",
    "east": __DIR__ "wroad2",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/bukuai": 2,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
