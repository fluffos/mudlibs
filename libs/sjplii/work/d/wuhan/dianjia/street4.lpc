// Room: /u/wuhan/dianjia/street4.c

inherit ROOM;

void create()
{
	set("short", "街道");
	set("long", @LONG
这里是点家庄的主街, 用碎石子铺成。街道不是太宽, 两旁种着
若干果树, 倒也清爽干净。
    这里西面是一片桃林。北面是一大块空地。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"street3",
  "west" : __DIR__"taolin",
  "north" : __DIR__"lianwuchang",
]) );
	setup();
	replace_program(ROOM);
}
