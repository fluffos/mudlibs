// Room: /u/wuhan/dianjia/lingu.c

inherit ROOM;

void create()
{
	set("short", "点灵姑的家");
	set("long", @LONG
点灵姑家里布置得很讲究。墙上琴剑高挂, 堂中屏风半掩, 看得
出这户人家是见过大世面的。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"linguyard",
]));
        set("objects", ([
	__DIR__"npc/linggu" : 1,
	    __DIR__"npc/leiting": 1,
]));
	setup();
}
