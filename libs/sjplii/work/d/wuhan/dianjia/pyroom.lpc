// Room: /u/wuhan/dianjia/pyroom.c

inherit ROOM;

void create() {
  set("short", "卧房");
  set("long", @LONG
这里是丫头们的卧房，房间不大，倒也整洁清新。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "pinggu",
  ]));
  set("objects", ([
    __DIR__ "npc/xiaohong": 1,
  ]));
  setup();
  replace_program(ROOM);
}
