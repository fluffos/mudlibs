// Room: /u/wuhan/hankou/yamen.c

inherit ROOM;

void create()
{
	set("short", "衙门大院");
	set("long", @LONG
大院门口放着一面大鼓, 大概是用来鸣冤的, 只是鼓面崭新如故, 好象
从来就没人用过. 院子里威风凛凛地站着几个衙役, 正冷冷地看着你. 一条
青砖路直通大堂, 路面已是光滑异常, 显然是踩的人太多了的缘故.
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
  "north" : __DIR__"yamenk",
  "west" : __DIR__"yamenhall",
  "east" : __DIR__"proad4",
]));

        set("objects", ([
	 __DIR__"npc/yayi" : 2,
	 __DIR__"npc/dyayi" : 2,
]) );
	setup();
	replace_program(ROOM);
}
