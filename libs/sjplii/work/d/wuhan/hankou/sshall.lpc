// Room: /u/wuhan/hankou/sshall.c

inherit ROOM;

void create() {
  set("short", "正厅");
  set("long", @LONG
这是一间装饰华丽的大厅，是这里待客的地方。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "out": __DIR__ "ssyard",
    "east": __DIR__ "ssgarden",
  ]));
  set("no_clean_up", 0);
  set("objects", ([
    __DIR__ "npc/ssleader": 1,
  ]));
  setup();
}
