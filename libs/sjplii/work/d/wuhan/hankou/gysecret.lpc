// Room: /u/wuhan/hankou/gysecret.c

inherit ROOM;

void create() {
  set("short", "秘室");
  set("long", @LONG
这是一间斗室。空气中洋溢着一种奇怪的气味，显然很久没人来过
这里了。房子里只挂了一幅画像，地上摆着个浦团，蒲团上坐着个孩童
大小的人型，却是个肉身。画像上有一行字：师尊  玄妙教主像。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "gyluohan",
  ]));
  set("no_clean_up", 0);
  set("objects", ([
    __DIR__ "obj/map4.lpc": 1,
  ]));
  setup();
}
