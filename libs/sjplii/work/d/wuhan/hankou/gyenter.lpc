// Room: /u/wuhan/hankou/gyenter.c

inherit ROOM;

void create()
{
	set("short", "归元寺");
	set("long", @LONG
高高的院墙，朱绿瓦，围着一块佛门圣地。一张金匾气象庄严地悬
在当头，金字曰：归元宝寺。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"proad4",
  "east" : __DIR__"gywc1",
]));
	set("outdoors", "hankou");
	set("objects", ([
	__DIR__"npc/gyshami" : 2, 
	]) );
	setup();
	replace_program(ROOM);
}
