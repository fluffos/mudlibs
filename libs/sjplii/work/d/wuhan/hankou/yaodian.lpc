// Room: /u/wuhan/hankou/yaodian.c

inherit ROOM;

void create()
{
	set("short", "药铺");
	set("long", @LONG
一家不大不小的药铺, 柜台上放了一杆小秤. 后面整整一面墙都是
大柜子, 药柜分成一格格的小抽屉, 每个上面都用小纸片作上记号, 写
的都是你不懂的药名.
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"hanz2",
]));
	set("objects", ([
	__DIR__"npc/herbalist" : 1,
	]) );
	setup();
}
