// sijimei.c

inherit ROOM;

create()
{
        set("short", "四季美");
        set("long", @LONG
四季美的汤包武汉没有人没尝过，以油腻称绝。这里铺面不大，吃包子的人要排队
排到街上去。因为是老字号，所以也从没想过搬迁。
LONG);
        set("exits", ([
                "south"  :  __DIR__"hanz2",
]) );
        set("objects", ([
                __DIR__"npc/baoer"  : 1
]) );
        setup();
}
