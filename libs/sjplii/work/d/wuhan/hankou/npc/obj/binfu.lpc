// binfu.c


inherit ITEM;

void create()
{
	set_name("兵符",({ "bingfu" }) );
	set("long",
		"这是一个将兵的兵符，上面有些你看不动的记号\n");
	set_weight(3000);
	if( clonep() )
		set_default_object(__FILE__);
	else 
		set("unit", "块");
	setup();
}
