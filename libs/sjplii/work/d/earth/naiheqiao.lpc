inherit ROOM;

void create ()
{
  set ("short", "奈何桥");
  set ("long", @LONG

这是一座仅数寸之宽，数长之长的石桥。上有寒风滚滚，下是
血浪滔滔。桥上左右无扶手，险峻之路如同匹练搭长江，桥下
之水阴气逼人，腥风扑鼻。
LONG);

  set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"bidouya",
  "north" : __DIR__"huangwai",
]));
  set("hell", 1);

  set("objects", ([ /* sizeof() == 2 */
     __DIR__"npc/mguard"+(string)(random(50)) : 1,
     __DIR__"npc/mguard"+(string)(random(50)) : 1,
]));
  setup();
	replace_program(ROOM);
}
