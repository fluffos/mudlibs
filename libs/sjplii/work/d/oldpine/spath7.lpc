// Room: /d/oldpine/spath7.c

inherit ROOM;

void create()
{
	set("short", "青石官道");
	set("long", @LONG
这是一条青石板铺成的官道，两旁是一望无际的田野和零散的村落，
南面连绵不断的就是十万大山。田野里有几个农夫在干活。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"spath6",
  "southwest" : "/d/shiwan/npath1",
]));
	set("no_clean_up", 0);
	set("objects", ([ /* sizeof() == 1 */
       __DIR__"npc/farmer" : 2,
]));

	setup();
	replace_program(ROOM);
}
