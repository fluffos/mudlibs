// Room: /u/fuzhou/tailory.c

inherit ROOM;

void create()
{
	set("short", "布庄");
	set("long", @LONG
你现在正站在布庄里，柜台上摆着不少布匹，可惜尽是一些蓝布，黑布，
没有什么好看的货色。
    靠近门口的墙壁上挂着(sign)。
LONG
	);
	set("item_desc", ([ /* sizeof() == 1 */
  "sign" : "写着一个斗大的布字。\n",
]));
	set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"nwroad2",
]));
	set("objects", ([ /* sizeof() == 3 */
  __DIR__"npc/laotou" : 1,
  __DIR__"npc/tailor" : 1,
  __DIR__"npc/laofu" : 1,
]));
	set("no_clean_up", 0);
	set("hell", 1);

	setup();

}
