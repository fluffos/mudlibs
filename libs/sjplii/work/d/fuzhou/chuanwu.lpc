// Room: sroad1.c

inherit ROOM;

void create() {
  set("short", "船坞");
  set("long", @LONG
这里是福州船坞。船坞很大，四处可见各类船只的模型，还有
一些正在建造中的木船。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "sroad1",
  ]));
  set("outdoors", "fuzhou");
  set("objects", ([
    __DIR__ "npc/chuanwu_boss": 1,
  ]));
  set("hell", 1);

  setup();
}
