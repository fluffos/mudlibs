// Room: /u/yandang/npath4.c

inherit ROOM;

void create() {
  set("short", "山头上的一块平地");
  set("long", @LONG
从这里往南下山就可以进入南诏境内，北面是一段险要的山路，
西面可通往主峰--万尊峰，那里是万兽尊者的所在地。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "westup": __DIR__ "wpath1",
    "southdown": __DIR__ "spath1",
    "northdown": __DIR__ "npath3",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/xiniu": 2,
  ]));
  set("no_clean_up", 0);
  set("outdoors", "shiwan");

  setup();
}
