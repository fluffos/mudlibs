// Room: /u/yandang/wpath2.c

inherit ROOM;

void create() {
  set("short", "山路");
  set("long", @LONG
这是上山的碎石大道，盘旋通往山上，路的左边是高高的石壁，
右边是陡峭的悬崖。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    //  "north" : __DIR__"wpath3",
    "eastdown": __DIR__ "wpath1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/bao": 3,
  ]));
  set("no_clean_up", 0);
  set("outdoors", "shiwan");

  setup();
  replace_program(ROOM);
}
