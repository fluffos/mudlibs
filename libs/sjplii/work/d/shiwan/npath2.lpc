// Room: /u/yandang/npath2.c

inherit ROOM;

void create() {
  set("short", "山路");
  set("long", @LONG
这是上山的碎石大道，盘旋通往山上，两旁是低矮的树丛。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "westup": __DIR__ "npath3",
    "northdown": __DIR__ "npath1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/snake": 1,
  ]));
  set("no_clean_up", 0);
  set("outdoors", "shiwan");

  setup();
  replace_program(ROOM);
}
