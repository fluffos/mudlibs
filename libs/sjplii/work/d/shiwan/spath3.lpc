// Room: /u/yandang/spath3.c

inherit ROOM;

void create() {
  set("short", "树林边缘");
  set("long", @LONG
这是树林的边缘，隐约可看到南面是一条大路。一条阴深的小径
通往西北方的树林深处。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "spath4",
    "northwest": __DIR__ "spath2",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/bear": 2,
  ]));
  set("no_clean_up", 0);
  set("outdoors", "shiwan");

  setup();
}
