//yangzhou  dufang3.c
inherit ROOM;

void create() {
  set("short", "赌房");
  set("long", @LONG
你来到这里,迎面看到一个巨大的赌台,赌台上铺着厚厚的绿绒垫。你已
经顾不得在看别的地方,就一头扎在人群之中.这里是赌xxx的地方,旁边立了
一块牌子,好象写的是赌场的规矩。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "dating",

  ]));

  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
