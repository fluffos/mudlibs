//yangzhou  wuqidian.c
inherit ROOM;

void create() {
  set("short", "铁匠铺");
  set("long", @LONG
走进这里, 只见四壁摆满了明晃晃的兵刃,有许多兵器你竟然都叫不
上名字。而震耳欲聋的打铁声也更见响亮。这里打造的兵器虽没有所谓的
神兵利器,但也都是百练精钢所铸，锋利异常。你要是缺少兵器的话,不妨
到这里来打造一件。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "nanjie3",

  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/tiejiang": 1,
    ]));

  set("no_clean_up", 0);

  setup();
}
