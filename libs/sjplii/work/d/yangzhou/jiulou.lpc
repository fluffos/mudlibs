//yangzhou  jiulou.c
inherit ROOM;

void create() {
  set("short", "伴月楼");
  set("long", @LONG
扬州小吃风闻天下。而扬州最佳者莫过于此楼，历代王候将相、豪
客名流皆不远千里，慕名而来。楼内阵阵酒香肉气，厨子刀杓声和跑堂
吆喝声响成一片。　　
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "siqiao",
    "up": __DIR__ "erlou",
  ]));

  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/jiubao": 1,
    ]));

  set("no_clean_up", 0);

  setup();
}
