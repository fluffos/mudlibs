//yangzhou  yamen2.c
inherit ROOM;

void create()
{
        set("short", "衙门大堂");
        set("long", @LONG
这里就是扬州府的控制中枢，扬州的所有命令都是由这儿发出的。由
于这里是办公要地，如果你没有什么事的话是不可随意进出这的。精壮的
衙役分立两厢，正当中一张红木桌后坐着一个仙风道骨的老人。
LONG
        );
        set("exits", ([/* sizeof() == 4 */
           "north" : __DIR__"yamen",

]));
        set("objects", ([
        __DIR__"npc/yayi1" : 2,
        __DIR__"npc/zhurt" : 1,
                        ]) );

        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}