//yangzhou  caifengdian.c
inherit ROOM;

void create() {
  set("short", "裁缝店");
  set("long", @LONG
你走进一间堆满了布匹的店铺,原来是一家裁缝店.看这里一副生意兴
隆的样子,往来者不乏高官显贵就可知道这里的手艺一定不凡,你要是有兴
趣的话可以在这里定做衣裳。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "nanjie1",

  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/budian_boss": 1,
    ]));

  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
