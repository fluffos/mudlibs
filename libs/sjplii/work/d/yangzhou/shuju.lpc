//yangzhou  shuju.c
inherit ROOM;

void create() {
  set("short", "书局");
  set("long", @LONG
你来到一间宽敞明亮的房间，只见四壁排满了书架，书架上的书籍从
诸子百家到试词歌赋应有尽有。房间当中有一紫檀书桌，桌上摆放着的笔
墨纸研无一不是精品。靠窗墙上悬挂着一副字画，画中翠竹虽寥寥数笔，
却有破纸而出之式。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "xijie3",

  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/bookseller": 1,
    ]));


  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
