#include <ansi.h>
inherit ROOM;
void create()
{
        set("short", HIC"半转星空"NOR);
        set("long", @LONG
你形随意走，身影在半空中连续几个转折，早已变换了方向。
LONG
        );
        setup();
	replace_program(ROOM);
}

