// yanzhi

inherit ITEM;
#include <ansi.h>

void create()
{
    set_name(WHT"象牙骨子"NOR, ({ "xy-guzi" }) );
    set_weight(500);
    set("long", @LONG 
这是一个象牙做成的骨子。
LONG
  );
    set("unit", "个");
    set("material", "teeth");
       set("value", 20000);
}

