// Room: /city/nandajie2.c

inherit ROOM;

void create()
{
	set("short", "北大街");
	set("long", @LONG
这是一条宽阔的青石街道，向南北两头延伸。北边是北城
门通向城外。东边是一家店铺，墙上写着一个大大的“当”字，
仔细倾听，可以听到压低的讨价还价的声音。
LONG
	);
	set("exits", ([
		"east" : __DIR__"yaopu",
		"south" : __DIR__"beidajie1",
		"west" : __DIR__"dangpu",
		"north" : __DIR__"beimen",
	]));
	set("objects", ([
	__DIR__"npc/rdnpc":1,
	]));
       set("no_clean_up", 0);
	set("outdoors","zhang");

	setup();
	replace_program(ROOM);
}
