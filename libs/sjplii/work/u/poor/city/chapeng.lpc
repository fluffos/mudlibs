// Room: /u/poor/yidao.c

inherit ROOM;

void create()
{
        set("short", "茶棚");
        set("long", @LONG
这是个路边卖茶的小棚子。棚子里只有两张桌子。有几个客
人在这里匆匆喝茶，准备继续赶路。
LONG
        );

        set("exits", ([
                "east" : __DIR__"nyidao3",
        ]));
	set("objects", ([
	__DIR__"npc/rdnpc":1,
	__DIR__"npc/tea_seller":1,
	__DIR__"npc/gdshang":1,
	__DIR__"npc/bing":1,
	]));
	set("outdoors","zhang");
        setup();
	replace_program(ROOM);
}
