// Room: /city/nandajie2.c

inherit ROOM;

void create() {
  set("short", "包子铺");
  set("long", @LONG
你走进了一家包子铺中，包子的香气早就传到了你的鼻子
里。现在不是吃饭的时候，客人并不多。
LONG
  );
  set("exits", ([
    "north": __DIR__ "dongdajie1",
  ]));
  set("objects", ([
    __DIR__ "npc/baozi_seller": 1,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
