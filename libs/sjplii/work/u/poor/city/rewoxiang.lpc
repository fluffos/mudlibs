// Room: /city/nandajie2.c

inherit ROOM;

void create() {
  set("short", "绣房");
  set("long", @LONG
你来到了香兰的房间，刚进屋就闻到了一阵阵的香气。在屋
子正中摆放着一个八仙桌，右边就是香兰的绣床。
LONG
  );
  set("exits", ([
    "out": __DIR__ "rewo2",
  ]));
  set("objects", ([
    __DIR__ "npc/xianglan": 1,
  ]));

  setup();
  replace_program(ROOM);
}
