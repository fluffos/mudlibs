// Room: /city/nandajie2.c

inherit ROOM;

void create() {
  set("short", "饺子馆");
  set("long", @LONG
这里是全镇唯一的一家饺子馆，这里做的饺子十分有名。
其中最好吃的是用鸡汤、虾肉、鸡蛋等做的饺子。在这里吃
的客人都是挺着肚子走的。
LONG
  );
  set("exits", ([
    "north": __DIR__ "dongdajie2",
  ]));
  set("objects", ([
    __DIR__ "npc/jiaozi_seller": 1,
    __DIR__ "npc/girl": 1,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
