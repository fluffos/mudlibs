// Room: /city/nandajie2.c

inherit ROOM;

void create()
{
	set("short", "福运酒楼二楼");
	set("long", @LONG
这里摆设十分讲究，在二楼摆放了约莫十张桌子，桌子都
擦的十分干净。从二楼向外望去，整个张家口尽收眼底。
LONG
	);
	set("exits", ([
		"down" : __DIR__"jiulou1",
	]));
	set("objects", ([
		__DIR__"npc/cai_seller" : 1,
		__DIR__"npc/hancao1" : 1,
		__DIR__"npc/rdnpc" : 2,
	]));
	set("no_fight", 1);
       set("no_clean_up", 0);

	setup();
}
