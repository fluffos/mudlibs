// stone.c

inherit ITEM;
#include <ansi.h>
void create()
{
    set_name( WHT "商代玉镯" NOR, ({ "sd-yuzhuo","yuzhuo" }) );
    set_weight(5000);
    set("long", @LONG 
这是一只做工精美的玉镯。
LONG
  );
    set("value", 8000);
    set("unit", "只");
    set("material", "gold");
}

