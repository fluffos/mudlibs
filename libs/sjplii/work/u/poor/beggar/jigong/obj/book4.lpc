// stone.c

inherit ITEM;

void create()
{
    set_name("《商殷物学》", ({ "earth book","book" }) );
    set_weight(500);
    set("long", @LONG 
这是一卷介绍商殷时期各方面的书籍。
LONG
  );
    set("value", 1);
    set("unit", "卷");
    set("material", "paper");
}

