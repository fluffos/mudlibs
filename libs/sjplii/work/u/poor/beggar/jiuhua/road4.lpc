// /u/poor/gaibang/hj
inherit ROOM;

void create() {
  set("short", "山腰");
  set("long", @LONG
你来到了九华山的山腰，在东边有一座飞鹰寨，寨中住着
很多绿林强盗。从这里就能看见飞鹰寨中的大旗。东边是铁索
桥，北边是高不可攀的峭壁，南边是一片树林，但是没有人敢
进去。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "qiaobi2",
    "south": __DIR__ "shulin1",
    "east": __DIR__ "fydoor",
    "west": __DIR__ "tiesuo1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/qiangdao": 1,
  ]));

  set("no_clean_up", 0);
  set("outdoors", "gaibang");

  setup();
}
