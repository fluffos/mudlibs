// banker.c

#include <ansi.h>

inherit NPC;

void create()
{
        set_name("喽罗", ({ "louluo" }) );
        set("title", "飞鹰寨秃鹰堂");
        set("gender", "男性" );
        set("age", 31);
        set("long",
"他是飞鹰寨中的喽罗，干尽了各种伤天害理的坏事。\n");
 
        set("combat_exp", 30000);
        set("attitude", "aggressive");

        set("max_force", 500);         set("force", 800);             
	 set("force_factor", 10);

        set_skill("unarmed", 50);
        set_skill("parry", 40);
        set_skill("dodge", 40);
        set_skill("force", 40);
        
        setup();
        
        carry_object(__DIR__"obj/cloth")->wear();
}

int accept_fight(object me)
{
        command("say 咦... 要打就真打吧，光是较量多没意思？");
        kill_ob(me);
        return 1;
}
 
