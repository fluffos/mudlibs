// /u/poor/gaibang/hj
inherit ROOM;

void create()
{
        set("short", "飞鹰寨练武场");
        set("long", @LONG
你来到了飞鹰寨的练武场，东边是飞鹰寨的大厅，西边
是山寨大门，北面和南面都是山寨喽罗住的地方。
LONG
        );
        set("exits", ([/* sizeof() == 1*/
          "west" : __DIR__"fydoor",
          "east" : __DIR__"fyhall",
          "north" : __DIR__"fyroom1",
          "south" : __DIR__"fyroom2",
]));
       set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/boss1" : 1,
  __DIR__"npc/louluo" : 3,
]));

        set("no_clean_up", 0);
	set("outdoors","gaibang");

        setup();
}
