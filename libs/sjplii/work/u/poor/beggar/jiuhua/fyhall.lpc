// Room: /jing/duankit.c

inherit ROOM;

void create()
{
	set("short", "飞鹰寨大厅");
	set("long", @LONG
你走进了飞鹰寨的大厅，这里四处灯火辉煌。中间铺了一张虎皮，
两边是几把太师椅。在中间坐的是飞鹰寨的寨主和寨主夫人。
LONG
	);
	set("item_desc", ([ /* sizeof() == 1 */
  "虎皮" : "一张白色的虎皮。",
]));
        
       set("exits", ([ /* sizeof() == 1 */
  "out" : __DIR__"fymid",
]));
       set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/boss" : 1,
  __DIR__"npc/furen" : 1,
]));


	set("no_clean_up", 0);

	setup();
}

