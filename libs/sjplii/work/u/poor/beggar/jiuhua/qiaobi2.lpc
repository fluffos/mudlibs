// /u/poor/gaibang/hj
inherit ROOM;

void create()
{
        set("short", "峭壁");
        set("long", @LONG
你的面前是高不可攀的峭壁，峭壁上边就是「九华十景」
中的一景“天柱仙踪”。这里的峭壁很光滑，不可能用任何
东西能爬上去。
LONG
        );
        set("exits", ([/* sizeof() == 1 */
           "south" : __DIR__"road4",
]));
        set("no_clean_up", 0);
	set("outdoors","gaibang");

        setup();
}
