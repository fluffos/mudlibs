inherit ROOM;

void create()
{
        set("short", "寺门");
        set("long", @LONG
你来到了「甘露寺」的寺门前，两扇红漆大门紧鼻着，旁边开
着一个小门。在门口有一个小和尚正在招呼进香的客人。
LONG
        );
        set("exits", ([/* sizeof() == 2 */
            "west" : __DIR__"road7",
            "east" : __DIR__"glqiangc",
]));
	set("objects",([
	__DIR__"npc/qingfeng" : 1,
	]));

        set("no_clean_up", 0);
	set("outdoors","gaibang");

        setup();
        replace_program(ROOM);
}
