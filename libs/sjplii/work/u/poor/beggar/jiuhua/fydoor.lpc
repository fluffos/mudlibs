// /u/poor/gaibang/hj
inherit ROOM;

void create()
{
        set("short", "飞鹰寨大门");
        set("long", @LONG
这里就是飞鹰寨的大门口，在大门的左右各有一根旗杆，
旗子上写着几个大字“飞鹰寨”。现在飞鹰寨大门进闭，只有
一个小喽罗在门口巡逻。
LONG
        );
        set("exits", ([/* sizeof() == 1*/
          "west" : __DIR__"road4",
]));
       set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/xiaolouluo" : 1,
]));

        set("no_clean_up", 0);
	set("outdoors","gaibang");

        setup();
}
