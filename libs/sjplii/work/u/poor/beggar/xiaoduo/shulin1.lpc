// Room: /beggar/door.c
#include <ansi.h>;

inherit ROOM;

void create() {
  set("short", "树林");
  set("long", @LONG
你来到了树林之中，四周都是茂密的树林，只有星星点点的
阳光穿过枝叶照在地上。不时能听见鸟叫的声音。
LONG
  );

  set("exits", ([ /* sizeof() == 3 */
    "south": "/u/poor/city/dyidao3",
    "northwest": __DIR__ "xdoor",
  ]));
  set("outdoors", "gaibang");
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
