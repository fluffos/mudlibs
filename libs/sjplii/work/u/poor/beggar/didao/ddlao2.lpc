// Room: /u/poor/gaibang/zhulin2.c

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIB "地牢" NOR);
  set("long", @LONG
你来到了一间地牢之中，这里四壁空空，没有什么东西。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "ddlao1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/qiufan1": 1,
    __DIR__ "npc/qiufan": 2,
  ]));
  set("no_clean_up", 0);

  setup();
}
