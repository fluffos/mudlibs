// Room: /u/poor/gaibang/zhulin2.c

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIB "牢房" NOR);
  set("long", @LONG
这里是间阴暗的牢房，在牢房的左边有一张石床。床的旁
边放着几个汤罐和饭碗。石床上躺着一个老人。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "out": __DIR__ "ddguan5",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/shihaifeng": 1,
  ]));
  set("no_clean_up", 0);

  setup();
}
