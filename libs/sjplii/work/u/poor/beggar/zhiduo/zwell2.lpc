// Room: /gaibang/gbwuch4.c

inherit ROOM;
#include <ansi.h>;

create()
{
     	set("short","水中");
	set("long", @LONG
你跳进了井水当中，这里一片朦胧，能够隐隐约约上
边有些亮光。
LONG
	);
       set("exits", ([ /* sizeof() == 1 */
  "up" : "/u/poor/beggar/didao/ddzhi",
]));
       set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}

