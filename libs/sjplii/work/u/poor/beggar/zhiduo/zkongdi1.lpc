// Room: /gaibang/gbwuch1.c

inherit ROOM;
#include <ansi.h>;

create()
{
     	set("short", HIB"林中空地" NOR);
	set("long", @LONG
这里是丐帮智舵弟子练功的地方，有不少丐帮的弟子在此修习
武功。南边是智舵的大门，东边是武器库。西北边好象有个小屋。
LONG
	);
       set("sleep_room",1);	
	set("exits", ([ /* sizeof() == 3 */
  "east" : __DIR__"zwuqi",
  "north" : __DIR__"zkongdi2",
  "south" : __DIR__"zdoor",
  "west" : __DIR__"zsonglin1",
]));
	set("outdoors","gaibang");
        set("no_clean_up", 0);
	set("valid_startroom", 1);

	setup();
	replace_program(ROOM);
}

