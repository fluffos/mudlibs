//dcdian  大成殿

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIB "方丈室" NOR);
  set("long", @LONG
这里就是这座破庙的方丈室，在正中的地上放着个很脏蒲团。
室中并无其他物品。在墙角躺着一个乞丐。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "lhouyuan",
  ]));
  set("objects", ([
    __DIR__ "npc/luohong": 1,
  ]));

  set("no_clean_up", 0);

  setup();

  replace_program(ROOM);
}
