// Room: /u/poor/gaibang/gbwuch2

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIB "院墙外" NOR);
  set("long", @LONG
你来到了一片树林的前边，向北走是曾层叠叠的树木。在
西边有一间草舍，东边就是古庙的院墙。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "lshulin",
    "west": __DIR__ "lshen",
  ]));
  set("outdoors", "gaibang");
  set("no_clean_up", 0);

  setup();
}

void init() {
  add_action("do_climb", "climb");
}

int do_climb(string arg) {
  if (arg == "院墙" || arg == "wall") {
    if (this_player()->query_skill("dodge", 1) <= 40) {
      write("你刚上院墙，一不小心摔了下来。\n");
      this_player()->unconcious();
      return 1;
    }
    message_vision("$N双脚用力，一下就跳过了院墙。\n", this_player());
    this_player()->move(__DIR__ "lhouyuan");
    message("vision", this_player()->name() + "爬过了院墙。\n",
      environment(this_player()), this_player());
    return 1;
  } else return 0;
}
