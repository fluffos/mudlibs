// /u/poor/gaibang/zhongxin
inherit ROOM;

void create()
{
        set("short", "打谷场");
        set("long", @LONG
这里是鸡华村的打谷场，平时农闲的时候，这里就是全村人
集会的地方。从这里能通到全村的每个地方。
LONG
        );
        set("exits", ([/* sizeof() == 4 */
           "north" : __DIR__"jhn1",
           "south" : __DIR__"jhs1",
           "west" : __DIR__"jhw1",
           "east" : __DIR__"jhe1",
]));
        set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/jhfarm1" : 1,
  __DIR__"npc/jhfarm2" : 1,
]));
        set("no_clean_up", 0);
	set("outdoors","gaibang");

        setup();
        replace_program(ROOM);
}