// fish

inherit ITEM;
inherit F_FOOD;

void create()
{
	set_name("鲫鱼", ({ "jiyu" }) );
	set_weight(80);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("long", "一条刚死不久的鲫鱼。\n");
		set("unit", "条");
		set("value", 1300);
		set("food_remaining", 3);
		set("food_supply", 20);
	}
}
