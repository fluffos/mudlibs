// stone.c

inherit ITEM;

void create()
{
    set_name("商殷藏宝图的右边", ({ "map-right" }) );
    set_weight(1000);
    set("long", @LONG 
                |
     ∧山寨     |
     ≌∧ ∧    |
     桥 ※※    |
     鸡 林海∧  |
     ∧ ※※◇  |
     ∧∧ ∧∧  |
LONG
  );
    set("unit", "张");
    set("material", "paper");
}

