// jhtown/jhboss   by poor
inherit ROOM;

void create()
{
        set("short", "村长的家");
        set("long", @LONG
村长的家布置的很简朴，几张旧花梨木椅子摆在两旁，一张半
新不旧的八仙桌。正对门口的墙壁上一幅画，画的是观世音菩萨，
看来主人还指望菩萨显灵，保佑他的全家。
LONG
        );
        set("exits", ([/* sizeof() == 2 */
           "south" : __DIR__"jhe1",
]));
        set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/jhboss" : 1,
]));
        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}