//huadian.c
inherit ROOM;

void create() {
  set("short", "花店");
  set("long", @LONG
这里是鸡华里唯一的一家花店，只有几种普普通通鲜花，主
要都是卖给有钱人的，贫民百姓很少来这里买花。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "jhw2",
  ]));
  set("objects", ([
    __DIR__ "npc/hua_seller": 1,
  ]));
  set("no_clean_up", 0);

  setup();
}
