// Room: /u/poor/gaibang/gbwuch2

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIB "石屋" NOR);
  set("long", @LONG
这是一间阴暗潮湿的石屋，在屋子的左边有一张石床，一张
石桌放在窗前。在屋子的正中站着个老者。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "zdroad2",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/u/poor/beggar/npc/wuyi/xiaomk": 1,
  ]));
  set("no_clean_up", 0);

  setup();
}
