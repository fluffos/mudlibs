// Room: /gaibang/gbwuch1.c

inherit ROOM;
#include <ansi.h>;

create()
{
     	set("short", HIB"南练武场" NOR);
	set("long", @LONG
这里是丐帮弟子的露天练武场，有许多丐帮弟子在此习武弄棒。
南边是丐帮的大门，北边和东边也是丐帮弟子练武的地方。
LONG
	);
	set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/beggar" : 2,
]));
       set("sleep_room",1);	
	set("exits", ([ /* sizeof() == 3 */
  "east" : __DIR__"zdwuch2",
  "north" : __DIR__"zdwuch3",
  "south" : __DIR__"zddoor",
  "west" : __DIR__"zdzoul1",
]));
	set("outdoors","gaibang");
        set("no_clean_up", 0);
	set("valid_startroom", 1);

	setup();
	replace_program(ROOM);
}

