// Room: /u/poor/beggar/zhulin3.c

inherit ROOM;

void create()
{
	set("short", "竹林");
	set("long", @LONG
一条小石路，路的两侧是大片的竹林，阳光透过竹林泻在路上，映射
出斑驳的影象，又不时有阵阵微风吹过，发出“沙，沙”地声音。
LONG
	);

	set("exits", ([ /* sizeof() == 1 */
    "north" : __DIR__"zhulin12",
    "east" : __DIR__"zhulin6",
    "west" : __DIR__"zhulin17",
    "south" : __DIR__"zhulin16",
]));
           set("objects", ([ /* sizeof() == 1 */
  __DIR__"obj/stone" : 1,
]));
    set("no_clean_up", 0);
	set("outdoors","gaibang");

	setup();
	replace_program(ROOM);
}
