// Room: /u/poor/gaibang/zhulin2.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIB"丹房"NOR);
	set("long", @LONG
这里是丐帮仁舵的丹房，在门左边的架子上摆放着两排小
瓶子，在左边有两把椅子，椅子旁边坐着一个女子。
LONG
	);

	set("exits", ([ /* sizeof() == 2 */
    "west" : __DIR__"renyzi2",
]));
       set("objects", ([ /* sizeof() == 1 */
  "/u/poor/beggar/npc/wuyi/liuwuyan" : 1,
__DIR__"obj/shuigang" : 1,
//  "/u/poor/beggar/npc/wuyi/liuwuyan" : 1,
]));
        set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
