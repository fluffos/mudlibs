// Room: /u/poor/gaibang/zhulin2.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIB"树林中"NOR);
	set("long", @LONG
你走进了树林之中，四周都是高大挺拔的树木，几乎没有
低矮灌木。
LONG
	);

	set("exits", ([ /* sizeof() == 2 */
    "north" : __DIR__"renhan2",
    "east" : __DIR__"renhan1",
    "south" : __DIR__"renhan2",
    "west" : __DIR__"renhan2",
]));
       set("objects", ([ /* sizeof() == 1 */
  "/u/poor/beggar/npc/wuyi/hanyuan" : 1,
]));
        set("no_clean_up", 0);
	set("outdoors","gaibang");

	setup();
	replace_program(ROOM);
}
