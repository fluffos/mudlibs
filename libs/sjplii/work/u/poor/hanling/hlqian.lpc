// Room: /beggar/door.c
#include <ansi.h>;

inherit ROOM;

void create() {
  set("short", "前院");
  set("long", @LONG
你走到了寒灵山庄的前院，在院子中种着两棵树，一棵红
松，另一棵是梧桐。北边是寒灵山庄的寒灵厅，西边和东边各
有一个房间。
LONG
  );

  set("exits", ([ /* sizeof() == 3 */
    "south": __DIR__ "hlroad5",
    "north": __DIR__ "hlhall",
    "west": __DIR__ "hlqin",
    "east": __DIR__ "hlye",
  ]));
  set("objects", ([
    //	__DIR__"npc/han1":2,
  ]));
  set("outdoors", "hanling");

  setup();
  replace_program(ROOM);
}
