// Room: /beggar/door.c
#include <ansi.h>;

inherit ROOM;

void create()
{
	set("short", "寒灵厅");
	set("long", @LONG
你来到了寒灵厅中，大厅中间的墙上挂了一张寒灵祖师的
画像，这好象就是天下武功第一的天帝。下边是张供桌，桌子
上边摆放着各式各样的供品。
LONG
	);

	set("exits", ([ /* sizeof() == 3 */
       "south" : __DIR__"hlqian",
]));
	set("objects", ([
	__DIR__"npc/hanpf":1,
]));

	setup();
	replace_program(ROOM);
}

