// stone.c

inherit ITEM;

void create()
{
    set_name("推荐信", ({ "tj-xin" }) );
    set_weight(100);
    set("long", @LONG 
这是一封玄难给师父无边大师的信。
LONG
  );
    set("value", 0);
    set("unit", "封");
    set("material", "stone");
}

