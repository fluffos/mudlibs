// Room: /beggar/door.c
#include <ansi.h>;

inherit ROOM;

void create()
{
	set("short", "寒灵山庄大门");
	set("long", @LONG
你来到了寒灵山庄的大门前，两扇红漆大门敞开着。在门
两边有各有一个石头怪兽，在门上挂着一块匾，上别写着四个
大字“武林至尊”。
LONG
	);

	set("exits", ([ /* sizeof() == 3 */
       "south" : __DIR__"hlroad5",
       "north" : __DIR__"hlqian",
]));
	set("objects", ([
	__DIR__"npc/han1":2,
]));
	set("outdoors","hanling");

	setup();
	replace_program(ROOM);
}

