// Room: /u/k/karrimor/temple/ladder4.c

inherit ROOM;

void create() {
  set("short", "大理石阶");
  set("long", @LONG
你正走在一道长长的大理石阶梯上, 阶梯的两侧种植著一排高耸
的松树, 杭州城就在东边不远的山下, 你隐约还能听到从城里传来的
话语声。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "eastup": __DIR__ "ladder3",
    "westdown": __DIR__ "ladder5",
  ]));
  set("outdoors", "temple");
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/guest": 1,
  ]));

  setup();
  replace_program(ROOM);
}
