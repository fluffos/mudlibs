inherit ROOM;

void create()
{
	set("short", "小路");
	set("long", @LONG
这是一条安静的小路，因为是农民工作的时间，这里很少人
，平时在家管事的男人们都会到这里的茶馆来，谈天说地，喝茶
听戏。对于书店，那里只是老学究们的地方。在远处有一个小酒
馆。
LONG
	);
	set("exits", ([ /* sizeof() == 4 */
	"north" : __DIR__"bookshop",
	"south" : __DIR__"tea",
	"west" : __DIR__"cross",
	"east" : __DIR__"jiuguan",
	]));
	set("no_clean_up", 0);
        set("outdoors", "jakey");
	setup();
	replace_program(ROOM);
}
