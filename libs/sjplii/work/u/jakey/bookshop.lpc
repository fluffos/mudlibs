inherit ROOM;

void create() {
  set("short", "书店");
  set("long", @LONG
你走进了一间书店，店里打扫的干干净净，一个戴着眼镜的
老人正在拿着个鸡毛掸子在书架上掸来掸去。里面的书桌上摆着
个帐本，一枝毛笔搁在砚台上。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "lpath3",
  ]));
  set("objects", ([
    __DIR__ "npc/zhuge": 1,
  ]));
  set("no_clean_up", 0);
  setup();
}
