#include <armor.h>

inherit ITEM;

void create()
{
	set_name("火形铁牌", ({ "fire token","token" }) );
	set_weight(700);
		set("long",
			"这是一个火焰形状的铁牌，不知道是干什么用的。\n");
		set("unit", "块");
		set("value", 10);
}
