inherit ROOM;

void create()
{
	set("short", "小巷");
	set("long", @LONG
这是一个居民区，远远的可以看见本镇的大户--李员外家。
北面和南面都是民宅。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	"east" : __DIR__"xx1",
	"west" : __DIR__"damen2",
	]));
	set("no_clean_up", 0);
        set("outdoors", "jakey");
	setup();
	replace_program(ROOM);
}
