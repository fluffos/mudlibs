// Room: /u/jakey/qspath1.c

inherit ROOM;

void create()
{
	set("short", "青石小路");
	set("long", @LONG
这是一条由青石铺成的小路。路上行人稀少，路边杂草丛生，远处
通向落月山庄。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "west" : "/u/huangshan/jiuhua2",
  "east" : __DIR__"qspath2",
]));
	set("no_clean_up", 0);
	set("outdoors", "jakey");

	setup();
	replace_program(ROOM);
}
