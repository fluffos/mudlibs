inherit ROOM;

void create()
{
	set("short", "小室");
	set("long", @LONG
棕黄色的木墙，棕黄色的地面，棕黄色的家具。一切都显得
那么的脱俗。在一个小柜子的上面摆着一个棕黄色的可爱小熊。
这里打扫的一尘不染，看来主人出去的时间不长。
LONG
	);
	set("exits", ([ /* sizeof() == x */
	"south" : __DIR__"home1",
	"east" : __DIR__"home3",
	"west" : __DIR__"home4",
	]));
	set("no_clean_up", 0);       
	setup();
	replace_program(ROOM);
}
