inherit ROOM;

void create() {
  set("short", "卧室");
  set("long", @LONG
这里是个小房间。虽然小，但是很朴素。有一张蓝色大床在
屋子的正中间摆着。洁白的床单，洁白的枕头，洁白的被子。在
四周有两个大桌子，上面摆着一些杂志还有一个木杯子。在木窗
上面挂着个蓝色的小风铃。
LONG
  );
  set("exits", ([ /* sizeof() == x */
    "east": __DIR__ "home2",
  ]));
  set("no_clean_up", 0);
  set("valid_startroom", 1);
  setup();
  replace_program(ROOM);
}
