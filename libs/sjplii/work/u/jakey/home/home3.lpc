inherit ROOM;

void create()
{
	set("short", "卧室");
	set("long", @LONG
这里是一间小小的卧室，里面摆着一张红木床，床不是很大
，上面铺着洁白的床单，还有一个大枕头。被子叠的很整齐的摆
在床脚。四周有着一个小桌子和一个小柜子。在地上还趴着一只
可爱的长毛大狗。
LONG
	);
	set("exits", ([ /* sizeof() == x */
	"west" : __DIR__"home2",
	]));
	set("no_clean_up", 0);       
	setup();
	set("valid_startroom",1);
	replace_program(ROOM);
}
