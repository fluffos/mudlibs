inherit ROOM;

void create() {
  set("short", "走廊");
  set("long", @LONG
这里已经没有东西遮挡你的上方，两旁是大红色的院墙，青
色的砖铺在地上，两旁每隔几尺就有一个石人，石人或哭或笑，
或嗔或怒，每个石人都不同，共有九个。再往前走几步就到小院
了。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "xyuan1",
    "southeast": __DIR__ "zl2",
  ]));
  set("no_clean_up", 0);

  setup();
}
