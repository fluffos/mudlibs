inherit ROOM;

void create()
{
	set("short", "民宅");
	set("long", @LONG
这里是有不大的小屋，里面摆着几个桌子，上面铺着些
帐本，四面墙上白白净净，有个胡子都花白的老人在仔细的
看着帐目。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
	"south" : __DIR__"yaodian",
	]));
	set("objects", ([ /* sizeof() == 1 */
	__DIR__"npc/wangqin" : 1,
	]));
	set("no_clean_up", 0);       
	setup();
	replace_program(ROOM);
}
