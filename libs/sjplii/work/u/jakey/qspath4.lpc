inherit ROOM;

void create() {
  set("short", "青石小路");
  set("long", @LONG
这是一条由青石铺成的小路。路边杂草丛生，向北就是通向
落月山庄的青石台阶。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "northup": __DIR__ "qstj1",
    "west": __DIR__ "qspath3",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "jakey");
  setup();
  replace_program(ROOM);
}
