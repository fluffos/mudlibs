inherit ROOM;

void create()
{
	set("short", "青石小路");
	set("long", @LONG
这条小路向西延伸着。有一条黄土小路从这青石小路上向北
分了出去。
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
	"east" : __DIR__"qspath3",
	"west" : __DIR__"qspath1",
	"north" : __DIR__"htpath1",
	]));
	set("no_clean_up", 0);
        set("outdoors", "jakey");
	setup();
	replace_program(ROOM);
}
