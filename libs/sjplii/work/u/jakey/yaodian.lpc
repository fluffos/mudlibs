inherit ROOM;

void create()
{
	set("short", "药店");
	set("long", @LONG
这里是个药店，已经开了很长时间了，信誉很好。不大的店
里到处弥漫着各种中草药淡淡的香气。在狭长的柜台后是一个个
的药柜。北面的墙上有一块匾，上书有“良药苦口”四个大字。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
	"west" : __DIR__"htpath3",
	]));
	set("objects",([
	__DIR__"npc/wang" : 1,
	]));
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
