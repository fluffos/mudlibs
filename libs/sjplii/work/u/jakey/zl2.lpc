inherit ROOM;

void create() {
  set("short", "走廊");
  set("long", @LONG
这是一条走廊，每二十步就有一盆花草，廊中的有一些字画
，虽说不是名家手笔，但是也给这里添加了不少情趣。再往西走
就到茅房了。向北走是一个小院。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "northwest": __DIR__ "zl1",
    "west": __DIR__ "wc",
    "east": __DIR__ "zl3",
  ]));
  set("no_clean_up", 0);

  setup();
}
