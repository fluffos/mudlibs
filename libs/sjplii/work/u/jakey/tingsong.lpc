inherit ROOM;

void create()
{
	set("short", "听松亭");
	set("long", @LONG
这里是坐落于落月山庄后院的听松亭。亭子坐落于落月山庄
的一个崖上，在亭子下面是层层的松海。风从松海上拂过发出的
声响的确好听极了。亭子当中摆放着一个石桌，上面还有图案呢。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	"southeast" : __DIR__"hspath",
	]));
	set("objects",([
	__DIR__"npc/hufa4" :1,
	]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
