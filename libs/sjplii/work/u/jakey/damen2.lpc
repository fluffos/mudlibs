inherit ROOM;

void create() {
  set("short", "大门");
  set("long", @LONG
你现在已经站在李员外的大门外了，朱红色大门漆的是光可
鉴人，由于是在巷子的深处，门口冷清清的，没有什么人来拜访
。一个家仆在打瞌睡。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "xx2",
    "west": __DIR__ "qianting",
  ]));
  set("objects", ([
    __DIR__ "npc/puren3": 1,
  ]));
  set("no_clean_up", 0);
  set("outdoors", "jakey");
  setup();
}
