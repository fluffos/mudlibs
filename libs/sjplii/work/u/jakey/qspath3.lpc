inherit ROOM;

void create() {
  set("short", "青石小路");
  set("long", @LONG
这是一条由青石铺成的小路。路边杂草丛生，远处通向落月
山庄。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "qspath4",
    "west": __DIR__ "qspath2",
  ]));
  set("objects",
    ([
      "/npc/nknpc": 1,
    ]));
  set("outdoors", "jakey");
  setup();
  replace_program(ROOM);
}
