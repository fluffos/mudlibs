inherit ROOM;

void create() {
  set("short", "铁铺");
  set("long", @LONG
这里是再来镇上铁匠铺，打的都是一些这里的农民需要的铁
制工具，里面烟雾弥漫，叮叮当当的打铁声不绝与耳。张铁匠正
在熊熊的炉火前忙碌着。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "htpath3",
  ]));
  set("objects", ([
    __DIR__ "npc/tiejiang": 1,
    __DIR__ "npc/xuetu": 1,
  ]));
  set("no_clean_up", 0);
  setup();
}
