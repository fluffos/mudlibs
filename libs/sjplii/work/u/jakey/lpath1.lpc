inherit ROOM;

void create() {
  set("short", "小路");
  set("long", @LONG
你走在一条通往山下的小路上，小路顺着山势蜿蜒而下，路
边开着野花。从这里，你能看见前面不远处有一个茅草亭。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "northup": __DIR__ "pianmen",
    "southdown": __DIR__ "caoting",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "jakey");
  setup();
}
