inherit ROOM;

void create()
{
	set("short", "小院");
	set("long", @LONG
这里是个小院子，院子了种了花草，中间有个小石桌子，还
有几个石墩子。现在这里没有人。前方是论剑轩，西边是棋斋，
东边是个会友园。那里通往书寓和练功房。
LONG
	);
	set("exits", ([ /* sizeof() == 4 */
	"east" : __DIR__"huiyou",
	"west" : __DIR__"qishi",
	"south" : __DIR__"zl6",
	"north" : __DIR__"lunjian",
	]));
	set("no_clean_up", 0);
        set("outdoors", "jakey");
	setup();
}

int valid_leave(object me,string dir)
{
	if (dir=="north")
	me->start_busy(3);
	return 1;
}

