inherit ROOM;

void create()
{
	set("short", "走廊");
	set("long", @LONG
这是一条走廊，每二十步就有一盆花草，廊中的有一些字画
，虽说不是名家手笔，但是也给这里添加了不少情趣。再往东走
就到厨房了。向北走是一个小院。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	"northeast" : __DIR__"zl6",
	"east" : __DIR__"chufang",
	"west" : __DIR__"zl4",
	]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
