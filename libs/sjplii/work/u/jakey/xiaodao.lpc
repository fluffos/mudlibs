inherit ROOM;

void create()
{
	set("short", "黑暗地道");
	set("long", @LONG
一条比较阴暗的地道，很干净。往下可以看到有光亮传了过
来，赶快走吧。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	"northdown" : __DIR__"mishi",
	"out" : __DIR__"bookroom",
	]));
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
