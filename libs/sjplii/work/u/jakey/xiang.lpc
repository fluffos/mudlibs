inherit ROOM;

void create() {
  set("short", "香堂");
  set("long", @LONG
这里面烟雾缭绕，供的是观音大士，两旁站着金童玉女，李
员外膝下无子，这尊观音神像听说是从南海求来的。李夫人现在
就在虔诚的祈求上苍呢。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "zl7",
  ]));
  set("objects", ([
    __DIR__ "npc/furen2": 1,
  ]));
  set("no_clean_up", 0);
  setup();
}
