inherit ROOM;

void create() {
  set("short", "水道");
  set("long", @LONG
这里有一个小小的石道，半截在水中，半截在地上，往上黑
不隆冬的，只有微弱的光线。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "westdown": __DIR__ "shuitan1",
    "eastup": __DIR__ "shuitan3",
  ]));
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
