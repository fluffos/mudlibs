inherit ROOM;

void create()
{
	set("short", "律院");
	set("long", @LONG
这里是落月山庄弟子最不愿意来的地方。因为来这里的弟子
只有一个原因，那就是因为触犯落月山庄的戒律被送来受罚的。
这里的管理者是落月山庄护法中脾气最暴躁的洛护法。他疾恶如
仇，赏罚分明。要是谁犯了错来到是这里少不了一顿痛打的。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
	"east" : __DIR__"qianyuan",
	]));
	set("objects", ([ 
	__DIR__"npc/hufa3" : 1,
	]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
