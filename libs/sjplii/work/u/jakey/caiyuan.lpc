inherit ROOM;

void create()
{
	set("short", "财院");
	set("long", @LONG
听名字就知道这里是落月山庄管理财物的地方。这里的总管
就是落月山庄的大总管--钱多多。屋里布置的富丽堂皇，屋子里
面摆着贵重的物品不说，最引人注目的要数钱大总管坐着的那把
摇椅了，听说这可是鲁班做的，共壹拾贰把，民间可就剩这一把
了。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
	"west" : __DIR__"qianyuan",
	]));
	set("objects",([
	__DIR__"npc/rich" :1,
	]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
