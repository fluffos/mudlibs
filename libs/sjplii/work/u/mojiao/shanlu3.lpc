#include <ansi.h>
#include <room.h>
inherit ROOM;

void create() {
  set("short", "山麓");
  set("long", @LONG
随着山势的陡峭，小路也越来越不好走起来，周围的山石突恶，已经
看不到前后的路，只有靠着攀沿着山石，脚踏着突起之处才可前进。山中
气温低，风一吹就让人心中发冷。
LONG
  );
  set("outdoors", "tianzhu");
  set("exits", ([
    "westdown": __DIR__ "shilin1",
    "eastup": __DIR__ "tianzhu0",
  ]));
  /*
	set("objects",([
	 	__DIR__"npc/master" : 1,
	]));
*/
  setup();
}
