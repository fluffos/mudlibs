//tulu2.c written by john darks
inherit ROOM;
void create()
{
	set("short", "黄土路");
	set("long", @LONG
一条黄土铺就的大路，两旁挺拔的白杨，微风吹来树叶哗哗做响。
偶尔有一匹快马从身边飞驰而过。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  	"east" : __DIR__"tulu2",
  	"west" : __DIR__"town1",
]));
	set("outdoors","tianzhu");
        set("objects", 
        ([
              "/npc/nknpc" : 1,
        ]));
	setup();
	replace_program(ROOM);
}
