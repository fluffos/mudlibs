// Room: /new/mojiao/w-road3.c

inherit ROOM;

void create() {
  set("short", "上山石级");
  set("long", @LONG
这是一条长长的上山石级。一直通到山上。两旁怪石嶙峋，长满青苔。
山壁非常陡峭。但见彩云飘飘，如入仙境，上面就是天柱山顶了。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "southdown": __DIR__ "w-road2",
    "northup": __DIR__ "shanding",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    "/obj/example/dragon": 1,
    "/obj/example/roc": 1,
  ]));
  set("no_clean_up", 0);
  set("valid_startroom", 1);

  setup();
}
