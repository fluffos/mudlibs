// Room: /d/temple/ladder3.c

inherit ROOM;

void create() {
  set("short", "大理石阶");
  set("long", @LONG
你正走在一道长长的大理石阶梯上, 阶梯的两侧种植著一排高耸
的松树, 阶梯从这里弯向南边。　
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "southup": __DIR__ "ladder2",
    "westdown": __DIR__ "ladder4",
  ]));
  set("objects",
    ([
      "/npc/nknpc": 1,
    ]));
  set("outdoors", "temple");

  setup();
}
