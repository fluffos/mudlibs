
inherit ROOM;

void create()
{
	set("short", "上山石级");
	set("long", @LONG
这是一条长长的上山石级。一直通到山上。两旁怪石嶙峋，长满青苔。
山壁非常陡峭。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "westdown" : __DIR__"shanjiao",
  "eastup" :  __DIR__"w-road2",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
