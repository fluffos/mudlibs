// Room: /u/qingcheng/ro4.c

inherit ROOM;

void create()
{
	set("short", "碎石路");
	set("long", @LONG
你走在山庄小路上，下面铺的是非常平整的均匀的石子，路的两边
是茂密的竹林，南面、北面各有一个岔道。到了这里，你才了解到什么
叫曲径通幽。走在路上随时可以听见咚咚的泉水声。
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"po1",
  "west" : __DIR__"dating.lpc",
  "east" : __DIR__"ro3",
  "south" : __DIR__"po2",
]));

	setup();
	replace_program(ROOM);
}
