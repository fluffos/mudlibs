// Room: /u/qingcheng/lian3.c

inherit ROOM;

void create() {
  set("short", "练功房");
  set("long", @LONG
    这里是山庄内部弟子用来练功的地方。房内有四个正在练功的
青城弟子,地板上不时可见练功留下的印痕。
LONG
  );
  set("no_clean_up", 0);
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/gdizi": 4,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "lian1",
  ]));

  setup();
}
