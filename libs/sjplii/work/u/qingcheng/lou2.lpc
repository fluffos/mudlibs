// Room: /u/qingcheng/lou2.c

inherit ROOM;

void create()
{
	set("short", "二楼");
	set("long", @LONG
这里是山庄的藏书之地。四周摆满了书架，书架上是数不清的
线装书，好象写着什么秘籍的字样 。
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "down" : __DIR__"dating",
  "up" : __DIR__"lou3",
]));

	setup();
	replace_program(ROOM);
}
