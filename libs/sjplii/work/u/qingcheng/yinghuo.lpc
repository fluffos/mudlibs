// Room: /u/qingcheng/yinghuo.c

inherit ROOM;

void create()
{
	set("short", "荧火亭");
	set("long", @LONG
这是后山的一个僻静的小亭子。建福宫作为练武场地之后，不习武
专修道的弟子便来到这里潜心研读道经，但因为此处密荫遮掩，月光难
以照到，便有人到了晚上捉来萤火虫充当照明之物，后人就称其亭为：
荧火亭。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"xiaolu28",
  "north" : __DIR__"qiaobi",
]));
	set("no_clean_up", 0);
	set("objects",([
		__DIR__"npc/questman" : 1,
       	]));

	setup();
	replace_program(ROOM);
}
