inherit ROOM;

void create()
{
	set("short", "黄土路");
	set("long", @LONG
一条黄土铺就的大路，两旁挺拔的白杨，微风吹来树叶哗哗做响。
偶尔有一匹快马从身边飞驰而过。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "southwest" : __DIR__"tulu2",
  "northeast" : "/d/xiangyang/xiaolu2",
]));
	set("objects", ([ ]));
	set("no_clean_up", 0);

	setup();
//	replace_program(ROOM);
}
void init()
{
        add_action("do_go","go");

}

int do_go()
{
if(random(3)){
this_player()->move(this_object());
return 1;
}
}
