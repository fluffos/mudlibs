// Room: /u/qingcheng/didao3.c

inherit ROOM;

void create()
{
	set("short", "地道");
	set("long", @LONG
这是一个地下通道，从天花板上透下一丝微光。
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "northeast" : __DIR__"didao2",
  "up" : __DIR__"dating",
]));

	setup();
	replace_program(ROOM);
}
