#include <room.h>
inherit ROOM;

void create() {
  set("short", "山洞");
  set("long", @LONG
钻进洞中抬头望去，小洞的空间很大，好象也因此明亮一些。仔细看时，里                            
面长满了大大小小，高高低低的石笋，有一些显然是从上面吊下来的。石笋发出
的幽幽的光使这里显得格外阴森恐怖。
LONG);
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "shandong1",
  ]));
  set("no_clean_up", 0);

  set("objects", ([
    __DIR__ "npc/zhizhu": 1,
  ]));

  //       set("outdoors", "quanzhen");

  setup();
}
