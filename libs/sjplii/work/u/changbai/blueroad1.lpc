#include <ansi.h>
inherit ROOM;
void create() {
  set("short", WHT "蓝石小径" NOR);
  set("long", @LONG
你怎么也没有想到山顶上竟然还有条路通向别处，仔细看周围
的景物，却又得不出什么结果，一切都那么平常。管他那么多，先
过去看看再说。
LONG);
  set("exits", ([
    "north": __DIR__ "blueroad2",
    "southwest": __DIR__ "shanding",
  ]));
  set("oldarea", 33);
  set("outdoors", "changbai");
  setup();
}
