#include <ansi.h>
inherit ROOM;
void create() {
  set("short", WHT "蓝石小径" NOR);
  set("long", @LONG
你脚下踩着的再不是雪，而是一堆堆的淡蓝色鹅软石铺成的小
路，看来铺路的一定是个很细心的人。
LONG);
  set("exits", ([
    "north": __DIR__ "blueroad3",
    "south": __DIR__ "blueroad1"
  ]));
  set("oldarea", 34);
  set("outdoors", "changbai");
  setup();
}
