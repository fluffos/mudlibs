#include <ansi.h>
inherit ROOM;
void create()
{
        set("short", WHT "长白山上" NOR);
        set("long", @LONG
这里已经是雪线以上了，遍地的白雪覆盖着整个你的视野，
白雪晶莹，天空蔚蓝，叫人怀疑自己到了一个天外世界。
LONG);
        set("exits", ([ 
//  "north" : __DIR__"shanxia",
   ]));
	set ("oldarea",17);
	set("outdoors","changbai");
	setup();
 }