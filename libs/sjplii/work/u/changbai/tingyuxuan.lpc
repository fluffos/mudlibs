#include <ansi.h>
inherit ROOM;
void create() {
  set("short", WHT "听雨轩" NOR);
  set("long", @LONG
一间充满诗情画意的亭子，却是干净的一尘不染，好象每天都
有人来清理一样。里面放着一面玉石桌，两三个石凳子。在柱子上
还刻有许多明代的图画。
LONG);
  set("exits", ([
    "eastdown": __DIR__ "blueroad4",
  ]));
  set("oldarea", 37);
  set("outdoors", "changbai");
  setup();
}
