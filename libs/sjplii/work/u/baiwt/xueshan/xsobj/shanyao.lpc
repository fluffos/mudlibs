#include <ansi.h>
inherit ITEM;
void create()
{
	set_name("野生草药", ({"caoyao"}) );
	set_weight(5);
	set("long",
		"这是一棵可以做药引的普通草药。\n");
	set("unit", "棵");
	set("identify_name", "山药");
	set("objattrib", 1);
        set("value", 0);
}

