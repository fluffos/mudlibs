inherit ROOM;
void create()
{
        set("short", "冰洞");
        set("long", @LONG
这是一个鲜为人知的小冰洞，洞内有一张小床，床边竟然有梳妆台，
看上去象女子的闺房。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "west" : __DIR__"houshan3",
]));
        set("outdoors", "xueshan");
        set("objects", ([
		__DIR__"npc/gulanxiang" : 1,
                        ]) );

        setup();
}
