inherit ROOM;
void create()
{
        set("short", "临时广场");
        set("long", @LONG
这里是NPC进行集会的临时广场。
LONG
        );
//        set("objects", ([
//		__DIR__"npc/master3" : 1,
 set("exits", 
([ //sizeof() == 4
"down" : "/d/city/ciensi",
]));
//       ]) );
        setup();
}

