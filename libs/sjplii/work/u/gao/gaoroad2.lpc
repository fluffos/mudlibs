// gate.c

inherit ROOM;

void create() {
  set("short", "土路");
  set("long", @LONG

周围是一片蹈田，一些农夫在田里赶着牲口耕种．东边望去有一座
山，雾气蒙蒙，看不清楚．南边是小镇，也是方圆几十里最为繁华
的地方．镇虽不大，但柴米油盐样样不缺．
LONG);
  set("exits", ([ /* sizeof() == 4 */
    //"north" : __DIR__"zhyuan",
    //"south"
    "east": __DIR__ "shanroad",
    "west": __DIR__ "gaoroad1",
  ]));
  set("objects",
    ([
      "/npc/nknpc": 1,
    ]));

  set("outdoors", 1);
  set("no_clean_up", 0);
  setup();
}
