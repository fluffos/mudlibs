inherit ROOM;

void create()
{
	set("short", "山腰");
	set("long", @LONG
山上云雾袅绕，远处群山叠峦，真是一幅壮丽的山水画。
你不禁想赞叹此地此景，却又想不出好的诗句。看来你还是
先去学学“诗词歌赋”吧。
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
  "southdown" : __DIR__"hillroad1",
  "northwest" : __DIR__"hillcross",
]));

	set("outdoors", "city");

	setup();
	replace_program(ROOM);
}