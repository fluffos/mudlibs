inherit ROOM;

void create() {
  set("short", "悬崖山道");
  set("long", @LONG
这是条很窄的山道，仅容一个人通过。往左边看是万丈深渊。
如果胆小，还是赶快回去吧。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "east": __DIR__ "shan1road2",
    "north": __DIR__ "shan3road2",
  ]));

  set("outdoors", "city");

  setup();
  replace_program(ROOM);
}
