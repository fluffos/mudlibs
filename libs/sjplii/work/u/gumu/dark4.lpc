// Room: /open/hurt/oldtomb/dark4.c

inherit ROOM;

void create() {
  set("short", "黑洞");
  set("long", @LONG
这里一片黑漆漆的，伸手不见五指，你什么都看不见。你只能
依稀的辨别出方向。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "northeast": __DIR__ "dark9",
    "west": __DIR__ "dark3",
    "east": __DIR__ "dark5",
    "northwest": __DIR__ "dark8",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
