inherit ROOM;

void create() {
  set("short", "小径");
  set("long", @LONG
一条小径，弯弯曲曲，不知通往哪里，走走看吧。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "westdown": __DIR__ "shan2road1",
    "eastup": __DIR__ "shan2road3",
  ]));

  set("outdoors", "city");

  setup();
}
