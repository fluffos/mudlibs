// Room: /open/hurt/oldtomb/dark6.c

inherit ROOM;

void create() {
  set("short", "黑洞");
  set("long", @LONG
这里一片黑漆漆的，伸手不见五指，你什么都看不见。你只能
依稀的辨别出方向。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "southeast": __DIR__ "dark2",
    "northeast": __DIR__ "dark10",
    "east": __DIR__ "dark7",
    "southwest": __DIR__ "dark1",
  ]));
  set("no_clean_up", 0);

  setup();
}
