// Room: /u/oldtomb/shanroad.c

inherit ROOM;

void create()
{
	set("short", "山路");
	set("long", @LONG
崎岖的山路越来越陡，但两旁却不再全是黄土，可以见到一点生气。
可以见到嫩绿的小树和长长的草。前面不远就是小孤山。
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/wvisitor" : 1,
]));
	set("outdoors", "city");
	set("exits", ([ /* sizeof() == 2 */
  "southeast" : __DIR__"hillcross",
  "northup" : __DIR__"shan1road1",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
