// Room: /u/oldtomb/hillroad.c

inherit ROOM;

void create() {
  set("short", "高梁地");
  set("long", @LONG
你走进了一片高粱地里，一条小径宛然曲折消失在连绵的山麓中，
东面是一条黄土大路。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "tulu",
    "northwest": __DIR__ "hillroad1.lpc",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "city");

  setup();
}
