// Room: /open/hurt/oldtomb/kitchen.c

inherit ROOM;

void create() {
  set("short", "食堂");
  set("long", @LONG
真没想到石洞里也有食堂，这里是整个山洞最亮的地方，而且
空气也很新鲜。中间放着一个长方形桌子，两边各是一排座位。桌
上放着些吃的。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "southeast": __DIR__ "corridor",
    "north": __DIR__ "lian1",
    "southwest": __DIR__ "flower",
  ]));
  set("objects", ([ /* sizeof() == 4 */
    __DIR__ "food/food3": 2,
    //  __DIR__"npc/teagirl" : 1,
    __DIR__ "food/cake": 2,
    __DIR__ "food/teacup": 2,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
