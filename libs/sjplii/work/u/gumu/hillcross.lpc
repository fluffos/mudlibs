// Room: /u/gumu/hillcross.c

inherit ROOM;

void create() {
  set("short", "山腰岔路");
  set("long", @LONG
这里是山腰，望着满山黄土，你不禁感到有点凄凉，山路并不好走。
LONG
  );
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/visitor": 1,
  ]));
  set("outdoors", "city");
  set("valid_startroom", 1);
  set("exits", ([ /* sizeof() == 2 */
    "southeast": __DIR__ "hillroad2",
    "northwest": __DIR__ "shanroad.lpc",
  ]));
  set("no_clean_up", 0);

  setup();
}
