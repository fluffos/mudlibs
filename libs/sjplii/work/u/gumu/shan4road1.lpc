inherit ROOM;

void create() {
  set("short", "树林");
  set("long", @LONG
这里是个树林，枝繁叶茂，一棵棵都是千年古树。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "west": __DIR__ "shan1road3",
    "eastdown": __DIR__ "mu_gate1",
  ]));

  set("outdoors", "city");

  setup();
  replace_program(ROOM);
}
