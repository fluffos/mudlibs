// Room: /open/hurt/oldtomb/lian3.c

inherit ROOM;

void create() {
  set("short", "练功房");
  set("long", @LONG
这里是古墓派弟子的练功房。由于古墓派历代相传的传统，
古墓弟子在没练成绝技以前不得出洞，所以个个心无杂念，练功
很勤。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "home",
    "east": __DIR__ "lian1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "obj/guancai": 1,
  ]));
  set("no_clean_up", 0);

  setup();
}
