inherit ROOM;

void create() {
  set("short", "悬崖");
  set("long", @LONG
这是个悬崖，如果脚下一个不留神，你就会摔得粉身碎骨，
死无全尸了。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */

    "south": __DIR__ "shan3road1",
  ]));

  set("outdoors", "city");

  setup();
  replace_program(ROOM);
}
