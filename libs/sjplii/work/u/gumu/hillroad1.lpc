// Room: /u/oldtomb/hillroad1.c

inherit ROOM;

void create() {
  set("short", "山脚");
  set("long", @LONG
你眼前就是那连绵不断的山麓。山非常崎岖，寸草不生，都是黄土。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "southeast": __DIR__ "hillroad",
    "northup": __DIR__ "hillroad2",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "city");

  setup();
  replace_program(ROOM);
}
