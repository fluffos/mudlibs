inherit ROOM;

void create() {
  set("short", "四轮马车");
  set("long", @LONG
这是一辆整洁宽敞的四轮马车，后面还拖着一节载货的车厢。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "grassland0.lpc",
  ]));
  set("zuo_trigger", 0);
  set("outdoors", 1);

  setup();
}
