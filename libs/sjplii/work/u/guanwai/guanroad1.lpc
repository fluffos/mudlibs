
inherit ROOM;

void create()
{
        set("short", "青石官道");
        set("long", @LONG
这是一条宽敞坚实的青石大道，向西延伸。
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
  "southwest" : __DIR__"guanroad2",
  "east" : __DIR__"tulu",
]));
        set("outdoors", "changan");
        set("objects", 
        ([
              "/npc/nknpc" : 1,
        ]));

        setup();
        replace_program(ROOM);
}
