#include <ansi.h>
inherit ITEM;
void create()
{
        set_name( "小鱼", ({ "fish" }));
	set_weight(5);
	set("long",
		"这是一条具有滋补功效的河鲜。\n");
	set("identify_name", "玻璃鱼");
	set("unit", "条");
        set("objattrib", 1);
        set("value", 0);
}

