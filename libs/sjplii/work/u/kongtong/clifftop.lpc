// Room: /d/waterfog/clifftop.c

inherit ROOM;

void create()
{
	set("short", "崆峒山半山亭");
	set("long", @LONG
这里是一个用白石砌成的亭子，亭子的东面和北面都是深不见底的
悬崖，从这里望出去只见一片云雾之中突出几座山峰，云雾之中隐隐可
以看到好似水波的微光，使你一时之间彷佛以为这座亭子是建在一个湖
中，往西北望去可以看到达天阁漂浮在云雾之中。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "westup" : __DIR__"stair5",
  "southdown" : __DIR__"stair4",
]));
	set("objects", ([
		__DIR__"npc/watcher": 1,
		__DIR__"npc/celes_tiger": 1 ]) );
	set("outdoors", "kongtong");

	setup();
	replace_program(ROOM);
}