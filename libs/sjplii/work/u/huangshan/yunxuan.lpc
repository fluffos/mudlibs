inherit ROOM;
void create ()
{
        set ("short", "梦云轩");
        set ("long", @LONG
这里是兵书宝剑侠的居所，是用清竹造就的一间草庐，虽然这里不华
丽，单却清新可人，淡雅脱俗，里面只有一张桌和一张竹椅，一张床，布
置的非常简单。
LONG);

       
        set("exits", 
        ([ //sizeof() == 4
		"north" : __DIR__"yaofang",
	"east" : __DIR__"fangju",
	"west" : __DIR__"xiuxi1",
       "southdown" : __DIR__"ywc1b",
           ]));

        set("objects", 
        ([ //sizeof() == 1
//		__DIR__"npc/huang" : 2,
        ]));
	set("NONPC",1);
	set("no_clean_up", 0);
	        setup();
}


