inherit ROOM;
void create()
{
        set("short", "休宁火鸡店");
        set("long", @LONG
这是一家火鸡店，店铺里琳琅满目地放着各式各样新鲜的火鸡
，香喷喷地令人垂涎欲滴。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "south"  : __DIR__"town2", 
]));
	 set("objects", ([
		__DIR__"npc/ji_boss": 1,
]));
        set("outdoors", "huangshan");
        setup();
        replace_program(ROOM);
}
