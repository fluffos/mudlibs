inherit ROOM;

void create() {
  set("short", "上山台阶");
  set("long", @LONG
举目远眺，只见对面山上数道瀑布飞珠溅玉，奔泻而下，回望群
山，已如蚁蛭，令人心胸顿时开阔。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "northup": __DIR__ "guansong",
    "southeast": __DIR__ "beilin",
  ]));

  set("no_clean_up", 0);
  set("outdoors", "shaolin");

  setup();
  replace_program(ROOM);
}
