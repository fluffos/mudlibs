// shengzi.c 

inherit ITEM;

void create() {
  set_name("绳子", ({ "shengzi" }));
  set_weight(180);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "根");
    set("long", "这是一根很长的绳子，里面掺了牛皮，很结实。\n");
    set("value", 0);
  }
}
