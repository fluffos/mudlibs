inherit ROOM;

void create()
{
        set("short", "瀑布");
        set("long", @LONG
一道瀑布凌空落下，水花飞溅，震耳欲聋。如洪涛奔吼，银河倒
泻，飞瀑万缕，状如霏玉喷珠，帘幕低垂。旁边山壁陡峭，长满青苔。
LONG
       );
        set("exits", ([/* sizeof() == 1 */
            "southwest" : __DIR__"xiaolu6",
            "northwest" : __DIR__"laoshulin3",
]));
	set("objects",([
	__DIR__"npc/youren" : 1,
	]));

        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}