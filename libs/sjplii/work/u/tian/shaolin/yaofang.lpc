//yaofang  药品房

inherit ROOM;

void create() {
  set("short", "药品房");
  set("long", @LONG
这是一间药品房，一个很大的架子，里面放着很多中药材，
散发出淡淡的药香。几个小和尚在忙着煎药，一个年长的老和尚
在给几个受伤的武僧搭脉诊治。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "liangong3",
  ]));
  set("objects", ([
    __DIR__ "npc/jingshan": 1,
  ]));

  set("no_clean_up", 0);

  setup();
}
