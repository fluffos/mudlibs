inherit ROOM;

void create() {
  set("short", "小路");
  set("long", @LONG
你走在一条狭窄的小路上。前面树林变得稀少了，光线也越来越亮，
不知道前面是什么地方，还是过去看看吧。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "northeast": __DIR__ "talin1",
    "west": __DIR__ "xiaolu4",
  ]));

  set("no_clean_up", 0);
  set("outdoors", "shaolin");

  setup();
}
