// shaolin_b.c

inherit BULLETIN_BOARD;

void create() {
  set_name("少林寺留言板", ({ "board" }));
  set("location", "/u/tian/shaolin/dcdian");
  set("board_id", "shaolin_b");
  set("long", "这是供少林弟子留言记事的留言板。\n");
  setup();
  set("capacity", 60);
  replace_program(BULLETIN_BOARD);
}
