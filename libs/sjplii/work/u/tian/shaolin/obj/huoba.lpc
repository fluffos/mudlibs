// huoba.c 火把

inherit ITEM;

void create() {
  set_name("火把", ({ "huo ba" }));
  set_weight(200);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "只");
    set("long", "这是一只松木火把，头上沾满松油，燃烧起来很持久。\n");
    set("value", 0);
  }
}
