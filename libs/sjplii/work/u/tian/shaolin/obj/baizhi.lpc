// baizhi.c 

inherit ITEM;

void create() {
  set_name("白纸", ({ "bai zhi" }));
  set_weight(180);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "张");
    set("long", "这是一张上好的白纸。\n");
    set("value", 0);
  }
}
