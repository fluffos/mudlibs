// slyuan  舍利院
#include <room.h>
inherit ROOM;

void create()
{
	set("short", "舍利院");
	set("long", @LONG
这里是舍利院。以前是供奉少林至宝--达摩祖师的佛骨舍利的地方。
正中是一座佛像，台前供桌上烛光荧荧，清烟绕绕，看来经常有人上香
参拜。
LONG
	);

	set("exits", ([
            "east": __DIR__"changlang3",
	]));
	set("objects",([
	__DIR__"npc/mingguang" : 1,
	]));
	
	setup();
//	replace_program(ROOM);
}
