//xiaoyuan5  小院
inherit ROOM;

void create()
{
        set("short", "小院");
        set("long", @LONG
这里是寺里的后院。微风吹来，飘来一阵油菜花香，偶尔有几只蜜
蜂飞来飞去，嗡嗡做响。北面是达摩院，东面是菜园。
LONG
        );
        set("exits", ([/* sizeof() == 3 */
           "east"  : __DIR__"caiyuan",
           "west"  : __DIR__"xiaoyuan4",
           "north" : __DIR__"dmyuan",
]));
	set("objects",([
	__DIR__"npc/bee" : 2,
	]));


        set("no_clean_up", 0);
	set("outdoors","shaolin");

        setup();
//        replace_program(ROOM);
}