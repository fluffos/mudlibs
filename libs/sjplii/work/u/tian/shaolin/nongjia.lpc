inherit ROOM;

void create() {
  set("short", "农家小院");
  set("long", @LONG
眼前豁然出现一块空地，居然有一座农家小院，门前有几只小鸡
正在啄食，看到人来，扑扑楞都躲到院墙根。看来这里还有人住，你
不禁大为惊奇。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "enter": __DIR__ "nongshe",
    "southeast": __DIR__ "xiaolu7",

  ]));
  set("objects", ([
    __DIR__ "npc/xiaoji": 3,
  ]));
  set("outdoors", "shaolin");
  set("no_clean_up", 0);

  setup();
}
