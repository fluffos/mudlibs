// Room: /u/wudang/wxiwu.c

inherit ROOM;

void create() {
  set("short", "西习武场");
  set("long", @LONG
这是武当的习武场，场上摆设着沙包，木人和一个兵器架，几个武当弟子
在练功。旁边站着一个威严的青年男子，正在指点着练武的弟子。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "guangchang",
    "west": __DIR__ "shijie7",
  ]));
  set("outdoors", "wudang");
  set("objects", ([
    __DIR__ "npc/wddizi1": 3,
  ]));

  setup();
  replace_program(ROOM);
}
