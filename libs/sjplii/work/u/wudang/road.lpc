// Room: /u/wudang/road.c

inherit ROOM;

void create()
{
        set("short", "小径");
        set("long", @LONG
南面是三清殿，北面是后殿。这是连接三清殿和后殿的一条小径，
两旁种满了梧桐树。青石板铺的地面，已有了年代的痕迹。
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"hdian",
  "east" :__DIR__"dhyuan",
  "south" : __DIR__"sqdian",
]));
	set("objects", ([ /* sizeof() == 1 */
//		__DIR__"npc/gongsun": 1,
]));

        set("outdoors", "wudang");

        setup();
        replace_program(ROOM);
}


