// Room: /u/wudang/hroad1.c

#include <room.h>
inherit ROOM;

void create() {
  set("short", "小路");
  set("long", @LONG
这里是武当的后院。东边是有一座拱门，西边有一个小院子，几个弟子
正在那里练功。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "hyuan",
    "west": __DIR__ "hroad2",
  ]));
  set("outdoors", "wudang");
  create_door("east", "檀木拱门", "west", DOOR_CLOSED);

  setup();
}
