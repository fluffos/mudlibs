// Room: /u/wudang/hyuan.c
#include <room.h>

inherit ROOM;

void create() {
  set("short", "后院");
  set("long", @LONG
这里是武当的后院。西边是有一座拱门，北边同向掌门人的卧室，
东面是兵器库，南面是后殿。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "wroom",
    "west": __DIR__ "hroad1",
    "north": __DIR__ "zmroom",
    "south": __DIR__ "hdian",
  ]));
  set("outdoors", "wudang");
  create_door("west", "檀木拱门", "east", DOOR_CLOSED);

  setup();
  replace_program(ROOM);
}
