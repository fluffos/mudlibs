// Room: /u/yanzi/huashan/gudi2.c

inherit ROOM;

void create() {
  set("short", "[1;36m谷底[2;37;0m");
  set("long", @LONG
你一走过来顿时大吃一惊，这里有着一个个小土堆，想是埋着人
的尸骨，你明白了为什么刚才看不见尸骨了，尸骨不会自己将自己埋
起来，定是有人埋的，难道这里居然还会有人居住？
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "gudi1",
    "east": __DIR__ "gudi3",
  ]));
  set("no_clean_up", 0);

  set("outdoors", "huashan");
  setup();
  replace_program(ROOM);
}
