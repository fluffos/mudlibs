// Room: /u/yanzi/huashan/house1.c

inherit ROOM;

void create() {
  set("short", "茅屋");
  set("long", @LONG
一间用茅草，干柴制成的小屋，墙边是一张粗木大床，一张简陋
的小桌放在窗前，桌上还放着个花瓶，里面稀疏地插着几朵小花。一
个身穿青杉，一白发的老者站在屋中。
LONG
  );
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/lizhip": 1,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "gudi3",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
