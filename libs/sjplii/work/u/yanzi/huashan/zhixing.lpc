// Room: /u/yanzi/huashan/zhixing.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"摘星石"NOR);
	set("long", @LONG
在这里有一块大石，石上刻有三字「摘星石」，是因为据称在这
块石头上便“手可摘星辰”，以此来形容西峰之高。
LONG
	);
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "eastup" : __DIR__"nanfeng",
  "westup" : __DIR__"xifeng",
]));

	setup();
	replace_program(ROOM);
}
