// Room: /u/yanzi/huashan/baichi.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"百尺峡"NOR);
	set("long", @LONG
这里是华山的又一处险路，一阶阶的台阶刚好仅容一人通过，身
边山风“呼”“呼”地吹来，使你担心自己随时就会掉入深崖之中。
LONG
	);
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "westdown" : __DIR__"qianchi",
  "north" : __DIR__"xianrenqiao",
]));

	setup();
	replace_program(ROOM);
}
