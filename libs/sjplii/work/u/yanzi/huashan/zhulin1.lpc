// Room: /u/yanzi/huashan/zhulin1.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"竹林"NOR);
	set("long", @LONG
这是一片密密的竹林。平时这里人迹罕至，走在林中，足底叩击
路面声，微风吹拂竹叶声，在这宁静的山林中显得格外的清晰。令人
尘心为之一涤。南方蝴蝶蜜蜂飞舞，似乎有个大花园。
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 3 */
  "northeast" : __DIR__"zlxj1",
  "east" : __DIR__"zlxj2",
  "southeast" : __DIR__"huayuan",
]));

	setup();
	replace_program(ROOM);
}
