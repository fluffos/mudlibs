//paper3.c
inherit ITEM;

void create() {
  set_name("无名纸片", ({ "paper", "zhipian" }));
  set_weight(500);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "张");
    set("long", "一张写满了字的纸片。\n");
    set("value", 0);
    set("material", "paper");
  }
}
