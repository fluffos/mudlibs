// tiefu1.c

inherit ITEM;

void create()
{
	set_name("斧头", ({ "futou" }) );
	set_weight(4000);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "柄");
		set("value", 500);
		set("material", "iron");
		set("long","一黝黑的铁斧，一般用来砍柴。\n");
	}
	setup();
}
