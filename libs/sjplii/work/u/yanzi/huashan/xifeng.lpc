// Room: /u/yanzi/huashan/xifeng.c

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIC "西峰" NOR);
  set("long", @LONG
这里是太华西峰，因为峰顶有一块巨石，状如莲花，覆盖崖巅，
因此又名莲花峰，也被叫做芙蓉峰。这里悬崖陡峭，壁立千仞，登临
远眺，秦川莽莽，渭、洛二水盘屈如带。
LONG
  );
  set("outdoors", "huashan");
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "pishan",
    "eastdown": __DIR__ "zhixing",
  ]));

  setup();
  replace_program(ROOM);
}
