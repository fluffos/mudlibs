// Room: /u/yanzi/huashan/huixin.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"回心石"NOR);
	set("long", @LONG
这里竖着一块巨大的石头，上刻三个工整有力的大字「回心石」
，因为从这里开始，上山的路都是由原地凿出的石阶，山道奇险，极
为险峻。因此这三字是告诉那些体力不济和胆子小的人，到这里就可
以回去了。
    从此北上即是「千尺幢」。
LONG
	);
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "northup" : __DIR__"qianchi",
  "west" : __DIR__"qingke",
]));

	setup();

}
#include "/u/yanzi/huashan/northup.h";