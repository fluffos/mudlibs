// Room: /u/yanzi/huashan/road4.c

inherit ROOM;

void create()
{
	set("short", "[1;36m山路[2;37;0m");
	set("long", @LONG
穿过苍龙岭，就来到了这条小山路，这儿地势较为平缓，不似其
它地方那么险峻。过了前面那条路就到鼎鼎大名的华山派了。
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/qiangdao" : 2,
]));
	set("no_clean_up", 0);
	set("outdoors", "huashan");
	set("exits", ([ /* sizeof() == 2 */
  "westdown" : __DIR__"road5",
  "northdown" : __DIR__"canglong",
]));

	setup();
}
int valid_leave(object me,string dir)
{
	object ob;
       	if(present("qiang dao",environment(me)) )
		return notify_fail("强盗一手拦住你，嘿嘿一笑：“想走？先留下买路钱。”\n");
	return ::valid_leave(me,dir);
}