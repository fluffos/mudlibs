// Room: /u/yanzi/huashan/moon.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"月石"NOR);
	set("long", @LONG
月石是一块玲珑剔透的大石头，每到晚上，如果明月当空，且天
气良好的话，你就能够在石头上看到月影，是华山一大景观，故
而得名。从这往东，就是观日出的朝阳峰了。
LONG
	);
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"xianzhang",
  "eastup" : __DIR__"dongfeng",
]));

	setup();

}
#include "/u/yanzi/huashan/eastup.h";