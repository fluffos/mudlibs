// Room: /u/yanzi/huashan/lian2.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"练功房"NOR);
	set("long", @LONG
这里是供华山弟子练功习武之用，几个华山弟子正在练功，非常
是热闹，房中站着几个陪练弟子，还有个专门指导武术的老师。
LONG
	);
	set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/lifeng" : 1,
  __DIR__"npc/peilian" : 4,
]));
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"lian1",
]));

	setup();
}
