// Room: /u/yanzi/huashan/laojun.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"老君犁沟"NOR);
	set("long", @LONG
这里是老君犁沟，它得名的由来传说是太上老君牵来自已的青牛
，在这个陡壁处犁出的一条小路。共有有五百多个石级，连绵不断，
直通华山的北峰。
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 3 */
  "eastup" : __DIR__"husun",
  "northdown" : __DIR__"qunxian",
  "south" : __DIR__"xianrenqiao",
]));
	set("outdoors", "huashan");

	setup();

}
#include "/u/yanzi/huashan/eastup.h";