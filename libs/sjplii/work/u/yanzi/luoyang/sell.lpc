// Room: /u/yanzi/luoyang/sell.c

inherit ROOM;

void create()
{
        set("short", "外卖部");
        set("long", @LONG
这儿是燕归来酒家的外卖部，后面连着厨房，这主要出售一些包子，
鸡腿之类的食物，供客人带走，各色菜肴则是一律不外卖的。一个三
十好几的伙计在这里招待客人。
LONG
        );
        set("no_study", 1);
        set("no_magic", 1);
        set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"yanziback",
]));
        set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/waiter" : 1,
]));
        set("no_fight", 1);
        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}

