// Room: /open/gdjk/ximen.c

inherit ROOM;

void create() {
  set("short", "弘农西门");
  set("long", @LONG
这里是军事重镇——弘农——的西城门，虽然鲜有居民来往，但是部队的马蹄
声却不绝于耳。突然，你觉得好象有人在注视着你，不由的打了个激灵，赶忙抬起
头来，只见几个军兵正虎视耽耽的盯着你。你只觉得腿有些发软。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    //  "west" : "/u/lark/hongnong/road4",
    "east": __DIR__ "jie1",
  ]));
  set("no_clean_up", 0);
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/bing": 4,
  ]));

  setup();
}
