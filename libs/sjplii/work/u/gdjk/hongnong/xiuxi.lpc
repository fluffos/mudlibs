// Room: /open/gdjk/xiuxi.c

inherit ROOM;

void create()
{
        set("short", "休息室");
        set("long", @LONG
这里是弘农主帅赤古的休息室。室内布置很简单，除了一张红木
雕花大床，就只有一些必备的家具了。
LONG
        );
        set("sleep_room", 1);
        set("no_fight", 1);
        set("no_clean_up", 0);
        set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"szhang",
]));
        set("no_magic", 1);

        setup();
        replace_program(ROOM);
}

