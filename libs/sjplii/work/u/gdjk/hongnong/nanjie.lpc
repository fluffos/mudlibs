// Room: /open/gdjk/nanjie.c

inherit ROOM;

void create()
{
        set("short", "弘农南街");
        set("long", @LONG
你正走在弘农南街上。街道的两边原来是店铺，如今却是人去楼
空。只有紧闭的大门上方半挂着的招牌在晃来晃去。
LONG
        );
        set("outdoors", "hongnong");
        set("exits", ([ /* sizeof() == 2 */
  "southeast" : __DIR__"nanjie1",
  "northwest" : __DIR__"jie4",
]));
        set("objects", 
        ([
              "/npc/nknpc" : 1,
        ]));

        setup();
        replace_program(ROOM);
}
