// Room: /open/gdjk/jie2.c

inherit ROOM;

void create()
{
	set("short", "弘农正街");
	set("long", @LONG
你走在弘农的大街上，北面是个杂货铺，不时有几个军人进进处处，
手里拿着什么东西。偶而也有一两个老农嘴里嘟嘟囔囔出来，似乎很不
满意。南面是个武馆，从门缝里可以看见有几个人在里面来来回回的走
动。
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/xingren" : 1,
]));
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"zahuopu",
  "west" : __DIR__"jie1",
  "south" : __DIR__"wuguan",
  "east" : __DIR__"jie-3",
]));

	setup();
	replace_program(ROOM);
}
