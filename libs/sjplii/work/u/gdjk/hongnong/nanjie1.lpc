// Room: /open/gdjk/nanjie1.c

inherit ROOM;

void create() {
  set("short", "弘农南街");
  set("long", @LONG
你正走在弘农南街上。街道上尘土飞扬，没有一个人。再
往南去，就到了弘农的南门。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "northwest": __DIR__ "nanjie",
    "south": __DIR__ "nanmen",
  ]));

  setup();
  replace_program(ROOM);
}
