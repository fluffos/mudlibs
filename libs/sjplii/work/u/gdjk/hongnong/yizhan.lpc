// Room: /open/gdjk/yizhan.c

inherit ROOM;

void create()
{
        set("short", "驿站");
        set("long", @LONG
这里因为常常发生抢劫，已经没人常来这里了，原来的老板也移居他
地了，只剩下空荡荡的房子。
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"jie1",
]));
        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}

