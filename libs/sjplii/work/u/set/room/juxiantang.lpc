inherit ROOM;
void create() {
  set("short", "聚贤堂");
  set("long", @LONG
这里是爱苗轩主人接待亲朋好友的地方，这里被女主人收拾的一尘不染，红木的桌子，红木
的椅子，流露出古香古色的气息，桌子上放着四时水果，精细茶点，透出主人的热情周到。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": "/u/set/room/hualang7",
  ]));
  set("no_clean_up", 0);
  set("no_fight", "1");
  //        set("valid_startroom", 1);
  set("no_spells", "1");
  set("no_magic", "1");
  setup();
  call_other("/u/set/obj/tiger_board", "???");
}
