// Room: /u/yanzi/huashan/xiuxi1.c

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", "休息室");
  set("long", @LONG
这是泰山弟子起居之所，泰山派要求众弟子生活简朴，因而这里
除了几张大床，再无其他的东西。
LONG
  );
  set("no_clean_up", 0);
  set("sleep_room", 1);
  set("exits", ([ /* sizeof() == 3 */
    "north": __DIR__ "yueguan",
    //  "southeast" : __DIR__"csf",
    //  "south" : __DIR__"xiuxi2",
  ]));

  setup();
}
