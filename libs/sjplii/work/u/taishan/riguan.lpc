inherit ROOM;

void create()
{
	set("short", "日观峰");
	set("long", @LONG
这里是位于玉皇顶东面的山岭，怪石攒簇，最高的地方一石
卓立，名为日观峰。往东走可以到达泰山的东天门。
LONG
	);

	set("exits", ([
		"westup" : __DIR__"yuhuang",
		"eastup" : __DIR__"tanhai",
	]));
	set("objects",([
		__DIR__"npc/tsdizi1" : 2,
	]));

	set("outdoors", "taishan");
	setup();
	replace_program(ROOM);
}

