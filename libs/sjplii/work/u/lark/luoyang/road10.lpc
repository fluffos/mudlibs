//changan city

inherit ROOM;

void create() {
  set("short", "官道");
  set("long", @LONG
一条修整的平整的青石板路，宽阔光滑大概可以并行三辆马车同时通过。
是连接东西的一条大干道.一直往西方向可以到达长安,一直向东则连接洛阳.
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "west": "/u/gao/shanroad",
      "east": __DIR__ "luoyang-w",  //goto luoyang

    ]));

  set("objects",
    ([  //sizeof() == 1
    "/u/lark/fnpc/kid": 2,
    ]));


  set("outdoors", "changan");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
