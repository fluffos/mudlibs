inherit ROOM;
void create()
{
        set("short", "洛阳南街");
        set("long", @LONG
漫步在洛阳宽阔的街道上,一片繁华熙攘的景象,各种各样的人
从你面前走过,各种各样的事物在你视线中出没,的确是大都会
的场面呀.往北就是洛阳的中心了,西面一座二层的小楼不时从
里面飘出诱人的香气,东面是一座客栈.
是
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"center",
  "south" : __DIR__"lys1",
  "west"  : __DIR__"yanzi",
  "east"  : "/open/agp/luoyang/awu",
]));
        set("objects", ([
        "/u/lark/fnpc/chefu": 2,

                        ]) );


        set("outdoors", "luoyang");
        setup();
}
