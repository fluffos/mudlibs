
#include <room.h>
inherit ROOM;

void create()
{
        set("short", "武器库");
        set("long", @LONG
十八般兵器，这里可谓应有尽有。四面一望，令你顿感眼花缭乱。
墙上砌了一个个小格子，每个都插满了各种各样的兵器。
LONG );
        set("exits", ([
                "east" : __DIR__"jwhall",
                //"north" : __DIR__"fangjuku",
        ]));

//        create_door("west","木门","east",DOOR_CLOSED);
        set("objects",([
                
                __DIR__"npc/wuqi" : 1,
               // __DIR__"obj/zhujian" : 1,
               // __DIR__"obj/mudao" : 1,
        ]));
//      set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}

