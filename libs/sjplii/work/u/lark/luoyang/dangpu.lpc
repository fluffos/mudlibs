// /d/city/tiejiangpu.c  created by Baiwt
#include <room.h>
inherit HOCKSHOP;

void create ()
{
  set ("short", "万金当铺");
  set ("long", @LONG
行走江湖难免有手头紧的时候，这个状况就要想起这里。
虽然不是怎么光彩，但能解了燃眉之急到也不用顾虑太多。
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"lyn2",
]));
  set("item_desc", ([ /* sizeof() == 1 */
  "paizi" : "
客官可以在这里：
    ＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊
    ＊  典当(pawn)               ＊
    ＊  卖断(sell)               ＊
    ＊  估价(value)              ＊
    ＊  购买(buy) 用list查看。   ＊
    ＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊
各种货物，铜钱、银两、黄金、银票一概通用。

"]));
  set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/dangpu_boss" : 1,
]));
  set("no_clean_up", 1);
        set("NONPC",1);

  setup();
}

