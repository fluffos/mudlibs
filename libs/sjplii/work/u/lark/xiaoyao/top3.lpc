//top3.c

inherit ROOM;

void create()
{
   set("short","飘渺峰顶");
   set("long",@LONG
来到树下原来这里有一个青衣老者，正作在一个石几前，呆呆的望着石几上的什么东西，
专注的甚至并未注意你的到来
LONG
);

   set("exits",([
    "west":__DIR__"top2",
]));
   set("objects",([
     __DIR__"npc/su":1,
     __DIR__"obj/qiju":1,
]));
   setup();
   replace_program(ROOM);
}
