// xy2.c

inherit ROOM;

void create() {
  set("short", "小场");
  set("long", @LONG
这里是山坡上一片不大的空地，是逍遥派弟子日常练武的地方，
一些弟子正在考较着武艺。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */

    "west": __DIR__ "xy4",
    //  "enter": __DIR__"hall",
    "southdown": __DIR__ "xy2",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/dizi4": 4,
  ]));
  set("no_clean_up", 0);
  set("valid_startroom", 1);

  setup();
}
