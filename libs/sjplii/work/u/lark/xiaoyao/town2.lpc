//town2.c

inherit ROOM;

void create()
{
  set("short","听香小镇");
  set("long",@LONG
这是一片安详的小镇，由于处在南北交通的枢纽地带，所以
街面上来来往往的人相当多。
LONG
);
   set("exits",([
    "north":__DIR__"huadian",
    "south":__DIR__"dian",
    "west":__DIR__"town1",
    "east":__DIR__"town3",
]));
    set("objects",([
     "/u/lark/fnpc/xingjiaoshang":2,
]));
     setup();
    replace_program(ROOM);
}
