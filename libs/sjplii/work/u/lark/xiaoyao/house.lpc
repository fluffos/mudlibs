//house.c
inherit ROOM;

void create()
{
        set("short", "草舍");
        set("long", @LONG
屋子不大, 因为收拾得很干净, 也不显得小。从打开的窗子向外
望去, 是一片已经泛出成熟的金黄的农田。
LONG
);
        set("exits", ([ /* sizeof() == 1 */
      "west" : __DIR__"xy",
]));
        set("objects", ([
        __DIR__"npc/tan" : 1,
]));
        setup();

}


