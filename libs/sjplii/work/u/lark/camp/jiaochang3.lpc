//jiaochang3.c

inherit ROOM;

void create()
{
  set("short","校场");
  set("long",@LONG
这里是军营内的校场，是兵士们日常训练的地方，整齐的方队，
划一的步伐，嘹亮的口号，闪亮的盔甲，让你不禁热血沸腾。
正有一队兵士正在操练武艺。
LONG
);
  set("exits",([
   "north":__DIR__"jiaochang2",
   "east":__DIR__"jiaochang4",
   "south":__DIR__"jingwuchang",
]));
  set("objects",([
__DIR__"npc/bing3":4,
]));
  setup();
  replace_program(ROOM);
}

