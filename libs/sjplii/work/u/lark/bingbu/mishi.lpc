//ROOM:/u/changan/bingbu/mishi.c

inherit ROOM;

void create()
{
set("short", "密室");
set("long",@LONG
这里是假山中暗藏的密室,周围都是结实的石壁,里面空空荡荡的，
黑暗中似乎有个人影靠墙角坐着。另一边的墙上有个铁环。
LONG
);
        set("objects", 
        ([ //sizeof() == 1
              "/d/city/npc/jinwei_master" : 1,
        ]));
setup();
}

void init()
{
 add_action("do_la",({"la"}));
}

int do_la(string arg)
{
  if(!arg) return notify_fail("拉什么?\n"); 
  if( arg!="铁环" ) return notify_fail("这个拉不动\n");
  message_vision("圆石唧唧哑哑的移开了一个空隙.\n",this_player());
  set("exits/up",__DIR__"jiashan");
  tell_room(__DIR__"jiashan","假山突然凭空出现了一个缝隙.\n");
  call_out("close_passage",3);
  return 1;
}

void close_passage()
{
  remove_call_out("close_passage");
  delete("exits/up");
  tell_room(__FILE__,"圆石又慢慢的移了回去,严丝合缝什么也看不出来\n");
  return;
}
