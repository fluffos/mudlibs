//neitang.c
#include <room.h>
inherit ROOM;

void create() {
  set("short", "内堂");
  set("long", @LONG
这里是大堂旁的一个僻静的小房间，陈设极其简单，两张红木椅
，一张茶几，房门密掩,窗户封闭绝对隔绝了与外部的任何联系，
外面的声音一点也传不进来。
LONG
  );
  set("exits", ([
    "north": __DIR__ "hall.lpc",
  ]));
  setup();
}
