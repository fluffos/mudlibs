//biaoju dayuan.c
inherit ROOM;

void create() {
  set("short", "大院");
  set("long", @LONG
这里是红旗镖局的大院，地方不小，现在看上去却冷冷清清的，
在大院的南边就是镖局的议事大厅了。

LONG);


  set("exits",
    ([  //sizeof() == 4
    "east": __DIR__ "bjdamen",

    ]));
  set("no_clean_up", 0);
  setup();
}
