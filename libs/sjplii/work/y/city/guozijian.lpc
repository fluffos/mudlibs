//changan city

inherit ROOM;

void create() {
  set("short", "国子监大院");
  set("long", @LONG
国子监是国家培养人才的地方，为了使人才不被埋没，朝廷开设国子
监来培养那些有潜力的学生来为朝廷效命。在这里可以看到三三两两的学
生模样的人在讨论问题，还有些老先生皱着眉头，不知道在考虑什么问题。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "east": __DIR__ "guobinguan",
      "west": __DIR__ "guozi",
    ]));

  set("objects",
    ([  //sizeof() == 1
    //                __DIR__"npc/xiucai1" : 1,
    ]));
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
