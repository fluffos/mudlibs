//changan city

inherit ROOM;

void create() {
  set("short", "书房");
  set("long", @LONG
但见房中一排排都是书架，架上都摆满了书，也不知有几千几万本书。
靠窗有一张极大的紫檀木的书桌．只见桌上摊着一本书，左首放着的砚台
笔筒也都雕刻精致。书桌右首是一只青铜古鼎，烧着檀香，鼎盖的兽头口
中袅袅吐出一缕缕青烟。
LONG);

  set("exits",
    ([  //sizeof() == 1
    "north": __DIR__ "yd-yz",
    ]));

  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/yaopu_boss": 1,
    ]));

  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
