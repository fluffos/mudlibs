// filename : /cmds/wiz/info.c

//inherit F_CLEAN_UP;

int help(object me);

int main(object me, string arg) {
  //	object obj;
  //	string str;

  if (!arg) return 0;

  printf("%O\n", EMOTE_D->query_emote(arg));
  //seteuid( geteuid( me ) );

  return 1;
}

int help(object me) {
  write(@HELP
HELP
  );
  return 1;
}
