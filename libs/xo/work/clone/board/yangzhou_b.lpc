// yangzhou_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("扬州小秦淮客寓留言本", ({ "board" }) );
	set("location", "/d/city/yangzhou/keyu");
	set("board_id", "yangzhou_b");
	set("long",	"这是一个留言记事的留言本。\n" );
	setup();
	set("capacity", 150);
	replace_program(BULLETIN_BOARD);
}
