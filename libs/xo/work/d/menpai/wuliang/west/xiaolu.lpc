  // Room: ../wu_liang/xiaolu.c

inherit ROOM;

void create()
{
        set("short", "山脚小路");
	set("long", @LONG
这是一条非常陡，也很窄的小路，两旁山壁峭立，甚至于你只能侧身而过。
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
            "northwest" : __DIR__"xiepo",
            "southeast" :__DIR__"qingshi_dao3",
]));

	set("outdoors","wuliang");
	setup();
	replace_program(ROOM);
}
