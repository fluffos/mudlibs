inherit ROOM;
void create()
{
    set("short", "门外石道");
    set("long", @LONG
北面山上就是剑湖宫了，一溜台阶一直通向大门口。抬眼看去，层层叠叠好
大一片房舍，都建在一带朱红围墙里面。东面有一条小道，弯弯曲曲不知通向何
处。
LONG);
    set("exits", ([
        "east" : __DIR__"chadao1",
        "southwest" : __DIR__"jieti1",
        "northup" : __DIR__"damen",
    ]));
    set("outdoors","wuliang");
    setup();
    replace_program(ROOM);
}
