// Room: /d/city/yangzhou/zizheng_shuyuan.c
#include <ansi.h>
inherit ROOM;
void create()
{
    set("short", YEL"资政书院"NOR);
    set("long", @LONG
资政书院是扬州官办的书院，扬州人大多把小孩送此处读书识字。进入书院，
院内一大院子，中间湖石堆了一个假山，几个学童在假山爬来穿去，边上搭一秋
千，几个女学童在耍秋千玩，两边各有一小竹棚，下摆几张木桌，一个学童躺在
桌上，枕着书似乎在睡觉。北向石阶上有间堂屋。
LONG
    );
    set("exits", ([ /* sizeof() == 2 */
        "east" : __DIR__"bei_xiaojie",
        "north" : __DIR__"tangwu",
    ]));
    set("objects", ([ /* sizeof() == 3 */
        __DIR__"npc/li_xiaoqi" : 1,
        __DIR__"npc/kong_xiaotian" : 1,
        __DIR__"npc/gu_yueer" : 1,
    ]));
    set("no_clean_up", 0);
    set("outdoors", "yangzhou");
    set("no_kill", "yangzhou");
    set("refuse_npc", 1);
    setup();
    
}
