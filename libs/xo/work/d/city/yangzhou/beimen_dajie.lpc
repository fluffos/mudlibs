// Room: /d/city/yangzhou/beimen_dajie.c

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", YEL "北门大街" NOR);
  set("long", @LONG
北门街宽约二十丈，中间以槐树隔开，行人分行左右。街边有排水沟直接通
向官河。街西是开明桥，街东通向虹桥街。北边是扬州的北门「镇淮门」。街西
边是名扬天下的崇雅书院，隐隐可以听到朗朗的读书声。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "hongqiao_jie",
    "south": __DIR__ "shili_changjie3",
    "west": __DIR__ "kaiming_qiao",
    "north": __DIR__ "zhenhuai_men",
  ]));

  set("no_clean_up", 0);
  set("outdoors", "yangzhou");
  set("no_kill", "yangzhou");

  setup();

}
