// 文件位置：d\city\shuyuan2.c
// 2008-02-12
inherit ROOM;
void create() {
  //房间名称叙述
  set("short", "朱熹的寝室");
  set("long", @LONG
朱熹一向过着简朴的生活，这里除了一张床以外，唯一值钱的
只有一个书架了。
LONG);
  //房间属性	
  set("no_clean_up", 0);
  set("no_fight", "1");
  set("sleep_room", "1");
  //房间出口	
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "shuyuan",
  ]));
  setup();
  replace_program(ROOM);
}
