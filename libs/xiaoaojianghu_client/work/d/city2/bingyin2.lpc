// 文件位置：d\city2\bingyin2.c
//2008-02-19

inherit ROOM;
void create()
{
//房间名称叙述
	set("short", "兵营");
	set("long", @LONG
这里是兵营，密密麻麻到处都是官兵，有的在武将的指挥下列队操练，
有的独自在练功，有的坐着、躺着正在休息。还有些太监在四处走动。
LONG);
//房间出口
	set("exits", ([
		"north" : __DIR__"xichangdamen",
	]));
//房间物品人物
	set("objects", ([
		"/d/huanggon/npc/taijian" : 1,
		"/d/city/npc/wujiang" : 1,
		"/d/city/npc/bing" : 2,
	]));
	setup();
	replace_program(ROOM);
}
