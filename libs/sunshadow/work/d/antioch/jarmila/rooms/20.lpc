
#include <std.h>
#include "../jarmila.h"
inherit STORAGE"hill.lpc";

void create(){
   ::create();
   set_exits(([
      "southwest" : ROOMS"19",
      "east" : ROOMS"21",

   ]));
}

void reset(){
   ::reset();
   if(!present("chunk of sunstone")){
      new(OBJ"sunstone.lpc")->move(TO);
   }
}



