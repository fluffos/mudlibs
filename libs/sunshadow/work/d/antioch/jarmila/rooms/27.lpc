
#include <std.h>
#include "../jarmila.h"
inherit STORAGE"hill.lpc";

void create(){
   ::create();
   set_exits(([
      "northwest" : ROOMS"26",
      "east" : ROOMS"28",
   ]));
}

void reset(){
   ::reset();
   if(!present("chunk of sunstone")){
      new(OBJ"sunstone.lpc")->move(TO);
   }
}



