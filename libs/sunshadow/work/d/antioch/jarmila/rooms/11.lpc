#include <std.h>
#include "../jarmila.h"
inherit STORAGE"military.lpc";

void create(){
   ::create();
   set_exits(([
      "southeast" : ROOMS"10",

   ]));
}

void reset(){
   ::reset();
   if(!present("heavy iron brazier")){
      new(OBJ"brazier.lpc")->move(TO);
   }
}
