
#include <std.h>
#include "../jarmila.h"
inherit STORAGE"hill.lpc";

void create(){
   ::create();
   set_exits(([
      "west" : ROOMS"20",

   ]));
}

void reset(){
   ::reset();
   if(!present("dragon")){
      new(MOBS"silverd.lpc")->move(TO);
   }
}

