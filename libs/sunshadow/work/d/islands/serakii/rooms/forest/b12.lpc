#include <std.h>
#include "../../serakii.h"

inherit STORAGE"corrupted_forest.lpc";

void create(){
   ::create();

set_exits(([
"east" : FOR"b13",

]));


}
void reset(){
   ::reset();
   if(!present("construct")){
      new(OBJ"hconstruct.lpc")->move(TO);
   }
}
