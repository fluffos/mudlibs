
#include <std.h>
#include "../../serakii.h"

inherit STORAGE"mist_forest.lpc";



void create(){
   ::create();


set_exits(([

    "east" : FOR"25",

]));

}

void reset(){
   ::reset();
   if(!present("corpses")){
      if(!random(2)) {
         new(OBJ"pile_of_corpses.lpc")->move(TO);
      }
   }
}

