#include <std.h>
#include "../../serakii.h"

inherit STORAGE"corrupted_forest.lpc";

void create(){
   ::create();

set_exits(([
"south" : FOR"b15",

]));

}

void reset(){
   ::reset();
   if(!present("archway")){
      new(OBJ"archway.lpc")->move(TO);
   }
}

