
#include <std.h>
#include "../../serakii.h"

inherit STORAGE"fallow_field.lpc";



void create(){
   ::create();


set_exits(([

"north" : FALLOW"41",





]));

}

void reset(){
   ::reset();
   if(!present("scarecrow")){
      new(MOBS"scarecrow.lpc")->move(TO);
   }
}
