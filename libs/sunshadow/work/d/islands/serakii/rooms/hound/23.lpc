#include <std.h>
#include "../../serakii.h"

inherit STORAGE"hound_camp.lpc";

void create(){
   ::create();

set_exits(([
"north" : HOUND"22",
"west" : HOUND"25",


]));

}

void reset(){
   ::reset();
   if(!present("arbalest")){
      new(OBJ"arbalest.lpc")->move(TO);
   }
}
