// girth.c

#include <armor.h>

inherit F_WAIST_EQ;

void create() {
  set_name("腰巾", ({ "girth" }));
  set_weight(400);
  setup_waist_eq();

  if (!clonep()) {
    set("unit", "条");
    set("long", "一条白布腰巾(如果洗乾净的话)　\n");
    set("value", 100);
    set("wear_as", "waist_eq");
  }
  setup();
}
