// Room: /d/snow/square_w.c

#include <room.h>
inherit ROOM;

void create() {
  set("short", "广场");
  set("long", @LONG
这里是雪亭镇广场的西边，往东可以看见广场中央的大榕树，一家
小客栈座落在西边不远处，客栈前的一串灯笼上写著「饮风客栈」四个
大字，广场上铺著整齐的石板　
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "south": __DIR__ "square_sw",
    "west": __DIR__ "inn_hall",
    "east": __DIR__ "square",
  ]));
  set("outdoors", "snow");

  setup();
  create_door("west", "店门", "east", 0);
}
