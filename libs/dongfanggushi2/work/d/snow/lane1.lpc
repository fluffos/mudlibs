// Room: /d/snow/lane1.c

inherit ROOM;

void create()
{
	set("short", "僻静小巷");
	set("long", @LONG
这里是条僻静小巷的转角处，小巷往北可以通往溪边，从这里就可
以听到溪流轰轰的声音，东边是一栋破旧的大宅院，门口的一的破匾写
著「长乐侯府」，不过里面已经是断垣残璧了，小巷南边有一座小土地
公庙　
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
		"east" : __DIR__"ruin1",
		"west" : __DIR__"epath",
		"north" : __DIR__"lane2",
]));
	set("outdoors", "snow");

	setup();
	replace_program(ROOM);
}
