// Room: /d/snow/nstreet1.c

inherit ROOM;

void create()
{
	set("short", "街道");
	set("long", @LONG
这里是雪亭镇北边的主要街道，往南通往镇上最热闹的广场，往北
则通往镇北的山路，你的两边都是一些卖杂货的店铺，这一代除了市集
的日子，　不常有太多人走动　
LONG
	);
	set("detail", ([ /* sizeof() == 1 */
		"店铺" : "各式各样的店铺都有，不过看起来不像是你会用得著的地方　
",
]));
	set("exits", ([ /* sizeof() == 2 */
		"south" : __DIR__"square_n",
		"north" : __DIR__"nstreet2",
]));
	set("outdoors", "snow");

	setup();
	replace_program(ROOM);
}
