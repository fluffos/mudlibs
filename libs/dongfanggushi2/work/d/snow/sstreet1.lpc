// Room: /d/snow/sstreet1.c

inherit ROOM;

void create()
{
	set("short", "街道");
	set("long", @LONG
这里是雪亭镇南边连接镇外的街道，西边有一间货栈，东边则是一
家杂货铺，街道往北通往广场南边，往南穿过一个草棚出镇外　
LONG
	);
	set("outdoors", "snow");
	set("exits", ([ /* sizeof() == 3 */
		"south" : __DIR__"sgate",
		"north" : __DIR__"square_s",
		"west" : __DIR__"store",
]));

	setup();
	replace_program(ROOM);
}
