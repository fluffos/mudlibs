// dumpling.c

inherit ITEM;
inherit F_FOOD;

void create()
{
	set_name("牛肉包子", ({ "beef dumpling", "dumpling" }) );
	set_weight(150);
	if( !clonep() ) {
		set("unit", "个");
		set("value", 10);
		set("food_stuff", 80);
		set("long", "一个香喷喷的牛肉包子　\n");
	}
	setup();
}

