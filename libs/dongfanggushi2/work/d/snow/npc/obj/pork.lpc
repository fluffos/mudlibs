// pork.c

inherit ITEM;
inherit F_FOOD;

void create()
{
	set_name("白切肉", ({ "pork" }) );
	set_weight(400);
	if( !clonep() ) {
		set("unit", "片");
		set("value", 200);
		set("food_stuff", 90);
		set("heal_kee", 10);
		set("long", "普通的水煮白切肉 ... 　\n");
	}
	setup();
}

