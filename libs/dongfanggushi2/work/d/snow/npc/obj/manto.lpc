// manto.c

inherit ITEM;
inherit F_FOOD;

void create() {
  set_name("馒头", ({ "manto" }));
  set_weight(100);
  if (!clonep()) {
    set("unit", "个");
    set("value", 10);
    set("food_stuff", 120);
    set("long", "馒头 ... 跟你想像中的一模一样，感动吧　\n");
  }
  setup();
}
