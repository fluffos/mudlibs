// Room: /d/hell/square_center.c

inherit ROOM;

void create()
{
	set("short", "地气柱顶");
	set("long", @LONG
你现在正站在一座高台前面，台旁一个篆字铭牌刻著「地气擎纲，
永镇幽冥」八个大字，许多鬼卒守在高台的台阶两旁，高台四周是个广
场，不过看起来倒像是城中所有的房舍都远远避开这座高台而建，你发
现这座高台看起来十分清晰，不像酆都城其他的建筑物都模模糊糊的　
LONG
	);
	set("exits", ([ /* sizeof() == 5 */
		"west" : __DIR__"square7",
		"north" : __DIR__"square5",
		"east" : __DIR__"square3",
		"up" : __DIR__"tower1",
		"south" : __DIR__"square1",
	]));
	set("objects", ([
		__DIR__"npc/hell_guard": 4,
		__DIR__"npc/capt_zhor" : 1
	]));

	setup();
	replace_program(ROOM);
}
