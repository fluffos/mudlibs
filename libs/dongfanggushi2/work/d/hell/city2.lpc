// Room: /d/hell/city2.c

inherit ROOM;

void create()
{
	set("short", "酆都城");
	set("long", @LONG
这里是酆都城的街道，你的东边是一栋巨大的宅院，从街道外就可
以看见宅院中的大屋与一株梧桐树，什麽样的人会在酆都城拥有这样一
栋豪华的大宅呢？街道西边是一些较小的房舍，似乎是地府鬼卒的居所
　
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"north" : __DIR__"city3",
		"south" : __DIR__"city1",
]));

	setup();
	replace_program(ROOM);
}
