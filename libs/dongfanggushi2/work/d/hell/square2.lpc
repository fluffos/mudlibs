// Room: /d/hell/square2.c

inherit ROOM;

void create()
{
	set("short", "广场");
	set("long", @LONG
这里是酆都城中心的广场，你的西北方有一座高台，广场四周的建
筑似乎都刻意远远避开那座高台，你的东边和南边都是一些模糊的房舍
黑影，广场往北边与西边延伸　
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"north" : __DIR__"square3",
		"west" : __DIR__"square1",
]));

	setup();
	replace_program(ROOM);
}
