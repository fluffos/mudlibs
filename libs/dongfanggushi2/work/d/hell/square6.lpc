// Room: /d/hell/square6.c

inherit ROOM;

void create() {
  set("short", "广场");
  set("long", @LONG
这里是酆督城中心的广场，你的东南边耸立著一座高台，这里有一
株高大的梧桐树，遮蔽了广场整个西北角，北边和西边都是黑漆漆的宅
院，广场往东边与南边延伸　
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "square7",
    "east": __DIR__ "square5",
  ]));

  setup();
  replace_program(ROOM);
}
