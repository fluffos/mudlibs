// Room: /d/hell/square1.c

inherit ROOM;

void create()
{
	set("short", "广场");
	set("long", @LONG
这里是酆都城中心的广场，就在你的正北方数十步远的地方耸立著
一座三层楼高的高台，从这里可以看见台顶似乎有什麽东西在发著微光
，往南是一条笔直的街道直通酆都城通往阳间的出口鬼门关　
LONG
	);
	set("exits", ([ /* sizeof() == 4 */
		"west" : __DIR__"square8",
		"north" : __DIR__"square_center",
		"south" : __DIR__"city3",
		"east" : __DIR__"square2",
]));

	setup();
	replace_program(ROOM);
}
