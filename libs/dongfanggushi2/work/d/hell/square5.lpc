// Room: /d/hell/square5.c

inherit ROOM;

void create()
{
	set("short", "广场");
	set("long", @LONG
这里是酆都城中心的广场，你的南边耸立著一座三层楼高的高台，
台顶不知道有些什麽东西发出幽幽的微光，你的正北方是一条笔直的大
道，但是路上浓雾一片看不清通往何处，广场往东西方向延伸　
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
		"north" : (: clone_object, __DIR__"mist1" :),
		"west" : __DIR__"square6",
		"south" : __DIR__"square_center",
		"east" : __DIR__"square4",
	]));

	setup();
	replace_program(ROOM);
}
