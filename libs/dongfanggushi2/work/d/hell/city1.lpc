// Room: /d/hell/city1.c

#include <room.h>

inherit ROOM;

void create() {
  set("short", "酆都城");
  set("long", @LONG
你现在来到了酆都城的城门口，街道上朦朦胧胧许多人影飘来飘去
，街道旁的房舍也都是若隐若现，一条笔直的街道往北通往都城中心，
从这里可以望见城中心建了一座高台　
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "city2",
    "south": __DIR__ "ghost_gate",
  ]));

  setup();
  create_door("south", "城门", "north", DOOR_CLOSED | DOOR_LOCKED);
}
