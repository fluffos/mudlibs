// Room: /d/hell/square7.c

inherit ROOM;

void create()
{
	set("short", "广场");
	set("long", @LONG
这里是酆都城中心的广场，往东你可以看到一座约三层楼高的高台
，台顶不知有些什麽东西正发出微微的光芒，你的西边是一间监狱模样
的房子，大门是坚硬的钢铁所铸，一块横匾上写著「浊河窟」三个大字
，广场往南北两边延伸　
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
		"south" : __DIR__"square8",
		"north" : __DIR__"square6",
		"east" : __DIR__"square_center",
]));

	setup();
	replace_program(ROOM);
}
