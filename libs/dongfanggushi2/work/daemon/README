□ 游戏系统整体性 DAEMON

这个目录下放的是跟游戏有关, 而且是整体性的 DAEMON　

什麽是 DAEMON 呢　也许学电脑的人都知道, 不过这里还是解释一下, DAEMON 是一种
在系统中默默工作, 提供各种服务(services)的程式, 在 es2 mudlib 中, DAEMON 也
一些提供(services)的物件, 平常我们不需要特意去将它 new 或 load 进来, 我们呼
叫 DAEMON 的方式通常是 xxx_D->function(...), 这些 xxx_D 大多数定义在 MudOS
的 global include file, (es2 中为 <globals.h>) 里, 以方便使用　

有许多跟系统相关的 DAEMON 被放在 /adm/daemons 下, 而这个目录存放的则是和游戏
本身定义直接相关的 DAEMON　

By Phoebus.suny(03/15/96)
