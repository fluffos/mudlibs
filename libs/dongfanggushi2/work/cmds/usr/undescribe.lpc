// describe.c

inherit F_CLEAN_UP;

void create() { seteuid(getuid()); }

int main(object me)
{

	seteuid(geteuid(this_player(1)));

	me->delete("long");
	write("Ok.\n");
	return 1;
}

int help()
{
	write(@TEXT
指令格式：undescribe

这个指令让你拿掉你自己的描述　
TEXT
	);
	return 1;
}
