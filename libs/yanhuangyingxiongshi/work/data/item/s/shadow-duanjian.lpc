// ITEM Made by player(月隐无:shadow) /data/item/s/shadow-duanjian.c
// Written by GAN JIANG(Doing Lu 1998/11/2)	Wed Jul 10 08:11:03 2002
#include <ansi.h>
#include <weapon.h>

inherit SWORD;
inherit F_ITEMMAKE;

void create() {
  set_name("[1;36m断剑[2;37;0m", ({ "duanjian" }));
  set_weight(5100);
  set("item_make", 1);
  set("unit", "柄");
  set("long", "这是由[1;36m离[1;31m火[1;36m玄[1;37m冰[1;36m玉[2;37;0m炼制而成的一柄剑。
剑柄上刻着一行小字：月隐无(shadow)
");
  set("value", 2000000);
  set("point", 121);
  set("material", "stone");
  set("wield_msg", "[1;36m$N[1;36m一声清啸，抖出一柄[1;36m$n[1;36m。[2;37;0m\n");
  set("unwield_msg", "$N随手一抹，收起了$n。\n");
  if (!check_clone()) return;
  restore();
  init_sword(apply_damage());

  setup();
}

string long() { return query("long") + item_long(); }
