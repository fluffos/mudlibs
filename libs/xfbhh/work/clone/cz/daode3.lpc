//rune33.c

// Zod薩德

#include <ansi.h>

inherit TESSERA;

int is_rune() { return 1; }

void create() {
  set_name(HIW "道德三" NOR, ({ "rune000" }));
  set_weight(10);
  set("cz", 4);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "符文：造化符，可以放入凹槽物品。\n");
    set("unit", "个");
    set("enchase/SN", 1);
    set("yuanbao", 888);
    set("enchase/type", "all");
    set("enchase/consistence", 100);
    set("enchase/weapon_prop", ([
      "damage": 1000000,
      "derive_effect": 4000,
      "research_times": 4000,
      "research_effect": 4000,
      "practice_effect": 4000,
      "study_times": 4000,
      "learn_times": 4000,
      "derive_times": 4000,
      "practice_times": 4000,
    ]));
    set("enchase/armor_prop", ([
      "derive_effect": 4000,
      "practice_effect": 4000,
      "research_times": 4000,
      "research_effect": 4000,
      "study_times": 4000,
      "learn_times": 4000,
      "derive_times": 4000,
      "practice_times": 4000,
    ]));
    set("value", 20000);
  }
  setup();
}

int query_autoload() { return 1; }
