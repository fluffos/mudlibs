//rune33.c

// Zod薩德

#include <ansi.h>

inherit TESSERA;

int is_rune() { return 9; }

void create() {
  set_name(HIW "无上道德" NOR, ({ "rune000" }));
  set_weight(90);
  set("cz", 9);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "符文：造化符，可以放入凹槽物品。\n");
    set("unit", "个");
    set("enchase/SN", 9);
    set("yuanbao", 888);
    set("enchase/type", "all");
    set("enchase/consistence", 900);
    set("enchase/weapon_prop", ([
      "damage": 9000000,
      "derive_effect": 9000,
      "research_times": 9000,
      "research_effect": 9000,
      "practice_effect": 9000,
      "study_times": 9000,
      "learn_times": 9000,
      "derive_times": 9000,
      "practice_times": 9000,
    ]));
    set("enchase/armor_prop", ([
      "derive_effect": 9000,
      "practice_effect": 9000,
      "research_times": 9000,
      "research_effect": 9000,
      "study_times": 9000,
      "learn_times": 9000,
      "derive_times": 9000,
      "practice_times": 9000,
    ]));
    set("value", 20000);
  }
  setup();
}

int query_autoload() { return 9; }
