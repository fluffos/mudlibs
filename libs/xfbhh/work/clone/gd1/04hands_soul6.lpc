#include <ansi.h>

inherit EQUIP_SOUL;

void create() {
  set_name(HIM "铁掌之魂" NOR, ({ "hands soul6" }));
  set_weight(1000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", HIM "完美的铁掌之魂，可用来改造铁掌。。\n" NOR);
    set("unit", "个");
    set("gongde", 100);
    set("value", 10000);
    set("no_sell", 1);
  }
  set("type", "hands");
  set("status", 6);
  set("auto_load", 1);

  setup();
}
