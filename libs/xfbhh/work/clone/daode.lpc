//rune33.c

// Zod薩德

#include <ansi.h>

inherit TESSERA;

int is_rune() { return 1; }

void create() {
  set_name(HIW "道德" NOR, ({ "rune000" }));
  set_weight(10);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "符文：造化符，可以放入凹槽物品。\n");
    set("unit", "个");
    set("enchase/SN", 1);
    set("yuanbao", 888);
    set("enchase/type", "all");
    set("enchase/consistence", 100);
    set("enchase/weapon_prop", ([
      "damage": 1000000,
      "derive_effect": 300,
      "research_times": 5000,
      "research_effect": 300,
      "practice_effect": 100,
      "study_times": 5000,
      "learn_times": 5000,
      "derive_times": 10000,
      "practice_times": 5000,
    ]));
    set("enchase/armor_prop", ([
      "derive_effect": 100,
      "practice_effect": 100,
      "research_times": 2500,
      "research_effect": 100,
      "study_times": 2500,
      "learn_times": 2500,
      "derive_times": 1000,
      "practice_times": 2500,
    ]));
    set("value", 20000);
  }
  setup();
}

int query_autoload() { return 1; }
