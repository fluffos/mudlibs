#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIW "混沌寒坑" NOR);
  set("long", @LONG
封印关键点之一！
LONG);

  set("maze", 1);
  set("no_magic", 1);
  set("virtual_room", 1);
  set("no_clean_up", 1);
  set("outdoors", "hunduntian");

  set("objects", ([
    "/maze/hunduntian/npc/king1": 1,
  ]));
  setup();
  // replace_program(ROOM);
}
