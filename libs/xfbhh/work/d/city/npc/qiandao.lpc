//签到打卡功能

#include <ansi.h>
#include <localtime.h>
#define QIANDAO "/data/qiandao.o"

inherit NPC;

int do_daka();
int do_daka1();
int do_daka2();
int do_daka3();
int do_daka4();
int do_daka5();
int do_daka6();
int do_daka7();
int do_query_time();

void create() {
  set_name(HIG "签到员" NOR, ({ "qiandao npc" }));
  set("gender", "男性");
  set("age", 20);
  set("per", 100);
  set("con", 100);
  set("int", 100);
  set("dex", 100);
  set("str", 100);
  set("sm", 10000);
  set("max_sm", 10000);
  set("js", 10000);
  set("fl", 10000);
  set("max_fl", 10000);
  set("max_jl", 10000);
  set("max_js", 10000);
  set("combat_exp", 100000000);
  set("no_suck", 1);
  set("title", HIG "游戏管理员" NOR);
  set("long", "他是泥潭7的管理员，专门负责玩家的签到记录工作。\n");
  set("no_kill", 1);
  set("inquiry", ([
    "签到": (: do_daka :),
    /*"新人签到1" : (: do_daka1 :),
		"新人签到2" : (: do_daka2 :),
		"新人签到3" : (: do_daka3 :),
		"新人签到4" : (: do_daka4 :),
		"新人签到5" : (: do_daka5 :),
		"新人签到6" : (: do_daka6 :),
		"新人签到7" : (: do_daka7 :),*/
    "查询签到": (: do_query_time :),
  ]));
  setup();
  carry_object("/clone/cloth/cloth")->wear();
}

int do_daka() {
  object me, ob, env;
  mixed *localtime, *localtime2;
  string writeFile, ip, ip2, *meFilePach, meFile, item_max, id, room, target;
  int i, j, ok, day, month, size, data;
  int level;

  ob = this_object();
  me = this_player();

  if (query_temp("qiandao_time", ob) > time()) {
    tell_object(me, CYN "签到员说道：慢慢来，让我喘口气。\n" NOR);
    return 1;
  }

  data = query("dacard/time", me);
  localtime = localtime(data);
  localtime2 = localtime(time());

  if (data && localtime[LT_MDAY] == localtime2[LT_MDAY]) {
    tell_object(me, CYN "签到员说道：你今天已经签过到了，明天再来吧。\n" NOR);
    return 1;
  }

  if (localtime[LT_MON] + 1 != localtime2[LT_MON] + 1)  //月份
  {
    delete("dacard", me);
  }

  set_temp("qiandao_time", time() + 2, ob);  //因该方式采取读写硬盘，故稍做限制
  writeFile = QIANDAO;

  if (writeFile) {
    meFile = read_file(writeFile);
    if (meFile)
      meFilePach = explode(meFile, "\n");

    size = sizeof(meFilePach);
    ip = query_ip_number(me);

    item_max = sprintf(
      "ID:%s$Ip:%s$Day:%d$Month:%d",
      query("id", me),
      ip,
      localtime2[LT_MON] + 1,
      localtime2[LT_MDAY]
    );

    for (i = 0; i < size; i++) {
      sscanf(meFilePach[i], "ID:%s$Ip:%s$Day:%d$Month:%d", id, ip2, day, month);

      if (ip == ip2 && localtime2[LT_MON] + 1 == day && localtime2[LT_MDAY] == month)  //相同文件内容表示本IP已经有人签到
      {
        ok = 1;
        break;
      } else if (ip == ip2) {  //只是IP相同，并没有签到
        ok = 2;
        meFilePach[i] = "";
        break;
      } else
        ok = 0;
    }
  }

  if (ok == 1) {
    tell_object(me, CYN "签到员：这个IP今天已经有人签过到了。\n" NOR);
    return 1;
  }
  if (ok == 2) {
    write_file(writeFile, "", 1);  //初始化该文件
    //重写文件
    for (i = 0; i < size; i++) {
      if (meFilePach[i] == "") continue;  //去掉IP相同的内容
      write_file(writeFile, sprintf("%s\n", meFilePach[i]));  //重写所有内容
    }
  }
  if (ok != 1) {
    write_file(writeFile, sprintf("%s\n", item_max));  //新记录加入文件
  }
  if (!objectp(env = environment(ob))
    || !stringp(room = query("short", env))) {
    ::die();
    return;
  }

  i = 50000;

  level = query("level", me);
  //在限定的日期内奖励增加
  if ((CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/01" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/02" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/03" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/04" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/05" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/06" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/07")
    j = 5 + random(6);
  //
  else  //过期后奖励下降
    j = 3 + random(3);

  j = level / 10 * 10;
  if (j < 5) j = 5;
  target = query("id", me);


  if (query("hhsjvip", me)) {
    addn("balance", i * 40, me);
    addn("combat_exp", 50000, me);
    addn("potential", 50000, me);
    addn("yuanbao", 1000, me);
  }
  if (query("dacard/cishu", me) >= 9) {
    addn("cz", 1, me);
    //addn("combat_exp", 50000, me);
    //	addn("potential", 50000, me);
    addn("yuanbao", 1000, me);
  }
  //addn("gq", 1, me);
  addn("combat_exp", i, me);
  addn("potential", i, me);
  addn("dacard/cishu", 1, me);
  addn("max_neili", 1000, me);
  addn("max_jingli", 1000, me);
  addn("gongde", 7000, me);
  addn("hdt", 1, me);
  addn("yuanbao", 1000, me);
  addn("vip/score", 70, me);
  //addn("hdt", 1, me);
  set("dacard/time", time(), me);
  message("party", (HIW + "【" + room + "】签到员：" + NOR + query("name", me)
    + "今日签到获得\n" NOR), users());
  tell_object(
    me,
    sprintf(CYN + "经验值 50000点，潜能 50000点,内力 1000点,精力 1000点,功德 7000点元宝1000，签到十天即可获得充值点。VIP贵宾可获得功德经验潜能翻倍哦,混沌天挑战副本一次\n" NOR)
  );
  tell_object(me, sprintf(CYN "今日签到成功！祝你游戏愉快！\n" NOR));

  return 1;
}

int do_daka1() {
  object me, ob, env;
  mixed *localtime, *localtime2;
  string writeFile, ip, ip2, *meFilePach, meFile, item_max, id, room, target;
  int i, j, ok, day, month, size, data;
  int level;

  ob = this_object();
  me = this_player();

  if (query_temp("qiandao_time", ob) > time()) {
    tell_object(me, CYN "签到员说道：慢慢来，让我喘口气。\n" NOR);
    return 1;
  }

  if (query("qiandao/2018911", me))
    return notify_fail("你已经领过礼物了\n");

  data = query("dacard/time", me);
  localtime = localtime(data);
  localtime2 = localtime(time());

  if (data && localtime[LT_MDAY] == localtime2[LT_MDAY]) {
    tell_object(me, CYN "签到员说道：你今天已经签过到了，明天再来吧。\n" NOR);
    return 1;
  }

  if (localtime[LT_MON] + 1 != localtime2[LT_MON] + 1)  //月份
  {
    delete("dacard", me);
  }

  set_temp("qiandao_time", time() + 2, ob);  //因该方式采取读写硬盘，故稍做限制
  writeFile = QIANDAO;

  if (writeFile) {
    meFile = read_file(writeFile);
    if (meFile)
      meFilePach = explode(meFile, "\n");

    size = sizeof(meFilePach);
    ip = query_ip_number(me);

    item_max = sprintf(
      "ID:%s$Ip:%s$Day:%d$Month:%d",
      query("id", me),
      ip,
      localtime2[LT_MON] + 1,
      localtime2[LT_MDAY]
    );

    for (i = 0; i < size; i++) {
      sscanf(meFilePach[i], "ID:%s$Ip:%s$Day:%d$Month:%d", id, ip2, day, month);

      if (ip == ip2 && localtime2[LT_MON] + 1 == day && localtime2[LT_MDAY] == month)  //相同文件内容表示本IP已经有人签到
      {
        ok = 1;
        break;
      } else if (ip == ip2) {  //只是IP相同，并没有签到
        ok = 2;
        meFilePach[i] = "";
        break;
      } else
        ok = 0;
    }
  }

  if (ok == 1) {
    tell_object(me, CYN "签到员：这个IP今天已经有人签过到了。\n" NOR);
    return 1;
  }
  if (ok == 2) {
    write_file(writeFile, "", 1);  //初始化该文件
    //重写文件
    for (i = 0; i < size; i++) {
      if (meFilePach[i] == "") continue;  //去掉IP相同的内容
      write_file(writeFile, sprintf("%s\n", meFilePach[i]));  //重写所有内容
    }
  }
  if (ok != 1) {
    write_file(writeFile, sprintf("%s\n", item_max));  //新记录加入文件
  }
  if (!objectp(env = environment(ob))
    || !stringp(room = query("short", env))) {
    ::die();
    return;
  }

  i = 50000;

  level = query("level", me);
  //在限定的日期内奖励增加
  if ((CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/04" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/05" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/10" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/07" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/08" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/09" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/10")
    j = 5 + random(6);
  else  //过期后奖励下降
    j = 3 + random(3);

  j = level / 10 * 10;
  if (j < 5) j = 5;
  target = query("id", me);


  if (query("hhsjvip", me)) {
    addn("balance", i * 40, me);
    addn("combat_exp", 50000, me);
    addn("potential", 50000, me);
    addn("gongde", 75, me);
  }
  addn("combat_exp", i, me);
  addn("potential", i, me);
  addn("dacard/cishu", 1, me);
  addn("max_neili", 1001000, me);
  addn("max_jingli", 1000, me);
  addn("gongde", 70, me);
  addn("hdt", 1, me);
  set("dacard/time", time(), me);
  set("qiandao/2018911", 1, me);
  message("party", (HIW + "【" + room + "】签到员：" + NOR + query("name", me)
    + "今日签到获得\n" NOR), users());
  tell_object(
    me,
    sprintf(CYN + "经验值 50000点，潜能 50000点,内力 1000点,精力 1000点,功德 75点。VIP贵宾可获得功德经验潜能翻倍哦,混沌天挑战副本一次\n" NOR)
  );
  tell_object(me, sprintf(CYN "今日签到成功！祝你游戏愉快！\n" NOR));

  return 1;
}

int do_daka2() {
  object me, ob, env;
  mixed *localtime, *localtime2;
  string writeFile, ip, ip2, *meFilePach, meFile, item_max, id, room, target;
  int i, j, ok, day, month, size, data;
  int level;

  ob = this_object();
  me = this_player();

  if (query_temp("qiandao_time", ob) > time()) {
    tell_object(me, CYN "签到员说道：慢慢来，让我喘口气。\n" NOR);
    return 1;
  }

  if (!query("qiandao/2018911", me))
    return notify_fail("按照顺序来。\n");

  if (query("qiandao/2018912", me))
    return notify_fail("你已经领过礼物了\n");

  data = query("dacard/time", me);
  localtime = localtime(data);
  localtime2 = localtime(time());

  if (data && localtime[LT_MDAY] == localtime2[LT_MDAY]) {
    tell_object(me, CYN "签到员说道：你今天已经签过到了，明天再来吧。\n" NOR);
    return 1;
  }

  if (localtime[LT_MON] + 1 != localtime2[LT_MON] + 1)  //月份
  {
    delete("dacard", me);
  }

  set_temp("qiandao_time", time() + 2, ob);  //因该方式采取读写硬盘，故稍做限制
  writeFile = QIANDAO;

  if (writeFile) {
    meFile = read_file(writeFile);
    if (meFile)
      meFilePach = explode(meFile, "\n");

    size = sizeof(meFilePach);
    ip = query_ip_number(me);

    item_max = sprintf(
      "ID:%s$Ip:%s$Day:%d$Month:%d",
      query("id", me),
      ip,
      localtime2[LT_MON] + 1,
      localtime2[LT_MDAY]
    );

    for (i = 0; i < size; i++) {
      sscanf(meFilePach[i], "ID:%s$Ip:%s$Day:%d$Month:%d", id, ip2, day, month);

      if (ip == ip2 && localtime2[LT_MON] + 1 == day && localtime2[LT_MDAY] == month)  //相同文件内容表示本IP已经有人签到
      {
        ok = 1;
        break;
      } else if (ip == ip2) {  //只是IP相同，并没有签到
        ok = 2;
        meFilePach[i] = "";
        break;
      } else
        ok = 0;
    }
  }

  if (ok == 1) {
    tell_object(me, CYN "签到员：这个IP今天已经有人签过到了。\n" NOR);
    return 1;
  }
  if (ok == 2) {
    write_file(writeFile, "", 1);  //初始化该文件
    //重写文件
    for (i = 0; i < size; i++) {
      if (meFilePach[i] == "") continue;  //去掉IP相同的内容
      write_file(writeFile, sprintf("%s\n", meFilePach[i]));  //重写所有内容
    }
  }
  if (ok != 1) {
    write_file(writeFile, sprintf("%s\n", item_max));  //新记录加入文件
  }
  if (!objectp(env = environment(ob))
    || !stringp(room = query("short", env))) {
    ::die();
    return;
  }

  i = 50000;

  level = query("level", me);
  //在限定的日期内奖励增加
  if ((CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/04" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/05" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/10" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/07" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/08" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/09" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/10")
    j = 5 + random(6);
  else  //过期后奖励下降
    j = 3 + random(3);

  j = level / 10 * 10;
  if (j < 5) j = 5;
  target = query("id", me);


  if (query("hhsjvip", me)) {
    addn("balance", i * 40, me);
    addn("combat_exp", 50000, me);
    addn("potential", 50000, me);
    addn("gongde", 75, me);
  }
  addn("combat_exp", i, me);
  addn("potential", i, me);
  addn("dacard/cishu", 1, me);
  addn("max_neili", 1000, me);
  addn("max_jingli", 501000, me);
  addn("gongde", 70, me);
  addn("hdt", 1, me);
  set("dacard/time", time(), me);
  set("qiandao/2018912", 1, me);
  message("party", (HIW + "【" + room + "】签到员：" + NOR + query("name", me)
    + "今日签到获得\n" NOR), users());
  tell_object(
    me,
    sprintf(CYN + "经验值 50000点，潜能 50000点,内力 1000点,精力 1000点,功德 75点。VIP贵宾可获得功德经验潜能翻倍哦,混沌天挑战副本一次\n" NOR)
  );
  tell_object(me, sprintf(CYN "今日签到成功！祝你游戏愉快！\n" NOR));

  return 1;
}

int do_daka3() {
  object me, ob, env;
  mixed *localtime, *localtime2;
  string writeFile, ip, ip2, *meFilePach, meFile, item_max, id, room, target;
  int i, j, ok, day, month, size, data;
  int level;

  ob = this_object();
  me = this_player();

  if (query_temp("qiandao_time", ob) > time()) {
    tell_object(me, CYN "签到员说道：慢慢来，让我喘口气。\n" NOR);
    return 1;
  }

  if (!query("qiandao/2018912", me))
    return notify_fail("按照顺序来。\n");

  if (query("qiandao/2018913", me))
    return notify_fail("你已经领过礼物了\n");

  data = query("dacard/time", me);
  localtime = localtime(data);
  localtime2 = localtime(time());

  if (data && localtime[LT_MDAY] == localtime2[LT_MDAY]) {
    tell_object(me, CYN "签到员说道：你今天已经签过到了，明天再来吧。\n" NOR);
    return 1;
  }

  if (localtime[LT_MON] + 1 != localtime2[LT_MON] + 1)  //月份
  {
    delete("dacard", me);
  }

  set_temp("qiandao_time", time() + 2, ob);  //因该方式采取读写硬盘，故稍做限制
  writeFile = QIANDAO;

  if (writeFile) {
    meFile = read_file(writeFile);
    if (meFile)
      meFilePach = explode(meFile, "\n");

    size = sizeof(meFilePach);
    ip = query_ip_number(me);

    item_max = sprintf(
      "ID:%s$Ip:%s$Day:%d$Month:%d",
      query("id", me),
      ip,
      localtime2[LT_MON] + 1,
      localtime2[LT_MDAY]
    );

    for (i = 0; i < size; i++) {
      sscanf(meFilePach[i], "ID:%s$Ip:%s$Day:%d$Month:%d", id, ip2, day, month);

      if (ip == ip2 && localtime2[LT_MON] + 1 == day && localtime2[LT_MDAY] == month)  //相同文件内容表示本IP已经有人签到
      {
        ok = 1;
        break;
      } else if (ip == ip2) {  //只是IP相同，并没有签到
        ok = 2;
        meFilePach[i] = "";
        break;
      } else
        ok = 0;
    }
  }

  if (ok == 1) {
    tell_object(me, CYN "签到员：这个IP今天已经有人签过到了。\n" NOR);
    return 1;
  }
  if (ok == 2) {
    write_file(writeFile, "", 1);  //初始化该文件
    //重写文件
    for (i = 0; i < size; i++) {
      if (meFilePach[i] == "") continue;  //去掉IP相同的内容
      write_file(writeFile, sprintf("%s\n", meFilePach[i]));  //重写所有内容
    }
  }
  if (ok != 1) {
    write_file(writeFile, sprintf("%s\n", item_max));  //新记录加入文件
  }
  if (!objectp(env = environment(ob))
    || !stringp(room = query("short", env))) {
    ::die();
    return;
  }

  i = 50000;

  level = query("level", me);
  //在限定的日期内奖励增加
  if ((CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/04" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/05" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/10" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/07" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/08" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/09" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/10")
    j = 5 + random(6);
  else  //过期后奖励下降
    j = 3 + random(3);

  j = level / 10 * 10;
  if (j < 5) j = 5;
  target = query("id", me);


  if (query("hhsjvip", me)) {
    addn("balance", i * 40, me);
    addn("combat_exp", 50000, me);
    addn("potential", 50000, me);
    addn("gongde", 75, me);
  }
  addn("combat_exp", i, me);
  addn("potential", i, me);
  addn("dacard/cishu", 1, me);
  addn("max_neili", 1000, me);
  addn("max_jingli", 1000, me);
  addn("gongde", 70, me);
  addn("hdt", 1, me);
  ob = new("/clone/3");
  ob->move(me, 1);
  set("dacard/time", time(), me);
  set("qiandao/2018913", 1, me);
  message("party", (HIW + "【" + room + "】签到员：" + NOR + query("name", me)
    + "今日签到获得\n" NOR), users());
  tell_object(
    me,
    sprintf(CYN + "经验值 50000点，潜能 50000点,内力 1000点,精力 1000点,功德 75点。VIP贵宾可获得功德经验潜能翻倍哦,混沌天挑战副本一次\n" NOR)
  );
  tell_object(me, sprintf(CYN "今日签到成功！祝你游戏愉快！\n" NOR));

  return 1;
}

int do_daka4() {
  object me, ob, env;
  mixed *localtime, *localtime2;
  string writeFile, ip, ip2, *meFilePach, meFile, item_max, id, room, target;
  int i, j, ok, day, month, size, data;
  int level;

  ob = this_object();
  me = this_player();

  if (query_temp("qiandao_time", ob) > time()) {
    tell_object(me, CYN "签到员说道：慢慢来，让我喘口气。\n" NOR);
    return 1;
  }

  if (!query("qiandao/2018913", me))
    return notify_fail("按照顺序来。\n");

  if (query("qiandao/2018914", me))
    return notify_fail("你已经领过礼物了\n");

  data = query("dacard/time", me);
  localtime = localtime(data);
  localtime2 = localtime(time());

  if (data && localtime[LT_MDAY] == localtime2[LT_MDAY]) {
    tell_object(me, CYN "签到员说道：你今天已经签过到了，明天再来吧。\n" NOR);
    return 1;
  }

  if (localtime[LT_MON] + 1 != localtime2[LT_MON] + 1)  //月份
  {
    delete("dacard", me);
  }

  set_temp("qiandao_time", time() + 2, ob);  //因该方式采取读写硬盘，故稍做限制
  writeFile = QIANDAO;

  if (writeFile) {
    meFile = read_file(writeFile);
    if (meFile)
      meFilePach = explode(meFile, "\n");

    size = sizeof(meFilePach);
    ip = query_ip_number(me);

    item_max = sprintf(
      "ID:%s$Ip:%s$Day:%d$Month:%d",
      query("id", me),
      ip,
      localtime2[LT_MON] + 1,
      localtime2[LT_MDAY]
    );

    for (i = 0; i < size; i++) {
      sscanf(meFilePach[i], "ID:%s$Ip:%s$Day:%d$Month:%d", id, ip2, day, month);

      if (ip == ip2 && localtime2[LT_MON] + 1 == day && localtime2[LT_MDAY] == month)  //相同文件内容表示本IP已经有人签到
      {
        ok = 1;
        break;
      } else if (ip == ip2) {  //只是IP相同，并没有签到
        ok = 2;
        meFilePach[i] = "";
        break;
      } else
        ok = 0;
    }
  }

  if (ok == 1) {
    tell_object(me, CYN "签到员：这个IP今天已经有人签过到了。\n" NOR);
    return 1;
  }
  if (ok == 2) {
    write_file(writeFile, "", 1);  //初始化该文件
    //重写文件
    for (i = 0; i < size; i++) {
      if (meFilePach[i] == "") continue;  //去掉IP相同的内容
      write_file(writeFile, sprintf("%s\n", meFilePach[i]));  //重写所有内容
    }
  }
  if (ok != 1) {
    write_file(writeFile, sprintf("%s\n", item_max));  //新记录加入文件
  }
  if (!objectp(env = environment(ob))
    || !stringp(room = query("short", env))) {
    ::die();
    return;
  }

  i = 50000;

  level = query("level", me);
  //在限定的日期内奖励增加
  if ((CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/04" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/05" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/10" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/07" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/08" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/09" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/10")
    j = 5 + random(6);
  else  //过期后奖励下降
    j = 3 + random(3);

  j = level / 10 * 10;
  if (j < 5) j = 5;
  target = query("id", me);


  if (query("hhsjvip", me)) {
    addn("balance", i * 40, me);
    addn("combat_exp", 50000, me);
    addn("potential", 50000, me);
    addn("gongde", 75, me);
  }
  addn("combat_exp", i, me);
  addn("potential", i, me);
  addn("dacard/cishu", 1, me);
  addn("max_neili", 1000, me);
  addn("max_jingli", 1000, me);
  addn("gongde", 70, me);
  addn("hdt", 1, me);
  ob = new("/clone/vip01/noname2");
  ob->move(me, 1);
  set("dacard/time", time(), me);
  set("qiandao/2018914", 1, me);
  message("party", (HIW + "【" + room + "】签到员：" + NOR + query("name", me)
    + "今日签到获得\n" NOR), users());
  tell_object(
    me,
    sprintf(CYN + "经验值 50000点，潜能 50000点,内力 1000点,精力 1000点,功德 75点。VIP贵宾可获得功德经验潜能翻倍哦,混沌天挑战副本一次\n" NOR)
  );
  tell_object(me, sprintf(CYN "今日签到成功！祝你游戏愉快！\n" NOR));

  return 1;
}

int do_daka5() {
  object me, ob, env;
  mixed *localtime, *localtime2;
  string writeFile, ip, ip2, *meFilePach, meFile, item_max, id, room, target;
  int i, j, ok, day, month, size, data;
  int level;

  ob = this_object();
  me = this_player();

  if (query_temp("qiandao_time", ob) > time()) {
    tell_object(me, CYN "签到员说道：慢慢来，让我喘口气。\n" NOR);
    return 1;
  }

  if (!query("qiandao/2018914", me))
    return notify_fail("按照顺序来。\n");

  if (query("qiandao/2018915", me))
    return notify_fail("你已经领过礼物了\n");

  data = query("dacard/time", me);
  localtime = localtime(data);
  localtime2 = localtime(time());

  if (data && localtime[LT_MDAY] == localtime2[LT_MDAY]) {
    tell_object(me, CYN "签到员说道：你今天已经签过到了，明天再来吧。\n" NOR);
    return 1;
  }

  if (localtime[LT_MON] + 1 != localtime2[LT_MON] + 1)  //月份
  {
    delete("dacard", me);
  }

  set_temp("qiandao_time", time() + 2, ob);  //因该方式采取读写硬盘，故稍做限制
  writeFile = QIANDAO;

  if (writeFile) {
    meFile = read_file(writeFile);
    if (meFile)
      meFilePach = explode(meFile, "\n");

    size = sizeof(meFilePach);
    ip = query_ip_number(me);

    item_max = sprintf(
      "ID:%s$Ip:%s$Day:%d$Month:%d",
      query("id", me),
      ip,
      localtime2[LT_MON] + 1,
      localtime2[LT_MDAY]
    );

    for (i = 0; i < size; i++) {
      sscanf(meFilePach[i], "ID:%s$Ip:%s$Day:%d$Month:%d", id, ip2, day, month);

      if (ip == ip2 && localtime2[LT_MON] + 1 == day && localtime2[LT_MDAY] == month)  //相同文件内容表示本IP已经有人签到
      {
        ok = 1;
        break;
      } else if (ip == ip2) {  //只是IP相同，并没有签到
        ok = 2;
        meFilePach[i] = "";
        break;
      } else
        ok = 0;
    }
  }

  if (ok == 1) {
    tell_object(me, CYN "签到员：这个IP今天已经有人签过到了。\n" NOR);
    return 1;
  }
  if (ok == 2) {
    write_file(writeFile, "", 1);  //初始化该文件
    //重写文件
    for (i = 0; i < size; i++) {
      if (meFilePach[i] == "") continue;  //去掉IP相同的内容
      write_file(writeFile, sprintf("%s\n", meFilePach[i]));  //重写所有内容
    }
  }
  if (ok != 1) {
    write_file(writeFile, sprintf("%s\n", item_max));  //新记录加入文件
  }
  if (!objectp(env = environment(ob))
    || !stringp(room = query("short", env))) {
    ::die();
    return;
  }

  i = 50000;

  level = query("level", me);
  //在限定的日期内奖励增加
  if ((CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/04" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/05" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/10" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/07" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/08" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/09" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/10")
    j = 5 + random(6);
  else  //过期后奖励下降
    j = 3 + random(3);

  j = level / 10 * 10;
  if (j < 5) j = 5;
  target = query("id", me);


  if (query("hhsjvip", me)) {
    addn("balance", i * 40, me);
    addn("combat_exp", 50000, me);
    addn("potential", 50000, me);
    addn("gongde", 75, me);
  }
  addn("combat_exp", i, me);
  addn("potential", i, me);
  addn("dacard/cishu", 1, me);
  addn("max_neili", 1000, me);
  addn("max_jingli", 1000, me);
  addn("gongde", 70, me);
  addn("hdt", 1, me);
  ob = new("/clone/vip/washgift");
  ob->move(me, 1);
  set("dacard/time", time(), me);
  set("qiandao/2018915", 1, me);
  message("party", (HIW + "【" + room + "】签到员：" + NOR + query("name", me)
    + "今日签到获得\n" NOR), users());
  tell_object(
    me,
    sprintf(CYN + "经验值 50000点，潜能 50000点,内力 1000点,精力 1000点,功德 75点。VIP贵宾可获得功德经验潜能翻倍哦,混沌天挑战副本一次\n" NOR)
  );
  tell_object(me, sprintf(CYN "今日签到成功！祝你游戏愉快！\n" NOR));

  return 1;
}

int do_daka6() {
  object me, ob, env;
  mixed *localtime, *localtime2;
  string writeFile, ip, ip2, *meFilePach, meFile, item_max, id, room, target;
  int i, j, ok, day, month, size, data;
  int level;

  ob = this_object();
  me = this_player();

  if (query_temp("qiandao_time", ob) > time()) {
    tell_object(me, CYN "签到员说道：慢慢来，让我喘口气。\n" NOR);
    return 1;
  }

  if (!query("qiandao/2018915", me))
    return notify_fail("按照顺序来。\n");

  if (query("qiandao/2018916", me))
    return notify_fail("你已经领过礼物了\n");

  data = query("dacard/time", me);
  localtime = localtime(data);
  localtime2 = localtime(time());

  if (data && localtime[LT_MDAY] == localtime2[LT_MDAY]) {
    tell_object(me, CYN "签到员说道：你今天已经签过到了，明天再来吧。\n" NOR);
    return 1;
  }

  if (localtime[LT_MON] + 1 != localtime2[LT_MON] + 1)  //月份
  {
    delete("dacard", me);
  }

  set_temp("qiandao_time", time() + 2, ob);  //因该方式采取读写硬盘，故稍做限制
  writeFile = QIANDAO;

  if (writeFile) {
    meFile = read_file(writeFile);
    if (meFile)
      meFilePach = explode(meFile, "\n");

    size = sizeof(meFilePach);
    ip = query_ip_number(me);

    item_max = sprintf(
      "ID:%s$Ip:%s$Day:%d$Month:%d",
      query("id", me),
      ip,
      localtime2[LT_MON] + 1,
      localtime2[LT_MDAY]
    );

    for (i = 0; i < size; i++) {
      sscanf(meFilePach[i], "ID:%s$Ip:%s$Day:%d$Month:%d", id, ip2, day, month);

      if (ip == ip2 && localtime2[LT_MON] + 1 == day && localtime2[LT_MDAY] == month)  //相同文件内容表示本IP已经有人签到
      {
        ok = 1;
        break;
      } else if (ip == ip2) {  //只是IP相同，并没有签到
        ok = 2;
        meFilePach[i] = "";
        break;
      } else
        ok = 0;
    }
  }

  if (ok == 1) {
    tell_object(me, CYN "签到员：这个IP今天已经有人签过到了。\n" NOR);
    return 1;
  }
  if (ok == 2) {
    write_file(writeFile, "", 1);  //初始化该文件
    //重写文件
    for (i = 0; i < size; i++) {
      if (meFilePach[i] == "") continue;  //去掉IP相同的内容
      write_file(writeFile, sprintf("%s\n", meFilePach[i]));  //重写所有内容
    }
  }
  if (ok != 1) {
    write_file(writeFile, sprintf("%s\n", item_max));  //新记录加入文件
  }
  if (!objectp(env = environment(ob))
    || !stringp(room = query("short", env))) {
    ::die();
    return;
  }

  i = 50000;

  level = query("level", me);
  //在限定的日期内奖励增加
  if ((CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/04" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/05" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/10" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/07" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/08" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/09" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/10")
    j = 5 + random(6);
  else  //过期后奖励下降
    j = 3 + random(3);

  j = level / 10 * 10;
  if (j < 5) j = 5;
  target = query("id", me);


  if (query("hhsjvip", me)) {
    addn("balance", i * 40, me);
    addn("combat_exp", 50000, me);
    addn("potential", 50000, me);
    addn("gongde", 75, me);
  }
  addn("combat_exp", i, me);
  addn("potential", i, me);
  addn("dacard/cishu", 1, me);
  addn("max_neili", 1000, me);
  addn("max_jingli", 1000, me);
  addn("gongde", 70, me);
  addn("hdt", 1, me);
  ob = new("/clone/qihun5");
  ob->move(me, 1);
  set("dacard/time", time(), me);
  set("qiandao/2018916", 1, me);
  message("party", (HIW + "【" + room + "】签到员：" + NOR + query("name", me)
    + "今日签到获得\n" NOR), users());
  tell_object(
    me,
    sprintf(CYN + "经验值 50000点，潜能 50000点,内力 1000点,精力 1000点,功德 75点。VIP贵宾可获得功德经验潜能翻倍哦,混沌天挑战副本一次\n" NOR)
  );
  tell_object(me, sprintf(CYN "今日签到成功！祝你游戏愉快！\n" NOR));

  return 1;
}

int do_daka7() {
  object me, ob, env;
  mixed *localtime, *localtime2;
  string writeFile, ip, ip2, *meFilePach, meFile, item_max, id, room, target;
  int i, j, ok, day, month, size, data;
  int level;

  ob = this_object();
  me = this_player();

  if (query_temp("qiandao_time", ob) > time()) {
    tell_object(me, CYN "签到员说道：慢慢来，让我喘口气。\n" NOR);
    return 1;
  }

  if (!query("qiandao/2018916", me))
    return notify_fail("按照顺序来。\n");

  if (query("qiandao/2018917", me))
    return notify_fail("你已经领过礼物了\n");

  data = query("dacard/time", me);
  localtime = localtime(data);
  localtime2 = localtime(time());

  if (data && localtime[LT_MDAY] == localtime2[LT_MDAY]) {
    tell_object(me, CYN "签到员说道：你今天已经签过到了，明天再来吧。\n" NOR);
    return 1;
  }

  if (localtime[LT_MON] + 1 != localtime2[LT_MON] + 1)  //月份
  {
    delete("dacard", me);
  }

  set_temp("qiandao_time", time() + 2, ob);  //因该方式采取读写硬盘，故稍做限制
  writeFile = QIANDAO;

  if (writeFile) {
    meFile = read_file(writeFile);
    if (meFile)
      meFilePach = explode(meFile, "\n");

    size = sizeof(meFilePach);
    ip = query_ip_number(me);

    item_max = sprintf(
      "ID:%s$Ip:%s$Day:%d$Month:%d",
      query("id", me),
      ip,
      localtime2[LT_MON] + 1,
      localtime2[LT_MDAY]
    );

    for (i = 0; i < size; i++) {
      sscanf(meFilePach[i], "ID:%s$Ip:%s$Day:%d$Month:%d", id, ip2, day, month);

      if (ip == ip2 && localtime2[LT_MON] + 1 == day && localtime2[LT_MDAY] == month)  //相同文件内容表示本IP已经有人签到
      {
        ok = 1;
        break;
      } else if (ip == ip2) {  //只是IP相同，并没有签到
        ok = 2;
        meFilePach[i] = "";
        break;
      } else
        ok = 0;
    }
  }

  if (ok == 1) {
    tell_object(me, CYN "签到员：这个IP今天已经有人签过到了。\n" NOR);
    return 1;
  }
  if (ok == 2) {
    write_file(writeFile, "", 1);  //初始化该文件
    //重写文件
    for (i = 0; i < size; i++) {
      if (meFilePach[i] == "") continue;  //去掉IP相同的内容
      write_file(writeFile, sprintf("%s\n", meFilePach[i]));  //重写所有内容
    }
  }
  if (ok != 1) {
    write_file(writeFile, sprintf("%s\n", item_max));  //新记录加入文件
  }
  if (!objectp(env = environment(ob))
    || !stringp(room = query("short", env))) {
    ::die();
    return;
  }

  i = 50000;

  level = query("level", me);
  //在限定的日期内奖励增加
  if ((CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/04" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/05" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/10" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/07" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/08" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/09" ||
    (CHINESE_D->chinese_time(7, ctime(time()))) == "2018/10/10")
    j = 5 + random(6);
  else  //过期后奖励下降
    j = 3 + random(3);

  j = level / 10 * 10;
  if (j < 5) j = 5;
  target = query("id", me);


  if (query("hhsjvip", me)) {
    addn("balance", i * 40, me);
    addn("combat_exp", 50000, me);
    addn("potential", 50000, me);
    addn("gongde", 75, me);
  }
  addn("combat_exp", i, me);
  addn("potential", i, me);
  addn("dacard/cishu", 1, me);
  addn("max_neili", 1000, me);
  addn("max_jingli", 1000, me);
  addn("gongde", 30070, me);
  addn("hdt", 1, me);
  set("dacard/time", time(), me);
  set("qiandao/2018917", 1, me);
  message("party", (HIW + "【" + room + "】签到员：" + NOR + query("name", me)
    + "今日签到获得\n" NOR), users());
  tell_object(
    me,
    sprintf(CYN + "经验值 50000点，潜能 50000点,内力 1000点,精力 1000点,功德 75点。VIP贵宾可获得功德经验潜能翻倍哦,混沌天挑战副本一次\n" NOR)
  );
  tell_object(me, sprintf(CYN "今日签到成功！祝你游戏愉快！\n" NOR));

  return 1;
}

int do_query_time() {
  int time;
  mixed *localtime;
  object me = this_player();

  time = query("dacard/time", me);
  localtime = localtime(time);
  tell_object(me, sprintf(CYN "你本月到目前为止总共签到 %d次。\n" NOR, query("dacard/cishu", me)));
  tell_object(me, sprintf(CYN "%s的上次签到时间是：%d年%d月%d日%d时%d分%d秒。\n" NOR,
    query("name", me), localtime[LT_YEAR], localtime[LT_MON] + 1,
    localtime[LT_MDAY], localtime[LT_HOUR], localtime[LT_MIN], localtime[LT_SEC]));
  return 1;
}

void die() {
  object me, ob;

  me = this_object();

  set("eff_qi", 10000);
  set("qi", 10000);
  set("eff_jing", 10000);
  set("jing", 10000);
  set("neili", 90000);
  set("max_jingli", 10000);

  say(me->name() + CYN "说道：我是不死的，你的行为已经被记录在案。\n" NOR);
  return;

}
