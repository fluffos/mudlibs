#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIM "混沌青殿" NOR);
  set("long", @LONG
封印神魔的所在之一青龙！
LONG);

  set("maze", 1);
  set("no_clean_up", 1);
  set("no_magic", 1);
  set("virtual_room", 1);
  set("outdoors", "hunduntian");
  set("exits", ([
    "up": "/d/city/guangchang",
  ]));

  set("objects", ([
    "/maze/hunduntian/npc/qinglong": 1,
  ]));
  setup();
  // replace_program(ROOM);
}
