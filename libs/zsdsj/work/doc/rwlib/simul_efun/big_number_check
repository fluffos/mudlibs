名稱：
	big_number_check() - 檢查是否為大數字串。
語法：
	string big_number_check(mixed bn);
用法：
	若 bn 符合大數格式，則回傳修改為標準大數字串後的 bn，bn 可以為整數或字串，
	若 bn 不符合大數格式，則回傳 0 值，bn 同時支援 3,123,141,333 的逗號數值格
	式，並回傳 3123141333。
參考：

作者：
	Clode@RevivalWorld
