名稱：
	multiwrap() - 多行字串自動換行處理。
語法：
	string multiwrap(string arg, int max_width)
用法：
	此函式可以傳入已有換行碼的字串 arg 或是未有換行碼的字串 arg，若 arg 本身已有換行碼
	且寬度不超過 width，則不會對 arg 進行自動換行的動作，保有原有的換行方式，但或字串
	本身並無換行碼，或是字串超過寬度 width 時，則會自動對其加入換行碼的動作。
參考：

作者：
	Clode@RevivalWorld
