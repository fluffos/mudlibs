名稱：
	floor() - 找到最靠近一個浮點數整數，小於或等於此浮點數。意即把一個浮點數的小數
		  位捨去.
語法：
	float floor( float f );

	浮點數 floor( 浮點數 f );
用法：
	本函式僅在定義 PACKAGE_MATH 時生效。

	傳回一個最靠近浮點數 f，而等於或小於 f 的整數。
譯註：
	意即把浮點數 f 的小數位捨去。
參考：
	ceil, to_int, to_float
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻譯：
	spock@muds.net		2000.May.28.	v22 版
