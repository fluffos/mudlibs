名稱：
	pow() - 計算一個浮點數 (float) 的乘冪 (exponent)。
語法：
	float pow( float x, float y );

	浮點數 pow( 浮點數 x, 浮點數 y );
用法：
	本函式僅在定義 PACKAGE_MATH 時生效。

	pow() 傳回 x 的 y 次方值。如果 x 是 0.0，y 必須為正數。如果 x 為負
	數，y 必須為一個整數。
參考：
	exp, log, sqrt
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻譯：
	spock@muds.net		2000.May.28.	v22 版
