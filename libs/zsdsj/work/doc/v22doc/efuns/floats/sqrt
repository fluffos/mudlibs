名稱：
	sqrt() - 傳回一個浮點數 (float) 的平方根 (square root)。
語法：
	float sqrt( float f );

	浮點數 sqrt( 浮點數 f );
用法：
	本函式僅在定義 PACKAGE_MATH 時生效。

	sqrt() 傳回參數 f 的正平方根。f 不可為負數。
參考：
	exp, log, pow
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻譯：
	spock@muds.net		2000.May.28.	v22 版
