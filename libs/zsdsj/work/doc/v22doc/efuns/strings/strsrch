名稱：
	strsrch - 在一個字串中尋找子字串 (substring)。
語法：
	int strsrch( string str, string substr | int char, int flag );

	整數 strsrch( 字串 ,字串 字串 子字串 或 整數 字元, 整數 旗標 );
用法：
	strsrch() 在字串「str」之中尋找第一個出現的「substr」。如果可選擇的
	第三個參數「flag」為 -1，則尋找最後一個出現的「substr」。如果第二個
	參數是整數，則只尋找該字元 (character)〈如同 C 的 strchr()／strrchr()
	〉。strsrch() 無法尋找空字串或 (empty string) 是虛值 (null value)。

	本函式傳回第一個符合或最後一個符合的字串出現在「str」中的位置。如果
	沒有符合的字串，則傳回 -1，或是錯誤〈有問題的參數等等〉。
參考：
	sscanf, replace_string, regexp
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻譯：
	spock@muds.net		2000.Aug.28.	v22 版
