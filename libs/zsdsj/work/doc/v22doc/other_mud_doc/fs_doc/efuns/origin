
origin(3)             LPC 系統函數             origin(3)

名稱
     origin() - 決定現在的函數是怎麼被呼叫的

語法
     int origin( void );

描述
     傳回值用來判斷現在的函數是如何被呼叫的, 也就是本函數應該是在
     其它函數中呼叫來判斷它是怎麼被呼叫的, 通常跟系統安全有關. 其
     傳回值的意義就代表被呼叫的方式, 內容如下:

     1      驅動程式呼叫的(如: heart_beats 等等)

     2      定義在 mudlib 物件中的局部函數.

     4      由 call_other() 所呼叫.

     8      由 simul_efun 物件(/adm/obj/simul_efun.c)透過 simul_efun 所呼叫的.

     16     由 call_out() 所呼叫的.

     32     由系統函數所呼叫的(如: sort_array 等等)

請參考
     previous_object(3), /include/origin.h

MudOS                Last change: 5 Sep 1994                    1

