acos(3)               LPC 系統函數               acos(3)

名稱
     acos() - 傳回反餘弦(arc cos())函數(三角函數)

語法
     float acos( float f );

描述
     傳回參數 <f> 的反餘弦函數值, 參數跟傳回值都是浮點數型態變數,
     其單位為弳度量(非角度量單位).

     弳度量 r 跟角度量 a 的關係如下:

     r = a * PI / 180.0;

請參考
     asin(3), atan(3), cos(3), sin(3), tan(3)

MudOS                Last change: 5 Sep 1994                    1

