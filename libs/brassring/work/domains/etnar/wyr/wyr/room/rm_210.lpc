/*   Modified by lash (ccoker) for use with:
 *
 *   The Dead Souls Mud Library
 *   developed by Cratylus
 *   http://www.dead-souls.net
 */

#include <lib.h>

inherit LIB_ROOM;

nosave void create() {

  room::create();
  SetClimate("outdoors");
  SetAmbientLight(30);
  SetShort("rm_210.lpc");
  SetLong("   rm_210"
  );
  SetItems(([
  ]));
  SetExits(([
    "west": "/domains/etnar/wyr/wyr/room/rm_209",
    "east": "/domains/etnar/wyr/wyr/room/rm_211.lpc",
  ]));

}

void init() {
  ::init();
}
