#pragma show_error_context
#include <globals.h>

void create() {}

string creator_file(string) {
	return ROOT_UID;
}

/* Archive fb() uses these; they are not efuns on this FluffOS. */
void prompt_off() {}
void prompt_on() {}
