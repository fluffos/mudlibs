// qload.c
// 修改：中山大学  张三丰（Micro）----Micro_Liang@Hotmail.Com		1997.6

inherit F_CLEAN_UP;

int main() {
  write(query_load_average() + "\n");
  return 1;
}

int help(object me) {
  write(@HELP
指令格式：qload 

显示目前 CPU 的负担：cmds/s 是每秒编译几个命令，comp lines/s 是每秒几行

HELP
  );

  return 1;
}
