// reclaim.c
// 修改：中山大学  张三丰（Micro）----Micro_Liang@Hotmail.Com		1997.6

int main(object me, string arg) {
  int mem;

  write("整理结果，共清除 " + reclaim_objects() + " 个变数。\n");
  return 1;
}

int help(object me) {
  write(@HELP
指令格式：reclaim

将存在于内存中无用的变数清除，以减少内存的使用。

HELP
  );

  return 1;
}
