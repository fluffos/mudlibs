// clear.c	(清屏)
// By Key	1999-03-03

#include <ansi.h>

inherit F_CLEAN_UP;

int main(object me, string arg)
{
	write(CLR);
	return 1;
}

int help(object me)
{
     write(@HELP

指令格式: clear

这个指令让你将屏幕上信息清除，让屏幕整洁。

HELP
    );
    return 1;
}
