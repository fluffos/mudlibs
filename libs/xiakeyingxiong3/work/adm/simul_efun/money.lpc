// money.c
// 编程：中山大学  张三丰（Micro）----Micro_Liang@Hotmail.Com    1997.06

// 以对等的币值返回相应的价值汉字字串。当价值为零时，无法判断价值
string price_str(int amount) 
{
        return MONEY_D->price_str(amount);
}

// 以对等的币值返回相应的价值汉字字串。当价值为零时，自动设定为一文铜板
string money_str(int amount)
{
        return MONEY_D->money_str(amount);
}

// 支付玩家一定的金钱（支持金票及银票）
void pay_player(object who, int amount)
{
        return MONEY_D->pay_player(who, amount);
}

// 判断玩家是否能付出一定的金钱（支持金票及银票）
int can_afford(object who, int amount) 
{
        return MONEY_D->can_afford(who, amount);
}

// 玩家付出一定的金钱（支持金票及银票）。成功返回真，否则返回零
int player_pay(object who, int amount) 
{
        return MONEY_D->player_pay(who, amount);
}
