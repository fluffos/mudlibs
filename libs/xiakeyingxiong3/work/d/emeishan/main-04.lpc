// main-04.c - 峨眉山主干线（华严寺）
// 编程：中山大学  张三丰（Micro）----exslyc@zsulink.zsu.edu.cn		1997.8

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "华严寺");
 	set("long", @LONG
								
    这里是峨眉山的华严寺。
LONG
	);

        set("outdoors", "emeishan");

 	set("exits", ([
		"northup"   : __DIR__"main-05",
		"eastdown"  : __DIR__"main-03",
	]));

	setup();

	replace_program(ROOM);

}

