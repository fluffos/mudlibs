// main-05.c - 峨眉山主干线（纯阳殿）
// 编程：中山大学  张三丰（Micro）----exslyc@zsulink.zsu.edu.cn		1997.8

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "纯阳殿");
 	set("long", @LONG
								
    这里是峨眉山的纯阳殿。
LONG
	);

        set("outdoors", "emeishan");

 	set("exits", ([
		"westup"    : __DIR__"main-06",
		"southdown" : __DIR__"main-04",
	]));

	setup();

	replace_program(ROOM);

}

