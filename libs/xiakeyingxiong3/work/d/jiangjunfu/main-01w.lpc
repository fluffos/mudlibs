// main-01w.c - 侠客城的将军府（西  房）
// 编程：中山大学  孔  雀（Key）----ln96wgx@nct.zsu.edu.cn         1997.9

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "西  房");
        set("long", @LONG
                                                                
    这里是侠客城将军府的西房。
LONG
        );

        // set("outdoors", "xiakecheng");

        set("exits", ([
                "east"  : __DIR__"main-01",
        ]));

        setup();

        replace_program(ROOM);

}