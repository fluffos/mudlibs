// main-09.c - 侠客城的将军府（前堂楼）
// 编程：中山大学  孔  雀（Key）----ln96wgx@nct.zsu.edu.cn         1997.9

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "前堂楼");
        set("long", @LONG
                                                                
    这里是侠客城将军府的前堂楼。
LONG
        );

        // set("outdoors", "xiakecheng");

        set("exits", ([
                "south"  : __DIR__"main-08",
                "north"  : __DIR__"main-10",
        ]));

        setup();

        replace_program(ROOM);
}