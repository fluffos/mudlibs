// main-07e.c - 侠客城的将军府（偏  堂）
// 编程：中山大学  孔  雀（Key）----ln96wgx@nct.zsu.edu.cn         1997.9

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "偏  堂");
        set("long", @LONG
                                                                
    这里是侠客城将军府的偏堂。
LONG
        );

        // set("outdoors", "xiakecheng");

        set("exits", ([
                "west"   : __DIR__"main-07",
        ]));

        setup();

        replace_program(ROOM);
}