// mz-02w.c - 杭州的梅庄（西厢房）
// 编程：中山大学  孔雀（Key）----ln96wgx@nct.zsu.edu.cn         1997.9

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "西厢房");
        set("long", @LONG
                                                                
    这里是梅庄的西厢房。
LONG
        );

        // set("outdoors", "meizhuang");

        set("exits", ([
                "east"  : __DIR__"mz-02",
        ]));

        setup();

        replace_program(ROOM);
}