// mz-09.c - 杭州的梅庄（后  院）
// 编程：中山大学  孔雀（Key）----ln96wgx@nct.zsu.edu.cn         1997.9

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "后  院");
        set("long", @LONG
                                                                
    这里是梅庄的后院。
LONG
        );

        set("outdoors", "meizhuang");

        set("exits", ([
                "south" : __DIR__"mz-08",
        ]));

        setup();

        replace_program(ROOM);
}