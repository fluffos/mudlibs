// mz-06w.c - 杭州的梅庄（书  房）
// 编程：中山大学  孔雀（Key）----ln96wgx@nct.zsu.edu.cn         1997.9

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "书  房");
        set("long", @LONG
                                                                
    这里是梅庄的书房。
LONG
        );

        // set("outdoors", "meizhuang");

        set("exits", ([
                "east"  : __DIR__"mz-06",
        ]));

        setup();

        replace_program(ROOM);
}