// mz-03e.c - 杭州的梅庄（东厅房）
// 编程：中山大学  孔雀（Key）----ln96wgx@nct.zsu.edu.cn         1997.9

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "东厅房");
        set("long", @LONG
                                                                
    这里是梅庄的东厅房。
LONG
        );

        // set("outdoors", "meizhuang");

        set("exits", ([
                "west"  : __DIR__"mz-03",
        ]));

        setup();

        replace_program(ROOM);
}