// main-02w.c - 终南山（桃源亭）
// 编程：中山大学  孔 雀（Key）----ln96wgx@nct.zsu.edu.cn         1997.9

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "桃源亭");
        set("long", @LONG
                                                                
    这里是终南山上的桃源亭。
LONG
        );

        // set("outdoors", "zhongnanshan");

        set("exits", ([
                "northwest"  :__DIR__ "fen-01",
                "east"       :__DIR__ "main-02",
        ]));

        setup();

        replace_program(ROOM);

}