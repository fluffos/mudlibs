// ngate1.c 紫禁城城门

#include <room.h>

inherit ROOM;

string look_sign(object me);

void create()
{
        set("short", "紫禁城");
	set("long", @LONG
由于还没有完工，所以这里显得特别荒凉。这里有一个牌子（paizi），不知道写些什么呢。
LONG
	);


	set("item_desc", ([
		"paizi": (: look_door :),
	]) );
	
	set("exits", ([
		"south"    	: __DIR__"ngate1",
		 ]) );

/*	set("objects", ([
		__DIR__"npc/shiwei1" : 2 ,
		__DIR__"npc/shiwei2" : 2,  //侍卫
       ]) );
*/
	create_door("south", "城门", "north", DOOR_CLOSED);

	setup();

}



string look_door(object me)
{
	return "游客止步。争取早日完工，开放参观。——大鱼启\n";
	
}
