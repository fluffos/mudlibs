// nw_road5.c- 新街口北大街

inherit ROOM;

void create()
{
        set("short", "新街口北大街");
        set("long", @LONG
北京的南北方向的主要街道，南边是商业区，再往北走，就要到护城墙了。
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"nw_road4",
//   "north" : __DIR__"nw_gate",
//   "west"  : __DIR__"bupt/xueyuannan_road",
]));
        set("no_clean_up", 0);
        set("outdoors", "beijing");
        setup();
        replace_program(ROOM);
}
