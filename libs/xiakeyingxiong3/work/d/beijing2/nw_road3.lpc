// nw_road3.c- 新街口

inherit ROOM;

void create()
{
        set("short", "新街口");
        set("long", @LONG
北京的主要道口之一，交通四通八达，南来北往的商贩大都经过这里。
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"nw_road4",
  "south" : __DIR__"nw_road2",
  "east"  : __DIR__"rouge_shop",
  "west"  : __DIR__"jewelry_shop",
]));
        set("no_clean_up", 0);
        set("outdoors", "beijing");
        setup();
        replace_program(ROOM);
}
