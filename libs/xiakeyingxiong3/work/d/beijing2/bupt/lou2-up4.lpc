// zhuxi.c -  北邮

#include <ansi.h>
inherit ROOM;


void create()
{
        set("short", "401教室");
        set("long", @LONG
91　无线　I 大班的同学正在这个教室里进行 《卫星通讯》的考试。透过窗口(window)
可以看到里面的情况。
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
  "down" : __DIR__"lou2-up3",
]));
        set("item_desc", ([
                "window" : @TEXT
大鱼动张西望，不知道想干什么。
TEXT
        ]) );


        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);

}


