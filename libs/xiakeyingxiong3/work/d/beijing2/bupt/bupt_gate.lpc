// bupt_gate.c -  北邮大门 

inherit ROOM;

void create()
{
        set("short", "北邮大门");
        set("long", @LONG
“北京邮电大学”，一个招牌挂在这里。没有什么装饰。远远得好象看见毛主席在向
你招手。
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"zhuxi",
  "south" : __DIR__"xueyuannan_road",
]));
        set("no_clean_up", 0);
        set("outdoors", "beijing");
        setup();
        replace_program(ROOM);
}
