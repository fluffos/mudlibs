// main-22.c - 华山主干线（巨灵足）
// 编程：中山大学  张三丰（Micro）----exslyc@zsulink.zsu.edu.cn		1997.8

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "巨灵足");
 	set("long", @LONG
								
    这里是华山的巨灵足。
LONG
	);

        set("outdoors", "huashan");

 	set("exits", ([
		"northup"  : __DIR__"main-21",
		"southup"  : __DIR__"main-23",
	]));

	setup();

	replace_program(ROOM);

}

