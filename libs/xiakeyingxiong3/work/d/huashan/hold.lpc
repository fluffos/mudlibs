
inherit ROOM;

void create()
{
        set("short", "神秘山洞");
        set("long", @LONG
    此处已经是甬道的尽头，空间突然变得开阔了。借着微亮的闪光，你
只看到地上到处都是枯骨和断掉的兵器，不明白这些死去的人是从那里来
的，也不知道他们为何葬身此地。
LONG
        );
        set("exits", ([ 
	  "southup"    : __DIR__"yongdao2",
]));
        set("objects", ([
                __DIR__"obj/skeleton" : 2,
                __DIR__"obj/hammer1" : 1,
       ]) );
        setup();
}

