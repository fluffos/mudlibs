// main-09.c - 紫禁城－保和殿

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "保和殿");
        set("long", @LONG

    这里是紫禁城的保和殿。
LONG
        );

        set("city", 1);
        // set("outdoors", "beijing");

        set("exits", ([
                "east"  : __DIR__"main-09-e1",
                "south" : __DIR__"main-08",
                "west"  : __DIR__"main-09-w1",
                "north" : __DIR__"main-10",
        ]));

        setup();

        replace_program(ROOM);
}
