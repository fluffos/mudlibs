// main-07-e1.c - 紫禁城－体仁阁

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "体仁阁");
        set("long", @LONG

    这里是紫禁城的体仁阁。
LONG
        );

        set("city", 1);
        // set("outdoors", "beijing");

        set("exits", ([
                "east"  : __DIR__"main-07-e2",
                "south" : __DIR__"main-06-e1",
                "west"  : __DIR__"main-07",
                "north" : __DIR__"main-08-e1",
        ]));

        setup();

        replace_program(ROOM);
}
