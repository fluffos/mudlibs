// main-10-e1.c - 紫禁城－奉先殿

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "奉先殿");
        set("long", @LONG

    这里是紫禁城的奉先殿。
LONG
        );

        set("city", 1);
        // set("outdoors", "beijing");

        set("exits", ([
                "east"  : __DIR__"main-10-e2",
                "south" : __DIR__"main-09-e1",
                "west"  : __DIR__"main-10",
                "north" : __DIR__"main-11-e1",
        ]));

        setup();

        replace_program(ROOM);
}
