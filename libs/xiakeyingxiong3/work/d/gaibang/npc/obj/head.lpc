inherit ITEM;
void create() {
  set_name("汉奸的头颅", ({ "head" }));
  set_weight(2000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "个");
    set("long", "这是一个汉奸的头颅。\n");
    set("value", 0);
    set("material", "flesh");
  }
}
