// main-w3s2.c - 泉州城－礼让巷

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "礼让巷");
        set("long", @LONG

    这里是泉州城的礼让巷。
LONG
        );

        set("city", 1);
        set("outdoors", "quanzhou");

        set("exits", ([
                "east"  : __DIR__"main-w2s2",
                "south" : __DIR__"main-w3s3",
                // "west"  : __DIR__"main-w4s2",
                "north" : __DIR__"main-w3s1",
        ]));

        setup();

        replace_program(ROOM);
}
