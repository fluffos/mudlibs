// Room: /d/quanzhou/main-e1n1.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create()
{
	set("short", "泉州客栈");
	set("long", @LONG

    这是一家价钱低廉的客栈，生意非常兴隆。顾客多是匆匆过路的游人，他们买
点包子、鸡腿、米酒就赶路去了。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"main-e1",
  "west" : __DIR__"main-n1",
]));
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/waiter" : 1,
]));
	set("city", 1);
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
