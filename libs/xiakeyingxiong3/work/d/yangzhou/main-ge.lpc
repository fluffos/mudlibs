// main-ge.c - 扬州城－东城门

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "东城门");
        set("long", @LONG

    这里是扬州城的东城门。
LONG
        );

        set("city", 1);
        set("outdoors", "yangzhou");

        set("exits", ([
                "west"     : __DIR__"main-e3",
                "eastdown" : __DIR__"yzg",
        ]));

        setup();

        replace_program(ROOM);
}
