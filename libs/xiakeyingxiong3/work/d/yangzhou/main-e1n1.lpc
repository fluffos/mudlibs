// main-e1n1.c - 扬州城－当  铺

#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "当  铺");
  set("long", @LONG

    这里是扬州城的当  铺。
LONG
  );

  set("city", 1);
  // set("outdoors", "yangzhou");

  set("exits", ([
    "east": __DIR__ "main-e2n1",
    "south": __DIR__ "main-e1",
    "west": __DIR__ "main-n1",
    "north": __DIR__ "main-e1n2",
  ]));

  set("objects", ([
    "/d/center/npc/hockman": 1,
  ]));
  setup();

  replace_program(ROOM);
}
