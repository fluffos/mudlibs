// dms-e1n1.c - 扬州城－平山堂

#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "平山堂");
  set("long", @LONG

    这里是扬州城大明寺的平山堂。
LONG
  );

  set("city", 1);
  // set("outdoors", "yangzhou");

  set("exits", ([
    // "east"  : __DIR__"dms-e1n1",
    "south": __DIR__ "dms-e1",
    // "west"  : __DIR__"dms-e1n1",
    // "north" : __DIR__"dms-e1n1",
  ]));

  setup();

  replace_program(ROOM);
}
