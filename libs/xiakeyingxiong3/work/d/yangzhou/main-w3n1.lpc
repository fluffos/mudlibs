// main-w3n1.c - 扬州城－石塔寺

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "石塔寺");
        set("long", @LONG

    这里是扬州城的石塔寺。
LONG
        );

        set("city", 1);
        // set("outdoors", "yangzhou");

        set("exits", ([
                "east"  : __DIR__"main-w2n1",
                "south" : __DIR__"main-w3",
                // "west"  : __DIR__"main-w4n1",
                "north" : __DIR__"main-w3n2",
        ]));

        set("objects", ([
                "/d/center/npc/lao-heshang": 1,
         ]) );
        setup();

        replace_program(ROOM);
}
