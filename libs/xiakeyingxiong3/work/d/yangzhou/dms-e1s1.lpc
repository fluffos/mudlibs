// dms-e1s1.c - 扬州城－金刚殿

#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "金刚殿");
        set("long", @LONG

    这里是扬州城大明寺的金刚殿。
LONG
        );

        set("city", 1);
        // set("outdoors", "yangzhou");

        set("exits", ([
                // "east"  : __DIR__"dms-e1s1",
                // "south" : __DIR__"dms-e1s1",
                // "west"  : __DIR__"dms-e1s1",
                "north" : __DIR__"dms-e1",
        ]));

        setup();

        replace_program(ROOM);
}
