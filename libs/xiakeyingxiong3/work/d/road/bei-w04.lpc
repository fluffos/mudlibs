// bei-w04.c - 北京城向西道路（太行山脚）
// 编程：中山大学  张三丰（Micro）----exslyc@zsulink.zsu.edu.cn		1997.8

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "太行山脚");
 	set("long", @LONG
								
    这里是太行山脚。
LONG
	);

	set("outdoors", "road");

 	set("exits", ([
		"east"  : __DIR__"bei-w03",
		"west"  : __DIR__"bei-w05",
	]));

	setup();

	replace_program(ROOM);

}
