// temp-xingsuhai.c - 这里是还没有建好的星宿海

inherit ROOM;

string look_sign(object me);

void create()
{
	set("short", "星宿海");
	set("long", @LONG

    这里是还没有建好的星宿海。
LONG
	);

	set("exits", ([ 
		"east"	: __DIR__"xing-e1", 
	]));

        set("objects", ([
                "/d/xingsuhai/npc/master-ding": 1,
	]) );

	setup();
}

