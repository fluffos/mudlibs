// Room: /d/road/xia-s4.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create()
{
	set("short", "聚龙山");
	set("long", @LONG
								
    这里是侠客城以南的聚龙山。
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
  "/d/quanzhou/npc/ant" : 1,
]));
	set("outdoors", "road");
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"xia-s3",
  "south" : __DIR__"xia-s5",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
