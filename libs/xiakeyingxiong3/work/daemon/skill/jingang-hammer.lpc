// jingang-hammer.c - 金刚锤法

inherit SKILL;

mapping *action = ({
  ([ "action": "$N一招「割肉饲鹰」，手中$w自下而上划了个半弧，向$n的$l挥去",
    "force": 50,
    "dodge": -10,
    "parry": -10,
    "damage": 5,
    "lvl": 0,
    "skill_name": "割肉饲鹰",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N左手虚指，一招「投身饿虎」，右手$w笔直击向$n的$l",
    "force": 80,
    "dodge": -5,
    "parry": -5,
    "damage": 10,
    "lvl": 10,
    "skill_name": "投身饿虎",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N一招「斫头谢天」，$w呼地一声自上而下向$n猛劈",
    "force": 100,
    "dodge": 1,
    "parry": 1,
    "damage": 20,
    "lvl": 20,
    "skill_name": "斫头谢天",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N右手一招「折骨出髓」，猛一挫身，$w直向$n的颈中击去",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 30,
    "lvl": 30,
    "skill_name": "折骨出髓",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N一招「挑身千灯」，化作团团锤影，向$n的$l挥去",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 40,
    "lvl": 40,
    "skill_name": "挑身千灯",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N双手合执$w，一招「挖眼布施」，拧身急转，手中$w直指向$n的双眼",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 50,
    "lvl": 50,
    "skill_name": "挖眼布施",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N一招「剥皮书经」，手中$w舞出一个大平十字，向$n纵横击去",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 60,
    "lvl": 60,
    "skill_name": "剥皮书经",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N将$w护在胸前，一招「剜心决志」，从一个意想不到的方位向$n的$l击去",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 70,
    "lvl": 70,
    "skill_name": "剜心决志",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N一招「烧身供佛」，$w仿佛化成一簇簇烈焰，将$n团团围绕",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 80,
    "skill_name": "烧身供佛",
    "damage_type": "挫伤"
  ]),
  ([ "action": "$N左手虚指，一招「刺血满地」，右手中$w如飓风般裹向$n的全身",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 100,
    "skill_name": "刺血满地",
    "damage_type": "挫伤"
  ]),
});

int valid_enable(string usage) { return (usage == "qimen") || (usage == "parry"); }

int valid_learn(object me) {
  object ob;
  int level;

  if ((int)me->query("max_force") < 100)
    return notify_fail("你的内力不够。\n");
  if (!(ob = me->query_temp("weapon"))
    || (string)ob->query("skill_type") != "hammer")
    return notify_fail("你必须先找一根杖才能练金刚锤法。\n");
  if ((int)me->query_skill("buddhism") < (int)me->query_skill("jingang-hammer") / 2)
    return notify_fail("佛门每种绝技都要有慈悲佛法为之化解，你好象学得不够呀！\n");
  level = me->query_skill("jingang-hammer");
  if (level * level * level / 10 > me->query("SEN"))
    return notify_fail("修练金刚锤法，要除暴安良，行侠仗义。你好象做得不够呀！\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("jingang-hammer", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  object weapon;

  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "hammer")
    return notify_fail("你使用的武器不对。\n");
  if ((int)me->query("kee") < 40)
    return notify_fail("你的体力不够练金刚锤法。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍金刚锤法。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "jingang-hammer/" + action;
}

int enable_factor() { return 100; }

int learn_factor(int n) {
  if (n < 100) return 120;
  else return 100;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 3; }

int practice_factor() { return 110; }
int practice_bonus() { return 1; }
