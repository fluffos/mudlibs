// tianying-zhua.c - 天鹰神爪

inherit SKILL;

mapping *action = ({
  ([ "action": "$N一招「天鹰神爪」，气凝丹田，右手成爪缓缓的抓向$n的$l",
    "force": 80,
    "dodge": 5,
    "parry": 5,
    "damage": 5,
    "lvl": 0,
    "skill_name": "天鹰神爪",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「天鹰神爪」，双爪一振，一股力道排山倒海般向$n的$l推了过去",
    "force": 100,
    "dodge": 5,
    "parry": 5,
    "damage": 10,
    "lvl": 10,
    "skill_name": "天鹰神爪",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「天鹰神爪」，大吼一声，一爪平平地抓向$n的$l",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 20,
    "lvl": 20,
    "skill_name": "天鹰神爪",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N施出「天鹰神爪」，左手护胸，右爪直直地抓向$n的$l",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 30,
    "lvl": 30,
    "skill_name": "天鹰神爪",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N纵身一跃，一招「天鹰神爪」，幻成一条疾光击向$n的$l",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 40,
    "lvl": 40,
    "skill_name": "天鹰神爪",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「天鹰神爪」，掌势如风，双手直击$n的$l",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 50,
    "lvl": 50,
    "skill_name": "天鹰神爪",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N身形一转，一招「天鹰神爪」，爪风不定，招招击向$n的$l要害",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 60,
    "lvl": 60,
    "skill_name": "天鹰神爪",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「天鹰神爪」，$n只觉得眼前一花，全身被笼罩在一团爪影之中",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 70,
    "skill_name": "天鹰神爪",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N错步上前，一招「天鹰神爪」，爪意若有若无，淡淡地向$n的$l击去",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 80,
    "skill_name": "天鹰神爪",
    "damage_type": "瘀伤"
  ]),
  ([ "action": "$N一招「天鹰神爪」，纵身飘开数尺，运发真气，双爪遥摇指向$n的$l",
    "force": 260,
    "dodge": 80,
    "parry": 80,
    "damage": 120,
    "lvl": 100,
    "skill_name": "天鹰神爪",
    "damage_type": "瘀伤"
  ]),
});

int valid_enable(string usage) { return (usage == "unarmed") || (usage == "parry"); }

int valid_learn(object me) {
  object ob;
  int level;

  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练天鹰神爪必须空手。\n");
  if ((int)me->query("max_force") < 150)
    return notify_fail("你的内力不够。\n");
  level = me->query_skill("tianying-zhua");       // 这里的负神不用要求太高！！
  if (-level * level * level / 20 < me->query("SEN"))
    return notify_fail("修练天鹰神爪，要心狠手辣，奸恶歹毒。你好象做得不够呀！\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("tianying-zhua", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  object weapon;

  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练天鹰神爪必须空手。\n");
  if ((int)me->query("kee") < 40)
    return notify_fail("你的体力不够练天鹰神爪。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍天鹰神爪。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "tianying-zhua/" + action;
}

int enable_factor() { return 100; }

int learn_factor(int n) {
  if (n < 100) return 115;
  else return 95;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 2; }

int practice_factor() { return 105; }
int practice_bonus() { return 1; }
