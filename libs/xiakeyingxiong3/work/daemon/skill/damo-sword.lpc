// damo-sword.c - 达摩剑法

inherit SKILL;
/*
少林达摩剑（金针渡劫）
*/
mapping *action = ({
([      "action" : "只见$N一招「万事随缘往」，手中$w嗡嗡微振，幻成一条疾光刺向$n的$l",
        "force"         : 100,
        "dodge"         : 10,
        "parry"         : 10,
        "damage"        : 10,
        "lvl"           : 0,
        "skill_name"    : "万事随缘往",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N错步上前，一招「来去若梦行」，剑意若有若无，$w淡淡地向$n的$l挥去",
        "force"         : 120,
        "dodge"         : 10,
        "parry"         : 10,
        "damage"        : 20,
        "lvl"           : 10,
        "skill_name"    : "来去若梦行",
        "damage_type"   : "割伤"
]),
([      "action" : "$N一式「浮世沧桑远」，纵身飘开数尺，运发剑气，手中$w遥摇指向$n的$l",
        "force"         : 140,
        "dodge"         : 20,
        "parry"         : 20,
        "damage"        : 30,
        "lvl"           : 20,
        "skill_name"    : "浮世沧桑远",
        "damage_type"   : "割伤"
]),
([      "action" : "$N纵身轻轻跃起，一式「轮回法舟轻」，剑光如轮疾转，霍霍斩向$n的$l",
        "force"         : 160,
        "dodge"         : 30,
        "parry"         : 30,
        "damage"        : 40,
        "lvl"           : 30,
        "skill_name"    : "轮回法舟轻",
        "damage_type"   : "割伤"
]),
([      "action" : "$N拔步挺腰，一式「水月通禅寂」，手中$w无声无息地对准$n的$l刺出一剑",
        "force"         : 180,
        "dodge"         : 40,
        "parry"         : 40,
        "damage"        : 50,
        "lvl"           : 40,
        "skill_name"    : "水月通禅寂",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N手中$w斜指苍天，剑芒吞吐，一式「鱼龙听梵音」，对准$n的$l斜斜击出",
        "force"         : 200,
        "dodge"         : 50,
        "parry"         : 50,
        "damage"        : 60,
        "lvl"           : 50,
        "skill_name"    : "鱼龙听梵音",
        "damage_type"   : "割伤"
]),
([      "action" : "$N左指凌空虚点，右手$w逼出丈许雪亮剑芒，一式「千里一苇去」刺向$n的$l",
        "force"         : 220,
        "dodge"         : 60,
        "parry"         : 60,
        "damage"        : 80,
        "lvl"           : 60,
        "skill_name"    : "千里一苇去",
        "damage_type"   : "刺伤"
]),
([      "action" : "只见$N凭空一指，一招「千里一苇去」，手中$w挟著闪闪剑光刺向$n的$l",
        "force"         : 240,
        "dodge"         : 70,
        "parry"         : 70,
        "damage"        : 100,
        "lvl"           : 70,
        "skill_name"    : "千里一苇去",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N剑光暴长，一式「禅心顿自明」，手中$w狂风骤雨般地向$n的胸前刺去",
        "force"         : 260,
        "dodge"         : 80,
        "parry"         : 80,
        "damage"        : 120,
        "lvl"           : 80,
        "skill_name"    : "禅心顿自明",
        "damage_type"   : "刺伤"
]),
([      "action" : "$N合掌跌坐，一式「禅心顿自明」，$w自怀中跃出，如疾电般射向$n的胸口",
        "force"         : 280,
        "dodge"         : 90,
        "parry"         : 90,
        "damage"        : 140,
        "lvl"           : 100,
        "skill_name"    : "禅心顿自明",
        "damage_type"   : "刺伤"
]),
});

int valid_enable(string usage) { return (usage == "sword") || (usage == "parry"); }

int valid_learn(object me)
{
        object ob;
        int level;

        if ((int)me->query("max_force") < 200)
                return notify_fail("你的内力不够。\n");
        if( !(ob = me->query_temp("weapon"))
        ||      (string)ob->query("skill_type") != "sword" )
                return notify_fail("你必须先找一把剑才能练达摩剑法。\n");
        if( (int)me->query_skill("buddhism") < (int)me->query_skill("damo-sword") / 2 )
                return notify_fail("佛门每种绝技都要有慈悲佛法为之化解，你好象学得不够呀！\n");
        level= me->query_skill("damo-sword");
        if (level * level * level/10 > me->query("SEN") )
                return notify_fail("修练达摩剑法，要除暴安良，行侠仗义。你好象做得不够呀！\n");
        return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
        level   = (int) me->query_skill("damo-sword",1);
        for(i = sizeof(action); i > 0; i--)
                if(level >= action[i-1]["lvl"]) return action[random(i)];
}

int practice_skill(object me)
{
        object weapon;

        if (!objectp(weapon = me->query_temp("weapon"))
        || (string)weapon->query("skill_type") != "sword")
                return notify_fail("你使用的武器不对。\n");
        if ((int)me->query("kee") < 50)
                return notify_fail("你的体力不够练达摩剑法。\n");
        me->receive_damage("kee", 35);
        write("你按著所学练了一遍达摩剑法。\n");
        return 1;
}

string perform_action_file(string action)
{
        return __DIR__"damo-sword/" + action;
}

int enable_factor() { return 190; }

int learn_factor(int n) { 
   if (n<100) return 110;
   else return 90;
 }
int learn_bonus()  { return 1; }

int study_factor() { return 100; }
int study_bonus()  { return 1; }

int practice_factor() { return 100; }
int practice_bonus()  { return 1; }
