// yingzhua-gong.c - 鹰爪功

inherit SKILL;

mapping *action = ({
([	"action" : "$N全身拔地而起，半空中一个筋斗，一式「凶鹰袭兔」，迅猛地抓向$n的$l",
	"force"  	: 50,
        "dodge"  	: -10,
        "parry"  	: -10,
	"damage" 	: 5,
	"lvl"    	: 0,
	"skill_name"  	: "凶鹰袭兔",
    "damage_type"   : "瘀伤"
]),
([	"action" : "$N单腿直立，双臂平伸，一式「雄鹰展翅」，双爪一前一后拢向$n的$l",
	"force"  	: 80,
        "dodge"  	: -5,
        "parry"  	: -5,
	"damage" 	: 10,
	"lvl"    	: 10,
	"skill_name"  	: "雄鹰展翅",
    "damage_type"   : "瘀伤"
]),
([	"action" : "$N一式「拔翅横飞」，全身向斜里平飞，右腿一绷，双爪搭向$n的肩头",
	"force"  	: 100,
        "dodge"  	: 1,
        "parry"  	: 1,
	"damage" 	: 20,
	"lvl"    	: 20,
	"skill_name"  	: "拔翅横飞",
    "damage_type"   : "瘀伤"
]),
([	"action" : "$N双爪交错上举，使一式「迎风振翼」，一拔身，分别袭向$n左右腋空门",
	"force"  	: 120,
        "dodge"  	: 10,
        "parry"  	: 10,
	"damage" 	: 30,
	"lvl"    	: 30,
	"skill_name"  	: "迎风振翼",
    "damage_type"   : "瘀伤"
]),
([	"action" : "$N全身滚动上前，一式「飞龙献爪」，右爪突出，鬼魅般抓向$n的胸口",
	"force"  	: 140,
        "dodge"  	: 20,
        "parry"  	: 20,
	"damage" 	: 40,
	"lvl"    	: 40,
	"skill_name"  	: "飞龙献爪",
    "damage_type"   : "瘀伤"
]),
([	"action" : "$N伏地滑行，一式「拨云瞻日」，上手袭向膻中大穴，下手反抓$n的裆部",
	"force"  	: 160,
        "dodge"  	: 30,
        "parry"  	: 30,
	"damage" 	: 50,
	"lvl"    	: 50,
	"skill_name"  	: "拨云瞻日",
    "damage_type"   : "瘀伤"
]),
([	"action" : "$N左右手掌爪互逆，一式「搏击长空」，无数道劲气破空而出，迅疾无比地击向$n",
	"force"  	: 180,
        "dodge"  	: 40,
        "parry"  	: 40,
	"damage" 	: 60,
	"lvl"    	: 60,
	"skill_name"  	: "搏击长空",
    "damage_type"   : "瘀伤"
]),
([	"action" : "$N腾空高飞三丈，一式「鹰扬万里」，天空中顿时显出一个巨灵爪影，缓缓罩向$n",
	"force"  	: 200,
        "dodge"  	: 50,
        "parry"  	: 50,
	"damage" 	: 70,
	"lvl"    	: 70,
	"skill_name"  	: "鹰扬万里",
    "damage_type"   : "瘀伤"
]),
([	"action" : "$N腾空高飞三丈，一式「鹰扬万里」，天空中顿时显出一个巨灵爪影，缓缓罩向$n",
	"force"  	: 220,
        "dodge"  	: 60,
        "parry"  	: 60,
	"damage" 	: 80,
	"lvl"    	: 80,
	"skill_name"  	: "鹰扬万里",
    "damage_type"   : "瘀伤"
]),
([	"action" : "$N腾空高飞三丈，一式「鹰扬万里」，天空中顿时显出一个巨灵爪影，缓缓罩向$n",
	"force"  	: 240,
        "dodge"  	: 70,
        "parry"  	: 70,
	"damage" 	: 100,
	"lvl"    	: 100,
	"skill_name"  	: "鹰扬万里",
    "damage_type"   : "瘀伤"
]),
});

int valid_enable(string usage) { return (usage == "unarmed") || (usage == "parry"); }

int valid_learn(object me)
{
        int level;

	if ((int)me->query("max_force") < 100)
		return notify_fail("你的内力不够。\n");
	if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
	        return notify_fail("练鹰爪功必须空手。\n");
	if( (int)me->query_skill("buddhism") < (int)me->query_skill("yingzhua-gong") / 2 )
		return notify_fail("佛门每种绝技都要有慈悲佛法为之化解，你好象学得不够呀！\n");
        level= me->query_skill("yingzhua-gong");
        if (level * level * level / 10 > me->query("SEN") )
                return notify_fail("修练鹰爪功，要除暴安良，行侠仗义。你好象做得不够呀！\n");
	return 1;
}

string query_skill_name(int level)
{
        int i;
        for(i = sizeof(action)-1; i >= 0; i--)
                if(level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon)
{
        int i, level;
    	level   = (int) me->query_skill("yingzhua-gong",1);
        for(i = sizeof(action); i > 0; i--)
                if(level >= action[i-1]["lvl"]) return action[random(i)];
}

int practice_skill(object me)
{
	if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
        return notify_fail("练鹰爪功必须空手。\n");
	if ((int)me->query("kee") < 40)
        return notify_fail("你的体力不够练鹰爪功。\n");
	me->receive_damage("kee", 35);
        write("你按著所学练了一遍鹰爪功。\n");
	return 1;
}

string perform_action_file(string action)
{
    return __DIR__"yingzhua-gong/" + action;
}

int enable_factor() { return 100; }

int learn_factor(int n) { 
   if (n<100) return 120;
   else return 100;
 }
int learn_bonus()  { return 1; }

int study_factor() { return 100; }
int study_bonus()  { return 3; }

int practice_factor() { return 110; }
int practice_bonus()  { return 1; }
