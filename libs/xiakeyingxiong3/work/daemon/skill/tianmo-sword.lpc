// tianmo-sword.c - 天魔剑法

inherit SKILL;
/*
泼墨披麻剑法：白虹贯日、春风杨柳、腾蛟起凤、玉龙倒悬                            
*/
mapping *action = ({
  ([ "action": "只见$N使一招「风兮破地」，手中$w剑势如风，刺向$n的$l要害",
    "force": 100,
    "dodge": 10,
    "parry": 10,
    "damage": 10,
    "lvl": 0,
    "skill_name": "风兮破地",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N错步上前，一招「水兮滔天」，只见漫天剑影有如排山倒海般的向$n压去",
    "force": 120,
    "dodge": 10,
    "parry": 10,
    "damage": 20,
    "lvl": 10,
    "skill_name": "水兮滔天",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N一招「火兮焚野」，猛地运发剑气，手中$w如火龙般向$n卷噬而下",
    "force": 140,
    "dodge": 20,
    "parry": 20,
    "damage": 30,
    "lvl": 20,
    "skill_name": "火兮焚野",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N催动内力，一招「山兮鬼神惊」，$n只觉得全身被笼罩在如山剑影之中。",
    "force": 160,
    "dodge": 30,
    "parry": 30,
    "damage": 40,
    "lvl": 30,
    "skill_name": "山兮鬼神惊",
    "damage_type": "割伤"
  ]),
  ([ "action": "只见$N仰天长啸，一招「雷兮天地碎」，$w幻出无数剑影如雷电般向$n击下",
    "force": 180,
    "dodge": 40,
    "parry": 40,
    "damage": 50,
    "lvl": 40,
    "skill_name": "雷兮天地碎",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N手中$w现出剑芒万丈，一招「天罗魔罟」，漫天交缠着的剑芒将$n重重围住",
    "force": 200,
    "dodge": 50,
    "parry": 50,
    "damage": 60,
    "lvl": 50,
    "skill_name": "天罗魔罟",
    "damage_type": "割伤"
  ]),
  ([ "action": "$N一跃而起，一招「魔动九天」，$w发出耀眼光茫，剑身幻成万道金光罩向$n的全身",
    "force": 220,
    "dodge": 60,
    "parry": 60,
    "damage": 80,
    "lvl": 60,
    "skill_name": "魔动九天",
    "damage_type": "刺伤"
  ]),
  ([ "action": "$N腾空而起，一招「人剑合一」，人与剑彼此连成一体，化作一道光芒射向$n",
    "force": 240,
    "dodge": 70,
    "parry": 70,
    "damage": 100,
    "lvl": 70,
    "skill_name": "人剑合一",
    "damage_type": "刺伤"
  ]),
  ([ "action": "只见$N剑光暴长，一式「千秋万载」，$w发出耀眼光芒，剑锋直出，绵绵不断",
    "force": 260,
    "dodge": 80,
    "parry": 80,
    "damage": 120,
    "lvl": 80,
    "skill_name": "千秋万载",
    "damage_type": "刺伤"
  ]),
  ([ "action": "只见$N手中$w斜指苍天，剑芒吞吐，一招「一统江湖」，狂风骤起，笼罩天地",
    "force": 280,
    "dodge": 90,
    "parry": 90,
    "damage": 140,
    "lvl": 100,
    "skill_name": "一统江湖",
    "damage_type": "刺伤"
  ]),
});

int valid_combine(string combo) { return combo == "tianmo-blade"; }

int valid_enable(string usage) { return (usage == "sword") || (usage == "parry"); }

int valid_learn(object me) {
  object ob;
  int level;

  if ((int)me->query("max_force") < 200)
    return notify_fail("你的内力不够。\n");
  if (!(ob = me->query_temp("weapon"))
    || (string)ob->query("skill_type") != "sword")
    return notify_fail("你必须先找一把剑才能练天魔剑法。\n");
  level = me->query_skill("tianmo-sword");
  if (-level * level * level / 10 < me->query("SEN"))
    return notify_fail("修练天魔剑法，要心狠手辣，奸恶歹毒。你好象做得不够呀！\n");
  return 1;
}

string query_skill_name(int level) {
  int i;
  for (i = sizeof(action) - 1; i >= 0; i--)
    if (level >= action[i]["lvl"]) return action[i]["skill_name"];
}

mapping query_action(object me, object weapon) {
  int i, level;
  level = (int)me->query_skill("tianmo-sword", 1);
  for (i = sizeof(action); i > 0; i--)
    if (level >= action[i - 1]["lvl"]) return action[random(i)];
}

int practice_skill(object me) {
  object weapon;

  if (!objectp(weapon = me->query_temp("weapon"))
    || (string)weapon->query("skill_type") != "sword")
    return notify_fail("你使用的武器不对。\n");
  if ((int)me->query("kee") < 50)
    return notify_fail("你的体力不够练天魔剑法。\n");
  me->receive_damage("kee", 35);
  write("你按著所学练了一遍天魔剑法。\n");
  return 1;
}

string perform_action_file(string action) {
  return __DIR__ "tianmo-sword/" + action;
}

int enable_factor() { return 100; }

int learn_factor(int n) {
  if (n < 100) return 110;
  else return 90;
}
int learn_bonus() { return 1; }

int study_factor() { return 100; }
int study_bonus() { return 1; }

int practice_factor() { return 100; }
int practice_bonus() { return 1; }
