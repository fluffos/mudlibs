// luoying-force.c - 落英心法

inherit FORCE;

int valid_enable(string usage) { return usage=="force"; }

int valid_learn(object me) 
{ 
	if( (int)me->query_skill("force") < 10 )
        	return notify_fail("你的基本内功火候不够，无法研习高深的内功。\n");
	return 1; 
}

int practice_skill(object me)
{
        return notify_fail("落英心法只能学习（learn），或是从运用（exert）中增加熟练度。\n");
}

string exert_function_file(string func)
{
        return __DIR__"luoying-force/" + func;
}
 
int enable_factor() { return 100; }

int learn_factor(int n) { 
   if (n<100) return 120;
   else return 100;
 }
int learn_bonus()  { return 1; }

int study_factor() { return 110; }
int study_bonus()  { return 2; }

