Simulated External Functions

string center(string, integer);

Returns the input string centered for a screen of width integer.  If a line of the input string is beyond the width of the screen then text is first wrapped, then centered.

The input string can have multiple newlines within it and what is returned is a string with multiple newlines in it, each line centered.

file: /doc/sefun/strings.c
