Simulated External Functions

string right_align(string str, int size);

This function takes the string str and puts spaces infront of it until the string length is equal to size.  If no size is specified or str happens to be longer then size then the string str is just returned as is.

file: /doc/sefun/strings.c
