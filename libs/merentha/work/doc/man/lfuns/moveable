Local Functions (/std/inheritables/object.c)

string moveable();

Returns the string which set_prevent_get() set.  If that value was 0 or if
that value was never sent 0 is returned.

See also: set_prevent_get()
