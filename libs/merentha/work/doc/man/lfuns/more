Local Functions (/std/inheritables/user.c)

void more(mixed input, int lines, int start, object return_object,
          string return_function, string topic, int x);

Takes the input and parses it through 'more'.  The input can be either a large string, or array of strings.
