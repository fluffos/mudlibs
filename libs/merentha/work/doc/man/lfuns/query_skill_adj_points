Local Functions (/std/inheritables/living.c)

int query_skill_adj_points(string skill);

Returns the number of skill adjustment points needed to lower a skill adjustment to the next level.

See also: set_skills(), set_skill(), query_skills(), query_skill_level(), query_skill_max(), query_base_skill(), set_skill_bonus(), add_skill_bonus(), query_skill_bonus(), add_skill_points()
