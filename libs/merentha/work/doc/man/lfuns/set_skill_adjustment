Local Functions (/std/inheritables/living.c)

void set_skill_adjustment(string skill int val);

Sets the adjustment of a living's skill.  Does not effect the adjustment given for classes, races or gender.

See also: use_skill(), set_skill_adjustment(), set_skills(), set_skill(), query_skills(), query_skill_level(), query_skill_adjustment(), query_base_skill(), set_skill_bonus(), add_skill_bonus(), query_skill_bonus(), add_skill_points()
