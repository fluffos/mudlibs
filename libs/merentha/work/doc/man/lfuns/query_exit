Local Functions (/std/inheritables/room.c)

string query_exit(string dir);

Returns the filename of the room an exit leads to.

See also: set_exits(), remove_exit(), add_exit(), query_exits(), set_exit_invis(), set_exit_vis()

