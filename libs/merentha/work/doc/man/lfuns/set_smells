Local Functions (/std/inheritables/room.c)

void set_smells(mapping map);

Sets the smells in a room.  When the key is a "default" the smell will appear in the room description.

See also: query_smells(), query_smell(), add_smell(), set_smell(), remove_smell(), set_sounds()
