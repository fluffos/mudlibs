Local Functions (/std/inheritables/room.c)

void set_sounds(mapping map);

Sets the sounds in a room.  When the key is a "default" the sound will appear in the room description.

See also: query_sounds(), query_sound(), add_sound(), set_sound(), remove_sound(), set_smells()
