The shell is part of the MUD which interprets your commands and sends them to the game.  For example the shell if the part of the game which takes your aliases and nicknames and makes them work.

The shell also allows you to use the ! operator to repeat previously typed commands ctill in your history.

See also: alias, nickname, history
