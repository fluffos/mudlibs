#include <lib.h>
inherit LIB_ROOM;

nosave void create() {
    room::create();
    SetShort("the freezer");
    SetLong("You are in the freezer.");
    SetExits( ([ "out" : "/domains/Standard/center" ]) );
}
