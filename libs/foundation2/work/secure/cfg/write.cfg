# /secure/cfg/write.cfg
# from the Foundation II LPC Library
# the write access configuration file
#
# format:
# (directory) GROUP:GROUP:creator

(/ftp/) all
(/log/errors/) all
(/log/reports/) all
(/tmp/) all
(/save/) MUDLIBPRIV:ASSIST
