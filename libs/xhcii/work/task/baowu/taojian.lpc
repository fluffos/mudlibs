// topkan@xhc
inherit ITEM;
#include "task.h"
#include <ansi.h>
void create()
{
        set_name(HIW "木桃剑", ({"mutao jian", "jian"}));
        set_weight(80);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("long", "桃剑。\n");
                set("unit", "把");
               set("owner_name",HIC "何足道");
               set("owner_id","he zudao");
      set("task_object", 1);
        }
}
