// topkan@xhc
inherit ITEM;
#include "task.h"
#include <ansi.h>
void create()
{
        set_name(GRN "烂鱼网", ({"lan yuwang", "wang"}));
        set_weight(80);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("long", "别看网烂，却可以捉到鱼。\n");
                set("unit", "条");
               set("owner_name",HIB "虚竹");
                set("owner_id","xu zhu");
      set("task_object", 1);
        }
}

