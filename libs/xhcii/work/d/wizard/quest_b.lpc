// /clone/board/wiz_b.c
#include <ansi.h>
inherit BULLETIN_BOARD;

void create() {
  set_name(HIC "任务问题留言簿" NOR, ({ "board" }));
  set("location", "/d/wizard/quest_room");
  set("board_id", "quest_b");
  set("long", "这是一个专供巫师使用的留言板。\n");
  setup();
  set("capacity", 30);
}
