// Room: /open/tendo/room/l1.1.c

inherit ROOM;

void create() {
  set("short", "浮桥");
  set("long", @LONG
	往下看是一片迷蒙的云雾，但这座桥竟然不需要桥墩的支撑
	只靠两端的衔接还如此的平稳，实在太不可思议了。


LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "l1.2",
    "south": __DIR__ "door",
  ]));

  setup();
}
