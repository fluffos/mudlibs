#include <command.h>
#include <globals.h>
#include <dbase.h>
inherit ROOM;

void create() {
  set("short", "Spirits 的工作室");
  set("long", @LONG
这里是 Spirits 的工作室，北边一个巨大的落地窗，望出去是一望无
际的星空，围绕着地板中央的巨大虎皮地毯，十二个纯金铸成的灯架上各
浮着一颗光球，正发出着温和的光芒，地毯中央有一张古藤摇椅和一个造
型十分现代感的支架，支架上放着一台银灰色的电脑。
LONG
  );
  set("exits", ([
    "start": "/open/wiz/entrance",
    "fighter": "/d/class/fighter/guildhall",
    "temple": "/d/temple/temple1",
  ]));
  set("valid_startroom", 1);
  set("objects", ([
    //             "/u/s/spirits/class/master.lpc" : 1,
  ]));
  setup();
}
