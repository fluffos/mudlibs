
这个程式目的是方便、大量地造出空的房间，
其实不是很适合 LP-mud...不过还是可以参考一下...

Make:
lex mapep.l
cc -o mapep lex.yy.c

Usage:
mapep <ascii.map> <files_name> <path> 
mapep <ascii.map> <files_name> <path> > <out.dat>

ex:
mapep sample.map sample SAMPATH
mapep sample.map sample SAMPATH > out.dat

