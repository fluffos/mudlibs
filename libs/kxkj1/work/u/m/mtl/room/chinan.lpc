#define _DIR_ "/u/m/mtl"
inherit ROOM;

void create() {
  set("short", "钦安殿");
  set("long", @LONG

    钦安殿是王公大臣等候晋见皇上的地方。

LONG);

  set("exits", ([ /* sizeof() == 4 */
    "south": _DIR_ "/room/senwuman.lpc",
    "north": _DIR_ "/room/garden.lpc",
  ]));
  set("light_up", 1);

  setup();
}
