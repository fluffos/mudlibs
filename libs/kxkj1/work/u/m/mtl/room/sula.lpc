#define _DIR_ "/u/m/mtl"
inherit ROOM;

void create() {
  set("short", "苏拉住处");
  set("long", @LONG

    苏拉是宫里从事卑贱工作的奴役。而这里是他们的住处。

LONG);

  set("exits", ([ /* sizeof() == 4 */
    "east": _DIR_ "/room/senwuman.lpc",
  ]));
  set("light_up", 1);

  setup();
}
