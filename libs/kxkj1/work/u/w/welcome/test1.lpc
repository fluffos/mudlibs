// Room: /u/w/welcome/test1
inherit ROOM;

void create() {
  set("short", "发呆处");
  set("long", @LONG
这里是随意而行工作累了的休息房间。放置简单,欢迎大家一起来喔....
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "work": __DIR__ "workroom",
  ]));

  setup();
}
