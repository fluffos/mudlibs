// Room: /u/t/tiny/kitchen.c
inherit ROOM;

void create() {
  set("short", "含与不死鸟的家--厨房");
  set("long", @LONG
含为了慰劳不死鸟的辛劳,特地从京城延聘最有名的厨师--小刀斋
而你看到的尽是一个不起眼的家伙,不禁怀疑他的手艺
而他忙进忙出,笨手笨脚的感觉,更让你不禁觉得好笑....
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "mid.lpc",
  ]));
  set("light_up", 1);
  set("no_fight", 1);
  set("no_magic", 1);

  setup();
}
