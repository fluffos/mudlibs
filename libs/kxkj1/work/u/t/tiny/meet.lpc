// Room: /u/t/tiny/meet.c
inherit ROOM;

void create() {
  set("short", "含与不死鸟的家--会客室");
  set("long", @LONG
在不死鸟的巧心布置下,让你一进到这里,就感觉无比的舒畅
而这里也是含与不死鸟合众为巫师共商大计的房间
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "mid.lpc",
  ]));
  set("light_up", 1);
  set("no_fight", 1);
  set("no_magic", 1);

  setup();
}
