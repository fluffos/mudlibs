// Room: /u/u/urd/ikkoku/enter.c
inherit ROOM;

void create() {
  set("short", "门口");
  set(
    "long",
    @LONG
这里就是一刻馆的门口，在不远的前方有一栋蛮旧的公寓，就是充满欢乐气息的一刻馆。公寓上头是瓦砾砌成的屋顶，正中央还放置了一个圆形的钟，门的两旁各有一根石柱，石柱的上头写着一刻馆几个大字；另外还有一个邮筒。.
LONG
  );

  set("item_desc", ([ /* sizeof() == 1 */
    "door": "这是一个门，
你可以推(push door)看看。
",
  ]));
  set("light_up", 100);
  set("outdoors", "/u/u");
  set("exits", ([ /* sizeof() == 1 */
    "down": "/u/u/urd/workroom.lpc",
  ]));

  setup();
}
