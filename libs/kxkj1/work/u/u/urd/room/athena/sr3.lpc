// Room: /u/c/carol/room/athena/sr3.c
inherit ROOM;

void create() {
  set("short", "圣殿之东--白银圣廊");
  set("long", @LONG
往南已经没路了, 不过你看到了一座巨大的白银像就挻立在这里, 你感觉到它似
乎是在守护着某样东西, 你猜想那一定是非常的重要的东西, 从白银像所散放出
来的光芒让你感到十分的舒适, 再也不会疲累了。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "sr2.lpc",
  ]));
  set("light_up", 1);

  setup();
}
