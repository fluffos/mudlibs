// Room: /u/o/oklatalk/h1.c
inherit ROOM;

void create() {
  set("short", "前院");
  set("long", @LONG

	  进得门来，赫然发现门内完全不同于门外的平凡。简单而
	尊贵典雅的花树布置，令你心情为之一畅。看来，传闻中的
	天下第一美女落雁阁女主人不仅人美，而且心细手巧。

	在你的东边是厢房，乃是主人朋友的住宿之处。
	在你的西边是花厅，乃是主人的宴客之处。

LONG);

  set("light_up", 1);
  set("outdoors", "/open/common");
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "r3",
    "north": __DIR__ "h2",
    "out": __DIR__ "entrance",
    "east": __DIR__ "r2",
  ]));

  setup();
}
