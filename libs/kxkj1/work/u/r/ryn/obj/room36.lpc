// room36.c by ryn

inherit ROOM;

void create() {
  set("short", "魔森林");
  set("long", @LONG
    这里是魔森林的内部,你走到这里,顿感妖气凝重,瞬时间感到气
血翻腾,只希望赶快走过这令人不安的地带。

LONG);

  set("light_up", -1);
  set("exits", ([ /* sizeof() == 3 */
    "north": __DIR__ "room35",
    "south": __DIR__ "room37",
  ]));

  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "mob": 1,
  ]));

  set("no_transmit", 1);
  set("no_auc", 1);
  setup();
}
