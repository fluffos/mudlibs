inherit ROOM;

void create() {
  set("short", "河边的小木屋");
  set("light_up", 1);
  set("long", @LONG
    这里是罗格纳退隐后, 跟他妻子安享余生的地方, 你可看到屋前的一弯小
    溪, 清澈的溪水正发出淅哩哗啦的流水声......
LONG);

  set("no_spells", 1);
  set("exits", ([ /* sizeof() == 6 */
    "enter": __DIR__ "room1.lpc",
  ]));
  set("no_magic", 1);
  set("valid_startroom", 1);

  setup();
}
