// Room: /u/r/ranko/tendo2.c
inherit ROOM;

void create() {
  set("short", "道场");
  set("long", @LONG
这是用来练习武功用的地方.
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "west": "/u/r/ranko/workroom.lpc",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "bonze.lpc": 1,
  ]));

  setup();
}
