#include <ansi.h>
inherit NPC;
void create() {
  set_name("test", ({ "test" }));
  set("long", @LONG

             test 中

LONG);
  set("gender", "雄性");
  set("race", "野兽");
  set("age", 5);

  set_skill("move", 65);
  setup();
}
int accept_fight(object who) {
  command("go south");
}
int accept_kill(object who) {
  command("go south");
}
