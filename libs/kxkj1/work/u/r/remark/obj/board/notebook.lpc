inherit BULLETIN_BOARD;
#include <ansi.h>
void create() {
  set_name(HIM "签名簿" NOR, ({ "note book", "book" }));
  set("board_id", "diary");
  set("long", HIG "记录亚里亚罕城各路英雄的名号。\n" NOR);
  setup();
  set("capacity", 35);
  set("master", ({ "remark" }));
}
