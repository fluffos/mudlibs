#include <room.h>
#include "/open/open.h"
#include <ansi.h>
inherit "/std/room/shop";

void create() {
  set("short", "[1;35m亚里亚罕商店[2;37;0m");
  set("long", @LONG
        
        这里是亚里亚罕城唯一的商店,在这你可以估价(value),
        卖断(sell),交易买货(trade),并可以察看这里有什么
        货品待售(list).

LONG);

  set("exits", ([ /* sizeof() == 1 */
    "north": "/u/r/remark/room/street/r4.lpc",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/u/r/remark/npc/human/trademan.lpc": 1,
  ]));
  set("light_up", 1);

  setup();
}
