// Room: /u/p/pyramid/yuroom3.c

inherit ROOM;

void create() {
  set("short", "[1;35m柔云小筑[2;37;0m");
  set("long", @LONG

                   ＊　柔   云   小   筑　＊

     暗黄色的烛焰在房中闪烁着, 你隐约闻到了一阵黑檀木檀香的熟悉味道.  
 粉色的绣花帐子罩在床铺上, 这里便是小鱼儿的歇息处了.   桌上的花瓶插着
 刚刚才摘下来的白玫瑰花, 花瓣上面还有未干的露水.
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "yuroom1",
  ]));
  set("item_desc", ([ /* sizeof() == 1 */
    "note": "一张典雅的小竹笺, 上面写着:天界动工中, 欢迎大家参观指教..:>l
",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "obj/rosetea.lpc": 2,
  ]));
  set("no_fight", 1);
  set("light_up", 1);
  set("no_clean_up", 0);
  set("no_magic", 1);
  set("valid_startroom", 1);

  setup();
}
