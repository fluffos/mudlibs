// Room: /u/p/pyramid/sky/room/dm9.c

inherit ROOM;

void create() {
  set("short", "[36m幻力森林[2;37;0m");
  set("long", @LONG
你来到了幻力森林的中心, 中间的大树(tree)像是包着什么东西, 
你不由得好奇的走过去看看. 
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "dm10",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/u/p/pyramid/sky/npc/xious.lpc": 1,
  ]));
  set("no_clean_up", 0);

  setup();
}
