// Room: /u/p/pyramid/sky/room/dm7.c

inherit ROOM;

void create() {
  set("short", "[36m荒芜寺院[2;37;0m");
  set("long", @LONG
到处都是残砖败瓦, 杂草丛生,   房舍一副随时会垮下来的样子.
这里是占星人．九曜从前的修行处, 现在则成了她逃难的临时居
所.
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "dm8",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/u/p/pyramid/sky/npc/joey.lpc": 1,
  ]));
  set("no_clean_up", 0);

  setup();
}
