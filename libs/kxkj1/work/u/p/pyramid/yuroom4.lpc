// Room: /u/p/pyramid/yuroom4.c

inherit ROOM;

void create() {
  set("short", "[35m锦云阁[2;37;0m");
  set("long", @LONG

                   ＊　锦      云      阁　＊

     这个小楼是小鱼儿收藏她的物品的地方, 四面的玻璃柜子各有不同的
 样式和花纹, 其中收藏的一些珍玩更是令人爱不释手.   正中一张小桌子,
 摆着一套茶具, 主人有时就在这里边品茗边欣赏她的收藏品.
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "yuroom1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "obj/shelf.lpc": 1,
  ]));
  set("light_up", 1);
  set("no_fight", 1);
  set("no_magic", 1);
  set("no_clean_up", 0);
  set("valid_startroom", 1);

  setup();
}
