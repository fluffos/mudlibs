inherit ROOM;

void create() {
  set("short", "水月阁");
  set("long", @LONG

这是一间什么都没有的房间。
 
LONG);

  set("light_up", 1);
  set("exits", ([ /* sizeof() == 1 */
    "down": __DIR__ "prmw5.lpc",
  ]));

  setup();
}
