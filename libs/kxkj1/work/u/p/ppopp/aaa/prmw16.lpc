inherit ROOM;

void create() {
  set("short", "镜花园");
  set("long", @LONG

这是一间什么都没有的房间。
 
LONG);

  set("light_up", 1);
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "prmw14",
  ]));

  setup();
}
