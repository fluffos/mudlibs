// Room: /u/q/quit/restroom
inherit ROOM;

void create() {
  set("short", "  卧房");
  set("long", @LONG
  这里是风云专用的起居室,前面有一张双人床(bed),床的旁边有盏落地灯,这里的布置
实在让人想躺下去睡觉。
LONG);

  set("object", ([ /* sizeof() == 2 */
    __DIR__ "npc/guard": 1,
    __DIR__ "npc/cat": 1,
  ]));
  set("item_desc", ([ /* sizeof() == 1 */
    "bed": "一张软绵绵的床,似乎可以移动。",
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "freeroom.lpc",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "npc/guard": 1,
    __DIR__ "npc/cat": 1,
  ]));

  setup();
}
