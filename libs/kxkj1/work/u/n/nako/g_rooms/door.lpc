// Room: /u/n/nako/door.c
inherit ROOM;

void create() {
  set("short", "怡红院大门");
  set("long", @LONG
这儿便是远近驰名的怡红院了, 只见大门朱漆镶金, 门上狮头竟是纯金所铸而成,
从门中望去, 一条青石铺道笔直前去, 通往远处一栋楼阁, 想来是怡红院主要之所在,
铺道两旁遍植草木, 绿意盎然, 庭园美景自是不在话下。想不到怡红院一处风月欢场,
竟也能有如是美景！
LONG);

  set("exits", ([ /* sizeof() == 2 */
    "south": "/u/n/nako/studyroom.lpc",
    "enter": __DIR__ "way1.lpc",
  ]));
  set("outdoors", "/u/n");

  setup();
}
