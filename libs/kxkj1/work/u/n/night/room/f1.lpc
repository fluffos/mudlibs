// Room: /open/ping/room/m1.c

inherit ROOM;

void create() {
  set("short", "风行山寨外");
  set("long", @LONG

这里就是风行山顶了,而风行山寨就建在这儿,山寨内好像有很多人在那走来走去的,如果你
想多活几年,现在下山去还来的及喔. 门口有两名守卫站着,看的出来,不是随便什么人都可
以进去的.


LONG);

  set("objects", ([ /* sizeof() == 2 */
  ]));

  set("outdoors", "/open/ping");

  set("exits", ([ /* sizeof() == 3 */
  ]));

  setup();
}
