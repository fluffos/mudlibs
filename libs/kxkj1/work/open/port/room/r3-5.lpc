// Room: /open/gblade/port/room/p1-1.c
inherit ROOM;

void create() {
  set("short", "学堂");
  set("long", @LONG
这里是枫林港一家颇具规模的学堂 . 这家学堂是公家的 , 
为大财主史怀恩所捐献 . 许多枫林的居民都曾在此接受过基本
教育 .  

LONG);

  set("light_up", 1);
  set("exits", ([ /* sizeof() == 2 */
    "south": "/open/port/room/r3-3",
  ]));
  set("objects", ([
  ]));

  setup();
}
