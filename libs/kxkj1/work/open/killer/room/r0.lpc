#include <room.h>
inherit ROOM;
void create() {
  set("short", "禁地");
  set("long", @LONG
  这是旧玩家照顾新玩家的地方!


LONG);
  set("light_up", 10);
  set("exits", ([
    "east": "/open/killer/room/blackwiz",
  ]));
  set("objects", ([
    "/open/killer/npc/boy.lpc": 1,
  ]));
  setup();
}
