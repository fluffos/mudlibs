inherit ROOM;

void create() {
  set("short", "山洞入口");
  set("long", @LONG
这是一个小山洞的入口,这里有微弱的光线照进来,
因此可以看到有两条不同方向的路,前途一片黑暗,
不知是否有怪物出没,皆为未知数。

LONG);
  set("light_up", 1);
  set("exits", ([
    "out": __DIR__ "l1.lpc",
    "north": __DIR__ "l3.lpc",
    "south": __DIR__ "l4.lpc", ]));
  setup();
}
