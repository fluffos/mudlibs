#include <ansi.h>
#include "/open/open.h"
inherit NPC;
void create() {
  set_name("访客", ({ "visitor" }));
  set("gender", "男性");
  set("combat_exp", 612);
  set("age", 26);
  set("str", 44);
  set("cor", 24);
  set("cps", 18);
  set("per", 24);
  set("int", 42);
  setup();
  carry_object("/open/poison/obj/card");
}
