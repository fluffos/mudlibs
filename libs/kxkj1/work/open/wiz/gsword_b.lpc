inherit BULLETIN_BOARD;

void create() {
  set_name("仙剑派留言版", ({ "gsword board", "board" }));
  set("location", "/open/wiz/gswordroom");
  set("board_id", "gsword");
  set("long", "仙剑派留言版。\n");
  setup();
  set("capacity", 30);
}
