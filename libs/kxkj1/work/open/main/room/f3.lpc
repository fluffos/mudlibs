// Room: /open/main/room/F3.c

inherit ROOM;

void create() {
  set("short", "密林");
  set("long", @LONG
这里一望无际的密林，四周伸手只见四指, 往东看过去, 只见狮鹿
依稀散落在大片草原上
LONG);

  set("exits", ([ /* sizeof() == 3 */
    "south": __DIR__ "F6",
    "west": __DIR__ "F2",
    "east": __DIR__ "s1",
  ]));

  set("objects", ([ /* sizeof() == 1 */
    "/open/main/npc/bloodbat": 1,
    "/open/main/obj/torch": 1,
  ]));

  setup();
}
