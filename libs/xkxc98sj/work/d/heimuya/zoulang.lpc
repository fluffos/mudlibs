// /u/dubei/zoulang.c

inherit ROOM;

void create()
{
	set("short", "走廊");
	set("long", @LONG
这条走廊通往成德殿。只见每根枋梁都绘有精美的彩画，风景、人物、
山水、花鸟，应有尽有。走廊上站满了武士，手中各挺一把明晃晃的长刀，
交叉上举，须弓腰低头放可过去。
LONG
	);
     
          set("exits", ([
		 "east" : __DIR__"cdian",
		 "west" :__DIR__"houting",
	]));

        set("objects", ([
//                __DIR__"npc/ " : 1,
        ]));
	setup();
	replace_program(ROOM);
}

