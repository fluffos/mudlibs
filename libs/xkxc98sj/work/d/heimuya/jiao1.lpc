// /u/dubei/jiao1.c

inherit ROOM;

void create() {
  set("short", "绞索一");
  set("long", @LONG
只见地下放着一只大竹篓，足可装得下十来石米。抬头只见头顶有数点
火星。原来黑木崖高得厉害，需用上面的绞索绞盘将人绞了上去。
LONG
  );
  set("outdoors", "黑木崖");

  set("exits", ([
    "southup": __DIR__ "jiao2",
    "northdown": __DIR__ "songmen",
  ]));

  set("objects", ([
    //                __DIR__"npc/ " : 1,
  ]));
  setup();
  replace_program(ROOM);
}
