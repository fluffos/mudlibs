// /u/dubei/caoan.c

inherit ROOM;

void create()
{
	set("short", "草庵");
	set("long", @LONG
只见道旁搭了一间小草庵，你刚走近，从屋中走出四人，都是身穿紫袍。
伸手拦住了你。
LONG
	);
        set("outdoors", "黑木崖");

	set("exits", ([
		 "southwest" : __DIR__"dadao",
		 "northdown" :__DIR__"pailou",
	]));

        set("objects", ([
//                __DIR__"npc/ " : 1,
        ]));
	setup();
	replace_program(ROOM);
}

