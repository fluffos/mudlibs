// /d/mingjiao/zhoulong2.c
// Jay 4/3/97 -qingyun
#include <room.h>
inherit ROOM;
void create()
{ 
       set("short","走廊");
       set("long", @LONG
这是一条走廊。一边是青砖墙壁，一边是古木雕成的围栏虽是普通过道，
却也隐射出江湖第一大教制端严。西面是练武场，东边是广场。
LONG);    
	set("exits", ([
          "west" : __DIR__"guangchang",
  	  "east" : __DIR__"wu4",
	]));    
        set("outdoors", "明教光明顶");         
        setup();
        replace_program(ROOM);
}

