#include <room.h>
inherit ROOM;
void create()
{ 
       set("short","小院子");
       set("long", @LONG
这是一所小院子。你一进来这里这闻到一股强烈的花香，只见院
子培植长着许多名贵的花草。西面则是一座厢房。
LONG);    
	set("exits", ([
		"southeast" : __DIR__"longwang",				
		"west" : __DIR__"wxiang",
	]));             
        set("objects",([
		__DIR__"npc/yahuan" : 1,
                __DIR__"npc/xiao-tong" : 1,
	]));   
        set("outdoors", "明教光明顶");
        setup();
        replace_program(ROOM);
}

