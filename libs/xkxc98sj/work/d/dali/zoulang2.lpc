// Room: /d/dali/zoulang2.c
// bbb 1997/06/10 
// Updated by jpei

inherit ROOM;

void create()
{
	set("short", "走廊");
	set("long", @LONG
这里是位于天龙寺后面的走廊，和一路行来金碧辉煌的厅完全不同。旁边
有几间屋，东边通向天龙寺的兵器房，西边是弟子休息室。
LONG
	);
        set("outdoors", "大理");

	set("exits", ([
		"south" : __DIR__"qianxun",
		"north" : __DIR__"zoulang1",
		"east" : __DIR__"tlbq",
		"west" : __DIR__"tlxx",
	]));
        set("objects", ([
		__DIR__"npc/shami" : 1,
        ]));

	setup();
	replace_program(ROOM);
}
