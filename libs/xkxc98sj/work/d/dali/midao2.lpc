inherit ROOM;

void create()
{
	set("short", "密道");
	set("long", @LONG
这里是密道，黑乎乎的，你只能摸索着向前走，很费劲的钻过
一个个的密道。
LONG
	);
        set("outdoors", "dali");

	set("exits", ([
		"enter" : __DIR__"midao3",
        "out" : __DIR__"midao1",
	]));

	setup();
	replace_program(ROOM);
}
