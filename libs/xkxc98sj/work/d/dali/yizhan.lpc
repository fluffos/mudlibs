inherit ROOM;

void create() {
  set("short", "驿站");
  set("long", @LONG
这是大理驿站，负责传送和大宋国及武林各大门派的的公文，密函。
这里戒备森严，静悄悄的，不时有人进来领命而去，门口边立了一快牌
子（sign)。
LONG
  );
  set("no_fight", 1);
  set("no_sleep_room", 1);

  set("item_desc", ([
    "sign": "请用\"ask wanli about 送信 \" 领取任务。\n",
  ]));

  set("exits", ([
    "east": __DIR__ "beijie4",
  ]));
  set("objects", ([
    __DIR__ "npc/zhuwanli": 1,
    __DIR__ "npc/bing": 2,
  ]));


  setup();
}
