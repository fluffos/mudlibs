// Room: 北大街/dali/beijie2.c

inherit ROOM;

void create()
{
    set("short", "大理北大街");
	set("long", @LONG
这是大理北大街，大理近百年来从无战事，一片太平盛世的模样。
西面不断有人骑马急驰而出，那里是一个驿站，东面建筑高大，是官府
的盐局，再往南就到大理城中心了。
LONG
	);
    set("exits", ([ /* sizeof() == 2 */
	    "south" : __DIR__"dalics",
	    "north" : __DIR__"beijie3",
	     "east" : __DIR__"yanju",
	     "west" : __DIR__"yizhan",
]));
   set("objects", ([
	      "/d/city/npc/yanxiao" : 1,
             "/d/city/npc/yanxiao2" : 1,
	]));

    set("outdoors", "大理");

	setup();
	replace_program(ROOM);
}
