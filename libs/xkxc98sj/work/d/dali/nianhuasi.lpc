inherit ROOM;

void create()
{
set("short", "拈花寺");
	set("long", @LONG
你举步入寺，只听得叮叮两声清磬，悠悠从后院传出，霎时之间，只感遍体清凉，
意静神闲。
LONG
	);
       set("outdoors", "dali");
	set("exits", ([ /* sizeof() == 1 */
	    "out" : __DIR__"nianhuasimen",
	    "north" : __DIR__"yuan1",
           ]));
       
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
