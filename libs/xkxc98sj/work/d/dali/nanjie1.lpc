inherit ROOM;

void create()
{
    set("short", "大理南大街");
	set("long", @LONG
你走在大理南大街上，这里安静极了，大理百姓走到这里，无不放慢了
脚步，以敬仰的目光向东看去，东边过去就是大理过皇宫了。西边是大理
御林军的兵营。
LONG
	);
    set("objects", ([
                __DIR__"npc/huasitu" : 1,
    ]));

	set("exits", ([ /* sizeof() == 2 */
	    "south" : __DIR__"nanmen",
	    "north" : __DIR__"nanjie4",
	     "east" : __DIR__"paifang",
	     "west" : __DIR__"bingying",
]));
    set("outdoors", "大理");

	setup();
	replace_program(ROOM);
}
