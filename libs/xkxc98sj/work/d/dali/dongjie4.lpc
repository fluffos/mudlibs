inherit ROOM;

void create() {
  set("short", "东大街");
  set("long", @LONG
这是大理东大街，城中一些王侯富户都集中的住在这里，这里安静极了，
大理百姓走到这里，无不放慢了脚北面南面是善阐侯高侯爷的府邸。再往东走，
就到东城门了。
LONG
  );
  set("outdoors", "dali");
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "dongmen",
    "west": __DIR__ "dongjie1",
    "north": __DIR__ "shanhou",
  ]));

  //         set("objects", ([
  //                        "/d/city/npc/jiading" : 2 
  //]) );
  setup();
  replace_program(ROOM);
}
