// Room: 北大街/dali/beijie3.c

inherit ROOM;

void create()
{
    set("short", "大理北大街");
	set("long", @LONG
这是大理北大街，大理近百年来从无战事，一片太平盛世的模样。停留
这里的人们不少是文人墨客，东面就是一家书店。往西进出的好象都是有钱
财主，再往南就到大理城中心了。
LONG
	);
    set("exits", ([ /* sizeof() == 2 */
	    "south" : __DIR__"beijie4",
        "north" : __DIR__"beijie2",
	     "east" : __DIR__"shudian",
	     "west" : __DIR__"qian",
]));
    set("outdoors", "大理");

	setup();
	replace_program(ROOM);
}
