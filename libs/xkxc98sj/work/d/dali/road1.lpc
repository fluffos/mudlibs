#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIW "山间小路" NOR);
  set("long", @LONG
你走在山上的一条小道上，这里十分荒僻，你不断的拨开挡在路上
的杂草，荆棘。
LONG);
  set("outdoors", "dali");
  set("exits", ([
    "southwest": __DIR__ "shanlu7",
    "northup": __DIR__ "road2",
  ]));

  setup();
}
