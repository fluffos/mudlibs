// Room: /d/songshan/zmwshi.c

inherit ROOM;

void create()
{
	set("short", "掌门卧室");
	set("long", @LONG
一进卧室，一股清新的味道扑鼻而来，看到墙上挂着各种宝剑和字画，
以及书架上摆满的书。从左冷禅的屋内装饰看，的确不失一代宗师的气派。
LONG);
	set("exits", ([ /* sizeof() == 1 */
	"east" : __DIR__"chanyuan",
	]));
	setup();
	replace_program(ROOM);
}
