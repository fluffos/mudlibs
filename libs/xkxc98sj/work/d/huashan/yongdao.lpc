inherit ROOM;

void create()
{
	set("short", "甬道");
	set("long", @LONG
这是一条长长的甬道，身旁石壁上都是利斧砍过的一片片切痕，砍过处十
分光滑，犹如刀切豆腐一般，往前数丈，甬道仍然未到尽头。
LONG);
	set("exits", ([
		"left"	: __DIR__"shidong",
	]));
	set("indoors", "华山" );

	setup();
	replace_program(ROOM);
}
