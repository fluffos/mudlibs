// changan wusheng
// baiyu6.c

inherit ROOM;
#include <room.h>

void create() {
  set("short", "圣火洞");
  set("long", @LONG

洞内红光闪闪，阵阵兽鸣在洞内回荡，丝丝腥味催人欲晕。
LONG);

  set("exits", ([ /* sizeof() == 3 */
    "north": __DIR__ "huoyun" + sprintf("%d", random(20)),
    "northeast": __DIR__ "huoyun" + sprintf("%d", random(20)),
    "northwest": __DIR__ "huoyun7",
  ]));

  set("no_clean_up", 1);
  setup();
}
int clean_up() {
  return 0;
}
