// npc: /d/xingxiu/npc/bayi.c
// Jay 3/25/96

inherit NPC;
//inherit F_DEALER;
#include <ansi.h>;

void create() {
  set_name("药铺小厮", ({ "yaopu xiaosi", "xiaosi" }));
  set("gender", "男性");
  set("age", 20);

  set("attitude", "heroism");

  set("combat_exp", 100);
  set("shen_type", 100);
  set("vendor_goods", ({
    // __DIR__"obj/ping",
    // __DIR__"obj/mianbao",
  }));
  setup();
  set("chat_chance", 10);
}
