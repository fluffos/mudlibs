inherit ROOM;
#include <ansi.h>
#include <room.h>
void create() {
  set("short", "生化楼");
  set("long", @LONG
这里是威龙中学的生化楼，就是供学生做化学和生物实验的地方，
各处都是实验室，生化楼是比较老的楼了，大厅显得阴暗潮湿，给人
一种恐怖的感觉，还有各种药品的气味，令每一个呼吸系统顺畅的同
学都觉得无法忍受，不是化学派的就不能再往上走了！否则就有中毒
的可能！
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "juqing",
    "south": __DIR__ "playgrnd1",
    "west": __DIR__ "eroad3",
    "up": __DIR__ "shenhualou2",
  ]));
  // 	set("valid_startroom",1);
  set("objects", ([
    __DIR__ "npc/wang": 1,
  ]));
  set("coor/x", 0);
  set("coor/y", -20);
  set("coor/z", 0);
  setup();
}
