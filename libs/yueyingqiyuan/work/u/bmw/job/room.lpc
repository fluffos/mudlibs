inherit ROOM;

void create() {
  set("short", "幻想西天美容院");
  set("long", @LONG

坐落在长安城内的一家超时空的现代化美容院，里面装备齐全，是你
美容变性花钱的好去处！
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "north": "/d/city/kezhan",
  ]));
  set("objects", ([
    "/u/bmw/npc/bmw": 1,
  ]));
  set("no_clean_up", 1);

  setup();
}
