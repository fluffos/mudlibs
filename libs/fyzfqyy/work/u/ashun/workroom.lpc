inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIR "血河车" NOR);
  set("long", @LONG
暂时为空。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "east": "/d/wiz/wizard_room",
    "up": "/d/fy/fysquare",
  ]));
  set("no_magic", 1);
  setup();
}
