int main(object me, string arg) {
  int i;
  object *user;

  user = users();
  i = sizeof(user);
  while (i--) {
    if (user[i]->query_temp("error")) printf("%O\n", user[i]);
  }
  return 1;
}
