// /clone/board/wiz_b.c

inherit BULLETIN_BOARD;

void create() {
  set_name("激情的故事", ({ "board" }));
  set("location", "/u/lost/workroom");
  set("board_id", "wiz_m");
  set("long", "激情的记事本。\n");
  setup();
  set("capacity", 60);
  replace_program(BULLETIN_BOARD);
}
