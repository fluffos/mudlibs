(/cfg/) ASSIST
(/cmds/) ASSIST
(/daemon/) ASSIST
(/doc/) ASSIST:MUSE
(/domains/) ASSIST:MUSE
(/domains/xandria/towns/lisht/save/) all
(/ftp/) all
(/include/) ASSIST
(/lib/) ASSIST
(/log/) ASSIST
(/log/edits/) all
(/log/errors/) all
(/log/players/) all
(/log/bugs/) all
(/log/reports/) all
(/log/open/) all
(/log/echo/) all
(/news/) ASSIST
(/obj/) ASSIST
(/old/) ASSIST
(/realms/) MUSE:ASSIST
(/save/) MUDLIBPRIV:ASSIST
(/save/boxes/) all
(/save/shops/) all
(/save/kills/) all
(/save/vehicles/) all
(/save/pshops/) all
(/save/phouses/) all
(/save/estates/) all
(/secure/save/) ADMIN:SECURE:MUDLIBPRIV
(/std/) ASSIST:MUSE
(/souls/) ASSIST:MUSE
(/tmp/) all
(/verbs/) ASSIST
(/worlds/) ASSIST
