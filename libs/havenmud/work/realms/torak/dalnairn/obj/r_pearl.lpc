//
// Dalnairn Total Recode
// Torak@Haven 2014 - 2017
// Random Pearls
//

#include <lib.h>
#include <vendor_types.h>
inherit LIB_ITEM;

nosave void create() {
  ::create();
  switch (random(12)) {
    case 0:
      SetKeyName("pearl");
      SetId(({ "pearl" }));
      SetAdjectives(({ "black", "perfect", "perfectly", "round" }));
      SetShort("a black pearl");
      SetMass(15);
      SetLong(
        "This is a perfectly round black pearl."
      );
      SetClass(1);
      SetMass(2);
      SetValue(100);
      SetVendorType(VT_TREASURE);
      break;

    case 1:
      SetKeyName("pearl");
      SetId(({ "pearl" }));
      SetAdjectives(({ "soft", "white", "perfect", "perfectly", "round" }));
      SetShort("a soft white pearl");
      SetMass(15);
      SetLong(
        "This is a perfectly round soft white pearl."
      );
      SetClass(1);
      SetMass(2);
      SetValue(75);
      SetVendorType(VT_TREASURE);
      break;

    case 2:
      SetKeyName("pearl");
      SetId(({ "pearl" }));
      SetAdjectives(({ "soft", "white", "perfect", "perfectly", "round" }));
      SetShort("a soft white pearl");
      SetMass(15);
      SetLong(
        "This is a perfectly round soft white pearl."
      );
      SetClass(1);
      SetMass(2);
      SetValue(75);
      SetVendorType(VT_TREASURE);
      break;

    case 3:
      SetKeyName("pearl");
      SetId(({ "pearl" }));
      SetAdjectives(({ "soft", "white", "blister", "half-sphere" }));
      SetShort("a soft white blister pearl");
      SetMass(15);
      SetLong(
        "A blister pearl, a half-sphere, formed flush against the shell of the pearl oyster."
      );
      SetClass(1);
      SetMass(2);
      SetValue(50);
      SetVendorType(VT_TREASURE);
      break;

    case 4:
      SetKeyName("pearl");
      SetId(({ "pearl" }));
      SetAdjectives(({ "soft", "white", "blister", "half-sphere" }));
      SetShort("a soft white blister pearl");
      SetMass(15);
      SetLong(
        "A blister pearl, a half-sphere, formed flush against the shell of the pearl oyster."
      );
      SetClass(1);
      SetMass(2);
      SetValue(50);
      SetVendorType(VT_TREASURE);
      break;

    case 5:
      SetKeyName("pearl");
      SetId(({ "pearl" }));
      SetAdjectives(({ "soft", "white", "blister", "half-sphere" }));
      SetShort("a soft white blister pearl");
      SetMass(15);
      SetLong(
        "A blister pearl, a half-sphere, formed flush against the shell of the pearl oyster."
      );
      SetClass(1);
      SetMass(2);
      SetValue(50);
      SetVendorType(VT_TREASURE);
      break;

    case 6:
      SetKeyName("pearl");
      SetId(({ "pearl" }));
      SetAdjectives(({ "soft", "white", "blister", "half-sphere" }));
      SetShort("a soft white blister pearl");
      SetMass(15);
      SetLong(
        "A blister pearl, a half-sphere, formed flush against the shell of the pearl oyster."
      );
      SetClass(1);
      SetMass(2);
      SetValue(50);
      SetVendorType(VT_TREASURE);
      break;

    case 7:
      SetKeyName("pearl");
      SetId(({ "pearl" }));
      SetAdjectives(({ "soft", "white", "oblong", "odd", "oddly", "shaped" }));
      SetShort("an oblong soft white pearl");
      SetMass(15);
      SetLong(
        "An oblong an oddly shaped soft white pearl."
      );
      SetClass(1);
      SetMass(2);
      SetValue(25);
      SetVendorType(VT_TREASURE);
      break;

    case 8:
      SetKeyName("pearl");
      SetId(({ "pearl" }));
      SetAdjectives(({ "soft", "white", "oblong", "odd", "oddly", "shaped" }));
      SetShort("an oblong soft white pearl");
      SetMass(15);
      SetLong(
        "An oblong an oddly shaped soft white pearl."
      );
      SetClass(1);
      SetMass(2);
      SetValue(25);
      SetVendorType(VT_TREASURE);
      break;

    case 9:
      SetKeyName("pearl");
      SetId(({ "pearl" }));
      SetAdjectives(({ "soft", "white", "oblong", "odd", "oddly", "shaped" }));
      SetShort("an oblong soft white pearl");
      SetMass(15);
      SetLong(
        "An oblong an oddly shaped soft white pearl."
      );
      SetClass(1);
      SetMass(2);
      SetValue(25);
      SetVendorType(VT_TREASURE);
      break;

    case 10:
      SetKeyName("pearl");
      SetId(({ "pearl" }));
      SetAdjectives(({ "soft", "white", "oblong", "odd", "oddly", "shaped" }));
      SetShort("an oblong soft white pearl");
      SetMass(15);
      SetLong(
        "An oblong an oddly shaped soft white pearl."
      );
      SetClass(1);
      SetMass(2);
      SetValue(25);
      SetVendorType(VT_TREASURE);
      break;

    case 11:
      SetKeyName("pearl");
      SetId(({ "pearl" }));
      SetAdjectives(({ "soft", "white", "oblong", "odd", "oddly", "shaped" }));
      SetShort("an oblong soft white pearl");
      SetMass(15);
      SetLong(
        "An oblong an oddly shaped soft white pearl."
      );
      SetClass(1);
      SetMass(2);
      SetValue(25);
      SetVendorType(VT_TREASURE);
      break;

  }
}
