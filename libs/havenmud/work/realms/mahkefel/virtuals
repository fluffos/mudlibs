typoed virtual locations WILL FUNCTION, so if someone leaves a room and returns and it's not the same room, there is probably a typo in there.

because the lib will find the virtual, it will have the x,y values, but the file path to the two rooms won't match, so they'll be different rooms.

