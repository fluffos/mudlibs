//  Alessandra 2011

#include <lib.h>
#include "../tam.h"
inherit LIB_ROOM;

nosave void create() {
  room::create();

  SetClimate("arid");
  SetShort("a room full of zombies");
  SetLong(
    "This is why we can't have nice things. "
  );
  SetSmell(([
    "default": "This is smells.",
  ]));
  SetListen(([
    "default": "This is noises.",
  ]));
  SetItems(([
  ]));
  SetItemAdjectives(([
  ]));
  SetInventory(([
  ]));
  SetEnters(([
  ]));
  SetExits(([
    "up": TAM_ROOM "death2",
    "north": TAM_ROOM "rn2",
  ]));
}
