//Created by RoomEditer on 00-9-9.
inherit ROOM;

void create()
{
	set("short","高速路入口");
	set("long",@LONG
一条公路
LONG
	);
	set("globe","odin");
	set("exits",([

		"south" : "/d/odin/starport",
		"north" : "/d/odin/highway1",
	]));
/*	set("objects",([
		"/d/odin/npc/waiter" : 1,
		"/d/odin/obj/disk" : 2,
	]));
*/
	set("coor/x",0);
	set("coor/y",30);
	set("coor/z",0);
	setup();
}
