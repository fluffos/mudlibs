//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "领主府秘室");
	set("long", @LONG
这是一个没有窗户的房间，墙壁以厚重的铅块围成，是一个不为
外人所知的密室。摆设除了中间的操纵台外一无所有，显然是一间专
用的TV通话间。
LONG
	);

/*
****************************************************
这里的秘密我打算设计成：
1，可以与地球建立联系，通过杀死德古斯比，得到钥匙之类；
2，用德的声音与地球说话；
3，得到去地球的秘密民船号码，在月台输入后进入“亲不孝”；
4，如果没有地球教信物“纱巾”，着陆后马上被追杀；
5，“纱巾”在黑狐的神秘山庄处通过杀死黑狐得到。
*******************************************************
*/
	set("globe","phezzan");

	set("exits", ([
		"up" : __DIR__"lord_office",
	]));

//	set("objects", ([
//		__DIR__"npc/degusibi" : 1,
//	]) );

	set("coor/x",0);
	set("coor/y",180);
	set("coor/z",-10);

	setup();
}
