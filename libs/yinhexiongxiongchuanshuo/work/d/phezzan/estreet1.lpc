//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create()
{
	set("short",GRN"东费沙大道"NOR);
	set("long", @LONG
费沙人勤劳、智慧。行星费沙能有今天这样一派自由、和平、繁
荣的景象，除了得益于此之外，更应该感谢历届领主坚持奉行、且都
视之为立国之本的不卑不亢的“中立政策”。这里北面是银河帝国高
等事务官邸，南面就是费沙的中央物资中心。
LONG
	);

	set("globe","phezzan");

	set("exits", ([
		"east" : __DIR__"estreet2",
		"south" : __DIR__"supply_center",
		"west" : __DIR__"estreet",
		"north" : __DIR__"empire_embassy",
	]));

//	set("objects", ([
//		__DIR__"npc/goer" : 1,
//	]) );

	set("coor/x",40);
	set("coor/y",0);
	set("coor/z",0);

	set("walk_bonus",5);

	setup();
}
