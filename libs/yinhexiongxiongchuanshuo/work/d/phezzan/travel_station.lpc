//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "旅游专线");
	set("long", @LONG
前来费沙度假旅游的主要是一些有钱的帝国贵族和同盟的富商巨
贾，费沙本地人倒是不多。这里十分钟就有一趟旅游长途地上车发出，
车站里大部分都是旅游的新婚夫妇。
LONG
	);

	set("globe","phezzan");

	set("exits", ([
		"west" : __DIR__"hotelstreet2",
	]));

/*	set("taxi",([
		__DIR__"farm" : 2000,
		__DIR__"wadi" : 4000,
		__DIR__"shore" : 10000,
	]) );
*/
	set("coor/x",-20);
	set("coor/y",-60);
	set("coor/z",0);

	setup();
}
