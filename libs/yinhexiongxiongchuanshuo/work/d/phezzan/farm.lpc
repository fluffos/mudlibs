//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "亚西尼波伊亚溪谷农场");
        set("long", @LONG
这里位于首都北方九○○公里。是属于以商业立国的费沙国内仍
末被利用的广大可耕地。近年由才由移民者的集团农场开始进行开拓。
一望无际的粮田闪耀着生命的希望。
*************************************************
迷宫的入口。通往farmfield.c
*************************************************
LONG
	);

	set("exits", ([
		"west" : __DIR__"farmfield",
	]));

	set("taxi",([
		__DIR__"travel_station" : 2000,
	]) );

	set("globe","phezzan");
	setup();
}
