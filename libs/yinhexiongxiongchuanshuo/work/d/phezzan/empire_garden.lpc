//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "后花园");
	set("long", @LONG
虽然帝国高等事务官邸早已人去楼空，更不用提花园的维护管理。
但是这里的残石小径却依然能够看出它主人在时的精美，只是现在已
经杂草丛生。一池愁水吹皱。留在这里的，只有萧瑟的秋风。
LONG
	);

	set("globe","phezzan");

	set("exits", ([
		"south" : __DIR__"empire_study",
	]));

//	set("objects", ([
//		__DIR__"obj/key2" : 1,
//	]) );

	set("coor/x",40);
	set("coor/y",40);
	set("coor/z",0);

	setup();
}
