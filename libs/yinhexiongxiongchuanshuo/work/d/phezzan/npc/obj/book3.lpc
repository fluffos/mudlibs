// book.c

inherit ITEM;

void create()
{
	set_name("战略的思考及战术的思考", ({ "consider book","book" }));
	set_weight(600);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "本");
		set("long",
			"《战略的思考及战术的思考》----杨威利的四次战争\n");
		set("value", 100);
		set("material", "paper");
	}
}
