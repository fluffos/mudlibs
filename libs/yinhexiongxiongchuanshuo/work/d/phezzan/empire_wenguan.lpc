//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "帝国文化参赞办公室");
	set("long", @LONG
虽然费沙内政上则拥有完全的自主权，但由于其形式上直隶银河
帝国皇帝的主权之下，须对帝国纳贡，其中还包括对自由行星同盟的
外交及通商。这里就是因应这一形式而专门设立的官僚机构。房间的
陈设除了帝国历代皇帝的画像外，其他摆设简单得出奇。
***这里的秘密尚未想好****
LONG
	);

	set("globe","phezzan");

	set("exits", ([
		"west" : __DIR__"empire_hall",
	]));

//	set("objects", ([
//	__DIR__"npc/bookseller" : 1,
//	]) );

	set("coor/x",50);
	set("coor/y",20);
	set("coor/z",0);

	setup();
}
