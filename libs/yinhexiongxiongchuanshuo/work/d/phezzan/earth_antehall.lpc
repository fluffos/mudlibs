//Modified by xyc
#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "小楼前厅");
	set("long", @LONG
纯木制的房屋，里面既阴暗又潮湿。旁边的衣帽架上积着厚厚的
浮土。里面即没有装饰，也没有窗户。完全靠木制墙壁的缝隙看见凹
凸不平的地板。
LONG
	);

	set("globe","phezzan");

	set("exits", ([
//		"west" : __DIR__"earth_refectory",
		"south" : __DIR__"oilcan5",
	]));

	set("coor/x",-130);
	set("coor/y",-30);
	set("coor/z",0);

	set("objects", ([
		__DIR__"npc/earth_monk" : 1,
	]) );

	set("allow_fight",1);

	create_door("south", "木门", "north", 1);

	setup();
}
