//Created by RoomEditer on 00-10-15.
inherit ROOM;

void create()
{
	set("short","下层中央大道");
	set("long",@LONG
这里和中层和上层不同，由于下层基本上是一个生活保障区，所
以环境和大自然里一样，有时在人造太阳下会产生一种远离战争的错
觉．
LONG
	);
	set("globe","geiersburg");
	set("exits",([
		"east" : "/d/geiersburg/guardroom2",
		"south" : "/d/geiersburg/otissdown1",
		"north" : "/d/geiersburg/otisndown1",
	]));
	set("coor/x",0);
	set("coor/y",0);
	set("coor/z",-60);
	setup();
}
