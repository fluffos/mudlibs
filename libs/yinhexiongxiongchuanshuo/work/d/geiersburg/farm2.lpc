//Created by RoomEditer on 00-10-15.
inherit ROOM;

void create()
{
	set("short","2号农场");
	set("long",@LONG
和１号农场不同的是，这里是以畜牧业为主的农场，要塞内部的
９９％的肉类、蛋类的来源都是这里，作为一个牧场，这里依稀有远
古地球的蒙古草原的景象。
LONG
	);
	set("globe","geiersburg");
	set("exits",([
		"east" : "/d/geiersburg/dsroad2",
	]));
	set("coor/x",-60);
	set("coor/y",-60);
	set("coor/z",-60);
	setup();
}
