
#include <armor.h>

inherit CLOTH;

void create()
{
	set_name("费沙军服", ({ "uniform" }) );
	set("long","费沙军服的长描述。\n");
	set("no_drop",1);
	set_weight(3000);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "件");
		set("material", "cloth");
		set("armor_prop/armor", 5);
	}
	setup();
}
