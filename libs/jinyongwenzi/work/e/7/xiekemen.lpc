// Room: xiekemen.c

inherit ROOM;
#include <room.h>

void create()
{
	set("short", "谢客亭");
	set("long", @LONG
前面一溜细青石覆檐砖墙，中间一扇黑漆木门，门额上有流云、仙鹤、行
龙、双凤等深浮雕图案，雕工细致，构思巧妙。正中匾额书“谢客亭”三字。
门旁立着个小牌子(sign)。
LONG
	);

        set("exits", ([
		"south" : __DIR__"zhudubadian",
		"north" : __DIR__"rimulundian",
	]));
        set("item_desc",([ "sign" : "		进香客步止于此。\n",     
           ]));
set("objects", ([
            "/clone/npc/e/houtonghai" : 1,
        ]));
	setup();
      //  create_door("enter", "黑漆木门", "out", DOOR_CLOSED);
}
