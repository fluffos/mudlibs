// Room: /open/world1/whoami/birdarea/en1.c

inherit ROOM;

void create()
{
	set("short", "[1;37m東北[0m澤林");
	set("long", @LONG
你來到了東北澤林的入口處，東北澤林的氣候十分燥熱又潮
濕，腳下的泥濘踩起來鬆鬆軟軟的，四周的樹木長的頗為高大，
蓋住了大部份的陽光。往東北向有個小路可以更深東北澤林裡。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "northeast" : __DIR__"en2",
]));
	set("outdoors", "forest");
	set("current_light", 1);
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
