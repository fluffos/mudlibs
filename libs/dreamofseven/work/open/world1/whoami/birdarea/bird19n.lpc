// Room: /open/world1/whoami/birdarea/bird19n.c

inherit ROOM;

void create()
{
	set("short", "[1;36m天梟教[0m內堂");
	set("long", @LONG
這裡是天梟教的內堂，你環顧了四周，發現有四根用大理石
做的天梟石柱，地板和天花板也是大理石做的，這內堂中的地上
擺滿了各式各樣的藥草，還有一地的藥草相關醫書。濃濃的藥草
味充滿整間內堂。
LONG
	);
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/fon2" : 1,
]));
	set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"bird19",
]));
	set("no_clean_up", 0);
	set("valid_startroom", 1);
	set("light", 1);
	set("current_light", 2);

	setup();
	replace_program(ROOM);
}
