// Room: /open/world1/whoami/birdarea/en76.c

inherit ROOM;

void create()
{
	set("short", "[1;37m東北[0m澤林");
	set("long", @LONG
你往北走了過來，附近的樹木還是一樣的茂密，但是你往更
遠的地方望去，卻被山群的峭壁所環繞，這條小路往東方延伸出
去，可以看到一處高高的石台，往北是附近唯一的村莊-北蠻村，
北蠻村的大多數村民，都非常好客，你可以考慮過去歇個腳。
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
  "south" : __DIR__"en75",
  "north" : "/open/world1/tmr/north_bar/town1",
  "east" : __DIR__"en78",
]));
	set("current_light", 1);
	set("no_clean_up", 0);
	set("outdoors", "forest");

	setup();
	replace_program(ROOM);
}
