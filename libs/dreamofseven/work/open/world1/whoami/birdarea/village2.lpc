// Room: /open/world1/whoami/birdarea/village2.c

inherit ROOM;

void create()
{
	set("short", "[1;37m東北[0m澤林");
	set("long", @LONG
你沿著泥土路走到了這裡，道路兩旁是兩排茂密的樹林，附近
有一些奇怪的花草，繼續往東邊走可以通到獵狐村，從這兒可以望
見東邊有個巨大無比的石拱門，還有幾個穿著奇裝異服的人。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"village3",
  "west" : __DIR__"village1",
]));
	set("outdoors", "forest");
	set("current_light", 1);
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
