#include <room.h>
inherit ROOM;
void create()
{
        set("short", "隱藏之間");
        set("long", @LONG
移動Mob(NPC)的出發點。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */ 
     "down":__DIR__"sec12.lpc",
     "east":__DIR__"sec11.lpc",      "west":__DIR__"sec3.lpc",
     "north":__DIR__"sec8.lpc",
     "up":__DIR__"min1.lpc",
     "south":__DIR__"equip.lpc",     
]));
        set("light",1);
        set("objects", ([ /* sizeof() == 1 */
]));

        setup();
        replace_program(ROOM);
}

