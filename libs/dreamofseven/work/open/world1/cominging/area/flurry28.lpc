#include <room.h>
inherit ROOM;
inherit DOOR;

void create()
{
	set("short", "走廊間");
	set("long", @LONG
這裡是東西南北四邊的走廊之間，也是莊內通往北南必經的路徑，
經常有些人從這來往，這兒的屋頂有些漏洞，若是下雨了必會漏水，看
來沒有人來修理的樣子。
LONG
	);
	set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"flurry32.lpc",
  "south" : __DIR__"flurry24.lpc",
  "west" : __DIR__"flurry27.lpc",
  "east" : __DIR__"flurry29.lpc",
]));
	create_door("south","木門","north",DOOR_CLOSED);
	set("no_clean_up", 0);
	set("light",1);
	setup();
}
