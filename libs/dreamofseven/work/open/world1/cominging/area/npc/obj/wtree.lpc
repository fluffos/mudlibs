inherit ITEM;

void create()
{
	set_name("梧桐樹",({"woo-ton tree","tree"}));
	set("long","梧桐樹在森林之中算是很普遍的樹木。\n");
	set_weight(9999);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit","棵");
		set("no_get",1);
	}
	setup();
}