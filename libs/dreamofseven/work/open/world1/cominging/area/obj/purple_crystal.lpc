#include <ansi.h>
inherit ITEM;

void create()
{
	set_name("[0;35m紫水晶[0m", ({ "purple crystal", "crystal" }));
	set("long", @LONG
這是一塊十分特殊的水晶，是放置在滄水水洞中的，是從前盤古開天闢
地之後，他的一顆牙齒所成的，傳說因為是在盤古的嘴巴中，在深山中
的一處洞內，如果將此石放上(lay on stone)，將會造出許多的水。
LONG	);
	set_weight(9999);
	if( clonep() )
		set_default_object(__FILE__);
	else
	{
		set("unit", "個");
		set("value", 1);
		set("no_sell", 1);
		set("no_sac", 1);
	}
	setup();
}