// Room: /u/l/lestia/area/sn14.c

inherit ROOM;

void create()
{
	set("short", "北方大森林-小徑");
	set("long", @LONG
越往大森林內部走，就越靠近靈幻宗的所在地，雖然這附近沒有什麼森
嚴的守備，但整座森林透露出一股不可冒犯的氣息。另外，你始終想不透為
什麼古老相傳，每個進入森林內處的人，總沒有人一個再回來。
LONG
	);
	set("no_clean_up", 0);
	set("world", "undefine");
	set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"sn15",
  "north" : __DIR__"sn13",
]));
	set("current_light", 3);

	setup();
	replace_program(ROOM);
}
