inherit ITEM;

void create()
{
    set_name("巨魔脊椎骨", ({"troll spinal bone", "bone"}));
    if( !clonep() ) {
        set("long", "一條巨魔的脊椎骨，似乎就像條鐵鞭。");
        set("value", 10);
        set("unit", "條");
        set("weight", 20);
         } else
		   set_default_object(__FILE__);
    setup();
}
