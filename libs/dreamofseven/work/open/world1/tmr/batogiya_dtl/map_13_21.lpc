#include <room.h>
inherit BANK;
void create()
{
	set("short", "錢莊");
        set("long", @LONG

LONG
);
	set("exits",([
	"north" : __DIR__"map_12_21",
	]));
        setup();
        set("map_long",1);     //show map as long
    replace_program(BANK);
}

