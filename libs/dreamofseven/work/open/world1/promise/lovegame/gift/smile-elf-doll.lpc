#include <ansi.h>
inherit ITEM;
void create()
{
        set_name("[0;1;35m[1mL[1m[1m[1;37m[1m[1m[1mF[0m", ({ "smile elf","smile","elf" }) );
        set_weight(11000);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("long","
這是一具精巧的手工娃娃，當你仔細把玩的時候，
似乎覺得她正在對你微笑著。"HIC"[2003年七夕紀念娃娃]
\n"NOR);
                set("unit", "隻");
                set("material", "gold");
                set("no_sell",1);
               set("no_drop",1);
               set("no_auc",1);
               set("no_get",1);
              set("no_give",1);   
                set("value",4800);
        }
              setup();
}
int query_autoload() { return 1; }

