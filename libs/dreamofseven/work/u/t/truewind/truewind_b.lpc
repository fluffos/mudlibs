#include <ansi.h>
inherit BULLETIN_BOARD;

void create()
{
    set_name(HIW"巨石"NOR, ({ "board" }) );
    set("location", "/u/t/truewind/workroom");
    set("board_id", "TR_board");
    set("long","巨石。\n" );
    setup();
    set("capacity", 99);
    set("master",({ "truewind" }));
}

