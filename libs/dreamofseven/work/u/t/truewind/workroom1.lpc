// Room: /u/t/truewind/workroom1.c

inherit ROOM;

void create()
{
	set("short", "[1;37m思過崖[0m");
	set("long", @LONG
此處是斷崖南方的一塊空地，觸目所及盡是白皚皚的景象，周圍
被山壁環抱，偶而會有野生動物經過。在蜿蜒狹窄的小道上依稀可以
看到一些大大小小的腳印和斷落的松樹枝。
LONG
	);
	set("outdoors", "land");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"workroom",
  "south" : "/u/t/truewind/Minetown/area/minetown",
]));
	set("current_light", 1);

	setup();
	replace_program(ROOM);
}
