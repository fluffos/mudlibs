// Room: /u/k/kenshin/sha-zin-gu/room23.c

inherit ROOM;

void create()
{
	set("short", "[1;33m大雄寶殿[0m");
	set("long", @LONG
這就是雷音寺的做早、晚課的必備場所─大雄寶殿了，到了這看
到雄偉的佛像，不禁讓人也得拜個他兩三下。
LONG
	);
	set("light", 1);
	set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"room22",
]));
	set("no_clean_up", 0);
	set("current_light", 4);

	setup();
	replace_program(ROOM);
}
