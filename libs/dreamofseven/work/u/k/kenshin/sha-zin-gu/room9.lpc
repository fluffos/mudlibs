// Room: /u/k/kenshin/sha-zin-gu/room9.c

inherit ROOM;

void create()
{
        set("short", "[0;1;32m[1m[1m[1m[1m[1m[0m");
        set("long", @LONG
這裡就是暇日谷的商店街了，這裡有許多的店家，例如武器舖、
防具店等等，如果你缺什麼的話，可以到此地來看看，應該有你缺的
物品。
LONG
        );
        set("outdoors", "land");
        set("current_light", 3);
        set("no_clean_up", 0);
        set("exits", ([ /* sizeof() == 5 */
  "west" : __DIR__"room8",
  "east" : __DIR__"room14",
  "northwest" : __DIR__"room15",
  "north" : __DIR__"room12",
  "south" : __DIR__"room13",
]));

        setup();
        replace_program(ROOM);
}

