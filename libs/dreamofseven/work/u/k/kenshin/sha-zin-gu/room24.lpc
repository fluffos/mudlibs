// Room: /u/k/kenshin/sha-zin-gu/room24.c

inherit ROOM;

void create()
{
        set("short", "[0;1;36m[1mZ[1m[1m[1m[1m[1m[1m[0m");
        set("long", @LONG
越來越接近武學之家，感受的壓迫感也更高了，從這裡就能聽到
裡面的弟子練拳的吆喝聲，真是有朝氣。往西走就能到達暇日谷的農
場─曙光農場。
LONG
        );
        set("outdoors", "land");
        set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"room25",
  "south" : __DIR__"room11",
]));
        set("no_clean_up", 0);
        set("current_light", 4);

        setup();
        replace_program(ROOM);
}

