// Room: /u/k/kenshin/sha-zin-gu/room22.c

inherit ROOM;

void create()
{
	set("short", "[1;33m雷音寺內[0m");
	set("long", @LONG
來到了寺內，看到許多人參拜如此傳神，真是讓人敬佩三分，還有
寺內的擺設也是井然有序，花草生長如此生氣盎然。
LONG
	);
	set("light", 1);
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"room23",
  "south" : __DIR__"room21",
]));
	set("no_clean_up", 0);
	set("current_light", 4);

	setup();
	replace_program(ROOM);
}
