// Room: /u/k/kenshin/sha-zin-gu/room17.c

inherit ROOM;

void create()
{
	set("short", "[33m九龍客棧[0m");
	set("long", @LONG
暇日谷裡唯一的客棧，一進去就聞到用道地黃山土雞所烹調的
黃山雞腿，聽說鎮上還有以百毒之王蜈蚣所調成的蜈蚣酒，但是最
主要的還是本客棧店小二的親切招呼，讓人覺得在這裡買東西是理
所當然的。
LONG
	);
	set("room_type", "hotel");
	set("no_fight", 1);
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"obj/npc/waiter" : 1,
]));
	set("current_light", 3);
	set("light", 1);
	set("exits", ([ /* sizeof() == 2 */
  "up" : __DIR__"room18",
  "east" : __DIR__"room16",
]));
	set("valid_startroom", 1);
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
