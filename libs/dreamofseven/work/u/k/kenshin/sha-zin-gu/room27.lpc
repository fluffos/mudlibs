// Room: /u/k/kenshin/sha-zin-gu/room27.c

inherit ROOM;

void create()
{
	set("short", "大廳[0m");
	set("long", @LONG
走到了大廳，看到許多總教頭鬱悶的樣子，不知是為何，應該要
好好的問清楚才是。一進來，就能看到許多兵器懸掛在牆上，還有一
些兵集放在左後方的置書櫃那，可令人想到除了教導弟子，自己也有
填充點知識。
LONG
	);
	set("light", 1);
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"room28",
  "south" : __DIR__"room26",
]));
	set("no_clean_up", 0);
	set("current_light", 4);

	setup();
	replace_program(ROOM);
}
