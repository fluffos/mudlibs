#include <room.h>;
#include <ansi.h>;
inherit ROOM;
void create()
{
 set("short",HIC"NPC測試處"NOR);


set("exits", ([
"east":__DIR__"workroom1.lpc",
]) );
set("objects",([
 __DIR__"origami/npc/master.lpc"   :1,
]));
set("light",1);
setup();
}
