#include <room.h>;
#include <ansi.h>;
inherit ROOM;
void create()
{
 set("short",HIC"Object測試處"NOR);
set("exits", ([
"west":__DIR__"workroom1.lpc",
 ]) );
set("objects",([
]));
set("light",1);
setup();
        replace_program(ROOM);
}
