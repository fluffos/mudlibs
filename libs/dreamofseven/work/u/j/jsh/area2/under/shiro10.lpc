#include <ansi.h> 
inherit ROOM; 
void create() 
{
        set("short", "修羅禁地");
        set("long", @LONG
test。
LONG
        );
        set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"shiro9", 
]));
        set("no_clean_up", 0);

        setup();
}

