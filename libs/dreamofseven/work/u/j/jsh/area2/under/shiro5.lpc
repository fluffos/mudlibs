#include <ansi.h> 
inherit ROOM; 
void create() 
{
        set("short", "修羅禁地");
        set("long", @LONG
test。
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"shiro6", 
  "south" : __DIR__"shiro4", 
]));
        set("no_clean_up", 0);

        setup();
}

