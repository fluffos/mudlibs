#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", HIB"地下墓穴"NOR);
        set("long", @LONG
test。

LONG
        );
        set("exits", ([ /* sizeof() == 1 */
  "southeast" : __DIR__"room10",
])); 
        set("no_light",1);
        set("no_clean_up", 0);

        setup();
        replace_program(ROOM);
}

