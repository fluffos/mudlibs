#include <armor.h>
#include <ansi.h> 
inherit FINGER;
void create()
{
        set_name(HIY"守護者之戒"NOR,({"ring of guard","ring"}) );
        set("long","");
        set_weight(2500);
  if ( clonep() )
    set_default_object(__FILE__);
else
{
                set("volume",3);
                set("unit","只");
                set("value",6000);
               // set("limit_lv",50); 
               // set("armor_prop/str",-1);
                set("armor_prop/armor",4);
                //set("armor_prop/show_damage",1); 
                set("armor_prop/bio",1);
}
setup();
}

