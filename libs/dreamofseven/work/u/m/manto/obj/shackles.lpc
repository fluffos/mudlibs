#include <ansi.h>
inherit ITEM;
void create()
{
        set_name("枷鎖",({"amk's shackles","amk's","shackles"}));
        set_weight(1000000000);
                set("value",0);
                set("no_sac",1);
                set("no_drop",1);
                set("can_give",1);
                set("unit","付");
 set("long",@LONG
AMK專屬的枷鎖
LONG
                );
setup();
}
int query_autoload() { return 1; }
