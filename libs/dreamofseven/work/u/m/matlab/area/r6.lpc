// Room: /u/m/matlab/area/r6.c

inherit ROOM;

void create()
{
	set("short", "空房間");
	set("long", @LONG
這是一間什麼也沒有的空房間。
LONG
	);
	set("exits", ([ /* sizeof() == 4 */
  "northeast" : __DIR__"r7-1.lpc",
  "north" : __DIR__"r7.lpc",
  "south" : __DIR__"r5.lpc",
  "northwest" : __DIR__"r7-2.lpc",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
