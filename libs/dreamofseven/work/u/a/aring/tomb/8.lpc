// Room: /u/a/aring/tomb/8.c

inherit ROOM;

void create()
{
	set("short", "[0m[35m大墓園－[1;33m西側大門[0m");
	set("long", @LONG
這裡是大墓園中的西側大門，說是門其實早已崩壞不堪，看起來倒
比較像是一個缺口，往西邊則是通往火山的山路。
LONG
	);
	set("no_clean_up", 0);
	set("light", "0");
	set("exits", ([ /* sizeof() == 2 */
  "southeast" : __DIR__"10.lpc",
  "north" : __DIR__"6.lpc",
]));
	set("current_light", 1);

	setup();
	replace_program(ROOM);
}
