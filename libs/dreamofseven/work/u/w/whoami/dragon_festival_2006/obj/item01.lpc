inherit ITEM;
#include <ansi.h>

void create()
{
        set_name("糯米", ({ "glutinous rice", "rice" }) );
        set_weight(10);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("unit", "份");
                set("value", 10);  
        }
}
