inherit ITEM;
#include <ansi.h>

void create()
{
        set_name("粽繩", ({ "zong string", "string" }) );
        set_weight(10);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("unit", "根");
                set("value", 10);  
        }
}
