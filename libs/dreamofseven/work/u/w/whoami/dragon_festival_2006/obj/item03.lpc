inherit ITEM;
#include <ansi.h>

void create()
{
        set_name("調味料", ({ "seasoning" }) );
        set_weight(10);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("unit", "份");
                set("value", 10);  
        }
}
