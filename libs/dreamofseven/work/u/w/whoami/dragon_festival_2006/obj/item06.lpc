inherit ITEM;
#include <ansi.h>

void create()
{
        set_name("紅豆", ({ "red beans", "beans" }) );
        set_weight(10);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("unit", "包");
                set("value", 10);  
        }
}
