// Room: /u/k/king/area/r19.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[1;37m-[1;35m鬼城[2;37;0m");
	set("long", @LONG
走了一段相當長的路，穿插於兵馬俑之間，週圍瀰漫著一股死氣，
實在不是乾淨之地，這裡距皇陵宮己有一里的光景，從這裡往四週看去
，只見後方有著更寬廣的地方，極目望去，在遠方隱約可以見到一座座
的巨大雕像，著實可疑。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"r20",
  "northwest" : __DIR__"r18",
]));
	set("world", "undefine");
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/man" : 3,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
