// Room: /u/k/king/area/r4.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[2;37;0m-不歸冥路");
	set("long", @LONG
這裡的屍骨一樣是一堆一堆的，似乎一座山丘一樣，聽說只要一句咒
語，就可以讓它們全部復活，面對這麼多的怪獸你將如何存活呢。

LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"r3",
  "east" : __DIR__"r5",
]));
	set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/dog" : 1,
  __DIR__"npc/corpse2" : 3,
]));
	set("world", "undefine");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
