// Room: /u/k/king/area/r3.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[2;37;0m-不歸冥路");
	set("long", @LONG
一堆堆的屍體不禁又讓你吐了好久，然而這些屍體卻是怪獸最喜歡的
食物了，你要是沒有足夠的功力，你很快就會變成一道怪獸的佳餚了。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"r2",
  "south" : __DIR__"r4",
]));
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/corpse" : 2,
]));
	set("world", "undefine");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
