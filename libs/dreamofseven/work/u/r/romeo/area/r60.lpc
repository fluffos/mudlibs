// Room: /u/k/king/area/r60.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[2;37;0m鬼王殿");
	set("long", @LONG
這裡就是大殿了，是周陰暗模糊，兩邊的兩根刻有龍的圖案的柱子是
支撐這裡的支柱，只要一倒看來這裡就會變成一堆荒塚了。
LONG
	);
	set("item_desc", ([ /* sizeof() == 1 */
  "柱子" : "一根刻著飛天神龍的柱子，兩邊的翅膀是用黃金所製造，看起來相當值錢，
下面的腳怎麼看起來好像一個把手呢。
",
]));
	set("no_recall", 1);
	set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"49",
  "north" : __DIR__"r61",
]));
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/pur" : 1,
]));
	set("world", "undefine");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
