// Room: /u/k/king/area/r5.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[2;37;0m-不歸冥路");
	set("long", @LONG
到了這裡，你看到一隻隻的怪物，正啃蝕著剛剛才要殺敵的武者，一
個個都是武林的佼佼者，如今都變成了食物，看來你也差不多嚕。

LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"r6",
  "west" : __DIR__"r4",
]));
	set("objects", ([ /* sizeof() == 3 */
  __DIR__"npc/dog" : 1,
  __DIR__"npc/bat" : 1,
  __DIR__"npc/corpse" : 1,
]));
	set("world", "undefine");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
