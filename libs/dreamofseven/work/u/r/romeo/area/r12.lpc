// Room: /u/k/king/area/r12.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[1;37m-[1;35m鬼城[2;37;0m");
	set("long", @LONG
這裡黃沙滾滾，其壯闊景象有如江河之浪滔滔不絕，只見數以萬計的
人形土製雕像整齊地排成一列列，只見在一旁有個巨大石碑，上頭寫著：
『始皇帝十三年，吾以得天歸，吾以皇陵為記，其陵中兵馬俑是為吾民，
帝國霸業，千秋萬世。』
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "northeast" : __DIR__"r13",
  "south" : __DIR__"r11",
]));
	set("world", "undefine");
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/guard4" : 1,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
