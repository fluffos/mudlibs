// Room: /u/k/king/area/r9.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[2;37;0m-不歸冥路");
	set("long", @LONG
這裡的地上有一堆灰燼，應該是有人來過，一邊好像畫有鬼城的地圖
，但是你看了還是看不懂，還是別浪費時間了，快進入鬼城吧。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"r8",
  "south" : __DIR__"r10",
]));
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/lee" : 1,
]));
	set("world", "undefine");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
