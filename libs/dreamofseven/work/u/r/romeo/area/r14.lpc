// Room: /u/k/king/area/r14.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[1;37m-[1;35m鬼城[2;37;0m");
	set("long", @LONG
跟左雁翼殿相比，這裡右雁翼殿的格局便顯得狹小，屋內約五十尺見
方，在正中央有個大丹爐，丹爐外頭被潻以黃金色粉，在提耳部分有著兩
隻黃龍，爐高約八尺，看來這是始皇晚年用以練丹之物。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "northeast" : __DIR__"r17",
  "west" : __DIR__"r11",
]));
	set("world", "undefine");
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/guard5" : 1,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
