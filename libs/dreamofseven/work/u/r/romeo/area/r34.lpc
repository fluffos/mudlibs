// Room: /u/k/king/area/r34.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[2;37;0m七星林");
	set("long", @LONG
這裡是四周都堆滿了武功秘笈，只留下一條小小的通道，在你的頭頂
的正上方有一塊匾額，上面寫著：「武功之家」，原來這裡是光明城所分
發的組織之一。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"r32",
  "north" : __DIR__"r40",
]));
	set("world", "undefine");
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/tammi" : 1,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
