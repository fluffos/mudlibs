// Room: /u/k/king/area/42.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[2;37;0m七星林");
	set("long", @LONG
這裡是十大殺手之一的秋末悲歌的住所，只有一張床，一張棉被，就
什麼都沒有了，據說他都沒有一定的時間回來，他要殺的人還沒有一個可以活
命的。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"r33",
  "east" : __DIR__"43",
]));
	set("world", "undefine");
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/swordgod" : 1,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
