// Room: /u/k/king/area/r6.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[2;37;0m-不歸冥路");
	set("long", @LONG
走了那麼久，殺了那麼多殭屍，但是路卻一直沒有盡頭，四周的景色
又是那麼的昏暗，身上的夜明珠有沒有了應有的光輝了。遠遠看去好像有
一座城，還是快快過去吧。

LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "southeast" : __DIR__"r7",
  "north" : __DIR__"r5",
]));
	set("world", "undefine");
	set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/child" : 2,
  __DIR__"npc/corpse" : 2,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
