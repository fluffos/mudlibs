// Room: /u/k/king/area/r18.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[1;37m-[1;35m鬼城[2;37;0m");
	set("long", @LONG
在這俑像之間有著相當多的小道，看去只見一片都是一個模子印出來
的桶兵，只見皇陵城門口處那巨大的石碑己漸漸遠去，不禁令人心焦，
在這裡將要迷路。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "southeast" : __DIR__"r19",
  "north" : __DIR__"r11",
]));
	set("world", "undefine");
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/mman" : 2,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
