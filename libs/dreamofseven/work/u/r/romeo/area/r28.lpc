// Room: /u/k/king/area/r28.c

inherit ROOM;

void create()
{
	set("short", "[1;35m鬼城[1;37m-[1;34m五斗迷星陣[2;37;0m");
	set("long", @LONG
這裡是通往鬼王殿的第一層機關，這邊的陣式是由五形變化
而來的，充滿了玄妙，跟一般的陣法完全不同，想要通過這裡並
不是如此容易。看看四周也都只有一堆堆的荒石，走著走著都一
直回到原地，等你走到精疲力盡的時候，將會有一群怪獸來撕裂
你，直到你死亡。
LONG
	);
	set("no_recall", 1);
	set("world", "undefine");
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/lie" : 1,
]));
	set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"r23",
  "west" : __DIR__"r29",
  "south" : __DIR__"r27",
  "east" : __DIR__"r23",
]));
	set("no_clean_up", 0);
	set("no_steal", 1);

	setup();
	replace_program(ROOM);
}
