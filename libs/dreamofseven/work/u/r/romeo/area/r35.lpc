// Room: /u/k/king/area/r35.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[2;37;0m七星林");
	set("long", @LONG
這裡是霧氣最重的地方，完完全全看不到一丁點的東西，不小心還踢
到石頭，痛到眼淚都快流出來了。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"44",
  "east" : __DIR__"r32",
]));
	set("world", "undefine");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
