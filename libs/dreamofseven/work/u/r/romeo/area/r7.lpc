// Room: /u/k/king/area/r7.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[2;37;0m-不歸冥路");
	set("long", @LONG
走著走著你踏到一顆人頭，你嚇了一跳，一看是剛剛給你喝水的農民
，心中不免一陣感慨，心想只有除魔才可以化解現在的的人間慘劇。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"r8",
  "northwest" : __DIR__"r6",
]));
	set("world", "undefine");
	set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/dog" : 2,
  __DIR__"npc/cool" : 1,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
