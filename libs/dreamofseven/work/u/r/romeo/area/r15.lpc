// Room: /u/k/king/area/r15.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[1;37m-[1;35m鬼城[2;37;0m");
	set("long", @LONG
這裡是正殿的副宮，在當時也就是所謂的行宮，這裡是文武百官上朝
時，用來待命之處，這裡有面牆是為黃色鑲框的，上頭掛了許多木板片，
在牆上寫了四個大字「章」「奏」「表」「議」，此字為小篆，把整面牆
分為四大部分，看來似乎是為了區分這些木板片的功用。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"r11",
  "northwest" : __DIR__"r16",
]));
	set("world", "undefine");
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/guard6" : 1,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
