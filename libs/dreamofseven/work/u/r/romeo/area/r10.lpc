// Room: /u/k/king/area/r10.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[2;37;0m-不歸冥路");
	set("long", @LONG
這裡就是鬼城的下方了，鬼城的陰氣並不是普通的重，而且鬼城好像
是用人頭砌成得，所使用的人頭實在無法計算，難怪有恢之不去的冤魂在
四周徘徊。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"r9",
  "up" : __DIR__"r11",
]));
	set("world", "undefine");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
