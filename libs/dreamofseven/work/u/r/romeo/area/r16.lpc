// Room: /u/k/king/area/r16.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[1;37m-[1;35m鬼城[2;37;0m");
	set("long", @LONG
這裡就是鬼城廚房，兩邊都有微弱的燈光，這裡的菜色好像都以人肉
為主，每一個時辰都會有一班守衛來巡邏，想在這裡打混是不可能的。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "southeast" : __DIR__"r15",
]));
	set("world", "undefine");
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/guard3" : 1,
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
