// Room: /u/k/king/area/r36.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[2;37;0m七星林");
	set("long", @LONG
這裡是一間小茅屋，跟平常看到的茅屋沒什麼不一樣，但是會在這裡
就是有那麼一點不平常，看看上面好像破一個洞，你可以試著爬爬看。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "southeast" : __DIR__"r32",
  "up" : __DIR__"46",
]));
	set("world", "undefine");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
