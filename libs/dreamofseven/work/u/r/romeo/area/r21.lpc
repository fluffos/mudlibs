// Room: /u/k/king/area/r21.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[1;37m-[1;35m鬼城[2;37;0m");
	set("long", @LONG
這裡就是戰國皇陵第一層了，你可以到這裡來已經代表你的武功修為已
經有一定的程度了，再下去你也只有死路一條，四周也都只有一堆堆的骷髏
頭，不知道多少古代的高手死在這裡。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"r22",
  "down" : __DIR__"r20",
]));
	set("world", "undefine");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
