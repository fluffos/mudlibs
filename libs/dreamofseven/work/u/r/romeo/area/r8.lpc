// Room: /u/k/king/area/r8.c

inherit ROOM;

void create()
{
	set("short", "[1;31m戰國[1;33m皇陵[2;37;0m-不歸冥路");
	set("long", @LONG
這裡已經快到鬼城的門口了，四周都有人走動的聲音，不知道是戰友
還是敵人，還是小心的前往比較好。不然隨時都有一堆怪物來殺你。

LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"r7.lpc",
  "east" : __DIR__"r9",
]));
	set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/ccoo" : 1,
  __DIR__"npc/bat" : 2,
]));
	set("world", "undefine");
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
