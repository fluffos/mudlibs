// Room: /u/l/luky/room/pearl_41.c
inherit ROOM;
void create()
{
set("short", "[1;35m地窖[m");
set("long", @LONG

  一走進這個黑暗而又潮濕的地窖，你不禁打了個寒顫，冰涼的
空氣似乎把時間都給凍結了。這裡終年不見天日，台階上的青苔
顯示出即使是修道院的人也很少進來這裡。火把的光線漸漸變暗
了，你現在只有一個念頭，那就是....立刻離開這裡!!

LONG
);
set("objects",([
__DIR__"npc/oten":1,
]));
set("exits", ([ /* sizeof() == 1 */
"up" : __DIR__"pearl_37",
]));
set("no_clean_up", 1);
set("light",0);
setup();
replace_program(ROOM);
}
