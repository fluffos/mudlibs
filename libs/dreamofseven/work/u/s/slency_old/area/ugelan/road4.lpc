// Room: /u/s/slency/area/ugelan/road4.c

inherit ROOM;

void create()
{
	set("short", "[1;33m幽遮蘭大道[2;37;0m");
	set("long", @LONG
一條由青石所鋪成的寬闊道路，走到此處，路突然變寬敞起來，來
往的人們也更多了，遠處你可以隱約看見一座宏偉的吊橋橫跨在護城河
的兩岸，整個氣象十分壯觀，東西向是一條寬廣的人行大道。
LONG
	);
	set("outdoors", "land");	
	set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"road15",
  "west" : __DIR__"road6",
]));
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
