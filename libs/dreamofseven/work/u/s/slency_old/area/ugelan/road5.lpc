// Room: /u/s/slency/area/ugelan/road5.c

inherit ROOM;

void create()
{
	set("short", "[1;33m幽遮蘭大道[2;37;0m");
	set("long", @LONG
這裡是這整座城的中心點，連接宮廷與城鎮的中心地區，這裡也是
各種行業的集散中心，往北便是通往皇宮的宏偉吊橋，由此往東有一間
教堂，往西可以到幽遮蘭城外的郊區，往南則是一座大型廣場。
LONG
	);
	set("outdoors", "land");	
	set("exits", ([ /* sizeof() == 4 */
  "west" : __DIR__"road13",
  "north" : __DIR__"pontlevis",
  "east" : __DIR__"road6",
  "south" : __DIR__"road18",
]));
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
