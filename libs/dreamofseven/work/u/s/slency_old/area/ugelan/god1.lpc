// Room: /u/s/slency/area/ugelan/god1.c
#include <room.h>
#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", "[1;37m【[2;37;0m羽聖雅教堂大門[1;37m】[2;37;0m");
	set("long", @LONG
這裡是羽聖雅教堂的大門口，從牆上精細的雕刻，落地窗上鮮艷的
彩色玻璃裝飾，便可想見內部的裝潢必是相當莊嚴神聖的，微微地你可
以聽見神樂的旋律在空中搖曳，大地在旁陪襯著這棟石頭建築，一切都
是如此的莊嚴肅穆，往西走可以回到城中。
LONG
	);
	set("item_desc", ([ /* sizeof() == 1 */
  "大門" : "雕刻十分精緻的一扇大門。
",
]));
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"church4",
  "east" : __DIR__"god2",
]));
	create_door("east", "大門", "west", DOOR_CLOSED);
	set("no_clean_up", 0);
	set("outdoors", "land");

	setup();
	replace_program(ROOM);
}
