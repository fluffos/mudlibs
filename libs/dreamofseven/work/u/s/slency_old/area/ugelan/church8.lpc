// Room: /u/s/slency/area/ugelan/church8.c

inherit ROOM;

void create()
{
	set("short", "森林外圍");
	set("long", @LONG
這裡是幽遮蘭城東北城後的一座森林的外圍部份，平常除了祡夫們
進入森林砍祡外，並沒有什麼人會無故進入此森林，由於這座森林過於
龐大，因此也沒有人清楚知道森林後方有什麼東西。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "southwest" : __DIR__"church7",
]));
	set("no_clean_up", 0);
	set("outdoors", "forest");

	setup();
	replace_program(ROOM);
}
