// Room: /u/s/slency/area/ugelan/inn.c

inherit ROOM;

void create()
{
	set("short", "[1;37m幽蘭旅店[2;37;0m");
	set("long", @LONG
這裡是本城最大的一間旅店，這裡的人潮來往熙攘，十分熱鬧，不
時可以聽到一些正在用飯的旅客們，大談闊論他們旅途中所發生的奇聞
異事，這裡供應食物及生活用品，需要什麼的話向服務生詢問，他會很
樂意替你解決生活的問題。
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
  "up" : __DIR__"innroom1",
  "south" : __DIR__"road1",
  "north" : __DIR__"road18",
]));
	set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/waiter" : 1,
  __DIR__"npc/obj/mapwood" : 1,
]));
        set("no_fight", 1);
	set("valid_startroom",1); 
	set("no_clean_up", 0);
        set("light",1);
	setup();
	replace_program(ROOM);
}
