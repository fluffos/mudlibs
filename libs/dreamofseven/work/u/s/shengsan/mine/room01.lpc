// room01.c
#include <path.h>
inherit ROOM;

void create()
{
	set("short", "[1;36m休憩庭院走廊[0m");
	set("long", @LONG
這個走廊充滿著高貴的氣氛，走廊外的兩旁種著長青樹、柏樹、松樹
以及還有一大片的鬱金香花園。
LONG
	);

	set("exits", ([ /* sizeof() == 7 */
		"east" : __DIR__"room02",
		"west" : SHENGSAN+"workroom",
		"north": __DIR__"sendroom",
	]));
	set("objects", ([ /* sizeof() == 1 */
		"/u/s/shengsan/npc/suya" : 1,
		"/u/s/shengsan/npc/robot01" : 1,
	]));
	set("light", 1);
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}
