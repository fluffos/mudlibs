
inherit ROOM;

void create()
{
	set("short", "[1;36m巫毒教教壇[m");
	set("long", @LONG
你看到了莊嚴的擺設及肅穆的裝飾品在教壇裡醒目的地方。這裡是教
徒們膜拜神靈的地方，每到重要的節日，教徒們都會在這開會及開設法壇
，巫毒教的教主也會主持祭典對教徒宣揚教威。
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
	    "south": __DIR__"room38",
            ]));

	set("objects", ([ /* sizeof() == 2 */
            __DIR__"mob/leader" : 1,
	    __DIR__"mob/protector" : 2,
            ]));

	set("no_clean_up", 0);

	set("light", "1");

	setup();
	replace_program(ROOM);
}
