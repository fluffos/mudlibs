#include <ansi2.h>
inherit ITEM;
void create()
{
        set_name("植物種子", ({"seeds" }) );
        set("long", 
"一粒一粒的植物種子。\n");
        if( clonep() )
                set_default_object(__FILE__);
        else {
        set("unit", "粒");
        }
        setup(); 
}