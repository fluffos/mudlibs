// event_test.c
// author: Spock 	97.Nov.20.
// last modified by Spock 	97.Nov.20.

#include <ansi.h>

inherit F_CLEAN_UP;

void time_report() {
	seteuid( getuid() );
	users()->receive_message("event", HIY "[报时] 现在时间: " + CHINESE_D->chinese_date( time(), 1 ) + ".\n" + NOR );
}