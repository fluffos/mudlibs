// query_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("種ǎは莱痙ē狾", ({ "board" }) );
	set("board_id", "query_b");
	set_weight(20000);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("long", "硂琌ㄑ痙ē癘ㄆ痙ē狾.\n" );
		set("unit", "");
		set("capacity", 100);
		set("value", 10 );
	}
	setup();
}
