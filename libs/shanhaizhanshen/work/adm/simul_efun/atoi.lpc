// atoi.c
// original ES 2 mudlib

// This function read a string of number, and returns as interger.
 
int atoi(string str)
{
	int v;

	if( !stringp(str) || !sscanf(str, "%d", v) ) return 0;
	return v;
}
