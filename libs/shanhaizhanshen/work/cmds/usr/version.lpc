// version.c
// Display MudOS & mudlib version.
// Original ES 2 command.
// Last modified by Spock @ FF	97.Sep.5.

#include <mudlib.h>

inherit F_CLEAN_UP;
inherit F_CMD;

int main(object me, string arg)
{
	write( "界面系统: " + __VERSION__ + "\n");
	write( "函数库版本: " + MUDLIB_VERSION + "\n");
	return 1;
}
int help(object me)
{
  write(@HELP
指令格式： version

这个指令会显示游戏目前所用的 MudOS driver 以及本 mudlib 的版本.
 
HELP
    );
    return 1;
}
 
