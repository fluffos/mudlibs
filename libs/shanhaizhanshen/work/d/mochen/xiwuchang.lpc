// Room : xiwuchang.c	
// Created by Atlus 1999.7.21
// Modified by Atlus 1999.7.21
//

inherit ROOM;
int do_look(string arg);
void init();


void create ()
{
        set ("short", "西练武场");
        set ("long", @LONG
这是一个宽敞的西练武场。场子的中央竟是一个立满了木桩的大
沙池几个兽人正在上面练习轻功身法，在木桩上跳来跳去，但不
时也有几个摔了下来，好在下面是沙池。否则要摔伤的。
LONG);
	set("outdoors", "mochen");
	set("exits", ([
                "north" : __DIR__"cailiaofang",
                "south" : __DIR__"fanting",
                "east" :__DIR__"qianting", 
                "west" :__DIR__"wroad", 
        ]));
         set("no_clean_up", 0);
        setup();
}

void init()
{
        

		add_action("do_look", "look");
}
//月影（neva）2002年4月28日删改前辈观星台源程序为动态房间。
int do_look(string arg)
{
	object me = this_player();
	mixed *local;
        int night;
	night = 0;
	local = localtime(time() * 60);
	if (local[2] < 5 || local[2] >= 19) night = 1;

	if (!arg) {

{
			if (local[2] < 5 || local[2] > 19)//半夜
				set("long", @LONG
深夜，夜幕低垂，满天繁星映衬着宁静的大地。这是一个宽敞的
西练武场。场子的中央竟是一个立满了木桩的大沙池几个兽人正
在上面练习轻功身法，在木桩上跳来跳去，但不时也有几个摔了
下来，好在下面是沙池。否则要摔伤的。
LONG
);
			
			else if (local[2] < 10)//清晨
				set("long",@LONG
东方的地平线上渐渐有了黎明的颜色。这是一个宽敞的西练武场。
场子的中央竟是一个立满了木桩的大沙池几个兽人正在上面练习
轻功身法，在木桩上跳来跳去，但不时也有几个摔了下来，好在
下面是沙池。否则要摔伤的。
LONG
);
			else if (local[2] < 15)//正午
				set("long",@LONG
太阳高挂在你的头顶正上方。这是一个宽敞的西练武场。场子的
中央竟是一个立满了木桩的大沙池几个兽人正在上面练习轻功身
法，在木桩上跳来跳去，但不时也有几个摔了下来，好在下面是
沙池。否则要摔伤的。
LONG
);
			else if (local[2] < 18)//傍晚
				set("long",@LONG
太阳孤独地向西方的天空中慢慢沉落。这是一个宽敞的西练武场。
场子的中央竟是一个立满了木桩的大沙池几个兽人正在上面练习
轻功身法，在木桩上跳来跳去，但不时也有几个摔了下来，好在
下面是沙池。否则要摔伤的。
LONG
);
			else//入夜
				set("long",@LONG
夜晚降临了，如洗的天空露出几点星星。这是一个宽敞的西练武
场。场子的中央竟是一个立满了木桩的大沙池几个兽人正在上面
练习轻功身法，在木桩上跳来跳去，但不时也有几个摔了下来，
好在下面是沙池。否则要摔伤的。
LONG
);
		}
		return 0;
	}
}


