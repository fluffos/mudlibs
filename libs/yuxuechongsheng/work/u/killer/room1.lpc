inherit ROOM;
void create() {
  set("short", "测试房间一");
  set("long", "这里是进行各种测试的房间。\n");

  set("exits", ([
    // "out" : __DIR__"workroom",
  ]));

  set("outdoors", "city");
  setup();
  replace_program(ROOM);
}
