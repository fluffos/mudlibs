inherit BULLETIN_BOARD;

void create()
{
        set_name("改动记录留言板",({ "board" }) );
          set("location", "/u/killer/workroom");
        set("board_id", "killer_b");
        set("long","这里记录了整个lib的文件改动情况。\n" );
        setup();
        set("capacity", 3000);
        replace_program(BULLETIN_BOARD);
}


