// sroad12.c
// Modified by Java Apr.27 1998

inherit ROOM;
void create()
{
        set("short","大雪山山谷");
        set("long",@LONG
山谷里遍地积雪，四周都是峭壁。只北面有一个出口，东面似乎有
个山洞。
LONG );
        set("exits",([
                "north" : __DIR__"midao1",
                "east"      : __DIR__"sroad11",
        ]));
        set("objects", ([
                  CLASS_D("xuedao")+"/shanyong" :1,
        ]));
        set("outdoors", "xueshan");
        setup();
        replace_program(ROOM);
}

