// sroad13.c
// Modified by Java Apr.27 1998

inherit ROOM;
void create()
{
        set("short","大雪山山谷");
        set("long",@LONG
山谷里遍地积雪，四周都是峭壁。只北面有一个出口，东面似乎有
个山洞。
LONG );
        set("exits",([
                "west"      : __DIR__"sroad12",
        ]));
        set("objects", ([
                 CLASS_D("xuedao")+"/baoxiang" :1,
        ]));
        set("outdoors", "xueshan");
        setup();
        replace_program(ROOM);
}

