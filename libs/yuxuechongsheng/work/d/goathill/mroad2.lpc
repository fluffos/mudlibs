#pragma save_binary
// Room: /d/goathill/mroad2.c

inherit ROOM;

void create()
{
	set("coor",({20,4950,10}));
	set("short", "山路");
	set("long",
"你现在正走在一条崎岖的山路上，路面到处生满了及膝的杂草丛，\n"
"显示这里已经有一段时间没有车马经过了，从这里往北就是野羊\n"
"山的地界，野羊山有一座山寨，住著许多凶恶的盗匪，你最好绕\n"
"道而行。\n"
);
	set("outdoors", "goathill");
	set("exits", ([ /* sizeof() == 2 */
		"northeast" : __DIR__"mroad3",
		"south" : __DIR__"mroad1",
]));

	setup();
	replace_program(ROOM);
}
