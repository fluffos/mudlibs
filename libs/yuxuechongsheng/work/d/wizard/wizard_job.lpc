// Room: /d/wizard/wizard_job.c
// This is a room made by roommaker. write by Fan. :)

inherit ROOM;

void create()
{
	set("short", "巫师会议室");
	set("long", @LONG
这里就是「未 明 空 间 2 」巫师的会议室，四周乱
七八糟地摆着几张凳子。中间桌子的案台上压着一本巫师工作计
划表。
LONG
);
	set("valid_startroom", 1);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "down" : __DIR__"wizard_room",
]));
	set("objects", ([ /* sizeof() == 1 */
  "/clone/misc/player" : 1,
]));
	set("no_fight", "1");

	setup();
	replace_program(ROOM);
}
