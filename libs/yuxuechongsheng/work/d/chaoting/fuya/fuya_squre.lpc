#pragma save_binary
// Room: /open/tony/xianya_square.c

inherit ROOM;

void create() {
  //      add_light(this_object(),1);
  set("short", "府衙大门");
  set("long", @LONG
这儿是金陵知府衙门，两旁站立着威风凛凛的衙役闷，时常有各
地的官员来到金陵抓差办案.有的官来时知府大人还得出门相迎。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "fuya",
    "west": "/d/chaoting/xianya/xianya_squre",
    "east": "/d/chaoting/jjf/jjf_0",
  ]));
  set("objects", ([ /* sizeof() == 0 */
    "/d/chaoting/xianya/npc/yayi1": 2,
    "/d/chaoting/xianya/npc/yayi2": 2,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
