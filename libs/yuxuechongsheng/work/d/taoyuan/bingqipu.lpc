// Room: /taoyuan/bingqipu.c
// TYC 1995/12/04 

inherit ROOM;

void create()
{
	set("short", "兵器铺");
	set("long", @LONG
这里是桃源城的兵器铺，兵器铺两边的墙壁上挂满了各类兵器。到处
银光闪闪，让人眼花缭乱。各色各样的兵器应有尽有，你一时不知道挑什
么好。
LONG );
	set("item_desc", ([
		"zhaopai": "请用 \"list\" 列出货物表，\"buy\" 向老板购物。\n",
	]));
	set("exits", ([
		"north" : __DIR__"xijie3",
	]));
	set("objects", ([
		"/d/taoyuan/npc/leng": 1,
	]));
//	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}

