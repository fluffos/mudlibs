inherit NPC;

void create()
{
        set_name("考生", ({ "kao sheng"}));
        set("age", 35);
        set("gender", "男性");
        set("long", "他是前来应试的考生，脸色苍白，看起来好象十分紧张。\n");
        setup();
        carry_object("/clone/misc/cloth")->wear();
}
