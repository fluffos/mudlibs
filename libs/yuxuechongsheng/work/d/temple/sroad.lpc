#pragma save_binary
// Room: /d/temple/sroad.c

inherit ROOM;

void create()
{
	set("coor",({1350,4900,10}));
	set("short", "青石官道");
	set("long",
"这是一条宽敞坚实的青石大道,大道沿著山腰往东向山下延伸,西\n"
"边是一条石英岩石阶蜿蜒往山上而去。在石阶的尽头,是一座颇\n"
"为雄伟的道观,在江湖跑过的人都知道,那是茅山派的所在地。\n"
);
	set("exits", ([ /* sizeof() == 2 */
    "westup" : __DIR__"ladder5",
    "east" : "/d/ny/eroad3",
//     "southdown"   : "/d/snow/eroad3",
]));
	set("outdoors", "snow");

	setup();
	replace_program(ROOM);
}
