// a skeleton for user rooms
inherit "/inherit/room/city";
void create() {
  set("short", "笑口常开与天地一心城");
  set("save_name", "cheerup1012073839");
  reload();
  set("long", @LONG
这是为纪念笑口常开与天地一心的相识而建立的。
网络并不总是飞尘与寒冷
网络自有真情的微笑与快乐在
这是天地一心面对网络最后的言语

LONG
  );
  set("exits", ([
    "down": "/p/residence/cheerup1011937682.lpc",
  ]));
  set("objects", ([
  ]));
  set("class", "天外飞仙");
  set("no_fight", 1);
  set("valid_startroom", 1);
  set("sleep_room", "1");
  set("no_clean_up", 1);

  setup();
}
int clean_up() {
  return 0;
}
