// a skeleton for user rooms
inherit ROOM;
void create()
{
set("short", "广场");
set("long", @LONG
来到山顶的广场，第一感觉就是气派，整个广场的地面，
都是由精心打磨的石板铺设，正前方有一个擂台，台边树立着
一枝高高的旗杆，精英会的旗帜正迎风飘扬。广场左侧有一个
武器库，擂台的后方就是精英会总坛。

LONG
);
        set("exits", ([
"north" : "/p/residence/nzg1018714432",
"west" : "/p/residence/nzg1018713682",
"southdown" : "/p/residence/nzg1018712258.lpc",
]));
set("objects", ([
       ]) );
        set("owner", "bigtie7001");
//         set("class", "【精英会】");
         set("banghui", "【精英会】");
        set("valid_startroom",1);
        set("no_fight",1);

setup();
}
void init()
{
        add_action("do_invite","invite");
        //add_action("do_setowner","setowner");
}
int valid_enter(object me)
{
        int flag;
// always let owner go in:
         if((string)me->query("banghui") != (string)query("banghui")
//         if((string)me->query("class") != (string)query("class")
        &&(string)query("invite") != (string)me->query("id")
        &&(string)me->query("id") != (string)query("owner"))
        return 1;       

}
int do_invite(string arg)
{
        object me;
        me= this_player();
        if((string)me->query("id") != (string)query("owner"))
        return 0;
        set("invite",arg);
        write("你邀请"+arg+"来你的房间。\n");
        return 1;
}

/*int do_setowner(string arg)
{
        string oldowner;
        oldowner = (string)query("owner");
        set("owner",arg);
        write("你将房间的主人暂时设为"+arg+"。\n");
        call_out("set_back",60, oldowner);
        return 1;
}

int set_back(string oldowner)
{
        set("owner", oldowner);
        return 1;
}
*/
