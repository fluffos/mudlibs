// a skeleton for user rooms
inherit "/inherit/room/city";
void create()
{
set("short", "天地一心文学城");
set("save_name","reme1012072707");
reload();
set("long", @LONG
这是天地一心行走江湖的世界。一支笔，一张纸，所有的真心凝聚笔下，而成为网络中的天地一心
想了解天地一心，请去看我的个人主页

LONG
);
        set("exits", ([
"east" : "/p/residence/reme1012072464.lpc",
]));
set("objects", ([
       ]) );
        set("class", "bigwiz7001");
        set("no_fight",1);
        set("valid_startroom",1);
        set("sleep_room", "1");
        set("no_clean_up",1);

setup();
}
int clean_up()
{return 0;}

