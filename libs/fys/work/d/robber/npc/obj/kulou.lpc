inherit ITEM;

void create() {
  set_name("白骨骷髅", ({ "kulou" }));
  set_weight(3000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long",
      "惨白的骷髅，露出残破不全的牙齿，似乎在狞笑。\n");
    set("unit", "具");
    set("value", 0);
  }
  setup();
}
