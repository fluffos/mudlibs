#include <ansi.h>
inherit ITEM;

void create() {
  set_name(RED "锦" NOR + GRN "色" NOR + YEL "羽" NOR + MAG "毛" NOR, ({ "yu mao" }));
  set_weight(300);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "色彩斑斓的羽毛.\n");
    set("unit", "羽");
    set("value", 1000);

  }
}
