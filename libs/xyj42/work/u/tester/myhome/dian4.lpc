
inherit ROOM;

void create()
{
  set ("short", "辞行院");
 set ("long", @LONG

寂寞山庄主人为人随和并且好客．辞行院是为送行客人而设．
殿内的桌子上摆着一壶清茶和两个杯子。

LONG);
set("exits", ([ /* sizeof() == 4 */
"west" : __DIR__"dian1",

]));
set("objects", ([
                __DIR__"obj/bei": 2,
                __DIR__"obj/seat": 2,
                __DIR__"obj/table": 1, 
]));

        set("item_desc", ([ /* sizeof() == 1 */
  "table" : "桌上摆着一壶清茶，两个杯子。
",
]));
        set("no_clean_up", 0);
        set("outdoors", 0);
        setup();
}
void init()
{ 
        add_action("do_stand", "stand");
        add_action("do_sit", "sit");
}

int valid_leave(object me, string dir)
{
        if((present("bei", me)))
            return notify_fail("此杯乃庄主心爱之物，带走不妥吧。\n");
    return ::valid_leave(me, dir);
}
 
int do_sit(string arg)  
{

 
        if ( !arg || ((arg != "seat")&&(arg != "deng")&&(arg != "table")
))
                return notify_fail("你要坐在什么上面？\n");

        if (this_player()->query_temp("marks/sit"))
                return notify_fail("你已经坐下了。\n");
 
      if ( arg != "seat"&&arg != "deng")
              return notify_fail("坐桌子上雅观吗？\n");

        this_player()->set_temp("marks/sit", 1);
        message_vision("$N轻轻巧巧地坐在石凳上。\n",this_player());
      return 1;
}

int do_stand(string arg)
{ 

 
      if (!this_player()->query_temp("marks/sit"))
          return notify_fail("你本来就站着。\n");
                
      this_player()->set_temp("marks/sit", 0);
      message_vision("$N想要离开，站了起来。\n", this_player());
      return 1;
}

