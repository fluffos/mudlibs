#include <ansi.h>
#include <room.h>

inherit ROOM;

void create() {
  set("short", HBBLU RED "寂寞山庄大门" NOR);
  set("long", @LONG

这里是通向寂寞山庄正殿的门厅。北面通向正殿大厅。从这
里向南可以看见一条笔直的青石板铺成的大道。两旁各有一
座小小的亭子，里面悬挂着灯笼。


LONG);

  set("exits", ([
    "south": __DIR__ "way6",
    "north": __DIR__ "yard",
    "west": __DIR__ "wting",
    "east": __DIR__ "eting",
  ]));
  set("valid_startroom", 1);

  set("outdoors", 0);
  set("no_clean_up", 0);
  setup();
}
