
inherit ROOM;

void create()
{
  set ("short", "使节殿");
  set ("long", @LONG

寂寞山庄主人接待客人的地方，曾经是昔日最热闹的
场所。这里有接待客人的伺女。


LONG);
set("exits", ([ /* sizeof() == 4 */
"east" : __DIR__"dian1",

]));
set("objects", ([
                __DIR__"npc/sinu": 1]) );

        set("no_clean_up", 0);
        set("outdoors", 0);
        set("no_fight", 1);
        set("no_magic", 1);
        set("sleep_room",1);
        set("if_bed",1);
        setup();
}
void init()
{
        add_action("block_cmd", "", 1);

        return;
}

int block_cmd(string args)
{
        string verb;

        verb = query_verb();
        if (verb == "xiudao") return 1;
        if (verb == "exert") return 1;
        return 0;
}

