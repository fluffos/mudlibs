#include <ansi.h>

inherit ROOM;

void create ()
{
  set ("short",BCYN RED "林荫大道" NOR);
  set ("long", @LONG

洒满阳光的青石大道，路边的白杨轻声对你歌唱。秋天的晚
风送来阵阵凉爽。南飞的大雁在天空鸣叫。。。一路之上，
马儿颈下的铃铛发出串串叮当声。秋天的阳光从树叶间透射
过来，更增添了一分宁静。


LONG);

  set("exits", ([
        "south" : __DIR__"way4",
        "north" : __DIR__"way6",
      ]));

  set("objects", ([
        __DIR__"npc/bird1" : 1,
]));


  setup();
}

