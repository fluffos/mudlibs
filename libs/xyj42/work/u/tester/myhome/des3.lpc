#include <ansi.h>

inherit ROOM;

void create() {
  set("short", BYEL BLU "荒原古道" NOR);
  set("long", @LONG

渐渐地可以望见一些小村镇了。这里是中原的大片土地，战
乱给这里留下的创伤还依稀可见。向南可以看见远处的城市，
向北是无边的荒漠。。。


LONG);

  set("exits", ([
    "south": __DIR__ "des4",
    "north": __DIR__ "des2",
  ]));

  setup();
}
