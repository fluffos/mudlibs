#include <ansi.h>
#include <room.h>
inherit ROOM;

void create()
{
  set ("short",BCYN HIR"白玉台阶"NOR);

  set ("long", @LONG

通往正厅的白玉台阶。左右各有一尊雕像，分别是一
只古琴和一把宝剑。

LONG);

  set("exits", ([ 
       "southdown" : __DIR__"taijie",
       "north" : __DIR__"dian2",
       "west" : __DIR__"dian3",
       "east" : __DIR__"dian4",
]));


        set("outdoors", 0);
        set("no_clean_up", 0);

        setup();
}



