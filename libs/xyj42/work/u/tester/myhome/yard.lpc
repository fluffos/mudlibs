#include <ansi.h>
#include <room.h>
inherit ROOM;

void create()
{
  set ("short",BCYN HIR"前院"NOR);
  set ("long", @LONG

寂寞山庄的前院。宽敞的院子里种满了各种花卉。传
说当年寂寞山庄主人曾经与一位知心的女子一起游历
天下名胜，所到之处第一件事就是为那女子寻觅鲜花
和首饰。这院子里的花卉也多是他从异域移植而来。

LONG);

  set("exits", ([ 
       "south" : __DIR__"sgate",
       "north" : __DIR__"taijie",
]));

  set("objects", ([
        __DIR__"obj/rose" : 1,
        __DIR__"obj/kangnaixin" : 1,
        __DIR__"obj/ziluolan" : 1,
]));

        set("outdoors", 0);
        set("no_clean_up", 0);

        setup();
}



