#include <ansi.h>
#include <room.h>
inherit ROOM;

void create()
{
  set ("short",BCYN HIR"白玉台阶"NOR);

  set ("long", @LONG

通往正厅的白玉台阶。左右各有一尊雕像，分别是一
只白鸽和一盆紫罗兰。

LONG);

  set("exits", ([ 
       "south" : __DIR__"yard",
       "northup" : __DIR__"dian1",
]));


        set("outdoors", 0);
        set("no_clean_up", 0);

        setup();
}



