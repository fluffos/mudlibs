#include <ansi.h>

inherit ROOM;

void create() {
  set("short", GRN "绿叶丛中" NOR);
  set("long", @LONG

这里是白杨树的树梢，周围是浓密翠绿的树叶。一阵风吹来
树叶发出哗啦啦的声响。偶尔有一两只小鸟落在树枝上。看
见你正在爬树又吓得飞走了。一只秋蝉正在唱歌。。。


LONG);

  set("exits", ([
    "down": __DIR__ "way3",
  ]));

  setup();
}
