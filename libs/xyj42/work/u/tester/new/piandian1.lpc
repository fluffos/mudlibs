// Sjmao... 09-13-97
// piandian1.c

inherit ROOM;

void create() {
  set("short", "清心堂");
  set("long", @LONG

龙宫客人的休息室．

LONG);
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "nanhai",

  ]));
  set("objects", ([
    __DIR__ "npc/gonge": 1 ]));

  set("no_clean_up", 0);
  set("outdoors", 0);
  set("no_fight", 1);
  set("no_magic", 1);
  set("sleep_room", 1);
  set("if_bed", 1);
  setup();
}
void init() {
  add_action("block_cmd", "", 1);

  return;
}

int block_cmd(string args) {
  string verb;

  verb = query_verb();
  if (verb == "xiudao") return 1;
  if (verb == "exert") return 1;
  return 0;
}
