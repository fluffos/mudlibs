// Sjmao... 09-13-97
// zhongting.c

#include <room.h>
inherit ROOM;

void create() {
  set("short", "钟亭");
  set("long", @LONG

这是龙宫的钟亭，里面挂着一口巨大的金钟。你看着大钟，
禁不住想撞一撞。
LONG);
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "huilangw2",

  ]));

  set("objects", ([ /* sizeof() == 2 */
    __DIR__ "obj/jinzhong": 1,
  ]));

  set("outdoors", 0);
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
