// Gate to 敖钦's room.
// sjmao, 09-13-97
#include <room.h>

inherit ROOM;


void create() {
  set("short", "龙宫正殿大门");
  set("long", @LONG

这里是通向南海龙宫正殿的门厅。南面就是正殿大厅。从这
里向南可以看见南海龙王敖钦的住所。从面向北方的殿门可
以看到一条笔直的大道，两旁有护卫的虾兵蟹将执枪站岗。
LONG);

  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "nanhai",
    "northdown": __DIR__ "taijie",
  ]));

  set("outdoors", 0);
  set("no_clean_up", 0);
  setup();
}
