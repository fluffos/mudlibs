// Room: /moon/nroom.c
//cglaem...12/13/96.

inherit ROOM;

void create() {
  set("short", "议事厅");
  set("long", @LONG

走进来才发现这间小屋原来并不小。屋内陈设简单。一鼎青
铜香炉正若有若无地吐着一缕淡香。正北面南摆着一张雕花
木椅。
LONG);

  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/magu.lpc": 1,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "xiaoyuan",
  ]));

  setup();
}
