#include <ansi.h>
inherit ROOM;

void create ()
{
  set ("short", "广寒宫正殿");
  set ("long", @LONG

迎面先看见一个赤金九龙青地大匾，上面写著斗大三个字：
「广寒宫」；大紫檀雕螭案上设著三尺来高青绿古鼎，悬著
待漏随朝墨龙大画，一边是金锥彝，一边是玻璃盒，底下两
溜十六张紫檀交椅。
　　　
LONG);
  set("exits", ([ /* sizeof() == 1 */
 "south" : __DIR__"road3",
 "west" : __DIR__"huilang.lpc",
 "east" : __DIR__"wulang.lpc",
]));

set("objects", ([ /* sizeof() == 5 */
  __DIR__"npc/fairy2.lpc" : 1,
  __DIR__"npc/fairy3.lpc" : 1,
]));
  setup();
}

