// room: /d/moon/xiaoyuan.c

#include <ansi.h>
#include <room.h>
inherit ROOM;

int do_stand(string temp2);
int do_sit(string temp3);

void create() {
  set("short", "听雨楼");
  set("long", @LONG

一座两层的碧绿色小竹楼, 三面环水, 南面对着半壁残荷，北面则是一个钓台, 屋中央摆了一张棋秤, 上面还有半局残棋．
屋子里空空荡荡, 只有三俩把竹椅．此处幽静宜人，似乎是一
个极好的静修之处。
LONG);


  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "road3",
    "up": __DIR__ "bedroom",
    "south": __DIR__ "lotuspond",
  ]));

  set("no_clean_up", 0);
  set("no_fight", 1);
  set("no_magic", 1);
  setup();
}
