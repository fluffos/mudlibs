// Room: /city/erlou.c

inherit ROOM;

void create()
{
        set("short", "休息室");
        set("long", @LONG

一间简朴的睡房，里面连一张床都没有．但却干干净净．
LONG
        );

        set("sleep_room",1);
        set("if_bed",1);
        set("exits", ([
                "south" : "/d/sea/girl2",
        ]));

        setup();
}

