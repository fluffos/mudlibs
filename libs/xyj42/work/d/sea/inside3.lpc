// inside3.c

inherit ROOM;

void create ()
{
  set ("short", "龙宫正殿");
  set ("long", @LONG

正殿之上悬一明珠，把诺大一殿照得通亮．两旁桌椅玉石打造而
成，当中还嵌着无数珠宝．两边墙上挂了两幅画，看起来相当的
陈旧．南北是两座偏殿．
LONG);

  set("exits", ([ /* sizeof() == 4 */
  "north" : __DIR__"inside6",
  "south" : __DIR__"inside5",
  "west" : __DIR__"inside2",
  "east" : __DIR__"inside4",
]));
  set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/longwang" : 1,
]));
  set("valid_startroom",1);

  setup();
  "/obj/board/longgong_b"->foo();
}
