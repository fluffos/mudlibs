// inside2.c

inherit ROOM;

void create() {
  set("short", "玉阶");
  set("long", @LONG

由此向上是龙宫正殿，东海龙王就住在里面．一级级的台阶都是
用玉砌成．阶上两边立着武士，银盔银甲，煞是威风．
LONG);

  set("exits", ([ /* sizeof() == 3 */
    "south": __DIR__ "food.lpc",
    "west": __DIR__ "inside1",
    "east": __DIR__ "inside3",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/wushi": 2,
  ]));

  setup();
}
