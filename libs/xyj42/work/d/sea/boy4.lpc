// hill.c

inherit ROOM;

void create() {
  set("short", "练功堂");
  set("long", @LONG

屋里十分宽敞，墙上画的是一些叉法分解图，靠墙的架子(rack)
上放了各种兵器。地下铺的是裁剪整齐的木块，踩在上面吱吱做
响。你如果想要用什么兵器不妨试试向龙表弟要(yao)。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "boy2",
  ]));
  set("objects", ([ /* sizeof() == 3 */
    __DIR__ "npc/long3.lpc": 1,
    __DIR__ "npc/biaodi": 1,
  ]));
  set("item_desc", ([
    "rack": "
兵器架上插着：

钢叉(gangcha)，木叉(mucha)，铁锤(hammer)，板斧(axe)，
长剑(sword)，长枪(spear)，单刀(blade)，和铁棍(stick)。

一边衣架上挂着：

藤甲(tengjia)，狼皮裙(skirt)。

",
  ]));

  setup();
}
