// Room: /d/sea/food.c
inherit ROOM;

void create ()
{
  set ("short", "餐房");
  set ("long", @LONG

龙宫内的一间餐房，一般只是宫中弟子使用。锅里沽嘟嘟
不知煮着什么东西，一个大箩筐里放着好些包子。
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"inside2",
]));
  set("objects", ([ /* sizeof() == 3 */
  "/d/obj/food/baozi.lpc" : 4,
  __DIR__"npc/long6.lpc" : 1,
  "/d/obj/food/niupi-jiudai.lpc" : 1,
]));

  set("resource", ([ /* sizeof() == 1 */
  "water" : 1,
]));
  setup();
}
