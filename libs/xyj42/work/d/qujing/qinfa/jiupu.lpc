inherit ROOM;

void create() {
  set("short", "酒铺");
  set("long", @LONG

酒铺外撑着一长棚，几张桌子从屋外一直铺到屋里，上面零乱
地放着酒罐子，粗酒碗和点心碟子，地上扔有一些花生皮壳。
几位酒客在摇头晃脑地猜着酒拳。

LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 1
    "west": __DIR__ "jiedao3",
    ]));

  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/zhou": 1,
    ]));


  set("no_clean_up", 0);
  setup();
}
