// biqiu

inherit ROOM;

void create ()
{
  set ("short", "林道");
  set ("long", @LONG
    
你行进在铺满树叶的道路上，风萧萧，阳光从稀稀松松的树枝
间散射下来。光景苍凉，山高水长道更长，只见是纷纷野花盈
径，杂树栖鸟鸟更啼。

LONG);

  set("exits", ([ /* sizeof() == 2 */
  "northwest" : "/d/qujing/wudidong/firemount-wudidong1",
  "east" : __DIR__"lin4",
]));
// set("projects",([ /* sizeof() == 1*/
//   __DIR__"npc/people" :1,
// ]));
  set("outdoors","/d/qujing/biqiu");

  setup();
}
