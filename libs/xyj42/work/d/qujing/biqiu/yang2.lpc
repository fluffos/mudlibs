// biqiu

inherit ROOM;

void create() {
  set("short", "杨树");
  set("long", @LONG

杨树林里，树木茂盛高大，枝叶青绿斑白。许多树(tree)的根
根叉叉交缠在一起，看上去十分奇特。风吹在树上，发出有一
点异样的声响。

LONG);
  set("item_desc", ([
    "tree": @TEXT
树的枝条绞在一起，长在同一个根上。
TEXT
  ]));


  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "yang1",
    "east": __DIR__ "zhuang",
    "southwest": __DIR__ "yang3",
  ]));
  setup();
}
