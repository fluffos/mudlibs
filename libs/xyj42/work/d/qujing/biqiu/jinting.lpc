// biqiu

inherit ROOM;

void create ()
{
  set ("short", "金亭馆");
  set ("long", @LONG

金亭馆为远道而来的客人而设，门有一对招牌红灯笼，屋里有
杨木茶桌和几把椅子，墙角有软草席纱罩木床。有几位客人正
在馆里休息聊天。

LONG);
  set("sleep_room",1);
  set("if_bed",1);
  set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"jie3",
]));
 set("objects",([ /* sizeof() == 1*/
   __DIR__"npc/suohana" :1,
]));

  setup();
}
