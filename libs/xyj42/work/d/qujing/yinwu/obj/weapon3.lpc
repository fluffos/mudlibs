#include <weapon.h>

inherit FORK;

void create() {
  set_name("槐刺叉", ({ "huaici cha", "cha" }));
  set_weight(1000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "枝");
    set("value", 1000);
    set("material", "metal");
  }
  init_fork(15);
  setup();
}
