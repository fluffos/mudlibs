#include <weapon.h>

inherit WHIP;

void create() {
  set_name("鹿皮鞭", ({ "lupi bian", "bian" }));
  set_weight(1000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "条");
    set("value", 1000);
    set("material", "metal");
  }
  init_whip(15);
  setup();
}
