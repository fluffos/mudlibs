// Room: /d/qujing/yuhua/ting.c
inherit ROOM;

void create() {
  set("short", "石墙");
  set("long", @LONG
    
墙的南面，高高的石墙下，有一狭窄的通道如小路一般，被人
踩得结结实实。

LONG);

  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "shi3",
  ]));
  /*
        set("objects", 
        ([ //sizeof() == 1
                __DIR__"npc/bing" : 4,
        ]));
*/
  set("outdoors", "yuhua");
  set("no_clean_up", 0);

  setup();
}
