// tianzhu/jinluan

inherit ROOM;

void create ()
{
  set ("short", "龙阁");
  set ("long", @LONG

整个楼阁为金黄色，阁内的西墙上有巨大的琉璃砖二龙戏珠，
楼阁里面的地上铺着暗红色的火方石，上面再铺设一条长长的
绣花地毯。

LONG);

  set("exits", ([ /* sizeof() == 3 */
  "north" : __DIR__"hou2",
  "east" : __DIR__"yuhua",
]));
  set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/taijian" : 2,
]));
  setup();
}
