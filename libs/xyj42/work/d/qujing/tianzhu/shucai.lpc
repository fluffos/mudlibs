// tianzhu/vendor

inherit ROOM;

void create ()
{
  set ("short", "蔬菜市场");
  set ("long", @LONG
    
蔬菜市场为一小空场地，地上堆了一些烂菜叶瓜皮之类的垃圾。
场地中有不少搭棚售菜的农妇，不时也可见到几位菜农挑着担
子来来往往。

LONG);

  set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"jiedao75",
]));
  set("objects",   ([ //sizeof() == 1
	__DIR__"npc/wei" : 1,
]));
  setup();
}
