// tianzhu/vendor

inherit ROOM;

void create ()
{
  set ("short", "竹器店");
  set ("long", @LONG
    
店门口放着一些编好的竹器，门檐上挂着一个招牌迎风轻晃着，
里面铺面不小，到处堆满着竹制品。西墙有一柜台，上面也堆
着正在出售的竹制品。

LONG);

  set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"jiedao16",
]));

  set("objects", 
  ([ //sizeof() == 1
	__DIR__"npc/shi" : 1,
]));
  setup();
}
