#include <weapon.h>

inherit DAGGER;

void create()
{
        set_name("单刃匕", ({ "danren bi", "bi" }) );
        set_weight(500);
        if( clonep() )
                set_default_object(__FILE__);
        else {
//                set("long", "这是一柄长长的四明铲，粗粗的杖头有一铲。\n");
                set("unit", "把");
                set("value", 100);
		set("material", "iron");
//		set("no_drop", 1);
//		set("no_sell", "这样东西不能买卖。\n");
        }
	init_dagger(15);
	set("wield_msg", "$N操起$n握在手里。\n");
//	set("unwield_msg", "$N收起$n，$n顿时一闪不见了。\n");		

	setup();
}