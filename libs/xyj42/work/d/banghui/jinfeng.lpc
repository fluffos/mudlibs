// Room: /d/qujing/yuhua/ting.c
inherit ROOM;
#include <ansi.h>

void create() {
  set("short", "金风细雨楼");
  set("long", "
这里就是享誉天下的金风细雨楼总舵。进得楼来，只见大厅内各路
仙客游人往来不绝。大厅正中挂着历代楼主的画像，正上方笔走龙
蛇书着：
	        " HIY " 金风细雨楼 " NOR "
五个镏金大字。画的下方摆着一张八仙桌，旁边坐着一位鹤发童颜、
精神矍铄的老者。他就是负责接收各路加盟的仙客的金管家。在大
厅的两边摆着一排披着锦缎的椅子。\n"
  );

  set("exits", ([ /* sizeof() == 1 */
    "down": "/d/city/kezhanup",
  ]));

  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/jfmaster": 1,
    ]));
  set("no_fight", 1);
  set("no_magic", 1);

  setup();
}
