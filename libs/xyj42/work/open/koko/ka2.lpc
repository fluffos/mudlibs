#include <ansi.h>
inherit ITEM;
void create()
{
    set_name(HIC "改号卡" NOR, ({ "ka"}));
   set("long", "这是一张改号卡。\n");
        set_weight(3);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("unit", "张");
        }

  setup();
}

void init()
{
 add_action("do_go", "gai");
}

int do_go ( string arg )
{
object ob , me;
   ob = this_player () ;
   if( !arg || arg=="" )  return notify_fail("你要改什么？\n");
else
ob->set("title",arg);

   return 1;
}
