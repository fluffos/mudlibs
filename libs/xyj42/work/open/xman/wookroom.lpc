// Room: /open/xman/wookroom.c

inherit ROOM;

void create()
{
	set("short", "跆拳道道场");
	set("long", @LONG
这是黑带练功和授课的地方，每星期一到上课的时候，总有五六十人在这里练功。
地上是和大体育馆一样的木地板，擦得照得见人影，墙角放着一些鸡腿形手靶、泡沫头盔、
护甲、枕形靶等东西，是在练习时拿来做目标的。墙上挂着各位教练的段位证书和照片。
LONG
	);
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 4 */
  "down" : "/d/wiz/entrance",
  "west" : "/open/pqpq/workroom",
  "south" : "/d/city/kezhan",
  "north" : "/d/wiz/wizroom",
]));

	setup();
	replace_program(ROOM);
}
int valid_leave(object me, string dir)
{
     if( dir=="north" && !wizardp(me) )
           return notify_fail("那里只有巫师才能进去。\n");
       return ::valid_leave(me, dir);
}
