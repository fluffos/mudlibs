// black_vest.c

#include <armor.h>
#include <ansi.h>

inherit CLOTH;

void create() {
  set_name(HIW "银丸" NOR, ({ "yin wan" }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "西行第二的奖品 用法：boost\n");
    set("unit", "颗");
    set("value", 400000);
  }

  set("is_monitored", 1);
  setup();
}

int query_autoload() { return 1; }
void init() {
  if (!wizardp(this_player())) {
    set("no_get", "你手里一松，金丹从指间滑落！\n");
    set("no_give", "这么珍贵的宝贝，哪能随便给人？\n");
    set("no_drop", "这么宝贵的丹药，扔了多可惜呀！\n");
  }

  if (this_player() == environment())
    add_action("boost_skill", "boost");
}

int boost_skill() {
  object ob, *list, couple_ob;
  string f_ski, s_ski, t_ski;
  int f_lvl = 0, s_lvl = 0, t_lvl = 0;
  mapping skl, lrn, map;
  string *sname, *mapped, target, cardname;
  int i, marry_flag;

  ob = this_player();
  /*
        skl = ob->query_skills();
        if(!sizeof(skl)) {
                write( "你目前并没有学会任何技能。\n");
                return 1;
        }
        sname  = sort_array( keys(skl), (: strcmp :) );

        map = ob->query_skill_map();
        if( mapp(map) ) mapped = values(map);
        if( !mapped ) mapped = ({});

        lrn = ob->query_learned();
        if( !mapp(lrn) ) lrn = ([]);

       for(i=0; i<sizeof(skl); i++) {
		if(skl[sname[i]] >= f_lvl) 
		{
			s_lvl = f_lvl;
			s_ski = f_ski;
			f_lvl = skl[sname[i]];
			f_ski = sname[i];
		}
	else if( skl[sname[i]] >= s_lvl)
		{
			s_lvl = skl[sname[i]];
                        s_ski = sname[i];
		}
					}
	if(!s_ski) s_ski = f_ski;
	ob->set_skill(s_ski, (int)s_lvl+1);
	message_vision("$N的"+to_chinese(s_ski)+"增强了。\n", this_player());
*/
  ob->add("combat_exp", 10000);
  message_vision("$N服下一粒药丸，顿时道行增加了。\n", this_player());
  destruct(this_object());
  return 1;
}
