#include <ansi.h>
inherit NPC;
#include "/d/REGIONS.h";

void create()
{
	set_name("一个男人", ({ "male","nanren" }));
	set("long", 
		"这只是一个男人而已。\n");
	set("gender", "男性");
	set("age", random(10) + 25);
	set("combat_exp", 1000);
	set("title", HIR"江洋大盗"NOR);
	JOBNAME_D->set_random_name(this_object());
	setup();
        carry_object("/clone/misc/cloth")->wear();
        add_money("silver", 50);
}
#include "/task/guanfu/npc.h"