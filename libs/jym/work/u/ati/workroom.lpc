inherit ROOM;

void create() {
  set("short", "办公室");
  set("long", @LONG
这里是ati工作，休息，发呆，偷懒的地方。
LONG);

  set("item_desc",
    ([
      "办公室": "
请自行编辑和换行
\n",
    ]));

  set("exits", ([
    "c1": "/d/wizard/center",
    "c2": "/d/wizard/center2",
  ]));

  set("no_fight", 1);
  set("no_kill", 1);
  set("sleep_room", 1);
  set("no_steal", 1);
  set("valid_startroom", 1);

  setup();
}
