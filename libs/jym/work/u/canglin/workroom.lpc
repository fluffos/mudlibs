inherit ROOM;

void create() {
  set("short", "办公室");
  set("long", @LONG
这里也许就是canglin的办公室。
LONG);

  set("item_desc",
    ([
      "办公室": "
请自行编辑和换行
\n",
    ]));

  set("exits", ([
    "zd": "/d/wizard/test_room",
    "kd": "/d/city/kedian",
  ]));

  set("no_fight", 1);
  set("no_kill", 1);
  set("sleep_room", 1);
  set("no_steal", 1);
  set("valid_startroom", 1);

  setup();
  replace_program(ROOM);
}
