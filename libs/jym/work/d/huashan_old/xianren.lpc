//Cracked by Roath
// final@hl 再自己单机mud更新
// 目的是为了个人用，禁止用来架站
// 如果有问题请mail kies9999@sina.com
// 或者http://finalmud.126.com

inherit ROOM;

void create()
{
        set("short", "仙人砭");
	set("long", @LONG
    仙人砭道不盈尺，下有深沟。从北峰南望，从三峰下来的游人，在此
处进入了白云飘渺之中，行走于悬崖之上，有如仙人一般，故名之。
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
  		"northdown" : __DIR__"husun",
  		"southup" : __DIR__"shangtian",
	]));

        set("objects", ([
//	"/d/village/npc/poorman" : 1,
        ]));

        set("no_clean_up", 0);
        set("outdoors", "xx" );

	set("cost", 2);
        setup();
        replace_program(ROOM);
}
 
