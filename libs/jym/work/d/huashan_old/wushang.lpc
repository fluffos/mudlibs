//Cracked by Roath
// final@hl 再自己单机mud更新
// 目的是为了个人用，禁止用来架站
// 如果有问题请mail kies9999@sina.com
// 或者http://finalmud.126.com

inherit ROOM;

void create()
{
        set("short", "无上洞");
	set("long", @LONG
    此洞面北而开，约十余丈见方。洞内壁上有题刻多处，都是斑斑驳驳。
从此向南便可到华山派总所玉女峰，经常可以看到佩剑的华山弟子三五成
群地走过。
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
  		"southwest" : __DIR__"yunu",
  		"east" : __DIR__"nantian",
	]));

        set("objects", ([
		__DIR__"npc/s-houzi" : 1,
        ]));

        set("no_clean_up", 0);
        set("outdoors", "xx" );

	set("cost", 2);
        setup();
        replace_program(ROOM);
}
 
