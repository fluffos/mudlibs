//Cracked by Roath
// qingke.c
// final@hl 再自己单机mud更新
// 目的是为了个人用，禁止用来架站
// 如果有问题请mail kies9999@sina.com
// 或者http://finalmud.126.com
inherit ROOM;

void create()
{
        set("short", "聚仙台");
	set("long", @LONG
    来到山腰下方一块大黑石上的平坦之处。台上有石圆桌一张，石墩六
个，周围环以短石栏柱及石栏板，皆雕凿而成，造型玲珑，布局精巧。相
传昔时常有仙人羽客栖集于此，因称“聚仙台”。
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
		"north" : __DIR__"xianrenqiao",
	]));

        set("objects", ([
//		__DIR__"npc/youke" : 2,
        ]));

        set("no_clean_up", 0);
        set("outdoors", "xx" );

	set("cost", 2);
        setup();
        replace_program(ROOM);
}
 
