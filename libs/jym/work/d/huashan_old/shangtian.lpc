//Cracked by Roath
// final@hl 再自己单机mud更新
// 目的是为了个人用，禁止用来架站
// 如果有问题请mail kies9999@sina.com
// 或者http://finalmud.126.com

inherit ROOM;

void create() {
  set("short", "上天梯");
  set("long", @LONG
    正当“山穷水尽疑无路”之际，忽见铁链下垂，笔直的石壁上凿有三
十六石级，开同上天之梯。游人上下，须手抓铁链，紧贴崖壁，侧身以行。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "northdown": __DIR__ "xianren",
    "southup": __DIR__ "yanwang",
  ]));

  set("objects", ([
    "/d/village/npc/poorman": 1,
  ]));

  set("no_clean_up", 0);
  set("outdoors", "xx");

  set("cost", 2);
  setup();
}
