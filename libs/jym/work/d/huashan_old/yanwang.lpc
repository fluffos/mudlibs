//Cracked by Roath
// final@hl 再自己单机mud更新
// 目的是为了个人用，禁止用来架站
// 如果有问题请mail kies9999@sina.com
// 或者http://finalmud.126.com

inherit ROOM;

void create() {
  set("short", "阎王砭");
  set("long", @LONG
    这里石崖壁立、下临深潭，河水墨绿、深不可测。行人莫不胆颤，骡
马亦惊疑不前，跌崖落水时有发生。　　
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "northdown": __DIR__ "shangtian",
    "southup": __DIR__ "canglong",
  ]));

  set("objects", ([
    //		__DIR__"npc/s-houzi" : 1,
  ]));

  set("no_clean_up", 0);
  set("outdoors", "xx");

  set("cost", 2);
  setup();
  replace_program(ROOM);
}
