// zhenyue.c
// final@hl 再自己单机mud更新
// 目的是为了个人用，禁止用来架站
// 如果有问题请mail kies9999@sina.com
// 或者http://finalmud.126.com
#include <ansi.h>;
inherit ROOM;
void create()
{
        set("short", "无法形容的地方");
	set("long", @LONG
   这里什么也没有，中间有一片泥潭，(这就是
xkx这次更新华山的quest的中心,具体还请你到x
kx中去体会(final@hl))。
LONG
        );
	    set("exits", ([
                "left" : __DIR__"jing",
        ]));       


	set("cost", 1);
        setup();
}
