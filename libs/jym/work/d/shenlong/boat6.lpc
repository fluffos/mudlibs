inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIR "炮船船舱" NOR);
  set("long", @LONG
黑黑的船舱，偶尔有一两只老鼠跑过。
这样的地方肯定有好东西，你不如找找(search)看。      
LONG);

  set("outdoors", "shenlong");
  set("exits", ([
    "south": __DIR__ "boat5",
    "up": __DIR__ "boat3",
  ]));
  set("objects", ([
    "/d/shenlong/npc/qingbing2": 1,
    "/d/shenlong/npc/qingbing4": 1,
    "/d/shenlong/npc/qingbing6": 1,
  ]));
  set("cost", 1);
  setup();
  replace_program(ROOM);
}
