// finalxkx
// final
// /d/final/pingyuan.c

inherit ROOM;
#include <ansi.h>

void create()
{
	set("short", "平原");
	set("long", @LONG
	一块广阔的平原,大的一眼望不到边,据说
平原的另一头是天云派的根据地。
		LONG
	);

	set("item_desc", ([

	set("exits", ([
		"west" : __DIR__"dating",
	]));

	set("objects", ([
		"obj/dandinghe" : 1,
	]));

	set("cost", 1);

	setup();
	replace_program(ROOM);
}
