// finalxkx
// final
// /d/final/pingyuan.c

inherit ROOM;
#include <ansi.h>

void create()
{
	set("short", "平原北");
	set("long", @LONG
	终于走到了平原的另一头,已经能看见起伏不平
的山峦,东北方就是天云派总坛,北方是一个小村子,
东面一条小路，不知道通向哪里。
LONG
	);
	set("outdoors", "muder");
	set("exits", ([ 
		"east" : __DIR__"xiaolu",
//		"north" : __DIR__"cunlu",
        "northeast" : __DIR__"dadao",
		"south" : __DIR__"pingyuan6",
                      ]));
        set("objects", ([
//增加npc
		]));
		set("cost", 1);
	setup();
        replace_program(ROOM);
}

