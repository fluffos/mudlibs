// finalxkx
// final
// /d/final/pingyuan.c

inherit ROOM;
#include <ansi.h>

void create()
{
	set("short", "平原");
	set("long", @LONG
	一块广阔的平原,大的一眼望不到边,据说
平原的另一头是天云派的根据地。
LONG
	);
	set("outdoors", "muder");
	set("exits", ([ 
        "southeast" : __DIR__"xiaodian",
        "east" : __DIR__"pingyuan2",
		"west" : __DIR__"pingyuan"+(random(4)+1),
        "north" : __DIR__"pingyuan"+(random(4)+1),
        "southwest" : "/d/changbai/damk",
                      ]));
        set("objects", ([
		__DIR__"npc/dandinghe" : 3,
		]));
	 set("cost", 2);
	setup();
      //  replace_program(ROOM);
}

