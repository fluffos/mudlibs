//Cracked by Roath
// 擂台
// sdong 09/28/98

#include <ansi.h>

inherit ROOM;


void create()
{
	set("short",HIG"擂台"NOR);
	set("long", HIY"用来比试武艺的擂台，正前方竖着一排兵器架，台下围满了看热闹的人。\n"NOR	);
	set("invalid_startroom", 1);
	set("no_death", 1);


		  set("exits", ([
					 "down":__DIR__"guangchang",
		  ]));

		setup();
}

