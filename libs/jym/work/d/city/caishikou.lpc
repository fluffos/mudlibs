#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "菜市口");
  set("long", @LONG
这里是扬州大广场北方，再往北走是一个大集市。这里也是扬州秋决行刑之
所。因此满场都有肃杀之气。在广场中央的告示墙上，贴了几张官府悬赏捉拿朝
廷要犯的海捕文书，下面围观了许多看热闹的人，当中不乏一些武林高手，但是
要捉拿的要犯都很扎手，很少有人随便揭榜(jie ban)。但也有不少武林后起之秀
在一旁跃跃欲试。
LONG
  );
  set("outdoors", "city");

  set("exits", ([
    "west": __DIR__ "yizhan",
    "east": __DIR__ "tianbaoge",
    "northwest": __DIR__ "majiu",
    "south": __DIR__ "guangchang",
    "north": __DIR__ "beidajie1",
  ]));
  set("objects", ([
    __DIR__ "npc/yayi2": 1,
  ]));

  set("cost", 1);
  setup();
  //	replace_program(ROOM);
}

#include "/task/guanfu/job.h"
