// gaoliangfan.c

inherit ITEM;

void create() {
  set_name("高粱饭", ({ "gaoliangfan" }));
  set_weight(40);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一碗高粱饭。\n");
    set("unit", "碗");
    set("value", 40);
  }
}
