// liangong-fu.c

inherit ITEM;

void create() {
  set_name("待定", ({ "huo" }));
  set("long", "待定。\n");
  set_weight(20000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("material", "iron");
    set("value", 500);
    set("unit", "份");
  }
  setup();
}
