//sku@00-11-25 
inherit ROOM;
void create()
{
	set("short", "大路");
	set("long", @LONG
这是一条比直的大路，路的人熙熙攘攘。路边的
叫卖声不停，眼看就要到北京城了。
LONG
	);
	set("exits", ([ 
              "north" : __DIR__"lu1",
              "southup" : __DIR__"shanpo",
              ]));
	set("outdoors", "beijing");
	set("coor/x",-1090);
	set("coor/y",50);
	set("coor/z",0);
	setup();
	replace_program(ROOM);
}
