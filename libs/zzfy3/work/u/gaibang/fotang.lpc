//sku@00-11-19 
#include <room.h>
inherit ROOM;
string look_sign(object me);
void create()
{
        set("short", "破庙佛堂");
        set("long", @LONG
这是破庙的佛堂。供台上放着一座佛像。
虽然这里非常破旧，但是还是给丐帮弟子打
扫的干干净净。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "south" : __DIR__"men",
  "north" : __DIR__ "huofang",
  "east" : __DIR__ "oshi",
]));
        set("objects", ([
        __DIR__"npc/master4" : 1,
                        ]) );
        set("coor/x",20);
        set("coor/y",2110);
        set("coor/z",0);
        setup();
        call_other( "/obj/board/gaibang_a", "???" );
}
