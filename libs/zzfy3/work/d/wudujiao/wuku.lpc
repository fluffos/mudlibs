// TIE@FY3 ALL RIGHTS RESERVED
inherit	ROOM;
void create()
{
	object con,item;
	set("short", "武库");
	set("long", @LONG
这里是五毒教存放武器的地方，一个很大的兵器架上放满了刀枪剑戟。
西墙上挂着一个蛇形的兵刃，看上去闪闪发光，一看就不是普通的兵刃。
LONG
	);
	set("exits", ([	
  "east" : __DIR__"lwchang",
  "southwest" :	 __DIR__"huayuan",
]));
	set("objects", ([
	__DIR__"npc/qingshe" : 1,
	__DIR__"obj/cq"	: 1,
	__DIR__"obj/xi"	: 1,
	]) );

	set("valid_startroom", 1);
	set("coor/x",-20);
	set("coor/y",-420);
	set("coor/z",0);
	set("coor/x",-20);
	set("coor/y",-420);
	set("coor/z",0);
	set("coor/x",-20);
	set("coor/y",-420);
	set("coor/z",0);
	setup();
	con = present("西墙",this_object());
	item= present("wudu sword", this_object());
	item->move(con);

}