// TIE@FY3 ALL RIGHTS RESERVED
inherit ROOM;
void create() {
  set("short", "白蟾分舵");
  set("long", @LONG
这里是白蟾分舵，房间里布置的古朴而不是尊严，但你不难发现有一个特色，那就
是屋内都布置成了白色：白色的墙，白色的门，白色的窗...
正对面一张挂毯，上面绣着一只肥硕的白蟾蜍。

LONG
  );
  set("exits", ([
    "north": __DIR__ "garden2",
    "west": __DIR__ "dadian3",
  ]));
  set("objects", ([
    __DIR__ "npc/baichan": 1,
    __DIR__ "obj/guatan3": 1,
  ]));

  set("coor/x", -50);
  set("coor/y", -450);
  set("coor/z", 0);
  set("coor/x", -50);
  set("coor/y", -450);
  set("coor/z", 0);
  set("coor/x", -50);
  set("coor/y", -450);
  set("coor/z", 0);
  setup();
}
