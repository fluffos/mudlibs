inherit ROOM;

void create() {
  set("short", "地牢");
  set("long", @LONG
这里是一间窄小的地牢，在微弱的光线下，你隐约看到一个上面铺着一
些乾草的小床上面躺着一个人。
LONG
  );
  set("objects", ([
    __DIR__ "npc/master": 1,
  ]));
  set("coor/x", -70);
  set("coor/y", -430);
  set("coor/z", -10);
  setup();

}
