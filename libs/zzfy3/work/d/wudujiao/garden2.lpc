inherit ROOM;
#include <room.h>
void create()
{
	set("short", "青蛇分舵");
	set("long", @LONG
这里是青蛇分舵，房间里布置的古朴而不是尊严，但你不难发现有一个特色，那就
是屋内都布置成了绿色：绿色的墙，绿色的门，绿色的窗...
正对面一张挂毯，上面绣着一只形神兼备的青蛇。

LONG
	);
	set("exits", ([	
  "east" : __DIR__"garden",
  "south" : __DIR__"dadian4",
  "north" : __DIR__"dadian1",
])); 
   set("objects", ([
		__DIR__"obj/guatan4" : 1,
       ]) );

	set("outdoors","wudujiao");
	create_door("east", "檀木门", "west", DOOR_CLOSED);
	set("coor/x",-50);
	set("coor/y",-430);
	set("coor/z",0);
	set("coor/x",-50);
	set("coor/y",-430);
	set("coor/z",0);
	set("coor/x",-50);
	set("coor/y",-430);
	set("coor/z",0);
	setup();
}