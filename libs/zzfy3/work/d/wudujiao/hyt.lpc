// Copyright (C) 1995, by Tie Yu and Daniel Yu.	All rights reserved.
// This	software can not be used, copied, or modified in any form without
// the written permission from authors.
inherit	ROOM;
void create()
{
	set("short", "议会厅");
	set("long", @LONG
这里是五毒教的议会厅，一圈檀木椅子有序的排列着，五毒教每当有事情要商量的
时候都会召集教徒来这里商议。
LONG
	);
	set("exits", ([	/* sizeof() == 4 */
  "north" : "/d/wudujiao/dadian2",
  "south" : "/d/wudujiao/dadian3",
  "west" : "/d/wudujiao/mroom",
]));

	set("outdoors",	"wudujiao");
	set("coor/x",-60);

	set("coor/y",-430);
	set("coor/z",0);
	set("coor/x",-60);
	set("coor/y",-430);
	set("coor/z",0);
	set("coor/x",-60);
	set("coor/y",-430);
	set("coor/z",0);
	setup();
	replace_program(ROOM);
}