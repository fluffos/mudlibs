inherit	ITEM;
void create()
{
	set_name("婴儿", ({ "kid"}) );
	set_weight(100);
	set_max_encumbrance(1000);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "个");
		set("long", "一个不满周岁正在啼哭的婴儿。\n");
		set("value", 0);
	}
}
int is_container() { return 1; }