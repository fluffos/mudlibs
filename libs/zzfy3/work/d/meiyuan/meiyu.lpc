inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIC "梅雨轩" NOR);
  set("long", @LONG
这里是主人接待客人的地方。宽敞的大厅，干净的桌椅
，富丽堂皇的装饰。门的两边摆了一地的鲜花，芳香袭人。
屋顶挂着一盏纯银的吊灯。一到晚间，灯火辉煌，其景也
不亚于皇宫别院。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "southdown": __DIR__ "taijie3",
    "north": __DIR__ "xifeng"

  ]));

  set("coor/x", -640);
  set("coor/y", -490);

  set("coor/z", 30);
  setup();
  replace_program(ROOM);
}
