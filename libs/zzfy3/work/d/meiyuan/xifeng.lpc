inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIC "栖凤阁" NOR);
  set("long", @LONG
这里是女主人平常休息的地方。也在此招待客人。这间
阁给的的感觉就是暖，温暖。地上铺着大红色的波斯羊绒地
毯,宽大的七彩锦缎窗帘一直垂到地上。阳光一丝都透不进来
，百于根手臂粗细的龙凤蜡烛把屋里照的通亮。
LONG
  );
  set("valid_startroom", 1);
  set("exits", ([ /* sizeof() == 4 */

    "south": __DIR__ "meiyu",
    "north": __DIR__ "xuanyuan",

  ]));


  set("coor/x", -640);
  set("coor/y", -490);
  set("coor/z", 30);
  setup();
  replace_program(ROOM);
}
