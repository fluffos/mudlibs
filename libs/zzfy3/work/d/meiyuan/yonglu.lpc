inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIY "甬路" NOR);
  set("long", @LONG
一条通向梅林的小路。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "meilin",
    "west": __DIR__ "huayuan",
  ]));

  set("coor/x", -640);
  set("coor/y", -490);
  set("coor/z", 30);
  setup();
  replace_program(ROOM);
}
