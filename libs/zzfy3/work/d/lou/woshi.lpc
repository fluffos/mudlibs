inherit ROOM;
void create() {
  set("short", "卧室");
  set("long", @LONG
这是楼主未婚妻雷纯的卧室，收拾的干干净净。屋里散发着淡淡的脂粉气，
让人产生一种昏昏欲睡的感觉。中间一张象牙雕成的床，甚是扎眼，精巧绝伦。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "dating",
  ]));
  set("objects", ([
    __DIR__ "npc/master": 1,
  ]));

  set("coor/x", 50);
  set("coor/y", -25);
  set("coor/z", 0);
  setup();

  replace_program(ROOM);
}
