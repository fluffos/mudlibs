// square.c

inherit ROOM;

void create() {
  set("short", "蜀中唐门");
  set("long", @LONG
这里是江湖上人称暗器冠绝天下的唐门。只见四个大字
                                        ==
   ==============    === === ===   ===========   = ===========
   ==   =  =   ==    === === ===   ==  ======    ==         ==
   ==   =  =   ==    === === ===   == =======    ==         == 
   ==  ==  ==  ==    === === ===   ==  ======    ==         ==
   == ==    == ==    === === ===   ==    ==      ==         == 
   ====      ====   ===  === ===   ==  ======    ==         ==
   ==          ==  ===   === ===  ==   =    =    ==       = ==
   ============== ===    === === ==    ======    ==         ==
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "northdown": __DIR__ "xiaozhen",
    "southup": "/u/tangmen/road1",
  ]));
  set("objects", ([
    "/u/tangmen/npc/peng": 1,
  ]));




  set("no_clean_up", 0);
  set("outdoors", "huashan");
  set("coor/x", -1220);
  set("coor/y", 20);
  set("coor/z", -80);

  set("coor/x", -1220);
  set("coor/y", 20);
  set("coor/z", -80);
  setup();
}
