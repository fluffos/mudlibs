#include <ansi.h>
inherit ROOM;
void create() {
  set("short", YEL "御书房" NOR);
  set("long", @LONG
御书房是当今皇帝读书的地方，这里的藏书之丰富，种类之完全
都是普通人无法想象的。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "lthoujie",
  ]));
  set("objects", ([
  ]));

  set("outdoors", "fengyun");
  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
}
