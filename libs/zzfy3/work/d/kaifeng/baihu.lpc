inherit	ROOM;
void create()
{
	 set("short", "清风细雨楼的白虎堂");
	set("long", @LONG
这里是楼中要地，他管辖着楼的南七省生意。屋中华丽无比，古玩甚多。
好多忙来忙去，但井然有序。堂主是早已在武林中成名多年的燕北飞。掌法
独步天下，罕有对手。
LONG
	);
	set("exits", ([	/* sizeof() == 4 */
     "north" : __DIR__"yanwu",
    "west" : __DIR__"dating"
]));	    
/*        set("objects", ([
	__DIR__"npc/master2" : 1,
			]) );
*/
	set("outdoors",	"fengyun");
	set("coor/x",55);

	set("coor/y",-20);
	set("coor/z",0);
	setup();
	replace_program(ROOM);
}
