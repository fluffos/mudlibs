#include <ansi.h>
inherit ROOM;
void create() {
  set("short", YEL "兵部正门" NOR);
  set("long", "        
    这里是兵部正门，门口标枪似的站着几个卫兵，除了当兵的，别人
根本无法入内，大门正上挂着当今皇上的御笔\n" HIR + "
                大　　宋　　兵　　部\n\n" NOR
  );
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "bingbu",
    "east": __DIR__ "yujie1",
  ]));
  set("objects", ([
  ]));

  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
  replace_program(ROOM);
}
