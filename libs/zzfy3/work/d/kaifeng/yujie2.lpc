#include <ansi.h>
inherit ROOM;
void create() {
  set("short", "御街");
  set("long", @LONG
这里是皇城汴梁的北大街，因向北正对皇宫，故称御街,这条大街
两边是朝廷的各个衙门。这里东面是朝廷兵部大堂，西面就是朝廷
吏部。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "yujie3",
    "north": __DIR__ "yujie1",
    "west": __DIR__ "libu",
  ]));
  set("objects", ([
  ]));

  set("outdoors", "fengyun");
  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
  replace_program(ROOM);
}
