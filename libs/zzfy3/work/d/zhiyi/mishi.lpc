inherit ROOM;
void create() {
  set("short", "密室");
  set("long", @LONG
这就是紫衣侯的练功密室了，四面是精铁所制，只有顶上有一个很小
的通风口。你这才发现这门从里面是打不开的！常人到了这里是根本无法
出去的了。想来紫衣侯每次进来门外都是有个侍卫的。屋里空荡荡的什么
也没有。四壁的墙上密密麻麻地刻满了文字和图像，象是武功心法,可惜
由于时日太久，很多都看不清了，只有东西两面墙上的还依稀可辨。
LONG
  );
  set("no_fight", 1);
  set("no_magic", 1);
  set("no_spells", 1);
  set("objects", ([
    __DIR__ "obj/dongqiang": 1,
    __DIR__ "obj/xiqiang": 1,
  ]));
  set("coor/x", 1010);
  set("coor/y", -300);
  set("coor/z", 0);
  setup();
}
