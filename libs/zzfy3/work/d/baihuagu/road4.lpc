// Copyright (C) 1995, by Tie Yu and Daniel Yu. All rights reserved.
// This software can not be used, copied, or modified in any form without
// the written permission from authors.
inherit ROOM;
void create()
{
        set("short", "山中小路");
        set("long", @LONG
眼前是一条山中小路，道路蜿蜒曲折，如羊肠盘旋，要是不认真辨认真不
容易发现......普天之下无论谁到了这里，也不能不说明有十足的运气。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "south" : __DIR__"huacong",
  "north" : __DIR__"road2",
]));

        set("outdoors", "fengyun");
        set("coor/x",110);
        set("coor/y",990);
        set("coor/z",0);
        set("coor/x",110);
        set("coor/y",990);
        set("coor/z",0);
        setup();
        replace_program(ROOM);
}