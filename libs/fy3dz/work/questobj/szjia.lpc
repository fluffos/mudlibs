#include <ansi.h>
inherit ITEM;
void create() {
  set_name(HIY "十字架" NOR, ({ "szjia" }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "个");
    set("material", "steel");
    set("long", "一个十字架\n");
  }

  setup();
}
