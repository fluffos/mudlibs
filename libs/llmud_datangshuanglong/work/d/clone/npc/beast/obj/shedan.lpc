inherit ITEM;
inherit F_FOOD;

void create() {
  set_name("蛇胆", ({ "she dan", "dan" }));
  set_weight(50);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "这是一枚蛇胆。\n");
    set("unit", "枚");
    set("food_remaining", 40);
    set("food_supply", 40);
  }
}
