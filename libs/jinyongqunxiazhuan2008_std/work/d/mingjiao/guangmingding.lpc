//htqdating.c
#include <ansi.h>
inherit ROOM;

void create()
{
	set("short", HIC"光明顶"NOR);
	set("long", @LONG
这里是明教光明顶。
LONG);
	set("exits", ([
		"down" : __DIR__"lhqpaifang",
	]));
	set("objects", ([
		"/kungfu/class/mingjiao/zhangwuji": 1,
	]));
	set("no_clean_up", 0);
	setup();
	replace_program(ROOM);
}