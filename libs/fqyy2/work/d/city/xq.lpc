// 神话世界·西游记·版本４．５０
/* <SecCrypt CPL V3R05> */

inherit ROOM;

void create() {
  set("short", "象棋室");
  set("long", @LONG

这是风起云涌的象棋室，你可以在这里稍微放松一下
从险恶的江湖中挣脱出来也不容易。

LONG);
  set("objects", ([ /* sizeof() == 3 */
    __DIR__ "obj/cch": 1,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "south": "/d/city/duchang",
  ]));

  set("no_fight", 1);
  setup();
}
