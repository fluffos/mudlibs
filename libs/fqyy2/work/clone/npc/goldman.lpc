#include <ansi.h>
inherit NPC;
void create()
{
        set_name(HIR"李登辉"NOR, ({"li denghui", "li", "denghui"}));
        set("age", 25);
        set("attitude", "aggressive");
set("combat_exp",2000);
        setup();
        add_money("gold",5);
}
