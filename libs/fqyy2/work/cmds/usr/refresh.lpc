// refresh.c by super at 2001

inherit F_CLEAN_UP;
inherit F_FROZEN;

int main(object me, string arg)
{
	refresh_screen_all(me);
	write("状态栏更新完毕！\n");
	return 1;
}

int help(object me)
{
     write(@HELP
指令格式: refresh

	更新你的状态栏。

HELP
    );
    return 1;
}
