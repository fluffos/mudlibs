// a skeleton for user rooms
inherit ROOM;
void create()
{
set("short", "任务室");
set("long", @LONG
这是无忧仙境考验门下弟子的地方
门下弟子可以到这里来向任务使领去任务，以彰其忠诚。

LONG
);
        set("exits", ([
"east" : "/p/residence/waaa1014217164.c",
]));
set("objects", ([
	"/p/npc/shi":1,
       "/p/npc/weishi":2,
       ]) );
	set("owner", "waaa");
	set("class", "无忧仙境　");
//	set("no_fight",1);

setup();
}
