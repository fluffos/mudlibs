#include <ansi.h>
inherit ITEM;
void create()
{
    set_name("还阳补血丹", ({ "buxue dan" }) );
    set_weight(1);
    if( clonep() )
            set_default_object(__FILE__);
    else {
            set("unit", "枚");
            set("long", "一枚还阳补血丹\n");
          }

    setup();
}
