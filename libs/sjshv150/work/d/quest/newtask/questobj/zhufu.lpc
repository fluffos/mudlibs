#include <ansi.h>
inherit ITEM;
void create() {
  set_name(HIG "绿玉竹符" NOR, ({ "lvyu zhufu" }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "只");
    set("long", "一只绿玉竹符\n");
  }

  setup();
}
