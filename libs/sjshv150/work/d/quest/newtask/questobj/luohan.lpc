#include <ansi.h>
inherit ITEM;
void create() {
  set_name("铁罗汉", ({ "tie luohan" }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "只");
    set("long", "一只铁罗汉\n");
  }

  setup();
}
