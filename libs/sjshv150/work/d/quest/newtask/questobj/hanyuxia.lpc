#include <ansi.h>
inherit ITEM;
void create() {
  set_name("玄阴寒玉匣", ({ "hanyu xia" }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "只");
    set("long", "一只玄阴寒玉匣\n");
  }

  setup();
}
