#include <ansi.h>
inherit ITEM;
void create()
{
    set_name("十香软筋散", ({ "ruanjin san" }) );
    set_weight(1);
    if( clonep() )
            set_default_object(__FILE__);
    else {
            set("unit", "瓶");
            set("long", "一瓶十香软筋散\n");
          }

    setup();
}
