// Created by kuku Mar 1 2003

inherit ITEM;

void create() {
  set_name("魔头", ({ "mo tou", "tou" }));
  set_weight(1000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一只断掉的魔王头。\n");
    set("unit", "只");
    set("material", "bone");
  }
  setup();
}
