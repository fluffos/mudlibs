//tianpei 天篷
//by calvin and leox 共同开发
//版权所有,未经许可不得翻录

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "天蓬园林");
  set("long", @LONG
这是通往元帅府内室的走廊!两边的柱子上雕龙画凤,
一看就有仙家气派.......
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));


  set("exits",
    ([  //sizeof() == 4
    "north": "/d/leox/tianpeng/zoulang3",
      "south": "/d/leox/tianpeng/zoulang1",
    ]));

  set("objects",
    ([  //sizeof() == 1
    ]));

  //        set("outdoors", "changan");
  set("no_clean_up", 0);
  setup();
}
