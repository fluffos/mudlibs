//tianpei 天篷
//by calvin and leox 共同开发
//版权所有,未经许可不得翻录

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", "天蓬府大门");
        set ("long", @LONG
这是天蓬大元帅的府第,威严的大门口站着几个把守的神兵
旁边的石柱瘢痕累累,但是非常之气魄,门前的大匾上龙飞凤舞的
写着几个大字:
           ********************
           ***天*蓬*元*帅*府***
           ********************
LONG);

        //set("item_desc", ([" *** " : " *** ", ]));

        set("exits", 
        ([ //sizeof() == 4
  "north" :"/d/leox/tianpeng/funei1",
  "south" : "/d/leox/tianpeng/lu8",
]));

        set("objects", 
        ([ //sizeof() == 1
        "/d/kaifeng/npc/zhubing" :  2,
        ]));
i set("disconnect","changan")
=
z

        set("outdoors", "changan");
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}
