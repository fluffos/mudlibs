//tianpei 天篷
//by calvin and leox 共同开发
//版权所有,未经许可不得翻录

#include <ansi.h>
inherit ROOM;

void create()
{
        set("short", "天蓬府内");
        set ("long", @LONG
这是通往元帅府走廊的一个过道!旁边的景色煞是好看.......
LONG);

        //set("item_desc", ([" *** " : " *** ", ]));


        set("exits", 
        ([ //sizeof() == 4
  "north" :"/d/leox/tianpeng/zoulang1",
  "south" :"/d/leox/tianpeng/meng",

        set("objects", 
        ([ //sizeof() == 1
        ]));

//        set("outdoors", "changan");
        set("no_clean_up", 0);
        setup();
        replace_program(ROOM);
}
