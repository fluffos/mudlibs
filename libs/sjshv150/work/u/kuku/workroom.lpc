inherit ROOM; 
#include <ansi.h>

void create ()
{
        set ("short",HIG"太"HIR"虚"HIW"幻"HIY"境"NOR);
        set ("long","

这是一间坐落在青山绿水间的小屋，房间内陈设简单，虽然没有豪华
的家具和物品，但是却很干净整洁，装修的典雅别致，给人一种温馨
愉快有如身临人间仙境之感。\n");
        set("exits", ([ /* sizeof() == 3 */ 
        "kz" : "/d/city/kezhan",
        "wizz": "/d/wiz/wizroom",
]));
        set("objects", ([ /* sizeof() == 2 */
        "/u/kuku/npc/cat" : 1,  
        ])); 
        set("no_time", 1); 
        set("no_kill", 1); 
        set("no_fight", 1); 
        set("no_magic", 1);
        set("valid_startroom", 1); 
        setup(); 
        call_other("/obj/board/kuku_b", "???"); 
}
