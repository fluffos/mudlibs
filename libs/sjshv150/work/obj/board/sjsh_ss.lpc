inherit BULLETIN_BOARD;
#include <ansi.h>

void create() {
  set_name(HIB "三界神话BUG报告板" NOR, ({ "board" }));
  set("location", "/d/city/xqt");
  set("board_id", "sjsh_ss");
  set("long", "这个留言板是为发现bug的玩家汇报给wiz的地方。\n");
  setup();
  set("capacity", 400);
  replace_program(BULLETIN_BOARD);
}
