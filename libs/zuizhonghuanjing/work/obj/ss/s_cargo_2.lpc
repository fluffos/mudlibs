// ss_s_cargo_2.c

#include <room.h>

inherit SS_ROOM;

void create()
{
	set("short", "轟天號貨艙");
	set("long", "這只是艘最簡陋的小型宇宙貨船.\n");

	set("exits", ([ /* sizeof() == 1 */
		"up"		: __DIR__"s_cargo_1",
        ]) );

	setup();
}
