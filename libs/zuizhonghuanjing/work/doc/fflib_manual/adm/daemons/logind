
ES2 mudlib - FF extension v0.6c 函式說明書

程式: /adm/daemons/logind.c

	此物件處理 login 手續, 與 /obj/login.c 及 /obj/user.c 有關.
	目前的 logind.c 是由 v1.1e 版的 ES2 lib 修改而來, 可能有
	security 漏洞.

	login 的流程是 driver 在要求連線時, 呼叫 /adm/obj/master.c
	的 connect() 函式, 再由 connect() 傳回的物件 (複製的
	/obj/login.c), 呼叫該傳回物件中的 logon() 函式. logon() 函式
	再呼叫 /adm/daemons/logind.c 的 logon() 函式以取得 login 手續
	所需的資料.

void logon( object ob )

	由 /obj/login.c 的 logon() 函式在連線手續時呼叫. 由於接下來的
	手續都使用 input_to() 外部函式處理, 所以此函式只是輸入資料的
	第一步驟.
	底下的幾個函式, 看中文訊息就知道在做啥了.

object make_body( object ob )

	如果人物已經存在, 取得密碼之後就會呼叫此函式製造身體.
	如果是新創造的人物, 取得 email 之後, 就會呼叫此函式製造身體.

private void init_new_player( object user )

	如果是新創造的人物, 呼叫 enter_world() 之前會呼叫此函式設定
	一些起始資料.

varargs void enter_world( object ob, object user, int silent )

	資料填完了就呼叫此函式進行把玩家物件搬到遊戲世界中.

varargs void reconnect( object ob, object user, int silent )

	處理斷線後重新連線的過程.

int check_legal_id( string id )

	檢查 id 取名的規定.

int check_legal_name( string name )

	檢查中文名字的取名規定.

object find_body( string name )

	如果需要找尋已在線上的使用者. 會呼叫此函式.

int set_wizlock( int level )

	限制 login 使用者權限時, 會呼叫此函式.

string wiz_lev( int lev )

	查詢各權限數字相對的中文名稱.

Spock of The Final Frontier	98.Sep.20.
