
ES2 mudlib - FF extension v0.6c 函式說明書

程式: /adm/simul_efun/file.c

void cat( string file )

	模擬 UNIX 的 cat 指令, 讀檔, 顯示內容.

void log_file( string file, string text )

	在事先定義的系統紀錄目錄中, 把 text 加進名為 file 的檔案中.

void assure_file( string file )

	如果名為 file 的檔案不存在, 就製造出此檔案的路徑.
	用來確保一個檔案所需要的路徑存在.

string base_name( object ob )

	傳回一個物件的檔案名稱, 去掉 file_name() 在複製物件的檔案名稱
	後面加上的編號.
 
Spock of The Final Frontier	98.Sep.16.
