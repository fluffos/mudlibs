
ES2 mudlib - FF extension v0.6c 函式說明書

程式: /adm/simul_efun/dump_variable.c

varargs string dump_variable(mixed arg, int indent, int flag)

	根據 arg 的資料型態, 將該變數內容或該物件設定的變數傾印出來.
	如果 arg 是陣列或映射變數, indent 設為非零值, 會傾印該變數內容.
	如果 arg 是物件, flag 不為零, 則會傾印物件以 set 和 set_temp
	設定的 dbase 資料.

static int sort_keys( string one, string two )

	用 strcmp() 外部函式比較兩個字串.

static int filter_props( string prop, object target )

	看來已經失去功能的函式.

Spock of The Final Frontier	98.Sep.16.
