	MudOS v21c2 原文文件中文翻譯 v2			98.Feb.3.

	﹝此份文件不包括 doc/efuns, 請另外索取 cefuns 中文翻譯文件﹞


	此份文件加上譯者日前所公開的 efuns  文件翻譯, 大致上已將 MudOS
	v21c2 之原文文件翻譯完成. 不過, 因為譯者所學不多, 難免有遺珠之憾
	. 未能翻譯之文件清單列於附註一.

	這份文件中的分類結構如下:

	doc
	├───applies			驅動程式對各個物件呼叫的函式
	│      ├───interactive	驅動程式對玩家物件呼叫的函式
	│      └───master		驅動程式對主宰物件呼叫的函式
	├───concepts		撰寫 LPC  所必備的一些概念
	├───lpc
	│      ├───constructs	LPC 程式的基本指令字
	│      ├───preprocessor	LPC 程式的前置處理區命令
	│      └───types		LPC 的變數、函式的型態定義
	└───ref			化石....:)

	文件中如有錯誤或是更好的翻譯, 請來函指正. 如果您願意翻譯附註一中
	所列之文件, 也請來函告知, 並允許譯者收錄於往後的版本中.

	此文件可以隨意流通, 但是您如果要用於公開架設之 mud  上, 請來函告
	知. 另外本文件不得用於商業營利用途.
						Spock @ FF	97.Aug.12.

附註一:
	以下二十九份檔案為譯者所力有未逮之處....:Q

	doc/concepts/oop
	doc/concepts/socket_efuns
	doc/defines 共十份文件
	doc/defines 共十一份文件
	doc/driver  共五份文件
	doc/lpc/types/substructures

附註二:
	在 ES 2 公開之 mudlib 程式中所附之 doc/ref  文件中, 已有六份文件
	是由 Wade 兄及 anslasax 兄在遠古以前所翻譯的古蹟. 這些文件都與譯
	者所翻譯者一起保留在同一個目錄中. 如果您看不慣譯者所譯之版本, 可
	有另外的選擇....

	以下由 wade 兄所翻譯:

		doc/ref/lpc.2darray
		doc/ref/lpc.driver
		doc/ref/lpc.function
		doc/ref/lpc.lpc
		doc/ref/lpc.object

	原文相同的文件:

		doc/concepts/lpc 與 lpc.lpc
		doc/concepts/MudOSdriver 與 lpc.driver
		doc/concepts/objects 與 lpc.object
		doc/lpc/types/array.2d 與 lpc.2darray
		doc/lpc/types/function 與 lpc.function

	以下文件由 anslasax 兄所翻譯: 

		ref/lpc.mud

	附註三:
		以下所採用之中文譯名, 文件中大多不再補注其英文原名.

		admin			mud 系統管理者
		administrator		admin 的原文, 但在此通常指
					  主機管理員
		Annihilator		安老大, 安尼席洛特
		argument		參數
		array			陣列
		buffer			暫存區
		character		字元
		driver			驅動程式
		efun			外部函式, external function
		element			元素
		euid			effective user id 之縮寫.
					  譯為有效使用者識別名稱.
		flag			旗標
		force, the		原力
		function		函式或函式指標
		global			全域
		heartbeat		心跳
		id			識別名稱
		information		資訊或資料
		interger		整數
		local			本域、區域
		mapping			映射
		message			訊息
		mixed			譯為「混合」, 此為 LPC  所
					  特有的資料型態, 允許此資
					  料為任何種類的資料型態.
		null			空值
		object			物件
		player			玩家
		pointer			指標
		query			詢問、查詢
		return			傳回 (動詞)
		runtime			編譯時段
		string			字串
		source code		原始程式碼
		system			系統
		target			目標
		uid			user id 之縮寫. 使用者識別
					  名稱
		value			值
		void			原意為「無」, 視情況翻譯為
					  無傳回值或無參數.
		wizard			巫師
		zero			零或零值

	v2 修訂: 修正譯者將外部函式誤植為超越函式之處.
