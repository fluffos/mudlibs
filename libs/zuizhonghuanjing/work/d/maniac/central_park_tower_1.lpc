// Room: /d/maniac/central_park_tower_1.c

#include <room.h>

inherit ROOM;

void create()
{
	set("short", "鐘塔底層");
	set("long", @LONG
你打開大門的動作, 揚起房間裡大量的灰塵. 空氣中充斥著霉味
, 顯然已經很久沒有人到過這裡了. 你的面前有一座狹窄的迴旋石梯
, 看來是通往塔頂的.
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"north" : __DIR__"central_park_4",
		"up" : __DIR__"central_park_tower_2",
]));
	set("doors", ([
		"north"	:	([
				"data" : "鐘塔大門",
				"other_side_dir" : "south",
				"status" : DOOR_CLOSED,
				"key_id" : "maniac/tower_key",
				]),
	]) );
	set("mov_cost", 10 );

//	create_door("north", "鐘塔大門", "south", DOOR_CLOSED, "maniac/tower_key");

	setup();
}
