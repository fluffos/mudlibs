// Room: /obj/f_road_1.c

inherit ROOM;

void create()
{
	set("short", "自由之路");
	set("long", @LONG
    道路往南北方向延伸, 往北銜接著橫跨梅尼亞克河的馬克菲爾大
橋 (Mackphil Bridge). 在你東邊是梅尼亞克市的中央廣場公園, 而
西邊是市立郵局第三支局.
LONG
	);
	set("exits", ([ /* sizeof() == 4 */
		"west" : __DIR__"post_office_3rd",
		"east" : __DIR__"central_park_2",
		"south": __DIR__"f_road_2",
		"north": __DIR__"f_road_3",
	]));
	set("no_clean_up", 0 );
	set("mov_cost", 20 );
	set("outdoors", 1 );

	set("note", "你聽到西南方隱隱有音樂聲傳來.\n");

	setup();
	replace_program(ROOM);
}
