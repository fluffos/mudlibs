// Room: /d/maniac/topaz_pub.c

inherit ROOM;

void create()
{
	set("short", "黃玉酒館");
	set("long", @LONG
你一走進黃玉酒館, 立刻發現這裡的裝潢老舊, 但是還算整潔.
空氣中傳來一陣酒味, 你低頭還看見一隻老鼠在你的腳邊竄過. 你懷
疑這間酒館是如何成為梅尼亞克市著名的酒館之一的呢 ?
    這裡的桌子排列成Ｕ字形，中央是一個鋪著木板的舞臺.
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
		"east"	: __DIR__"f_road_2",
	]));
	
	set("objects", ([
		__DIR__"npc/bartender" : 1,
	]));

	set("mov_cost", 15 );

	setup();
	replace_program(ROOM);
}
