// m.c

#include <room.h>

inherit SPACE;
inherit PLANET;

void create()
{
	if( clonep())
		set_default_object(__FILE__);
	else {
		set("short", "Ｍ級恆星");
		set("long", @LONG
Ｍ級恆星是質量中等的中年恆星, 穩定地進行核融合並發出能量.
LONG
		);
		set("habitability", 0 );
	}

	setup();
}
