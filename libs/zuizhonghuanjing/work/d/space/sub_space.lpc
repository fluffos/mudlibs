// Room: /d/space/sub_space.c
// We put all space ships jumping into warp in its inventory.

inherit SPACE;

void create()
{
	set("short", "子宇宙");
	set("long", @LONG
這裡是未知的子宇宙空間.
LONG
	);

	set("terrain", "space");
	setup();
}
