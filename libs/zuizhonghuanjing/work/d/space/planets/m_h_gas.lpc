// m_h_gas.c

#include <room.h>

inherit SPACE;
inherit PLANET;

void create()
{
	if( clonep())
		set_default_object(__FILE__);
	else {
		set("short", "巨型氣體行星");
		set("long", @LONG
一顆中年的巨型氣體行星. 在大氣層下有固態的地表存在.
LONG
		);
		set("habitability", 0 );
	}

	setup();
}
