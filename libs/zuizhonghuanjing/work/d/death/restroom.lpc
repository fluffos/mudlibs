// Room: /d/wiz/restroom.c

inherit ROOM;

void create()
{
	set("short", "休息室");
	set("long", @LONG
這裡是什麼也沒有的一個小房間, 整個房間籠罩在柔和的白光中.
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
		"up" : "/d/maniac/central_park_1",
	]));
	set("no_clean_up", 0);
	set("no_fight", 1);
	set("move_cost", 1);

	setup();
	replace_program(ROOM);
}
