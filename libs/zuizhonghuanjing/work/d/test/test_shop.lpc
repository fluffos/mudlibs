// Room: /d/test/test_shop.c

inherit SHOP;

void create()
{
	set("short", "測試用商店");
	set("long", @LONG
這裡是巫師用來測試用的商店.
在此你可以用 list, value, sell, buy 四個指令與商店交易.
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
		"out"		: "/d/maniac/central_park_1",
]));
	set("no_clean_up", 0);
	set("terrain", "plain");
	setup();
}
