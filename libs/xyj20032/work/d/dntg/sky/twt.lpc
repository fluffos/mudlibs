#include <ansi.h>
inherit ROOM;

void create()
{
  set ("short", HIY"天外天"NOR);
  set ("long", @LONG

这里是天外天。

LONG);

  set("exits", ([ /* sizeof() == 1 */
    "enter" : __DIR__"tst",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__"npc/hjlz" : 1,
  ]));
  setup();
}