#include <ansi.h>
inherit ROOM;

void create()
{
  set ("short", "囚室");
  set ("long", @LONG

这里是一间囚室。

LONG);

  set("exits", ([ /* sizeof() == 1 */
    "north" : __DIR__"tst",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__"npc/ttjz" : 1,
  ]));
  setup();
}