#include <ansi.h>
inherit ITEM;
void create() {
  set_name(HIR "女儿红" NOR, ({ "nverhong" }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "瓶");
    set("long", "一瓶女儿红\n");
  }

  setup();
}
