#include <ansi.h>
inherit ITEM;
void create()
{
    set_name("七星海棠", ({ "qixing haitang" }) );
    set_weight(1);
    if( clonep() )
            set_default_object(__FILE__);
    else {
            set("unit", "颗");
            set("long", "一颗七星海棠\n");
          }

    setup();
}
