#include <ansi.h>
inherit ITEM;
void create() {
  set_name("五虎保命丹", ({ "baomingdan" }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "枚");
    set("long", "一枚五虎宝命丹\n");
  }

  setup();
}
