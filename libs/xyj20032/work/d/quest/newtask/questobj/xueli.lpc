#include <ansi.h>
inherit ITEM;
void create()
{
    set_name(RED"血梨"NOR, ({ "xue li" }) );
    set_weight(1);
    if( clonep() )
            set_default_object(__FILE__);
    else {
            set("unit", "只");
            set("long", "一只血梨\n");
          }

    setup();
}
