//changan city

inherit ROOM;

void create() {
  set("short", "破旧堂屋");
  set("long", @LONG
屋子里潮湿而阴暗，屋子并不十分窄小，但只有一桌，一床，
一凳．更显得四壁箫然，空洞寂寞．也衬得那一盏孤灯更昏黄暗淡．
壁上的积尘未除，屋面上还结着蛛网．
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "north": __DIR__ "beiyin1",
    ]));
  /*
        set("objects", 
        ([ //sizeof() == 4
                __DIR__"egui/npc/egui_master" : 1,
                __DIR__"egui/npc/wen" : 1,
                __DIR__"egui/npc/ning" : 1,
//		   __DIR__"obj/huolu" : 1,
        ]));
*/
  setup();
  replace_program(ROOM);
}
