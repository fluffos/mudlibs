//changan city

inherit ROOM;

void create() {
  set("short", "曲江池");
  set("long", @LONG
这是位于长安城外的一个湖泊，水面广阔，港汊交错，湖岸曲折。曲
江池两岸各有一行杨柳碧桃。每逢春季，柳丝泛绿，桃颜带红，犹如湖中
锦带，明媚秀丽。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 2
    "southeast": "/u/maoshan/sroad",  //goto maoshan and mojiao
    "northwest": __DIR__ "qixia-men",
    ]));

  set("objects",
    ([  //sizeof() == 1
    //                __DIR__"npc/wujiang" : 1,
    //		__DIR__"npc/bing" : 3,
    ]));


  set("outdoors", "changan");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
