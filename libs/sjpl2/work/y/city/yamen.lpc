inherit ROOM;
void create() {
  set("short", "长安府衙门");
  set("long", @LONG
这里就是长安府衙，衙门口是一对威武的石狮，深红色的大门紧闭
着。大门两旁站着两排军士，不愧是天子脚下，连长安府衙都威风八面。
门上也贴着一张封条（sign）。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "qinglong-s1",
    //  "west" : __DIR__"datang",
  ]));
  set("item_desc", ([ "sign": "暂封府衙----秦王谕旨\n", ]));

  set("objects", ([
    __DIR__ "npc/yayi1": 3,
  ]));
  setup();
}
