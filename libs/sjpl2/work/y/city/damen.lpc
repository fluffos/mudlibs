//ROOM:/u/changan/bingbu/damen.c

inherit ROOM;

void create() {
  set("short", "大门");
  set("long", @LONG
这里是兵部府的大门平时是关闭的,只有迎接大人物或贵客的时候
才会打开.现在门上插着一个巨大的门杼.看来没有把子力气是打不开的.
LONG
  );
  set("exits", ([
    "west": __DIR__ "zhuque-n3",
  ]));
  set("inem_desc", ([
    "门杼": "看起来很沉重,不知道能不能推开\n",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "changan");
  setup();
  replace_program(ROOM);
}
