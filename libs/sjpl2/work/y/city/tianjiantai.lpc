//changan city

inherit ROOM;

void create() {
  set("short", "天监台");
  set("long", @LONG
这里就是长安城的天监台，为了每年都能风调雨顺，在每年三月皇上都
会到这里祭天，以求上苍保佑，这里暗合八卦之势，乃张天师早年所建，周
围还摆这一些祭天时所用的器具。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "west": __DIR__ "baihu-n3",
    ]));

  set("objects",
    ([  //sizeof() == 1
    //                __DIR__"npc/yuantiangang" : 1,
    ]));


  set("outdoors", "changan");
  set("no_clean_up", 0);
  setup();
}
