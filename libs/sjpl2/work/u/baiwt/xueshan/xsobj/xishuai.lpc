#include <ansi.h>
inherit ITEM;
void create() {
  set_name("蟋蟀", ({ "cricket" }));
  set_weight(5);
  set("long",
    "这是一只跳来跳去，神气活现的大头蟋蟀。\n");
  set("unit", "只");
  set("objattrib", 1);
  set("value", 0);
}
