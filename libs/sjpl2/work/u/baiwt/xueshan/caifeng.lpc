inherit ROOM;

void create() {
  set("short", "采风宫");
  set("long", @LONG
雪山上凛冽的寒风一直是人们对雪山望而却步的原因之一。要在如
此恶劣的环境下生活，必须经过长年的锻炼。琼宫主人就是在这里学会
了抵御寒风的本领，并且利用寒风创造了一种称为大风云飞符的宝物。
LONG
  );
  set("exits", ([
    "west": __DIR__ "changl5",
    "east": __DIR__ "changl10",
    "south": __DIR__ "changl6",

  ]));
  set("objects", ([
    __DIR__ "npc/wei": 1,
    __DIR__ "npc/snowmen": 4 ]));

  setup();
  replace_program(ROOM);
}
