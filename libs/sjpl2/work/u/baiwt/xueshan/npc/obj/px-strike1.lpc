// 飘雪云穿掌法图

inherit ITEM;

void create() {
  set_name("飘雪云穿掌法图上卷", ({ "zhangfatu1", "tu" }));
  set_weight(200);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "幅");
    set("long", "这是一幅飘雪云穿掌法图，上面画着一些纷繁复杂的招式。\n");
    set("value", 500);
    set("no_get", 1);
    set("material", "paper");
    set("skill", ([
      "name": "piaoxue-strike",
      "exp_required": 10000,  // minimum combat experience required
      "gin_cost": 20,  // jing cost every time study this
      "difficulty": 20,  // the base int to learn this skill
      "max_skill": 50,  // the maximum level you can learn
    ]));
  }
}
