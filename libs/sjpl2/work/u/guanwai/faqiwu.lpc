inherit ROOM;

void create() {
  set("short", "法器屋");
  set("long", @LONG
这是全寺储藏法器的仓库，每逢举行法事活动时，喇嘛们都会到
这里来取法器。
LONG
  );
  set("exits", ([
    "north": __DIR__ "guangchang",
  ]));
  set("objects", ([
    __DIR__ "npc/naqv": 1,
    "/obj/weapon/chanzhang": 2,
    "/obj/weapon/muzhang": 4,
  ]));
  set("outdoors", "guanwai");
  setup();

}
