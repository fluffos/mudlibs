inherit ROOM;

void create() {
  set("short", "黄土路");
  set("long", @LONG
一条黄土铺就的大路，两旁挺拔的白杨，微风吹来树叶哗哗做响。
偶尔有一匹快马从身边飞驰而过。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "chezhan",
    "eastup": __DIR__ "nowo4",
  ]));
  set("objects",
    ([
      "/npc/nknpc": 1,
    ]));
  set("outdoors", "changan");
  setup();
}
