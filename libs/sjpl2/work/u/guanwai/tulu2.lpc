inherit ROOM;

void create() {
  set("short", "山路");
  set("long", @LONG
道路变的陡峭起来，已是进入山里，清新的山风扑面而来，令人
精神顿为一爽。远处传来淙淙水声。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "shihai1",
    "westup": __DIR__ "nowo1",
  ]));
  set("outdoors", "changan");
  setup();
  replace_program(ROOM);
}
