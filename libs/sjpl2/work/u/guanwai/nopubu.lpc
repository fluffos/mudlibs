inherit ROOM;

void create() {
  set("short", "诺日朗瀑布");
  set("long", @LONG
在数不清的瀑布中，诺日朗瀑布最为壮观，水流从高二十米处各个树
林中奔流而下，消逝于谷底，来无踪，去无影，却声闻数里。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "jiuzhai",
    "westup": __DIR__ "nowo4",
    "southdown": __DIR__ "wuhuahai",
  ]));
  set("objects",
    ([
      "/npc/nknpc": 1,
    ]));
  set("outdoors", "changan");
  setup();
  replace_program(ROOM);
}
