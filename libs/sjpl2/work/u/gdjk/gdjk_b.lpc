inherit BULLETIN_BOARD;

void create() {
  set("board_id", "gdjk_b");
  set("location", "/u/gdjk/workroom");
  setup();
  set_name("心得交流", ({ "board" }));
  set("long",
    "这是一块白玉雕成的留言板，上面刻有图案，可供留言使用。\n");
  set("capacity", 180);
  replace_program(BULLETIN_BOARD);
}
