#include <ansi.h>
inherit ROOM;

void create() {
  set("short", HIY "毒谷庄园" NOR);
  set("long", @LONG
这是一间什麽也没有的空房间。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "jie4",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
