// Room: /open/gdjk/zahuopu.c

inherit ROOM;

void create() {
  set("short", "杂货行");
  set("long", @LONG
这是弘农杂货铺。这里虽说地方不大，但是东西的花样却不少。据说
这里的老板是黑道上的，弘农发生了这么多的抢劫案，却从未动过这
里。但是这的老板轻易不肯露面，越发让人怀疑。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "jie2",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "song": 1,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
