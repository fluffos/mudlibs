//changan city

inherit ROOM;

void create() {
  set("short", "官道");
  set("long", @LONG
这里是一条尘土飞扬的黄土路,一直通向东南方向,
路上几乎没有什么行人,只有偶尔几匹快马飞驰而过.
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "northup": "/u/xiyu/xxroad3",
      "southeast": __DIR__ "road2",
    ]));

  set("outdoors", "hongnong");
  set("objects",
    ([
      "/npc/nknpc": 1,
    ]));
  setup();
  replace_program(ROOM);
}
