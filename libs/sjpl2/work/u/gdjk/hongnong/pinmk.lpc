// Room: /open/gdjk/pinmk.c

inherit ROOM;

void create() {
  set("short", "贫民窟");
  set("long", @LONG
这是一间四处漏风的草棚，摇摇欲坠，赶紧出去吧。地上
是一张破旧的草席。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "jie-3",
  ]));
  set("objects",
    ([
      "/npc/nknpc": 1,
    ]));

  setup();
}
