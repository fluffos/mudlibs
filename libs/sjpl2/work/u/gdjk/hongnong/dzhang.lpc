// Room: /open/gdjk/dzhang.c

inherit ROOM;

void create() {
  set("short", "兵营校场");
  set("long", @LONG
这里就是弘农大营的校场。据说这个校场是全国最大的，足可以容纳近
十万军兵进行操练。通常例行的训练只有几百人。正东是这里的主帅办公
的地方。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 3 */
    "west": __DIR__ "dgate",
    "north": __DIR__ "chufang",
    "east": __DIR__ "szhang",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/army": 4,
  ]));

  setup();
  replace_program(ROOM);
}
