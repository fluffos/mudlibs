// Room: /open/gdjk/dgate.c

inherit ROOM;

void create() {
  set("short", "弘农兵营大门");
  set("long", @LONG
这里是弘农大营所在地，弘农的破落并没有影响到军营的发展。
弘农大营比以前似乎更加雄伟壮观。正上方是高高的门楼，两边是
两个巨大的雄狮石像。大门中间站着四个雄伟的军兵，巡视着来往
的部队。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 3 */
    "northeast": __DIR__ "beimen",
    "southwest": __DIR__ "jie4",
    "east": __DIR__ "dzhang",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/bing": 4,
  ]));

  setup();
  replace_program(ROOM);
}
