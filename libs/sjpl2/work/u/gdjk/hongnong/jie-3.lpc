// Room: /open/gdjk/jie-3.c

inherit ROOM;

void create() {
  set("short", "弘农正街");
  set("long", @LONG
这里离弘农大营已经很近了。已经可以听到大营里军兵训练时嘴里喊
的“杀杀”声，中间夹杂着马的嘶鸣声。街北面是个钱庄，好象是不久才
开张的，不知老板是何许人也。让你感到别扭的是，街南是个十分破旧的
草房。你不由得感到一些纳闷。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "qian",
    "west": __DIR__ "jie2",
    "south": __DIR__ "pinmk",
    "east": __DIR__ "jie4",
  ]));
  set("objects",
    ([
      "/npc/nknpc": 1,
    ]));

  setup();
}
