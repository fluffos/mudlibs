// Room: /u/qingcheng/xiuxi.c

inherit ROOM;

void create() {
  set("short", "休息室");
  set("long", @LONG
这里是山庄内部使用的休息室。屋内陈设高雅，但不繁琐。一张雕花
檀木大床尤其珍贵。
LONG
  );
  set("sleep_room", 1);
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "po2",
  ]));

  setup();
}
