// Room: /u/qingcheng/mi1.c

inherit ROOM;

void create() {
  set("short", "仓库");
  set("long", @LONG
这里是山庄的仓库，除了些大米等食物之外，一无所有。你
突然发现一只耗子朝一个黑洞跑了过去，你走过去，仔细看了看
发现居然有一个地下通道。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "down": __DIR__ "didao1.lpc",
    "south": __DIR__ "ro2.lpc",
  ]));

  setup();
  replace_program(ROOM);
}
