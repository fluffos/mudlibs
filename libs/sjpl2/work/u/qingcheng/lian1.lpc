// Room: /u/qingcheng/lian1.c

inherit ROOM;

void create() {
  set("short", "练功房");
  set("long", @LONG
这里是山庄内部弟子用来练功的地方。房内有四个用来练功的
木人,地板上不时可见练功留下的印痕。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "ro3",
    "east": __DIR__ "lian3",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/muren": 4,
  ]));

  setup();
  replace_program(ROOM);
}
