#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "尚武殿");
  set("long", @LONG
这里也是一个六角形的房间, 墙上、地面上、桌上，挂着、
架着的都是宝剑，各式各样，积满了尘土，再也没有夕日
的光华。 

LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": "/u/losey/bedroom",
  ]));
  setup();
  set("item_desc", ([
    "地面": "你忽然发现墙角好象有什么东西发出点点的亮光。\n",
    "亮光": "你运足目力看去,那点亮光好象是一件暗器。\n",
  ]));

}
/*
void init()
{
        add_action("do_step", "step");
        add_action("do_pick","pick");
}

int do_step(string arg)
{
        object  me, lu;

        if( arg != "墙角") 
                return notify_fail("什么？\n");
        me = this_player();
        if(me->query_temp("marks/step"))

  return notify_fail("只听砰的一声,你的头重重的撞在墙上。\n");
 

        me->set_temp("marks/step", 1);
        message_vision("\n$N慢慢的走到墙角处,蹲了下来。\n", me);
        return 1;
}

int do_pick(string arg)
{
       object me,lu;

       if(arg != "暗器")
                 return notify_fail("你要拿什么？ \n");
         me = this_player();
         if(!me->query_temp("marks/step"))
return notify_fail("距离太远了,你的手好象没这么长。\n");
        if( (int)me->query("marks/kongql")>0 )  
return notify_fail("你太贪心了，杀人的利器有一个就足够了。\n");
        lu = new(__DIR__"obj/kongql");
        lu->move(me);
        me->add("marks/kongql", 1);
        message_vision("\n$N小心翼翼的从地上捡起一支孔雀翎。\n", me);
        return 1;
}
*/
