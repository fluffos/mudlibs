// Room: /d/sanyen/back_temple.c

inherit ROOM;

void create() {
  set("short", "後殿");
  set("long", @LONG
一间宽大的後殿, 正中有一尊药师如来的金身塑像。四周的墙上
唯妙唯肖地彩绘著十八罗汉, 每个罗汉的旁边并写著他得道升天的过
程。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "corridor",
    "east": __DIR__ "tower",
  ]));

  set("objects", ([
    __DIR__ "npc/little_bonze": 1 ]));
  set("sleep_room", 1);
  set("no_fight", 1);
  set("no_steal", 1);
  set("no_study", 1);
  set("no_magic", 1);
  set("NONPC", 1);

  setup();
}
