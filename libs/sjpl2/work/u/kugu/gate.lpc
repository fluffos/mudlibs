// Room: /d/sanyen/gate.c

inherit ROOM;

void create() {
  set("short", "枯骨寺山门");
  set("long", @LONG
这里是往枯骨寺的山门，北边是一条穿过山腰的隧道，枯骨寺在你
的东北方不远，由於寺中的老方丈枯骨如来医术通神，不少受过恩惠的
各地富豪将枯骨寺修整得金碧辉煌，庄严雄伟，因此枯骨寺规模虽然不
及京城的白象禅院，但是名气却是大得多了。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "tunnele",
    "east": __DIR__ "front_yard",
    //		"south" : __DIR__"/d/latemoon/sroad1"
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/greeting": 2,
  ]));
  set("outdoors", "sanyen");

  setup();
  replace_program(ROOM);
}
