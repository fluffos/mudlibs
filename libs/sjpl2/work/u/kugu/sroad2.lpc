// Room: /d/sanyen/sroad2.c

inherit ROOM;

void create() {
  set("short", "五台山路");
  set("long", @LONG
这条山路到这里转了个弯，往西穿过一个山壁上凿出来的隧道，往
东则顺著山势缓缓下山。　
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "sroad1",
    "west": __DIR__ "tunnel",
  ]));
  set("outdoors", "sanyen");

  setup();
  replace_program(ROOM);
}
