//huangshan xiuxi1.c
inherit ROOM;

void create() {
  set("short", "休息室");
  set("long", @LONG
这里是可以供比武者休息的地方，一样布置的简单实用，有一张大
床，可以睡七，八个人，在床上正有两个刚刚比完武的人在休息。
LONG);


  set("exits",
    ([  //sizeof() == 4
    "east": __DIR__ "yunxuan",

    ]));

  set("objects",
    ([  //sizeof() == 1
    //		__DIR__"npc/huang" : 2,
    ]));
  set("sleep_room", 1);
  set("if_bed", 1);
  set("no_clean_up", 0);
  setup();
}
