//dingxinshi.c written by john darks
inherit ROOM;
void create() {
  set("short", "定心石");
  set("long", @LONG
自下而上的山势突然急转，在这里变得平坦起来，抬眼望去，飞来峰
已经近在眼前了。你甚至可以看到有黑色的石头柱子在山顶处显露出来。
上面好像还有人影闪动，看的不怎么真切。
LONG
  );
  set("outdoors", "tianzhu");
  set("exits", ([
    "south": __DIR__ "wanshou1",
    "northup": __DIR__ "feilai",
  ]));
  set("objects", ([
    __DIR__ "npc/dizi540000": 3,
  ]));
  setup();
  replace_program(ROOM);
}
