// Room: /new/mojiao/fantang.c

inherit ROOM;

void create() {
  set("short", "饭堂");
  set("long", @LONG
教中弟子一般都在这里进食，圣教对弟子实行包食宿，包....反正就是
实行三包，只要为圣教卖命的，吃喝一律免费。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "lianwu-2",
  ]));
  set("objects", ([ /* sizeof() == 2 */
    "/obj/food/jitui": 4,
    "/obj/food/shuigang": 1,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
