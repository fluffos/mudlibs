// n-road2.c written by john darks
inherit ROOM;
void create() {
  set("short", "大路");
  set("long", @LONG
这是一条大路，路是由黑黑的泥土铺的，上面撒了些细纱。路一直往西北
和南方延伸。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "n-road1",
    "northwest": __DIR__ "n-road3",
  ]));
  set("outdoors", "tianzhu");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
