// roadd.c

inherit ROOM;

void create() {
  set("short", "黄土路");
  set("long", @LONG
顺着黄土路走到这里山脉豁然出现了一个空暇，前面出现了一片无垠的平原。
顺着风，你似乎嗅到了一种淡淡的幽香。。。。。。。那是什么地方？
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "dingx",
    "northwest": __DIR__ "roadc",
  ]));

  setup();
  replace_program(ROOM);
}
