// dingx.c

inherit ITEM;

void create() {
  set_name("衫木", ({ "wood" }));
  set_weight(1000);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "一段径直的衫木，看来是块做房梁的好家伙。\n");
    set("unit", "根");
    set("value", 100);

  }

  setup();
}
