// dingx.c
#include <ansi.h>
#include <armor.h>

inherit ITEM;

void create() {
  set_name(HIG "猪草" NOR, ({ "zhucao" }));
  set_weight(10);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("long", "猪的美食\n");
    set("unit", "捆");

  }

  setup();
}
