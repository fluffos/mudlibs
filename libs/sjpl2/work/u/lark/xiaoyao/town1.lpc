//town1.c

inherit ROOM;

void create() {
  set("short", "听香小镇");
  set("long", @LONG
这是一片安详的小镇，由于处在南北交通的枢纽地带，所以
街面上来来往往的人相当多。
LONG
  );
  set("exits", ([
    "north": __DIR__ "roadt5",
    //    "south":__DIR__"roadt6",
    "east": __DIR__ "town2",
    "west": __DIR__ "lou",
  ]));
  set("objects", ([
    "/u/lark/fnpc/xingjiaoshang": 2,
  ]));
  setup();
  replace_program(ROOM);
}
