//bingqiku.c

inherit ROOM;

void create() {
  set("short", "兵器库");
  set("long", @LONG
这里是营内的兵器库，为士兵门配备各样的兵器和装备，征战
沙场的时候，如果还想活命，请光顾这里。从库房的门里望进
去可以看到里面挂满了枪，刀，戟，甲。
LONG
  );
  set("exits", ([
    "west": __DIR__ "jiaochang2",
  ]));
  set("objects", ([
    __DIR__ "npc/an": 1,
  ]));
  setup();
}
