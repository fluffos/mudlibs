//jiaochang4.c

inherit ROOM;

void create() {
  set("short", "校场");
  set("long", @LONG
这里是军营内的校场，是兵士们日常训练的地方，整齐的方队，
划一的步伐，嘹亮的口号，闪亮的盔甲，让你不禁热血沸腾。
正有一队兵士正在操练武艺。
LONG
  );
  set("exits", ([
    "west": __DIR__ "jiaochang3",
  ]));
  set("objects", ([
    __DIR__ "npc/bing4": 4,
  ]));
  setup();
}
