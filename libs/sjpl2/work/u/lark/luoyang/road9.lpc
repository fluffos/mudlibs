//changan city

inherit ROOM;

void create() {
  set("short", "官道");
  set("long", @LONG
往南不远就是洛阳的北门了，远远看过去高额耸立的样子到也壮观，
一点都看不出以前战火摧残的痕迹。往北就是洛河渡口了。
LONG);

  //set("item_desc", ([" *** " : " *** ", ]));

  set("exits",
    ([  //sizeof() == 4
    "north": __DIR__ "dukous",
      "south": __DIR__ "luoyang-n",  //goto luoyang
    ]));

  set("objects",
    ([  //sizeof() == 1

    "/u/lark/fnpc/liumang": 2,
    ]));


  set("outdoors", "changan");
  set("no_clean_up", 0);
  setup();
}
