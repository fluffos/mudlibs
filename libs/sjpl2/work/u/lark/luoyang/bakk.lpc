// ROOM:/u/changan/bingbu/zoulang2.c

inherit ROOM;

void create() {
  set("short", "莺海燕园");
  set("long", @LONG
这种地方什么朝代都会有，不管现在还是以前，只要有男人，就会有它的存在。
因为这里充满了别样的刺激，完全不同的感受,当然只要你有钱。
LONG);
  set("exits", ([
    "east": __DIR__ "lyn3",
    "west": __DIR__ "jiyuan1",
  ]));
  set("objects", ([
    "/u/lark/fnpc/guinu": 2,
  ]));
  setup();
}
