//ROOM:/u/changan/bingbu/halls.c

inherit ROOM;

void create() {
  set("short", "偏厅");
  set("long", @LONG
这里是大厅南侧的一个偏厅,靠着右手一侧是一排书柜,左手有
一张长案(table),上面排放着文房四宝,散布着一些簿册.乃是兵部府内务
总管的办公所在.
LONG
  );
  set("objects", ([
    __DIR__ "npc/jing": 1,
  ]));
  set("exits", ([
    "north": __DIR__ "hall",
  ]));
  setup();
  replace_program(ROOM);
}
