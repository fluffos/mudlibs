#include <ansi.h>
inherit ROOM;
void create() {
  set("short", WHT "峭壁" NOR);
  set("long", @LONG
你的面前是高不可攀的悬崖峭壁，峭壁上边就是「长白九景」中的一
景“天柱仙踪”。左右的山脊如被刀刃削过一般，你往两边一看深不见底
，前面的长白山顶峰矗立在云雾中，隐约可见。
LONG);
  set("exits", ([
    "southdown": __DIR__ "shulin6",
    "eastup": __DIR__ "baitoufeng",
  ]));
  set("oldarea", 10);
  set("outdoors", "changbai");
  setup();
}
