#include <ansi.h>
inherit ROOM;
void create() {
  set("short", WHT "蓝石小径" NOR);
  set("long", @LONG
终于走到头了，你深深嘘了口气。看见前面一座亭子，日光正
照在它的屋檐上，反射出一片银光。
LONG);
  set("exits", ([
    "east": __DIR__ "tingyuxuan",
    "southwest": __DIR__ "blueroad3"
  ]));
  set("oldarea", 36);
  set("outdoors", "changbai");
  setup();
}
