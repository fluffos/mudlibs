//wsshe1  武僧舍
inherit ROOM;

void create() {
  set("short", "武僧舍");
  set("long", @LONG
这里是寺里的武僧居住的地方。三张床，三把椅子，三个小桌。
屋里收拾的干净利索，几个僧人正在休息。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "wsshe2",
  ]));
  set("objects", ([
    __DIR__ "npc/wuseng1": 3,
  ]));

  set("no_clean_up", 0);

  setup();
}
