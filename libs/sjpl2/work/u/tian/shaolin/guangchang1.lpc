//guangchang1  小广场
inherit ROOM;

void create() {
  set("short", "小广场");
  set("long", @LONG
这里是大成殿前的小广场，几长见方，大块的青石铺成。四角种
着松树，还有几个香客在树下阴凉处休息。
    北面是少林寺的前殿--大成殿，东西各有一条走廊，南面就是少
林寺的正门。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "zoulang2",
    "south": __DIR__ "simen",
    "west": __DIR__ "zoulang1",
    "north": __DIR__ "dcdian",
  ]));

  set("objects", ([
    __DIR__ "npc/juechan": 1 ]));

  set("outdoors", "shaolin");
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
