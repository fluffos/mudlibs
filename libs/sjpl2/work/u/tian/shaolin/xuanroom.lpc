inherit ROOM;

void create() {
  set("short", "茅屋");
  set("long", @LONG
这是一间简陋的茅屋，屋子里边只有一个蒲团。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "xiaolu1",
  ]));
  set("objects", ([
    __DIR__ "npc/xuanbei": 1,
  ]));

  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
