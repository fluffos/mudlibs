inherit ROOM;

void create() {
  set("short", "山路");
  set("long", @LONG
这里是嵩山南麓的少室山山下，一条碎石铺就的山路蜿蜒而上，
虽是山路但也颇为平整宽阔，看来几百年来少林寺香火不断。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "southwest": __DIR__ "tulu3",
    "northup": __DIR__ "shanlu3",
  ]));

  set("no_clean_up", 0);
  set("outdoors", "shaolin");

  setup();
}
