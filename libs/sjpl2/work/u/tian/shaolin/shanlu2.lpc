inherit ROOM;

void create() {
  set("short", "山路");
  set("long", @LONG
山势变的陡峭起来，已是进入山里，清新的山风扑面而来，令人
精神顿为一爽。远处传来淙淙水声。
    西面有一条小路，鲜有人迹。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "west": __DIR__ "xiaolu7",
    "southwest": __DIR__ "yiweitin",
    "northup": __DIR__ "fuhupou",
  ]));

  set("objects",
    ([
      "/npc/nknpc": 1,
    ]));
  set("outdoors", "shaolin");

  setup();
}
