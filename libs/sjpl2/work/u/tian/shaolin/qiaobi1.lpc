//qiaobi1.c

inherit ROOM;

void create() {
  set("short", "峭壁");
  set("long", @LONG
你站在一处陡峭的峭壁边上，左右的山脊如被刀刃削过一般，你往两边
一看深不见底，前面的少室山顶峰矗立在云雾中，隐约可见。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "northup": __DIR__ "dingfeng",
    "southwest": __DIR__ "shanlu1",
  ]));
  set("item_desc", ([
    "峭壁": "是一个万丈峭壁，往下一看云雾缭绕，深不可测。\n",
  ]));
  set("outdoors", "shaolin");
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
