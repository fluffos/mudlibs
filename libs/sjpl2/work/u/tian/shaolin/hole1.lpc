inherit ROOM;

void create() {
  set("short", "山洞");
  set("long", @LONG
你走进一个山洞，这里黑黢黢的，伸手不见五指。你心里不由的嘣
嘣直跳，还是赶快出去吧。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "mishi",
    "southeast": __DIR__ "hole2",
  ]));

  set("no_clean_up", 0);

  setup();
}
