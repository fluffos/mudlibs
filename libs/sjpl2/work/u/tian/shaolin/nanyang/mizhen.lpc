inherit ROOM;

void create() {
  set("short", "迷阵");
  set("long", @LONG

需进一步的描述
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "xiaolu13", /*小路*/
  ]));

  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
