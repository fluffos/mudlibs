inherit ROOM;

void create() {
  set("short", "舟内");
  set("long", @LONG

需进一步的描述
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    //       "out" : "/u/shaolin/ruzhou/dukou1",

  ]));

  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
