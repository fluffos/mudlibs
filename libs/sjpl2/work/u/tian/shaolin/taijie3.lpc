inherit ROOM;

void create() {
  set("short", "上山台阶");
  set("long", @LONG
沿着台阶转过一个弯，遥见黄墙碧瓦，好大一片寺院，只见丛山
环绕，古林蔽日，殿宇巍峨，气像壮严。你心中不由的赞叹一声：好
气魄的少林寺。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "northup": __DIR__ "guangchang",
    "southwest": __DIR__ "guansong",
  ]));

  set("no_clean_up", 0);
  set("outdoors", "shaolin");

  setup();
  replace_program(ROOM);
}
