// Room: /u/wudang/exiwu.c

inherit ROOM;

void create() {
  set("short", "东习武场");
  set("long", @LONG
这是武当的习武场，场上摆设着沙包，木人和一个兵器架，几个武当
弟子在练功。旁边站着一个威严的青年男子，正在指点着练武的弟子。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "guangchang",
  ]));
  set("outdoors", "wudang");
  set("objects", ([
    __DIR__ "npc/feng": 1,
    __DIR__ "npc/wddizi1": 2,
  ]));

  setup();
  replace_program(ROOM);
}
