#include <room.h>

inherit ROOM;

void create() {
  set("short", "花园");
  set("long", @LONG
这里是一个种满各种鲜花的花园。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "road",
    "east": __DIR__ "wuxing",
  ]));
  set("objects", ([
    __DIR__ "npc/baozhenzi": 1,
  ]));
  set("outdoors", "wudang");
  setup();
}
