// Room: /city/nandajie2.c

inherit ROOM;

void create() {
  set("short", "死胡同");
  set("long", @LONG
这里是一个死胡同，只有南边有条通向大街的路。
LONG
  );
  set("exits", ([
    "south": __DIR__ "lizi2",
  ]));
  set("objects", ([
    "/u/poor/beggar/npc/wuyi/poorman": 1,
  ]));
  set("outdoors", "zhang");
  set("no_clean_up", 0);

  setup();
}
