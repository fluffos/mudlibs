// Room: /city/nandajie2.c

inherit ROOM;

void create() {
  set("short", "药铺");
  set("long", @LONG
这是一家药铺，一股浓浓的药味让你几欲窒息，那是从药
柜上的几百个小抽屉里散发出来的。一名小伙计站在柜台后招
呼着顾客。
LONG
  );
  set("exits", ([
    "west": __DIR__ "beidajie2",
  ]));
  set("objects", ([
    __DIR__ "npc/yao_seller": 1,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
