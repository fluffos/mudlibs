// stone.c

inherit ITEM;

void create() {
  set_name("纸条", ({ "zhitiao" }));
  set_weight(100);
  set("long", @LONG 
纸条上写着：
    今日有缘，
    相聚于此，
    若念佳人，
    热窝柳燕。
    
    切记珍重！
LONG
  );
  set("unit", "张");
  set("material", "paper");
}
