// Room: /beggar/door.c
#include <ansi.h>;

inherit ROOM;

void create() {
  set("short", "松林");
  set("long", @LONG
茂密的松林，不时能见到几只松鼠在树上跳来跳去。虽然只
有几丝阳光能透林而入，却不显得阴森可怖。习习凉风不时迎面
吹来，使人浑身舒畅。在右边的地上有个洞。
LONG
  );

  set("exits", ([ /* sizeof() == 3 */
    "southwest": __DIR__ "shulin1",
    "down": __DIR__ "dhole",
  ]));
  set("objects", ([
    __DIR__ "npc/songshu": 2,
  ]));
  set("outdoors", "gaibang");

  setup();
  replace_program(ROOM);
}
