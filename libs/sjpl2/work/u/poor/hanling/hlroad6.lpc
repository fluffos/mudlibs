// Room: /beggar/door.c
#include <ansi.h>;

inherit ROOM;

void create() {
  set("short", "上山路");
  set("long", @LONG
弯弯曲曲的泥土路，却很平坦。左右两边都是茂盛的松林，
雨过天晴时，泥土的香气和松树的芬芳混在一起，能使人心胸开
朗，忘记一切烦恼。前边有个山洞。
LONG
  );

  set("exits", ([ /* sizeof() == 3 */
    "southeast": __DIR__ "hlroad2",
    "northwest": __DIR__ "hlhole",
  ]));
  set("outdoors", "hanling");
  set("objects",
    ([
      "/npc/nknpc": 1,
    ]));

  setup();
}
