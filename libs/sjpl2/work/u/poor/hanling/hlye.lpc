// Room: /beggar/door.c
#include <ansi.h>;

inherit ROOM;

void create() {
  set("short", "练功室");
  set("long", @LONG
这里是寒灵山庄的练功房，有许多寒灵山庄的庄客在这里
练功。有时寒灵的四大高手也偶尔来指点几招。
LONG
  );

  set("exits", ([ /* sizeof() == 3 */
    "west": __DIR__ "hlqian",
  ]));
  set("objects", ([
    __DIR__ "npc/han1": 2,
    __DIR__ "npc/hanye": 1,
  ]));

  setup();
  replace_program(ROOM);
}
