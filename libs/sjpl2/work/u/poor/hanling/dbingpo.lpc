// Room: /beggar/door.c
#include <ansi.h>;

inherit ROOM;

void create() {
  set("short", "地道");
  set("long", @LONG
你走在寒灵山附近的地道之中，这里光线十分昏暗，几乎
看无清四周的景物。不时能听到“嘀嗒、嘀嗒”的水声。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "out": __DIR__ "droad6",
  ]));
  set("objects", ([
    "/u/poor/obj/pill/bingpo": 1,
  ]));
  setup();
}
