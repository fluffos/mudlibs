// Room: /beggar/door.c
#include <ansi.h>;

inherit ROOM;

void create() {
  set("short", "上山石阶");
  set("long", @LONG
你正走在一道长长的石阶上, 阶梯的两侧种植著一排高耸
的松树。前边就是寒灵山庄的大门。一股股白色的烟雾从山庄
中升起。
LONG
  );

  set("exits", ([ /* sizeof() == 3 */
    "southdown": __DIR__ "hlroad4",
    "north": __DIR__ "hldoor",
  ]));
  set("outdoors", "hanling");

  setup();
  replace_program(ROOM);
}
