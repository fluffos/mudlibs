// yanzhi

inherit ITEM;
#include <ansi.h>

void create() {
  set_name("地图", ({ "yijin-map" }));
  set_weight(500);
  set("long", @LONG 
地图上画着：
		图一				图二

	|   ∧∧∧		|	|	※※	※≌	|
	|   寒灵山		|	|	  ※   ※※	|
	|  ∧	∧◇		|	|	   巨石		|
	|		※※※	|	|	   ※※※	|
	|		张家口	|	|		※	|	
	|		※※※	|	|		树林	|
LONG
  );
  set("unit", "张");
  set("material", "teeth");
}
