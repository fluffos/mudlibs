// Room: /u/poor/yidao.c

inherit ROOM;

void create() {
  set("short", "议事厅");
  set("long", @LONG
你走进了黑风寨的大厅，这里四处灯火辉煌。中间铺了
一张虎皮，两边是几把太师椅。寨主目前不在这里。
LONG
  );

  set("exits", ([
    "south": __DIR__ "hfqian",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/louluo": 2,
    __DIR__ "npc/xueku": 1,
  ]));
  setup();
  replace_program(ROOM);
}
