// Room: /jing/duankit.c

inherit ROOM;

void create() {
  set("short", "洞中");
  set("long", @LONG
这里一片漆黑，只能借助洞口的点点光亮来分辨四周
围的事物。不时能听到石钟乳流下的水声。这里是一个死洞。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "hole2",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/tiger": 1,
  ]));
  set("no_clean_up", 0);

  setup();
}
