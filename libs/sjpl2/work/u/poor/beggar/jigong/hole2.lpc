// Room: /jing/duankit.c

inherit ROOM;

void create() {
  set("short", "洞中");
  set("long", @LONG
这里一片漆黑，只能借助洞口的点点光亮来分辨四周
围的事物。不时能听到石钟乳流下的水声，在东边和南边各
有一个小洞。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "hole3",
    "out": __DIR__ "hole1",
    "east": __DIR__ "hole4",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
