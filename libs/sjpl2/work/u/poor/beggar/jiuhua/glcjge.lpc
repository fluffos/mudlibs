//cjge  藏经阁
inherit ROOM;

void create() {
  set("short", "藏经阁");
  set("long", @LONG
这里就是甘露寺的藏经阁，左右的几排书架上放满了佛经。有几个
僧人正在整理佛经。这里十分干净，一尘不染。
    你可以在这里买些经书诵读。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "out": __DIR__ "glyuan",
  ]));
  set("objects", ([
    __DIR__ "npc/jingwu": 1,
  ]));

  set("no_sleep", 1);
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
