// Room: /u/poor/gaibang/zhulin2.c

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIB "石室" NOR);
  set("long", @LONG
这是一间丐帮弟子休息的空屋子，屋子中什么都没有。只
有一大张席子铺在地上。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "ddroad12",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/zhangtong": 1,
    __DIR__ "obj/shuigang": 1,
  ]));
  set("no_clean_up", 0);
  set("sleep_room", 1);

  setup();
}
