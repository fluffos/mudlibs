// Room: /beggar/door.c
#include <ansi.h>;

inherit ROOM;

void create() {
  set("short", HIB "林中空地" NOR);
  set("long", @LONG
这是树林中的一块空地，四周都是茂密的树林，显然这空地
是人工砍伐而形成的。在西北的远处有间木屋。
LONG
  );

  set("exits", ([ /* sizeof() == 3 */
    "northwest": __DIR__ "xshulin1",
    "northeast": __DIR__ "xshulin2",
    "south": __DIR__ "xdoor",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/beggar": 3,
  ]));
  set("outdoors", "gaibang");
  set("no_clean_up", 0);

  setup();
}
