// Room: /beggar/door.c
#include <ansi.h>;

inherit ROOM;

void create() {
  set("short", HIB "树林中" NOR);
  set("long", @LONG
你来到了树林之中，四周都是茂密的树林，只有星星点点的
阳光穿过枝叶照在地上。不时能听见鸟叫的声音。在西边有一座
小木屋。
LONG
  );

  set("exits", ([ /* sizeof() == 3 */
    "west": __DIR__ "xliu",
    "southeast": __DIR__ "xkongdi",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/wangqr": 1,
  ]));
  set("outdoors", "gaibang");
  set("no_clean_up", 0);

  setup();
}
