// 杂货铺
inherit ROOM;

void create() {
  set("short", "杂货铺");
  set("long", @LONG
你走进了一家小杂货铺，这里是鸡华村唯一的一家杂货铺，
在屋子的左边放着一些杂物，右边放着几只锄头和一些绳子。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "jhw1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/zh_seller": 1,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
