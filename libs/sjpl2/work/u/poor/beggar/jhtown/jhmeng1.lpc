// Room: /u/poor/gaibang/zhulin3.c

inherit ROOM;

void create() {
  set("short", "茅屋");
  set("long", @LONG
这是一间由干草很木头搭成的小屋，在屋子的中间放着一个
圆形的木盘。旁边的桌子上放着本易经。在屋子的角落盘腿坐着
一个白眉，白发老人。
LONG
  );
  set("item_desc", ([ /* sizeof() == 1 */
    "木盘": "木盘四周刻着许多天干、地支和节气的名称。  
",
    "易经": "一本表面发黄的书籍。  
",
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "jhmeng",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/meng": 1,
  ]));

  setup();
}
