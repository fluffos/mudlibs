// Room: /u/poor/gaibang/gbwuch2

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIB "前院" NOR);
  set("long", @LONG
这里是一个寺庙的前院，现在早已经破烂不堪。有许多乞丐聚
集在这里。南边是寺庙的大门，北边是大成殿。东西各有几间厢房。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "ldcdian",
    "west": __DIR__ "lroom2",
    "south": __DIR__ "ldoor",
    "east": __DIR__ "lroom1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/u/poor/beggar/zongduo/npc/beggar1": 3,
  ]));
  set("sleep_room", 1);
  set("outdoors", "gaibang");
  set("no_clean_up", 0);

  setup();
}
