// Room: /beggar/door.c
#include <ansi.h>;

inherit ROOM;

void create() {
  set("short", HIB "打狗阵入口" NOR);
  set("long", @LONG
你已经走进天下文明的丐帮打狗阵了，这里机关重重，并有
许多丐帮高手在这里埋伏，只要的一招不慎，就会死无葬身之地。
LONG
  );

  set("exits", ([ /* sizeof() == 3 */
    "north": __DIR__ "dagou2",
    "out": __DIR__ "zdzoul1",
  ]));
  set("outdoors", "gaibang");
  set("no_clean_up", 0);

  setup();
}
