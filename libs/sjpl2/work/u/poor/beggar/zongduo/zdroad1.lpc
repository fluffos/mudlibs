// Room: /u/poor/gaibang/gbwuch2

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIB "翠竹林" NOR);
  set("long", @LONG
这里是丐帮总舵北边的一片竹林，不时有阵阵微风吹过，发出
“沙，沙”地声音。全然不像是在丐帮的总舵之中，从这里能远远
看到北边有几间小屋。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "northwest": __DIR__ "zdshi",
    "northeast": __DIR__ "zdroad2",
    "south": __DIR__ "zdwuch4",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/beggar1": 1,
    __DIR__ "npc/beggar": 1,
  ]));
  set("sleep_room", 1);
  set("outdoors", "gaibang");
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
