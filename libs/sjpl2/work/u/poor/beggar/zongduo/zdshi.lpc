// Room: /u/poor/gaibang/zhulin3.c

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIB "石屋" NOR);
  set("long", @LONG
这是一间阴暗潮湿的石屋，在屋子的左边有一张石床，一张
石桌放在窗前。桌上放着几本册子，在屋子的正中站着个老者。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "southeast": __DIR__ "zdroad1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/shifeng": 1,
    __DIR__ "obj/bamboo": 1,
  ]));

  setup();
}
