// Room: /u/yanzi/huashan/husun.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"猢狲愁"NOR);
	set("long", @LONG
这里是一段十分陡峭的山路，传说从前从华山出来，想下山的猢
狲，每每到这里就因为这里的陡峭的地势而不得不返回，连它们也感
到难以逾越，至今仍能在这里看到四只铁猿，盘踞台畔。有此向东就
能够登上华山的东峰。
LONG
	);
	set("no_clean_up", 0);
	set("outdoors", "huashan");
	set("exits", ([ /* sizeof() == 2 */
  "eastup" : __DIR__"beifeng",
  "westdown" : __DIR__"laojun",
]));

	setup();

}
#include "/u/yanzi/huashan/eastup.h";