// Room: /u/yanzi/huashan/nanfeng.c

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIC "南峰" NOR);
  set("long", @LONG
这里是太华五峰之中最高的南峰，站在峰顶，整个华山都在自己
的脚下，当真有“会当凌绝顶，一览众山小”的豪壮。
    从这里下去往西往东，就能分别前往五峰中的其余两峰--西峰、
中峰。
LONG
  );
  set("outdoors", "huashan");
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 4 */
    "southdown": __DIR__ "jintian",
    "westdown": __DIR__ "zhixing",
    "eastdown": __DIR__ "zhfeng",
    "northdown": __DIR__ "road6",
  ]));

  setup();
  replace_program(ROOM);
}
