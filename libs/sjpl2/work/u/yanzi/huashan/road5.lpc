// Room: /u/yanzi/huashan/road5.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"下山路"NOR);
	set("long", @LONG
这是一段坡度较缓的下山路，从这里折向了西南面，那里有一大
片建筑群，便是华山剑派的所在了。
LONG
	);
	set("no_clean_up", 0);
	set("outdoors", "huashan");
	set("exits", ([ /* sizeof() == 2 */
  "southwest" : __DIR__"zhyue",
  "eastup" : __DIR__"road4",
]));

	setup();

}
#include "/u/yanzi/huashan/eastup.h";