// Room: /u/yanzi/huashan/road3.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"下山路"NOR);
	set("long", @LONG
这里是从北峰下来，通往仙人砭的一段下山路，你只觉山路又逐
渐开始陡峭起来。
LONG
	);
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "northup" : __DIR__"beifeng",
  "southdown" : __DIR__"xianren",
]));

	setup();

}
#include "/u/yanzi/huashan/northup.h";