// Room: /u/yanzi/huashan/xianren.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"仙人砭"NOR);
	set("long", @LONG
下了北峰，山路的倾斜度越来越大，到了这里，山路几乎是笔直
窜上天空，就象一枚巨大石针，可见华山之险。
LONG
	);
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "northup" : __DIR__"road3",
  "southup" : __DIR__"tianti",
]));

	setup();

}
#include "/u/yanzi/huashan/northup.h";