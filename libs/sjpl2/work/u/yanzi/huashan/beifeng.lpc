// Room: /u/yanzi/huashan/beifeng.c

inherit ROOM;

void create() {
  set("short", "[1;36m北峰[2;37;0m");
  set("long", @LONG
你终于登上了华山的北峰，这里高达1500丈。这里四面悬崖绝壁
，恰如一座平台，因而又被称为云台峰。
LONG
  );
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/qiangdao": 3,
  ]));
  set("outdoors", "huashan");
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 3 */
    "westdown": __DIR__ "husun",
    "southdown": __DIR__ "road3",
    "northeast": __DIR__ "ynchi",
  ]));

  setup();
}
int valid_leave(object me, string dir) {
  object ob;
  if (present("qiang dao", environment(me)))
    return notify_fail("强盗一手拦住你，嘿嘿一笑：“想走？先留下买路钱。”\n");
  return ::valid_leave(me, dir);
}
