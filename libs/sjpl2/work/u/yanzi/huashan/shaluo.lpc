// Room: /u/yanzi/huashan/shaluo.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"莎萝坪"NOR);
	set("long", @LONG
踏着一级级的石阶迤逦而上，就来到了莎萝坪。从这里开始华山
的险峻逐渐显现，南面是一条迂回盘旋的山道，称作「十八盘」，通
往山上。
LONG
	);
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "southup" : __DIR__"18pan",
  "eastdown" : __DIR__"shanmen",
]));

	setup();

}
#include "/u/yanzi/huashan/southup.h";