// Room: /u/yanzi/huashan/xiaoj1.c

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIC "小径" NOR);
  set("long", @LONG
一条用碎石铺成的小径，这里十分地幽静。北面是兵器房，南面
则不断有阵阵菜香飘来。
LONG
  );
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "zhyue",
    "east": __DIR__ "xiaoj2",
    "south": __DIR__ "fanting",
    "north": __DIR__ "wuqiku",
  ]));

  setup();
}
