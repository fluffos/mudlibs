//letter.c
inherit ITEM;

void create() {
  set_name("书信", ({ "letter" }));
  set_weight(500);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "封");
    set("long", "一封华山掌门的亲笔书信。\n");
    set("value", 0);
    set("material", "paper");
  }
}
