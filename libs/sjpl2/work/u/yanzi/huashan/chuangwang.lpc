// Room: /u/yanzi/huashan/chuangwang.c

inherit ROOM;
#include <ansi.h>;

void create()
{
	set("short", HIC"阎王砭"NOR);
	set("long", @LONG
这里坡度略有降低，但也十分惊险，和前面一段仙人砭十分相似
，闯过这里就要到连韩愈都要投遗书的苍龙岭。
LONG
	);
	set("outdoors", "huashan");
	set("no_clean_up", 0);
	set("exits", ([ /* sizeof() == 2 */
  "southup" : __DIR__"canglong",
  "northdown" : __DIR__"tianti",
]));

	setup();

}
#include "/u/yanzi/huashan/southup.h";