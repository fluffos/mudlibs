// Room: /u/yanzi/huashan/gudi1.c 

inherit ROOM;
#include <ansi.h>;

void create() {
  set("short", HIC "谷底" NOR);
  set("long", @LONG
这里就象一处室外仙境，周围长着许多大树，地上铺着有厚又软
的草地，就是他们刚刚救了你的命，奇怪地是有不少人跳入这里寻死
，却不见有人的尸骨，一条小溪缓缓地流向东面。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "gudi2",
  ]));
  set("no_clean_up", 0);

  set("outdoors", "huashan");
  setup();
  replace_program(ROOM);
}
