inherit ROOM;

void create() {
  set("short", "山顶");
  set("long", @LONG
你走到了山顶上，不远处有一个小草屋，也不知有否人家。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "southeast": __DIR__ "shan1road3",
    "north": __DIR__ "shan1road5",
    "westdown": __DIR__ "shan2road3",
  ]));

  set("outdoors", "city");

  setup();
  replace_program(ROOM);
}
