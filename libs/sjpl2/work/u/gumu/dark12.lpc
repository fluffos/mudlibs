// Room: /open/hurt/oldtomb/dark12.c

inherit ROOM;

void create() {
  set("short", "黑洞");
  set("long", @LONG
这里一片黑漆漆的，伸手不见五指，你什么都看不见。你只能
依稀的辨别出方向。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "southeast": __DIR__ "dark9",
    "west": __DIR__ "dark11",
    "southwest": __DIR__ "dark8",
  ]));
  set("no_clean_up", 0);

  setup();
}
