inherit ROOM;

void create() {
  set("short", "山顶");
  set("long", @LONG
这里有一间小草屋，屋子里空空如也。
LONG
  );
  set("exits", ([ /* sizeof() == 3 */
    "south": __DIR__ "shan1road4",
  ]));

  set("outdoors", "city");
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/yulu": 1,
    __DIR__ "npc/lu": 1,
  ]));

  setup();
}
