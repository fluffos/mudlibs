inherit ROOM;

void create() {
  set("short", "肉铺");
  set("long", @LONG
这里是再来镇的肉铺，每日供应再来镇上的肉类消费。由于
是个老店了，价格也不贵，不过卫生状况可就有点不太好了。这
里苍蝇飞舞，腥气熏人。唉，该打扫了。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "htpath2",
  ]));
  set("objects", ([
    __DIR__ "npc/zheng": 1,
  ]));
  set("no_clean_up", 0);

  setup();
}
