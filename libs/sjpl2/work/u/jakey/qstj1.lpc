inherit ROOM;

void create() {
  set("short", "青石台阶");
  set("long", @LONG
这是通向山上的青石台阶，因为有人经常打扫，台阶很干净
，两旁种着矮矮的松树。偶尔有一两只小鸟飞过，清脆的鸣叫着。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "northup": __DIR__ "qstj2",
    "southdown": __DIR__ "qspath4",
  ]));
  set("objects",
    ([
      "/npc/nknpc": 1,
    ]));
  set("outdoors", "jakey");
  setup();
  replace_program(ROOM);
}
