inherit ROOM;

void create() {
  set("short", "小路");
  set("long", @LONG
走到这里，已经到了山脚了，路的两旁全是农田，可以看到
三三两两的农夫在田里干活，再往南走，就到山下的再来镇了。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "northup": __DIR__ "caoting",
    "south": __DIR__ "htpath3",
  ]));
  set("objects",
    ([
      "/npc/nknpc": 1,
    ]));
  set("no_clean_up", 0);
  set("outdoors", "jakey");
  setup();
  replace_program(ROOM);
}
