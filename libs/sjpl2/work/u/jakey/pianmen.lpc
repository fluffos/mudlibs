inherit ROOM;

void create() {
  set("short", "偏门");
  set("long", @LONG
这是一个偏门，从这里再往南就出了落月山庄了。一条土黄
色的小路向山下延伸着。门口有两个守卫，正在警惕的守护着。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "northeast": __DIR__ "xyuan1",
    "southdown": __DIR__ "lpath1",
  ]));
  set("objects", ([
    __DIR__ "npc/guard": 2,
  ]));
  set("no_clean_up", 0);
  set("outdoors", "jakey");
  setup();
  replace_program(ROOM);
}
