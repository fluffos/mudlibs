inherit ROOM;

void create() {
  set("short", "后山小路");
  set("long", @LONG
这是后山的一条小路，弯弯曲曲的在树林中延伸着。树叶被
山风吹得哗哗直响。有时山风卷起几片落叶在你面前打个滚，迅
速的躲进草丛里。		
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "hspath3",
    "southeast": __DIR__ "xyuan1",
    "east": __DIR__ "huafang",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "jakey");
  setup();
}
