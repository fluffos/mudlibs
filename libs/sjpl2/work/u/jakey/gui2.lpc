inherit ROOM;

void create() {
  set("short", "桂楼一层");
  set("long", @LONG
这里是一间厅室，厅里四个大红柱，上面贴着一副对联。对
联上写着：“屋对江山如画静，气含蔬荀到公无”。厅里面供着
一尊菩萨像，成夫人现在正跪在蒲团上对着菩萨进行祈祷，也不
知是在祈祷什么。旁边一个丫环伺候着。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "out": __DIR__ "gui1",
    "up": __DIR__ "gui3",
  ]));
  set("objects", ([
    __DIR__ "npc/furen1": 1,
    __DIR__ "npc/laner": 1,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
