inherit ROOM;

void create() {
  set("short", "崖底");
  set("long", @LONG
这里是悬崖的崖底，四面绝壁。崖底布满乱石。石间隐约可
见一些人或者动物的骨骸，看来只要到了这里就不可能活着出去
了。
LONG
  );
  //	set("exits", ([ /* sizeof() == x */
  //	"" : __DIR__"",
  //	]));
  set("no_clean_up", 0);
  set("outdoors", "jakey");
  setup();
}
