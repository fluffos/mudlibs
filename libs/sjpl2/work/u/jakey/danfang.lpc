#include <localtime.h>
inherit ROOM;

void create() {
  set("short", "丹房");
  set("long", @LONG
这里是山庄的丹房。房间的中央是座古色古香的炼丹炉，四
周堆放着各种各样的珍贵药材。因为炉火整日不熄，炼丹房里总
是烟雾缭绕，平添一种神秘气氛。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "xyuan1",
  ]));
  set("no_clean_up", 0);
  set("objects", ([
    __DIR__ "npc/laodao": 1,
  ]));
  setup();
}
