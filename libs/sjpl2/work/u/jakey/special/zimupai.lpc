#include <armor.h>

inherit ITEM;

void create() {
  set_name("紫木牌", ({ "zimu pai", "token" }));
  set_weight(500);
  set("long",
    "一块紫色的木牌，上面刻有一些花纹，不知道是用来做什么的。\n");
  set("unit", "块");
}
