inherit ROOM;

void create() {
  set("short", "当铺");
  set("long", @LONG
这里是个小当铺，虽说不大但可是什么都收。高高的柜台上
小小的窗口，让来这的人觉得低人一等。不到万不得已，镇上的
人都不来这里的。现在日子还不错，所以这里比较冷清。一个小
伙计在柜台里面坐着。  
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "htpath3",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
