inherit ROOM;

void create() {
  set("short", "草地");
  set("long", @LONG
这里豁然开朗起来，远处竟然是一片翠绿的草地，在草地上
开着无数的白的，兰的花朵。在草地上面还盖着一个小小的木屋
。棕色的木屋旁还有几株桃树，花瓣落在地上，一抹绯红。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": "/u/jakey/xiaojing1",
    "north": __DIR__ "home2",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "jakey");
  setup();
  replace_program(ROOM);
}
