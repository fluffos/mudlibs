// Room: /d/oldpine/spath4.c

inherit ROOM;

void create() {
  set("short", "下坡道");
  set("long", @LONG
这是一条笔直的坡道，北高南低，道路两旁长满了许多松树，从松
林间隐约可以听到东边传来流水的声音。南面是一条青石官道。
LONG
  );
  set("objects", ([ /* sizeof() == 1 */
    "/obj/npc/tiaofu": 2,
  ]));
  set("outdoors", "oldpine");
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "spath3",
    "south": __DIR__ "spath5",
  ]));
  set("no_clean_up", 0);

  setup();
}
