// Room: /d/oldpine/spath5.c

inherit ROOM;

void create() {
  set("short", "青石官道");
  set("long", @LONG
这是一条青石板铺成的官道，是连接广西、云南两省的主干，往北翻过
山坡就是位于苗疆北部边缘的莽林。不过山上有个老松寨，是个土匪窝，来
往客商可得小心点。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "spath6",
    "north": __DIR__ "spath4",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
