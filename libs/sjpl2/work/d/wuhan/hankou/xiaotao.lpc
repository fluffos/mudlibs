// Room: /u/wuhan/hankou/xiaotao.c

inherit ROOM;

void create() {
  set("short", "小桃园");
  set("long", @LONG
小桃园的鸡汤是远近闻名的, 据说这里卖的一种鸡汤对学武的人大有好处, 所以
人们经常可以在这里看到些配刀带剑的豪强.
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "proad2",
  ]));
  set("objects", ([
    __DIR__ "npc/xiaoboss": 1,
  ]));
  setup();
  replace_program(ROOM);
}
