// Room: /u/wuhan/hankou/gysc1.c

inherit ROOM;

void create() {
  set("short", "藏经阁");
  set("long", @LONG
这里收藏着许多佛教经典，不过你都不感兴趣。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "gysc2",
    "north": __DIR__ "gyec2",
  ]));
  set("movedirection", "west");
  set("no_clean_up", 0);

  setup();
}

void reset() {
  ::reset();
  if (query("exits/down")) {
    delete("exits/down");
  }
}
