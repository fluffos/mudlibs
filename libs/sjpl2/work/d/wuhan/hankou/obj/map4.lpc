// map1.c

inherit ITEM;

void create() {
  set_name("地图的左下角", ({ "map sw", "map" }));
  set("long", "这上面画着些你看不懂的符号, 看来是一张地图的左上角.\n"
    "这东西纸不象纸革不似革, 看得出是非常古老的物什.\n");
  set("unit", "片");
  setup();
}
