// redkey.c

inherit ITEM;

void create() {
  set_name("锈迹斑斑的铜钥匙", ({ "red key" }));
  set("long",
    "一把锈迹斑斑的铜钥砛n");
  if (clonep())
    set_default_object(__FILE__);
  else
    set("unit", "把");
  setup();
}
