// Room: /u/wuhan/hankou/longju.c

inherit ROOM;

void create() {
  set("short", "农具店");
  set("long", @LONG
这是一家中等的农具店. 墙上地上挂的摆的都是大大小小的锄头钉钯, 店除此
之外只有一个桌子算是柜台, 一个老汉毕恭毕敬地站在旁边.
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "hanz3",
  ]));
  set("objects", ([
    __DIR__ "npc/nongboss": 1,
  ]));
  setup();
  replace_program(ROOM);
}
