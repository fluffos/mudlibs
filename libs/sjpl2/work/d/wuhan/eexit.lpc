// Room: /u/wuhan/eexit.c

inherit ROOM;

void create() {
  set("short", "官道");
  set("long", @LONG
这条官道向东一直走可以到浙江。南边是一岱连绵的山峦。那里
闹匪已经有千年的历史了，如果不怕死你可以去闯一闯。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "shanzhai/path1",
    "west": __DIR__ "dhbian1",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "wuhan");
  setup();
}
