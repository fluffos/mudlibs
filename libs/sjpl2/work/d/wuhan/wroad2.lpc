// wroad1.c

inherit ROOM;

void create() {
  set("short", "青石马路");
  set("long", @LONG
这是一条青石大马路, 可以并排跑五匹高头大马。街道两侧的梧桐十步一株, 茂密的
阔叶遮起一路阴凉。由于武昌海鲸帮在长江水道上的兴起, 这一带到处都是江湖人物。
    从这里往北就是远近闻名的蛇山, 南边开了一家兵器铺。LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "west"  : __DIR__"wroad1",
  "east"  : __DIR__"wroad3",
  "south" : __DIR__"weaponors",
  "north" : __DIR__"snakec1",
]) );
	set("objects", ([
              "/npc/nknpc" : 1,
	__DIR__"npc/boy" : 1,
]) );
	setup();
	set("outdoors", "jinling");
}

