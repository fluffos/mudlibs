// Room: /u/wuhan/dragon_school.c

inherit ROOM;

void create() {
  set("short", "书院");
  set("long", @LONG
这里是海鲸帮开的书院, 专门请了一位老秀才教帮内年青弟子
习文。正面墙上挂了幅大隶, 写着

		书山有路

两边则挂着几幅水墨, 手法不凡, 据说是余帮主的手迹。
    北边是海鲸帮弟子专用的练功场。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "dragon_lgc",
    "east": __DIR__ "dragon_yard",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/teacher": 1,
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
