// Room: /new/jinling/jiangbian2.c

inherit ROOM;

void create() {
  set("short", "江边小路");
  set("long", @LONG
长江下游沿岸的风光虽然比不上三峡，但江面宽阔，滔滔江水，却
另有一番景象。
LONG
  );
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/heshang": 2,
  ]));
  set("outdoors", "jinling");
  set("exits", ([ /* sizeof() == 2 */
    "northeast": __DIR__ "jiangb1",
    "southwest": __DIR__ "jiangb3",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
