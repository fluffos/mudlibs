// Room: /u/wuhan/dianjia/phome.c

inherit ROOM;

void create() {
  set("short", "萍姑的家");
  set("long", @LONG
这里是点萍姑的家，右面墙头放了张雕花大床，左面角上摆着口
大箱子，正中间是一张红木八仙桌，桌面镶的是整整一块青玉。房间
里四处都是花瓶，瓶内都是新采摘的鲜花。南面的窗子大开，可以看
见院子里的老柏树苍桑的身影。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "pinggu",
  ]));
  set("objects", ([
    __DIR__ "npc/pinggu": 1,
  ]));
  setup();
}
