// Room: /u/wuhan/dianjia/secretroom.c

inherit ROOM;

void create() {
  set("short", "点家密室");
  set("long", @LONG
这是一间什麽也没有的空房间。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "eastup": __DIR__ "phome",
  ]));
  set("no_clean_up", 0);

  setup();
  replace_program(ROOM);
}
