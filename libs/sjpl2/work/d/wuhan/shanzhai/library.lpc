// Room: /u/wuhan/shanzhai/library.c

inherit ROOM;

void create() {
  set("short", "书房");
  set("long", @LONG
这里是寨主的书房，布置很有些书香气。墙上平排挂着两幅画
像，年代都已久远，但人物依然栩栩如生，画像右下角分别写着：
本门祖师  玄妙教主像  和  先祖  师基像。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "hall",
  ]));
  set("objects", ([
    __DIR__ "npc/shichang": 1,
    __DIR__ "npc/bonne": 1,
  ]));

  setup();
  replace_program(ROOM);
}
