// Room: /u/wuhan/shanzai/path3.c

inherit ROOM;

void create() {
  set("short", "上山路");
  set("long", @LONG
你顺着残缺的石阶拾级而上。这里已经可以看到些伟岸的松树
，偶尔还能听到泉水的叮咚声。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "southeast": __DIR__ "path4",
    "northdown": __DIR__ "path2",
  ]));

  set("objects", ([
    __DIR__ "npc/guard1": 2,
  ]));
  setup();
}
