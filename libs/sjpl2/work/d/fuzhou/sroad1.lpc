// Room: sroad1.c

inherit ROOM;

void create() {
  set("short", "福州城街道");
  set("long", @LONG
这里是福州城南，通往过江的渡口。东面就是船坞。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "cross",
    "east": __DIR__ "chuanwu",
    "south": __DIR__ "dukou",
  ]));
  set("outdoors", "fuzhou");
  set("objects",
    ([  //sizeof() == 1
    "/obj/npc/farmer": 1,
    ]));
  set("hell", 1);

  setup();
  replace_program(ROOM);
}
