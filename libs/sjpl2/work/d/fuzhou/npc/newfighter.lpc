// bfighter.c

inherit NPC;

void create() {
  set_name("趟子手", ({ "nfighter" }));
  set("gender", "男性");
  set("age", 28);
  set("combat_exp", 1000);
  set("attitude", "heroism");
  set_skill("unarmed", 20);
  set_skill("parry", 20);
  set_skill("dodge", 20);

  set("long", @LONG
这是一位镖局的趟子手，负责镖局的护镖任务。
LONG);
  create_family("振远镖局", 3, "趟子手");
  //        set("chat_chance", 20);
  //       set("chat_msg", ({
  //               "趟子手喊道：振远镖局..., 盛名远扬...\n",
  //              (:random_move :)
  //      }) );

  //        set("chat_chance_combat", 40);
  //        set("chat_msg_combat", ({
  //               "趟子手喊道：教你尝尝我的厉害！\n",
  //                "趟子手叫道：我们镖局可不是好惹的！\n"
  //                "趟子手大叫：我一定要杀了你，替镖局扬名！\n"
  //        }) );

  setup();
  add_money("silver", 1);
}
