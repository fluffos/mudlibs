// Room: /u/fuzhou/wroad2.c

inherit ROOM;

void create() {
  set("short", "福州城街道");
  set("long", @LONG
这里是福州城西，东边就是十字路口。南面的院子里甚是喧闹，象
是有很多人的样子。北面是杂货铺，不过门不朝这个方向开。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "monky",
    "east": __DIR__ "cross",
    "west": __DIR__ "minzhai3",
  ]));
  set("no_clean_up", 0);
  set("outdoors", "fuzhou");
  set("hell", 1);

  setup();
  replace_program(ROOM);
}
