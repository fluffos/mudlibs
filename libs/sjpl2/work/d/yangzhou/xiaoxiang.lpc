//yangzhou  xiaoxiang.c
inherit ROOM;

void create() {
  set("short", "小巷");
  set("long", @LONG
你走进一条阴暗的小巷之中。这里远没有外面的喧哗吵闹之声,到
处布满了碧绿的苔癣,现的分外阴森,你不禁加快了脚步。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "dongjie3",
    "north": __DIR__ "dufang",
  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/thief": 1,
    ]));

  set("outdoors", "changan");

  set("no_clean_up", 0);

  setup();
}
