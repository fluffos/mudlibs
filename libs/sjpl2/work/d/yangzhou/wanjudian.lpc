//yangzhou  wanjudian.c
inherit ROOM;

void create() {
  set("short", "玩具店");
  set("long", @LONG
你走进了一家玩具店,不要小瞧这里,这里可有不少好东西呢。虽说没
有太大的用处,但关键时刻也能发挥很重要的功效。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "nanjie2",

  ]));

  set("no_clean_up", 0);

  setup();
}
