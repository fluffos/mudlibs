//yangzhou  wuqidian.c
inherit ROOM;

void create() {
  set("short", "武器店");
  set("long", @LONG
你走进了一间挂满各种兵器的店铺,原来是家兵器店。看起来这里的
兵器种类繁多,买不到的兵器可能不多。这里的主人是一个兵器收藏家,
你要是肯花大的价钱说不定能搞到称心如意的兵器。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "nanjie1",

  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/bingqi_boss": 1,
    ]));



  set("no_clean_up", 0);

  setup();
}
