// gao.c

inherit NPC;
void create() {
  set_name("老万", ({ "lao wan", "wan" }));

  set("long", "老万是个走南闯北的商人，据说是个隐居江湖的高人。\n");
  set("gender", "男性");
  set("age", 52);
  set("title", "商人");
  set("attitude", "peaceful");
  set("shen_type", 1);
  set("combat_exp", 85000);
  set_skill("unarmed", 25);
  set_skill("dodge", 15);
  set_skill("dagger", 35);
  set_skill("parry", 45);
  set("inquiry", ([
    "name": "老夫姓万,你有什么事吗？\n",
    "here": "这是我的家.\n",
  ]));

  setup();
  add_money("silver", 5 + random(5));
  carry_object("/u/baiwt/xueshan/npc/obj/yandou")->wield();
  carry_object("/d/city/obj/cloth/choupao")->wear();
}
void init() {
  object ob;
  ::init();
  if (interactive(ob = this_player()) && !is_fighting()) {
    remove_call_out("greeting");
    call_out("greeting", 1, ob);
  }
}
void greeting(object ob) {
  object biaoche, wage;
  if (!ob || environment(ob) != environment()) return;
  if (present("biao che", environment(ob)) && ob->query("quest/biaoju") == "yangzhou") {
    biaoche = present("biao che", environment(ob));
    wage = new("/obj/money/10silver");
    command("say 镖车送到了吗？太好了，有劳你了！替老夫向邱总镖头问好。\n");
    destruct(biaoche);
    ob->set("quest/biaoju", 0);
    ob->add("combat_exp", 160);
    ob->add("potential", 50);
    ob->add("score", 5);
    ob->add("life_exp", 10);
    wage->move(ob);
  }
  return;
}
