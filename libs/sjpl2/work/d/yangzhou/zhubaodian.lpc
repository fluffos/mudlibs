//yangzhou  zhubaodian.c
inherit ROOM;

void create() {
  set("short", "藏珍斋");
  set("long", @LONG
这是一家专门出售贵重宝物的珠宝店。整个珠宝店设计的古香古色，
别具特色。你看这各种奇珍异宝不禁眼花缭乱，流连忘反。要是你想你
想去讨好你的心上人的话就不要再犹豫了。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "xijie3",

  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/zhubao_boss": 1,
    ]));

  set("no_clean_up", 0);

  setup();
}
