// Room: /u/shandong/ta-woshi.c

#include <room.h>

inherit ROOM;

void create() {
  set("short", "王太太的卧室");
  set("long", @LONG
这里是王太太的卧室。王太太是远近闻名的一位大美人，但自从王
员外去世后整日泪流不止现在看上去甚是憔悴。现在她正坐在床边给宝
宝缝制过年的衣衫。
    往东走是王员外家的院子。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "ta-wangjia",
  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/wangfuren": 1,
    ]));
  create_door("east", "木门", "west", DOOR_CLOSED);
  set("hell", 1);

  setup();
}
