// Room: /u/shandong/ta-wuqidian.c

inherit ROOM;

void create() {
  set("short", "武器店");
  set("long", @LONG
这里是一家武器店。屋里熊熊的炉火卷起一阵阵热浪向你扑面而来，
铁匠师傅正一锤又一锤的敲打着一块通红的铁块。四面的墙壁上挂满了
各种各样的兵器，墙角边还放着块黑幽幽的东西，仔细一看竟然是一块
上好的镔铁。正中的墙上写着一个刚劲有力的"武"字。
    往南走是一条东西走向的小街。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "south": __DIR__ "ta-road7",
  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/bingqi_boss": 1,
    ]));
  set("hell", 1);

  setup();
  replace_program(ROOM);
}
