// Room: /u/shandong/ta-zahuopu.c

inherit ROOM;

void create() {
  set("short", "杂货铺");
  set("long", @LONG
这里是一家普通的杂货铺。这家杂货铺的门面不是很大，卖的也只
不过是些寻常的小玩意。可能生意不是太好，这里很清淡。娄老板正靠
在柜台上打瞌睡。你可以(list)查看出售的物品， (buy)买下某物品，
如果你有没用的物品这里也折价收购。
LONG
  );
  set("exits", ([
    "north": __DIR__ "ta-road2",
  ]));
  set("objects",
    ([  //sizeof() == 1
    __DIR__ "npc/zahuo_boss": 1,
    ]));
  set("hell", 1);
  setup();
  replace_program(ROOM);
}
