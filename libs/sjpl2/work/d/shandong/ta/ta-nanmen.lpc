// Room: /u/shandong/ta-nanmen.c

inherit ROOM;

void create() {
  set("short", "泰安城南门");
  set("long", @LONG
这里是泰安城的南门。泰安原来只是个小镇，由于沾了泰山的光
渐渐发展成了一个小型城市。泰安城并不大但作买卖的人却不少，一
般的日常用品这里都能买到。一些附近的农民挑着扁担，在这里进进
出出。由于进出的人鱼龙混杂，官兵们盘查的非常严格。
    往北走是一条小街，两旁有几家店铺，
    往南走就出城了。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "ta-road8",
    "south": __DIR__ "guanroad1",
  ]));
  set("outdoors", "shandong");
  set("objects",
    ([  //sizeof() == 1
    "/npc/newnpc/xuantong": 1,
    ]));
  set("hell", 1);

  setup();
  replace_program(ROOM);
}
