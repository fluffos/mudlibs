// Room: /u/shandong/hl-caodi.c

inherit ROOM;

void create() {
  set("short", "黑龙潭的草地");
  set("long", @LONG
这里是一片绿油油的草地。一条小溪穿行其中，小溪旁有几条小蛇，
可能是听到你的脚步声了吧，都停止了爬行不停的吐着芯子。你不禁有
气，什么黑蛇、黑龙全都是瞎话！几条小蛇而已！东面有一条小径虽然
不是很明显，但终究逃不过你的眼睛。
    往东走是一条小径；
    往南走是一片回去的路了。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */

    "south": __DIR__ "hl-xiaojing2",
    "east": __DIR__ "hl-xiaojing3",
  ]));
  set("objects",
    ([  //sizeof() == 1
    "/npc/newnpc/pangshiqi": 1,
    ]));

  set("outdoors", "shandong");
  set("hell", 1);
  setup();

}
