inherit ROOM;
void create() {
  set("short", "裁缝店");
  set("long", @LONG
裁缝店里摆满了绫罗绸缎，这里专门订作，裁剪，改装各种衣衫．
这里做的衣服不但款式新颖，而且经久耐用．男女老少们穿的大部份
都是这里出的．门口有一个大木牌（ｓｉｇｎ）．
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "west": __DIR__ "qinglong-n1",
  ]));
  set("objects", ([
    __DIR__ "npc/caifeng": 1,
  ]));
  set("item_desc", ([
    "sign": @TEXT
这里是裁缝店，目前我们可订作（make）：

外衣（ｓｕｉｔ），

每件一两黄金．

例子：

make suit $HIY$ 金龙袍 robe

裁缝店就会帮你作一件黄色的 ｒｏｂｅ　叫＂金龙袍＂ 

$BLK$ - 黑色            $NOR$ - 恢复正常颜色
$RED$ - 红色            $HIR$ - 亮红色
$GRN$ - 绿色            $HIG$ - 亮绿色
$YEL$ - 土黄色          $HIY$ - 黄色
$BLU$ - 深蓝色          $HIB$ - 蓝色
$MAG$ - 浅紫色          $HIM$ - 粉红色
$CYN$ - 蓝绿色          $HIC$ - 天青色
$WHT$ - 浅灰色          $HIW$ - 白色

TEXT
  ]));

  setup();
  replace_program(ROOM);

}
