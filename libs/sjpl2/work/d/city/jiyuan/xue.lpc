inherit ROOM;
void create() {
  set("short", "寒烟翠");
  set("long", @LONG
碧云天，黄叶地，秋色连波，波上寒烟翠。
山映斜阳天接水，芳草无情，更在斜阳外。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "liu",
    "southwest": __DIR__ "nu",
  ]));
  set("objects", ([
    __DIR__ "npc/xue": 1,
  ]));

  setup();
}
