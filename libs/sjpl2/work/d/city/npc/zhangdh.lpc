#include <ansi.h>
inherit NPC;
string kill_passenger(object who);
void create() {
  set_name("张大狠", ({ "zhang dahen" }));
  set("gender", "男性");
  set("age", 35);
  set("str", 10);
  set("cor", 50);
  set("cps", 5);
  set("title", "长安地头蛇");
  set("per", 5);
  set("int", 20);
  set("force", 200);
  set("max_force", 200);
  set("force_factor", 4);
  set("attitude", "heroism");
  set("chat_chance_combat", 10);
  set("chat_chance", 20);
  set("chat_msg", ({
    (: random_move :),
    "张大狠扯开嗓子嚷道：老子武功天下无敌，总有一天要扫清江湖各大门派！\n"
  }));

  set("long", "长安地头蛇，整天横行霸道。\n");

  set("combat_exp", 70000);

  set_skill("dodge", 30);
  set_skill("parry", 30);
  set_skill("move", 30);
  set_skill("unarmed", 1);
  set_temp("apply/defense", 20);
  set_temp("apply/move", 20);

  setup();

  carry_object("/obj/cloth")->wear();
}
void init() {
  object ob;
  ::init();
  if (interactive(ob = this_player()) && !is_fighting()) {
    remove_call_out("checklooking");
    call_out("checklooking", 1, ob);
  }
}

void checklooking(object ob) {
  ob = this_player();
  if (random((int)ob->query("per")) < 5) {
    command("say 站住！你干什么的。");
    command("say 看你那贼眉鼠眼，尖嘴猴腮的样儿，决不是什么好东西！");
    kill_passenger(ob);
  }
}

string kill_passenger(object who) {
  kill_ob(who);
  return 0;
}
