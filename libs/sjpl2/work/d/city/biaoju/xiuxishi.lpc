//biaoju xiuxishi.c
inherit ROOM;

void create() {
  set("short", "休息室");
  set("long", @LONG
这里是休息室。房中靠墙摆这几张木床，屋子收拾的很干净，可以说是
一尘不染。房中的光线很暗，窗子也都关着，一进来你就不由自主的想睡觉。
LONG);


  set("exits",
    ([  //sizeof() == 4
    "east": __DIR__ "changlang1",
    ]));
  set("sleep_room", 1);
  set("if_bed", 1);
  set("no_clean_up", 0);
  setup();
}
