// common_b.c

inherit BULLETIN_BOARD;

void create() {
  set_name("《城南旧事》留言板", ({ "board" }));
  set("location", "/d/yangzhou/kezhan");
  set("board_id", "lark_b");
  set("long", "我们都有过去，都有想要忘掉却总是每每想起的昨天\n");
  setup();
  set("capacity", 150);
  replace_program(BULLETIN_BOARD);
}
