// netoff.c

#include "/doc/help.h"
inherit F_CLEAN_UP;
#include <net/daemons.h>

int main() {
  if (find_object(DNS_MASTER)) DNS_MASTER->send_shutdown();
  write("Can not find DNS_MASTER! Handle aborted! \n");
  write("Ok.\n");
  return 1;
}
