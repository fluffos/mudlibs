// Tie@fengyun
#include <room.h>
inherit ROOM;
void create() {
  set("short", "浩然堂");
  set("long", @LONG
这是一间明亮的大堂。丈余的中堂画的是风竹，两边一幅对联：
		一点浩然气
		十里快哉风
条桌上左镜右瓶，中间是前任老堂主的牌位．
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "south": __DIR__ "kuaixuan",
  ]));
  set("objects", ([
    __DIR__ "npc/master": 1,
  ]));
  set("invalid_startroom", 1);

  set("coor/x", 9);
  set("coor/y", 30);
  set("coor/z", 0);
  setup();

}
