//sku@00-11-25 
inherit ROOM;
void create()
{
	set("short", "门房");
	set("long", @LONG
门房里摆着一张桌子，一把太师椅。桌子上堆着一堆
登记本，太师椅上坐着一个人正在忙碌着为人登记。这里
是给外乡人进北京城登记的地方。
LONG
	);
	set("exits", ([ 
              "east" : __DIR__"nanmen",
              ]));
        set("objects", ([
                __DIR__"npc/menguan" : 1,
               ]) );
        set("no_fight", 1);
        set("no_kill", 1);
	set("coor/x",-1100);
	set("coor/y",1000);
	set("coor/z",0);
	setup();
	replace_program(ROOM);
}
