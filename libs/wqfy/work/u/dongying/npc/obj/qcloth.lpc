
#include <armor.h>
#include <ansi.h>
inherit CLOTH;

void create()
{
        set_name( HIC "青色武士服" NOR , ({ "cloth" }) );
        set_weight(2000);
        if( clonep() )
                set_default_object(__FILE__);
        else {
                set("value", 200);
                set("material", "cloth");
                set("unit", "件");
                set("armor_prop/armor", 15);
        }
        setup();
}
