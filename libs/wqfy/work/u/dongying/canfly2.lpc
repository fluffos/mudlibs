inherit ROOM;

void create()
{
	set("short", "屋顶");
	set("long", @LONG
站在这里,整个武士院的情况一目了然,你立刻就认清了方向.
LONG
	);
 set("exits",([
 "westdown" :__DIR__"gate",
               ]));
	set("no_magic",1);
	set("coor/x",0);
	set("coor/y",0);
	set("coor/z",0);
	setup();
        replace_program(ROOM);
}
