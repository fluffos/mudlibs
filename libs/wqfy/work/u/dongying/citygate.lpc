#include <ansi.h>

inherit ROOM;

void create()
{
        set("short", "城门");
        set("long", @LONG
###########################
LONG
        );
        set("exits", ([ 
  "east" : __DIR__"migong1.lpc",
]));
        set("objects", ([
                __DIR__"npc/wushi1" : 2,
        ]) );
		set("no_magic", 1);
        set("coor/x",0);
	set("coor/y",0);
	set("coor/z",0);
	setup();
}
void init()
{
add_action("do_dazuo","dazuo");
}

int do_dazuo()
{       message("vision","这里不能打坐\n",this_object());
		return 1;
		}
