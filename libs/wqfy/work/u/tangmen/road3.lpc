// Room: xiuxishi.c
inherit ROOM;
void create()
{
	set("short", "小路");
	set("long", @LONG
再往下走，就到了一个路口。
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"northdown" : __DIR__"road2",
	      "southup" :__DIR__"road",
	]));
	set("no_clean_up", 0);
	set("outdoors", "tangmen" );
set("coor/x",-1220);
	set("coor/y",-10);
	set("coor/z",-50);

	set("coor/x",-1220);
	set("coor/y",-10);
	set("coor/z",-50);
	setup();
	replace_program(ROOM);
}


