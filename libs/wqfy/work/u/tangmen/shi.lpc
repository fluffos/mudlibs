// Room: xiuxishi.c

inherit ROOM;

void create()
{
	set("short", "卧室");
	set("long", @LONG
唐门卧室.
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	
	           "east" :__DIR__"ting",
            
]));
set("objects", ([
       "/u/tangmen/npc/fang" : 1,

                        ]) );


	

	set("no_clean_up", 0);
	set("outdoors", "tangmen" );
set("coor/x",-1260);
	set("coor/y",-50);
	set("coor/z",-40);


	setup();
	replace_program(ROOM);
}

