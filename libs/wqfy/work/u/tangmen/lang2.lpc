// Room: xiuxishi.c

inherit ROOM;

void create()
{
	set("short", "走廊");
	set("long", @LONG
到处都是机关，走错一步，呵呵--
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
		"north" : __DIR__"lang1",
	      "south" :__DIR__"ting",
	]));

	

	set("no_clean_up", 0);
	set("outdoors", "tangmen" );
set("coor/x",-1250);
	set("coor/y",-40);
	set("coor/z",-40);

	setup();
	replace_program(ROOM);
}

