#include <ansi.h>
void create() {
  set_name("引线", ({ "line" }));
  set_weight(40);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "根");
    set("value", 100);
    set("material", "wood");
  }
  setup();
}
