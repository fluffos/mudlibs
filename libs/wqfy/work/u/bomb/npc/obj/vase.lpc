inherit ITEM;
inherit F_LIQUID;
void create()
{
	set_name("石臼", ({ "stonevase", "vase" }) );
	set_weight(700);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("long", "一个用来研磨颗粒得石臼\n");
		set("unit", "个");
		set("value", 100);
	}
	setup();

}
