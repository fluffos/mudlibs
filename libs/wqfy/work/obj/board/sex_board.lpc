inherit BULLETIN_BOARD;
#include <ansi.h>
void create() {
  set_name(HIM "文彩风流" NOR, ({ "sex_board" }));
  set("location", "/d/qianjin/pingfeng");
  set("board_id", "sex-board");
  set("long", "这是一个记载风流侠客的韵事。\n");
  setup();
  set("capacity", 1000);
  replace_program(BULLETIN_BOARD);
}
