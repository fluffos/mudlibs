inherit F_CLEAN_UP;
#include <ansi.h>

int main()
{
 object *user ;
 int i;
         user = users();
        message("system", HIR "\n 自动存档．．．", users());
        for(i=0; i<sizeof(user); i++) 
        user[i]->save();
        message("system", HIG "．．．存档完毕 \n" NOR, users()); 
 return 1;
}
