inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIC "听雨轩" NOR);
  set("long", @LONG
整个梅园摆设最简单的，莫过于这间听雨轩了。整间
屋子是由竹子搭建起来的，房间三面均可看到外面，屋子
正中间相对摆放了两把竹椅，其他就没有什么东西了。据
说梅园主人喜欢在这里一边听下雨的声音。一边思考事情。

LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "down": __DIR__ "xuanyuan",
    "up": __DIR__ "sihai"

  ]));

  set("coor/x", -640);

  set("coor/y", -490);
  set("coor/z", 30);
  setup();
}
