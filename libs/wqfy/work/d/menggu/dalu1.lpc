// dalu1.c
// data by xgchen
#include <room.h>
inherit ROOM;
void create()
{
        set("short", "大路");
        set("long", @LONG
路的东边是一座小山。山上郁郁葱葱，种满了树。
LONG);
        set("exits", ([
            "eastup" : __DIR__"zhenyaota",
            "west"     : __DIR__"dadao3",
        ]));
//        set("no_clean_up", 0);
        set("outdoors", "hangzhou");
        setup();
}
