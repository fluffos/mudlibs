// Copyright (C) 1995, by Tie Yu and Daniel Yu. All rights reserved.
// This software can not be used, copied, or modified in any form without
// the written permission from authors.
inherit ROOM;
void create()
{
        set("short", "百花谷外");
        set("long", @LONG

爬上陡峭的山崖，放眼望去，只见天地间仿佛已被鲜花充满，
但却是无人认得出的花朵的名字，只觉得这些花无比的鲜艳，
无比的美丽。真是令人想不到，无名山中，竟有这样的一个地方。
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "south" : __DIR__"road1",
        ]));
        set("objects", ([
                __DIR__"npc/laoren" : 1,
       ]) );
        set("outdoors", "fengyun");
        set("coor/x",80);
        set("coor/y",1040);
        set("coor/z",100);
        setup();
        replace_program(ROOM);
}
