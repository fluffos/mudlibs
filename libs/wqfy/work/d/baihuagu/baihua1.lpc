inherit ROOM;

void create()
{
        set("short", "上山路");
        set("long", @LONG
这是一条极其隐蔽的小路，两旁是茂密的树木。但前面白雾
朦朦，路已经看不到了。
LONG
        );
        set("exits", ([ /* sizeof() == 2 */
  "northup" : __DIR__"baihua2",
  "eastdown" : __DIR__ "huacong1",
]));
        set("outdoors", "fengyun");
        set("coor/x",80);
        set("coor/y",1030);
        set("coor/z",50);
        setup();
        replace_program(ROOM);
}
