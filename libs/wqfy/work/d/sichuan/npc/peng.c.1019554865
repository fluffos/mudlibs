// waiter.c

inherit NPC;
inherit F_VENDOR;

void create()
{
asdas
q
q
	set("gender", "男性" );
	set("age", 41);
	set("long",
		"这位彭老板一脸和气，正笑咪咪地忙著，还不时招呼各位。\n");
	set("combat_exp", 30);
	set("attitude", "friendly");
	set("rank_info/respect", "老板");

        set("chat_msg", ({
                "彭老板自言自语：现在生意不好做呀。。．．\n",
		  "彭老板自言自语：菜如果再做不好，我就让那个掌勺的下岗。\n",
                "彭老板叹了口气：俞师傅去那里了呢？。\n",

        }) );

set("vendor_goods", ([
                "/obj/example/chicken_leg" : 10,
                "/d/fy/npc/obj/qcsh" : 10,
                "/d/fy/npc/obj/yxrs" : 10,
	]) );

	setup();
}

void init()
{	
	object ob;

	::init();
	//if( interactive(ob = this_player()) && !is_fighting() ) {
	//	remove_call_out("greeting");
	//	call_out("greeting", 1, ob);
	//}
	add_action("do_vendor_list", "list");
}
