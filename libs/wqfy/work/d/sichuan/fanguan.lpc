inherit ROOM;
void create()
{
        set("short", "四海春饭馆");
        set("long", @LONG
也许你并不知道蜀中的唐家用毒天下一流，但你肯定吃过蜀中的川菜，四海春饭
馆是蜀中最有名气的川菜馆，南来的，北往的，都喜欢在这里歇脚，点上几盘麻
辣可口的小菜，喝着陈年的老酒。
LONG
        );

        set("exits", ([ /* sizeof() == 4 */
  	"south" : __DIR__"xiaozhen",
  		]) );

	set("objects", ([
		__DIR__"npc/peng" : 1,
                       __DIR__"npc/waiter" : 1,

	]) );

   	set("outdoors","taoyuan");
        set("coor/x",1500);
	set("coor/y",980);
set("coor/z",-100);
	set("coor/x",1500);
	set("coor/y",980);
	set("coor/z",-100);
	setup();
        replace_program(ROOM);

}
