#include <ansi.h>
inherit ROOM;
void create() {
  set("short", YEL "龙亭台阶" NOR);
  set("long", @LONG
这里有长达72级的宽大石阶，两旁砌磴，正中石质御道上蟠龙
盘绕，直通龙亭大殿。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "southdown": __DIR__ "yudao",
    "northup": __DIR__ "longting",
  ]));
  set("objects", ([
  ]));

  set("outdoors", "fengyun");
  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
}
