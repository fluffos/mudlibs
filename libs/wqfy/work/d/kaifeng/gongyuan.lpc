#include <ansi.h>
inherit ROOM;
void create() {
  set("short", YEL "贡院" NOR);
  set("long", @LONG
这里是开封汴梁大宋朝的贡院，也就是全国最高的学府了，同时
这里也是朝廷开科取士的地方。这里的老师可是最好的老师呀！
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "wroad1",
  ]));
  set("objects", ([
  ]));

  set("outdoors", "fengyun");
  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
}
