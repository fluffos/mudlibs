#include <ansi.h>
inherit	ROOM;
void create()
{
	 set("short", "侠隐巷");
	set("long", @LONG
相对于街口的热闹,这里宁静多了,这里是开封城里最宁静的角
落,路上的你不时遇到些奇侠异客,但他们都是目光和蔼,与世
无争。
LONG
	);
	set("exits", ([	/* sizeof() == 4 */
     "east" : __DIR__"sroad4",
 "west" : __DIR__"xy",
]));	    
	set("objects", ([
			]) );

	set("outdoors",	"fengyun");
	set("coor/x",55);

	set("coor/y",-20);
	set("coor/z",0);
	setup();
	replace_program(ROOM);
}
