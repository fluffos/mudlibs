#include <ansi.h>
inherit ROOM;
void create() {
  set("short", YEL "吏部大堂" NOR);
  set("long", @LONG        
这里是朝廷吏部大堂，掌管风云里各个官员的升迁废降管理,同
时也监管各个武林门派。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "yujie2",
  ]));
  set("objects", ([
  ]));

  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
}
