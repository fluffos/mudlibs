#include <ansi.h>
inherit ROOM;
void create() {
  set("short", "侠隐堂");
  set("long", @LONG
房子里的摆设极为简单，而且大多是手工编制而成的，后院里种
着各种蔬菜，真没想到在豪华富丽的开封京都也会有如此的地方。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "xyroad",
  ]));
  set("objects", ([
  ]));

  set("outdoors", "fengyun");
  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
}
