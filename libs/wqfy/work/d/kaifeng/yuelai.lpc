inherit ROOM;
void create() {
  set("short", "悦来客栈");
  set("long", @LONG
这里可是汴梁的最高档的客栈，招牌竟然是用名贵的檀木烫金
制作，前厅装修的十分豪华，能来此居住的不是地方的官员，就
是富甲一方的豪富。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "sroad1",
  ]));
  set("objects", ([
    __DIR__ "npc/waiter": 1,
  ]));

  set("coor/x", 10);
  set("coor/y", -10);
  set("coor/z", 0);
  setup();
  replace_program(ROOM);
}
