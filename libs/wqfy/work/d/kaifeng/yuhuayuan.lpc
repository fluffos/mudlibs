#include <ansi.h>
inherit	ROOM;
void create()
{
	 set("short", YEL"御花园"NOR);
	set("long", @LONG
御花园是皇帝游玩的地方，这里亭台楼阁，数不尽数，奇花异木
罗列其间，珍奇鸟兽更是前所为见，人处其间，真如仙境。
LONG
	);
	set("exits", ([	/* sizeof() == 4 */
"east":__DIR__"lthoujie",
]));	    
	set("objects", ([
			]) );

	set("outdoors",	"fengyun");
	set("coor/x",55);

	set("coor/y",-20);
	set("coor/z",0);
	setup();
	replace_program(ROOM);
}
