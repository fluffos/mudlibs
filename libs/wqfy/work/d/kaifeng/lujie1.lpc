inherit ROOM;
void create() {
  set("short", "午朝门");
  set("long", @LONG
这里是皇城汴梁的午朝门，从此向南就是汴梁的御街，向北就是皇
宫了，这里戒备森严，除了朝廷官员，一般人是不让进入的。
独步天下，罕有对手。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
  ]));
  set("objects", ([
  ]));

  set("outdoors", "fengyun");
  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
}
