#include <ansi.h>
#include <ansi.h>
inherit	ROOM;
void create()
{
	 set("short", BLINK YEL "汴梁南门" NOR NOR);
	set("long", @LONG
这是大宋京都开封汴梁的南门，城墙有数长之高，都是有宽石建造
而成，十分的坚固，城门里有数名护卫看守。
LONG
	);
	set("exits", ([	/* sizeof() == 4 */
     "south" : __DIR__"lu",
    "north" : __DIR__"sroad4"
]));	    
	set("objects", ([
			]) );

	set("outdoors",	"fengyun");
	set("coor/x",55);

	set("coor/y",-20);
	set("coor/z",0);
	setup();
	replace_program(ROOM);
}
