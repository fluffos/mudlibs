#include <ansi.h>
inherit ROOM;
void create() {
  set("short", YEL "兵部大堂" NOR);
  set("long", @LONG        
这里是大宋朝的兵部大堂，这里庄严肃穆，显示一个大国的军威，
国家的各种军事命令都是由这里发往全国的。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "east": __DIR__ "bingbugate",
  ]));
  set("objects", ([
  ]));

  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
}
