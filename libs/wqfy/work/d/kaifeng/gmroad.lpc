#include <ansi.h>
inherit	ROOM;
void create()
{
	 set("short", YEL"宫门内大街"NOR);
	set("long", @LONG
这里是深宫要道，向北就到了紫禁城的后门，向南直达龙亭，向
东西都是各位宾妃居处。
LONG
	);
	set("exits", ([	/* sizeof() == 4 */
"north":__DIR__"zijinmen",
"south":__DIR__"lthoujie",
//"east":__DIR__"hougong1",
//"west":__DIR__"hougong2",
]));	    
	set("objects", ([
			]) );

	set("coor/x",55);

	set("coor/y",-20);
	set("coor/z",0);
	setup();
	replace_program(ROOM);
}
