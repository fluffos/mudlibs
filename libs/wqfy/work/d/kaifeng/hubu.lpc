#include <ansi.h>
inherit ROOM;
void create() {
  set("short", YEL "户部大堂" NOR);
  set("long", @LONG        
这里是朝廷户部大堂，掌管风云里各个居民的户籍管理,同时也
是给居民提供更名,修建房屋等管理的机关。
LONG
  );
  set("exits", ([ /* sizeof() == 4 */
    "west": __DIR__ "yujie1",
  ]));
  set("objects", ([
  ]));

  set("coor/x", 55);

  set("coor/y", -20);
  set("coor/z", 0);
  setup();
}
