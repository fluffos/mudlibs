// TIE@FY3 ALL RIGHTS RESERVED
inherit	ROOM;
void create()
{
	set("short", "墨蛛分舵");
	set("long", @LONG
这里是墨蛛分舵，房间里布置的古朴而不是尊严，但你不难发现有一个特色，那就
是屋内都布置成了墨色：墨色的墙，墨色的门，墨色的窗...
正对面一张挂毯，上面绣着一只张牙舞爪的八爪蜘蛛。

LONG
	);
	set("exits", ([	
  "south" : __DIR__"garden2",
   "west" : __DIR__"dadian2",
]));
	set("objects", ([  
		__DIR__"npc/mozhu" : 1,
		__DIR__"obj/guatan" : 1,
       ]) );

	set("coor/x",-50);
	set("coor/y",-410);
	set("coor/z",0);
	set("coor/x",-50);
	set("coor/y",-410);
	set("coor/z",0);
	set("coor/x",-50);
	set("coor/y",-410);
	set("coor/z",0);
	setup();
	replace_program(ROOM);
}