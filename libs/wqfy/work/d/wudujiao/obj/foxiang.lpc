inherit	ITEM;

void create()
{
	set_name("佛香", ({ "foxiang" ,	"佛香"}) );
	set_weight(5);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "只");
		set("long", "一只被点燃的佛香。	\n");
		set("value", 2);
	}
}