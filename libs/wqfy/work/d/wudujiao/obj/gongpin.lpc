inherit	ITEM;

void create()
{
	set_name("供品", ({ "gongpin" ,	"供品"}) );
	set_weight(1);
	if( clonep() )
		set_default_object(__FILE__);
	else {
		set("unit", "盘");
		set("long", "一盘色彩斑斓的供品。 \n");
		set("value", 2);
	}
}