// chaoyao.c

inherit COMBINED_ITEM;

void create() {
  set_name("普通草药", ({ "caoyao", "yao" }));
  set("long",
    "这是一捆看上去普普通通的草，看上去是制造解毒丹药的好材料。\n");
  set("unit", "捆");
  set("base_unit", "捆");
  set("base_weight", 5);
  set("value", 1000);
  set_amount(1);
}
