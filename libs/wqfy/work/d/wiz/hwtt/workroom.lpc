inherit ROOM;
#include <ansi.h>
void create() {
  set("short", HIY "听涛小筑" NOR);
  set("long", @LONG
这里是一间整洁明亮大厅。窗旁摆放着一盆娇兰，衬托出主人清雅脱俗。
屋内正中摆放着一个书桌，迎面的墙上有一个横幅，上面是几个大字(zi)。
书桌傍边有一个的古筝，地上有几本翻开的曲谱(qupu)。似乎一切都给人
一种宁静温馨的气氛。


                        逍   遥   快   乐



LONG
  );

  set("no_magic", "1");
  set("no_dazuo", "1");
  set("objects", ([ /* sizeof() == 1 */
    //  __DIR__"npc/chatgirl" : 1,
  ]));

  set("no_fight", 1);
  set("exits", ([ /* sizeof() == 3 */
    "job": "/d/wiz/jobroom",
    "down": "/d/fy/church",
    "north": __DIR__ "keting",
  ]));
  set("no_spells", 1);
  set("no_death_penalty", 1);

  setup();
  // call_other( "/obj/board/wizard_m", "???" );
  //  replace_program(ROOM);
}
