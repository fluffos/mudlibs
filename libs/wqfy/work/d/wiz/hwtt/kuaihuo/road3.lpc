//writted by tiandi
inherit ROOM;
void create() {
  set("short", "山路");
  set("long", @LONG
黄山的美，不是在于它的山多高，而是在于它的风。无论你站在何处，你都能感受到
一股清凉的风，时时刻刻地抚摩着你。
LONG
  );
  set("exits", ([
    "westdown": __DIR__ "roadend",
    "eastup": __DIR__ "road2",
  ]));
  set("outdoors", "huangshan");
  set("coor/x", -660);
  set("coor/y", -530);
  set("coor/z", 20);
  set("no_magic", 1);
  setup();
  replace_program(ROOM);

}
