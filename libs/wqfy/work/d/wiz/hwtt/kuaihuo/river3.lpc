//writted by tiandi
inherit ROOM;
void create()
{
        set("short", "小河流边");
        set("long", @LONG
你是不是没有想到房后竟然是这么一个世界，绿绿的草皮，悠悠的小河，时而还有鸟儿在歌唱。
要不是自己的那一跳，怎么会来到这里呢？你不由地笑了起来。
LONG
        );
	set("exits", ([ 
  		"south" : __DIR__"river4",
		"north" : __DIR__"river2",
]));
	set("no_magic",1);
	setup();
}
