//3300,0,10
inherit ROOM;
void create() {
  set("short", "山路");
  set("long", @LONG
自古华山一条道，但是黄山却不是如此，黄山上上下下道路不计其数，只是有些鲜为人知，这
条便是其中的一条。由采石峰向西而行，可遇一条小道，沿道直行，便可来此。
LONG
  );
  set("exits", ([
    "west": __DIR__ "road1",
    "east": "/d/huangshan/caishi",
  ]));
  set("outdoors", "huangshan");
  set("coor/x", -650);
  set("coor/y", -530);
  set("coor/z", 40);
  set("no_magic", 1);
  setup();

}
