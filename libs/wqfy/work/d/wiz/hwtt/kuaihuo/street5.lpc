//writted by tiandi
inherit ROOM;
void create()
{
        set("short", "一字长街");
        set("long", @LONG
快活城内的道路简简单单，一条主干道自西向东，名为一字。每日都有快活城的弟子巡逻，负责城
里的安全。这里并不富丽堂皇，但是人人皆流出喜悦的心情，无忧无虑，确实快活至极。这里已经是长
街的最西面了，再过去就是平时快活城弟子练武的广场了。
LONG
        );
 	set("exits", ([ 
		  "west" : __DIR__"square",
		  "east" : __DIR__"street4",
		]));         
       set("objects", ([       
		 __DIR__"npc/guards" : 2,	
	]) );
	set("no_magic",1);
	set("outdoors", "kuaihuo");
	setup();
}
