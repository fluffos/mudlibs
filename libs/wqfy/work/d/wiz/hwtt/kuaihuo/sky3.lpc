//writted by tiandi
inherit ROOM;
void create() {
  set("short", "天空中");
  set("long", @LONG
它带着你盲目地飞着，你根本不知道目的地是何处，只希望它能快点停下来。远离地面数百丈，
你要是掉了下去，嘿嘿，没准还能活着，你要不试试？
LONG
  );
  set("outdoors", "kuaihuo");
  set("no_magic", 1);
  setup();
}
