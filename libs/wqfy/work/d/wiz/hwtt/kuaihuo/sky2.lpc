//writted by tiandi
inherit ROOM;
void create() {
  set("short", "天空中");
  set("long", @LONG
人在半空，你是拿它一点办法都没有，要是能的话，你真想把它宰了烤了吃。想到这里，你的口水
都快要留出来了。
LONG
  );
  set("outdoors", "kuaihuo");
  set("no_magic", 1);
  setup();
}
