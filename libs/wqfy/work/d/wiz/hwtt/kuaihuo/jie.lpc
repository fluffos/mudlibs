//writted by tiandi
inherit ROOM;
void create()
{
        set("short", "台阶");
        set("long", @LONG
再往上走，就是快活王所住的欢喜殿了。往东却是个宽敞的广场。
LONG
        );
 	set("exits", ([ 
		  "eastdown" : __DIR__"square",
 		"westup" : __DIR__"dian",

		]));         
	set("no_magic",1);
	setup();
}
