//writted by tiandi
inherit ROOM;
void create() {
  set("short", "神秘树上");
  set("long", @LONG
站在这里，你可以看见一望无际的绿林，茫茫的一片，似乎永远没有尽头。夕阳照
下，你突然觉得这里好美。你仔细一听，却听到鸟叫的声音，随即望去，你“啊”
了一声，原来树上竟有个鸟巢。
LONG
  );
  set("exits", ([
    "down": __DIR__ "tree2",
  ]));
  set("item_desc", ([
    "鸟巢": "里面整齐地排着几个鸟蛋，不过却似少了一个。\n"
  ]));
  set("objects", ([
    __DIR__ "npc/bird": 1,
  ]));
  set("no_magic", 1);
  setup();
}
