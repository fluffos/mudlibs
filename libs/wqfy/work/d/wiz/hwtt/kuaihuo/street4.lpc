//writted by tiandi
inherit ROOM;
void create()
{
        set("short", "一字长街");
        set("long", @LONG
快活城内的道路简简单单，一条主干道自西向东，名为一字。每日都有快活城的弟子巡逻，负责城
里的安全。这里并不富丽堂皇，但是人人皆流出喜悦的心情，无忧无虑，确实快活至极。
LONG
        );
 	set("exits", ([ 
		  "west" : __DIR__"street5",
		  "east" : __DIR__"street3",
		  "north": __DIR__"lou",
		  "south": __DIR__"jia",
		]));         
     /*  set("objects", ([       
		 __DIR__"npc/guard" : 1,	
	]) );*/
	set("no_magic",1);
	set("outdoors", "kuaihuo");
	setup();
}
