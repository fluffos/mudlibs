//writted by tiandi
inherit ROOM;
void create()
{
        set("short", "城门内");
        set("long", @LONG
走进快活城，你不禁有些压抑感，浑身皆起鸡皮疙瘩，觉得浑然不对劲。来往的人群中，个个看似
简单，实际上却身怀武功，有的还是一等一的高手，跟风云城里的百姓全然不同。
LONG
        );
 	set("exits", ([ 
		  "west" : __DIR__"street1",
		  "east" : __DIR__"outgate",
		]));         
      /* set("objects", ([       
		 __DIR__"npc/guard" : 2,	
	]) );*/
	set("no_magic",1);
	set("outdoors", "kuaihuo");
	setup();
}
