//writted by tiandi
inherit ROOM;
void create() {
  set("short", "兵器铺");
  set("long", @LONG
这里是快活城内唯一的买兵器的地方。虽然在这里没有什么上等的兵器，但是想要在城内动刀动枪的，
一定非得来这里不成。
LONG
  );
  set("exits", ([
    "south": __DIR__ "street2",
  ]));
  set("objects", ([
    __DIR__ "npc/weaponer": 1,
  ]));
  set("no_magic", 1);
  setup();
}
