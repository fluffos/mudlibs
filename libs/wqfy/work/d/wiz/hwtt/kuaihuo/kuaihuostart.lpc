//writted by tiandi
inherit ROOM;
void create() {
  set("short", "树林的边缘");
  set("long", @LONG
西边是一片大树林，也许再往里走，你将永远也出不来了。你是不是已经决定了要进去？
LONG
  );
  set("exits", ([
    "west": __DIR__ "tree1",
    "east": __DIR__ "roadend",
  ]));
  set("outdoors", "kuaihuo");
  set("no_magic", 1);
  setup();

}
