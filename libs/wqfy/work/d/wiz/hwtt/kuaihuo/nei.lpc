//writted by tiandi
inherit ROOM;
void create()
{
        set("short", "内殿");
        set("long", @LONG
这里有张龙椅，左边扶手上是条青龙，右边的则是条白龙。靠墙边还有张桌子。
LONG
        );
 	set("exits", ([ 
		  "east" : __DIR__"dian",
		]));         
       set("objects", ([       
		 __DIR__"npc/king" : 1,	
		 __DIR__"npc/dugu" : 1,	

	]) );
	set("no_magic",1);
	setup();
}
