//writted by tiandi
inherit ROOM;
void create() {
  set("short", "楼外楼");
  set("long", @LONG
楼外楼的名气在快活城内可是数一数二的，不是因为它的菜，也不是因为它的酒，而是因为它
的主人，正是快活城的色使。
LONG
  );
  set("exits", ([
    "south": __DIR__ "street4",
    "up": __DIR__ "lou2",
  ]));
  set("objects", ([
    __DIR__ "npc/waiter": 1,
  ]));
  set("no_magic", 1);
  setup();
}
