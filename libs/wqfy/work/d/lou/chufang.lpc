inherit	ROOM;
void create()
{
	set("short", "品香园");
	set("long", @LONG
这里是楼主款待贵客的地方，屋里布置的古色古香。名人对联、字画很有讲究。
更诱人的是，后院厨房飘来的阵阵菜香，那是由京城最著名的王一刀做满汉全席。
真是个吃饭的好地方。
LONG
	);
	set("exits", ([	/* sizeof() == 4 */
  "north" : __DIR__"goldlion",
]));
      //  set("objects", ([
     //	  __DIR__"npc/goldcook"	: 1,
      //		  ]) );

	set("coor/x",10);
	set("coor/y",30);
	set("coor/z",0);

	setup();
	replace_program(ROOM);
}