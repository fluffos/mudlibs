inherit	ROOM;
void create()
{
	 set("short", "清风细雨楼大厅");
	set("long", @LONG
地上铺着厚厚的猩猩红地毯，中间一把虎皮的太师椅，两边各有两把上等楠木雕刻
的椅子。大厅正中悬挂着大牌子，同样是古色古香，但是却多了几分庄严肃穆。这里是
楼主和兄弟们议事的地方，平时别人不能进入。
LONG
	);
	set("exits", ([	/* sizeof() == 4 */
  "south" : __DIR__"woshi",
  "north" : __DIR__"tingyuan2",
  "east" : __DIR__"baihu",
  "eastup" : __DIR__"bailou",
  "west" : __DIR__"qinglong",
  "westup" : __DIR__"honglou",
]));
	set("objects", ([
	__DIR__"npc/dizi3" : 3,

			]) );

	set("coor/x",50);
	set("coor/y",-20);
	set("coor/z",0);
	setup();
	replace_program(ROOM);
}