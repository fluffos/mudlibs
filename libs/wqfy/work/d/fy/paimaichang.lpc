inherit ROOM;
void create()
{
        set("short", "风云拍卖行");
        set("long", @LONG
这里就是风云的拍卖行，这里有许多珍贵的物品，拍卖师
正在现在准备着拍卖的物品，一个个英雄都坐得好好的，面目
紧张，神色凝重。
paimai      拍卖的指令
yao         购得的事物
paimailist  拍卖的列表
LONG
        );
        set("exits", ([ /* sizeof() == 4 */
  "down" : __DIR__"yuljade",
]));
        set("objects", ([
//        __DIR__"npc/paimaishi" : 1,
                        ]) );

	set("coor/x",-1);
	set("coor/y",-10);
        set("coor/z",10);
	set("coor/x",-1);
	set("coor/y",-10);
	set("coor/z",-10);
	set("coor/x",-1);
	set("coor/y",-10);
	set("coor/z",0);
	set("coor/x",-1);
	set("coor/y",-10);
	set("coor/z",0);
	setup();
        replace_program(ROOM);
}
