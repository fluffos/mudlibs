#include <ansi.h>
inherit ITEM;
void create() {
  set_name(HIW "血书" NOR, ({ "xuebook" }));
  set_weight(1);
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "张");
    set("material", "paper");
  }
  setup();
}
