// 这是玩家自创武功程序 原由maxim@nju_fengxue编写，现由xgchen@zzfy修改
// bww
// 霸王丸
// 梦幻城　第八代前任城主
// 13651311
// 男性
// blade
// 天霸封神裂斩





inherit SKILL;

////////////////////////////////

mapping *action = ({

// ZHAOSHI :0

([
"action" :"$N一凝神，手中的河豚毒飘忽的对准$n的咽喉刺出。",
"damage" :50,
"dodge" :20,
"parry" :20,
"damage_type": "割伤",
"skill_name" : "海客谈嬴州"
]),
// ZHAOSHI :1

([
"action" :"$N一运气，手中的$w竟发出无穷的霸气，简简单单的劈向$n的$l。",
"damage" :80,
"dodge" :10,
"parry" :20,
"damage_type": "割伤",
"skill_name" : "越人语天姥"
]),
// ZHAOSHI :2

([
"action" :"$N一式$HIB$黄河之水天上来$NOR，$w发出的无尽的霸气席卷$n的全身，$n受到严重的创伤！",
"damage" :100,
"dodge" :10,
"parry" :10,
"damage_type": "割伤",
"skill_name" : "将进酒"
]),
// ZHAOSHI :3

([
"action" :"$N挥出$w，一股刀气已在$n上横穿而过，$n的血泳了出来！",
"damage" :130,
"dodge" :5,
"parry" :10,
"damage_type": "割伤",
"skill_name" : "蜀道难"
]),
// ZHAOSHI :4

([
"action" :"$N一式简单的横劈，$n却没有避开，$l受到了$w的攻击，竟然无法恢复伤口！",
"damage" :150,
"dodge" :-5,
"parry" :5,
"damage_type": "割伤",
"skill_name" : "霸王劈"
]),
// ZHAOSHI :5

([
"action" :"$N使出了必杀技，天地为之变色，$w犹如一团光芒在$n身上游走!",
"damage" :180,
"dodge" :-5,
"parry" :5,
"damage_type": "割伤",
"skill_name" : "天霸封神斩"
]),
// ZHAOSHI :6

([
"action" :"$N凝神聚气，手中的$w竟渐渐发出耀眼的光芒，闪电般的劈向$n!",
"damage" :200,
"dodge" :-10,
"parry" :5,
"damage_type": "割伤",
"skill_name" : "超天霸封神斩"
]),
// ZHAOSHI :7

([
"action" :"$N使出了天霸封神斩的终式危炜罩辛殉鲆坏拦饷⒄赵w上，$w发出的霸气竟使流水突然停顿，$n目瞪口呆，坐以待毙！",
"damage" :220,
"dodge" :-10,
"parry" :5,
"damage_type": "割伤",
"skill_name" : "无式"
]),
 });
// ZHAOSHI :8
int valid_learn(object me)
{
object weapon; if (!objectp(weapon = me->query_temp("weapon"))
     || (string)weapon->query("skill_type") != "blade") return notify_fail("你使用的武器不对。\n");
	if( (int)me->query("max_force") < 50 )
		return notify_fail("你的内力太弱，无法练"+"天霸封神裂斩"+"。\n");
	return 1;
}
int valid_enable(string usage) { return usage=="blade" || usage=="parry"; }
mapping query_action(object me, object weapon)
{
        return action[random(sizeof(action))];
}
int effective_level() { return 30;}
int practice_skill(object me)
{
object weapon; 
if (!objectp(weapon = me->query_temp("weapon")) || (string)weapon->query("skill_type") != "blade")
                      return notify_fail("你使用的武器不对。\n");	
if( (int)me->query("kee") < 40 )
		return notify_fail("你的体力不够了，休息一下再练吧。\n");
	if( (int)me->query("force") < 40 )
		return notify_fail("你的内力不够了，休息一下再练吧。\n");
	me->receive_damage("kee", 40);
	me->add("force", -40);
	return 1;
} 
string *parry_msg = ({
	"$n使出一招虚招，手中$v虚点一招，突然回招换式，护住$n的全身。\n",
});
string *unarmed_parry_msg = ({
"$n一招反攻，用手中的$v荡开了$N的招式。\n",
});
string query_parry_msg(object weapon)
{
        if( weapon )
                return parry_msg[random(sizeof(parry_msg))];
        else
                return unarmed_parry_msg[random(sizeof(unarmed_parry_msg))];
}
int learn_bonus()
{	return -150;
}
int practice_bonus()
{	return -150;
}
int black_white_ness()
{	return 30;
}
string perform_action_file(string action)
{
        return CLASS_D("player") + "/"+"weapon"+"/" + action;
}
