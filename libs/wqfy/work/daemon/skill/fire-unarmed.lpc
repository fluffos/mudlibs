// 这是玩家自创武功程序 原由maxim@nju_fengxue编写，现由xgchen@zzfy修改
// bbww
// 烈焰
// 普通百姓　老百姓
// 20497868
// 男性
// unarmed
// 怒焰伏龙





inherit SKILL;

////////////////////////////////

mapping *action = ({

  // ZHAOSHI :0

  ([
    "action": "$N右臂轻挑,双拳如火,一式「长相思」,径直击向$n后肩",
    "damage": 50,
    "dodge": 10,
    "parry": 10,
    "damage_type": "瘀伤",
    "skill_name": "长相思"
  ]),
  // ZHAOSHI :1

  ([
    "action": "$N足尖点地.单拳后撤,前臂一耸,一式「梅心惊破」,直攻$n眉心",
    "damage": 80,
    "dodge": 15,
    "parry": 15,
    "damage_type": "瘀伤",
    "skill_name": "梅心惊破"
  ]),
  // ZHAOSHI :2

  ([
    "action": "$N丹田用力,突发一式一拳早已击向$n胸口",
    "damage": 100,
    "dodge": 20,
    "parry": 20,
    "damage_type": "瘀伤",
    "skill_name": "一帘风月闲"
  ]),
  // ZHAOSHI :3

  ([
    "action": "$N暗暗聚力,双拳借力用力,一式急攻$n小腹",
    "damage": 130,
    "dodge": 25,
    "parry": 30,
    "damage_type": "瘀伤",
    "skill_name": "人比黄花瘦"
  ]),
  // ZHAOSHI :4

  ([
    "action": "$N高高跃起,一式已经朝向$n头部击去",
    "damage": 150,
    "dodge": 30,
    "parry": 40,
    "damage_type": "瘀伤",
    "skill_name": "天上人间"
  ]),
  // ZHAOSHI :5

  ([
    "action": "$N右手持龙首,左手一式招「永不言是」已经朝$n肩部击了过去",
    "damage": 180,
    "dodge": 40,
    "parry": 50,
    "damage_type": "瘀伤",
    "skill_name": "永不言是"
  ]),
  // ZHAOSHI :6

  ([
    "action": "$N双手朝天环龙,一招招「亢龙有悔」,恨恨的在$n面部就是一击",
    "damage": 200,
    "dodge": 50,
    "parry": 60,
    "damage_type": "瘀伤",
    "skill_name": "亢龙有悔"
  ]),
  // ZHAOSHI :7

  ([
    "action": "$N潇洒出了一式招「静」,$n的胸口已经被砸了一个大洞",
    "damage": 220,
    "dodge": 80,
    "parry": 70,
    "damage_type": "瘀伤",
    "skill_name": "静"
  ]),
});
// ZHAOSHI :8
int valid_learn(object me) {
  if (me->query_temp("weapon") || me->query_temp("secondary_weapon"))
    return notify_fail("练" + "怒焰伏龙" + "必须空手。\n");
  if ((int)me->query("max_force") < 50)
    return notify_fail("你的内力太弱，无法练" + "怒焰伏龙" + "。\n");
  return 1;
}
int valid_enable(string usage) { return usage == "unarmed" || usage == "parry"; }
int effective_level() { return 20; }
mapping query_action(object me, object weapon) {
  return action[random(sizeof(action))];
}
int practice_skill(object me) {
  if ((int)me->query("kee") < 30)
    return notify_fail("你的体力不够了，休息一下再练吧。\n");
  if ((int)me->query("force") < 30)
    return notify_fail("你的内力不够了，休息一下再练吧。\n");
  me->receive_damage("kee", 30);
  me->add("force", -30);
  return 1;
}
string *parry_msg = ({
  "$n利用身法引偏了$N的$w。\n",
});
string *unarmed_parry_msg = ({
  "$n轻描淡写的化解了$N的攻势。\n",
});
string query_parry_msg(object weapon) {
  if (weapon)
    return parry_msg[random(sizeof(parry_msg))];
  else
    return unarmed_parry_msg[random(sizeof(unarmed_parry_msg))];
}
int learn_bonus() {
  return 30;
}
int practice_bonus() {
  return -30;
}
int black_white_ness() {
  return 320;
}
string perform_action_file(string action) {
  return CLASS_D("player") + "/" + "unarmed" + "/" + action;
}
