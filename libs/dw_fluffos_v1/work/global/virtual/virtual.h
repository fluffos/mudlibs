// AGENTS.md conversion note: this must stay an angle-bracket include --
// it resolves via the configured include path to the DIFFERENT file
// /include/virtual.h, not to itself.
#include <virtual.h>

#define SERVER "/global/virtual/server"

#define LOAD   1
#define CLONE  2
