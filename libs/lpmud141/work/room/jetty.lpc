#include "std.h"

#undef EXTRA_RESET
#define EXTRA_RESET\
    if (!present("bag"))\
        clone_object("obj/bag")->move_object(resolve_ob(this_object()));
TWO_EXIT("room/vill_shore", "west",
	 "room/sea", "east",
	 "Jetty",
	 "You are at a jetty. The waves rolls in from east.\nA small path leads back to west.\n", 1)
