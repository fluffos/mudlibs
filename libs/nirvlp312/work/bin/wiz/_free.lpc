int cmd_free(string str) {
  object obj;
  obj = environment(this_player());

  this_player()->move_object(resolve_ob("/room/prison"));
  command("verte_clear "+str, this_player());
  this_player()->move_object(resolve_ob(obj));
  return 1;
}
