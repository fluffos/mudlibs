// Room: /d/jingcheng/ylby.c
// Made by: ysgl

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "[1;37m御林军营[2;37;0m");
  set("long", @LONG
御林军营里果然比别处兵营纪律森严。西边一排士兵正在操练兵器，
北面的兵丁在打桩练力气。靠门口处几员将军正在来回巡视。看他们
顶盔贯甲地走来走去，真是威风凛凛。
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "bdj2",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/fu-j": 3 + random(3),
  ]));
  set("no_clean_up", 0);

  setup();
}
