// Room: /d/jingcheng/nchkzh.c
// Made by: ysgl

#include <ansi.h>
inherit ROOM;

void create() {
  set("short", "[1;37m南城客栈[2;37;0m");
  set("long", @LONG
这家客栈紧靠京城中心，所以生意非常兴隆。兼之价钱低廉，更
是成了外地游客落脚的首选。露天摆了七八张桌子，三教九流人等
在此进进出出，络绎不绝，几个跑堂的小二来来回回招待着四方来
客。二楼上是雅厅，后面是客房。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "up": __DIR__ "nchkzh2",
    "west": __DIR__ "ndj1",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    __DIR__ "npc/xiaoer1": 1,
  ]));
  set("no_clean_up", 0);

  setup();
}
