#include <ansi.h>
#include <room.h>

inherit ROOM;
int drop_him(object me);
void create() {
  object san, sna;
  set("short", HIC "驿道" NOR);
  这 是 一 条 驿 道 ， 向 西 北 望 去 可 以 看 到 兰 州 城 了 ， 这 不 由 得 你 精 神 一 震 ， 加 快 了 脚 步 。

  一 条 驿 道 静 静 的 向 远 方 延 伸 ， 好 象 永 远 没 有 尽 头 。 LONG
);
set("exits", ([
  "southeast": __DIR__ "rcatolz1",
  "northwest": __DIR__ "lanzhou",
]));

set("outdoors", "road");

setup();

}

void init() {
  return;
}

int is_road() {
  return (1);
}
