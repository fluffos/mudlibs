// judge.c

#include <login.h>
#include <ansi.h>

inherit NPC;

int in_ask, answer;

void create()
{
	set_name("糵﹛", ({ "judge" }) );
	set("short", "盡癸诀竟" HIY "糵﹛" NOR "(judge)" );
	set("long",
		"硂琌︗糵琩產琌琌诀竟糵﹛ゲ斗氮癸(answer)拜肈\n"
		"叉瞒磝搐\n");

	set("gender", "╧┦");
	set("age", 40);

	set("str", 100);
	set("int", 100);
	set("cor", 100);

	set("combat_exp", 9000000);
	set("score", 0);

	set_temp("apply/attack", 200);
	set_temp("apply/damage", 500);

	setup();

	in_ask = 0;
}

void init()
{
	::init();
	if( !query_heart_beat(this_object()) ) set_heart_beat(1);
	add_action("do_answer", "answer");
}

void chat()
{
	int a, b, c;
	string oper;
	
	if( in_ask || random(20) > 10 ) return;
	delete_temp("wrong");
	a = random(100) + 1;
	b = random(100) + 1;
	switch(random(5)) {
		case 0: 
			answer = a + b;
			oper = chinese_number(a) + "" + chinese_number(b)+ "单ぶ";
			break;
		case 1: 
			answer = a - b;
			oper = chinese_number(a) + "搭" + chinese_number(b)+ "单ぶ";
			break;
		case 2: 
			if( a * b < 200 ) {
				answer = a * b;
				oper = chinese_number(a) + "" + chinese_number(b)+ "单ぶ";
			} else {
				answer = (a * b) % 10;
				oper = chinese_number(a) + "" + chinese_number(b)+ "︗计单ぶ";
			}
			break;
		case 3: 
			answer = a % b;
			oper = chinese_number(a) + "埃" + chinese_number(b)+ "緇ぶ";
			break;
		case 4:
			oper = chinese_number(a) + "㎝" + chinese_number(b)+ "程そ计琌ぶ";
			if( b < a ) { c = b; b = a; a = c; }
			while( a > 1 && b%a > 1 ) {
				c = a;
				a = b%a;
				b = c;
			}
			answer = b%a == 1 ? 1 : a;
			break;
	}
	command("say " + oper + "叫ず氮(answer)");
	in_ask = 1;
	call_out("say_answer", 20);
}

void say_answer()
{
	command("say 硂或虏虫常ぃ穦氮单" + chinese_number(answer) + "");
	in_ask = 0;	
}

int do_answer(string arg)
{
	string s;
	int ans;

	if( !in_ask ) return notify_fail("ゲ斗单肈\n");
	if( !arg ) return notify_fail("叫氮计\n");

	message_vision( CYN "$N氮笵" + arg + "\n" NOR, this_player());
	
	if( sscanf(arg, "%d", ans)==1 ) {
		if( ans==answer ) {
			this_player()->add_temp("robot_check", 1);
			command("say 氮癸");
			command("pat " + this_player()->query("id") );
			remove_call_out("say_answer");
			in_ask = 0;
			if( this_player()->query_temp("robot_check") >= 3 ) {
				command("say 癬ㄓぃ钩诀竟ǐ\n");
				if( stringp(s = this_player()->query_temp("old_startroom")) )
					this_player()->set("startroom", s);
				this_player()->delete_temp("robot_check");
				if( stringp(s = this_player()->query_temp("old_location")) )
					this_player()->move(s);
				else
					this_player()->move(START_ROOM);
			}
		} else {
			command("say 岿");
			add_temp("wrong/" + this_player()->query("id"), 1);
			if( query_temp("wrong/" + this_player()->query("id")) > 3 ) {
				command("say " + this_player()->name() + "倒и睹瞦摆\n");
				this_player()->receive_damage("kee", 100, this_object());
				this_player()->die();
			}
		}
	}
	return 1;
}
