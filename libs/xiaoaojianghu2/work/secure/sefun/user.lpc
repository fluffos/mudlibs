/* /secure/simul_efun/users.c
 * from XO Object library I
 * sort users
 * created by trill 11/3/97 11:23
 * version: @(#) user.c 1.0@(#)
 */

#ifndef SORT_D

#define SORT_D          "/system/daemon/sort"

#endif

object *users()                    //override efun users
{
  return efun::users();
  /*
   return sort_array( efun::users(), function( object Player1, object Player2 )
   {
   return ( - 1 ) * (  SORT_D->ComparePlayer( Player1, Player2 ) );    
   } ); */
}
