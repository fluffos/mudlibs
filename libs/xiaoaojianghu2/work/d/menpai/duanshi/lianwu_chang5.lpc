#include <room.h>
#include <ansi.h>

inherit ROOM;


void create()
{
        set("short", "练武场");
        set("long", @LONG
这里就是镇南王府的练武场了，练武场位于王府的正南方，边上有两排大柳
树，角落里是些石担、石锁、石轱辘。靠墙是一排木人，边上一排兵器架，王府
卫士们平时就在这里练功，地上踩了不少深深的脚印。练武场的东西两侧各有一
扇厚重的铁门。
LONG
        );
    set("exits", ([ 
                "north"  : __DIR__"lianwu_chang4",
                "east"   : __DIR__"wuqi_ku",
                        "west"   : __DIR__"kuijia_ku",
        ]));
        set("objects", ([
                        __DIR__"npc/weishi2" : random(5),
                        __DIR__"obj/shisuo"  : random(3),
                ]));
        set("outdoors", "dali");
  //      create_door("east", "那扇标有「武器库」的厚重铁门", "west", DOOR_CLOSED);
  //      create_door("west", "那扇标有「盔甲库」的厚重铁门",     "east", DOOR_CLOSED);
        setup();
}                       

