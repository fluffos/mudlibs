// Room: /d/menpai/wuliang/west/nvdizi_shi.c

inherit ROOM;



void create()
{
	set("short", "女弟子休息室");
	set("long", @LONG
这里是女弟子休息室，里头被打扫得很干净。门的左右两边各有几张床。床
上的被子叠得整整齐齐，旁边还放着几件干净的女服。屋子的里头摆着一副铜镜。
除此以外，你就找不着什麽装饰的东西。不过房间倒很宽敞，并无局促之感。
LONG
	);
	set("sleep_room", "无量剑派西宗");
	set("no_fight", 1);
	set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"xishi_lu",
]));
	set("no_clean_up", 0);

	setup();
	//place_program(ROOM);
}
