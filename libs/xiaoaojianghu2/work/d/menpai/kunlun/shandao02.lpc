// shandao02.c
// Ffox 98-4-13 20:27
#define KL_DOOR
#include "kl_r.h"



void create()
{
    set( "short", "山道" );
    set( "long", @LONG
一阵阵云雾象半透明的乳白色的轻纱从面前、身边不停的飘过，渐渐的眼前
到处都是白茫茫的一片。只让人觉得它柔软、清凉、轻盈、飘忽、不可捉摸。模
模糊糊的可以看见山道向北面延伸而上。
LONG );
    set("exits", ([ /* sizeof() == 2 */
    "southeast" : __DIR__"cemen",
    "northup" : __DIR__"shandao03",
]) );
    create_door( "southeast", "侧门", "northwest", DOOR_CLOSED );
    setup();
}
