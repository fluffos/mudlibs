// nansleep.c
// Ffox 98-4-13 20:08

#include "kl_r.h"



void create()
{
    set( "short", "男弟子卧室" );
    set( "long", @LONG
男弟子卧室，这里是昆仑门下男弟子休息的卧室，一般也将这里作为休息室
。这里并排摆着些床铺，地上也有些莆团，可以打坐、练习内功等。
LONG );
    set( "exits", ([ /* sizeof() == 1 */
    "north" : __DIR__"huilang03",
]) );
    set( "sleep_room", "昆仑派" );
    setup();
}
