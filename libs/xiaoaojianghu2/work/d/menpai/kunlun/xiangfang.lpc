// xiangfang.c
// Ffox 98-4-13 20:08

#include "kl_r.h"


void create() {
  set("short", "厢房");
  set("long", @LONG
厢房一间，供来这里的客人居住，房间里平铺着几张床，还摆有一张桌子，
在靠墙的地方竖着一个立柜，可以挂衣物等。窗明几净，光线甚好。弟子们很少
到这里来，主要是怕影响客人们的休息，可见昆仑门派的声望也不是白挣来的。
LONG);
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "huilang04",
  ]));
  set("objects", ([
    __DIR__ "npc/jianke": 1,
  ]));

  setup();
  //place_program(ROOM);
}
