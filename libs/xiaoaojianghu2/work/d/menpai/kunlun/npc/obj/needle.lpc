// needle.c       针
// Ffox 98-4-20 20:20

inherit ITEM;



void create() {
  set_name("针", ({ "needle", "zhen" }));
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "根");
    set("long", "一根针，用来缝补衣物之用。\n");
    set("value", 1);
  }
  setup();
}
