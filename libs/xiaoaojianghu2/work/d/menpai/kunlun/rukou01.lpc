// rukou01.c
// Ffox 98-4-13 19:49

inherit ROAD;


void create() {
  set("short", "山坳入口");
  set("long", @LONG
幽静，就是这里的特点，两边山峰高耸，顶峰和山麓都消失于那一片白色之
中，只露出山腰的一角，如飘自天边，显得那么轻盈。山脚处的路边生长着一些
不知名的杂草，再往里就是一个山坳了。
LONG);
  set("no_clean_up", 0);
  set("exits", ([ /* sizeof() == 2 */
    "northeast": __DIR__ "rukou02",
    "west": __DIR__ "gudi1",
  ]));
  setup();
  //place_program(ROOM);
}
