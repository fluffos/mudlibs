// langdao03.c
// Ffox 98-4-13 20:15

#include "kl_r.h"



void create()
{
    set("short", "廊道");
    set("long", @LONG
一条低矮的廊道，处于屋檐之下，廊道旁边是一排长凳，坐在凳子上，面对
的就是一个天井。廊道狭长，有很多木刻，雕刻着一些山水人物，虽然不大，但
也颇见雕刻的水平，也显出这里的气派。
LONG );
    set("no_clean_up", 0);
    set("exits", ([ /* sizeof() == 4 */
    "west" : __DIR__"bingqi",
    "north" : __DIR__"tianjing",
    "south" : __DIR__"shufang",
    "east" : __DIR__"langdao04",
]) );
    setup();
    //place_program(ROOM);
}
