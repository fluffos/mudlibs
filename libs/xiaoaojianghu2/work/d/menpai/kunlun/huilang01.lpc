// huilang01.c
// Ffox 98-4-14 17:30

#include "kl_r.h"



void create()
{
    set( "short", "回廊" );
    set( "long", @LONG
一道回廊，在练武场的周围，供人行走方便。回廊是木质结构，雕花栏杆，
很有特色，显现得很有气派的样子。回廊里会不时有弟子走过，大家都在忙着自
己的事情，行色匆匆，没有人来和你说话，这也显得昆仑派对本派弟子的严格要
求。
LONG );
    set( "no_clean_up", 0 );
    set( "exits", ([ /* sizeof() == 3 */
    "north" : __DIR__"chufang",
    "south" : __DIR__"wuchang",
    "east" : __DIR__"huilang02",
]) );
    set( "kunlun/water", 1 );
    setup();
    //place_program(ROOM);
}
