// diaopi1  银貂皮
// Ffox 98-4-20 20:20

#include "kl_o.h"



void create() {
  set_name(WHT "银貂皮" NOR, ({ "diao pi", "diaopi" }));
  if (clonep())
    set_default_object(__FILE__);
  else {
    set("unit", "张");
    //      set("no_get",1);
    set("long", "银貂皮，积20许方成一裘。");
    set("value", 500);
  }
  setup();
}
