// Room: /d/shaolin/banruo_tang2.c

inherit ROOM;



void create()
{
    set("short","首座居室");
    set("long", @LONG
空落落的屋子里，连张床都没有，只在靠墙处摆放着一个非常破旧的蒲团，
实在很难令人相信，这间简陋的小屋居然就是位高权重的般若堂首座玄苦大师住
的地方。
LONG
    );
    set("exits", ([ 
        "west"      : __DIR__"banruo_tang1.lpc",
    ]));

    set("objects",([
        __DIR__"npc3/xuanku":1,
    ]) );
    setup();
    //place_program(ROOM);
}
