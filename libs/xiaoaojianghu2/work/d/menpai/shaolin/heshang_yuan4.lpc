// Room: /d/shaolin/heshang_yuan4.c

inherit ROOM;



void create() {
  set("short", "僧舍");
  set("long", @LONG
一间空荡荡的僧舍，除了一张竹床，两个蒲团外什么都没有。这里只是供僧
人们清修和休憩的地方。东面有一扇小门，门外是一条弯弯曲曲的青砖小路，通
向前方。
LONG
  );
  set("exits", ([
    "east": __DIR__ "heshang_yuan1.lpc",
  ]));
  set("sleep_room", "少林派");
  set("need_clean", "少林派");

  set("objects", ([
    __DIR__ "npc/xuxian": 1,
  ]));

  setup();
  //place_program(ROOM);
}
