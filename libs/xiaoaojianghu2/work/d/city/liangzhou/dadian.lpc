//dadian.c

inherit ROOM;

void create() {
  set("short", "佛殿");
  set("long", @LONG
一间不算太大的佛殿，正中央是一座如来佛像，面带笑容，安详地接受众人
的礼拜。佛像前面是一个案几，上面放着香烛、供品。还有就是两个蒲团，一个
是供香客跪拜用，另一个正有一和尚坐在那里。
LONG
  );
  set("exits", ([
    "south": __DIR__ "tingyuan",
  ])
  );
  set("objects", ([
    __DIR__ "npc/fangzhang": 1,
  ]));
  //      //place_program(ROOM);
  setup();
}
