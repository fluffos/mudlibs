// Room: /d/city/matou/mache.c

inherit ROOM;

void create()
{
	set("short", "马车");
	set("long", @LONG
这是定期来往于扬州和大理之间的马车，车夫驾驭着四匹骏马。马车的车厢
宽敞，摆着几张凳子，坐着几个商人模样的人。由于大理到扬州的路遥远，又驿
道尚未完全修好，来往于大理和扬州的旅客大多搭乘马车。
LONG
	);
	set("exits", ([ ]));
	set("objects", ([ ]));
	set("detail", ([ ]));
	set("no_clean_up", 0);
	set("moving", 0);
	set("refuse_npc", 1);

	setup();
	//place_program(ROOM);
}
