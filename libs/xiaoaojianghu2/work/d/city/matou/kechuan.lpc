// Room: /d/city/matou/kechuan.c

inherit ROOM;

void create()
{
	set("short", "客船");
	set("long", @LONG
这是定期来往于扬州和汴京的单帆乌蓬客船，船尾几个船工在摇撸，中舱是
客舱，摆着几张凳子，坐着几个商人模样的人。由于汴京到扬州的路遥远，又驿
道尚未修好，来往于汴京和扬州的旅客大多搭乘客船。
LONG
	);
	set("exits", ([ ]));
	set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/chuanzhu" : 1,
]));
	set("detail", ([ ]));
	set("moving", 0);
//	set("no_fight", 1);
	set("refuse_npc", 1);

	setup();
	//place_program(ROOM);
}
