// room:/d/dali/yamen.c writen by king Jan-20-97

#include <room.h>
#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "府衙");
	set("long", @LONG
这是大理府衙所在地，黑色大门平时紧紧闭着，门前四个身着皂服的衙役没
精打采的拄着水火棍站着。边上有个大鼓。因为当时大理民风朴实，大理城作为
都城，治安情况特别好，平时衙门根本没什么喊冤的。难怪衙役们打不起精神了。
LONG
	);
    set("exits", ([ 
	        "east"  : __DIR__"beijie",
        ]));
	set("objects",([
		__DIR__"npc/yayi" : 1,
		]) );

//	set("objects", ([
//                   __DIR__"npc/waiter.lpc" : 1 ]) );
	
	setup();
	//place_program(ROOM);
}                       

