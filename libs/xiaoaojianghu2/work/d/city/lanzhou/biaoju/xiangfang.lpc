#include <room.h>

inherit DOOR_ROOM;

void create() {
  set("short", "厢房");
  set("long", @LONG
这里是镖局的仓库，存放着尚未送出的货物和镖局的财物，家当。平时是不
许人进来的。地上积了一层灰尘，几只箱子放在墙角。
LONG);
  set("exits", ([
    "east": __DIR__ "dayuan",
    "south": __DIR__ "chufang",
  ]));
  set("objects", ([
    __DIR__ "obj/shitou": 2,
    BLADE_DIR + "mudao": 2,
  ]));

  set("need_clean", "龙门镖局");
  //    create_door("east","门","west",DOOR_CLOSED);
  setup();
}
