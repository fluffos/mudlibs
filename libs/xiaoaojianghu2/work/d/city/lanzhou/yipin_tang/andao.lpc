//  一品堂的暗道    /d/city/lanzhou/yipin_tang/andao.c 
//  By Lala, 1998-03-20

inherit ROOM;

void create()
{
    set( "short", "暗道" );
    set( "long", @LONG
上面的翻板砰的一声合上了，这里面黑漆漆一团，什么也看不到，只能嗅到一
阵潮湿的霉腐味道。一面隐隐有些光亮，还有些凉湿的风吹进来，看来是个出
口了，不知道通向哪里。
LONG 
    );
    set( "exits", ([
        "out"       :   __DIR__"jing",      //一品堂的井
    ]) );    

    setup();
}

