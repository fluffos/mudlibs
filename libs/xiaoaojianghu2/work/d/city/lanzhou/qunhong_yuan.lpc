//  兰州群红院  /d/city/lanzhou/qunhong_yuan.c
//  By lin
//  Changed by Lala, 1998-03-20

inherit ROOM;

void create()
{
    set( "short", "群红院" );
    set( "long", @LONG
这里是兰州最有名的一家妓院，原本华丽无比。不过最近边境军事紧迫，州
府下令严禁优娼事务，因此这里一下子就冷清了。不知道什么时候才会再兴旺起
来。
LONG
    );
    set( "exits", ([
        "east"      :   __DIR__"nanjie1",   //南大街
    ]) );
    set( "objects", ([
        __DIR__"npc/chuner"     :   1,      //春儿
        __DIR__"npc/talker"     :   1,      //说书人
        NPC_DIR + "boy"         :   1,      //标准小男孩
    ]) );

    setup();
}
