// Room: /d/city/yangzhou/hejia_jie.c

#include <ansi.h>
inherit ROOM;



void create()
{
    set("short", YEL"禾嘉街"NOR);
    set("long", @LONG
禾嘉街南面是城守署，几个士兵在站岗，不时有士兵校尉进出。行人到此，
多不敢停留，怕惹是非。街道宽敞整洁，也少有泼皮在此打闹。北面多为民宅，
屋檐比次密接。中间有一小巷通向扬州治园名家「仇好石」的住所。
LONG
    );
    set("no_kill", "yangzhou");
    set("exits", ([ /* sizeof() == 2 */
        "west" : __DIR__"nanmen_dajie",
        "north" : __DIR__"rongxi_yuan",
        "east" : __DIR__"xiaodongmen_qiao",
    ]));
    set("objects", ([ /* sizeof() == */
        __DIR__"npc/xiaowei" : 2,
    ]));
    set("outdoors", "yangzhou");
    set("no_clean_up", 0);
    setup();
    //place_program(ROOM);
}

