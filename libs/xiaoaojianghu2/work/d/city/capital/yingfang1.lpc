// 汴京 yingfang1.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit SROOM;
void create()
{
    set("short","营房");
    set("long", @LONG            
这里是天武第七军的指挥所。平时军都指挥使大人常在这里和指挥使，都头
们商量些事情，或是传达宫中，上级的命令。屋中挂着兵甲腰刀等物，大案上一
个青缎面木匣子，大概是放兵符，军印一类的东西吧。
LONG
    );
    set("objects", ([ 
    ]));
    set("exits", ([    
        "south" : __DIR__"caolian_chang4",
    ]));
    set("detail",([
    ]));     
    setup();
}
