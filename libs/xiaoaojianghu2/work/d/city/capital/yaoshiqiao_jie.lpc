// 汴京   yaoshiqiao_jie.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
	set("short","药市桥街");
	set("long", @LONG  
街沿旧城北墙而行，南面是护城河，有个美名叫“景龙江”，金水河流至此
处，以高架水槽引过景龙江之上，穿过旧城城墙，入大内北苑----延福宫去了。
北面是一座气派堂皇的王府。东面是景龙门；西头拐角处有一座大庙。
LONG
	);
	set("objects", ([        
         ]));
	set("exits", ([  
	    "west" : __DIR__"chenghuang_miao",
	    "east" : __DIR__"jinglong_men",
    ]));
    set("detail",([
    ]));
	setup();
}
