// 汴京   yujie5.c
#include <room.h>
#include <ansi.h>
#include "bianjing.h"
inherit STREET;
void create() {
  set("short", HIW "御  街" NOR);
  set("long", @LONG
过了州桥向南，御路转向东弯了一下，又向南笔直延伸开去。。大道两旁尽
开些店铺茶肆，道上人来人往，车马喧沸，络绎不绝。北面是州桥。南面就是旧
城城墙了，高大的朱雀门几乎挡住了阳光。西面是一片热闹的市井。
LONG
  );
  set("objects", ([
  ]));
  set("exits", ([
    //	    "northwest" : __DIR__"tianhan_qiao",
    "south": __DIR__ "zhuque_men",
    "east": __DIR__ "chejia_tandian",
  ]));
  set("detail", ([
  ]));
  setup();
}
void init() {}
