// 汴京 yingfang14.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit YINGFANG;
void create() {
  set("short", "营房");
  set("long", @LONG     
一幢坚固的营房，外间是宽敞的操练场；黄土铺地，专门用来给士兵们练习
阵法之用；当中立着几根梅花桩。这间营房是通向军械库的必经之路：房子正中
摆放着一张大木桌，一名军官带领着几个士兵日夜把守在这里防止未经许可的人
进入军械库。桌子上放着一卷白浆纸，一根毛笔，用来记录前来领取兵器的士兵
名单以便核对。
LONG
  );
  set("objects", ([
    // 这里应该有执勤卫士。
  ]));
  set("exits", ([
    "north": __DIR__ "junxie_ku4",
    "south": __DIR__ "junxie_ku2",
    "east": __DIR__ "junxie_ku3",
    "west": __DIR__ "caolian_chang9",
  ]));
  set("detail", ([
  ]));
  setup();
}
