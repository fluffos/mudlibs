// 汴京 yingfang18.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit YINGFANG;
void create()
{
	set("short","营房");
	set("long", @LONG
这里是军队中用来考核士兵和军官的地方，房间特别宽大，却没有窗户，靠
近屋顶的四角点着很亮的火把，地下干干净净，连一根稻草都没有，全是方砖铺
就，严丝合缝，光可鉴人。几名军官站在一边，穿着比武专用的紧身服，脸上全
是不屑的神情。
LONG
	);
	set("objects", ([ 
    ]));
	set("exits", ([    
	    "east" : __DIR__"caolian_chang11",
    ]));
    set("detail",([
    ]));     
	setup();
}
