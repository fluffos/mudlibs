// 汴京  jinglong_men.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
	set("short", "景龙门" );
	set("long", @LONG   
景龙门是内城的正北门，门里沿大内东墙的街道却是相当狭窄。西面城墙内
便是皇宫后苑--延福宫，一路南去，两边宫观不断。北面出城门迎面是一个道观。
LONG
	);
	set("objects", ([ 
         ]));
	set("exits", ([    
	    "south" : __DIR__"donghuamen_jie",
	    "west" : __DIR__"yaoshiqiao_jie",       
	    "east" : __DIR__"anyuan_men",
         ]));
        set("detail",([
        ]));
        set("outdoors", "capital");
	setup();
}
