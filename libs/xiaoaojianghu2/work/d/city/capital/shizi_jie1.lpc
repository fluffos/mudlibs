// 汴京  shizi_jie1.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
	set("short",HIW "十字街" NOR);
	set("long", @LONG   
从这里向北，是城中最热闹的街区，由于毗邻宫城，小贩摊商极多，尽卖些
奇珍异味。东面是通旧城曹门的街道。一路上人声鼎沸，车马川流不息。南边是
一片里坊民舍；一家纱行临街开店；西去就是皇城前的广场了。
LONG
	);
	set("objects", ([ 
         ]));
	set("exits", ([    
	    "west" : __DIR__"dong_jiaolou",
	    "east" : __DIR__"panlou_jie1",
	    "north" : __DIR__"gaotou_jie",
         ]));
        set("detail",([
        ]));
        set("outdoors", "capital");
	setup();
}
