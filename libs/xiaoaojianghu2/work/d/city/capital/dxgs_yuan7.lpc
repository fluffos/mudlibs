// 汴京     /d/city/capital/dxgs_yuan7.c
#include <room.h>
#include <ansi.h>
#include "bianjing.h"

inherit BROOM;
void create() {
  set("short", HIW "智海院" NOR);
  set("long", @LONG
大相国寺中僧众上万，房舍众多，大家都是分灶起火，因为房屋零乱，非常
不安全，经常有火灾发生；为了防治这种现象，相国寺中将几十个院落改建成八
个大院，分别设定管院负责。每个院子都是一个不小的寺院。
LONG);
  set("exits", ([
    "east": __DIR__ "dxgs_xilang4",
  ]));
  set("detail", ([
  ]));
  set("objects", ([
  ]));
  set("outdoors", "capital");
  setup();
}
void init() {
  ::init();
}
