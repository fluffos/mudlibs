// 汴京    shulin1.c

#include "bianjing.h"

inherit BROOM;

#include <room.h>
#include <ansi.h> 

void create()
{
	set("short",GRN "树林" NOR);
	set("long", @LONG
跨过护城河，旧城之外竟然是一片茂密的树林；青松如盖，碧草似毡，触目
皆是青翠。偶尔可以见到一些三五成群负手闲游的人们，耳边不时响起鸟儿的叫
声。回首而望，城墙在树梢上隐约而现，一股花香被从金水河边吹来的微风鼓动
着，扑面而来。
LONG
	);
	set("objects", ([        
         ]));
	set("exits", ([  
	    "east" : __DIR__"jiucheng_qianggen",
	    "southwest" : __DIR__"shulin2",
    ]));
    set("detail",([
    ]));
	setup();
}
