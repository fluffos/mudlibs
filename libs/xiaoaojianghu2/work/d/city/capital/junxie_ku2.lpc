// 汴京 junxie_ku2.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit SROOM;
void create()
{
    set("short",RED"军械库房"NOR);
    set("long", @LONG    
这里是禁军的一间军械库房。里面放着禁军士兵使用的刀枪斧戟，弓箭铁弩，
以及铠甲，头盔，鞋帽等物，摆放得整整齐齐。有条有理。平时这里都有专门的
士兵把守，由于没有军情，平时也很少使用，有的兵器上面都生锈了。 
LONG
	);
    set("objects", ([ 
    ]));
    set("exits", ([    
        "north" : __DIR__"yingfang14",
    ]));
    set("detail",([
    ]));     
    setup();
}
