// 汴京     /d/city/capital/dxgs_jianyuan.c
#include <room.h>
#include <ansi.h>
#include "bianjing.h"

inherit DOOR_ROOM;
//inherit SROOM;  
void create()
{
    set("short", "监院" );
    set("long",@LONG
一间小小的房间，收拾得干干净净。这里是大相国寺监院的居所。
LONG);
    set("exits",([
        "east" : __DIR__"dxgs_zisheng_men",
    ]));
    set("detail",([
    ]));
    set("objects",([
    ]));
    create_door("east","木门","west",DOOR_CLOSED);
    setup();
}

void init()
{
//    ::init();
}
