// 汴京   longjin_qiao.c
#include <room.h>
#include <ansi.h> 
#include "bianjing.h"
inherit STREET;
void create()
{
	set("short",HIW "龙津桥" NOR);
	set("long", @LONG
龙津桥建在蔡河之上，正对着朱雀门御街，桥的式样和州桥十分类似，也是
因为通御路的缘故，异常坚固。脚下蔡河似玉带，蜿蜒而东西，形成数里长的港
湾----蔡河湾，是汴京城里第一等繁华去处。
LONG
	);
	set("objects", ([       
        __DIR__"npc/xiaofan3" : 1, 
    ]));
	set("exits", ([  
	    "north" : __DIR__"zhuque_men",
	    "south" : __DIR__"yujie6",
        "west" : __DIR__"leijia_qiao",
        "east" : __DIR__"xi_jiaofang",
    ]));
    set("detail",([
    ]));
	setup();
}
