// 汴京   /d/city/capital/zhanyun_guan.c
#include <room.h>
#include <ansi.h>
inherit ROOM;
void create()
{
	set("short",  "瞻云馆" );
	set("long", @LONG
瞻云馆位于宜秋门外，是专门用来接待西北蕃部入供见驾的使臣的。几幢二
层小楼临街而建，经常有西域龟兹，于稹高昌，吐蕃等国的人员进进出出。北面
窗外是汴河南岸，柳树成行，青石栏杆；河中行满商船。
LONG
	);
	set("objects", ([ 
         ]));
	set("exits", ([ 
               "south"     : __DIR__"xinz_dajie1",
         ]));
        set("detail",([
        ]));
//        set("outdoors", "capital");
	setup();
}
void init()
{
}
