// Room: /d/place/wiz/inn_hall

inherit ROOM;

#include <ansi.h>
void create()
{
	set("short", RED"神仙居"NOR);
	set("long", @LONG

        这是神仙们住的地方；
        然而什么摆设都没有；
        这里感觉有些不对劲：
        为什么神仙们不练功？
        
LONG 
	);
        set("not_startroom",1);
	set("exits", ([ /* sizeof() == 7 */
        "dl" : "/d/city/dali/jiulou",
        "ny" : "/d/city/nanyang/chufang",
        "lz" : "/d/city/lanzhou/kezhan",
        "yz" : "/d/city/yangzhou/jiulou",
        "sl" : "/d/menpai/shaolin/puti_houdian",
        "xq" : "/d/city/xingqing/road4",
//        "gc": "/d/city/gaochang/ximen",
        "cz": "/d/city/chuzhou/xishi",
        "bj": "/d/city/capital/chengqing_jie2",
    ]));
	set("need_clean", "蓬莱派");
    call_other( CLONE_DIR "board/common_b", "inn" );
           set( "objects", ([
       "/d/place/wiz/sharenmo"   :   4, 
        "/clone/money/gold"   :   100, 
        ]) );
	setup();
}
void init()
{
        add_action("do_update","update"); 
        add_action("do_get",   "get"); 
}

int do_update(string arg)
{
return 1;}
int do_get(string arg)
{
	object me=this_player();
	if(wizardp(me))
	return 0;
return 1;}
