// filename short.c
// 
#include <weapon.h>

inherit WEAPON;

varargs void init_short(int damage, int wound, int flag)
{
    init_weapon( damage, wound, flag | EDGED | SECONDARY );
    set_weapon_type("short");
    set( "skill_type", "short" );
    set( "verbs", ({ "impale","point" }) );
}

