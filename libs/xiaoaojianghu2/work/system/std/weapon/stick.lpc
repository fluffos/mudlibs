// stick.c

#include <weapon.h>

inherit WEAPON;

varargs void init_stick(int damage, int wound, int flag)
{
    init_weapon( damage, wound, flag | EDGED );
    set_weapon_type("stick");
	set( "skill_type", "blade" );
	set( "verbs", ({ "crush", "bash", "slam" }) );
}

