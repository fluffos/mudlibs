// hammer.c

#include <weapon.h>

inherit WEAPON;

varargs void init_hammer(int damage, int wound, int flag)
{
    init_weapon( damage, wound, flag | EDGED );
    set_weapon_type( "hammer" );
	set( "skill_type", "short" );
	set( "verbs", ({ "bash", "crush", "slam" }) );
}

