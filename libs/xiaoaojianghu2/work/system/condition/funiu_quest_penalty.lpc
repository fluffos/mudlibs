// funiu_quest_penalty.c

#include <condition.h>

inherit F_CLEAN_UP;

int update_condition(object me, int duration)
{
	me->apply_condition("funiu_quest_penalty", duration - 1);
	if( !duration ) return 0;

	return CND_CONTINUE;
}
