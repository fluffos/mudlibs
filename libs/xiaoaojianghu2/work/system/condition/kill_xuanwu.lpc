// kill_xuanwu.c
// by lnwm

inherit F_CLEAN_UP;

int update_condition(object me, int duration) {
  me->apply_condition("kill_xuanwu", duration - 1);
  if (!duration) {
    return 0;
  }
  return 1;
}
