// kill_dali.c

inherit F_CLEAN_UP;

int update_condition(object me, int duration)
{
    me->apply_condition("kill_dali", duration - 1);
    if( !duration )
    {
        write("由于时日长久，大理皇家决定放弃对你的追捕。\n");
        return 0;
    }
    return 1;
}

