//fumo-jian.c伏魔剑  二级剑 
//by wyh


inherit SKILL;

mapping *action = ({
  ([ "action":
    "$N怒目圆睁，使出一招「安如磐石」，手中$w一抖，直刺$n的$l  ",
    "lian":
    "$N怒目圆睁，使出一招「安如磐石」，手中$w一抖，直刺出去",
    "zhaoshi": "安如磐石",
    "dodge": 10,
    "sen": 2,
    "jibie": 0,
    "parry": 15,
    "force": 18,
    "damage": 15,
    "damage_type": "刺伤",
  ]),
  ([ "action":
    "$N使出一式「昂然直入」，足下一滑，剑芒闪向$n的$l  ",
    "lian":
    "$N使出一式「昂然直入」，足下一滑，剑芒直闪出去",
    "zhaoshi": "昂然直入",
    "dodge": 20,
    "parry": 24,
    "force": 30,
    "sen": 2,
    "jibie": 12,
    "damage": 25,
    "damage_type": "刺伤",
  ]),
  ([ "action":
    "$N使出一招「天悬地隔」，纵身跃入半空，$w如山崩般劈向$n的$l  ",
    "lian":
    "$N使出一招「天悬地隔」，纵身跃入半空，$w如山崩般劈了出去",
    "zhaoshi": "天悬地隔",
    "dodge": 23,
    "sen": 2,
    "parry": 22,
    "jibie": 25,
    "force": 40,
    "damage": 30,
    "damage_type": "刺伤",
  ]),
  ([ "action":
    "$N使出「随风转舵」，将$w舞得如纺车一般，忽然一剑刺向$n的$l  ",
    "lian":
    "$N使出「随风转舵」，将$w舞得如纺车一般，忽然一剑疾刺出去",
    "zhaoshi": "随风转舵",
    "sen": 3,
    "jibie": 35,
    "dodge": 25,
    "parry": 40,
    "force": 40,
    "damage": 30,
    "damage_type": "刺伤",
  ]),
  ([ "action":
    "$N使出一招「天各一方」，全身劲气贯于$w中，剑气直指$n的$l  ",
    "lian":
    "$N使出一招「天各一方」，全身劲气贯于$w中，剑气直刺出去",
    "zhaoshi": "天各一方",
    "dodge": 20,
    "sen": 3,
    "jibie": 44,
    "parry": 18,
    "force": 70,
    "damage": 60,
    "damage_type": "刺伤",
  ]),
  ([ "action":
    "$N单足独立，使出一式「图穷匕见」，一道剑光如白影般向$n的$l扫去  ",
    "lian":
    "$N单足独立，使出一式「图穷匕见」，一道剑光如白影般直扫出去",
    "zhaoshi": "图穷匕见",
    "dodge": 20,
    "parry": 19,
    "force": 30,
    "sen": 2,
    "jibie": 55,
    "damage": 65,
    "damage_type": "刺伤",
  ]),
});

string *parry_msg = ({
  "$n一招「群魔乱舞」，手中剑架住了$N的所有攻势.\n",
  "$n身形飘出，一招「铁画银钩」躲开了$N的所有攻势.\n",
  "$n见招架不即，一招「四面楚歌」，手中$w化作一片剑影硬封了出去.\n",
});

int valid_learn(object me) {
  if (me->query_jibie("buddhism") < 70)
    return notify_fail("你的佛学修为不够，强练绝技会伤及身体。\n");
  if ((int)me->query("max_force") < 250)
    return notify_fail("你的内力不够,先练好内力再说吧！\n");
  return 1;
}

int valid_lian(object me) {
  object ob;
  ob = me->query_temp("weapon");
  if (!ob || (string)ob->query("weapon_type") != "sword")
    return notify_fail("你必须先找一把剑才能练！\n");
  return valid_learn(me);
}

mapping query_action(int jibie) {
  int i, size;
  size = sizeof(action);
  for (i = 0; i < sizeof(action); i++)
    if (action[i]["jibie"] > jibie) {
      size = i;
      break;
    }
  return action[random(size)];
}

string query_zhaoshi(int jibie) {
  int i;
  for (i = 0; i < sizeof(action); i++) {
    if (action[i]["jibie"] >= jibie)
      return action[i]["zhaoshi"];
  }
  return "";
}

string query_xiuwei_type() {
  return "sword";
}

string query_weapon_type() {
  return "sword";
}

int query_dengji() {
  return 3;
}

string query_parry_msg(object weapon) {
  return parry_msg[random(sizeof(parry_msg))];
}

int query_youxiao_jibie() {
  return 7;
}

int query_parry_ratio() {
  return 6;
}
