//JMQJIGUAN.C

inherit ROOM;

void create()
{
	set("short", "巨木旗神木");
	set("long", @LONG

你眼前只见一棵大树，高有百丈，十人也环抱不住。巨树倚绝壁而
生，枝叶繁茂，藤萝环绕，竟然无法一览全貌。树身有一处光滑无
比，是常有人摩挲而成。
LONG
	);

	set("exits", ([
		"east" : __DIR__"jmqshulin6",
	]));

	set("outdoors", "mingjiao");
	setup();
//	replace_program(ROOM);
}

void init()
{
	// AGENTS.md: file genuinely truncated in the raw archive itself
	// (confirmed byte-identical to jinyongqunxiazhuan2008(#91)'s raw
	// copy of the same file -- not a conversion artifact). Closing the
	// truncated function body with an empty statement so the file is
	// syntactically valid; create()'s real content above is untouched.
}
