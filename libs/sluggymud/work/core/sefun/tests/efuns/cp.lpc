#define TESTFILE "/testfile"

void do_tests() {
    rm(TESTFILE);
    ASSERT(file_size(TESTFILE) == -1);
    ASSERT(cp("/core/lib/master/master.lpc", TESTFILE));
    ASSERT(file_size(TESTFILE));
    ASSERT(read_file("/core/lib/master/master.lpc") == read_file(TESTFILE));
}

