//Last Create  By PKYOU@DHXY 2002/05/25
#include "city.h"

void create() {
  set("short", "鞋帽店");
  set("long", @LONG

这里的东西都相当不错，城里的太太小姐多来这里买衣服。逢年
过节来光顾的人就更多了。屋中横放一张七尺多长的柜台，柜台
后的架子上是一匹匹的布料和成衣。
LONG);


  set("exits", ([
    "south": CITY "zhuque-e1",
  ]));

  set("objects", ([
    CNPC "eryi": 1,
    CNPC "xiaoliu": 1,
  ]));

  set("area", "鞋帽店");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
