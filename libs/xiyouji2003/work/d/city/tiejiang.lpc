//Last Create  By PKYOU@DHXY 2002/05/25
#include "city.h"

void create() {
  set("short", "铁匠铺");
  set("long", @LONG

这里是一间打铁铺子，从火炉中冒出的火光将墙壁映得通红，屋
子的角落里堆满了象斧子、铁锤、长剑等各式铁器，叮叮当当的
声音敲得满屋子响。
LONG);
  set("exits",
    ([
      "west": CITY "qinglong3.lpc",
    ]));
  set("objects",
    ([
      CNPC "work": 1,
    ]));

  set("no_get_from", 1);
  set("no_kill", 1);
  set("no_fight", 1);
  set("no_magic", 1);
  set("no_clean_up", 0);
  set("no_magic", 1);
  setup();
  replace_program(ROOM);
}
