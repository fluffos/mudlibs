//Last Create  By PKYOU@DHXY 2002/05/25
#include "city.h"

void create() {
  set("short", "城隍庙");
  set("long", @LONG

城隍庙里香火极盛。大多是来保佑家里平安，生意兴隆之类。正
中前方一座白白胖胖，满脸福相的城隍，左边有一位手持帐簿和
毛笔的判官。一位庙祝走前走后，和大家打着招呼。
LONG);

  set("exits",
    ([
      "west": CITY "qinglong-e2",
    ]));

  set("objects",
    ([
      CNPC "keeper": 1,
    ]));
  set("no_fight", 1);
  set("no_magic", 1);

  set("no_clean_up", 0);
  setup();
}
