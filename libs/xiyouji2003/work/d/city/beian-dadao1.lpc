// Update by waiwai@2003/04/25

#include "city.h"

void create() {
  set("short", "北安大道");
  set("long", @LONG

你走在一条宽阔的石板大街上，北面就快到城中心了，可以看到钟楼
耸立于前。
LONG);

  set("exits", ([
    //              "west"        : CITY"xuanyuan-miao",
    "east": CITY "juyiting",
    "north": CITY "center2",
    "south": CITY "jinshui-n1",
  ]));

  set("objects", ([
    //                CNPC"libai" : 1,
  ]));


  set("outdoors", "CITY");
  set("no_clean_up", 0);
  setup();
}
