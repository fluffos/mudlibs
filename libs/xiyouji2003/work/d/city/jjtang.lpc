
inherit ROOM;

void create()
{
	set("short", "讲经堂");
	set("long", @LONG_DESC

唐僧西天取经回来以后，就在化生寺开了一个讲经堂，目的是为了普
渡众生。只见很多善男信女坐在认真的听着唐僧的讲经。
LONG_DESC);

	set("objects", ([
		__DIR__"npc/tangmonk" : 1,
	]));
	
	set("exits", ([
		"north" : __DIR__"baodian",
	]));
	
	// Added by waiwai@2003/04/15
	set("no_upgrade_age",1);
	set("NONPC", 1);
	set("no_fight", 1);
	set("no_magic", 1);
	setup();
}