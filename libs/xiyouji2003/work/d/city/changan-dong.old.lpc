#include "city.h"

void create() {
  set("short", "长安东口");
  set("long", @LONG

长安城自古是繁华胜地，为数代皇朝建都之地，又为盐商大贾所
聚集，殷富甲于天下。长安东口是一个四通八达的路口，西方有
条大路通往东安大道，东面是长安东城门，南北方向是青龙大街。 
LONG);

  set("exits",
    ([
      "west": CITY "dongan-dadao",
      "north": CITY "qinglong-e2",
      "south": CITY "qinglong3",
      "east": CITY "dongmen",
    ]));

  set("objects",
    ([
      CNPC "luren": 2,
    ]));


  set("outdoors", "CITY");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
