//Last Create  By PKYOU@DHXY 2002/05/25
#include "city.h"

void create() {
  set("short", "首饰店");
  set("long", @LONG

这里是长安内，生意最火旺的首饰店了，来到了这里，你不会感
到空虚，南面是玄武大街，来往的人还非常的多，还有些小贩正
在往这里走来，可能是来处理首饰的。
LONG);

  set("exits",
    ([
      "south": CITY "xuanwu-e5",
    ]));

  set("objects",
    ([
      CNPC "lilaoban": 1,
    ]));


  set("outdoors", "CITY");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
