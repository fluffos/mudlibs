//Last Create  By PKYOU@DHXY 2002/05/25
#include "city.h"

void create() {
  set("short", "青龙大街");
  set("long", @LONG

你走到长安城的青龙街，街上行人很多，来来往往还有不少车辆
西面是长安城的唐王建立法台，法师袁天罡在此作法除妖，东面
有一家生意不错的镖局你远远还能听到标车的"哗哗"声。
LONG);

  set("exits",
    ([
      "south": CITY "qinglong-e2",
      "north": CITY "changan-bei",
      "west": CITY "tianjiantai",
      "east": CITY "biaoju",
    ]));

  set("objects",
    ([
      CNPC "luren": 1,
    ]));


  set("outdoors", "CITY");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
