#include "city.h"

void create() {
  set("short", "聚义厅");
  set("long", @LONG

你走在一条宽阔的石板大街上，西面就快到城中心了，可以看到钟楼
耸立于前。北面是长安武馆，门匾上四个金字闪闪发光。南边是一家
兵器铺子，是城内的振远镖局开的，专卖一些兵器。
LONG);

  set("exits",
    ([
      "west": CITY "beian-dadao1",
      "south": CITY "yanwuchang",
    ]));

  set("objects",
    ([
      //                CNPC"libai" : 1,
    ]));


  set("outdoors", "CITY");
  set("no_clean_up", 0);
  setup();
}
