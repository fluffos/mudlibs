//Last Create  By PKYOU@DHXY 2002/05/25
#include "city.h"

void create() {
  set("short", "青楼");
  set("long", @LONG

青楼是长安城首屈一指的大妓院。这里不是普通人能来的地方
如果你不是富甲一方的财主，如果你不是雄霸一方的帮主掌门
如果你不是王公贵族，就连进这个门也别想。楼中雇的打手更
是多多，不是有头有脸的见了这阵仗早就绕开了去。大门贴着
告示(gaoshi)一张。
LONG);

  set("item_desc", ([
    "gaoshi": "已搬迁至东邻。。。\n" NOR ]));

  set("exits", ([
    "north": CITY "xuanwu-e4",
  ]));

  set("objects", ([
    CNPC "fei1": 2,
  ]));


  set("outdoors", "CITY");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
