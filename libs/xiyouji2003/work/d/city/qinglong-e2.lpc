//Last Create  By PKYOU@DHXY 2002/05/25
#include "city.h"

void create() {
  set("short", "青龙大街");
  set("long", @LONG

你走在一条宽阔的石板大街上，南面就是长安城的东门口了，西
面是城中的清虚观，有些道士模样的人出入，东面是一座城隍庙
香烟缭绕，善男信女们进进出出，颇为喧闹。
LONG);

  set("exits", ([
    "south": CITY "dongkou",
    "north": CITY "qinglong-e1",
    "west": CITY "qingxuguan",
    "east": CITY "chenghuang",
    "northwest": CITY "tianjiantai",
    "northeast": CITY "biaoju",
  ]));

  set("objects", ([
    CNPC "luren": 1,
  ]));


  set("outdoors", "长安城");
  setup();
  replace_program(ROOM);
}
