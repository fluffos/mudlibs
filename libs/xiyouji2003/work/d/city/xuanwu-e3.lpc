// Update by waiwai@2003/04/25

#include "city.h"

void create() {
  set("short", "玄武大街");
  set("long", @LONG

这里的路相当的宽，能容好几匹马车并行，也是长安城最繁华的
路段，这里四处都是商家，买卖也非常红火，北面是一家长安闻
名的书局，南边是有名的董记当铺。
LONG);

  set("exits", ([
    "south": CITY "dangpu",
    "north": CITY "bookstore",
    "west": CITY "xuanwu-e2",
    "east": CITY "center2",
  ]));

  set("objects",
    ([
    ]));


  set("outdoors", "CITY");
  set("no_clean_up", 0);
  setup();
}
