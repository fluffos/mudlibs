//Last Create  By PKYOU@DHXY 2002/05/25
#include "city.h"

void create() {
  set("short", "法台");
  set("long", @LONG

这里便是长安城钦天监台，朝廷每年的祭天仪式都是在这里举行
中央广场上画着八卦图形，周围还摆这一些施法时用的器具，天
监台的总管是正先生袁天罡，总可在这里看到他的身影。
LONG);

  set("exits",
    ([
      "down": CITY "tianjiantai",
      "west": CITY "fatai-w",
      "east": CITY "fatai-e",
    ]));

  set("objects",
    ([
      CNPC "fashi": 1,
    ]));


  set("outdoors", "CITY");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
