// Update by waiwai@2003/04/25

inherit ROOM;
#include <ansi.h>

void create()
{
        set("short", HIB"丽春院地下室"NOR);
        set("long", @LONG

这是丽春院的一处地下暗室。
LONG 
     );
        set("exits", ([
            "up" : __DIR__"lichunyuan",
        ]));

//        set("objects", ([
//            __DIR__"npc/guinu4" : 1,
//        ]));

	set("no_clean_up", 0);
        set("zy_limit", "1");
        set("piao_room", "1");
        setup();
}
