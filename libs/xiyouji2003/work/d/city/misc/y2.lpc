// Update by waiwai@2003/04/25

#include <allcity.h>

void create ()
{
  set ("short", "兵营");
  set ("long", @LONG

这里是兵营，到处都是官兵走来走去，有的在武将的指挥下列队操
练，有的独自在练功，有的坐着、躺着正在休息。大柳树下树阴里
坐着主帅，不动声色地寻视着四周。随时准备擒拿宵小杀人越货之
人。
LONG);

	set("exits", ([
	"southeast" : CITY_ROOM"center",
	]));

	set("objects", ([ 
	CITY_ROOM"misc/npc/bing" : 2+random(2),
	]));

	setup();
}

