//Last Create  By PKYOU@DHXY 2002/05/25
#include "city.h"

void create() {
  set("short", "春和楼");
  set("long", @LONG

这里正在举行一个喜宴，堂内张灯结彩，台上高点红烛，一些歌
女舞娘正随乐而舞，四五个小伙计麻利的上菜送酒。周围坐满了
前来祝贺的客人，热闹非凡。
LONG);

  set("exits",
    ([
      "north": CITY "xuanwu-e6",
    ]));

  set("objects",
    ([
      CNPC "boss": 1,
    ]));


  set("outdoors", "CITY");
  set("no_clean_up", 0);
  setup();
  replace_program(ROOM);
}
