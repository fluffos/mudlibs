#include <room.h>

inherit ROOM;

int is_chat_room() { return 1; }

void create() {
  set("short", "猪圈");
  set("long", @LONG

这里是一个臭烘烘的猪圈，栏上挂着一个牌子(paizi)。
LONG);

  set("no_fight", 1);
  set("no_sleep_room", 1);
  set("item_desc", ([
    "paizi": "大家一起来发呆，发呆死了好投胎。。。。。。\n据说当年猪八戒就是发呆发的才投胎到猪圈的。\n",
  ]));

  set("exits", ([
    //   		"toutai": "/adm/reg/reg_room", 
    "toutai": "/d/city/kezhan",
  ]));

  setup();
}
