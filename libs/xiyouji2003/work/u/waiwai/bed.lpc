// Created by waiwai@2000/08/20

#include <ansi.h>

inherit ROOM;

void create() {
  set("short", HIM "歪歪的床上" NOR);
  set("long", @LONG

这是歪歪的床上，一天24小时一般有23.9小时歪歪是不会在这里睡觉的。
LONG);

  set("exits", ([
    "out": __DIR__ "workroom",
  ]));

  set("sleep_room", 1);
  set("if_bed", 1);

  setup();
}
