// Created by waiwai@2001/04/22

#include "ansi.h"

inherit J_BOARD;

void create()
{
	set_name(HIR"西游记"HIW"工作管理板"NOR, ({ "board" }) );
	set("location", "/d/wiz/jobroom");
	set("board_id", "work_b");
	set("long", "这是一个供巫师们汇报讨论工作进程的专用板子。\n" );
	setup();
	set("capacity", 999);
        replace_program(J_BOARD);
}
