// Room: /open/su/room/hall/gateway_3.c

inherit ROOM;

void create()
{
	set("short", "箱房走道");
	set("long", @LONG
这是一间什么也没有的空房间。
LONG
	);
	set("exits", ([ /* sizeof() == 3 */
  "north" : __DIR__"hall_room2",
  "west" : __DIR__"hall_room1",
  "south" : __DIR__"gateway_2",
]));
	set("no_clean_up", 0);

	setup();
	replace_program(ROOM);
}
