// Room: /open/capital/room/king/kr4-4-2
inherit ROOM;

void create ()
{
  set ("short", "朝圣殿");
  set ("long", @LONG
  测试中!!
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "north" : "/u/s/smirk/room/workroom",
]));

  set("objects", ([ /* sizeof() == 2 */
  "/u/s/smirk/npc/Psoldier" : 2 ,
]));
  setup();
}

int valid_leave(object me,string dir){
  if( dir=="north" && !wizardp(me))
  return notify_fail("站住!! \n");
  return ::valid_leave(me,dir);
}
