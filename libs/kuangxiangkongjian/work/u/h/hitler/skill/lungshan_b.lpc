//Code by Fex ..

inherit BULLETIN_BOARD;

void create()
{
        set_name("泷山慕日铭", ({"Lungshan Board","board" }) );
        set("location", "/open/wu/room/school8" );
        set("board_id", "lungshan_b");
        set("long",
                "这是一个泷山派专用的布告栏, 供本派弟子使用\n");
        setup();
        set("capacity", 30);
        // replace_program(BULLETIN_BOARD);
        // 设这样就能让那些人 discard post 啰, 
        // 这是 wade 新增功能 11/22/1995
        set("master", ({"hitler","devils"}) );
}
