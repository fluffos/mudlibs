// Room: /u/o/oklatalk/r3.c
inherit ROOM;

void create() {
  set("short", "宴客花厅");
  set("long", @LONG

	  有朋自远方来，不亦乐乎。中国人嘛，总免不了要吃吃喝
	喝的。落雁阁的厨艺可是一流的，只可惜除了主人的至亲好
	友之外，别人是没有机会品尝得到的。

LONG);

  set("exits", ([ /* sizeof() == 1 */
    "east": __DIR__ "h1",
  ]));
  set("light_up", 1);

  setup();
}
