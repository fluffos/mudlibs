// Room: /u/o/oklatalk/entrance.c
inherit ROOM;

void create ()
{
  set ("short", "福地洞天");
  set ("long", @LONG

	  话说剑魔以独孤九剑纵横天下之际，遇见了宿命中的她。
	她就是天下第一美女，落雁阁的女主人 --- 琪琪。

	  独孤求败看破了世上的争名夺利。遂与琪琪隐居在这世外
	桃源之中。眼前所见的平凡房舍，会是这两位名动天下的人
	物的居所吗？

LONG);

  set("light_up", 1);
  set("outdoors", "/open/common");
  set("exits", ([ /* sizeof() == 1 */
  "enter" : __DIR__"h1",
]));

  setup();
}
