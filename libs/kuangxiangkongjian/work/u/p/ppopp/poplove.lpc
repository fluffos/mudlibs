#include <ansi.h>
#include <room.h>
inherit ROOM;

void create() {
  set("short", "蜜月小屋");
  set("long", @LONG

描述一个房间.

LONG);
  set("item_desc", ([ /* sizeof() == 1 */
    "sign": "

描述sign这个东西.\n",

  ]));

  //  set("objects", ([ /* sizeof() == 数字 */
  //     "/u/p/ppopp/npc/parrot.lpc" : 1,
  //]));
  set("light_up", 1);
  set("exits", ([ /* sizeof() == 1 */
    "out": "/u/p/ppopp/popenter",
  ]));
  create_door("out", HIR "爱之门" NOR, "enter", DOOR_CLOSED);
  setup();
}
