inherit ROOM;

void create() {
  set("short", "钟楼顶楼");
  set("long", @LONG

这是一间什么都没有的房间。
 
LONG);

  set("light_up", 1);
  set("no_transmit", 1);
  set("exits", ([ /* sizeof() == 1 */
    "down": __DIR__ "prmw13",
  ]));

  setup();
}
