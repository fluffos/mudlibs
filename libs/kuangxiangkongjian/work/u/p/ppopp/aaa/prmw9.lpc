inherit ROOM;

void create ()
{
  set ("short", "碎梦阁");
  set ("long", @LONG

这是一间什么都没有的房间。
 
LONG);

  set("light_up", 1);
  set("exits", ([ /* sizeof() == 1 */
  "down" : __DIR__"prmw8",
]));

  setup();
}
