#include <ansi.h>
#include <room.h>
inherit ROOM;

void create ()
{
  set ("short", "琪琪私人厨房");
  set ("long", @LONG

有一句话是这样说的‘每个有成就的男人, 背后一定有一个伟大的女
人’给予他支持鼓励, 但最重要的就是要给予他最好的照料. 独孤求
败有一个挑嘴的胃, 琪琪当然要亲自下厨满足他啦, 瞧!!! 还请了一
个嬷嬷帮忙呢. 瞧她忙进忙出的样子不晓得要做些什么好菜??!!

LONG);

  set("objects", ([ /* sizeof() == 1 */
     "/u/p/ppopp/npc/mama.lpc" : 1,
]));
  set("light_up", 1);
  set("exits", ([ /* sizeof() == 1 */
     "south" : "/u/p/ppopp/workroom",
]));
  create_door("south", "雕花紫檀木门", "north", DOOR_CLOSED);
  setup();
}
