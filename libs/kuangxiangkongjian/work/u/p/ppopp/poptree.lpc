#include <ansi.h>
#include <room.h>
inherit ROOM;

void create() {
  set("short", "榕树上");
  set("long", @LONG

你现在位于榕树主干上, 四周围绕着各式各样的小鸟, 你的上方有一个
树洞, 隐隐约约地透出金色光芒. 看到这如此奇特的现象, 你的直觉告
诉你, 那个树洞绝对不寻常!!!

LONG);
  set("light_up", 1);
  set("exits", ([ /* sizeof() == 2 */
    "down": "/u/p/ppopp/popenter",
    "up": "/u/p/ppopp/pophold",
  ]));
  setup();
}
