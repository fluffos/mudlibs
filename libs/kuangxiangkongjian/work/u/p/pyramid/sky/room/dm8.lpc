// Room: /u/p/pyramid/sky/room/dm8.c

inherit ROOM;

void create() {
  set("short", "[36m幻力森林[2;37;0m");
  set("long", @LONG
纠结的藤蔓挡住了你的去路, 藤蔓不断的向你伸出魔爪, 你感到
十分的不安, 但是当夜摩刀一出鞘, 这些低等生物便自动后退, 
让出了一条小径.
LONG
  );
  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "dm9",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/u/p/pyramid/sky/npc/yamado.lpc": 1,
  ]));
  set("no_clean_up", 0);

  setup();
}
