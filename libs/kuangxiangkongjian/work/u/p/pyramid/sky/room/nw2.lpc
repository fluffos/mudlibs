// Room: /u/p/pyramid/sky/room/nw2.c

inherit ROOM;

void create()
{
	set("short", "西城.");
	set("long", @LONG
这里是昆沙门天的妻子, 吉祥天的住处. 帝释天把她许配给了爱将昆沙门天,一方面是.
为了监视先帝的爱女
LONG
	);
	set("light_up", 1);
	set("exits", ([ /* sizeof() == 4 */
  "south" : __DIR__"nw4",
  "north" : __DIR__"nw3",
  "west" : __DIR__"nw5",
  "east" : __DIR__"nw1",
]));
	set("no_clean_up", 0);
	set("lomg", "这里是昆沙门天的妻子, 吉祥天的住处. 帝释天把她许配给了爱将昆沙门天,一方面是为了监视先帝的唯一爱女.");

	setup();
	}
