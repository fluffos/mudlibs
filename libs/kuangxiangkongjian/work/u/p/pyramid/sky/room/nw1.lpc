// Room: /u/p/pyramid/sky/room/nw1.c

inherit ROOM;

void create()
{
	set("short", "西城");
	set("long", @LONG
这里是昆沙门天的妻子, 吉祥天的住处. 帝释天把她许配给了爱将昆沙门天,
一方面是为了监视先帝的唯一爱女.
LONG
	);
	set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"nw2",
  "east" : __DIR__"nc1",
]));
	set("light_up", 1);
	set("no_clean_up", 0);

	setup();
	}
