// Room: /u/p/pyramid/room/r1.c

inherit ROOM;

void create()
{
	set("short", "小鱼儿的谈心雅室");
	set("long", @LONG
这是个窗明几净的小房间, 隐藏在暗墙的后面.   典雅的陈设和摆饰让你
有种宾至如归的感觉.   茶几上刚泡好的玫瑰花茶散出淡淡的香气, 还有几样精致
的可口小点.
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "down" : "/open/common/room/inn",
]));
	set("objects", ([ /* sizeof() == 1 */
  "/u/p/pyramid/obj/cake.lpc" : 5,
]));
	set("no_fight", 1);
	set("light_up", 1);
	set("no_clean_up", 0);
	set("no_magic", 1);

	setup();
	}
