// function.c
#include <func.h>


int main(object me)
{
         int i;
      int func;
      string *c = ({ "1","2","3","4","5","6","7","8","9","0" });
          for(i=0;i<10;i++)
          {
      if(!me->query("func"+c[i]))
              break;
         func=me->query("func"+c[i]);
      printf("%s (%s)",func_c_name[func],func_e_name[func]);
	write("\n");
      }
	return 1;
}
