// dumpling.c

inherit ITEM;
inherit F_FOOD;

void create(object ob)
{
	set_name("包子", ({ "dumpling" }) );
	set_weight(80);
		set("long", "一个香喷喷、热腾腾的大包子。\n");
         ob->set("int",100);
}
