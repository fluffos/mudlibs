 
#define SECURITY	__DIR__"securityd.lpc"
 
int update( mixed code )
{
	seteuid(getuid());
	if( SECURITY->wizlevel(this_player())!="(admin)" && strsrch( code , "efun::" ) != -1 ) {
		error("ERROR : 你没有使用 efun:: 的权力。\n");
		return 1;
	}
	return catch( code );
}
