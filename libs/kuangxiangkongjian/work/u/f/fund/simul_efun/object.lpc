 
 
string call_source()
{	
	return file_name(previous_object());
}
string *all_source()
{
	object *all = all_previous_objects();
	string concate = "";
	for( int i = 0 ; i <sizeof(all) ; i ++ )
		concate += ( base_name(all[i]) + " " );
	return explode( concate , " " );
}
