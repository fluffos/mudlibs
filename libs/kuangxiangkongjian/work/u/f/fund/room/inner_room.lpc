#include <room.h>

inherit ROOM;

void create()
{
         set("s         set("short","内室");
          set("long",@LONG
这里是雪苍派掌门平时休息的地方，里面的摆设极整齐且清朴，只是墙上挂了一对刀剑，显得极具气派，相传这对刀剑乃是雪苍派开山祖师从一位异人手中得来的，只是可惜雪苍派开山祖师鬼影老人不会刀剑，否则鬼影老人就能以拳脚、刀、剑得到武林三绝之美誉。
LONG);
        set("exits", ([
        ]) );
        setup();
}
