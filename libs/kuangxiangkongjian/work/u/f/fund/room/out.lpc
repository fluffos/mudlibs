#include <room.h>

inherit ROOM;

void create()
{
        set("short","洞穴外");
         set("long","这里是洞穴的外面，似乎这里已不在蜀中城内，在前方不远处可以看一座雪苍山。\n");
        set("exits", ([
          "north": "/u/f/fund/room/room3",
         "south": "/u/f/fund/room/hole3",
        ]) );
        setup();
}
