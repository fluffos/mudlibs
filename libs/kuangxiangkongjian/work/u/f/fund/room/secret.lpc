#include <room.h>

inherit ROOM;

void create()
{
           set("short","密室");
          set("long","这里是一间密室，四周光线极弱，看起来像关犯人的监牢，眼看前面似乎有个老人在那，不知道是何方人士？\n");
        set("exits", ([
        "out": "/u/f/fund/room/hole2",
        ]) );
         set("objects",([__DIR__"npc/dunno":1]));
        setup();
}
