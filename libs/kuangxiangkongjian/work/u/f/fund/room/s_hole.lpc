#include <room.h>

inherit ROOM;

void create()
{
         set("short","小洞内");
     set("long","一个极为窄小的小洞内。\n");
        set("exits", ([
        "out": "/u/f/fund/room/water6",
        ]) );
       set("objects",(["/open/su/obj/ten_2":1]));
        setup();
}
