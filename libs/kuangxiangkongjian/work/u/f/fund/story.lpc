inherit F_CLEAN_UP;

int main(object me, string arg) {

  if (file_size("/doc/story/" + arg) > 0) {
    cat("/doc/story/" + arg);
    return 1;
  }
  return 1;
}

int help() {
  write(@HELP
指令格式：story <人物名称>
 
这个指令让你可以知道一些名人的事绩。
HELP
  );
  return 1;
}
