#pragma save_binary
#include <mudlib.h>
#include <path.h>
inherit ROOM;

void create() {
  ::create();
  seteuid(getuid(this_object()));
  set("short", "?");
  set("long", "?\n");
  reset();
}
