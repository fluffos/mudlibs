#include <room.h>

 inherit ROOM;

 void create()
{
      
        set("short", "仙剑巫师交谊厅");
        set("long", @LONG
你看到几个仙剑的成员们，正为了仙剑派的大大小小
事务忙的焦头烂哦，不禁打从心底佩服他们的敬业态度。
LONG
        );

        set("exits", ([
		"north": __DIR__"program_room",
        ]) );
setup();

}
