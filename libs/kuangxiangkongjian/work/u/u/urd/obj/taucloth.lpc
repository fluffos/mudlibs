#include <ansi.h>
#include <armor.h>
inherit EQUIP;
void create()
{
  set_name(HIY "金牛座圣衣" NOR,({"taurus cloth","taurus","cloth"}));
  set_weight(5000);
  if( clonep() )
    set_default_object(__FILE__);
  else {
    set("unit","件");
    set("long","这是金牛座圣斗士--亚尔格第的圣衣，也是代表着黄金圣斗士的象征．\n");
    set("material","crimsonsteel");
    set("value",10000);
    set("armor_type","cloth");
    set("armor_prop/armor",17);
    set("armor_prop/dodge",10);
    set("wear_msg", "$n突然从牡羊的型态分开，自动的戴在$N的身上，霎那间，$N感到一股温和的小宇宙充满了全身．\n");
    set("unwear_msg", "$n从$N的身上飞出，在$N不远处合成了牡羊的型态，$N感到虽然$n已离开，但那股小宇宙并没有跟着离去．\n");
       }
  setup();
}
