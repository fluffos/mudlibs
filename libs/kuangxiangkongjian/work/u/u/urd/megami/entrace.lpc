// Room: /u/u/urd/megami/entrace.c
inherit ROOM;

void create() {
  set("short", "他力本愿寺");
  set("long", @LONG
这里就是萤一跟三位女神同居的寺院，旁边还停着萤一的爱车，从里面传来一股祥和的气纷来，看起来这真是一个充满了欢笑的地方。
LONG);

  set("light_up", 1);
  set("outdoors", "/u/u");
  set("exits", ([ /* sizeof() == 1 */
    "down": "/u/u/urd/workroom.lpc",
  ]));

  setup();
}
