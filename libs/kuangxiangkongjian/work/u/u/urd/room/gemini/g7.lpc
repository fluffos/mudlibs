// Room: /u/c/carol/room/gemini/g1.c
inherit ROOM;

void create ()
{
  set ("short", "宫殿内");
  set ("long", @LONG
你在这里感觉不到任何的生气, 似乎是整座宫殿里的空气都凝固了一般, 可是却
还是让人从心里透出一股寒意．
LONG);

  set("exits", ([ /* sizeof() == 3 */
  "west" : __DIR__"g8.lpc",
  "south" : __DIR__"g4.lpc",
  "east" : __DIR__"g6.lpc",
]));
  set("light_up", 1);

  setup();
}
