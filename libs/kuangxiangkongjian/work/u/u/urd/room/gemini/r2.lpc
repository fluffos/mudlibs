// Room: /u/c/carol/room/gemini/r2.c
inherit ROOM;

void create ()
{
  set ("short", "巨石大道");
  set ("long", @LONG
这里似乎是个人迹罕至的地方,道路上都长满了浓密的青苔,而在这也感觉不
到有何的压迫感,或许前方的宫殿是无人看守的吧！ 
LONG);

  set("light_up", 1);
  set("outdoors", "/u/c");
  set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"r3.lpc",
  "east" : __DIR__"r1.lpc",
]));

  setup();
}
