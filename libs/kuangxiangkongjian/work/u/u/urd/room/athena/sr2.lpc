// Room: /u/c/carol/room/athena/sr2.c
inherit ROOM;

void create() {
  set("short", "圣殿之东--白银圣廊");
  set("long", @LONG
圣廊上发出了阵阵的声响引起了你身上盔甲的共鸣, 你处在这种谐合的韵律下
不但没有感到任何的不适, 甚至到还感到非常的安详和谐, 让你几乎要忘掉了前
面那段路程的艰苦。
LONG);

  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "sr3.lpc",
    "north": __DIR__ "silver1.lpc",
  ]));
  set("light_up", 1);

  setup();
}
