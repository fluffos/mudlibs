// Room: /u/c/carol/room/athena/silver1
inherit ROOM;

void create() {
  set("short", "圣殿之东--白银圣廊");
  set("long", @LONG
从廊上的墙壁散发出了一道道耀眼的银白色光芒, 就算是在黑夜里它也依然是
那么样的光彩夺目, 你看到了在南方似乎有个奇怪的东西站着, 不过看起来是一
动也不动地, 八成是什么雕像之类的东西吧。
LONG);

  set("exits", ([ /* sizeof() == 2 */
    "west": __DIR__ "gold1.lpc",
    "south": __DIR__ "sr2.lpc",
  ]));
  set("light_up", 1);

  setup();
}
