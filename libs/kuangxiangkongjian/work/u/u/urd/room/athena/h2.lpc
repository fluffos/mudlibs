// Room: /u/c/carol/room/athena/hstair.c
inherit ROOM;

void create ()
{
  set ("short", "圣梯");
  set ("long", @LONG
这是条一直向上走去的台阶, 四周充满着祥和之气, 而你的脚步声也一直在此回
响着, 就在不远处....你看见了一座高耸的雅典娜雕像。
LONG);

  set("exits", ([ /* sizeof() == 2 */
  "southdown" : __DIR__"hstair.lpc",
  "northup" : __DIR__"h3.lpc",
]));
  set("light_up", 1);

  setup();
}
