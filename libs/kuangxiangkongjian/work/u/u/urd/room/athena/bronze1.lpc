// Room: /u/c/carol/room/athena/bronze1.c
inherit ROOM;

void create ()
{
  set ("short", "圣殿之西--青铜圣廊");
  set ("long", @LONG
你正站在一条绽放着青色光芒的走廊上, 两旁刻画着许多古远的雕刻和图样
(sign), 走廊一直向着南方伸展出去, 在青色光辉的照映下, 你可以很清楚的看到
就在你的南方不远处伫立着一座巨型的青铜像。
LONG);

  set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"b2.lpc",
  "east" : __DIR__"gold1.lpc",
]));
  set("light_up", 1);

  setup();
}
