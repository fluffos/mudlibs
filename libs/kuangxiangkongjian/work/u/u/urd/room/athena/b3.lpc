// Room: /u/c/carol/room/athena/b3.c
inherit ROOM;

void create() {
  set("short", "圣殿之西--青铜圣廊");
  set("long", @LONG
你已经走到了圣廊的尽头, 走到这里你才真正的可以看清青铜像, 它似乎是具有
生命一般地伫立在这守护着某样东西, 你很好奇它到底是在守护着什么东西, 而
在走廊的墙壁上篆写了许多你无法理解的上古[文字]。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "b2.lpc",
  ]));
  set("light_up", 1);

  setup();
}
