// Room: /u/c/carol/room/athena/b2.c
inherit ROOM;

void create ()
{
  set ("short", "圣殿之西--青铜圣廊");
  set ("long", @LONG
你正走向青铜圣廊的最深处, 而你发现到走廊上的青色光芒原来是由南方的那
座青铜像所发散出来的, 你对于此感到十分的困惑, 不过你可以感觉到这里似乎
没有危险, 所以可以放心地向南走去。
LONG);

  set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"b3.lpc",
  "north" : __DIR__"bronze1.lpc",
]));
  set("light_up", 1);

  setup();
}
