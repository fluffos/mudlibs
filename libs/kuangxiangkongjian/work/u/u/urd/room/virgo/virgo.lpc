// Room: /u/c/carol/room/virgo/virgo.c
inherit ROOM;

void create ()
{
  set ("short", "处女宫");
  set ("long", @LONG
你看到从宫殿的外围放射出万道的霞光, 把整座宫殿都包围着, 不让任何邪恶的
事物接近, 让所有的邪恶都转变为善良, 也使你的内心也感觉到无比的祥和,不
受邪恶的侵扰。
LONG);

  set("exits", ([ /* sizeof() == 4 */
  "south" : __DIR__"vr3.lpc",
  "west" : __DIR__"v2.lpc",
  "north" : __DIR__"v4.lpc",
  "east" : __DIR__"v1.lpc",
]));
  set("light_up", 1);

  setup();
}
