// Room: /u/c/carol/room/taurus/tr1.c
inherit ROOM;

void create ()
{
  set ("short", "巨石大道");
  set ("long", @LONG
你看到路上堆积着许多硕大的石块,道路上也留着一些像是被陨石撞击过
的痕迹,你可以看到一座座的巨大建筑耸立在你的前方.
LONG);

  set("light_up", 1);
  set("outdoors", "/u/c");
  set("exits", ([ /* sizeof() == 2 */
  "north" : "/u/c/carol/room/stone.lpc",
  "south" : __DIR__"r10.lpc",
]));

  setup();
}
