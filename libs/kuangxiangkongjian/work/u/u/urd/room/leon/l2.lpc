// Room: /u/c/carol/room/leo/l1.c
inherit ROOM;

void create ()
{
  set ("short", "宫殿内");
  set ("long", @LONG
这里的梁柱看起来有点残破不堪, 似乎是发生过了一场激战, 而宫殿的四周则是
环绕着一股强大的气流, 引发了一阵阵的旋风。
LONG);

  set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"l3.lpc",
  "east" : __DIR__"leo.lpc",
]));
  set("light_up", 1);

  setup();
}
