// Room: /u/c/carol/room/cancer/r1.c
inherit ROOM;

void create ()
{
  set ("short", "阴暗大道");
  set ("long", @LONG
在这里你看不到一点光线, 所以你也无法察觉到四周的任何景物, 不过你还是可
以感觉到一股强烈的杀气充满这里．
LONG);

  set("exits", ([ /* sizeof() == 2 */
  "west" : "/u/c/carol/room/stone.lpc",
  "east" : __DIR__"r2.lpc",
]));
  set("light_up", 1);

  setup();
}
