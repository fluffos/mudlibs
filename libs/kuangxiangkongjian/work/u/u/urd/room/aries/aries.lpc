// Room: /u/c/carol/room/aries/aries.c
inherit ROOM;

void create() {
  set("short", "牡羊宫");
  set("long", @LONG
在你面前的就是黄道十二宫之首的牡羊宫,巨大的石柱支撑起整座建筑,磅薄的
气势让你感动莫明,你可以看到一股淡淡的微光从宫殿的顶端直向天际射去.
LONG);

  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "a4.lpc",
    "west": __DIR__ "a2.lpc",
    "south": __DIR__ "r3.lpc",
    "east": __DIR__ "a1.lpc",
  ]));
  set("objects", ([ /* sizeof() == 1 */
    "/u/c/carol/npc/seiya.lpc": 1,
  ]));
  set("light_up", 1);

  setup();
}
