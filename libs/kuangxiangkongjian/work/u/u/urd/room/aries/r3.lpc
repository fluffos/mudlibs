// Room: /u/c/carol/room/aries/r3.c
inherit ROOM;

void create ()
{
  set ("short", "巨石道上");
  set ("long", @LONG
你正站在巨石大道上,四周都是建筑物的断垣残壁,而向下则是深不可测的悬崖,
而在前方不远处你看到一栋宏伟的建筑.
LONG);

  set("light_up", 1);
  set("outdoors", "/open/snow");
  set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"aries.lpc",
  "south" : __DIR__"r2.lpc",
]));

  setup();
}
