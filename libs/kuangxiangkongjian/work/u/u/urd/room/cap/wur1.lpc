// Room: /u/c/carol/room/capricorn/wur1.c
inherit ROOM;

void create() {
  set("short", "无底地堑");
  set("long", @LONG
这里到处都是深不见底的地缝, 一个不留神就再也没有人能找到你了, 你最好是
步步为营才是上上之策。
LONG);

  set("light_up", 1);
  setup();
}
