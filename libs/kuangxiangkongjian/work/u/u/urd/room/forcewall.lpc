// Room: /u/c/carol/room/forcewall.c
inherit ROOM;

void create() {
  set("short", "力墙之内");
  set("long", @LONG
力墙之内散发出无数道刺眼的光芒, 就算闭上了眼睛仍然是让你的双眼感到非
常的不适, 而且有股力量包围着你让你动弹不得。
LONG);

  set("exits", ([ /* sizeof() == 2 */
    "south": __DIR__ "virgo/v7.lpc",
    "no": __DIR__ "libra/li1.lpc",
  ]));
  set("light_up", 1);

  setup();
}
