// Room: /u/c/carol/room/libra/li1.c
inherit ROOM;

void create() {
  set("short", "巨石大道");
  set("long", @LONG
一堵力墙就挡在你的正前方, 而你可以感到一股祥合之气从南方向你而来, 让你
不禁地心情放松了下来。
LONG);

  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "libra.lpc",
    "south": "/u/c/carol/room/forcewall.lpc",
  ]));
  set("light_up", 1);

  setup();
}
