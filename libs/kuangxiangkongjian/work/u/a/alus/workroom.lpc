
#include <room.h>
#include <ansi.h>

inherit ROOM;

void create ()
{
  set ("short", HIC "一座古典的凉亭□ NOR);
  set ("long", @LONG

                望  潮  轩 　
     
      投笔由来羡虎头；须教谈笑觅封侯
      胸中浩气凌霄汉；腰下青萍射斗牛  
      要识它年恢骏业；全凭今日具鸿猷
      承时得展男儿更；万里鹏程回不侔

  这是雅露思的工作室.也是丐帮预备发展处之所在.
目前Alus住在此地.兼且学习中.
 

LONG);

  set("light_up", 1);
  set("valid_startroom", 1);
  set("exits", ([ /* sizeof() == 6 */
  "up"     :"/u/a/alus/room/roomup.lpc",
  "down"    :"/u/a/alus/dagger/room/roomstart.lpc",
  "east"     :"/u/a/alus/room/roome.lpc",
  "cgy"     :"/u/c/cgy/workroom.lpc",
  "west"    :"/u/a/alus/room/roomw.lpc",
]));

  setup();
  call_other("/u/a/alus/alusboard.lpc","???");
}
