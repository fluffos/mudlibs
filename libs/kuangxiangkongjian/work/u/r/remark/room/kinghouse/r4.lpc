// Room: /u/r/remark/room/kinghouse/r4
inherit ROOM;
#include <ansi.h>
void create ()
{
  set ("short", HIM"金皇大厅"NOR);
  set ("long", @LONG

亚里亚罕王的办公大厅,高高在上的王座显的几分威武,
  或许可以上去坐坐(sit)

LONG);

  set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"r3.lpc",
]));
  set("item_desc", ([ /* sizeof() == 1 */
  "throne" : "王座大椅
",
]));
  set("light_up", 1);

  setup();
}
