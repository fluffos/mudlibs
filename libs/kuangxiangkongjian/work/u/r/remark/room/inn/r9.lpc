// Room: /u/r/remark/room/inn/r9
inherit ROOM;
#include <ansi.h>
void create ()
{
  set ("short", "[1;35m龙虎阁[2;37;0m");
  set ("long", @LONG
  
         露依达酒店的重点地区,各路英雄好汉的聚集地,是寻找伙伴的地方,
         可以召集自己想要的伙伴,是旅行的开始;这里有提供一些粮食,
         可以多买一些储存以防不时之需,大厅之内有个柜台(counter)
        
LONG);

  set("item_desc", ([ /* sizeof() == 1 */
  "counter" : "[1;37m柜台上有签名簿,填了各路英雄的大名,你可以去写一写
[2;37;0m",
]));
  set("exits", ([ /* sizeof() == 3 */
  "west" : __DIR__"r11.lpc",
  "up" : "/u/r/remark/workroom.lpc",
  "east" : __DIR__"r10.lpc",
]));
  set("light_up", 1);
  set("objects", ([ /* sizeof() == 3 */
  "/u/r/remark/npc/human/blademan.lpc" : 1,
  "/u/r/remark/npc/human/swordman.lpc" : 1,
  "/u/r/remark/obj/board/notebook.lpc" : 1,
]));

  setup();
}
