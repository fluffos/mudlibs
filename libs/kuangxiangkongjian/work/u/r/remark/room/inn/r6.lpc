// Room: /u/r/remark/room/inn/r6.c
inherit ROOM;
#include <ansi.h>
void create ()
{
  set ("short",HIM "jessica 的寝室"NOR);
  set ("long", @LONG
   
            一进门就会闻到一股清香,这是 jessica 特爱的香水,
            大床摆在一旁,堆满了熊宝宝(bear),好像是给牠们睡的一样
            大床旁是梳妆台,不善化妆的 jessica 只是用来梳梳头,
            整理仪容;床头放着一闹钟(clock),好像蛮有趣的。

LONG);

  set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"r7.lpc",
]));
  set("item_desc", ([ /* sizeof() == 2 */
  "bear" : "毛茸茸的玩具布偶娃娃,看起来蛮可爱的,可是都不是罗德送的...
",
  "clock" :HIR "一个小闹钟,滴滴答答的走着,它是 jessica
心爱的东西,别乱碰(touch) "NOR,
]));
  set("light_up", 1);

  setup();
}
