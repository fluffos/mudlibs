// Room: /u/r/remark/room/inn/r4
inherit ROOM;
#include <ansi.h>
void create ()
{
  set ("short", "[1;35m露依达酒店仓库[2;37;0m");
  set ("long", @LONG
        
          乱七八糟的储藏室,放着一大堆东西,墙角都是蜘蛛网,
          地上也是厚厚的一层尘土,或许你会想说: jessica 真是个懒女孩,
          仓库内层摆着一个大柜子(cabinet),堆放了从前露衣达收集的宝物。

LONG);

  set("item_desc", ([ /* sizeof() == 1 */
  "cabinet" : "[1;37m里头好像有些东西,找找吧(search)
[2;37;0m",
]));
  set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"r3.lpc",
]));
  set("objects", ([ /* sizeof() == 1 */
  "/u/r/remark/npc/animal/mouse.lpc" : 1,
]));
  set("light_up", 1);

  setup();
}
