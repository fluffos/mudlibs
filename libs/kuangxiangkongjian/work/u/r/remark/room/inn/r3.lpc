// Room: /u/r/remark/room/inn/r3
inherit ROOM;
#include <ansi.h>
void create ()
{
  set ("short", "[1;35m小庭院[2;37;0m");
  set ("long", @LONG
          
          万紫千红的颜色往你眼里送,使你不自觉的揉揉眼睛,
          小小的院子种满了各式各样的花朵,花儿似乎在彼此较劲,
          在这季节中同时开放,吸引了许多人的目光,驻足欣赏
          庭院旁是一个小水池(pool)似乎有什么在里头东来动去的。

LONG);

  set("item_desc", ([ /* sizeof() == 1 */
  "pool" : "  一个小水池,里头有几条鲤鱼悠闲的游来游去 :O~~~
",
]));
  set("objects", ([ /* sizeof() == 1 */
  "/u/r/remark/npc/animal/tortoise" : 1,
]));
  set("light_up", 1);
  set("outdoors", "/u/r/remark/room/inn");
  set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"r1.lpc",
  "east" : __DIR__"r4.lpc",
]));

  setup();
}
