// Room: /u/r/remark/room/street/r2
inherit ROOM;
#include <ansi.h>
void create ()
{
  set ("short", HIM"亚里亚罕城西城门"NOR);
  set ("long", @LONG

       高伟的城门用于防卫魔王军的侵入,显的特别的重要,往西即是野外,
     许多的魔物正虎视眈眈,如果对自己的能力有相当的把握,可以外出闯一闯,
      
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"r1.lpc",
]));

  setup();
}
