
inherit ROOM;

void create ()
{
  set ("short", "河边的小木屋");
  set ("light_up",1);
  set ("long", @LONG
    这是罗格纳的寝宫, 墙上挂了一张枪与玫瑰主唱的海报, 旁边是爱妻的画
    像地上的一套银色音响颇引人侧目, 音响旁百满了摇滚乐吸滴.....
LONG);

  set("no_spells", 1);
  set("exits", ([ /* sizeof() == 6 */
       "leave" : __DIR__"workroom.lpc",
     ]));
  set("no_magic", 1);
  set("valid_startroom", 1);

  setup();
}
