// Room: /u/o/oda/room/workroom.c

inherit ROOM;

void create ()
{
  set ("short", "笨蛋信长的小卧室");
  set ("long", @LONG
你来到一间温暖的小卧室, 窗边的书桌上堆了厚厚的一层灰, 看来很久都没人去动
过了. 房间的另一边有一张小小的床, 棉被乱七八糟的丢在一旁, 这里的主人想必是整
天睡觉才会这么笨吧!
LONG);

  set("no_spells", 1);
  set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"testroom.lpc",
  "west" : __DIR__"transroom.lpc",
]));
  set("no_fight", 1);
  set("no_magic", 1);
  set("valid_startroom", 1);
  set("light_up",1);

  setup();
}
