// room11.c by ryn

inherit ROOM;

void create() {
        set ("short","魔森林");
        set ("long", @LONG

这里是魔森林深邃的内部,来到这里,你突然觉得心里更加发毛,
但好像又有一股不知名的吸引力,使你越想往内一探究竟。

LONG);

        set("light_up",-1);
        set("exits",([ /* sizeof() == 3 */
            "north" : __DIR__"room15",
            "east"  : __DIR__"room18",
            "west" : __DIR__"room11",
      ]));

        set("objects",([ /* sizeof() == 1 */
            __DIR__"mob" : 1,
      ]));

        set("no_transmit",1);
        set("no_auc",1);
        setup();
}
