// room11.c by ryn

inherit ROOM;

void create() {
        set ("short","魔森林");
        set ("long", @LONG

来到这里,你觉得四周的气氛突然凝重起来,似乎魔森林内的妖怪
全聚集在这里,使你不敢再往前面通行,只想赶快离开这妖气深重
的地方。

LONG);

        set("light_up",-1);
        set("exits",([ /* sizeof() == 3 */
            "north" : __DIR__"room18",
            "west"  : __DIR__"room17",
      ]));

        set("objects",([ /* sizeof() == 1 */
            __DIR__"mob" : 1,
      ]));

        set("no_transmit",1);
        set("no_auc",1);
        setup();
}
