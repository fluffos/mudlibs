inherit NPC;

void create()
{
	set_name("书生", ({ "scholar"}) );
	set("race", "人类");
	set("age", 28);
	set("gender", "男性");
	set("long",
"书生装扮的人，手上还拿着一把扇子。 \n"
            );
        set("attitude", "friendly");
	set_temp("apply/attack", 10);
	setup();
        add_money("coin",10);
}

