// Room: /u/n/nako/g_rooms/garden1
inherit ROOM;

void create ()
{
  set ("short", "小鱼池畔");
  set ("long", @LONG
庭园来到这靠近围墙的一角, 居然还挖得有个鱼池, 虽然约莫不过五尺见方大小,
其中所养名贵鱼类却是不少。看那些赏玩用的锦鲤只只长得色彩鲜明, 健壮饱满, 在
池中悠游自得, 想必有专人细心照料吧？池边立着有个招牌 (sign) , 似乎写得有字。
LONG);

  set ("item_desc" , ([ /* sizeof() == 1 */
       "sign" : "请勿喂食专用饲料以外之食物！
",
]));

  set("exits", ([ /* sizeof() == 2 */
  "west" : __DIR__"garden5",
  "north" : __DIR__"garden2",
]));
  set("outdoors", "/u/n");

  setup();
}
