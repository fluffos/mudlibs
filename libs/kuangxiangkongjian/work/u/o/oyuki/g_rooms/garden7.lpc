// Room: /u/n/nako/g_rooms/garden7
inherit ROOM;

void create ()
{
  set ("short", "前院庭园");
  set ("long", @LONG
此处一片绿草如茵, 是个躺下来仰望蓝天的好地方。北方在树枝掩映间依稀可见
到一座竹亭, 朝南方走可回到青石走道上。步行于草上, 软棉棉的触感只让你觉得似
乎漫步于云端...
LONG);

  set("exits", ([ /* sizeof() == 3 */
  "south" : __DIR__"way2.lpc",
  "north" : __DIR__"garden8",
  "east" : __DIR__"garden3.lpc",
]));
  set("outdoors", "/u/n");

  setup();
}
