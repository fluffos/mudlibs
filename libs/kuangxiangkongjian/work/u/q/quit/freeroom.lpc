// Room: /u/q/quit/freeroom
inherit ROOM;

void create ()
{
  set ("short", "  会客室");
  set ("long", @LONG
这是一间摆设华丽又宽敞的房间,里面光线充足而且有淡淡的花香,
这里是风云招待贵宾的地方,前面有张大的圆桌,那是风云跟贵宾讨论事
情的地方,来此的人都会有宾至如归的感觉。
LONG);

  set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"restroom.lpc",
  "west" : __DIR__"road",
]));
  set("valid_startroom", 1);

  setup();
}
