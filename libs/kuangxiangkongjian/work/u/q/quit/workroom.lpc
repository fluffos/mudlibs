// Room: /u/q/quit/workroom.c
#include <room.h>
inherit ROOM;

void create() {
  set("short", "  风云工作室");
  set("long", @LONG
这里是风云的的庭院,这里种了很多的花,前面还有一个鱼池,里面有各式的
鱼游来游去,在鱼池的旁边还有一座假山,这里的设计有如世外桃园,美不胜收。
LONG);

  set("exits", ([ /* sizeof() == 2 */
    "north": __DIR__ "outroom.lpc",
    "enter": __DIR__ "road.lpc",
  ]));
  set("valid_startroom", 1);

  setup();
}
