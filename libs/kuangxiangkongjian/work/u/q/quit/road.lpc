// Room: /u/q/quit/road
#include <room.h>
inherit ROOM;

void create ()
{
  set ("short", "  回廊");
  set ("long", @LONG
一条长长的走道,这里的灯光不是很明亮,两旁挂着艺术品,浏览一下这里的艺术品,
不知不觉的自己也陶醉在艺术的领域中。
LONG);

  set("exits", ([ /* sizeof() == 3 */
  "south" : __DIR__"warehouse.lpc",
  "out" : __DIR__"workroom.lpc",
  "east" : __DIR__"freeroom.lpc",
]));

 create_door("south","木门","north",DOOR_CLOSED);
  setup();
}
