// Room: /u/q/quit/testroom.c
inherit ROOM;

void create() {
  set("short", "  战斗房");
  set("long", @LONG
这是一间风云所属的战斗场,这里摆满了各式的武器,来此的人不是刹人就是被刹,
这里充满了刹气,所以自认为强者欢迎来此挑战。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "north": __DIR__ "warehouse.lpc",
  ]));
  set("light_up", 1);

  setup();
}
