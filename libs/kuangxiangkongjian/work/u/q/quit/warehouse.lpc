// Room: /u/q/quit/warehouse
#include <room.h>
inherit ROOM;

void create ()
{
  set ("short", "  仓库");
  set ("long", @LONG
这里是风云的储存室,摆了许多器具与武器,看起来乱乱的,好像好久没有整理了,
虽然是乱,但好东西确是不少。
LONG);

  set("exits", ([ /* sizeof() == 2 */
  "north" : __DIR__"road.lpc",
  "south" : __DIR__"testroom.lpc",
]));

 set("objects", ([ /* sizeof() == 1 */
     "/u/q/quit/obj/blade.lpc" : 1,
     "/u/q/quit/obj/super.lpc" : 1,
    ]));
 create_door("north","木门","south",DOOR_CLOSED);
  setup();
}
