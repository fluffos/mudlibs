// Room: /u/d/datas/workroom.c
inherit ROOM;

void create() {
  set("short", "骆风的房间");
  set("long", @LONG
这是骆风的房间, 布置地干净整洁, 虽然只不过是几尺见地
但却把颜色配和地恰到好处, 显是对此下了一翻功夫, 地上堆放
了几本语言程式书, 但你一撇间看却看到了一本杂志, 仔细一看
竟是一本花花公子 (playboy), 想来是骆风排遣无聊时间看的。
LONG);

  set("exits", ([ /* sizeof() == 2 */
    "wuroom": "/open/wu/hall/wizroom",
    "hugh": "/u/h/hugh/workroom",
    "carol": "/u/c/carol/workroom",
  ]));
  set("item_desc", ([ /* sizeof() == 1 */
    "playboy": "你一翻开这本书，就看到那些ＸＸ的图片，直瞧的
你脸红心跳、血压上升，全身有如烈火烧身，你不
敢再看，赶忙把书拿开。
",
  ]));
  set("objects",
    ([ "/u/d/datas/npc/mylove.lpc": 1,
      "/u/d/datas/npc/cctest.lpc": 1, ]));
  set("valid_startroom", 1);
  setup();
}
