// Room: /u/d/devils/proom1.c

inherit ROOM;

void create() {
  set("short", "小凯测试屋");
  set("long", @LONG
在你眼前的，是一间布置得十分素雅的房间：一张普普通通的书桌
、一张老旧的椅子，唯一吸引你目光的，是满桌子都是女孩写来的仰慕
信，以墙上挂的那一幅梵谷的″向日葵″画作。
LONG
  );
  set("exits", ([ /* sizeof() == 2 */
    "hole": __DIR__ "workroom",
    "jump": "/u/p/pokai/workroom",
  ]));
  set("no_clean_up", 0);

  setup();
}
