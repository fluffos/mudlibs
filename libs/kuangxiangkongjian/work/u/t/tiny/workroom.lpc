// Room: /u/t/tiny/workroom.c
inherit ROOM;

void create ()
{
  set ("short", "含与不死鸟的家--不死鸟的工作室");
  set ("long", @LONG
这理是不死鸟的个人工作室
清净典雅的房间,就像不死鸟给人的感觉
在这里,你看到不死鸟正为了fs的发展而绞尽脑汁,使你不禁钦佩他的精神
而对他肃然起敬!!
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "west" : __DIR__"living.lpc",
]));
  set("no_fight", 1);
  set("light_up", 1);
  set("no_magic", 1);

  setup();
}
