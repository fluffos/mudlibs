// Room: /u/t/tiny/mid.c
inherit ROOM;

void create() {
  set("short", "含与不死鸟的家--中庭");
  set("long", @LONG
进到中庭,你发现几只仙鹤在水池游玩
环顾四周,简直就是一个世外桃源,难怪不死鸟要在这里隐居
东边是厨房,西边是会客室,往北则是不死鸟的起居室
LONG);

  set("light_up", 1);
  set("no_magic", 1);
  set("outdoors", "/u/t");
  set("exits", ([ /* sizeof() == 4 */
    "north": __DIR__ "living.lpc",
    "west": __DIR__ "meet.lpc",
    "out": __DIR__ "outdoor.lpc",
    "east": __DIR__ "kitchen.lpc",
  ]));
  set("no_fight", 1);

  setup();
}
