// n_area0.c by konn

#include <"/u/k/konn/work/n_area.h">
inherit ROOM;

void create()
{
	set("short", "西大街");
	set("long", 
             n_area(0) 
);

	set("exits", ([
      ]));
	    
	set("objects", ([
      ]));

	setup();
}
