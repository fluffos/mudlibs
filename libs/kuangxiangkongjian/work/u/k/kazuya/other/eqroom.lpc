// Room: /open/gsword/room/wghouse.c
inherit ROOM;

void create ()
{
  set ("short", "仙剑EQ储藏室");
  set ("long", @LONG
这里是仙剑师兄弟们为了造福刚login没EQ的玩家所特别开辟的房间 ,
看着地上堆满的EQ ,你不禁诚心感谢重师兄弟的热心.
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "down" :"/open/gsword/room/g5-1.lpc",
]));

  set("light_up", 1);
  set("no_clean_up",1);
  setup();
}
//因为怕其它门派的人抗议,所以.....嗯...等抗议再说 
void init()
{
  add_action("do_pass","pass");
}

int do_pass()
{
   object me=this_player();
   me->move("/open/ping/room/road3");
   return 1;
} 