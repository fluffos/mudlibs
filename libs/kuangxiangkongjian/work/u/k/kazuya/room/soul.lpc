#include <room.h>
#include <ansi.h>

inherit ROOM;

void create() {
  set("short", "灵界之门");
  set("valid_startroom", 1);
  set("long", @LONG

        
        修改中～～～～～～～～～～～～


LONG);
  set("exits", ([ /* sizeof() == ? */
    "down": "/open/wiz/hall1",
    "guest": "/u/k/kazuya/room/k4",
    "apple": "/u/k/kazuya/workroom",
    "gsword": "/open/gsword/room/g5-1",
    "lum": "/u/l/lum/workroom",
    "night": "/u/n/night/workroom",
    "know": "/u/k/know/workroom",
    "oyuki": "/u/o/oyuki/workroom",
    "girl": "/open/center/room/inn",
  ]));
  set("objects", ([
    "/u/k/kazuya/npc/guard": 2,
  ]));
  set("light_up", 1);
  setup();

}

/* void init()
{
      add_action("do_ponder","ponder");
}

int do_ponder(string str)
{
  object me =this_player();
      if(me->query("id")=="kazuya")
         {
          me->move(__DIR__"room/k7");
          return 1 ;
         }
} */
int valid_leave(object who, string dir) {
  if (dir == "down" && !wizardp(who))
    return notify_fail("对不起,此路只准巫师们走 。\n");
  return ::valid_leave(who, dir);
}
