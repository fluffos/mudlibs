// Room: /u/k/kissme/room/bedroom.c

inherit ROOM;

void create ()
{
  set ("short", "吻我吧的起居室");
  set ("light", 1);
  set ("long", @LONG
    这里是吻我吧的起居室, 这里真的是简单的可以! 一进门来只有看见一张
小小的床,书桌上堆满一些书,还有一张可爱的相片,里面有他的一些猪朋狗友,
有小迈迈,小华华,小钦钦....还有一个帅哥叫虫虫的...有太多的回忆....
LONG);

  set("no_spells", 1);
  set("exits", ([ /* sizeof() == 1 */
       "south" : __DIR__"livingroom.lpc",
       ]));
  set("no_fight", 1);
  set("no_magic", 1);
  set("valid_startroom", 1);

  setup();   
}
