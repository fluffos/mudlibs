
inherit ROOM;
void create ()
{
        set("short", "练武馆");
        set("long", @LONG
在这你可见到一个放满各式兵器的武器架,刀.剑.棍.棒.枪...应有尽有
     旁边有一群标师们正挥汗练着拳,看他们以熟练的拳法套招着
    精湛的拳法令人也跃跃欲试,他们到底多强?


LONG);

  set("exits", ([ /* sizeof() == 1 */
  "west" : "/u/h/healii/p00-1",
]) );

  set("objects", ([ /* sizeof() == 1 */
"/u/h/healii/mercenary.lpc" :3,
]));
  set("light_up", 1);

  setup();
}
