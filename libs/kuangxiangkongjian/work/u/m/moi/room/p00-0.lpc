inherit ROOM;

void create() {
  set("short", "武威镖局分舵");
  set("long", @LONG
枫林港是一个商业发达的腹地,当然有很多商品得从这进进出出
    这里是一个犯罪率极高的地方,要想安全的在此运送货物
    非得有强力的靠山不可
LONG);

  set("objects", ([ /* sizeof() == 1 */
    "/u/h/healii/parrot.lpc": 1,
  ]));
  set("exits", ([ /* sizeof() == 1 */
    "enter": "/u/h/healii/p00-5",
  ]));
  set("no_clean_up", 0);

  set("outdoors", "n");



  setup();
}
