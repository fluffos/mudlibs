#include <room.h>
#include <ansi.h>

 inherit ROOM;

 void create()
{
        set("short", "阿奇的家");
        set("long", @LONG

	奇    魅    工    作    室

   出口:
	East : 巫师集会所大厅
        West : 菠萝面包工作室
        North: 进入模型屋
        South: 萱萱的家
	Up: 兀儿德女王的城堡
	Down : 进入世界

LONG
        );
        set("valid_startroom", 1);
        set("light_up",1);

        set("exits",([ /* sizeof() == 4 */
                "down" : "/open/snow/room/black4.lpc",
                "north" : "/u/m/mtl/room/senwuman.lpc",
                "south" : "/u/c/carol/workroom.lpc",
                "west"  : "/u/w/wade/workroom.lpc",
                "east" : "/open/wiz/hall1.lpc",
	"up" : "/u/u/urd/workroom.lpc",
        ]) );
        set("item_desc", ([ /* sizeof() == 1 */
                      ]) );
        set("resource", ([ /* sizeof() == 1 */
  "water" : "1",
]));

        setup();
        call_other("/u/m/mtl/mtl_b.lpc","????");
}

