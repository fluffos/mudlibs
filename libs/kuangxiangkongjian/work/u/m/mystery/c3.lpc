#include <room.h>
#include <ansi.h>
inherit ROOM;

void create ()
{
  set ("short", "卧房");
  set ("long", @LONG

  这儿是不对外开放的，若有误闯此地的，请尽快离开吧！以免主人不高
  兴之下，把你禁锢起来。

LONG);

  set("exits", ([ /* sizeof() == 3 */
  "west" : __DIR__"c1",
]));
  set("no_fight", 1);
  set("no_magic", 1);
  setup();
}

