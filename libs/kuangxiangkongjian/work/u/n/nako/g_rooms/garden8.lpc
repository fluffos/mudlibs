// Room: /u/n/nako/g_rooms/garden8
inherit ROOM;

void create ()
{
  set ("short", "小凉亭");
  set ("long", @LONG
这是一间什么都没有的房间。
LONG);

  set("exits", ([ /* sizeof() == 2 */
  "south" : __DIR__"garden7",
  "east" : __DIR__"garden4",
]));
  set("outdoors", "/u/n");

  setup();
}
void init ()
{
  add_action ("do_search" , "search");
}

int do_search(string arg)
{
  object me = this_player();
  message_vision("你发现西边的柳树林中似乎有条小路, 便走了过去。\n" , me);
  me -> move(__DIR__"way4");
  return 1;
}
