// Room: /u/n/nako/g_rooms/lake_ting
inherit ROOM;

void create() {
  set("short", "翠水亭");
  set("long", @LONG
这是一间什么都没有的房间。
LONG);

  set("exits", ([ /* sizeof() == 1 */
    "southeast": __DIR__ "bridge",
  ]));
  set("outdoors", "/open/capital");

  setup();
}
