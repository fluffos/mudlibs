// Room: /u/n/nako/g_rooms/garden3
inherit ROOM;

void create ()
{
  set ("short", "前院庭园");
  set ("long", @LONG
行至此处, 你只觉得天色忽地暗了下来, 抬头仰望, 才发现大树枝繁叶茂, 竟是
将一片蓝天遮去了十之八九。原来怡红院将庭园北向这一面植树成林, 以添园景之美,
想这怡红院建成也不过是这两年的事, 能有如此大树, 自是从他处移来栽植的。不过
林中树木不下百棵, 说全是移来的似乎又不尽然。
LONG);

  set("exits", ([ /* sizeof() == 3 */
  "south" : __DIR__"way1",
  "north" : __DIR__"garden4",
  "west" : __DIR__"garden7.lpc",
]));
  set("outdoors", "/u/n");

  setup();
}
