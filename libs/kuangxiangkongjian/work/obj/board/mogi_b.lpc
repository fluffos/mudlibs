// board.c by konn
inherit BULLETIN_BOARD;

void create()
{
        set_name("魔界讨论区留言版", ({ "board" }) );
        set("location", "/u/k/konn/workroom");
        set("board_id", "mogi_b");
        set("long", @LONG
这个板是让写魔界的巫师们用来讨论心得与问题的地方, 若没事的话
请不要在上面画●●ＸＸㄛ... 有事找konn的话可以用mail的, konn
会跟您联络。

LONG);
        setup();
        set("capacity", 100);
}
