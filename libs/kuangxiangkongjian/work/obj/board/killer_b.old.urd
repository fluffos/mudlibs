// chen_b.c

inherit BULLETIN_BOARD;

void create()
{
	set_name("黑名单",({"black note","note"}));
	set("location","/open/snow/room/black4");
	set("board_id","killer_b");
	set("long","这是一分杀手用来记事的黑名单。\n");
	setup();
	set("capacity", 30);
	// replace_program(BULLETIN_BOARD);
}
