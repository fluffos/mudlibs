// made by funkcat, copy and re-edit by ccat
inherit BANK;
void create() {
  set("short", "史家钱庄");
  set("long", @LONG
这里是本地最大的钱庄 , 这边的存款额在全国是数一数二的
 , 你可以安心的将前存在这里 . 这家钱庄的老板是大财主史怀
恩 , 你可以看看这儿的公告(board),看看有甚么服务.

LONG);

  set("exits", ([ /* sizeof() == 1 */
    "east": "/open/port/room/r1-2",
  ]));
  set("item_desc", ([ /* sizeof() == 1 */
    "board": "
    balance: 查看您现有的存款    convert: 换钱币
    withdraw: 提款               deposit: 存款    
 ",
  ]));
  set("light_up", 1);
  set("objects", ([ /* sizeof() == 1 */
  ]));

  setup();
}
