// Room: /open/gblade/port/room/p1-1.c
inherit ROOM;

void create ()
{
	set("short","东大街");
  set ("long", @LONG
你正走在东大街上 . 这里已经远离闹市中心了 , 路上的行
人大多是要前往芳林园的 . 南边是阜财巷 , 住的都是有钱人 ,
 . 北边即是芳林园 .

LONG);

  set("light_up", 1);
	set("outdoors", "/open/port");
  set("exits", ([ /* sizeof() == 2 */
	"west" : "/open/port/room/r3-3",
	"south" : "/open/port/room/r3-6",
]));
	set("objects",([
	]));

  setup();
}
