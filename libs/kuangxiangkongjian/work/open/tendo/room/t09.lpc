// Room: /open/tendo/room/t09.c

inherit ROOM;

void create()
{
	set("short", "炼丹房");
	set("long", @LONG
终年热呼呼的炼丹房，这里可以让你炼制保命的丹药但你必须先
学会炼丹术及备有适合的材料。

LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"t10",
]));
        set("objects",([
        "/open/tendo/npc/taoist2" : 1,
        ]));

	setup();
}
