// n_area12.c by konn
inherit ROOM;
#include "/open/gblade/room/n_area.h"

void create()
{
        set("short", "树林");
        set("long",
             n_area(12)
);

        set("exits", ([
            "east"  : __DIR__"n_area11",
            "south" : __DIR__"n_area13",
      ]));

        set("objects", ([
      ]));

        setup();
}
