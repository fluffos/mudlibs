// Room: /open/common/room/inn.c

inherit ROOM;

void create() {
  set("short", "世界的入口");
  set("long", @LONG

□ 更改命令的方式, 对于 localcmds 已经可以用 !, !! 取代.
□ 增加忙碌方式, 通常是跟 get, drop, remove, wear 等等有关, 请自行揣摩.
□ okip 采强迫的方式, 对于没有设定的, 一律以现在连线 IP 设为 okip.

□ 这儿是世界的入口, 如果你对狂想空间的帮派有兴趣, 不妨 help 帮派简介
□ 如果想知道有哪些命令可以用, 可以 help cmds,
□ 如果想知道有哪些辅助讯息可以获得, 可以 help helps
□ 如果你想抓这边的程式, 可以 ftp 140.122.77.126 4321, login anonymous
□ 对于新手, 请 help newbie, 想知道地图, 可以 help main_map
□ 欢迎在这儿破斯特有关 tintin 的东西, 或 help tintin 取得说明.
□ 请随时用 help 取得 help 资讯

往下走你将会正式进入狂想空间的世界。

LONG);

  set("exits", ([ /* sizeof() == 1 */
    "down": "/open/center/room/inn",
  ]));
  set("light_up", 1);
  set("valid_startroom", 1);
  set("no_fight", 1);

  setup();
  call_other("/obj/board/fs_b.lpc", "????????????????????????");
}
