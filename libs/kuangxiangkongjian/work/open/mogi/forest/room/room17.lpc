// room17.c by konn

inherit ROOM;

void create() {
  set("short", "魔森林");
  set("long", @LONG
你一走过来，发觉这里似乎异常的安静。低下头一看，发现地上有一个闪
闪发亮的东西，它的光芒使得你无法睁开双眼来正视它，这或许会是一个
对你行走魔界非常有帮助的东西。


LONG);

  set("evil", 1);
  set("no_auc", 1);
  set("no_transmit", 1);
  set("light_up", -1);
  set("exits", ([ /* sizeof() == 2 */
    "east": __DIR__ "room19",
    "west": __DIR__ "room12",
  ]));

  set("objects", ([ /* sizeof() == 1 */
    "/open/mogi/forest/obj/obj_1": 1,
  ]));

  setup();
}
