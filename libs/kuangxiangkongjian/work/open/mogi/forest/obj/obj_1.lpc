// 玉如意 (wishful jade) <<made by ryn>>

inherit ITEM;

void create() {
  set_name("玉如意", ({ "wishful jade", "wishful", "jade" }));
  set_weight(40);
  set("long", "这是一块玉如意,也可称做如意玉,这是用来通过魔森林的媒介。\n");
  set("unit", "块");
  set("no_sell", 1);
  set("no_give", 1);

  setup();
}
