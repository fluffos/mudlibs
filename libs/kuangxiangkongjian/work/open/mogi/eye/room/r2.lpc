inherit ROOM;

void create ()
{
   set  ("short", "魔眼洞");
  set ("long", @LONG
 
    石洞中不时传来阴深恐怖的声音,让人不觉的毛骨悚然
   
    北方的洞穴似乎和前面的洞穴不同....

LONG);

  set("exits", ([ /* sizeof() == 1 */
    "west"  : __DIR__"r1",
   "north"  : __DIR__"r5",
]));
   set("objects",([ /* sizeof() == 1 */
       "/open/mogi/eye/npc/beast.lpc" : 2,
      ]));
  set("light_up", 1);

  set("evil",1);
  set("no_auc",1);
  set("no_transmit",1);
  setup();
}
