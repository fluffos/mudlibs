inherit ROOM;

void create ()
{
   set  ("short", "魔眼洞");
  set ("long", @LONG
 
    石洞中不时传来阴深恐怖的声音,让人不觉的毛骨悚然
    北方的洞穴内传来不知名的哀嚎声~~~~~
   

LONG);

  set("exits", ([ /* sizeof() == 1 */
    "east"  : __DIR__"r1",
    "west"  : __DIR__"r4",
    "north" : __DIR__"r7",
]));
   set("objects",([ /* sizeof() == 1 */
       "/open/mogi/eye/npc/beast.lpc" : 2,
      ]));
  set("light_up", 1);

  set("evil",1);
  set("no_auc",1);
  set("no_transmit",1);
  setup();
}
