// Room: /open/capital/room/r08.c

inherit ROOM;

void create ()
{
  set ("short", "环宫大道");
  set ("long", @LONG
往北是进出皇宫的唯一通道, 但若不是什么皇亲国戚或是文武百官
等大人物, 是不准平常老百姓进出的。南方有道楼梯, 直通一座高台,
是皇帝祭祖、昭告的地方, 你想上去瞧瞧吗?
    道路旁有块告示版(board).

LONG);

  set("item_desc", ([ /* sizeof() == 1 */
  "board" : "
            「皇城重地，闲杂人等请勿在此逗留。”
	    「现在限制玩家进入，故通往皇宫的道路封避。”
                三品御前带刀侍卫队长：王彪
",
]));
  set("objects", ([ /* sizeof() == 1 */
  "/open/capital/npc/cleanman" : 1,
]));
  set("outdoors", "/open/capital");
  set("exits", ([ /* sizeof() == 5 */
  "north" : __DIR__"D04.lpc",
  "west" : __DIR__"r07",
  "south" : __DIR__"r74",
  "east" : __DIR__"r09",
  "southup" : __DIR__"tower",
]));
  set("gopath", "east");

  setup();
}
int valid_leave(object who,string dir){
  if(dir=="north" && !wizardp(who))
  return notify_fail("皇宫内传来一阵呐喊声,使你经不住缩回了脚步。\n");
return :: valid_leave(who,dir);
}
