---
import IndexPage from '../../components/IndexPage.astro';
import { loadMuseum } from '../../lib/museum';
const data = loadMuseum();
const base = data.indexes.zh;
if (!base) throw new Error('missing indexes.zh');
const index = { ...base, canonical_url: `${data.site_url}/zh/` };
---
<IndexPage index={index} />
